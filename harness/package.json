{
  "name": "cadre-agent-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-agent, human-governed software delivery harness",
  "type": "module",
  "scripts": {
    "install:project": "node scripts/install.mjs --agent all --scope project",
    "test": "node --test",
    "validate": "node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
