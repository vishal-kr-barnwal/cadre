{
  "version": 1,
  "schema": "cadre.styleguide.v1",
  "kind": "styleguide",
  "id": "swift",
  "title": "Swift API Design Guidelines Style Guide Summary",
  "summary": "This document summarizes key rules and best practices from the Swift API Design Guidelines and Swift.org style conventions for writing idiomatic, safe, and expressive Swift code.",
  "sections": [
    {
      "heading": "1. Formatting",
      "body": "- **SwiftFormat / SwiftLint:** Format with `swiftformat` and lint with `swiftlint`. Enforce both in CI. Include `.swiftlint.yml` in the repository root.\n- **Indentation:** 4 spaces. No tabs.\n- **Line Length:** 120 characters maximum (SwiftLint default; adjust to team preference).\n- **Blank lines:** One blank line between declarations. Two blank lines between major sections (e.g., before `// MARK:` sections)."
    },
    {
      "heading": "2. Naming",
      "body": "- **`UpperCamelCase`:** For types (class, struct, enum, protocol, typealias) and enum cases.\n- **`lowerCamelCase`:** For functions, methods, properties, parameters, and local variables.\n- **Clarity at the call site:** Name things so usage reads like prose: `list.insert(element, at: index)`, not `list.insert(element, position: index)`.\n- **Omit needless words:** Do not repeat the type in the name. `backgroundColor`, not `backgroundColorOfView`. `users.remove(at: index)`, not `users.removeElement(at: index)`.\n- **Boolean properties:** Start with `is`, `has`, `can`, or `should`: `isEnabled`, `hasChildren`, `canClose`.\n- **Protocol naming:**\n  - Protocols describing what something *is*: noun (`Collection`, `Sequence`).\n  - Protocols describing a *capability*: `-able`, `-ible`, `-ing` suffix (`Hashable`, `Codable`, `Equatable`).\n- **Avoid abbreviations:** Use full words. `destinationURL` not `destURL`, `error` not `err`, `index` not `idx`. Well-known abbreviations (`URL`, `ID`, `JSON`) are fine.\n- **First argument label:** Omit when it is obviously part of the function name: `x.addSubview(y)` not `x.addSubview(view: y)`. Include when it adds clarity: `a.move(from: b, to: c)`."
    },
    {
      "heading": "3. Types",
      "body": "- **`struct` by default:** Prefer value types (`struct`, `enum`) over reference types (`class`). Use `class` only when identity semantics, inheritance, or reference sharing is required.\n- **`final` by default:** Mark classes `final` unless designed for subclassing. Subclassable classes are a design decision, not a default.\n- **`enum` for closed sets:** Use `enum` for a fixed set of related values. Use `CaseIterable` when you need to enumerate cases.\n- **Associated values:** Use `enum` with associated values as discriminated unions: `enum Result<T> { case success(T); case failure(Error) }`.\n- **`typealias`:** Use to clarify the role of complex types: `typealias Completion = (Result<User, Error>) -> Void`.\n- **`extension` over subclass:** Prefer extending a type to add behavior over subclassing. Extensions cannot store state — use composition for that."
    },
    {
      "heading": "4. Optionals",
      "body": "- **Avoid `!` force-unwrap:** Force-unwrap is a crash waiting to happen. Use `guard let`, `if let`, or `??` instead.\n- **`guard let` for early exit:** Use `guard let` to unwrap optionals at the top of a function when `nil` means \"cannot proceed\". This reduces nesting.\n- **`if let` for optional use:** Use `if let` when the `nil` case is a legitimate alternate path.\n- **Optional chaining (`?.`):** Use for accessing properties/methods on optionals where `nil` propagation is correct.\n- **`??` for defaults:** Use the nil-coalescing operator for fallback values: `let name = user?.name ?? \"Unknown\"`.\n- **Implicit optionals (`!` type):** Use `@IBOutlet weak var button: UIButton!` and similarly initialized-before-use patterns — keep to `@IBOutlet` and injection patterns only. Never use `!` types for general logic."
    },
    {
      "heading": "5. Functions and Closures",
      "body": "- **Single responsibility:** Each function does one thing. Break down large functions.\n- **`@discardableResult`:** Add only when ignoring the result is a common and correct pattern. Do not add it to silence warnings mechanically.\n- **Trailing closure syntax:** Use trailing closure syntax when the closure is the last parameter and its role is clear: `UIView.animate(withDuration: 0.3) { ... }`.\n- **`@escaping`:** Mark closures `@escaping` when they outlive the function call. Document the lifecycle implications.\n- **`inout`:** Use `inout` sparingly. Prefer returning a new value over mutating in place unless performance is critical.\n- **`throws` / `async throws`:** Use `throws` for functions that can fail with typed errors. Use `async` for asynchronous work. Combine as `async throws` for async-fallible functions."
    },
    {
      "heading": "6. Error Handling",
      "body": "- **`throws` + `try`:** Use Swift's typed error handling for recoverable errors. Functions that can fail return via `throw`, not via optional or sentinel values.\n- **`do / catch`:** Handle errors at the appropriate layer. Do not `try!` unless a failure is genuinely impossible and you can prove it.\n- **`Result<T, Error>`:** Use `Result` for asynchronous completions that predate `async/await`, or when you need to store a result for later.\n- **Custom error types:** Define `enum MyError: Error, LocalizedError` with associated values for structured error information. Implement `errorDescription` for user-facing messages.\n- **`try?`:** Use when `nil` on failure is acceptable and the error is irrelevant to the caller."
    },
    {
      "heading": "7. Concurrency (Swift Concurrency)",
      "body": "- **`async`/`await`:** Prefer Swift Concurrency over completion handlers, Combine, or DispatchQueue-based async code for new code.\n- **`Task { }`:** Use to bridge from synchronous to async context. Use `Task.detached` only when the task must not inherit actor context.\n- **`@MainActor`:** Annotate types or functions that must run on the main thread. Use `@MainActor` on ViewModels/Controllers rather than `DispatchQueue.main.async`.\n- **`actor`:** Use `actor` types for shared mutable state. Actors serialize access and eliminate data races.\n- **`Sendable`:** Mark types crossing actor boundaries as `Sendable`. Do not suppress `Sendable` warnings with `@unchecked Sendable` without justification.\n- **`async let`:** Use `async let` for independent concurrent work within a function: `async let user = fetchUser(); async let feed = fetchFeed(); let (u, f) = try await (user, feed)`."
    },
    {
      "heading": "8. Memory Management",
      "body": "- **ARC is automatic:** Do not call `retain`/`release`. ARC handles memory.\n- **`weak` for delegates and callbacks:** Mark delegate properties `weak var` to avoid retain cycles. Capture `[weak self]` in closures that outlive the object.\n- **`unowned` only when guaranteed:** Use `unowned` instead of `weak` only when the referenced object is guaranteed to outlive the closure/property. Incorrect `unowned` crashes; incorrect `weak` just produces `nil`.\n- **`[weak self]` + guard:** Pattern: `{ [weak self] in guard let self = self else { return } ... }` (or `[weak self] in self?.doWork()`)."
    },
    {
      "heading": "9. Collections and Functional Patterns",
      "body": "- **`map`, `filter`, `compactMap`, `reduce`:** Use functional transforms over `for` loops for collection transformations.\n- **`compactMap`:** Use to filter `nil` from a sequence of optionals: `names.compactMap { user(id: $0)?.name }`.\n- **`flatMap` on sequences:** Use to flatten sequences: `[[1,2],[3,4]].flatMap { $0 }` → `[1,2,3,4]`.\n- **`lazy`:** Use `collection.lazy.filter { }.map { }` to avoid allocating intermediate arrays for long pipelines."
    },
    {
      "heading": "10. Documentation",
      "body": "- **`///` doc comments:** Use triple-slash comments for all public API. Xcode renders these in Quick Help.\n- **Summary + discussion:** First line is the summary. Add a blank line then the discussion for longer explanations.\n- **`- Parameter`, `- Returns`, `- Throws`:** Document parameters, return values, and thrown errors using these list items.\n- **`- Note:`, `- Important:`, `- Warning:`:** Use callout keywords for additional reader guidance.\n- **Code examples:** Include brief usage examples in doc comments for complex APIs using fenced code blocks.\n\n*Sources: [Swift API Design Guidelines](https://swift.org/documentation/api-design-guidelines/) · [Swift.org Style Guide](https://google.github.io/swift/) · [Swift Concurrency](https://docs.swift.org/swift-book/documentation/the-swift-programming-language/concurrency/)*"
    }
  ],
  "languages": [
    "swift"
  ],
  "frameworks": [],
  "file_patterns": [],
  "applies_to": [
    "planning",
    "implementation",
    "review",
    "tests"
  ],
  "rules": [
    {
      "id": "swift-rule-1",
      "severity": "guidance",
      "summary": "**SwiftFormat / SwiftLint:** Format with `swiftformat` and lint with `swiftlint`. Enforce both in CI. Include `.swiftlint.yml` in the repository root."
    },
    {
      "id": "swift-rule-2",
      "severity": "guidance",
      "summary": "**Indentation:** 4 spaces. No tabs."
    },
    {
      "id": "swift-rule-3",
      "severity": "guidance",
      "summary": "**Line Length:** 120 characters maximum (SwiftLint default; adjust to team preference)."
    },
    {
      "id": "swift-rule-4",
      "severity": "guidance",
      "summary": "**Blank lines:** One blank line between declarations. Two blank lines between major sections (e.g., before `// MARK:` sections)."
    },
    {
      "id": "swift-rule-5",
      "severity": "guidance",
      "summary": "**`UpperCamelCase`:** For types (class, struct, enum, protocol, typealias) and enum cases."
    },
    {
      "id": "swift-rule-6",
      "severity": "guidance",
      "summary": "**`lowerCamelCase`:** For functions, methods, properties, parameters, and local variables."
    },
    {
      "id": "swift-rule-7",
      "severity": "guidance",
      "summary": "**Clarity at the call site:** Name things so usage reads like prose: `list.insert(element, at: index)`, not `list.insert(element, position: index)`."
    },
    {
      "id": "swift-rule-8",
      "severity": "guidance",
      "summary": "**Omit needless words:** Do not repeat the type in the name. `backgroundColor`, not `backgroundColorOfView`. `users.remove(at: index)`, not `users.removeElement(at: index)`."
    },
    {
      "id": "swift-rule-9",
      "severity": "guidance",
      "summary": "**Boolean properties:** Start with `is`, `has`, `can`, or `should`: `isEnabled`, `hasChildren`, `canClose`."
    },
    {
      "id": "swift-rule-10",
      "severity": "guidance",
      "summary": "**Protocol naming:**"
    },
    {
      "id": "swift-rule-11",
      "severity": "guidance",
      "summary": "Protocols describing what something *is*: noun (`Collection`, `Sequence`)."
    },
    {
      "id": "swift-rule-12",
      "severity": "guidance",
      "summary": "Protocols describing a *capability*: `-able`, `-ible`, `-ing` suffix (`Hashable`, `Codable`, `Equatable`)."
    },
    {
      "id": "swift-rule-13",
      "severity": "guidance",
      "summary": "**Avoid abbreviations:** Use full words. `destinationURL` not `destURL`, `error` not `err`, `index` not `idx`. Well-known abbreviations (`URL`, `ID`, `JSON`) are fine."
    },
    {
      "id": "swift-rule-14",
      "severity": "guidance",
      "summary": "**First argument label:** Omit when it is obviously part of the function name: `x.addSubview(y)` not `x.addSubview(view: y)`. Include when it adds clarity: `a.move(from: b, to: c)`."
    },
    {
      "id": "swift-rule-15",
      "severity": "guidance",
      "summary": "**`struct` by default:** Prefer value types (`struct`, `enum`) over reference types (`class`). Use `class` only when identity semantics, inheritance, or reference sharing is required."
    },
    {
      "id": "swift-rule-16",
      "severity": "guidance",
      "summary": "**`final` by default:** Mark classes `final` unless designed for subclassing. Subclassable classes are a design decision, not a default."
    },
    {
      "id": "swift-rule-17",
      "severity": "guidance",
      "summary": "**`enum` for closed sets:** Use `enum` for a fixed set of related values. Use `CaseIterable` when you need to enumerate cases."
    },
    {
      "id": "swift-rule-18",
      "severity": "guidance",
      "summary": "**Associated values:** Use `enum` with associated values as discriminated unions: `enum Result<T> { case success(T); case failure(Error) }`."
    },
    {
      "id": "swift-rule-19",
      "severity": "guidance",
      "summary": "**`typealias`:** Use to clarify the role of complex types: `typealias Completion = (Result<User, Error>) -> Void`."
    },
    {
      "id": "swift-rule-20",
      "severity": "guidance",
      "summary": "**`extension` over subclass:** Prefer extending a type to add behavior over subclassing. Extensions cannot store state — use composition for that."
    },
    {
      "id": "swift-rule-21",
      "severity": "guidance",
      "summary": "**Avoid `!` force-unwrap:** Force-unwrap is a crash waiting to happen. Use `guard let`, `if let`, or `??` instead."
    },
    {
      "id": "swift-rule-22",
      "severity": "guidance",
      "summary": "**`guard let` for early exit:** Use `guard let` to unwrap optionals at the top of a function when `nil` means \"cannot proceed\". This reduces nesting."
    },
    {
      "id": "swift-rule-23",
      "severity": "guidance",
      "summary": "**`if let` for optional use:** Use `if let` when the `nil` case is a legitimate alternate path."
    },
    {
      "id": "swift-rule-24",
      "severity": "guidance",
      "summary": "**Optional chaining (`?.`):** Use for accessing properties/methods on optionals where `nil` propagation is correct."
    },
    {
      "id": "swift-rule-25",
      "severity": "guidance",
      "summary": "**`??` for defaults:** Use the nil-coalescing operator for fallback values: `let name = user?.name ?? \"Unknown\"`."
    },
    {
      "id": "swift-rule-26",
      "severity": "guidance",
      "summary": "**Implicit optionals (`!` type):** Use `@IBOutlet weak var button: UIButton!` and similarly initialized-before-use patterns — keep to `@IBOutlet` and injection patterns only. Never use `!` types for general logic."
    },
    {
      "id": "swift-rule-27",
      "severity": "guidance",
      "summary": "**Single responsibility:** Each function does one thing. Break down large functions."
    },
    {
      "id": "swift-rule-28",
      "severity": "guidance",
      "summary": "**`@discardableResult`:** Add only when ignoring the result is a common and correct pattern. Do not add it to silence warnings mechanically."
    },
    {
      "id": "swift-rule-29",
      "severity": "guidance",
      "summary": "**Trailing closure syntax:** Use trailing closure syntax when the closure is the last parameter and its role is clear: `UIView.animate(withDuration: 0.3) { ... }`."
    },
    {
      "id": "swift-rule-30",
      "severity": "guidance",
      "summary": "**`@escaping`:** Mark closures `@escaping` when they outlive the function call. Document the lifecycle implications."
    },
    {
      "id": "swift-rule-31",
      "severity": "guidance",
      "summary": "**`inout`:** Use `inout` sparingly. Prefer returning a new value over mutating in place unless performance is critical."
    },
    {
      "id": "swift-rule-32",
      "severity": "guidance",
      "summary": "**`throws` / `async throws`:** Use `throws` for functions that can fail with typed errors. Use `async` for asynchronous work. Combine as `async throws` for async-fallible functions."
    },
    {
      "id": "swift-rule-33",
      "severity": "guidance",
      "summary": "**`throws` + `try`:** Use Swift's typed error handling for recoverable errors. Functions that can fail return via `throw`, not via optional or sentinel values."
    },
    {
      "id": "swift-rule-34",
      "severity": "guidance",
      "summary": "**`do / catch`:** Handle errors at the appropriate layer. Do not `try!` unless a failure is genuinely impossible and you can prove it."
    },
    {
      "id": "swift-rule-35",
      "severity": "guidance",
      "summary": "**`Result<T, Error>`:** Use `Result` for asynchronous completions that predate `async/await`, or when you need to store a result for later."
    },
    {
      "id": "swift-rule-36",
      "severity": "guidance",
      "summary": "**Custom error types:** Define `enum MyError: Error, LocalizedError` with associated values for structured error information. Implement `errorDescription` for user-facing messages."
    },
    {
      "id": "swift-rule-37",
      "severity": "guidance",
      "summary": "**`try?`:** Use when `nil` on failure is acceptable and the error is irrelevant to the caller."
    },
    {
      "id": "swift-rule-38",
      "severity": "guidance",
      "summary": "**`async`/`await`:** Prefer Swift Concurrency over completion handlers, Combine, or DispatchQueue-based async code for new code."
    },
    {
      "id": "swift-rule-39",
      "severity": "guidance",
      "summary": "**`Task { }`:** Use to bridge from synchronous to async context. Use `Task.detached` only when the task must not inherit actor context."
    },
    {
      "id": "swift-rule-40",
      "severity": "guidance",
      "summary": "**`@MainActor`:** Annotate types or functions that must run on the main thread. Use `@MainActor` on ViewModels/Controllers rather than `DispatchQueue.main.async`."
    },
    {
      "id": "swift-rule-41",
      "severity": "guidance",
      "summary": "**`actor`:** Use `actor` types for shared mutable state. Actors serialize access and eliminate data races."
    },
    {
      "id": "swift-rule-42",
      "severity": "guidance",
      "summary": "**`Sendable`:** Mark types crossing actor boundaries as `Sendable`. Do not suppress `Sendable` warnings with `@unchecked Sendable` without justification."
    },
    {
      "id": "swift-rule-43",
      "severity": "guidance",
      "summary": "**`async let`:** Use `async let` for independent concurrent work within a function: `async let user = fetchUser(); async let feed = fetchFeed(); let (u, f) = try await (user, feed)`."
    },
    {
      "id": "swift-rule-44",
      "severity": "guidance",
      "summary": "**ARC is automatic:** Do not call `retain`/`release`. ARC handles memory."
    },
    {
      "id": "swift-rule-45",
      "severity": "guidance",
      "summary": "**`weak` for delegates and callbacks:** Mark delegate properties `weak var` to avoid retain cycles. Capture `[weak self]` in closures that outlive the object."
    },
    {
      "id": "swift-rule-46",
      "severity": "guidance",
      "summary": "**`unowned` only when guaranteed:** Use `unowned` instead of `weak` only when the referenced object is guaranteed to outlive the closure/property. Incorrect `unowned` crashes; incorrect `weak` just produces `nil`."
    },
    {
      "id": "swift-rule-47",
      "severity": "guidance",
      "summary": "**`[weak self]` + guard:** Pattern: `{ [weak self] in guard let self = self else { return } ... }` (or `[weak self] in self?.doWork()`)."
    },
    {
      "id": "swift-rule-48",
      "severity": "guidance",
      "summary": "**`map`, `filter`, `compactMap`, `reduce`:** Use functional transforms over `for` loops for collection transformations."
    },
    {
      "id": "swift-rule-49",
      "severity": "guidance",
      "summary": "**`compactMap`:** Use to filter `nil` from a sequence of optionals: `names.compactMap { user(id: $0)?.name }`."
    },
    {
      "id": "swift-rule-50",
      "severity": "guidance",
      "summary": "**`flatMap` on sequences:** Use to flatten sequences: `[[1,2],[3,4]].flatMap { $0 }` → `[1,2,3,4]`."
    },
    {
      "id": "swift-rule-51",
      "severity": "guidance",
      "summary": "**`lazy`:** Use `collection.lazy.filter { }.map { }` to avoid allocating intermediate arrays for long pipelines."
    },
    {
      "id": "swift-rule-52",
      "severity": "guidance",
      "summary": "**`///` doc comments:** Use triple-slash comments for all public API. Xcode renders these in Quick Help."
    },
    {
      "id": "swift-rule-53",
      "severity": "guidance",
      "summary": "**Summary + discussion:** First line is the summary. Add a blank line then the discussion for longer explanations."
    },
    {
      "id": "swift-rule-54",
      "severity": "guidance",
      "summary": "**`- Parameter`, `- Returns`, `- Throws`:** Document parameters, return values, and thrown errors using these list items."
    },
    {
      "id": "swift-rule-55",
      "severity": "guidance",
      "summary": "**`- Note:`, `- Important:`, `- Warning:`:** Use callout keywords for additional reader guidance."
    },
    {
      "id": "swift-rule-56",
      "severity": "guidance",
      "summary": "**Code examples:** Include brief usage examples in doc comments for complex APIs using fenced code blocks."
    }
  ],
  "source": "bundled_template"
}
