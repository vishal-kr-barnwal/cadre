{
  "version": 1,
  "schema": "cadre.protocol.v1",
  "id": "cadre-release",
  "description": "Cut a local release - changelog plus version evidence across completed tracks",
  "workflow": "release",
  "markdownUse": "human_projection_only",
  "preconditions": [
    {
      "tool": "cadre_project",
      "arguments": {
        "action": "ping"
      },
      "requirement": "Cadre MCP must be available"
    },
    {
      "requirement": "Every project-scoped packet call includes root"
    },
    {
      "requirement": "If a required packet returns ok:false, halt and report the packet error"
    }
  ],
  "packetFlow": [
    {
      "step": 1,
      "instruction": "Resolve the project root with `cadre_project` using `action: \"root\"`."
    },
    {
      "step": 2,
      "instruction": "Call `cadre_workflow` with `workflow: \"release\"` and any requested version, date, or scope arguments as structured JSON fields."
    },
    {
      "step": 3,
      "instruction": "Review returned completed tracks, release evidence, warnings, proposed artifacts, and current `approval.current_stage` review_bundle manifest/path list without inlining large artifacts into model context."
    },
    {
      "step": 4,
      "instruction": "To write release artifacts, call `cadre_workflow` again with `workflow: \"release\"`, `execute: true`, and `approvalComplete: true`."
    },
    {
      "step": 5,
      "instruction": "Summarize release scope, version intent, generated artifacts, changed canonical JSON/JSONL state, generated human projections, and remaining blockers."
    }
  ],
  "confirmation": {
    "requiredForMutation": true,
    "dryRunFirst": true,
    "executeArgument": "execute",
    "approvalArgument": "approvalComplete",
    "stagedApproval": true
  },
  "forbiddenActions": [
    "Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands",
    "Do not read or write Markdown projections as canonical state",
    "Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"
  ],
  "responseSummary": [
    "packet ok/error state",
    "selected track or scope",
    "warnings and blockers",
    "changed canonical JSON/JSONL artifacts",
    "generated human projections when relevant",
    "next recommended packet"
  ],
  "references": [
    "mcp-contract"
  ]
}
