{
  "version": 1,
  "schema": "cadre.styleguide.v1",
  "kind": "styleguide",
  "id": "rust",
  "title": "Effective Rust Style Guide Summary",
  "summary": "This document summarizes key rules and best practices from the official Rust API Guidelines, Rust Style Guide, and idiomatic Rust conventions for writing safe, performant, and maintainable Rust code.",
  "sections": [
    {
      "heading": "1. Formatting",
      "body": "- **`rustfmt`:** All Rust code **must** be formatted with `rustfmt`. This is non-negotiable. Run via `cargo fmt`.\n- **`clippy`:** Run `cargo clippy` and fix all warnings before review. Treat lints as errors in CI: `RUSTFLAGS=\"-D warnings\"`.\n- **Indentation:** 4 spaces. No tabs.\n- **Line Length:** 100 characters maximum (rustfmt default)."
    },
    {
      "heading": "2. Naming",
      "body": "- **`snake_case`:** For functions, methods, variables, modules, and crates.\n- **`UpperCamelCase`:** For types, traits, enums, and struct names.\n- **`SCREAMING_SNAKE_CASE`:** For constants and statics.\n- **`snake_case`:** For crate names (hyphens in `Cargo.toml` map to underscores in code).\n- **Acronyms:** Treat acronyms as words — `HttpClient`, not `HTTPClient`; `JsonParser`, not `JSONParser`.\n- **Getters:** Do not prefix with `get_`. A getter for `name` is `name()`, not `get_name()`.\n- **Setters:** Use `set_name()`.\n- **Builders:** Use the `Builder` suffix and return `Self` or `Result<T>` from `build()`.\n- **Iterator adaptors:** Name the struct for its iterator, e.g., `Lines` for a `lines()` method."
    },
    {
      "heading": "3. Ownership and Borrowing",
      "body": "- **Prefer borrowing over cloning:** Pass `&T` or `&mut T` unless ownership transfer is required.\n- **Prefer `&str` over `&String`:** Accept `&str` in function arguments; callers with `String` can deref. Same for `&[T]` over `&Vec<T>` and `&Path` over `&PathBuf`.\n- **Minimize `clone()`:** A `clone()` call is a code smell if it can be avoided with a borrow.\n- **Avoid `unwrap()` in library code:** Use `?` to propagate errors. Reserve `unwrap()` for prototypes, tests, or genuinely infallible paths (document why).\n- **Use `expect()` over `unwrap()`** when a panic is intended: `expect(\"config file must exist\")` produces better diagnostics."
    },
    {
      "heading": "4. Error Handling",
      "body": "- **`Result<T, E>` is the standard:** Use it for all fallible functions. Never silently discard errors.\n- **`?` operator:** Use `?` to propagate errors up the call stack. Do not write `match` blocks for simple propagation.\n- **Error types:** In libraries, define a custom error type implementing `std::error::Error`. Use `thiserror` crate. In applications, use `anyhow` for ergonomic error context.\n- **`panic!`:** Reserved for unrecoverable programming errors (invariant violations). Libraries must not panic in normal operation.\n- **`unwrap_or_else`, `map`, `and_then`:** Chain `Result`/`Option` combinators for concise, readable error paths."
    },
    {
      "heading": "5. Types and Traits",
      "body": "- **Newtype pattern:** Wrap primitive types in newtypes to enforce invariants: `struct Meters(f64);`.\n- **Derive common traits:** Derive `Debug`, `Clone`, `PartialEq`, `Eq`, `Hash`, `Default` where appropriate. Always derive `Debug`.\n- **`Default` trait:** Implement `Default` for types that have a sensible zero/empty value. Use `#[derive(Default)]` when possible.\n- **Trait objects vs. generics:** Prefer generics (`fn foo<T: Trait>(x: T)`) for performance. Use `dyn Trait` only when type erasure is needed (heterogeneous collections, dynamic dispatch).\n- **`impl Trait` in return position:** Use for simple cases to hide concrete return types without boxing.\n- **Sealed traits:** Use the sealed-trait pattern to prevent external implementations of traits not designed for extension.\n- **Iterator:** Implement `Iterator` for custom collections. Return `impl Iterator` from methods rather than concrete types."
    },
    {
      "heading": "6. Enums and Pattern Matching",
      "body": "- **Enums over booleans:** Prefer `enum Direction { Left, Right }` over `bool` for clarity.\n- **`match` is exhaustive:** Always handle all variants. Prefer explicit arms over `_ =>` wildcards when variants may be added.\n- **`if let` / `while let`:** Use for single-pattern matching instead of a full `match` block.\n- **`Option` instead of null:** Never model absence with sentinel values. Use `Option<T>`.\n- **Avoid nested `match`:** Flatten with combinators (`map`, `and_then`, `unwrap_or`) or `?`."
    },
    {
      "heading": "7. Concurrency",
      "body": "- **Fearless concurrency:** Let the compiler enforce thread safety via `Send` and `Sync` bounds.\n- **Prefer channels over shared state:** Use `std::sync::mpsc` or `tokio::sync` channels.\n- **`Arc<Mutex<T>>`:** Standard pattern for shared mutable state. Lock for the minimum scope needed.\n- **`async`/`await`:** Use for I/O-bound concurrency. Use `tokio` or `async-std` as the runtime. Do not block inside async functions — use `tokio::task::spawn_blocking` for CPU-bound work.\n- **`rayon`:** Use for data-parallel CPU-bound work instead of manual threading."
    },
    {
      "heading": "8. Modules and Visibility",
      "body": "- **`pub` is an API contract:** Only `pub` what is intended to be part of the public API. Default to private.\n- **`pub(crate)`:** Use for items shared within a crate but not externally.\n- **`mod.rs` vs. inline modules:** Prefer `module/mod.rs` for multi-file modules. Prefer inline `mod` for small, single-file submodules.\n- **`use` declarations:** Group imports: std → external crates → local. Use `rustfmt` to sort.\n- **Re-exports:** Re-export public types at the crate root to flatten the API surface."
    },
    {
      "heading": "9. Performance",
      "body": "- **Zero-cost abstractions:** Trust iterators, closures, and generics — they compile to the same code as manual loops.\n- **Avoid unnecessary allocation:** Prefer stack types and slices. Profile before switching to `Box`, `Vec`, or `Arc`.\n- **`Cow<str>` / `Cow<[T]>`:** Use when a function sometimes needs to allocate and sometimes doesn't.\n- **Inline:** Use `#[inline]` on small hot functions. Use `#[inline(always)]` sparingly."
    },
    {
      "heading": "10. Documentation",
      "body": "- **`///` doc comments:** All public items **must** have doc comments. Include an example in `# Examples` for non-trivial functions.\n- **`//!` module docs:** Add module-level documentation explaining purpose and usage.\n- **`# Panics` / `# Errors` / `# Safety`:** Document these sections on any function that panics, returns `Result`, or is `unsafe`.\n- **`cargo doc --open`:** Verify documentation renders correctly before publishing.\n- **`unsafe` blocks:** Every `unsafe` block **must** have a `// SAFETY:` comment explaining why the invariants are upheld.\n\n*Sources: [Rust API Guidelines](https://rust-lang.github.io/api-guidelines/) · [Rust Style Guide](https://doc.rust-lang.org/nightly/style-guide/) · [The Rust Book](https://doc.rust-lang.org/book/)*"
    }
  ],
  "languages": [
    "rust"
  ],
  "frameworks": [],
  "file_patterns": [],
  "applies_to": [
    "planning",
    "implementation",
    "review",
    "tests"
  ],
  "rules": [
    {
      "id": "rust-rule-1",
      "severity": "guidance",
      "summary": "**`rustfmt`:** All Rust code **must** be formatted with `rustfmt`. This is non-negotiable. Run via `cargo fmt`."
    },
    {
      "id": "rust-rule-2",
      "severity": "guidance",
      "summary": "**`clippy`:** Run `cargo clippy` and fix all warnings before review. Treat lints as errors in CI: `RUSTFLAGS=\"-D warnings\"`."
    },
    {
      "id": "rust-rule-3",
      "severity": "guidance",
      "summary": "**Indentation:** 4 spaces. No tabs."
    },
    {
      "id": "rust-rule-4",
      "severity": "guidance",
      "summary": "**Line Length:** 100 characters maximum (rustfmt default)."
    },
    {
      "id": "rust-rule-5",
      "severity": "guidance",
      "summary": "**`snake_case`:** For functions, methods, variables, modules, and crates."
    },
    {
      "id": "rust-rule-6",
      "severity": "guidance",
      "summary": "**`UpperCamelCase`:** For types, traits, enums, and struct names."
    },
    {
      "id": "rust-rule-7",
      "severity": "guidance",
      "summary": "**`SCREAMING_SNAKE_CASE`:** For constants and statics."
    },
    {
      "id": "rust-rule-8",
      "severity": "guidance",
      "summary": "**`snake_case`:** For crate names (hyphens in `Cargo.toml` map to underscores in code)."
    },
    {
      "id": "rust-rule-9",
      "severity": "guidance",
      "summary": "**Acronyms:** Treat acronyms as words — `HttpClient`, not `HTTPClient`; `JsonParser`, not `JSONParser`."
    },
    {
      "id": "rust-rule-10",
      "severity": "guidance",
      "summary": "**Getters:** Do not prefix with `get_`. A getter for `name` is `name()`, not `get_name()`."
    },
    {
      "id": "rust-rule-11",
      "severity": "guidance",
      "summary": "**Setters:** Use `set_name()`."
    },
    {
      "id": "rust-rule-12",
      "severity": "guidance",
      "summary": "**Builders:** Use the `Builder` suffix and return `Self` or `Result<T>` from `build()`."
    },
    {
      "id": "rust-rule-13",
      "severity": "guidance",
      "summary": "**Iterator adaptors:** Name the struct for its iterator, e.g., `Lines` for a `lines()` method."
    },
    {
      "id": "rust-rule-14",
      "severity": "guidance",
      "summary": "**Prefer borrowing over cloning:** Pass `&T` or `&mut T` unless ownership transfer is required."
    },
    {
      "id": "rust-rule-15",
      "severity": "guidance",
      "summary": "**Prefer `&str` over `&String`:** Accept `&str` in function arguments; callers with `String` can deref. Same for `&[T]` over `&Vec<T>` and `&Path` over `&PathBuf`."
    },
    {
      "id": "rust-rule-16",
      "severity": "guidance",
      "summary": "**Minimize `clone()`:** A `clone()` call is a code smell if it can be avoided with a borrow."
    },
    {
      "id": "rust-rule-17",
      "severity": "guidance",
      "summary": "**Avoid `unwrap()` in library code:** Use `?` to propagate errors. Reserve `unwrap()` for prototypes, tests, or genuinely infallible paths (document why)."
    },
    {
      "id": "rust-rule-18",
      "severity": "guidance",
      "summary": "**Use `expect()` over `unwrap()`** when a panic is intended: `expect(\"config file must exist\")` produces better diagnostics."
    },
    {
      "id": "rust-rule-19",
      "severity": "guidance",
      "summary": "**`Result<T, E>` is the standard:** Use it for all fallible functions. Never silently discard errors."
    },
    {
      "id": "rust-rule-20",
      "severity": "guidance",
      "summary": "**`?` operator:** Use `?` to propagate errors up the call stack. Do not write `match` blocks for simple propagation."
    },
    {
      "id": "rust-rule-21",
      "severity": "guidance",
      "summary": "**Error types:** In libraries, define a custom error type implementing `std::error::Error`. Use `thiserror` crate. In applications, use `anyhow` for ergonomic error context."
    },
    {
      "id": "rust-rule-22",
      "severity": "guidance",
      "summary": "**`panic!`:** Reserved for unrecoverable programming errors (invariant violations). Libraries must not panic in normal operation."
    },
    {
      "id": "rust-rule-23",
      "severity": "guidance",
      "summary": "**`unwrap_or_else`, `map`, `and_then`:** Chain `Result`/`Option` combinators for concise, readable error paths."
    },
    {
      "id": "rust-rule-24",
      "severity": "guidance",
      "summary": "**Newtype pattern:** Wrap primitive types in newtypes to enforce invariants: `struct Meters(f64);`."
    },
    {
      "id": "rust-rule-25",
      "severity": "guidance",
      "summary": "**Derive common traits:** Derive `Debug`, `Clone`, `PartialEq`, `Eq`, `Hash`, `Default` where appropriate. Always derive `Debug`."
    },
    {
      "id": "rust-rule-26",
      "severity": "guidance",
      "summary": "**`Default` trait:** Implement `Default` for types that have a sensible zero/empty value. Use `#[derive(Default)]` when possible."
    },
    {
      "id": "rust-rule-27",
      "severity": "guidance",
      "summary": "**Trait objects vs. generics:** Prefer generics (`fn foo<T: Trait>(x: T)`) for performance. Use `dyn Trait` only when type erasure is needed (heterogeneous collections, dynamic dispatch)."
    },
    {
      "id": "rust-rule-28",
      "severity": "guidance",
      "summary": "**`impl Trait` in return position:** Use for simple cases to hide concrete return types without boxing."
    },
    {
      "id": "rust-rule-29",
      "severity": "guidance",
      "summary": "**Sealed traits:** Use the sealed-trait pattern to prevent external implementations of traits not designed for extension."
    },
    {
      "id": "rust-rule-30",
      "severity": "guidance",
      "summary": "**Iterator:** Implement `Iterator` for custom collections. Return `impl Iterator` from methods rather than concrete types."
    },
    {
      "id": "rust-rule-31",
      "severity": "guidance",
      "summary": "**Enums over booleans:** Prefer `enum Direction { Left, Right }` over `bool` for clarity."
    },
    {
      "id": "rust-rule-32",
      "severity": "guidance",
      "summary": "**`match` is exhaustive:** Always handle all variants. Prefer explicit arms over `_ =>` wildcards when variants may be added."
    },
    {
      "id": "rust-rule-33",
      "severity": "guidance",
      "summary": "**`if let` / `while let`:** Use for single-pattern matching instead of a full `match` block."
    },
    {
      "id": "rust-rule-34",
      "severity": "guidance",
      "summary": "**`Option` instead of null:** Never model absence with sentinel values. Use `Option<T>`."
    },
    {
      "id": "rust-rule-35",
      "severity": "guidance",
      "summary": "**Avoid nested `match`:** Flatten with combinators (`map`, `and_then`, `unwrap_or`) or `?`."
    },
    {
      "id": "rust-rule-36",
      "severity": "guidance",
      "summary": "**Fearless concurrency:** Let the compiler enforce thread safety via `Send` and `Sync` bounds."
    },
    {
      "id": "rust-rule-37",
      "severity": "guidance",
      "summary": "**Prefer channels over shared state:** Use `std::sync::mpsc` or `tokio::sync` channels."
    },
    {
      "id": "rust-rule-38",
      "severity": "guidance",
      "summary": "**`Arc<Mutex<T>>`:** Standard pattern for shared mutable state. Lock for the minimum scope needed."
    },
    {
      "id": "rust-rule-39",
      "severity": "guidance",
      "summary": "**`async`/`await`:** Use for I/O-bound concurrency. Use `tokio` or `async-std` as the runtime. Do not block inside async functions — use `tokio::task::spawn_blocking` for CPU-bound work."
    },
    {
      "id": "rust-rule-40",
      "severity": "guidance",
      "summary": "**`rayon`:** Use for data-parallel CPU-bound work instead of manual threading."
    },
    {
      "id": "rust-rule-41",
      "severity": "guidance",
      "summary": "**`pub` is an API contract:** Only `pub` what is intended to be part of the public API. Default to private."
    },
    {
      "id": "rust-rule-42",
      "severity": "guidance",
      "summary": "**`pub(crate)`:** Use for items shared within a crate but not externally."
    },
    {
      "id": "rust-rule-43",
      "severity": "guidance",
      "summary": "**`mod.rs` vs. inline modules:** Prefer `module/mod.rs` for multi-file modules. Prefer inline `mod` for small, single-file submodules."
    },
    {
      "id": "rust-rule-44",
      "severity": "guidance",
      "summary": "**`use` declarations:** Group imports: std → external crates → local. Use `rustfmt` to sort."
    },
    {
      "id": "rust-rule-45",
      "severity": "guidance",
      "summary": "**Re-exports:** Re-export public types at the crate root to flatten the API surface."
    },
    {
      "id": "rust-rule-46",
      "severity": "guidance",
      "summary": "**Zero-cost abstractions:** Trust iterators, closures, and generics — they compile to the same code as manual loops."
    },
    {
      "id": "rust-rule-47",
      "severity": "guidance",
      "summary": "**Avoid unnecessary allocation:** Prefer stack types and slices. Profile before switching to `Box`, `Vec`, or `Arc`."
    },
    {
      "id": "rust-rule-48",
      "severity": "guidance",
      "summary": "**`Cow<str>` / `Cow<[T]>`:** Use when a function sometimes needs to allocate and sometimes doesn't."
    },
    {
      "id": "rust-rule-49",
      "severity": "guidance",
      "summary": "**Inline:** Use `#[inline]` on small hot functions. Use `#[inline(always)]` sparingly."
    },
    {
      "id": "rust-rule-50",
      "severity": "guidance",
      "summary": "**`///` doc comments:** All public items **must** have doc comments. Include an example in `# Examples` for non-trivial functions."
    },
    {
      "id": "rust-rule-51",
      "severity": "guidance",
      "summary": "**`//!` module docs:** Add module-level documentation explaining purpose and usage."
    },
    {
      "id": "rust-rule-52",
      "severity": "guidance",
      "summary": "**`# Panics` / `# Errors` / `# Safety`:** Document these sections on any function that panics, returns `Result`, or is `unsafe`."
    },
    {
      "id": "rust-rule-53",
      "severity": "guidance",
      "summary": "**`cargo doc --open`:** Verify documentation renders correctly before publishing."
    },
    {
      "id": "rust-rule-54",
      "severity": "guidance",
      "summary": "**`unsafe` blocks:** Every `unsafe` block **must** have a `// SAFETY:` comment explaining why the invariants are upheld."
    }
  ],
  "source": "bundled_template"
}
