{
  "version": 1,
  "schema": "cadre.styleguide.v1",
  "kind": "styleguide",
  "id": "kotlin",
  "title": "Kotlin Coding Conventions Style Guide Summary",
  "summary": "This document summarizes key rules and best practices from the official Kotlin Coding Conventions and JetBrains style guide for writing idiomatic, concise, and safe Kotlin code.",
  "sections": [
    {
      "heading": "1. Formatting",
      "body": "- **ktlint / detekt:** Format with `ktlint` and lint with `detekt`. Enforce both in CI.\n- **Indentation:** 4 spaces. No tabs. Continuation lines: 4 spaces (not 8).\n- **Line Length:** 120 characters maximum (Kotlin convention; some projects use 100).\n- **Trailing lambdas:** When the last parameter is a lambda, move it outside the parentheses. Omit the parentheses entirely if the lambda is the only parameter."
    },
    {
      "heading": "2. Naming",
      "body": "- **`UpperCamelCase`:** For classes, objects, interfaces, and enum entries.\n- **`lowerCamelCase`:** For functions, properties, local variables, and parameters.\n- **`SCREAMING_SNAKE_CASE`:** For `const val` top-level and companion object constants only.\n- **`_` prefix:** Do not use. Kotlin backing properties use `_name` as a convention for private backing fields exposed via a public property — this is the single accepted use.\n- **Acronyms:** Use `UpperCamelCase` even for acronyms: `XmlParser`, `HttpClient`, not `XMLParser`.\n- **Boolean properties:** Name as `isEnabled`, `hasError`, `canClose`. Prefix with `is`, `has`, or `can`.\n- **Test functions:** Use backtick names for test readability: `` `should throw when user is null`() ``."
    },
    {
      "heading": "3. Null Safety",
      "body": "- **Non-nullable by default:** Prefer non-nullable types. Add `?` only when `null` is a genuinely valid value.\n- **Avoid `!!`:** The `!!` operator is a code smell. It bypasses null safety and throws `NullPointerException`. Use `?.`, `?:`, `let`, `also`, or explicit null checks instead.\n- **`?.let { }`:** Use for null-conditional execution. Prefer `if (x != null)` when the block is long and `x` is used multiple times (smart cast).\n- **Elvis operator `?:`:** Use for default values: `val name = user?.name ?: \"Anonymous\"`.\n- **`requireNotNull` / `checkNotNull`:** Use to assert non-nullability at runtime with a clear message: `requireNotNull(user) { \"User must not be null\" }`."
    },
    {
      "heading": "4. Classes and Objects",
      "body": "- **`data class`:** Use for classes that hold data. They get `equals`, `hashCode`, `toString`, `copy`, and `componentN` for free.\n- **`sealed class`:** Use for closed type hierarchies (discriminated unions). Prefer `sealed class` over `enum class` when variants carry different data.\n- **`object`:** Use for singletons and companion objects. Do not use a class with a private constructor + static `getInstance()`.\n- **`companion object`:** Use for factory methods and constants logically associated with a class. Name the companion only if it will be referenced by name.\n- **`val` over `var`:** Prefer immutable `val` properties. Use `var` only when mutation is necessary.\n- **`init` blocks:** Minimize logic in `init` blocks. Prefer constructor parameter defaults and `lazy` properties.\n- **`lazy`:** Use `by lazy { }` for properties that are expensive to compute and not always needed."
    },
    {
      "heading": "5. Functions",
      "body": "- **Expression bodies:** Use `= expression` for single-expression functions. Omit braces and `return`.\n- **Default parameter values:** Use default values instead of overloaded functions.\n- **Named arguments:** Use named arguments when calling functions with many parameters or when the parameter type is a primitive and meaning is unclear.\n- **Extension functions:** Use extension functions to add behavior to existing types without subclassing. Place extensions in a file named `TypeExtensions.kt`.\n- **Scope functions (`let`, `run`, `with`, `apply`, `also`):**\n  - `apply`: Configure an object — use when returning the object. (`builder.apply { ... }`)\n  - `also`: Perform side effects — use when returning the object. (`list.also { log(it) }`)\n  - `let`: Transform a value, especially with nullability. (`str?.let { process(it) }`)\n  - `run`/`with`: Group a block of operations on an object. Prefer `run` for nullable receivers, `with` for non-null.\n  - **Do not nest scope functions more than 2 levels deep.**\n- **`operator fun`:** Only implement operator overloads when the semantics are universally clear (e.g., `+` for numeric types, `get`/`set` for collections)."
    },
    {
      "heading": "6. Collections",
      "body": "- **Immutable collections:** Prefer `listOf`, `mapOf`, `setOf`. Use `mutableListOf` etc. only when mutation is needed.\n- **`Sequence` for lazy pipelines:** Use `asSequence()` for long transformation chains on large collections to avoid creating intermediate lists.\n- **Avoid `null` in collections:** Prefer `emptyList()` over `null` for absent collections. Use `filterNotNull()` to clean mixed lists.\n- **`associate`, `groupBy`, `partition`:** Use the stdlib collection transforms instead of manual loops.\n- **Destructuring:** Use destructuring declarations in `for` loops and `map` operations: `for ((key, value) in map)`."
    },
    {
      "heading": "7. Coroutines",
      "body": "- **`suspend` functions:** Mark any function that calls another `suspend` function or performs async I/O as `suspend`. Do not block inside `suspend` functions — use `withContext(Dispatchers.IO)`.\n- **Structured concurrency:** Always launch coroutines in a `CoroutineScope`. Never use `GlobalScope` in application code.\n- **`Dispatchers.Main` / `.IO` / `.Default`:** Use the correct dispatcher — `Main` for UI, `IO` for blocking I/O, `Default` for CPU work.\n- **`Flow`:** Use `Flow<T>` for streams of values. Use `StateFlow`/`SharedFlow` for hot streams. Call `.collect {}` in a lifecycle-aware scope.\n- **Error handling:** Use `try/catch` inside coroutines. Use `CoroutineExceptionHandler` for unhandled exceptions in `launch`. `async`/`await` propagates exceptions at the `await` site.\n- **`viewModelScope` / `lifecycleScope`:** Use these scopes in Android (see Android guide) to tie coroutine lifetime to the component."
    },
    {
      "heading": "8. Error Handling",
      "body": "- **Exceptions for exceptional cases:** Do not use exceptions for normal control flow. Return `Result<T>`, `sealed class`, or nullable values for expected failure paths.\n- **`runCatching`:** Use `runCatching { }` for concise try-catch with `Result<T>` return.\n- **Avoid catching `Throwable`:** Catch specific exception types. Catching `Throwable` also catches `Error` (e.g., `OutOfMemoryError`)."
    },
    {
      "heading": "9. Documentation",
      "body": "- **KDoc (`/** */`):** Use KDoc for all public API documentation.\n- **`@param`, `@return`, `@throws`:** Include these tags for public functions with non-obvious parameters or return values.\n- **`@since`:** Mark API additions with `@since <version>`.\n- **Avoid redundant comments:** Do not restate the function name in the doc. Add information about behavior, edge cases, or constraints.\n\n*Source: [Kotlin Coding Conventions](https://kotlinlang.org/docs/coding-conventions.html)*"
    }
  ],
  "languages": [
    "kotlin"
  ],
  "frameworks": [],
  "file_patterns": [],
  "applies_to": [
    "planning",
    "implementation",
    "review",
    "tests"
  ],
  "rules": [
    {
      "id": "kotlin-rule-1",
      "severity": "guidance",
      "summary": "**ktlint / detekt:** Format with `ktlint` and lint with `detekt`. Enforce both in CI."
    },
    {
      "id": "kotlin-rule-2",
      "severity": "guidance",
      "summary": "**Indentation:** 4 spaces. No tabs. Continuation lines: 4 spaces (not 8)."
    },
    {
      "id": "kotlin-rule-3",
      "severity": "guidance",
      "summary": "**Line Length:** 120 characters maximum (Kotlin convention; some projects use 100)."
    },
    {
      "id": "kotlin-rule-4",
      "severity": "guidance",
      "summary": "**Trailing lambdas:** When the last parameter is a lambda, move it outside the parentheses. Omit the parentheses entirely if the lambda is the only parameter."
    },
    {
      "id": "kotlin-rule-5",
      "severity": "guidance",
      "summary": "**`UpperCamelCase`:** For classes, objects, interfaces, and enum entries."
    },
    {
      "id": "kotlin-rule-6",
      "severity": "guidance",
      "summary": "**`lowerCamelCase`:** For functions, properties, local variables, and parameters."
    },
    {
      "id": "kotlin-rule-7",
      "severity": "guidance",
      "summary": "**`SCREAMING_SNAKE_CASE`:** For `const val` top-level and companion object constants only."
    },
    {
      "id": "kotlin-rule-8",
      "severity": "guidance",
      "summary": "**`_` prefix:** Do not use. Kotlin backing properties use `_name` as a convention for private backing fields exposed via a public property — this is the single accepted use."
    },
    {
      "id": "kotlin-rule-9",
      "severity": "guidance",
      "summary": "**Acronyms:** Use `UpperCamelCase` even for acronyms: `XmlParser`, `HttpClient`, not `XMLParser`."
    },
    {
      "id": "kotlin-rule-10",
      "severity": "guidance",
      "summary": "**Boolean properties:** Name as `isEnabled`, `hasError`, `canClose`. Prefix with `is`, `has`, or `can`."
    },
    {
      "id": "kotlin-rule-11",
      "severity": "guidance",
      "summary": "**Test functions:** Use backtick names for test readability: `` `should throw when user is null`() ``."
    },
    {
      "id": "kotlin-rule-12",
      "severity": "guidance",
      "summary": "**Non-nullable by default:** Prefer non-nullable types. Add `?` only when `null` is a genuinely valid value."
    },
    {
      "id": "kotlin-rule-13",
      "severity": "guidance",
      "summary": "**Avoid `!!`:** The `!!` operator is a code smell. It bypasses null safety and throws `NullPointerException`. Use `?.`, `?:`, `let`, `also`, or explicit null checks instead."
    },
    {
      "id": "kotlin-rule-14",
      "severity": "guidance",
      "summary": "**`?.let { }`:** Use for null-conditional execution. Prefer `if (x != null)` when the block is long and `x` is used multiple times (smart cast)."
    },
    {
      "id": "kotlin-rule-15",
      "severity": "guidance",
      "summary": "**Elvis operator `?:`:** Use for default values: `val name = user?.name ?: \"Anonymous\"`."
    },
    {
      "id": "kotlin-rule-16",
      "severity": "guidance",
      "summary": "**`requireNotNull` / `checkNotNull`:** Use to assert non-nullability at runtime with a clear message: `requireNotNull(user) { \"User must not be null\" }`."
    },
    {
      "id": "kotlin-rule-17",
      "severity": "guidance",
      "summary": "**`data class`:** Use for classes that hold data. They get `equals`, `hashCode`, `toString`, `copy`, and `componentN` for free."
    },
    {
      "id": "kotlin-rule-18",
      "severity": "guidance",
      "summary": "**`sealed class`:** Use for closed type hierarchies (discriminated unions). Prefer `sealed class` over `enum class` when variants carry different data."
    },
    {
      "id": "kotlin-rule-19",
      "severity": "guidance",
      "summary": "**`object`:** Use for singletons and companion objects. Do not use a class with a private constructor + static `getInstance()`."
    },
    {
      "id": "kotlin-rule-20",
      "severity": "guidance",
      "summary": "**`companion object`:** Use for factory methods and constants logically associated with a class. Name the companion only if it will be referenced by name."
    },
    {
      "id": "kotlin-rule-21",
      "severity": "guidance",
      "summary": "**`val` over `var`:** Prefer immutable `val` properties. Use `var` only when mutation is necessary."
    },
    {
      "id": "kotlin-rule-22",
      "severity": "guidance",
      "summary": "**`init` blocks:** Minimize logic in `init` blocks. Prefer constructor parameter defaults and `lazy` properties."
    },
    {
      "id": "kotlin-rule-23",
      "severity": "guidance",
      "summary": "**`lazy`:** Use `by lazy { }` for properties that are expensive to compute and not always needed."
    },
    {
      "id": "kotlin-rule-24",
      "severity": "guidance",
      "summary": "**Expression bodies:** Use `= expression` for single-expression functions. Omit braces and `return`."
    },
    {
      "id": "kotlin-rule-25",
      "severity": "guidance",
      "summary": "**Default parameter values:** Use default values instead of overloaded functions."
    },
    {
      "id": "kotlin-rule-26",
      "severity": "guidance",
      "summary": "**Named arguments:** Use named arguments when calling functions with many parameters or when the parameter type is a primitive and meaning is unclear."
    },
    {
      "id": "kotlin-rule-27",
      "severity": "guidance",
      "summary": "**Extension functions:** Use extension functions to add behavior to existing types without subclassing. Place extensions in a file named `TypeExtensions.kt`."
    },
    {
      "id": "kotlin-rule-28",
      "severity": "guidance",
      "summary": "**Scope functions (`let`, `run`, `with`, `apply`, `also`):**"
    },
    {
      "id": "kotlin-rule-29",
      "severity": "guidance",
      "summary": "`apply`: Configure an object — use when returning the object. (`builder.apply { ... }`)"
    },
    {
      "id": "kotlin-rule-30",
      "severity": "guidance",
      "summary": "`also`: Perform side effects — use when returning the object. (`list.also { log(it) }`)"
    },
    {
      "id": "kotlin-rule-31",
      "severity": "guidance",
      "summary": "`let`: Transform a value, especially with nullability. (`str?.let { process(it) }`)"
    },
    {
      "id": "kotlin-rule-32",
      "severity": "guidance",
      "summary": "`run`/`with`: Group a block of operations on an object. Prefer `run` for nullable receivers, `with` for non-null."
    },
    {
      "id": "kotlin-rule-33",
      "severity": "guidance",
      "summary": "**Do not nest scope functions more than 2 levels deep.**"
    },
    {
      "id": "kotlin-rule-34",
      "severity": "guidance",
      "summary": "**`operator fun`:** Only implement operator overloads when the semantics are universally clear (e.g., `+` for numeric types, `get`/`set` for collections)."
    },
    {
      "id": "kotlin-rule-35",
      "severity": "guidance",
      "summary": "**Immutable collections:** Prefer `listOf`, `mapOf`, `setOf`. Use `mutableListOf` etc. only when mutation is needed."
    },
    {
      "id": "kotlin-rule-36",
      "severity": "guidance",
      "summary": "**`Sequence` for lazy pipelines:** Use `asSequence()` for long transformation chains on large collections to avoid creating intermediate lists."
    },
    {
      "id": "kotlin-rule-37",
      "severity": "guidance",
      "summary": "**Avoid `null` in collections:** Prefer `emptyList()` over `null` for absent collections. Use `filterNotNull()` to clean mixed lists."
    },
    {
      "id": "kotlin-rule-38",
      "severity": "guidance",
      "summary": "**`associate`, `groupBy`, `partition`:** Use the stdlib collection transforms instead of manual loops."
    },
    {
      "id": "kotlin-rule-39",
      "severity": "guidance",
      "summary": "**Destructuring:** Use destructuring declarations in `for` loops and `map` operations: `for ((key, value) in map)`."
    },
    {
      "id": "kotlin-rule-40",
      "severity": "guidance",
      "summary": "**`suspend` functions:** Mark any function that calls another `suspend` function or performs async I/O as `suspend`. Do not block inside `suspend` functions — use `withContext(Dispatchers.IO)`."
    },
    {
      "id": "kotlin-rule-41",
      "severity": "guidance",
      "summary": "**Structured concurrency:** Always launch coroutines in a `CoroutineScope`. Never use `GlobalScope` in application code."
    },
    {
      "id": "kotlin-rule-42",
      "severity": "guidance",
      "summary": "**`Dispatchers.Main` / `.IO` / `.Default`:** Use the correct dispatcher — `Main` for UI, `IO` for blocking I/O, `Default` for CPU work."
    },
    {
      "id": "kotlin-rule-43",
      "severity": "guidance",
      "summary": "**`Flow`:** Use `Flow<T>` for streams of values. Use `StateFlow`/`SharedFlow` for hot streams. Call `.collect {}` in a lifecycle-aware scope."
    },
    {
      "id": "kotlin-rule-44",
      "severity": "guidance",
      "summary": "**Error handling:** Use `try/catch` inside coroutines. Use `CoroutineExceptionHandler` for unhandled exceptions in `launch`. `async`/`await` propagates exceptions at the `await` site."
    },
    {
      "id": "kotlin-rule-45",
      "severity": "guidance",
      "summary": "**`viewModelScope` / `lifecycleScope`:** Use these scopes in Android (see Android guide) to tie coroutine lifetime to the component."
    },
    {
      "id": "kotlin-rule-46",
      "severity": "guidance",
      "summary": "**Exceptions for exceptional cases:** Do not use exceptions for normal control flow. Return `Result<T>`, `sealed class`, or nullable values for expected failure paths."
    },
    {
      "id": "kotlin-rule-47",
      "severity": "guidance",
      "summary": "**`runCatching`:** Use `runCatching { }` for concise try-catch with `Result<T>` return."
    },
    {
      "id": "kotlin-rule-48",
      "severity": "guidance",
      "summary": "**Avoid catching `Throwable`:** Catch specific exception types. Catching `Throwable` also catches `Error` (e.g., `OutOfMemoryError`)."
    },
    {
      "id": "kotlin-rule-49",
      "severity": "guidance",
      "summary": "**KDoc (`/** */`):** Use KDoc for all public API documentation."
    },
    {
      "id": "kotlin-rule-50",
      "severity": "guidance",
      "summary": "**`@param`, `@return`, `@throws`:** Include these tags for public functions with non-obvious parameters or return values."
    },
    {
      "id": "kotlin-rule-51",
      "severity": "guidance",
      "summary": "**`@since`:** Mark API additions with `@since <version>`."
    },
    {
      "id": "kotlin-rule-52",
      "severity": "guidance",
      "summary": "**Avoid redundant comments:** Do not restate the function name in the doc. Add information about behavior, edge cases, or constraints."
    }
  ],
  "source": "bundled_template"
}
