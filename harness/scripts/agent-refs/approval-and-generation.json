{
  "version": 1,
  "schema": "cadre.reference.v1",
  "id": "approval-and-generation",
  "title": "Approval and generation",
  "markdownUse": "human_projection_only",
  "rules": [
    "Generate, materialize, and ask for explicit user approval of only the current stage.",
    "A returned decision.resume or decision.amend with session_id-only approval state is not approval.",
    "Never send stage, stage_hash, stage_revision, approved_stages, or complete before explicit user approval of the exact current review set.",
    "Never inline generated artifact bodies when review paths are available."
  ],
  "sections": [
    {
      "heading": "Resume",
      "body": "For current-stage clarification or formatting, fill only decision.writable_paths in the returned decision.resume and invoke that full call. For an explicit current-stage revision, use decision.amend the same way. Both preserve the approved prefix and never approve, reopen, or pre-generate later stages."
    },
    {
      "heading": "Stage",
      "body": "Review every returned artifact for decision.stage as one atomic set. After explicit approval, call the same workflow with `approval: {session_id, stage, stage_hash, stage_revision, approved_stages}` using the exact values from that decision and exactly the next cumulative prefix."
    },
    {
      "heading": "Execute",
      "body": "When no approval stage remains, invoke the exact returned `next` unchanged. It carries `execute:true`, the session, the approved prefix, and completion authorization."
    }
  ]
}
