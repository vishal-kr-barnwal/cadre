#!/usr/bin/env node
// AUTO-GENERATED by pnpm build from src/*.ts -- do not edit by hand.
// Edit TypeScript sources under src/ instead.
const __CADRE_EMBEDDED_ASSETS__ = {"skill":{"version":1,"schema":"cadre.skill.v1","id":"cadre","name":"Cadre","markdownUse":"human_projection_only","description":"Context-driven development methodology for organized, spec-first coding. JSON is the authoritative agent/runtime contract; Markdown exists only as platform shim or generated human projection.","activation":{"triggers":["project has cadre/","user mentions specs, plans, tracks, or context-driven development","user asks for cadre-* workflow","user asks about Cadre project status, track management, review, ship, land, release, archive, setup, or artifact sync","project is a polyrepo control repo with cadre/repos.json"],"requiredRuntime":"Cadre MCP","startup":[{"tool":"cadre_project","arguments":{"action":"ping"},"purpose":"Verify Cadre MCP availability before every Cadre workflow"},{"rule":"Every project-scoped Cadre MCP call must include a per-call root argument pointing at the project root or a path inside it"},{"rule":"If Cadre MCP tools are unavailable, halt and ask the user to install, enable, or restart the Cadre plugin"}]},"lazyContext":{"defaultResponseMode":"compact","detailMode":"Use responseMode=detail only for full plans, code-intelligence evidence, or long style-guide content","canonicalArtifacts":["cadre/product.json","cadre/product_guidelines.json","cadre/tech-stack.json","cadre/workflow.json","cadre/patterns.jsonl","cadre/tracks.json","track metadata.json/spec.json/plan.json/learnings.jsonl/handoff.json","cadre/styleguides/*.json"],"humanProjections":["cadre/product.md","cadre/product_guidelines.md","cadre/workflow.md","cadre/patterns.md","track spec.md/plan.md/learnings.md/HANDOFF.md","cadre/code_styleguides/*.md"],"rules":["Do not read Markdown projections as canonical truth","Do not eagerly read the whole cadre/ tree on activation","Resolve live status from MCP packets and track metadata, not generated indexes"]},"workflows":{"setup":{"intent":"Setup project","protocol":"cadre://workflow-protocol?workflow=setup"},"newtrack":{"intent":"Create work","protocol":"cadre://workflow-protocol?workflow=newtrack"},"implement":{"intent":"Start or resume implementation","protocol":"cadre://workflow-protocol?workflow=implement"},"status":{"intent":"Status, team board, or fleet board","protocol":"cadre://workflow-protocol?workflow=status"},"validate":{"intent":"Validate project state","protocol":"cadre://workflow-protocol?workflow=validate"},"flag":{"intent":"Flag blocked or skipped work","protocol":"cadre://workflow-protocol?workflow=flag"},"revise":{"intent":"Revise spec or plan","protocol":"cadre://workflow-protocol?workflow=revise"},"review":{"intent":"Review quality gate","protocol":"cadre://workflow-protocol?workflow=review"},"ship":{"intent":"Ship monorepo work","protocol":"cadre://workflow-protocol?workflow=ship"},"land":{"intent":"Land polyrepo work","protocol":"cadre://workflow-protocol?workflow=land"},"handoff":{"intent":"Handoff context","protocol":"cadre://workflow-protocol?workflow=handoff"},"archive":{"intent":"Archive completed tracks","protocol":"cadre://workflow-protocol?workflow=archive"},"release":{"intent":"Release summary or version","protocol":"cadre://workflow-protocol?workflow=release"},"refresh":{"intent":"Refresh stale context","protocol":"cadre://workflow-protocol?workflow=refresh"},"revert":{"intent":"Revert tracked commits","protocol":"cadre://workflow-protocol?workflow=revert"},"formula":{"intent":"Formula or template operations","protocol":"cadre://workflow-protocol?workflow=formula"},"artifacts":{"intent":"Sync canonical artifacts","protocol":"cadre://workflow-protocol?workflow=artifacts"}},"references":{"mcp-contract":{"uri":"cadre://agent-reference?name=mcp-contract"},"provider-evidence":{"uri":"cadre://agent-reference?name=provider-evidence"},"team-ops":{"uri":"cadre://agent-reference?name=team-ops"},"cadre-sync":{"uri":"cadre://agent-reference?name=cadre-sync"},"ownership-guard":{"uri":"cadre://agent-reference?name=ownership-guard"},"parallel-execution":{"uri":"cadre://agent-reference?name=parallel-execution"},"polyrepo-git":{"uri":"cadre://agent-reference?name=polyrepo-git"},"template-locator":{"uri":"cadre://agent-reference?name=template-locator"}},"operatingDefaults":["Setup writes native Cadre event, message, formula, and local wisp state through packets","Shared sync is recommended for teams and packet-owned","Hosted provider evidence must come through official provider MCPs and be written back through Cadre packets","LSP/code-intelligence packets should be used for repo maps, impact checks, review assistance, diagnostics, and test-impact hints"],"forbidden":["Do not mutate cadre/, provider state, indexes, worker state, event/message state, or merge/cleanup state outside Cadre packets","Do not use Markdown as alternate input, legacy import source, or runtime state","Do not manually edit generated Markdown projections as workflow state"]},"protocols":{"archive":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-archive","description":"Archive completed tracks","workflow":"archive","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"archive\"` and the resolved root. Pass `trackId`, cutoff, or scope arguments as structured JSON fields only."},{"step":3,"instruction":"Present the dry-run `tracks` list, packet warnings, cleanup scope, and any review_bundle manifest/path list without inlining large artifact contents into model context."},{"step":4,"instruction":"To archive reviewed work, call `cadre_workflow` again with `workflow: \"archive\"`, `execute: true`, and `humanConfirmed: true`."},{"step":5,"instruction":"Summarize archived track ids, packet warnings, changed canonical JSON/JSONL artifacts, generated human projections, and any packet errors."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"artifacts":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-artifacts","description":"Sync canonical Cadre artifacts with generated human projections","workflow":"artifacts","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"For discovery, call `cadre_artifact` with `action: \"catalog\"`, `\"schema\"`, `\"validate\"`, `\"render\"`, or `\"diff\"` and structured JSON arguments."},{"step":3,"instruction":"For preview, call `cadre_artifact` with `action: \"sync\"`, the requested scope, and no legacy Markdown import fields."},{"step":4,"instruction":"Review `review_bundle` when present. The bundle contains a manifest/path list for full proposed artifacts so large Markdown projections stay out of model context."},{"step":5,"instruction":"To apply reviewed synchronization, call `cadre_artifact` again with `action: \"sync\"`, `execute: true`, and `humanConfirmed: true`."},{"step":6,"instruction":"Summarize changed canonical JSON/JSONL artifacts, regenerated human projections, skipped unmarked projections, warnings, and next recommended packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract","template-locator"]},"flag":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-flag","description":"Flag the current task as blocked or skipped with a reason","workflow":"flag","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"flag\"`, the resolved root, `trackId`, desired status, reason, and any task selectors as structured JSON fields."},{"step":3,"instruction":"Present the dry-run status change, selected track, reason, warnings, and review_bundle manifest/path list when returned."},{"step":4,"instruction":"When mutation is intended, pass `execute: true` and `humanConfirmed: true` so Cadre packets update canonical JSON state and generated projections."},{"step":5,"instruction":"Summarize selected track, resulting status, reason, changed canonical JSON artifacts, generated human projections, and packet warnings."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"formula":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-formula","description":"Manage native Cadre formulas and local wisps","workflow":"formula","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"formula\"` and the requested native formula operation as structured JSON arguments: list, show, cook, wisp_create, wisp_list, wisp_update_step, wisp_squash, wisp_burn, or pour."},{"step":3,"instruction":"Use `pour` for reviewed persistent output; it routes generated spec and plan JSON through normal `cadre-newtrack` creation. Local wisps remain under `cadre/local/wisps/` until squashed or burned."},{"step":4,"instruction":"Summarize available formulas, generated draft tracks, canonical JSON artifacts, or packet errors."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":false,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"handoff":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-handoff","description":"Create context handoff for transferring implementation to next section/session","workflow":"handoff","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"handoff\"` and any `trackId`, recipient, audience, or scope arguments as structured JSON fields."},{"step":3,"instruction":"Review the returned `track_context`, native message/event state, handoff path, warnings, and review_bundle manifest/path list without inlining large artifacts into model context."},{"step":4,"instruction":"To write the handoff, call `cadre_workflow` again with `workflow: \"handoff\"`, `execute: true`, and `humanConfirmed: true`."},{"step":5,"instruction":"Summarize the handoff path, recipient or audience, open risks, changed canonical JSON artifacts, generated human projections, and next packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"implement":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-implement","description":"Execute tasks from a track's implementation plan","workflow":"implement","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"implement\"`, the resolved root, and optional track/task selectors as structured JSON fields."},{"step":3,"instruction":"Use the returned `prepare_implementation.styleGuides` JSON context before product edits; Markdown style-guide projections are human-readable only."},{"step":4,"instruction":"Use the returned `prepare_implementation` and `phase_schedule` to choose the next ready task while respecting ownership, repo, and dependency JSON fields."},{"step":5,"instruction":"For parallel work, use `cadre_parallel` packets for planning, worker setup, finish recording, merge-back, and cleanup."},{"step":6,"instruction":"After each task, call `cadre_complete_task` with coverage, commit, verification, and summary evidence as structured JSON fields."},{"step":7,"instruction":"Summarize completed work, style guides used, tests run, coverage, commits, changed canonical JSON artifacts, generated human projections, and next packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":false,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["parallel-execution","mcp-contract","ownership-guard"]},"land":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-land","description":"Open and link the cross-repo PR group for a polyrepo track, then let the merge train land it","workflow":"land","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"land\"` and the requested `trackId` as structured JSON."},{"step":3,"instruction":"Use the returned topology, polyrepo preflight, review gate, fleet status, provider actions, git actions, and review_bundle manifest/path list."},{"step":4,"instruction":"When the packet reports `phase_state: \"ready\"` or `\"pending_provider\"`, call the packet again with `execute: true` only after required review and provider evidence are available."},{"step":5,"instruction":"If the packet returns `phase_state: \"pending_provider\"`, execute only the returned provider MCP actions and write evidence back through Cadre packets."},{"step":6,"instruction":"Summarize every repo result, linked review artifact, CI state, packet warnings, changed canonical JSON artifacts, generated human projections, and remaining blockers."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":false,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["provider-evidence","polyrepo-git","mcp-contract"]},"newtrack":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-newtrack","description":"Create a new feature or bug track with spec and plan","workflow":"newtrack","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Draft structured `spec` and `plan` JSON from the user request and current project context. Do not send Markdown fields such as `specText`, `planText`, or `planPath`."},{"step":3,"instruction":"Call `cadre_workflow` with `workflow: \"newtrack\"`, `trackId`, `spec`, `plan`, and any metadata as structured JSON fields."},{"step":4,"instruction":"Review the returned canonical spec/plan JSON artifacts, generated Markdown projection previews, learnings starter, plan assistance, and review_bundle manifest/path list without inlining large artifacts into model context."},{"step":5,"instruction":"To create the track after approval, call `cadre_workflow` again with `workflow: \"newtrack\"`, `execute: true`, and `humanConfirmed: true` using the same JSON payload."},{"step":6,"instruction":"Summarize created track id, native event record, worktree plan, changed canonical JSON artifacts, generated human projections, and next packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"refresh":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-refresh","description":"Sync context docs with current codebase state","workflow":"refresh","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"refresh\"` and any requested scope as structured JSON fields."},{"step":3,"instruction":"Use the returned doctor, workspace diagnostics, dependency graph, LSP evidence, and review_bundle manifest/path list to identify stale canonical JSON/JSONL context without inlining large artifacts into model context."},{"step":4,"instruction":"When packet-supported document updates are requested, call the workflow packet again with `execute: true` and `humanConfirmed: true`."},{"step":5,"instruction":"Summarize refreshed canonical context, regenerated human projections, stale areas, diagnostics, and packet warnings."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"release":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-release","description":"Cut a local release - changelog plus version evidence across completed tracks","workflow":"release","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"release\"` and any requested version, date, or scope arguments as structured JSON fields."},{"step":3,"instruction":"Review returned completed tracks, release evidence, warnings, proposed artifacts, and review_bundle manifest/path list without inlining large artifacts into model context."},{"step":4,"instruction":"To write release artifacts, call `cadre_workflow` again with `workflow: \"release\"`, `execute: true`, and `humanConfirmed: true`."},{"step":5,"instruction":"Summarize release scope, version intent, generated artifacts, changed canonical JSON/JSONL state, generated human projections, and remaining blockers."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"revert":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-revert","description":"Revert tracks, phases, or tasks through Cadre packets","workflow":"revert","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"revert\"` and the requested `trackId`, commit, repo, or scope arguments as structured JSON fields."},{"step":3,"instruction":"Present the packet dry-run revert scope, planned git actions, affected canonical JSON artifacts, generated projections, warnings, and review_bundle manifest/path list."},{"step":4,"instruction":"To execute an approved revert, call `cadre_workflow` again with `workflow: \"revert\"`, `execute: true`, and `humanConfirmed: true`."},{"step":5,"instruction":"Summarize reverted scope, tests requested or run, changed canonical JSON/JSONL artifacts, generated human projections, packet warnings, and follow-up packets."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"review":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-review","description":"Review a track's diff before shipping (quality gate)","workflow":"review","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"review\"` and the requested `trackId` as structured JSON."},{"step":3,"instruction":"Use the returned track context, review assistance, machine gate, review gate, provider evidence state, and review_bundle manifest/path list."},{"step":4,"instruction":"When provider evidence is required, call `cadre_review` with structured provider MCP evidence; do not fetch or mutate provider state through shell commands."},{"step":5,"instruction":"To record a verdict, call `cadre_mutate` with `action: \"record_review\"` and the structured verdict/evidence payload."},{"step":6,"instruction":"Summarize findings first, then gate state, evidence, changed canonical JSON artifacts, generated human projections, and next packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":false,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"revise":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-revise","description":"Update spec/plan when implementation reveals issues","workflow":"revise","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"revise\"`, the requested `trackId`, and proposed structured `spec` or `plan` JSON fields. Do not send Markdown fields such as `specText`, `planText`, or `planPath`."},{"step":3,"instruction":"Use the returned track context and impact analysis to review proposed canonical JSON changes and generated Markdown projection previews."},{"step":4,"instruction":"When packet-supported mutation is requested, call the workflow packet again with `execute: true` and `humanConfirmed: true` after reviewing the review_bundle manifest/path list without inlining large artifacts into model context."},{"step":5,"instruction":"Summarize changed requirements, affected tasks, tests to update, changed canonical JSON artifacts, generated human projections, and packet warnings."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"setup":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-setup","description":"Initialize project with Cadre context-driven development","workflow":"setup","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Call `cadre_project` with `action: \"ping\"` to confirm MCP availability."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"setup\"`, the root candidate, structured `product`, optional `productGuidelines`, optional `workflowPolicy`, and `techStack` JSON fields. Use the returned setup review artifacts and native state summary for the user review."},{"step":3,"instruction":"Use the returned doctor report, workspace diagnostics, dependency graph, LSP evidence, template inventory, and selected JSON style guides."},{"step":4,"instruction":"Present returned setup review artifacts using the review_bundle manifest/path list so large generated Markdown projections do not enter model context."},{"step":5,"instruction":"When the user confirms the setup payload and review artifacts, call `cadre_workflow` again with `workflow: \"setup\"`, `execute: true`, and `humanConfirmed: true`."},{"step":6,"instruction":"Summarize created canonical JSON/JSONL files, generated human projections, selected code style guides, native state initialization, warnings, and next packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":true,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["template-locator","mcp-contract"]},"ship":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-ship","description":"Rebase a reviewed track onto main, publish it, and prepare the PR","workflow":"ship","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"ship\"` and the requested `trackId` as structured JSON."},{"step":3,"instruction":"Use the returned review gate, git actions, provider actions, CI evidence, warnings, and review_bundle manifest/path list."},{"step":4,"instruction":"When the packet reports `phase_state: \"ready\"` or `\"pending_provider\"`, call the packet again with `execute: true` only after required review and provider evidence are available."},{"step":5,"instruction":"If the packet returns `phase_state: \"pending_provider\"`, execute only returned provider MCP actions and write evidence back through Cadre packets."},{"step":6,"instruction":"Summarize branch, review gate, provider artifact, CI state, changed canonical JSON artifacts, generated human projections, and next packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":false,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["provider-evidence","polyrepo-git","mcp-contract"]},"status":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-status","description":"Display current Cadre project progress","workflow":"status","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"status\"` and a `mode` such as `live`, `team`, `fleet`, or `review` as structured JSON fields."},{"step":3,"instruction":"For rich resources, read the matching MCP resource when useful instead of eagerly loading the whole Cadre control plane."},{"step":4,"instruction":"Summarize active track, ownership, blockers, review queue, fleet health, canonical JSON state, generated projection status, and next packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":false,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]},"validate":{"version":1,"schema":"cadre.protocol.v1","id":"cadre-validate","description":"Validate Cadre project integrity","workflow":"validate","markdownUse":"human_projection_only","preconditions":[{"tool":"cadre_project","arguments":{"action":"ping"},"requirement":"Cadre MCP must be available"},{"requirement":"Every project-scoped packet call includes root"},{"requirement":"If a required packet returns ok:false, halt and report the packet error"}],"packetFlow":[{"step":1,"instruction":"Resolve the project root with `cadre_project` using `action: \"root\"`."},{"step":2,"instruction":"Call `cadre_workflow` with `workflow: \"validate\"` and any requested track, scope, or repair intent as structured JSON fields."},{"step":3,"instruction":"Use the returned doctor report, team inventory, plan integrity, collision scan, provider status, and review_bundle manifest/path list."},{"step":4,"instruction":"When packet-supported repair is requested, call the workflow packet again with `execute: true` and `humanConfirmed: true` after reviewing proposed canonical JSON changes."},{"step":5,"instruction":"Summarize errors before warnings, then repaired canonical JSON/JSONL artifacts, generated human projections, remaining risks, and next packet."}],"confirmation":{"requiredForMutation":true,"dryRunFirst":false,"executeArgument":"execute","humanConfirmationArgument":"humanConfirmed"},"forbiddenActions":["Do not recreate Cadre control-plane, native event/message state, index, worktree, provider, merge, or cleanup state with shell commands","Do not read or write Markdown projections as canonical state","Do not use Markdown payload fields, legacy Markdown import, or prompt-side state repair"],"responseSummary":["packet ok/error state","selected track or scope","warnings and blockers","changed canonical JSON/JSONL artifacts","generated human projections when relevant","next recommended packet"],"references":["mcp-contract"]}},"references":{"cadre-sync":{"version":1,"schema":"cadre.reference.v1","id":"cadre-sync","title":"Control-Plane Sync","markdownUse":"human_projection_only","rules":["Spec, plan, repository config, provider config, and topology conflicts are","Ephemeral worker state and native local state details are MCP-owned and regenerated or","Track-id collisions, shared-mode retries, and sync safety classification are"],"sections":[{"heading":"Predicate","body":"`cadre_project` with `action: \"topology\"` reports whether the project uses local\nor shared control-plane sync. Shared sync applies equally to monorepo and\npolyrepo projects. Product code publication is separate and happens only through\nship or land packets."},{"heading":"Preamble","body":"Before any workflow mutates Cadre state, call the workflow packet requested by\nthe protocol. The packet is responsible for shared-mode preflight, remote\nverification, native state sync, merge-driver health, conflict detection, and\nbounded retry policy.\n\nIf the packet reports a sync conflict, halt and summarize the conflicting files\nor state categories from the packet result. Do not hand-merge Cadre files\nstate, or generated state JSON."},{"heading":"Postamble","body":"After a workflow packet mutates Cadre state, use the next packet named by the\nresult for sync or publication. `cadre_project` with\n`action: \"sync_control_plane\"` owns shared control-plane publication and refuses\nto publish when product-code changes are mixed into the control-plane surface."},{"heading":"Conflict Policy","body":"- Spec, plan, repository config, provider config, and topology conflicts are\n  surfaced to the user by packet results.\n- Ephemeral worker state and native local state details are MCP-owned and regenerated or\n  repaired only through packets.\n- Track-id collisions, shared-mode retries, and sync safety classification are\n  handled by packets. Agents report the packet result and stop when it blocks."}]},"mcp-contract":{"version":1,"schema":"cadre.reference.v1","id":"mcp-contract","title":"Cadre MCP Contract","markdownUse":"human_projection_only","rules":["`cadre_workflow` coordinates setup, newtrack, implement, status, validate,","`cadre_project` handles ping, doctor, root, topology, tech-stack summary,","`cadre_status` returns live, team, mine, available, collision, fleet, and","`cadre_track` returns bounded track context, parsed plans, integrity checks,","`cadre_parallel` plans task waves, sets up workers, records worker finish","`cadre_mutate` owns control-plane writes such as claim, heartbeat, status,","`cadre_complete_task` performs the journaled task-completion transaction.","`cadre_review` owns review assistance, machine gates, review gates,","`cadre_intel` owns repo maps, LSP setup/review/impact, workspace diagnostics,","`cadre_artifact` owns canonical artifact catalog/schema/import/validation,","`cadre_project action: integrations` and `cadre://integrations` report","`cadre_job` starts, lists, cancels, and reads async job artifacts.","`cadre://team-board?root=...`","`cadre://fleet-board?root=...`","`cadre://my-next-actions?root=...`","`cadre://review-queue?root=...`","`cadre://handoff-inbox?root=...`","`cadre://track-context?root=...&trackId=...`","`cadre://review-evidence?root=...&trackId=...`","`cadre://track-plan?root=...&trackId=...`","`cadre://track-spec?root=...&trackId=...`","`cadre://parallel-state?root=...&trackId=...`","`cadre://quality-gate?root=...&trackId=...`","`cadre://repo-map?root=...`","`cadre://integrations?root=...`","`cadre://workspace-health?root=...`","`cadre://workspace-diagnostics?root=...`","`cadre://test-impact?root=...&files=...`","`cadre://lsp-status?root=...`","`cadre://repo-topology?root=...`","`cadre://provider-actions?root=...&trackId=...&workflow=ship|land`","`cadre://ship-plan?root=...&trackId=...`","`cadre://land-plan?root=...&trackId=...`","`cadre://release-plan?root=...`","`cadre://job-result?root=...&jobId=...`","`cadre://artifact-catalog?root=...`","`cadre://artifact-schema?root=...&artifact=...`","`cadre://artifact-preview?root=...&artifact=...`","`cadre://artifact-sync-plan?root=...&scope=...`","`cadre://styleguide-selection?root=...&trackId=...`"],"sections":[{"heading":"Primary Packets","body":"- `cadre_workflow` coordinates setup, newtrack, implement, status, validate,\n  review, ship, land, archive, release, handoff, refresh, flag, revert, revise,\n  and formula flows.\n- `cadre_project` handles ping, doctor, root, topology, tech-stack summary,\n  shared sync, and polyrepo preflight.\n- `cadre_status` returns live, team, mine, available, collision, fleet, and\n  native state summary views.\n- `cadre_track` returns bounded track context, parsed plans, integrity checks,\n  phase schedules, implementation prep, planning evidence, and worktree plans.\n- `cadre_parallel` plans task waves, sets up workers, records worker finish\n  state, merges safe workers, and cleans merged workers.\n- `cadre_mutate` owns control-plane writes such as claim, heartbeat, status,\n  metadata, review, worker state, task result, and index regeneration.\n- `cadre_complete_task` performs the journaled task-completion transaction.\n- `cadre_review` owns review assistance, machine gates, review gates,\n  provider-evidence writes, and PR/MR/CI status normalization.\n- `cadre_intel` owns repo maps, LSP setup/review/impact, workspace diagnostics,\n  test impact, dependency graphs, and daemon lifecycle.\n- `cadre_artifact` owns canonical artifact catalog/schema/import/validation,\n  deterministic projection rendering, diffing, and sync review bundles.\n- `cadre_project action: integrations` and `cadre://integrations` report\n  configured optional MCPs plus LSP coverage in one bounded view.\n- `cadre_job` starts, lists, cancels, and reads async job artifacts."},{"heading":"Response Modes","body":"Default compact responses include summaries, counts, warnings, next actions, and\nresource URIs. Use `responseMode: \"detail\"` for full plan bodies, full LSP\nevidence, long command outputs, or full style-guide content."},{"heading":"Compact Resources","body":"Use MCP resources for bounded dashboards:\n\n- `cadre://team-board?root=...`\n- `cadre://fleet-board?root=...`\n- `cadre://my-next-actions?root=...`\n- `cadre://review-queue?root=...`\n- `cadre://handoff-inbox?root=...`\n- `cadre://track-context?root=...&trackId=...`\n- `cadre://review-evidence?root=...&trackId=...`\n- `cadre://track-plan?root=...&trackId=...`\n- `cadre://track-spec?root=...&trackId=...`\n- `cadre://parallel-state?root=...&trackId=...`\n- `cadre://quality-gate?root=...&trackId=...`\n- `cadre://repo-map?root=...`\n- `cadre://integrations?root=...`\n- `cadre://workspace-health?root=...`\n- `cadre://workspace-diagnostics?root=...`\n- `cadre://test-impact?root=...&files=...`\n- `cadre://lsp-status?root=...`\n- `cadre://repo-topology?root=...`\n- `cadre://provider-actions?root=...&trackId=...&workflow=ship|land`\n- `cadre://ship-plan?root=...&trackId=...`\n- `cadre://land-plan?root=...&trackId=...`\n- `cadre://release-plan?root=...`\n- `cadre://job-result?root=...&jobId=...`\n- `cadre://artifact-catalog?root=...`\n- `cadre://artifact-schema?root=...&artifact=...`\n- `cadre://artifact-preview?root=...&artifact=...`\n- `cadre://artifact-sync-plan?root=...&scope=...`\n- `cadre://styleguide-selection?root=...&trackId=...`\n\nResource templates are discoverable through MCP, so clients should prefer\ntemplate discovery when available."}]},"ownership-guard":{"version":1,"schema":"cadre.reference.v1","id":"ownership-guard","title":"Ownership Guard","markdownUse":"human_projection_only","rules":[],"sections":[{"heading":"Packet Flow","body":"1. Resolve the project root with `cadre_project` using `action: \"root\"`.\n2. For track context, call `cadre_track` with `action: \"context\"` and the target\n   `trackId`.\n3. To take or refresh ownership, call `cadre_mutate` with `action: \"claim\"` or\n   use the workflow packet that performs the claim for the current operation.\n4. For long-running work, call `cadre_mutate` with `action: \"heartbeat\"` when the\n   workflow packet asks for a heartbeat."},{"heading":"Foreign Holder","body":"If a packet reports that the track is held by another operator, present the\nholder, status, and packet-provided options to the user. Continue only by calling\nthe takeover packet with the packet-prescribed argument. If the user declines or\nthe packet returns `ok:false`, halt."},{"heading":"Handoff Exception","body":"If the packet reports a pending handoff addressed to the current operator, use\nthe pickup or claim packet path named by the result. Do not clear handoff labels\nor mirror ownership directly."},{"heading":"Staleness","body":"The canonical stale ownership window is thirty minutes. Packets compute and\nenforce this value. Agents should report stale reclaim evidence returned by the\npacket and then continue through packetized claim or heartbeat calls only."}]},"parallel-execution":{"version":1,"schema":"cadre.reference.v1","id":"parallel-execution","title":"Parallel Worker Dispatch","markdownUse":"human_projection_only","rules":["Work only on the product files assigned in the packet payload.","Run product tests, builds, linters, and commits according to the worker prompt.","Do not mutate Cadre control-plane files, native Cadre event/message state, provider state, worktree","Return structured evidence to the coordinator: worker id, task id, repo,","If product implementation fails, return the failure evidence. The coordinator","Use only `cadre_parallel`, `cadre_complete_task`, and other Cadre MCP packets","Treat `parallel_state.json` and native Cadre event/message state as packet-owned artifacts.","A worker conflict, failed test, missing commit, low coverage, or missing native task evidence","Product shell commands are allowed only for product implementation,"],"sections":[{"heading":"Packet Flow","body":"1. Call `cadre_workflow` with `workflow: \"implement\"` and the selected `trackId`\n   to obtain implementation prep and the phase schedule.\n2. For a parallel phase, call `cadre_parallel` with `action: \"plan\"` or\n   `action: \"next_wave\"` to obtain the ready worker set.\n3. Call `cadre_parallel` with `action: \"setup_workers\"` and the current client\n   `agentIdentifier` (`\"claude\"` or `\"codex\"`) to get the packet-owned\n   worktree/setup plan and selected dispatch adapter. Add `execute:true` only\n   when the user or workflow has authorized mutation.\n4. Dispatch exactly the workers returned by the packet. Do not add, remove,\n   reorder, split, or merge workers outside the packet result.\n5. As each worker finishes, call `cadre_parallel` with `action: \"record_finish\"`\n   and the worker result evidence. Use `execute:true` when recording completion.\n6. After the wave finishes, call `cadre_parallel` with `action: \"merge_back\"` for\n   the packet-owned merge plan. Add `execute:true` only when ready to mutate.\n7. Call `cadre_parallel` with `action: \"cleanup\"` for packet-owned cleanup. Add\n   `execute:true` only after merge-back is clean or the packet explicitly marks a\n   worker safe to clean.\n8. Repeat `next_wave` until the packet reports no ready workers.\n\nAll dependency gates, file collision checks, repo routing, event/message updates,\nparallel audit state, merge conflict state, task completion, and cleanup are\npacket-owned."},{"heading":"Dispatching Each Worker","body":"<!-- AGENT:claude -->\nPass `agentIdentifier:\"claude\"` to `cadre_parallel` when setting up workers.\nThen call the **`Task` tool** once per packet worker using the MCP-returned\n`selected_dispatch`, worker prompt, worktree, owned files, and expected result\nschema. Worker calls are awaitable; wait for every worker in the wave before\ncalling the next Cadre packet.\n<!-- /AGENT:claude -->\n<!-- AGENT:codex -->\nPass `agentIdentifier:\"codex\"` to `cadre_parallel` when setting up workers.\nThen use Codex multi-agent tools from the MCP-returned `selected_dispatch`.\nSearch tool discovery for `multi_agent_v1.spawn_agent`, spawn one worker agent\nper packet worker, pass the worker prompt and dispatch payload, and wait with\nthe corresponding multi-agent wait tool before calling the next Cadre packet.\nIf the selected dispatch mechanism is unavailable, halt with a clear\n`dispatch-unavailable` message instead of simulating worker state locally.\n<!-- /AGENT:codex -->"},{"heading":"Worker Rules","body":"- Work only on the product files assigned in the packet payload.\n- Run product tests, builds, linters, and commits according to the worker prompt.\n- Do not mutate Cadre control-plane files, native Cadre event/message state, provider state, worktree\n  topology, merge state, or cleanup state directly.\n- Return structured evidence to the coordinator: worker id, task id, repo,\n  commit evidence, tests run, coverage when available, files changed, summary,\n  and blockers.\n- If product implementation fails, return the failure evidence. The coordinator\n  records it through Cadre packets."},{"heading":"Coordinator Rules","body":"- Use only `cadre_parallel`, `cadre_complete_task`, and other Cadre MCP packets\n  named by packet results for Cadre state changes.\n- Treat `parallel_state.json` and native Cadre event/message state as packet-owned artifacts.\n- A worker conflict, failed test, missing commit, low coverage, or missing native\n  task evidence is handled by the packet result. Report the packet error and halt when\n  it is blocking.\n- Product shell commands are allowed only for product implementation,\n  verification, and commits inside worker scope."}],"platforms":{"codex":{"agentIdentifier":"codex","dispatch":"Pass agentIdentifier:\"codex\" to setup_workers. Use the returned selected_dispatch to discover multi_agent_v1.spawn_agent, spawn one worker per packet worker, wait for all workers, then call the next Cadre packet. If unavailable, halt with dispatch-unavailable."},"claude":{"agentIdentifier":"claude","dispatch":"Pass agentIdentifier:\"claude\" to setup_workers. Use the returned selected_dispatch to call the Task tool once per MCP-provided worker prompt. Pass dispatch payload, worktree, owned files, expected result schema, and wait for every worker before calling the next Cadre packet."}}},"polyrepo-git":{"version":1,"schema":"cadre.reference.v1","id":"polyrepo-git","title":"Polyrepo Routing And Worktrees","markdownUse":"human_projection_only","rules":["`cadre_track` with `action: \"context\"`","`cadre_track` with `action: \"worktree_plan\"`","`cadre_parallel` worker packets","`cadre_workflow` with `workflow: \"ship\"`, `workflow: \"land\"`, or"],"sections":[{"heading":"Topology","body":"Call `cadre_project` with `action: \"topology\"` or the workflow packet requested\nby the protocol. The packet reports whether the project is monorepo or polyrepo,\nthe control repo, enabled product repos, default repo, and any topology warnings.\n\nIf topology data is missing or invalid, halt on the packet error. Do not infer\nrepository layout by reading or rewriting Cadre state directly."},{"heading":"Task Repository Routing","body":"Plan tasks may carry repo annotations, and polyrepo metadata may carry per-repo\nbranch and worktree information. Agents consume this routing only through packet\nresults such as:\n\n- `cadre_track` with `action: \"context\"`\n- `cadre_track` with `action: \"worktree_plan\"`\n- `cadre_parallel` worker packets\n- `cadre_workflow` with `workflow: \"ship\"`, `workflow: \"land\"`, or\n  `workflow: \"validate\"`\n\nTask completion, commit evidence, repo-specific SHA evidence, and progress\nrecording go through `cadre_complete_task` or the packet named by the current\nworkflow result."},{"heading":"Product Commands","body":"Workers may run product implementation, verification, and commit commands inside\nthe repo/worktree path provided by a packet. These commands are for target\nproject code only. They must not mutate Cadre control-plane files, native Cadre event/message state,\nworktree topology, merge state, or cleanup state."},{"heading":"Ship, Land, Revert, And Cleanup","body":"Use `cadre_workflow` packets for ship, land, revert, archive, and release\noperations. Use `cadre_parallel` packets for worker setup, merge-back, and\ncleanup. If a packet returns a hard gate for missing submodules, branch state,\nprovider evidence, review state, or validation, halt and report it."}]},"provider-evidence":{"version":1,"schema":"cadre.reference.v1","id":"provider-evidence","title":"Provider Evidence","markdownUse":"human_projection_only","rules":["Local review may return `phase_state: \"pending_provider\"` when hosted evidence","Ship and land packets fail closed when hosted evidence is required for the","Provider action packets return the exact evidence key and minimum fields the","`cadre_review` with `action: \"provider_evidence\"`","`cadre_workflow` with `workflow: \"ship\"` or `workflow: \"land\"` and","`cadre_mutate` with `action: \"record_review\"` when recording the final review","Cadre native Cadre packets for notes or labels when the workflow packet asks for them"],"sections":[{"heading":"Review Versus Publication","body":"- Local review may return `phase_state: \"pending_provider\"` when hosted evidence\n  is unavailable. Treat this as pending evidence, not as a local review failure.\n- Ship and land packets fail closed when hosted evidence is required for the\n  selected provider mode and has not been written back.\n- Provider action packets return the exact evidence key and minimum fields the\n  provider MCP result must satisfy."},{"heading":"Write-Back Targets","body":"Use these packet paths to persist provider evidence:\n\n- `cadre_review` with `action: \"provider_evidence\"`\n- `cadre_workflow` with `workflow: \"ship\"` or `workflow: \"land\"` and\n  `providerEvidence`\n- `cadre_mutate` with `action: \"record_review\"` when recording the final review\n  verdict\n- Cadre native Cadre packets for notes or labels when the workflow packet asks for them\n\nNever treat a hosted label, provider issue state, or PR/MR review as Cadre state\nuntil a Cadre packet records the corresponding decision."}]},"team-ops":{"version":1,"schema":"cadre.reference.v1","id":"team-ops","title":"Team Operations","markdownUse":"human_projection_only","rules":["`in_progress` after worker setup","`awaiting_merge` after tested worker completion with commit evidence","`merged` after safe merge-back","`failed` or `conflict` when the worker cannot be merged safely","GitHub or GitLab provider MCP for PR/MR metadata, review approvals, CI status,","Sourcegraph or internal code-search MCP for cross-repo symbol search when","Issue tracker MCP such as Jira or Linear when roadmap evidence lives outside","CI-specific MCP for Buildkite, CircleCI, Argo, Jenkins, or other systems that","Sentry, Datadog, Honeycomb, or logging MCP for incident and runtime evidence.","Knowledge-base MCP for product/domain documents used as read-only evidence."],"sections":[{"heading":"Sync Mode","body":"Use `local` mode for solo work or private experiments. Use `shared` mode when\nmultiple people need to see ownership, leases, review queues, blockers, and\navailable work. Shared sync is packet-owned; agents should rely on Cadre packet\nresults for pre/post sync status and recovery instructions.\n\nGenerated setup can configure `.gitattributes` for parallel worker state so shared control-plane merges stay low-conflict."},{"heading":"Parallelism","body":"Parallel work is task-level and dependency-aware. `cadre_parallel` returns only\ntasks whose phase, dependencies, worker state, and file claims are ready. A\nsequential phase dispatches one unfinished task at a time. A parallel phase may\ndispatch multiple independent tasks when file claims do not conflict.\n\nWorker status transitions:\n\n- `in_progress` after worker setup\n- `awaiting_merge` after tested worker completion with commit evidence\n- `merged` after safe merge-back\n- `failed` or `conflict` when the worker cannot be merged safely\n\nCleanup removes merged workers by default; force is required for unfinished,\nfailed, or conflicted workers."},{"heading":"MCP And LSP Use","body":"Use `cadre_intel` before prompt-side repo scans when possible. It can return\nrepo maps, dependency graphs, workspace diagnostics, test impact, LSP setup,\nLSP impact, and warm review evidence. Polyrepo projects return repo-qualified\nresults so clients can distinguish control-plane files from product-repo files.\nUse `cadre://integrations` when you need one bounded view of provider, code\nsearch, issue, CI, logging, knowledge-base, and LSP coverage.\n\nRecommended optional MCP additions for larger teams:\n\n- GitHub or GitLab provider MCP for PR/MR metadata, review approvals, CI status,\n  and hosted provider evidence.\n- Sourcegraph or internal code-search MCP for cross-repo symbol search when\n  product repos are not fully local.\n- Issue tracker MCP such as Jira or Linear when roadmap evidence lives outside\n  Cadre.\n- CI-specific MCP for Buildkite, CircleCI, Argo, Jenkins, or other systems that\n  are not covered by GitHub/GitLab checks.\n- Sentry, Datadog, Honeycomb, or logging MCP for incident and runtime evidence.\n- Knowledge-base MCP for product/domain documents used as read-only evidence."}]},"template-locator":{"version":1,"schema":"cadre.reference.v1","id":"template-locator","title":"Cadre Template Assets","markdownUse":"human_projection_only","rules":["`cadre_workflow` with reviewable dry-runs reports review artifacts and a","`cadre_workflow` with `workflow: \"setup_scaffold\"` and `execute:true` writes","`cadre_workflow` with `workflow: \"newtrack\"` writes per-track files, including","`cadre_intel` owns LSP setup, warm review, cold review, and daemon status","`cadre_mutate` owns derived index refreshes and other Cadre state mutations."],"sections":[{"heading":"Packet Ownership","body":"- `cadre_workflow` with reviewable dry-runs reports review artifacts and a\n  token-safe `review_bundle` with full proposed files on disk. Setup, new-track,\n  revise, refresh document updates, release artifacts, and handoff artifacts use\n  this pattern.\n- `cadre_workflow` with `workflow: \"setup_scaffold\"` and `execute:true` writes\n  the setup scaffold from bundled templates and confirmed setup payload.\n- `cadre_workflow` with `workflow: \"newtrack\"` writes per-track files, including\n  template-backed track learnings.\n- `cadre_intel` owns LSP setup, warm review, cold review, and daemon status\n  checks.\n- `cadre_mutate` owns derived index refreshes and other Cadre state mutations."},{"heading":"Style Guides","body":"Setup always selects the general style guide when available, adds guides derived\nfrom structured `techStack` JSON, and unions any explicit `styleGuideIds` passed\nto the packet.\nThe setup packet returns `styleGuides.detected`, `styleGuides.selected`,\n`styleGuides.written`, `styleGuides.skipped`, and `styleGuides.missing`.\n\nIf `styleGuides.missing` is non-empty, surface the packet warning and keep the\nvalid selected guides. Do not remove user-supplied `styleGuideIds` just to make a\ndry-run warning disappear. If the packet returns `ok:false`, halt and surface the\npacket error. Do not replace the packet with manual copying."},{"heading":"Workflow Review Bundles","body":"Reviewable workflow dry-runs return `review_artifacts` as metadata only and\n`review_bundle` with a manifest path, absolute review file paths, and optional\nshell commands for local rendering. Use those files for human review instead of\npasting complete generated files into chat/model context by default."},{"heading":"Agent Use","body":"Use template references only to understand what the packet-created files mean.\nAll setup, new-track, LSP, style-guide, and index writes are packet-owned."}]}},"templates":{"ci/cadre-merge-train.github.yml":"# Cadre cross-repo merge train (GitHub Actions).\n#\n# Scaffolded into the CONTROL repo at .github/workflows/cadre-merge-train.yml\n# by /cadre-setup when provider_mode == \"github\" and merge_train.enabled.\n#\n# What it does: when every PR in a track group (label `cadre-track:<id>`,\n# spread across the control repo + product submodule repos) is APPROVED and\n# CI-GREEN, it merges them in a fixed order — PRODUCT repos first, then bumps\n# each submodule gitlink to the freshly-merged SHA on the control track branch,\n# then merges the CONTROL PR LAST. The control PR is the single \"seal\" commit;\n# there is no true cross-repo atomic merge, so on any failure it HALTS, leaves\n# already-merged product PRs in place, comments the failure, and re-fires once\n# the failing repo is green again.\n#\n# PREREQUISITE — cross-repo token: the default GITHUB_TOKEN cannot act on other\n# repos. Provide a PAT or GitHub App installation token (scopes: repo, workflow,\n# read:org) with write access to EVERY product repo, stored as the\n# `CADRE_TRAIN_TOKEN` secret in this control repo.\n#\n# Also configure branch protection on every product repo's default branch and on\n# the control branch: require approvals + the relevant status checks. The train\n# only automates the merge click; protection is what enforces review/CI.\n\nname: cadre-merge-train\n\n# TRIGGER MODEL: pull_request_review / check_suite fire only for events in THIS\n# (control) repo — i.e. when the CONTROL PR is reviewed or its checks complete, the\n# job derives the track from that PR's group label and tries to land. A review/green\n# on a PRODUCT repo does NOT fire this workflow. To auto-fire on product-repo\n# approvals, either (a) have each product repo send a `repository_dispatch` to this\n# repo on approval (carrying the track id), or (b) dispatch manually with the track\n# id, or (c) add a `schedule:` and extend the job to scan open control-repo PRs for\n# group labels (the job is idempotent + re-runnable). Re-running `/cadre-land`\n# also re-checks status from your machine.\non:\n  pull_request_review:\n    types: [submitted]\n  check_suite:\n    types: [completed]\n  # schedule:\n  #   - cron: \"*/5 * * * *\"   # uncomment for auto-fire on product-repo approvals\n  workflow_dispatch:\n    inputs:\n      track:\n        description: \"Track id to attempt to land (e.g. auth_20260609)\"\n        required: true\n\n# Per-track concurrency: serialize landings WITHIN a track, but let different\n# tracks land in parallel — otherwise one stalled/conflicted track blocks every\n# other (see /cadre-land §7). The track id is known here only for workflow_dispatch;\n# for review/check events we key on the triggering control PR (one control PR per\n# track, so per-PR == per-track), falling back to run_id so we never collapse\n# unrelated events onto one global lane.\nconcurrency:\n  group: cadre-merge-train-${{ github.event.inputs.track || github.event.pull_request.number || github.event.check_suite.pull_requests[0].number || github.run_id }}\n  cancel-in-progress: false\n\njobs:\n  land:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout control repo\n        uses: actions/checkout@v4\n        with:\n          token: ${{ secrets.CADRE_TRAIN_TOKEN }}\n          submodules: false\n          fetch-depth: 0\n\n      - name: Run merge train\n        env:\n          GH_TOKEN: ${{ secrets.CADRE_TRAIN_TOKEN }}\n          EVENT_TRACK: ${{ github.event.inputs.track }}\n        run: |\n          set -euo pipefail\n\n          # --- Resolve the track id and the group label -------------------------\n          PREFIX=\"$(jq -r '.merge_train.group_label_prefix // \"cadre-track\"' cadre/config.json)\"\n          AUTO_FIRE=\"$(jq -r '.merge_train.auto_fire // true' cadre/config.json)\"\n          CONTROL_REPO=\"${GITHUB_REPOSITORY}\"\n\n          # Derive the candidate track from the triggering PR's labels, or from\n          # the manual dispatch input.\n          TRACK=\"${EVENT_TRACK:-}\"\n          if [ -z \"$TRACK\" ]; then\n            PR_NUMBER=\"$(jq -r '.pull_request.number // .check_suite.pull_requests[0].number // empty' \"$GITHUB_EVENT_PATH\")\"\n            if [ -n \"$PR_NUMBER\" ]; then\n              TRACK=\"$(gh pr view \"$PR_NUMBER\" --repo \"$CONTROL_REPO\" --json labels \\\n                       --jq \".labels[].name | select(startswith(\\\"${PREFIX}:\\\")) | sub(\\\"${PREFIX}:\\\"; \\\"\\\")\" || true)\"\n            fi\n          fi\n          if [ -z \"$TRACK\" ]; then echo \"No track group on this event; nothing to do.\"; exit 0; fi\n          LABEL=\"${PREFIX}:${TRACK}\"\n          echo \"Track=$TRACK  Label=$LABEL  auto_fire=$AUTO_FIRE\"\n          if [ \"$AUTO_FIRE\" != \"true\" ] && [ -z \"$EVENT_TRACK\" ]; then\n            echo \"auto_fire disabled; only workflow_dispatch may land. Exiting.\"; exit 0\n          fi\n\n          # --- Helper: is a PR approved AND all required checks green? -----------\n          pr_ready() { # repo  pr_number\n            local repo=\"$1\" num=\"$2\"\n            local decision state\n            decision=\"$(gh pr view \"$num\" --repo \"$repo\" --json reviewDecision --jq '.reviewDecision')\"\n            state=\"$(gh pr view \"$num\" --repo \"$repo\" --json statusCheckRollup \\\n                     --jq '[.statusCheckRollup[]?.conclusion // .statusCheckRollup[]?.state] | map(select(. != null and . != \"SUCCESS\" and . != \"NEUTRAL\" and . != \"SKIPPED\")) | length')\"\n            [ \"$decision\" = \"APPROVED\" ] && [ \"$state\" = \"0\" ]\n          }\n\n          # --- Gather the group from metadata.json ------------------------------\n          META=\"cadre/tracks/${TRACK}/metadata.json\"\n          if [ ! -f \"$META\" ]; then echo \"No metadata for $TRACK; abort.\"; exit 1; fi\n\n          # Ordered repo list: metadata.merge_order entries first (in their given\n          # order), then the alphabetical remainder. Never drop a repo, never\n          # duplicate one — a name listed in merge_order but absent from .repos is\n          # ignored; a repo absent from merge_order falls into the alpha tail.\n          # De-dupe $head preserving first-seen order — a user typo such as\n          # \"repo-order: api > api > web\" would otherwise process api twice.\n          mapfile -t REPO_NAMES < <(jq -r '\n            (.merge_order // []) as $order\n            | (.repos | keys) as $all\n            | ($order\n               | map(select(. as $n | $all | index($n)))\n               | reduce .[] as $n ([]; if (index($n)) then . else . + [$n] end)) as $head\n            | ($all | map(select(. as $n | $head | index($n) | not)) | sort) as $tail\n            | ($head + $tail)[]\n          ' \"$META\")\n          CONTROL_PR=\"$(jq -r '.control_pr_url // empty' \"$META\" | sed -E 's#.*/([0-9]+)/?$#\\1#')\"\n\n          # --- Phase 1: verify EVERY sibling PR is approved + green -------------\n          # Also classify each product PR as landed (already MERGED) vs blocking\n          # (not ready), so a HALF-LANDED train can surface what is still holding\n          # it up on the control PR instead of failing silently.\n          echo \"::group::Pre-flight verification\"\n          LANDED=\"\"; BLOCKING=\"\"; HALT=0\n          for name in \"${REPO_NAMES[@]}\"; do\n            repo=\"$(jq -r --arg n \"$name\" '.repos[$n].repo_slug // empty' \"$META\")\"\n            pr=\"$(jq -r --arg n \"$name\" '.repos[$n].pr_url // empty' \"$META\" | sed -E 's#.*/([0-9]+)/?$#\\1#')\"\n            [ -z \"$pr\" ] && continue\n            echo \"check $repo#$pr\"\n            state=\"$(gh pr view \"$pr\" --repo \"$repo\" --json state --jq '.state')\"\n            if [ \"$state\" = \"MERGED\" ]; then\n              LANDED=\"${LANDED:+$LANDED, }${name} (${repo}#${pr})\"\n              continue\n            fi\n            if ! pr_ready \"$repo\" \"$pr\"; then\n              BLOCKING=\"${BLOCKING:+$BLOCKING, }${name} (${repo}#${pr})\"\n              HALT=1\n            fi\n          done\n          # If the train is half-landed (>=1 product PR merged) and something is\n          # still blocking, post/refresh a status comment on the control PR.\n          if [ \"$HALT\" = \"1\" ]; then\n            if [ -n \"$LANDED\" ] && [ -n \"$CONTROL_PR\" ]; then\n              MARKER=\"<!-- cadre-merge-train:status -->\"\n              BODY=\"${MARKER}\n          🚧 **cadre merge-train — half landed.**\n          Already merged: ${LANDED:-(none)}\n          Still blocking: ${BLOCKING:-(none)}\n\n          The merged product PRs cannot be reverted automatically; the train will\n          re-fire and complete once the blocking repos are approved + green.\"\n              # Refresh the existing status comment if present, else create one.\n              existing=\"$(gh pr view \"$CONTROL_PR\" --repo \"$CONTROL_REPO\" --json comments \\\n                          --jq \"[.comments[] | select(.body | startswith(\\\"${MARKER}\\\"))][0].id // empty\" || true)\"\n              if [ -n \"$existing\" ]; then\n                gh api -X PATCH \"repos/${CONTROL_REPO}/issues/comments/${existing}\" -f body=\"$BODY\" >/dev/null || true\n              else\n                gh pr comment \"$CONTROL_PR\" --repo \"$CONTROL_REPO\" --body \"$BODY\" || true\n              fi\n            fi\n            echo \"Product PR(s) not ready (blocking: ${BLOCKING:-?}); halting.\"\n            echo \"::endgroup::\"\n            exit 0\n          fi\n          if [ -n \"$CONTROL_PR\" ]; then\n            pr_ready \"$CONTROL_REPO\" \"$CONTROL_PR\" || { echo \"Control PR not ready; halting.\"; exit 0; }\n          fi\n          echo \"::endgroup::\"\n\n          # --- Phase 2: merge each PRODUCT PR, capture new default-branch SHA ---\n          git config user.name  \"cadre-merge-train\"\n          git config user.email \"cadre-merge-train@users.noreply.github.com\"\n          BRANCH=\"track/${TRACK}\"\n          git fetch origin \"$BRANCH\" && git checkout \"$BRANCH\"\n\n          for name in \"${REPO_NAMES[@]}\"; do\n            repo=\"$(jq -r --arg n \"$name\" '.repos[$n].repo_slug // empty' \"$META\")\"\n            pr=\"$(jq -r --arg n \"$name\" '.repos[$n].pr_url // empty' \"$META\" | sed -E 's#.*/([0-9]+)/?$#\\1#')\"\n            sub=\"$(jq -r --arg n \"$name\" '.repos[$n].submodule_path // empty' \"$META\")\"\n            [ -z \"$pr\" ] && continue\n            echo \"::group::Merge product $repo#$pr\"\n            # Idempotent re-fire: if this PR is ALREADY merged (e.g. an earlier\n            # train run merged it but then halted on a sibling), do NOT re-merge —\n            # but STILL reconstruct its gitlink bump below so a re-fire converges.\n            pr_state=\"$(gh pr view \"$pr\" --repo \"$repo\" --json state --jq '.state')\"\n            if [ \"$pr_state\" = \"MERGED\" ]; then\n              echo \"  $repo#$pr already MERGED; skipping merge, reconstructing gitlink bump.\"\n            else\n              pr_ready \"$repo\" \"$pr\" || { echo \"REGRESSED before merge: $repo#$pr. Halting.\"; exit 1; }\n              # Merge with a MERGE COMMIT (squash disabled by guardrail) so the\n              # gitlink can pin to a single deterministic merge SHA.\n              gh pr merge \"$pr\" --repo \"$repo\" --merge --delete-branch=false\n            fi\n            # Pin the gitlink to the MERGE COMMIT (not the branch head): the base\n            # branch may have advanced past our merge by the time we read it, so\n            # pinning the head would silently capture unrelated commits.\n            base=\"$(gh pr view \"$pr\" --repo \"$repo\" --json baseRefName --jq '.baseRefName')\"\n            new_sha=\"$(gh pr view \"$pr\" --repo \"$repo\" --json mergeCommit --jq '.mergeCommit.oid')\"\n            if [ -z \"$new_sha\" ] || [ \"$new_sha\" = \"null\" ]; then\n              # Merge commit (squash disabled by guardrail) not yet readable; do\n              # NOT bump to a wrong/empty SHA — halt and let the train re-fire.\n              echo \"  ${name}: merge commit not yet available; halting to re-fire.\"\n              echo \"::endgroup::\"\n              exit 1\n            fi\n            echo \"  ${name} -> ${new_sha} (merge commit)\"\n            # Bump the submodule gitlink on the control track branch.\n            # Fetch the base branch ref (fetch-by-bare-SHA is rejected by many hosts),\n            # which now contains the just-merged commit, then pin the gitlink to it.\n            git -C \"$sub\" fetch origin \"$base\" || git submodule update --init \"$sub\"\n            git -C \"$sub\" checkout \"$new_sha\"\n            git add \"$sub\"\n            echo \"::endgroup::\"\n          done\n\n          # --- Phase 3: commit submodule bumps, push control track branch ------\n          if ! git diff --cached --quiet; then\n            git commit -m \"cadre(train): bump submodules to merged SHAs for ${TRACK}\"\n            git push origin \"$BRANCH\"\n          fi\n\n          # --- Phase 4: re-await control PR check on bumped commit, merge LAST -\n          if [ -n \"$CONTROL_PR\" ]; then\n            echo \"Waiting for control PR #$CONTROL_PR checks on bumped commit...\"\n            for _ in $(seq 1 30); do\n              if pr_ready \"$CONTROL_REPO\" \"$CONTROL_PR\"; then\n                # Merge commit (squash disabled by guardrail) for the control seal PR too.\n                gh pr merge \"$CONTROL_PR\" --repo \"$CONTROL_REPO\" --merge --delete-branch\n                echo \"Merge train complete for ${TRACK}.\"\n                exit 0\n              fi\n              sleep 20\n            done\n            gh pr comment \"$CONTROL_PR\" --repo \"$CONTROL_REPO\" \\\n              --body \"⚠️ cadre merge-train: product PRs merged and submodules bumped, but the control PR's checks did not go green in time. Re-run when CI passes.\"\n            echo \"Control PR not green after bump; product PRs already merged. Halting (re-fires later).\"\n            exit 1\n          fi\n","ci/cadre-merge-train.gitlab.yml":"# Cadre cross-repo merge train (GitLab CI).\n#\n# Scaffolded into the CONTROL repo by /cadre-setup when provider_mode == \"gitlab\"\n# and merge_train.enabled. Either commit this as `.gitlab-ci.yml` or `include:` it\n# from an existing pipeline.\n#\n# What it does: when every Merge Request in a track group (label\n# `cadre-track:<id>`, across the control project + product submodule projects)\n# is APPROVED and pipeline-GREEN, it merges them in a fixed order — PRODUCT\n# projects first, then bumps each submodule gitlink to the merged SHA on the\n# control track branch, then merges the CONTROL MR LAST. No true cross-project\n# atomic merge exists, so on any failure it HALTS, leaves already-merged product\n# MRs in place, comments the failure, and re-fires when the failing project is\n# green again.\n#\n# PREREQUISITES:\n#   - CADRE_TRAIN_TOKEN: a GROUP access token (or PAT) with `api` + `write_repository`\n#     scope and Developer+ on EVERY product project, set as a masked CI/CD variable.\n#   - `glab` available in the runner image, or use the REST API via curl (token in\n#     CADRE_TRAIN_TOKEN). The job below uses `glab`.\n#   - Branch protection / approval rules on each product project's default branch\n#     and the control branch. GitLab's own \"Merge when pipeline succeeds\" and\n#     merge trains are complementary; this job is the cross-project coordinator.\n\nstages:\n  - merge-train\n\ncadre-merge-train:\n  stage: merge-train\n  image: registry.gitlab.com/gitlab-org/cli:latest\n  # Serialize trains PER TRACK so two pipelines never merge/bump submodules\n  # concurrently for the same track, while different tracks still land in parallel\n  # (a single global lane would let one stalled track block every other — see\n  # /cadre-land §7). CADRE_TRACK is the required track id for this run.\n  resource_group: cadre-merge-train-$CADRE_TRACK\n  rules:\n    # GitLab has no cross-project \"sibling MR approved\" webhook, so this job is\n    # driven by: a recurring **pipeline schedule** (set CADRE_TRACK on the\n    # schedule, or leave it unset to scan — see note below), a **manual run**\n    # (CI → Run pipeline → web), or an external **trigger** (e.g. a webhook from a\n    # product project on approval). This is the \"re-fires when green\" mechanism on\n    # GitLab — a schedule every few minutes is the simplest setup.\n    - if: '$CI_PIPELINE_SOURCE == \"schedule\"'\n    - if: '$CI_PIPELINE_SOURCE == \"web\"'\n    - if: '$CI_PIPELINE_SOURCE == \"trigger\"'\n  variables:\n    GITLAB_TOKEN: $CADRE_TRAIN_TOKEN\n  script:\n    - apk add --no-cache jq git bash >/dev/null 2>&1 || true\n    - |\n      set -euo pipefail\n      export GITLAB_HOST=\"${CI_SERVER_HOST}\"\n      PREFIX=\"$(jq -r '.merge_train.group_label_prefix // \"cadre-track\"' cadre/config.json)\"\n      AUTO_FIRE=\"$(jq -r '.merge_train.auto_fire // true' cadre/config.json)\"\n      TRACK=\"${CADRE_TRACK:-}\"\n      if [ -z \"$TRACK\" ]; then echo \"Set CADRE_TRACK (CI/CD var or trigger) to the track id.\"; exit 0; fi\n      # Honor auto_fire: on a scheduled run, only proceed when auto_fire is on.\n      # Manual (web) / trigger runs always proceed (explicit human/system intent).\n      if [ \"$AUTO_FIRE\" != \"true\" ] && [ \"$CI_PIPELINE_SOURCE\" = \"schedule\" ]; then\n        echo \"auto_fire disabled; scheduled runs are skipped. Trigger manually to land.\"; exit 0\n      fi\n      LABEL=\"${PREFIX}:${TRACK}\"\n      META=\"cadre/tracks/${TRACK}/metadata.json\"\n      if [ ! -f \"$META\" ]; then echo \"No metadata for $TRACK; abort.\"; exit 1; fi\n      echo \"Track=$TRACK Label=$LABEL auto_fire=$AUTO_FIRE\"\n\n      # mr_ready PROJECT IID -> approved AND pipeline success\n      mr_ready() {\n        local proj=\"$1\" iid=\"$2\" approved pipe\n        approved=\"$(glab api \"projects/$(printf '%s' \"$proj\" | sed 's#/#%2F#g')/merge_requests/${iid}/approvals\" --jq '.approved // false')\"\n        pipe=\"$(glab api \"projects/$(printf '%s' \"$proj\" | sed 's#/#%2F#g')/merge_requests/${iid}\" --jq '.pipeline.status // .head_pipeline.status // \"unknown\"')\"\n        [ \"$approved\" = \"true\" ] && [ \"$pipe\" = \"success\" ]\n      }\n\n      # Ordered repo list: metadata.merge_order entries first (in their given\n      # order), then the alphabetical remainder. Never drop or duplicate a repo;\n      # a name in merge_order but absent from .repos is ignored, a repo absent\n      # from merge_order falls into the alpha tail.\n      # De-dupe $head preserving first-seen order — a user typo such as\n      # \"repo-order: api > api > web\" would otherwise process api twice.\n      mapfile -t REPO_NAMES < <(jq -r '\n        (.merge_order // []) as $order\n        | (.repos | keys) as $all\n        | ($order\n           | map(select(. as $n | $all | index($n)))\n           | reduce .[] as $n ([]; if (index($n)) then . else . + [$n] end)) as $head\n        | ($all | map(select(. as $n | $head | index($n) | not)) | sort) as $tail\n        | ($head + $tail)[]\n      ' \"$META\")\n      CONTROL_PROJ=\"${CI_PROJECT_PATH}\"\n      CONTROL_MR=\"$(jq -r '.control_pr_url // empty' \"$META\" | sed -E 's#.*/([0-9]+)/?$#\\1#')\"\n\n      # Phase 1: verify every sibling MR approved + green. Also classify each\n      # product MR as landed (already merged) vs blocking (not ready), so a\n      # HALF-LANDED train can surface what is still holding it up on the control\n      # MR instead of failing silently.\n      LANDED=\"\"; BLOCKING=\"\"; HALT=0\n      for name in \"${REPO_NAMES[@]}\"; do\n        proj=\"$(jq -r --arg n \"$name\" '.repos[$n].repo_slug // empty' \"$META\")\"\n        iid=\"$(jq -r --arg n \"$name\" '.repos[$n].pr_url // empty' \"$META\" | sed -E 's#.*/([0-9]+)/?$#\\1#')\"\n        [ -z \"$iid\" ] && continue\n        enc=\"$(printf '%s' \"$proj\" | sed 's#/#%2F#g')\"\n        state=\"$(glab api \"projects/${enc}/merge_requests/${iid}\" --jq '.state')\"\n        if [ \"$state\" = \"merged\" ]; then\n          LANDED=\"${LANDED:+$LANDED, }${name} (${proj}!${iid})\"\n          continue\n        fi\n        if ! mr_ready \"$proj\" \"$iid\"; then\n          BLOCKING=\"${BLOCKING:+$BLOCKING, }${name} (${proj}!${iid})\"\n          HALT=1\n        fi\n      done\n      # If the train is half-landed (>=1 product MR merged) and something is still\n      # blocking, post/refresh a status note on the control MR.\n      if [ \"$HALT\" = \"1\" ]; then\n        if [ -n \"$LANDED\" ] && [ -n \"$CONTROL_MR\" ]; then\n          cenc=\"$(printf '%s' \"$CONTROL_PROJ\" | sed 's#/#%2F#g')\"\n          MARKER=\"<!-- cadre-merge-train:status -->\"\n          BODY=\"${MARKER}\n      🚧 **cadre merge-train — half landed.**\n      Already merged: ${LANDED:-(none)}\n      Still blocking: ${BLOCKING:-(none)}\n\n      The merged product MRs cannot be reverted automatically; the train will\n      re-fire and complete once the blocking projects are approved + green.\"\n          # Refresh the existing status note if present, else create one.\n          existing=\"$(glab api \"projects/${cenc}/merge_requests/${CONTROL_MR}/notes\" --jq \"[.[] | select(.body | startswith(\\\"${MARKER}\\\"))][0].id // empty\" 2>/dev/null || true)\"\n          if [ -n \"$existing\" ]; then\n            glab api --method PUT \"projects/${cenc}/merge_requests/${CONTROL_MR}/notes/${existing}\" -f body=\"$BODY\" >/dev/null || true\n          else\n            glab mr note \"$CONTROL_MR\" -m \"$BODY\" || true\n          fi\n        fi\n        echo \"Product MR(s) not ready (blocking: ${BLOCKING:-?}); halting.\"; exit 0\n      fi\n      if [ -n \"$CONTROL_MR\" ]; then mr_ready \"$CONTROL_PROJ\" \"$CONTROL_MR\" || { echo \"Control MR not ready; halting.\"; exit 0; }; fi\n\n      git config user.name \"cadre-merge-train\"\n      git config user.email \"cadre-merge-train@users.noreply.gitlab.com\"\n      BRANCH=\"track/${TRACK}\"\n      git fetch origin \"$BRANCH\" && git checkout \"$BRANCH\"\n\n      # Phase 2: merge product MRs, bump submodules\n      for name in \"${REPO_NAMES[@]}\"; do\n        proj=\"$(jq -r --arg n \"$name\" '.repos[$n].repo_slug // empty' \"$META\")\"\n        iid=\"$(jq -r --arg n \"$name\" '.repos[$n].pr_url // empty' \"$META\" | sed -E 's#.*/([0-9]+)/?$#\\1#')\"\n        sub=\"$(jq -r --arg n \"$name\" '.repos[$n].submodule_path // empty' \"$META\")\"\n        [ -z \"$iid\" ] && continue\n        enc=\"$(printf '%s' \"$proj\" | sed 's#/#%2F#g')\"\n        target=\"$(glab api \"projects/${enc}/merge_requests/${iid}\" --jq '.target_branch')\"\n        # Idempotent re-fire: if this MR is ALREADY merged (e.g. an earlier train\n        # run merged it but then halted on a sibling), do NOT re-merge — but STILL\n        # reconstruct its gitlink bump below so a re-fire converges.\n        mr_state=\"$(glab api \"projects/${enc}/merge_requests/${iid}\" --jq '.state')\"\n        if [ \"$mr_state\" = \"merged\" ]; then\n          echo \"  $proj!$iid already merged; skipping merge, reconstructing gitlink bump.\"\n          new_sha=\"$(glab api \"projects/${enc}/merge_requests/${iid}\" --jq '.merge_commit_sha // .sha // empty')\"\n        else\n          mr_ready \"$proj\" \"$iid\" || { echo \"REGRESSED: $proj!$iid. Halting.\"; exit 1; }\n          # Merge with a MERGE COMMIT (squash disabled by guardrail — do NOT set\n          # squash=true) so the gitlink can pin to a single deterministic merge\n          # SHA. Read it from the merge PUT response (.merge_commit_sha, falling\n          # back to .sha) — not the branch head, which may have advanced past our\n          # merge and would silently capture unrelated commits.\n          merge_resp=\"$(glab api --method PUT \"projects/${enc}/merge_requests/${iid}/merge\")\"\n          new_sha=\"$(printf '%s' \"$merge_resp\" | jq -r '.merge_commit_sha // .sha // empty')\"\n        fi\n        if [ -z \"$new_sha\" ] || [ \"$new_sha\" = \"null\" ]; then\n          # Merge commit (squash disabled by guardrail) not yet readable; do NOT\n          # bump to a wrong/empty SHA — halt and let the train re-fire.\n          echo \"  ${name}: merge commit not yet available; halting to re-fire.\"; exit 1\n        fi\n        echo \"  ${name} -> ${new_sha} (merge commit)\"\n        # Fetch the target branch ref (fetch-by-bare-SHA is rejected by many hosts),\n        # which now contains the just-merged commit, then pin the gitlink to it.\n        git -C \"$sub\" fetch origin \"$target\" || git submodule update --init \"$sub\"\n        git -C \"$sub\" checkout \"$new_sha\"\n        git add \"$sub\"\n      done\n\n      # Phase 3: commit + push submodule bumps\n      if ! git diff --cached --quiet; then\n        git commit -m \"cadre(train): bump submodules to merged SHAs for ${TRACK}\"\n        git push \"https://oauth2:${CADRE_TRAIN_TOKEN}@${CI_SERVER_HOST}/${CI_PROJECT_PATH}.git\" \"HEAD:${BRANCH}\"\n      fi\n\n      # Phase 4: merge control MR LAST\n      if [ -n \"$CONTROL_MR\" ]; then\n        enc=\"$(printf '%s' \"$CONTROL_PROJ\" | sed 's#/#%2F#g')\"\n        for _ in $(seq 1 30); do\n          if mr_ready \"$CONTROL_PROJ\" \"$CONTROL_MR\"; then\n            # Merge commit (squash disabled by guardrail — do NOT set squash=true)\n            # for the control seal MR too.\n            glab api --method PUT \"projects/${enc}/merge_requests/${CONTROL_MR}/merge\" >/dev/null\n            echo \"Merge train complete for ${TRACK}.\"; exit 0\n          fi\n          sleep 20\n        done\n        glab mr note \"$CONTROL_MR\" -m \"⚠️ cadre merge-train: product MRs merged and submodules bumped, but the control MR pipeline did not go green in time. Re-run when CI passes.\" || true\n        echo \"Control MR not green after bump; product MRs already merged. Halting (re-fires later).\"; exit 1\n      fi\n","ci/cadre-monorepo-check.github.yml":"# Cadre target-project drift gate (GitHub Actions).\n#\n# Scaffolded by cadre-setup into .github/workflows/cadre-monorepo-check.yml.\n# This template is for Cadre-initialized product repositories, not the Cadre\n# harness package. It intentionally avoids harness-package drift gates,\n# generated plugin checks, and generator maintenance commands.\n\nname: cadre-monorepo-check\n\non:\n  pull_request:\n\nconcurrency:\n  group: cadre-monorepo-check-${{ github.ref }}\n  cancel-in-progress: true\n\njobs:\n  cadre-project-check:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout repo\n        uses: actions/checkout@v4\n\n      - name: tracks.json index is fresh\n        run: |\n          set -euo pipefail\n          if [ ! -f cadre/tracks.json ]; then\n            echo \"No cadre/tracks.json — skipping freshness gate.\"\n            exit 0\n          fi\n          entries=\"$(mktemp)\"\n          expected=\"$(mktemp)\"\n          actual=\"$(mktemp)\"\n          : > \"$entries\"\n          if [ -d cadre/tracks ]; then\n            find cadre/tracks -mindepth 2 -maxdepth 2 -name metadata.json -print | sort | while read -r md; do\n              dir=\"${md%metadata.json}\"\n              jq \\\n                --arg metadata_path \"$md\" \\\n                --arg spec_path \"${dir}spec.json\" \\\n                --arg plan_path \"${dir}plan.json\" \\\n                '{track_id, name:(.name // .description // .track_id), status:(.status // \"new\"), priority:(.priority // \"medium\"), owner:(.owner // null), reviewer:(.reviewer // null), tags:(.tags // []), review:(.review // null), metadata_path:$metadata_path, spec_path:$spec_path, plan_path:$plan_path}' \\\n                \"$md\" >> \"$entries\"\n            done\n          fi\n          jq -S 'del(.generated_at)' cadre/tracks.json > \"$actual\"\n          jq -S '{\n            version: 1,\n            schema: \"cadre.tracks_index.v1\",\n            counts: (reduce .[] as $t ({new:0,in_progress:0,completed:0,blocked:0,skipped:0}; .[$t.status] = ((.[$t.status] // 0) + 1))),\n            tracks: (sort_by(.track_id))\n          }' \"$entries\" > \"$expected\"\n          if ! diff -u \"$expected\" \"$actual\"; then\n            echo \"::error::cadre/tracks.json is stale. Run cadre_mutate action regen_index through Cadre MCP and commit the result.\"\n            exit 1\n          fi\n          echo \"cadre/tracks.json matches track metadata.\"\n\n      - name: Shared sync merge contract present\n        run: |\n          set -euo pipefail\n          mode=\"$(jq -r '.sync_mode // \"local\"' cadre/config.json 2>/dev/null || echo local)\"\n          if [ \"$mode\" != \"shared\" ]; then\n            echo \"sync_mode is '$mode' — skipping .gitattributes guard.\"\n            exit 0\n          fi\n          missing=0\n          for line in \\\n            'cadre/tracks/**/parallel_state.json  merge=ours'; do\n            pat=\"$(printf '%s' \"$line\" | sed 's/  */[[:space:]][[:space:]]*/g')\"\n            if ! grep -Eq \"$pat\" .gitattributes 2>/dev/null; then\n              echo \"::error::Missing required shared-mode .gitattributes line: $line\"\n              missing=1\n            fi\n          done\n          [ \"$missing\" -eq 0 ] || exit 1\n          echo \".gitattributes contains the required shared-mode merge rules.\"\n\n      - name: Configured project gate\n        run: |\n          set -euo pipefail\n          if [ ! -f cadre/config.json ]; then\n            echo \"No cadre/config.json — skipping project gate.\"\n            exit 0\n          fi\n          gate=\"$(jq -r '.ci_check_command // .check_command // .review_machine_gate_command // .machine_gate_command // .test_command // empty' cadre/config.json)\"\n          if [ -z \"$gate\" ]; then\n            echo \"No Cadre project gate command configured.\"\n            exit 0\n          fi\n          echo \"Running Cadre project gate: $gate\"\n          bash -lc \"$gate\"\n\n      - name: Review verdict cleared for track PR\n        env:\n          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}\n          HEAD_REF: ${{ github.event.pull_request.head.ref }}\n        run: |\n          set -euo pipefail\n          if [ ! -d cadre ]; then\n            echo \"No cadre/ directory — review gate not initialized.\"\n            exit 0\n          fi\n          case \"$HEAD_REF\" in\n            track/*) track_id=\"${HEAD_REF#track/}\" ;;\n            *)\n              echo \"Head branch '$HEAD_REF' is not a track branch — review gate not applicable.\"\n              exit 0 ;;\n          esac\n          META=\"cadre/tracks/$track_id/metadata.json\"\n          if [ ! -f \"$META\" ]; then\n            echo \"::error::Track branch '$HEAD_REF' has no $META — cannot verify review.\"\n            exit 1\n          fi\n          verdict=\"$(jq -r '.review.verdict // \"absent\"' \"$META\")\"\n          blocking=\"$(jq -r '.review.blocking_count // 0' \"$META\")\"\n          self_reviewed=\"$(jq -r '.review.self_reviewed // false' \"$META\")\"\n          reviewed_sha=\"$(jq -r '.review.reviewed_sha // \"\"' \"$META\")\"\n          require_second=\"$(jq -r '.require_second_reviewer // false' cadre/config.json 2>/dev/null || echo false)\"\n          if [ \"$verdict\" = \"absent\" ]; then\n            echo \"::error::Track '$track_id' has no recorded review verdict. Run cadre-review before merging.\"\n            exit 1\n          fi\n          if [ \"$verdict\" != \"approved\" ] || [ \"$blocking\" -gt 0 ]; then\n            echo \"::error::Track '$track_id' has not cleared review (verdict: $verdict, blocking findings: $blocking).\"\n            exit 1\n          fi\n          if [ \"$require_second\" = \"true\" ] && [ \"$self_reviewed\" = \"true\" ]; then\n            echo \"::error::Track '$track_id' has only a self-review but require_second_reviewer is set.\"\n            exit 1\n          fi\n          if [ -z \"$reviewed_sha\" ]; then\n            echo \"::error::Track '$track_id' is missing review.reviewed_sha. Re-run cadre-review.\"\n            exit 1\n          fi\n          if [ \"$reviewed_sha\" != \"$PR_HEAD_SHA\" ]; then\n            echo \"::warning::PR head $PR_HEAD_SHA differs from review.reviewed_sha $reviewed_sha. This can be expected after ship/land rebases; Cadre enforces the hard pre-rebase guard during publication.\"\n          fi\n          echo \"Track '$track_id' review cleared.\"\n","ci/cadre-monorepo-check.gitlab.yml":"# Cadre target-project drift gate (GitLab CI).\n#\n# Scaffolded by cadre-setup into .gitlab-ci.yml. This template is for\n# Cadre-initialized product repositories, not the Cadre harness package. It\n# intentionally avoids harness-package drift gates, generated plugin checks,\n# and generator maintenance commands.\n\nstages:\n  - check\n\ncadre-monorepo-check:\n  stage: check\n  image: alpine:latest\n  rules:\n    - if: '$CI_PIPELINE_SOURCE == \"merge_request_event\"'\n  script:\n    - apk add --no-cache bash jq git >/dev/null 2>&1 || true\n    - |\n      set -eu\n      if [ ! -f cadre/tracks.json ]; then\n        echo \"No cadre/tracks.json — skipping freshness gate.\"\n      else\n        entries=\"$(mktemp)\"\n        expected=\"$(mktemp)\"\n        actual=\"$(mktemp)\"\n        : > \"$entries\"\n        if [ -d cadre/tracks ]; then\n          find cadre/tracks -mindepth 2 -maxdepth 2 -name metadata.json -print | sort | while read -r md; do\n            dir=\"${md%metadata.json}\"\n            jq \\\n              --arg metadata_path \"$md\" \\\n              --arg spec_path \"${dir}spec.json\" \\\n              --arg plan_path \"${dir}plan.json\" \\\n              '{track_id, name:(.name // .description // .track_id), status:(.status // \"new\"), priority:(.priority // \"medium\"), owner:(.owner // null), reviewer:(.reviewer // null), tags:(.tags // []), review:(.review // null), metadata_path:$metadata_path, spec_path:$spec_path, plan_path:$plan_path}' \\\n              \"$md\" >> \"$entries\"\n          done\n        fi\n        jq -S 'del(.generated_at)' cadre/tracks.json > \"$actual\"\n        jq -S '{\n          version: 1,\n          schema: \"cadre.tracks_index.v1\",\n          counts: (reduce .[] as $t ({new:0,in_progress:0,completed:0,blocked:0,skipped:0}; .[$t.status] = ((.[$t.status] // 0) + 1))),\n          tracks: (sort_by(.track_id))\n        }' \"$entries\" > \"$expected\"\n        if ! diff -u \"$expected\" \"$actual\"; then\n          echo \"ERROR: cadre/tracks.json is stale. Run cadre_mutate action regen_index through Cadre MCP and commit the result.\"\n          exit 1\n        fi\n        echo \"cadre/tracks.json matches track metadata.\"\n      fi\n    - |\n      set -eu\n      mode=\"$(jq -r '.sync_mode // \"local\"' cadre/config.json 2>/dev/null || echo local)\"\n      if [ \"$mode\" != \"shared\" ]; then\n        echo \"sync_mode is '$mode' — skipping .gitattributes guard.\"\n      else\n        missing=0\n        for line in \\\n          'cadre/tracks/**/parallel_state.json  merge=ours'; do\n          pat=\"$(printf '%s' \"$line\" | sed 's/  */[[:space:]][[:space:]]*/g')\"\n          if ! grep -Eq \"$pat\" .gitattributes 2>/dev/null; then\n            echo \"ERROR: Missing required shared-mode .gitattributes line: $line\"\n            missing=1\n          fi\n        done\n        [ \"$missing\" -eq 0 ] || exit 1\n        echo \".gitattributes contains the required shared-mode merge rules.\"\n      fi\n    - |\n      set -eu\n      if [ ! -f cadre/config.json ]; then\n        echo \"No cadre/config.json — skipping project gate.\"\n      else\n        gate=\"$(jq -r '.ci_check_command // .check_command // .review_machine_gate_command // .machine_gate_command // .test_command // empty' cadre/config.json)\"\n        if [ -z \"$gate\" ]; then\n          echo \"No Cadre project gate command configured.\"\n        else\n          echo \"Running Cadre project gate: $gate\"\n          bash -lc \"$gate\"\n        fi\n      fi\n    - |\n      set -eu\n      if [ ! -d cadre ]; then\n        echo \"No cadre/ directory — review gate not initialized.\"\n        exit 0\n      fi\n      HEAD_REF=\"${CI_MERGE_REQUEST_SOURCE_BRANCH_NAME:-}\"\n      MR_HEAD_SHA=\"${CI_MERGE_REQUEST_SOURCE_BRANCH_SHA:-}\"\n      case \"$HEAD_REF\" in\n        track/*) track_id=\"${HEAD_REF#track/}\" ;;\n        *)\n          echo \"Source branch '$HEAD_REF' is not a track branch — review gate not applicable.\"\n          exit 0 ;;\n      esac\n      META=\"cadre/tracks/$track_id/metadata.json\"\n      if [ ! -f \"$META\" ]; then\n        echo \"ERROR: Track branch '$HEAD_REF' has no $META — cannot verify review.\"\n        exit 1\n      fi\n      verdict=\"$(jq -r '.review.verdict // \"absent\"' \"$META\")\"\n      blocking=\"$(jq -r '.review.blocking_count // 0' \"$META\")\"\n      self_reviewed=\"$(jq -r '.review.self_reviewed // false' \"$META\")\"\n      reviewed_sha=\"$(jq -r '.review.reviewed_sha // \"\"' \"$META\")\"\n      require_second=\"$(jq -r '.require_second_reviewer // false' cadre/config.json 2>/dev/null || echo false)\"\n      if [ \"$verdict\" = \"absent\" ]; then\n        echo \"ERROR: Track '$track_id' has no recorded review verdict. Run cadre-review before merging.\"\n        exit 1\n      fi\n      if [ \"$verdict\" != \"approved\" ] || [ \"$blocking\" -gt 0 ]; then\n        echo \"ERROR: Track '$track_id' has not cleared review (verdict: $verdict, blocking findings: $blocking).\"\n        exit 1\n      fi\n      if [ \"$require_second\" = \"true\" ] && [ \"$self_reviewed\" = \"true\" ]; then\n        echo \"ERROR: Track '$track_id' has only a self-review but require_second_reviewer is set.\"\n        exit 1\n      fi\n      if [ -z \"$reviewed_sha\" ]; then\n        echo \"ERROR: Track '$track_id' is missing review.reviewed_sha. Re-run cadre-review.\"\n        exit 1\n      fi\n      if [ -n \"$MR_HEAD_SHA\" ] && [ \"$reviewed_sha\" != \"$MR_HEAD_SHA\" ]; then\n        echo \"WARNING: MR head $MR_HEAD_SHA differs from review.reviewed_sha $reviewed_sha. This can be expected after ship/land rebases; Cadre enforces the hard pre-rebase guard during publication.\"\n      fi\n      echo \"Track '$track_id' review cleared.\"\n","config.json":"{\n  \"sync_mode\": \"local\",\n  \"auto_open\": false,\n  \"require_second_reviewer\": false,\n  \"allow_unreviewed_ship\": false,\n  \"allow_unpinned_review_ship\": false,\n  \"provider_mode\": \"local\",\n  \"provider_mcp_required\": false,\n  \"remote_host\": \"\",\n  \"control_remote\": \"origin\",\n  \"control_branch\": \"main\",\n  \"pull_on_command_start\": true,\n  \"coverage_command\": \"\",\n  \"merge_train\": {\n    \"enabled\": true,\n    \"auto_fire\": true,\n    \"group_label_prefix\": \"cadre-track\"\n  }\n}\n","learnings_seed.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.learnings_seed.v1\",\n  \"id\": \"initial\",\n  \"kind\": \"learnings_seed\",\n  \"text\": \"# Track Learnings: {{track_id}}\\n\\nPatterns, gotchas, and context discovered during implementation.\\n\\n## Codebase Patterns (Inherited)\\n\\n<!-- Patterns summarized from canonical cadre/patterns.jsonl events or archived track learnings -->\\n\\n---\\n\\n<!-- Learnings from implementation will be appended below -->\\n<!-- Format for each entry:\\n\\n## [YYYY-MM-DD HH:MM] - Phase N Task M: <task_name>\\nkey: <track_id>:p<i>:t<j>:<sha7>\\nSession/thread ref if available\\n- **Implemented:** <brief description of what was done>\\n- **Files changed:** <list of files modified/created>\\n- **Commit:** <sha_7chars>\\n- **Learnings:**\\n  - Patterns: <reusable patterns discovered>\\n  - Gotchas: <things to watch out for>\\n  - Context: <useful context for future>\\n---\\n\\n-->\\n\"\n}\n","manifest.json":"{\n  \"templates\": [\n    {\n      \"id\": \"product\",\n      \"source\": \"product.json\",\n      \"target\": \"cadre/product.json\",\n      \"projection\": \"cadre/product.md\",\n      \"purpose\": \"Product summary, users, workflows, domain model, invariants, architecture boundaries, data ownership, and release expectations.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"workflow\",\n      \"source\": \"workflow.json\",\n      \"target\": \"cadre/workflow.json\",\n      \"projection\": \"cadre/workflow.md\",\n      \"purpose\": \"Project development, verification, review, and commit expectations.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"product-guidelines\",\n      \"source\": \"product_guidelines.json\",\n      \"target\": \"cadre/product_guidelines.json\",\n      \"projection\": \"cadre/product_guidelines.md\",\n      \"purpose\": \"Product principles, user promises, non-goals, and decision rules reviewed during setup.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"patterns\",\n      \"source\": \"patterns_seed.json\",\n      \"target\": \"cadre/patterns.jsonl\",\n      \"projection\": \"cadre/patterns.md\",\n      \"purpose\": \"Reusable codebase patterns and completed-track learnings.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"learnings\",\n      \"source\": \"learnings_seed.json\",\n      \"target\": \"cadre/tracks/<track_id>/learnings.jsonl\",\n      \"projection\": \"cadre/tracks/<track_id>/learnings.md\",\n      \"purpose\": \"Per-track learning journal starter.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"config\",\n      \"source\": \"config.json\",\n      \"target\": \"cadre/config.json\",\n      \"purpose\": \"Cadre sync, provider, review, and quality defaults.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"repos\",\n      \"source\": \"repos.json\",\n      \"target\": \"cadre/repos.json\",\n      \"purpose\": \"Polyrepo topology starter.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"style-guides\",\n      \"source\": \"styleguides/*.json\",\n      \"target\": \"cadre/styleguides/*.json\",\n      \"projection\": \"cadre/code_styleguides/*.md\",\n      \"purpose\": \"Language-specific implementation guidance selected by setup packets.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"target-monorepo-ci\",\n      \"source\": \"ci/cadre-monorepo-check.github.yml\",\n      \"target\": \".github/workflows/cadre-monorepo-check.yml\",\n      \"purpose\": \"Cadre target-project drift and review gate for GitHub monorepos.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"target-monorepo-ci-gitlab\",\n      \"source\": \"ci/cadre-monorepo-check.gitlab.yml\",\n      \"target\": \".gitlab-ci.yml\",\n      \"purpose\": \"Cadre target-project drift and review gate for GitLab monorepos.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"polyrepo-merge-train-github\",\n      \"source\": \"ci/cadre-merge-train.github.yml\",\n      \"target\": \".github/workflows/cadre-merge-train.yml\",\n      \"purpose\": \"Cross-repo merge train for GitHub polyrepo control repositories.\",\n      \"scope\": \"target-project\"\n    },\n    {\n      \"id\": \"polyrepo-merge-train-gitlab\",\n      \"source\": \"ci/cadre-merge-train.gitlab.yml\",\n      \"target\": \".gitlab-ci.yml\",\n      \"purpose\": \"Cross-repo merge train for GitLab polyrepo control repositories.\",\n      \"scope\": \"target-project\"\n    }\n  ]\n}\n","patterns_seed.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.patterns_seed.v1\",\n  \"id\": \"initial\",\n  \"kind\": \"patterns_seed\",\n  \"text\": \"# Codebase Patterns\\n\\nReusable patterns discovered during development. **Read this before starting new work.**\\n\\nThis file is the project's institutional knowledge - learnings extracted from completed tracks that help future development.\\n\\n---\\n\\n## Code Conventions\\n\\n<!-- Patterns related to code style, naming, imports -->\\n\\n## Architecture\\n\\n<!-- Patterns related to architecture decisions, module structure -->\\n\\n## Gotchas\\n\\n<!-- Common mistakes to avoid, things that are easy to forget -->\\n\\n## Testing\\n\\n<!-- Patterns for testing approaches -->\\n\\n## Context\\n\\n<!-- Useful context about where things are located -->\\n\\n---\\n\\nLast refreshed: YYYY-MM-DD\\n\"\n}\n","product.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.product.v1\",\n  \"kind\": \"product\",\n  \"title\": \"Product Context\",\n  \"summary\": \"Use this file to keep product intent detailed enough for agents to make\\nimplementation decisions without re-discovering the business model every time.\",\n  \"sections\": [\n    {\n      \"heading\": \"Product Summary\",\n      \"body\": \"- What the product is:\\n- Who it serves:\\n- Primary value proposition:\\n- Deployment or operating model:\"\n    },\n    {\n      \"heading\": \"Users And Personas\",\n      \"body\": \"- Primary users:\\n- Secondary users:\\n- Operators, administrators, or integrators:\\n- Permissions, tenancy, or access boundaries that shape user workflows:\"\n    },\n    {\n      \"heading\": \"Core Workflows\",\n      \"body\": \"- Workflow 1:\\n- Workflow 2:\\n- Workflow 3:\\n- Setup, onboarding, or provisioning flow:\\n- Review, audit, support, or recovery flow:\"\n    },\n    {\n      \"heading\": \"Domain Model\",\n      \"body\": \"- Core entities:\\n- Important relationships:\\n- State machines or lifecycle stages:\\n- Identifiers, keys, scopes, or hierarchy rules:\"\n    },\n    {\n      \"heading\": \"Product Invariants\",\n      \"body\": \"- Behaviors that must remain stable:\\n- Security, privacy, or compliance guarantees:\\n- Determinism, compatibility, or migration guarantees:\\n- Performance or availability expectations:\"\n    },\n    {\n      \"heading\": \"Architecture Boundaries\",\n      \"body\": \"- Public trust boundaries:\\n- Service or module ownership:\\n- API, SDK, CLI, or UI responsibilities:\\n- Generated artifacts or contracts that must stay in sync:\"\n    },\n    {\n      \"heading\": \"Data And Integrations\",\n      \"body\": \"- Primary data stores and ownership:\\n- External services or provider dependencies:\\n- Event, analytics, audit, or observability expectations:\\n- Data retention, privacy, or redaction rules:\"\n    },\n    {\n      \"heading\": \"Quality And Release Expectations\",\n      \"body\": \"- Test surfaces that protect product behavior:\\n- Documentation, API contract, or SDK fixture expectations:\\n- Rollout, migration, compatibility, or rollback requirements:\"\n    },\n    {\n      \"heading\": \"Open Questions\",\n      \"body\": \"- Product assumptions that still need confirmation:\\n- Known risks or decisions to revisit:\"\n    }\n  ]\n}\n","product_guidelines.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.product_guidelines.v1\",\n  \"kind\": \"product_guidelines\",\n  \"title\": \"Product Guidelines\",\n  \"summary\": \"Use this file to capture product rules that should shape implementation choices,\\nreview decisions, and acceptance criteria.\",\n  \"sections\": [\n    {\n      \"heading\": \"Product Principles\",\n      \"body\": \"- Put the primary user workflow first.\\n- Prefer changes that reduce ambiguity in the user's next action.\\n- Keep product behavior explainable from the UI, docs, or API contract.\\n- Preserve explicit product invariants unless a spec intentionally revises them.\"\n    },\n    {\n      \"heading\": \"User Promises\",\n      \"body\": \"- Record promises the product must keep for users, teams, operators, or\\n  integrators.\\n- Note the situations where reliability, privacy, accessibility, or performance\\n  matter more than feature breadth.\"\n    },\n    {\n      \"heading\": \"Trust And Safety Boundaries\",\n      \"body\": \"- Record authentication, authorization, tenancy, privacy, and audit guarantees.\\n- Identify public trust boundaries and internal-only surfaces.\\n- State which data must be redacted, encrypted, scoped, or excluded from logs.\"\n    },\n    {\n      \"heading\": \"Domain And Workflow Rules\",\n      \"body\": \"- List lifecycle, state-machine, ordering, idempotency, and concurrency rules.\\n- Capture deterministic evaluation, attribution, or compatibility requirements.\\n- Note workflows that require preview, rollback, migration, or audit support.\"\n    },\n    {\n      \"heading\": \"Data Ownership\",\n      \"body\": \"- Map major product data to its owning store, service, module, or external\\n  provider.\\n- Record generated artifacts, contracts, fixtures, migrations, or SDK assets that\\n  must be updated alongside behavior changes.\"\n    },\n    {\n      \"heading\": \"Non-Goals\",\n      \"body\": \"- List behaviors, audiences, platforms, or workflows this project intentionally\\n  does not serve yet.\\n- Record tempting shortcuts or alternate architectures that should not be used\\n  without revising product context first.\"\n    },\n    {\n      \"heading\": \"Decision Rules\",\n      \"body\": \"- When product tradeoffs appear, write the rule the team should use to choose.\\n- Link decisions back to product context, usage evidence, support requests, or\\n  explicit user feedback.\\n- Prefer tests and review evidence around product invariants, trust boundaries,\\n  tenancy, authorization, data ownership, and public contracts.\"\n    },\n    {\n      \"heading\": \"Review Checklist\",\n      \"body\": \"- Does the change preserve the product invariants in `cadre/product.json`?\\n- Are user-visible behavior, docs, API contracts, SDK fixtures, generated\\n  artifacts, and migrations updated together when needed?\\n- Are privacy, audit, observability, and rollback implications covered?\\n- Are non-goals and compatibility boundaries still respected?\"\n    }\n  ]\n}\n","repos.json":"{\n  \"mode\": \"polyrepo\",\n  \"control_repo\": { \"name\": \"REPLACE_control_repo_name\", \"path\": \".\" },\n  \"default_repo\": \"REPLACE_default_repo_name\",\n  \"repos\": [\n    {\n      \"name\": \"REPLACE_repo_name\",\n      \"submodule_path\": \"repos/REPLACE_repo_name\",\n      \"url\": \"git@host:org/REPLACE_repo_name.git\",\n      \"default_branch\": \"main\",\n      \"enabled\": true\n    }\n  ]\n}\n","styleguides/android.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"android\",\n  \"title\": \"Android (Kotlin) Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the Android Kotlin Style Guide and Architecture Guidelines for building idiomatic, maintainable Android applications.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting and Tooling\",\n      \"body\": \"- **ktlint:** Enforce via Gradle plugin. All code must pass `./gradlew ktlintCheck` before review.\\n- **detekt:** Use for static analysis beyond formatting. Fail CI on new issues.\\n- **Android Lint:** Run `./gradlew lint` and resolve all errors and relevant warnings before release builds.\\n- Follow all base [Kotlin Coding Conventions](kotlin.md) for formatting, naming, and language features.\"\n    },\n    {\n      \"heading\": \"2. Architecture (MVVM + Clean)\",\n      \"body\": \"- **MVVM:** Use ViewModel + LiveData/StateFlow + View/Fragment as the standard architecture. ViewModels must not hold references to `Context`, `View`, or `Fragment`.\\n- **Single-activity:** Prefer a single-activity app with multiple Fragments or Jetpack Compose destinations over multiple Activities.\\n- **Repository pattern:** Abstract data sources behind a `Repository` interface. ViewModels call repositories; repositories call data sources (API, Room, DataStore).\\n- **Use cases / Interactors:** Extract business logic into `UseCase` classes (one responsibility each) for complex or reused logic. Keep ViewModels thin.\\n- **Unidirectional data flow (UDF):** State flows down (ViewModel → View); events flow up (View → ViewModel). Use `StateFlow`/`SharedFlow` or sealed `UiState` classes.\"\n    },\n    {\n      \"heading\": \"3. ViewModels\",\n      \"body\": \"- **`viewModelScope`:** Always use `viewModelScope` to launch coroutines from a ViewModel. Coroutines are cancelled automatically when the ViewModel is cleared.\\n- **Expose `StateFlow`/`LiveData`, not `MutableStateFlow`/`MutableLiveData`:** Use backing properties: `private val _state = MutableStateFlow(...)` exposed as `val state: StateFlow<...> = _state.asStateFlow()`.\\n- **`UiState` sealed class:** Model screen state with a sealed class: `sealed class UiState { object Loading; data class Success(val data: T); data class Error(val message: String) }`.\\n- **No Android framework imports:** ViewModels must not import `android.*` classes (except `Application` in `AndroidViewModel`). This ensures testability.\\n- **`SavedStateHandle`:** Use `SavedStateHandle` in ViewModels to survive process death, not `onSaveInstanceState` in Fragments.\"\n    },\n    {\n      \"heading\": \"4. Fragments and Activities\",\n      \"body\": \"- **Fragment arguments via `newInstance`:** Always pass arguments via `Bundle` in a static `newInstance()` factory method. Never set properties directly on a Fragment after construction.\\n- **View binding:** Use View Binding (`viewBinding = true`) over `findViewById`. Never use synthetic imports (Kotlin Android Extensions is deprecated).\\n- **Lifecycle awareness:** Collect `Flow` in `lifecycleScope.launch { repeatOnLifecycle(STARTED) { ... } }`. Do not collect in `onCreate` without `repeatOnLifecycle`.\\n- **`onViewCreated`:** Set up views and observers in `onViewCreated`, not `onCreateView`. Release view references in `onDestroyView` (especially with View Binding: set binding to `null`).\\n- **Avoid back-stack manipulation:** Let the NavController manage the back stack. Do not call `fragmentManager.popBackStack()` manually.\"\n    },\n    {\n      \"heading\": \"5. Jetpack Navigation\",\n      \"body\": \"- **Single NavGraph per activity:** Define all destinations and actions in `nav_graph.xml`. Use nested graphs for feature modules.\\n- **Safe Args:** Use the Safe Args Gradle plugin for type-safe navigation arguments. Do not use raw `Bundle` keys for navigation data.\\n- **Deep links:** Declare deep link URIs in the NavGraph, not in `AndroidManifest.xml` `intent-filter` manually.\"\n    },\n    {\n      \"heading\": \"6. Room (Database)\",\n      \"body\": \"- **`@Entity`:** Every table is a `data class` annotated with `@Entity`. Name tables explicitly with `tableName`.\\n- **`@Dao` interfaces:** Use `suspend` functions in DAOs for insert/update/delete. Return `Flow<T>` for queries to observe changes reactively.\\n- **No business logic in DAOs:** DAOs perform only data access. Transformations and filtering go in the repository or use case.\\n- **Database migrations:** Always write an explicit `Migration` for schema changes. Never use `fallbackToDestructiveMigration()` in production.\\n- **`@TypeConverter`:** Register converters for non-primitive types (e.g., `Date`, enums, custom objects).\"\n    },\n    {\n      \"heading\": \"7. Networking (Retrofit + OkHttp)\",\n      \"body\": \"- **Suspend functions in API interfaces:** Use `suspend fun` in Retrofit service interfaces — no `Call<T>`, no RxJava.\\n- **Sealed result types:** Wrap API responses in a `Result<T>` or `NetworkResult<T>` sealed class in the repository. Never expose raw Retrofit exceptions to ViewModels.\\n- **OkHttp interceptors:** Use interceptors for auth headers, logging (`HttpLoggingInterceptor`), and retry logic. Do not add auth logic inside API service interfaces.\\n- **Timeouts:** Always configure connect, read, and write timeouts on `OkHttpClient`. Never use the default (unlimited) timeouts.\"\n    },\n    {\n      \"heading\": \"8. Dependency Injection (Hilt)\",\n      \"body\": \"- **Hilt over manual DI:** Use Hilt (built on Dagger) as the standard DI framework. Avoid `ServiceLocator` patterns.\\n- **`@HiltViewModel`:** Annotate all ViewModels. Inject via `by viewModels()` in Fragments/Activities.\\n- **`@Singleton` scope:** Scope repositories and network clients to `@Singleton`. Scope ViewModels to `@HiltViewModel` (automatic).\\n- **Modules for external dependencies:** Create `@Module` + `@InstallIn` classes to provide third-party types (Retrofit, Room, etc.) that cannot be annotated directly.\"\n    },\n    {\n      \"heading\": \"9. Permissions\",\n      \"body\": \"- **Request at the latest possible moment:** Ask for permissions immediately before the feature that needs them, not at app launch.\\n- **`ActivityResultContracts`:** Use `registerForActivityResult(RequestPermission())` — not `onRequestPermissionsResult`.\\n- **Handle denial gracefully:** Always handle both granted and denied cases. Show rationale with `shouldShowRequestPermissionRationale` before re-requesting.\"\n    },\n    {\n      \"heading\": \"10. Testing\",\n      \"body\": \"- **ViewModel tests:** Test ViewModels with JUnit 4/5 and `kotlinx-coroutines-test`. Use `TestCoroutineDispatcher`/`UnconfinedTestDispatcher` and `Turbine` for `Flow` assertions.\\n- **Robolectric for unit tests:** Use Robolectric for tests needing Android framework context without an emulator.\\n- **Espresso / Compose UI tests:** Write UI tests for critical user journeys. Use `IdlingResource` for async operations in Espresso.\\n- **Fake over mock:** Prefer fake implementations of repositories/data sources over Mockito mocks for ViewModel tests. Fakes are more maintainable and realistic.\"\n    },\n    {\n      \"heading\": \"11. Resources\",\n      \"body\": \"- **Naming conventions:**\\n  - Layouts: `fragment_user_profile.xml`, `item_order_card.xml`, `view_loading_spinner.xml`\\n  - IDs: `@+id/tvUserName` (type prefix) or `@+id/userNameText` (descriptive) — pick one and be consistent.\\n  - Strings: `user_profile_title`, `error_network_unavailable` — use `feature_description` format.\\n  - Drawables: `ic_` for icons, `bg_` for backgrounds, `img_` for images.\\n- **`strings.xml`:** All user-visible strings go in `strings.xml`. No hardcoded strings in layouts or code.\\n- **Dimensions:** Define reusable spacing in `dimens.xml`. Use `8dp` grid system.\\n\\n*Sources: [Android Kotlin Style Guide](https://developer.android.com/kotlin/style-guide) · [Guide to app architecture](https://developer.android.com/topic/architecture) · [Hilt docs](https://developer.android.com/training/dependency-injection/hilt-android)*\"\n    }\n  ],\n  \"languages\": [\n    \"android\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"android-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**ktlint:** Enforce via Gradle plugin. All code must pass `./gradlew ktlintCheck` before review.\"\n    },\n    {\n      \"id\": \"android-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**detekt:** Use for static analysis beyond formatting. Fail CI on new issues.\"\n    },\n    {\n      \"id\": \"android-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Android Lint:** Run `./gradlew lint` and resolve all errors and relevant warnings before release builds.\"\n    },\n    {\n      \"id\": \"android-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Follow all base [Kotlin Coding Conventions](kotlin.md) for formatting, naming, and language features.\"\n    },\n    {\n      \"id\": \"android-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**MVVM:** Use ViewModel + LiveData/StateFlow + View/Fragment as the standard architecture. ViewModels must not hold references to `Context`, `View`, or `Fragment`.\"\n    },\n    {\n      \"id\": \"android-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Single-activity:** Prefer a single-activity app with multiple Fragments or Jetpack Compose destinations over multiple Activities.\"\n    },\n    {\n      \"id\": \"android-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Repository pattern:** Abstract data sources behind a `Repository` interface. ViewModels call repositories; repositories call data sources (API, Room, DataStore).\"\n    },\n    {\n      \"id\": \"android-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Use cases / Interactors:** Extract business logic into `UseCase` classes (one responsibility each) for complex or reused logic. Keep ViewModels thin.\"\n    },\n    {\n      \"id\": \"android-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Unidirectional data flow (UDF):** State flows down (ViewModel → View); events flow up (View → ViewModel). Use `StateFlow`/`SharedFlow` or sealed `UiState` classes.\"\n    },\n    {\n      \"id\": \"android-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`viewModelScope`:** Always use `viewModelScope` to launch coroutines from a ViewModel. Coroutines are cancelled automatically when the ViewModel is cleared.\"\n    },\n    {\n      \"id\": \"android-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Expose `StateFlow`/`LiveData`, not `MutableStateFlow`/`MutableLiveData`:** Use backing properties: `private val _state = MutableStateFlow(...)` exposed as `val state: StateFlow<...> = _state.asStateFlow()`.\"\n    },\n    {\n      \"id\": \"android-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`UiState` sealed class:** Model screen state with a sealed class: `sealed class UiState { object Loading; data class Success(val data: T); data class Error(val message: String) }`.\"\n    },\n    {\n      \"id\": \"android-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**No Android framework imports:** ViewModels must not import `android.*` classes (except `Application` in `AndroidViewModel`). This ensures testability.\"\n    },\n    {\n      \"id\": \"android-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`SavedStateHandle`:** Use `SavedStateHandle` in ViewModels to survive process death, not `onSaveInstanceState` in Fragments.\"\n    },\n    {\n      \"id\": \"android-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Fragment arguments via `newInstance`:** Always pass arguments via `Bundle` in a static `newInstance()` factory method. Never set properties directly on a Fragment after construction.\"\n    },\n    {\n      \"id\": \"android-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**View binding:** Use View Binding (`viewBinding = true`) over `findViewById`. Never use synthetic imports (Kotlin Android Extensions is deprecated).\"\n    },\n    {\n      \"id\": \"android-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Lifecycle awareness:** Collect `Flow` in `lifecycleScope.launch { repeatOnLifecycle(STARTED) { ... } }`. Do not collect in `onCreate` without `repeatOnLifecycle`.\"\n    },\n    {\n      \"id\": \"android-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`onViewCreated`:** Set up views and observers in `onViewCreated`, not `onCreateView`. Release view references in `onDestroyView` (especially with View Binding: set binding to `null`).\"\n    },\n    {\n      \"id\": \"android-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid back-stack manipulation:** Let the NavController manage the back stack. Do not call `fragmentManager.popBackStack()` manually.\"\n    },\n    {\n      \"id\": \"android-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Single NavGraph per activity:** Define all destinations and actions in `nav_graph.xml`. Use nested graphs for feature modules.\"\n    },\n    {\n      \"id\": \"android-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Safe Args:** Use the Safe Args Gradle plugin for type-safe navigation arguments. Do not use raw `Bundle` keys for navigation data.\"\n    },\n    {\n      \"id\": \"android-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Deep links:** Declare deep link URIs in the NavGraph, not in `AndroidManifest.xml` `intent-filter` manually.\"\n    },\n    {\n      \"id\": \"android-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@Entity`:** Every table is a `data class` annotated with `@Entity`. Name tables explicitly with `tableName`.\"\n    },\n    {\n      \"id\": \"android-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@Dao` interfaces:** Use `suspend` functions in DAOs for insert/update/delete. Return `Flow<T>` for queries to observe changes reactively.\"\n    },\n    {\n      \"id\": \"android-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**No business logic in DAOs:** DAOs perform only data access. Transformations and filtering go in the repository or use case.\"\n    },\n    {\n      \"id\": \"android-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Database migrations:** Always write an explicit `Migration` for schema changes. Never use `fallbackToDestructiveMigration()` in production.\"\n    },\n    {\n      \"id\": \"android-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@TypeConverter`:** Register converters for non-primitive types (e.g., `Date`, enums, custom objects).\"\n    },\n    {\n      \"id\": \"android-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Suspend functions in API interfaces:** Use `suspend fun` in Retrofit service interfaces — no `Call<T>`, no RxJava.\"\n    },\n    {\n      \"id\": \"android-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Sealed result types:** Wrap API responses in a `Result<T>` or `NetworkResult<T>` sealed class in the repository. Never expose raw Retrofit exceptions to ViewModels.\"\n    },\n    {\n      \"id\": \"android-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**OkHttp interceptors:** Use interceptors for auth headers, logging (`HttpLoggingInterceptor`), and retry logic. Do not add auth logic inside API service interfaces.\"\n    },\n    {\n      \"id\": \"android-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Timeouts:** Always configure connect, read, and write timeouts on `OkHttpClient`. Never use the default (unlimited) timeouts.\"\n    },\n    {\n      \"id\": \"android-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Hilt over manual DI:** Use Hilt (built on Dagger) as the standard DI framework. Avoid `ServiceLocator` patterns.\"\n    },\n    {\n      \"id\": \"android-rule-33\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@HiltViewModel`:** Annotate all ViewModels. Inject via `by viewModels()` in Fragments/Activities.\"\n    },\n    {\n      \"id\": \"android-rule-34\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@Singleton` scope:** Scope repositories and network clients to `@Singleton`. Scope ViewModels to `@HiltViewModel` (automatic).\"\n    },\n    {\n      \"id\": \"android-rule-35\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Modules for external dependencies:** Create `@Module` + `@InstallIn` classes to provide third-party types (Retrofit, Room, etc.) that cannot be annotated directly.\"\n    },\n    {\n      \"id\": \"android-rule-36\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Request at the latest possible moment:** Ask for permissions immediately before the feature that needs them, not at app launch.\"\n    },\n    {\n      \"id\": \"android-rule-37\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`ActivityResultContracts`:** Use `registerForActivityResult(RequestPermission())` — not `onRequestPermissionsResult`.\"\n    },\n    {\n      \"id\": \"android-rule-38\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Handle denial gracefully:** Always handle both granted and denied cases. Show rationale with `shouldShowRequestPermissionRationale` before re-requesting.\"\n    },\n    {\n      \"id\": \"android-rule-39\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**ViewModel tests:** Test ViewModels with JUnit 4/5 and `kotlinx-coroutines-test`. Use `TestCoroutineDispatcher`/`UnconfinedTestDispatcher` and `Turbine` for `Flow` assertions.\"\n    },\n    {\n      \"id\": \"android-rule-40\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Robolectric for unit tests:** Use Robolectric for tests needing Android framework context without an emulator.\"\n    },\n    {\n      \"id\": \"android-rule-41\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Espresso / Compose UI tests:** Write UI tests for critical user journeys. Use `IdlingResource` for async operations in Espresso.\"\n    },\n    {\n      \"id\": \"android-rule-42\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Fake over mock:** Prefer fake implementations of repositories/data sources over Mockito mocks for ViewModel tests. Fakes are more maintainable and realistic.\"\n    },\n    {\n      \"id\": \"android-rule-43\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Naming conventions:**\"\n    },\n    {\n      \"id\": \"android-rule-44\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Layouts: `fragment_user_profile.xml`, `item_order_card.xml`, `view_loading_spinner.xml`\"\n    },\n    {\n      \"id\": \"android-rule-45\",\n      \"severity\": \"guidance\",\n      \"summary\": \"IDs: `@+id/tvUserName` (type prefix) or `@+id/userNameText` (descriptive) — pick one and be consistent.\"\n    },\n    {\n      \"id\": \"android-rule-46\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Strings: `user_profile_title`, `error_network_unavailable` — use `feature_description` format.\"\n    },\n    {\n      \"id\": \"android-rule-47\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Drawables: `ic_` for icons, `bg_` for backgrounds, `img_` for images.\"\n    },\n    {\n      \"id\": \"android-rule-48\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`strings.xml`:** All user-visible strings go in `strings.xml`. No hardcoded strings in layouts or code.\"\n    },\n    {\n      \"id\": \"android-rule-49\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Dimensions:** Define reusable spacing in `dimens.xml`. Use `8dp` grid system.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/compose-multiplatform.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"compose-multiplatform\",\n  \"title\": \"Compose Multiplatform Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices for building idiomatic, high-performance, and maintainable Compose Multiplatform (CMP) applications across Android, iOS, Desktop, and Web.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting and Tooling\",\n      \"body\": \"- **ktlint & detekt:** Enforce via Gradle plugins. All code must pass linting before merging.\\n- **Compose Rules (detekt-compose):** Use Twitter/Facebook/Custom Compose lint rules to catch common performance pitfalls (e.g., \\\"MutableState in ViewModel should be private\\\").\\n- **Trailing lambdas:** Always use trailing lambda syntax for the content parameter of a Composable.\\n- **Naming:**\\n  - **Composables:** `UpperCamelCase` (e.g., `PrimaryButton`). Names must be nouns.\\n  - **Composable factories:** `UpperCamelCase` if they return `Unit`.\\n  - **Modifier extensions:** `lowerCamelCase` (e.g., `Modifier.customPadding()`).\"\n    },\n    {\n      \"heading\": \"2. Architecture (UDF + Component-based)\",\n      \"body\": \"- **Unidirectional Data Flow (UDF):** State flows down (State); Events flow up (Callbacks).\\n- **State over Events:** Prefer modeling screen state as a single `UiState` data class rather than multiple independent flows.\\n- **ViewModel (Multiplatform):** Use `androidx.lifecycle.ViewModel` (now multiplatform-ready) or a shared `CmpViewModel` to hold logic.\\n- **Component-based UI:** Break UI into small, reusable, and stateless Composables. A Composable over 50 lines is a candidate for decomposition.\\n- **Stateless vs Stateful:**\\n  - **Stateless:** Accepts state as parameters and emits events via callbacks. High reusability.\\n  - **Stateful:** Manages its own state or interacts with a ViewModel. Use sparingly at the screen/feature root.\"\n    },\n    {\n      \"heading\": \"3. Composable Design\",\n      \"body\": \"- **Modifier as first optional parameter:** Every UI Composable should accept a `modifier: Modifier = Modifier` as its first optional parameter.\\n  ```kotlin\\n  @Composable\\n  fun MyComponent(\\n      text: String,\\n      modifier: Modifier = Modifier,\\n      onClicked: () -> Unit\\n  ) { ... }\\n  ```\\n- **Explicit parameter ordering:** Required parameters first, then `modifier`, then optional parameters, then the trailing lambda (if any).\\n- **Stability:** Ensure parameters are stable or immutable. Use `@Stable` or `@Immutable` on domain models if Compose cannot infer it. Avoid passing `List` directly; use `ImmutableList` (from kotlinx-collections-immutable) or a wrapper.\\n- **Slot API:** Use the \\\"Slot\\\" pattern (accepting `@Composable () -> Unit` lambdas) to increase flexibility and avoid deep parameter propogation.\"\n    },\n    {\n      \"heading\": \"4. State Management\",\n      \"body\": \"- **`remember` & `mutableStateOf`:** Use for local, UI-only state. Always use `rememberSaveable` for state that must survive configuration changes (Android).\\n- **`StateFlow` to `State`:** Collect flows using `collectAsStateWithLifecycle()` (requires `lifecycle-runtime-compose` multiplatform) to ensure lifecycle-aware updates.\\n- **State Hoisting:** Lift state up to make Composables stateless. The caller should control the state.\\n- **Don't pass `MutableState`:** Pass the raw value and a callback function, never a `MutableState<T>` object, to child Composables.\\n- **Side Effects:** Use `LaunchedEffect`, `SideEffect`, and `DisposableEffect` correctly. Never perform side effects directly in the Composable's body.\"\n    },\n    {\n      \"heading\": \"5. Multiplatform Patterns (Expect/Actual)\",\n      \"body\": \"- **Platform-specific UI:** Use `expect`/`actual` for platform-native components (e.g., `MapView`, `WebView`).\\n- **CompositionLocal:** Use `CompositionLocal` for cross-cutting concerns like Themes, Localizations, or Platform-specific configurations.\\n- **Platform Check:** Use `LocalWindowInfo.current` or custom platform-specific `CompositionLocal` instead of hardcoded platform checks inside generic UI code.\\n- **Shared Resources:** Use `Compose Multiplatform Resources` (lib) for shared strings, fonts, and images.\"\n    },\n    {\n      \"heading\": \"6. Layout and Modifiers\",\n      \"body\": \"- **Modifier Order Matters:** Modifiers are applied sequentially. `Modifier.padding(8.dp).clickable { }` is different from `Modifier.clickable { }.padding(8.dp)`.\\n- **Prefer `weight` in Rows/Columns:** Use `Modifier.weight()` for flexible layouts rather than hardcoded sizes.\\n- **Avoid `fillMaxSize()` in nested scrolling:** Use `wrapContentHeight()` or specific sizes to avoid infinite height constraints in `LazyColumn`.\\n- **Custom Modifiers:** Extract repeated modifier chains into extension functions for readability.\"\n    },\n    {\n      \"heading\": \"7. Navigation\",\n      \"body\": \"- **Compose Navigation Multiplatform:** Use the official `androidx.navigation:navigation-compose` (now Multiplatform).\\n- **Type-safe Navigation:** Use Kotlin Serialization with the Navigation component for type-safe arguments.\\n- **Single NavHost:** Keep the `NavHost` at the top level of each platform's entry point or in a shared `App()` Composable.\"\n    },\n    {\n      \"heading\": \"8. Performance\",\n      \"body\": \"- **Minimize Recomposition:** \\n  - Pass only necessary data.\\n  - Use `derivedStateOf` for state that depends on other state to avoid redundant recompositions.\\n  - Use `remember` with keys to cache expensive calculations.\\n- **Lazy Collections:** Always use `LazyColumn`, `LazyRow`, and `LazyVerticalGrid` for large or dynamic lists. Use `key` in `items()` for stable identification.\\n- **Avoid heavy computation in `@Composable`:** Move business logic and heavy mapping to the ViewModel or use `remember`.\\n- **Phase isolation:** Use `Modifier.graphicsLayer` or lambda-based modifiers (e.g., `Modifier.offset { ... }`) for frequent updates like animations to skip the \\\"recomposition\\\" and \\\"layout\\\" phases.\"\n    },\n    {\n      \"heading\": \"9. Testing\",\n      \"body\": \"- **Compose UI Test:** Use `createComposeRule()` (Android) and `runComposeUiTest` (Multiplatform) to verify UI behavior.\\n- **Semantics:** Use `Modifier.semantics { contentDescription = \\\"...\\\" }` to make UI testable and accessible.\\n- **Screenshot Testing:** Use libraries like Roborazzi or Paparazzi for cross-platform visual regression testing.\"\n    },\n    {\n      \"heading\": \"10. Desktop & iOS Specifics\",\n      \"body\": \"- **Desktop:** Handle window resizing, keyboard shortcuts, and mouse hover states using `onPointerEvent`.\\n- **iOS:** Ensure `UIKitView` is used correctly for native interop. Respect safe area insets via `WindowInsets.safeDrawing`.\\n- **Web (Wasm/JS):** Be mindful of heavy computations as Wasm/JS performance characteristics differ from JVM/Native.\\n\\n*Sources: [Compose Multiplatform Docs](https://www.jetbrains.com/lp/compose-multiplatform/) · [Jetpack Compose Best Practices](https://developer.android.com/develop/ui/compose/performance) · [Kotlin Multiplatform Guidelines](https://kotlinlang.org/docs/multiplatform.html)*\"\n    }\n  ],\n  \"languages\": [\n    \"compose-multiplatform\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"compose-multiplatform-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**ktlint & detekt:** Enforce via Gradle plugins. All code must pass linting before merging.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Compose Rules (detekt-compose):** Use Twitter/Facebook/Custom Compose lint rules to catch common performance pitfalls (e.g., \\\"MutableState in ViewModel should be private\\\").\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Trailing lambdas:** Always use trailing lambda syntax for the content parameter of a Composable.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Naming:**\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Composables:** `UpperCamelCase` (e.g., `PrimaryButton`). Names must be nouns.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Composable factories:** `UpperCamelCase` if they return `Unit`.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Modifier extensions:** `lowerCamelCase` (e.g., `Modifier.customPadding()`).\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Unidirectional Data Flow (UDF):** State flows down (State); Events flow up (Callbacks).\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**State over Events:** Prefer modeling screen state as a single `UiState` data class rather than multiple independent flows.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**ViewModel (Multiplatform):** Use `androidx.lifecycle.ViewModel` (now multiplatform-ready) or a shared `CmpViewModel` to hold logic.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Component-based UI:** Break UI into small, reusable, and stateless Composables. A Composable over 50 lines is a candidate for decomposition.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Stateless vs Stateful:**\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Stateless:** Accepts state as parameters and emits events via callbacks. High reusability.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Stateful:** Manages its own state or interacts with a ViewModel. Use sparingly at the screen/feature root.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Modifier as first optional parameter:** Every UI Composable should accept a `modifier: Modifier = Modifier` as its first optional parameter.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Explicit parameter ordering:** Required parameters first, then `modifier`, then optional parameters, then the trailing lambda (if any).\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Stability:** Ensure parameters are stable or immutable. Use `@Stable` or `@Immutable` on domain models if Compose cannot infer it. Avoid passing `List` directly; use `ImmutableList` (from kotlinx-collections-immutable) or a wrapper.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Slot API:** Use the \\\"Slot\\\" pattern (accepting `@Composable () -> Unit` lambdas) to increase flexibility and avoid deep parameter propogation.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`remember` & `mutableStateOf`:** Use for local, UI-only state. Always use `rememberSaveable` for state that must survive configuration changes (Android).\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`StateFlow` to `State`:** Collect flows using `collectAsStateWithLifecycle()` (requires `lifecycle-runtime-compose` multiplatform) to ensure lifecycle-aware updates.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**State Hoisting:** Lift state up to make Composables stateless. The caller should control the state.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Don't pass `MutableState`:** Pass the raw value and a callback function, never a `MutableState<T>` object, to child Composables.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Side Effects:** Use `LaunchedEffect`, `SideEffect`, and `DisposableEffect` correctly. Never perform side effects directly in the Composable's body.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Platform-specific UI:** Use `expect`/`actual` for platform-native components (e.g., `MapView`, `WebView`).\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**CompositionLocal:** Use `CompositionLocal` for cross-cutting concerns like Themes, Localizations, or Platform-specific configurations.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Platform Check:** Use `LocalWindowInfo.current` or custom platform-specific `CompositionLocal` instead of hardcoded platform checks inside generic UI code.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Shared Resources:** Use `Compose Multiplatform Resources` (lib) for shared strings, fonts, and images.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Modifier Order Matters:** Modifiers are applied sequentially. `Modifier.padding(8.dp).clickable { }` is different from `Modifier.clickable { }.padding(8.dp)`.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Prefer `weight` in Rows/Columns:** Use `Modifier.weight()` for flexible layouts rather than hardcoded sizes.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `fillMaxSize()` in nested scrolling:** Use `wrapContentHeight()` or specific sizes to avoid infinite height constraints in `LazyColumn`.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Custom Modifiers:** Extract repeated modifier chains into extension functions for readability.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Compose Navigation Multiplatform:** Use the official `androidx.navigation:navigation-compose` (now Multiplatform).\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-33\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Type-safe Navigation:** Use Kotlin Serialization with the Navigation component for type-safe arguments.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-34\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Single NavHost:** Keep the `NavHost` at the top level of each platform's entry point or in a shared `App()` Composable.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-35\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Minimize Recomposition:**\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-36\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Pass only necessary data.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-37\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Use `derivedStateOf` for state that depends on other state to avoid redundant recompositions.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-38\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Use `remember` with keys to cache expensive calculations.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-39\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Lazy Collections:** Always use `LazyColumn`, `LazyRow`, and `LazyVerticalGrid` for large or dynamic lists. Use `key` in `items()` for stable identification.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-40\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid heavy computation in `@Composable`:** Move business logic and heavy mapping to the ViewModel or use `remember`.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-41\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Phase isolation:** Use `Modifier.graphicsLayer` or lambda-based modifiers (e.g., `Modifier.offset { ... }`) for frequent updates like animations to skip the \\\"recomposition\\\" and \\\"layout\\\" phases.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-42\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Compose UI Test:** Use `createComposeRule()` (Android) and `runComposeUiTest` (Multiplatform) to verify UI behavior.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-43\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Semantics:** Use `Modifier.semantics { contentDescription = \\\"...\\\" }` to make UI testable and accessible.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-44\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Screenshot Testing:** Use libraries like Roborazzi or Paparazzi for cross-platform visual regression testing.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-45\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Desktop:** Handle window resizing, keyboard shortcuts, and mouse hover states using `onPointerEvent`.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-46\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**iOS:** Ensure `UIKitView` is used correctly for native interop. Respect safe area insets via `WindowInsets.safeDrawing`.\"\n    },\n    {\n      \"id\": \"compose-multiplatform-rule-47\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Web (Wasm/JS):** Be mindful of heavy computations as Wasm/JS performance characteristics differ from JVM/Native.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/dart.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"dart\",\n  \"title\": \"Effective Dart Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the official \\\"Effective Dart\\\" guide for writing idiomatic, performant, and maintainable Dart code.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting\",\n      \"body\": \"- **`dart format`:** All Dart code **must** be formatted with `dart format` (formerly `dartfmt`). Non-negotiable.\\n- **Indentation:** 2 spaces. No tabs.\\n- **Line Length:** 80 characters maximum.\\n- **Trailing commas:** Add a trailing comma after the last item in multi-line argument/parameter lists to keep `dart format` from collapsing them to one line.\"\n    },\n    {\n      \"heading\": \"2. Naming\",\n      \"body\": \"- **`UpperCamelCase`:** For classes, enums, typedefs, and type parameters.\\n- **`lowerCamelCase`:** For variables, parameters, named parameters, functions, methods, and constant variables.\\n- **`lowercase_with_underscores`:** For libraries, packages, directories, and source files.\\n- **`_lowerCamelCase`:** For private identifiers (prefix with `_`).\\n- **Acronyms:** Capitalize only the first letter — `Http`, `Url`, `Json`, not `HTTP`, `URL`, `JSON` (exception: well-known two-letter acronyms like `IO`).\\n- **Boolean naming:** Name boolean variables and getters as affirmative phrases: `isEmpty`, `hasChildren`, `canClose`. Avoid `isNotEmpty` as a variable name.\"\n    },\n    {\n      \"heading\": \"3. Types\",\n      \"body\": \"- **Prefer type inference:** Let Dart infer types for local variables (`var x = ...`). Annotate when the type is not obvious or when required for API clarity.\\n- **`dynamic`:** Avoid `dynamic`. Use it only when genuinely dynamic behavior is needed. Prefer `Object?` for unknown values.\\n- **`Object` vs `dynamic`:** Prefer `Object?` for a type that accepts any value — it still participates in type checking; `dynamic` opts out entirely.\\n- **`var` vs explicit type:** Use `var` for local variables. Use explicit types for top-level and class-level declarations.\\n- **Nullable vs non-nullable:** With null safety, prefer non-nullable types. Only add `?` when `null` is a genuinely valid value.\\n- **Late variables:** Use `late` only when initialization is deferred but guaranteed before use. Prefer passing values in constructors.\"\n    },\n    {\n      \"heading\": \"4. Functions and Methods\",\n      \"body\": \"- **Named parameters:** Prefer named parameters for functions with more than two parameters or when parameter meaning is not obvious from the call site.\\n- **Required named parameters:** Mark mandatory named parameters `required`. Do not simulate with assertions on nullable params.\\n- **Arrow functions:** Use `=>` for simple single-expression functions and getters.\\n- **`async`/`await`:** Prefer `async`/`await` over raw `Future` chaining (`.then`, `.catchError`). Never mix them in the same function body.\\n- **Avoid `async` without `await`:** Do not mark a function `async` if it does not contain `await`.\\n- **`Stream`:** Return `Stream` for multiple asynchronous values. Use `async*` with `yield` to create streams.\"\n    },\n    {\n      \"heading\": \"5. Variables and Control Flow\",\n      \"body\": \"- **`final` by default:** Prefer `final` for variables that are not reassigned. Use `const` for compile-time constants.\\n- **`const` constructors:** Define `const` constructors for immutable classes. Use `const` at call sites for constant objects.\\n- **`??` and `?.`:** Use the null-aware operators instead of explicit null checks. Use `??=` for lazy initialization.\\n- **Cascade (`..`):** Use cascades to perform a sequence of operations on the same object. Do not cascade when the receiver is a temporary.\\n- **`is` over `as`:** Prefer `if (obj is Foo)` over `(obj as Foo)` to avoid runtime cast errors.\\n- **Avoid `late final` + nullable:** Pick one. Either the variable is `late` and always initialized, or it is nullable.\"\n    },\n    {\n      \"heading\": \"6. Collections\",\n      \"body\": \"- **Collection literals:** Use `[]`, `{}`, `<K,V>{}` over `List()`, `Set()`, `Map()` constructors.\\n- **Collection-if and collection-for:** Use these inside literals instead of building collections with `add`/`addAll` in a loop.\\n- **Spreads (`...`):** Use the spread operator to combine collections. Prefer over `addAll`.\\n- **`where`, `map`, `toList()`:** Chain iterable methods for transformations. Call `toList()` at the end to materialize only once.\\n- **Avoid `length` for emptiness:** Use `isEmpty`/`isNotEmpty` instead of `list.length == 0`.\"\n    },\n    {\n      \"heading\": \"7. Classes and Constructors\",\n      \"body\": \"- **Initializer lists:** Initialize fields in the initializer list, not the constructor body.\\n- **Shorthand field initialization:** Use `this.field` constructor parameters to initialize fields directly.\\n- **Factory constructors:** Use `factory` for constructors that return a cached or subtype instance.\\n- **`@immutable`:** Annotate classes with `@immutable` when all fields are `final`.\\n- **No public `late` fields:** Avoid public `late` fields. Prefer constructor initialization.\\n- **Extension methods:** Use extensions to add methods to existing types without subclassing.\"\n    },\n    {\n      \"heading\": \"8. Error Handling\",\n      \"body\": \"- **Exceptions for programming errors:** Throw `Error` subtypes (e.g., `ArgumentError`, `StateError`) for programming mistakes.\\n- **Exceptions for runtime failures:** Throw `Exception` subtypes for runtime failures the caller can recover from.\\n- **Avoid catching `Error`:** Catch specific exception types. Catching `Object` or `Error` is a sign of poor error modeling.\\n- **`on` before `catch`:** Use `on ExceptionType catch (e)` to restrict what you catch. Never use a bare `catch` without `on`.\"\n    },\n    {\n      \"heading\": \"9. Documentation\",\n      \"body\": \"- **`///` doc comments:** Use `///` (not `/** */`) for documentation on all public APIs.\\n- **Single-sentence summary:** Start every doc comment with a one-sentence summary that ends with a period.\\n- **Markdown:** Doc comments support Markdown. Use code blocks (`` ` ``) for inline code references.\\n- **`@param`, `@returns`:** Do not use these tags — Dart doc tools read the prose. Write parameter descriptions inline.\\n- **Prose over tags:** Describe behavior, edge cases, and usage in full sentences.\\n\\n*Source: [Effective Dart](https://dart.dev/effective-dart)*\"\n    }\n  ],\n  \"languages\": [\n    \"dart\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"dart-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`dart format`:** All Dart code **must** be formatted with `dart format` (formerly `dartfmt`). Non-negotiable.\"\n    },\n    {\n      \"id\": \"dart-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** 2 spaces. No tabs.\"\n    },\n    {\n      \"id\": \"dart-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Line Length:** 80 characters maximum.\"\n    },\n    {\n      \"id\": \"dart-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Trailing commas:** Add a trailing comma after the last item in multi-line argument/parameter lists to keep `dart format` from collapsing them to one line.\"\n    },\n    {\n      \"id\": \"dart-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`UpperCamelCase`:** For classes, enums, typedefs, and type parameters.\"\n    },\n    {\n      \"id\": \"dart-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`lowerCamelCase`:** For variables, parameters, named parameters, functions, methods, and constant variables.\"\n    },\n    {\n      \"id\": \"dart-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`lowercase_with_underscores`:** For libraries, packages, directories, and source files.\"\n    },\n    {\n      \"id\": \"dart-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`_lowerCamelCase`:** For private identifiers (prefix with `_`).\"\n    },\n    {\n      \"id\": \"dart-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Acronyms:** Capitalize only the first letter — `Http`, `Url`, `Json`, not `HTTP`, `URL`, `JSON` (exception: well-known two-letter acronyms like `IO`).\"\n    },\n    {\n      \"id\": \"dart-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Boolean naming:** Name boolean variables and getters as affirmative phrases: `isEmpty`, `hasChildren`, `canClose`. Avoid `isNotEmpty` as a variable name.\"\n    },\n    {\n      \"id\": \"dart-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Prefer type inference:** Let Dart infer types for local variables (`var x = ...`). Annotate when the type is not obvious or when required for API clarity.\"\n    },\n    {\n      \"id\": \"dart-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`dynamic`:** Avoid `dynamic`. Use it only when genuinely dynamic behavior is needed. Prefer `Object?` for unknown values.\"\n    },\n    {\n      \"id\": \"dart-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Object` vs `dynamic`:** Prefer `Object?` for a type that accepts any value — it still participates in type checking; `dynamic` opts out entirely.\"\n    },\n    {\n      \"id\": \"dart-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`var` vs explicit type:** Use `var` for local variables. Use explicit types for top-level and class-level declarations.\"\n    },\n    {\n      \"id\": \"dart-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Nullable vs non-nullable:** With null safety, prefer non-nullable types. Only add `?` when `null` is a genuinely valid value.\"\n    },\n    {\n      \"id\": \"dart-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Late variables:** Use `late` only when initialization is deferred but guaranteed before use. Prefer passing values in constructors.\"\n    },\n    {\n      \"id\": \"dart-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Named parameters:** Prefer named parameters for functions with more than two parameters or when parameter meaning is not obvious from the call site.\"\n    },\n    {\n      \"id\": \"dart-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Required named parameters:** Mark mandatory named parameters `required`. Do not simulate with assertions on nullable params.\"\n    },\n    {\n      \"id\": \"dart-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Arrow functions:** Use `=>` for simple single-expression functions and getters.\"\n    },\n    {\n      \"id\": \"dart-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`async`/`await`:** Prefer `async`/`await` over raw `Future` chaining (`.then`, `.catchError`). Never mix them in the same function body.\"\n    },\n    {\n      \"id\": \"dart-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `async` without `await`:** Do not mark a function `async` if it does not contain `await`.\"\n    },\n    {\n      \"id\": \"dart-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Stream`:** Return `Stream` for multiple asynchronous values. Use `async*` with `yield` to create streams.\"\n    },\n    {\n      \"id\": \"dart-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`final` by default:** Prefer `final` for variables that are not reassigned. Use `const` for compile-time constants.\"\n    },\n    {\n      \"id\": \"dart-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`const` constructors:** Define `const` constructors for immutable classes. Use `const` at call sites for constant objects.\"\n    },\n    {\n      \"id\": \"dart-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`??` and `?.`:** Use the null-aware operators instead of explicit null checks. Use `??=` for lazy initialization.\"\n    },\n    {\n      \"id\": \"dart-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Cascade (`..`):** Use cascades to perform a sequence of operations on the same object. Do not cascade when the receiver is a temporary.\"\n    },\n    {\n      \"id\": \"dart-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`is` over `as`:** Prefer `if (obj is Foo)` over `(obj as Foo)` to avoid runtime cast errors.\"\n    },\n    {\n      \"id\": \"dart-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `late final` + nullable:** Pick one. Either the variable is `late` and always initialized, or it is nullable.\"\n    },\n    {\n      \"id\": \"dart-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Collection literals:** Use `[]`, `{}`, `<K,V>{}` over `List()`, `Set()`, `Map()` constructors.\"\n    },\n    {\n      \"id\": \"dart-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Collection-if and collection-for:** Use these inside literals instead of building collections with `add`/`addAll` in a loop.\"\n    },\n    {\n      \"id\": \"dart-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Spreads (`...`):** Use the spread operator to combine collections. Prefer over `addAll`.\"\n    },\n    {\n      \"id\": \"dart-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`where`, `map`, `toList()`:** Chain iterable methods for transformations. Call `toList()` at the end to materialize only once.\"\n    },\n    {\n      \"id\": \"dart-rule-33\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `length` for emptiness:** Use `isEmpty`/`isNotEmpty` instead of `list.length == 0`.\"\n    },\n    {\n      \"id\": \"dart-rule-34\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Initializer lists:** Initialize fields in the initializer list, not the constructor body.\"\n    },\n    {\n      \"id\": \"dart-rule-35\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Shorthand field initialization:** Use `this.field` constructor parameters to initialize fields directly.\"\n    },\n    {\n      \"id\": \"dart-rule-36\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Factory constructors:** Use `factory` for constructors that return a cached or subtype instance.\"\n    },\n    {\n      \"id\": \"dart-rule-37\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@immutable`:** Annotate classes with `@immutable` when all fields are `final`.\"\n    },\n    {\n      \"id\": \"dart-rule-38\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**No public `late` fields:** Avoid public `late` fields. Prefer constructor initialization.\"\n    },\n    {\n      \"id\": \"dart-rule-39\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Extension methods:** Use extensions to add methods to existing types without subclassing.\"\n    },\n    {\n      \"id\": \"dart-rule-40\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Exceptions for programming errors:** Throw `Error` subtypes (e.g., `ArgumentError`, `StateError`) for programming mistakes.\"\n    },\n    {\n      \"id\": \"dart-rule-41\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Exceptions for runtime failures:** Throw `Exception` subtypes for runtime failures the caller can recover from.\"\n    },\n    {\n      \"id\": \"dart-rule-42\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid catching `Error`:** Catch specific exception types. Catching `Object` or `Error` is a sign of poor error modeling.\"\n    },\n    {\n      \"id\": \"dart-rule-43\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`on` before `catch`:** Use `on ExceptionType catch (e)` to restrict what you catch. Never use a bare `catch` without `on`.\"\n    },\n    {\n      \"id\": \"dart-rule-44\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`///` doc comments:** Use `///` (not `/** */`) for documentation on all public APIs.\"\n    },\n    {\n      \"id\": \"dart-rule-45\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Single-sentence summary:** Start every doc comment with a one-sentence summary that ends with a period.\"\n    },\n    {\n      \"id\": \"dart-rule-46\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Markdown:** Doc comments support Markdown. Use code blocks (`` ` ``) for inline code references.\"\n    },\n    {\n      \"id\": \"dart-rule-47\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@param`, `@returns`:** Do not use these tags — Dart doc tools read the prose. Write parameter descriptions inline.\"\n    },\n    {\n      \"id\": \"dart-rule-48\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Prose over tags:** Describe behavior, edge cases, and usage in full sentences.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/flutter.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"flutter\",\n  \"title\": \"Effective Flutter Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices for writing idiomatic Flutter code, building on Effective Dart with Flutter-specific widget, state, and performance conventions.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting and Tooling\",\n      \"body\": \"- **`dart format`:** All Flutter code must be formatted with `dart format`. Run via `flutter format .`.\\n- **`flutter analyze`:** Treat all analyzer warnings as errors. Enforce in CI.\\n- **Trailing commas:** Always add trailing commas after the last argument in multi-line widget trees — this keeps `dart format` from collapsing widget hierarchies to one line.\\n- **Import ordering:** `dart:` → `package:flutter/` → other `package:` → relative imports.\"\n    },\n    {\n      \"heading\": \"2. Widget Design\",\n      \"body\": \"- **`StatelessWidget` by default:** Reach for `StatelessWidget` first. Only use `StatefulWidget` when local mutable state is genuinely needed.\\n- **Small, composable widgets:** Prefer extracting reusable pieces into their own widgets over large `build` methods. A build method over ~50 lines is a sign it should be split.\\n- **`const` constructors everywhere:** Define `const` constructors on all widgets. Use `const` at call sites to skip unnecessary rebuilds.\\n- **`Key` usage:** Pass `Key` to the parent `Widget` constructor, not to child widgets inside `build`. Use `ValueKey`, `ObjectKey`, or `UniqueKey` — not `GlobalKey` unless absolutely required (e.g., form validation, imperative scroll).\\n- **`Widget` over helper methods:** Extract sub-trees into separate `Widget` classes rather than private `_buildXxx()` methods. Widget classes participate in the element tree and optimize rebuilds; helper methods do not.\"\n    },\n    {\n      \"heading\": \"3. State Management\",\n      \"body\": \"- **Lift state up:** Keep state as low in the tree as needed and no lower. Share state by lifting to a common ancestor.\\n- **`setState` scope:** Call `setState` with the minimum code needed. Only pass the mutation, not the full rebuild computation.\\n- **Avoid storing `BuildContext` across async gaps:** After an `await`, check `mounted` before using `context`. Never store `context` in a field for later use.\\n- **Inherited widgets / providers:** Use `InheritedWidget`, `Provider`, `Riverpod`, or `Bloc` for state that many widgets need. Do not prop-drill through 3+ layers.\\n- **`initState` / `dispose`:** Always pair resource acquisition (`initState`, `addListener`) with release (`dispose`, `removeListener`). Never call `setState` in `initState`.\\n- **`didChangeDependencies`:** Use for operations that depend on `InheritedWidget` values, not `initState`.\"\n    },\n    {\n      \"heading\": \"4. Performance\",\n      \"body\": \"- **`const` widgets:** Mark widgets and their constructors `const` whenever possible — this is the single highest-ROI optimization in Flutter.\\n- **`ListView.builder`:** Use `ListView.builder` (or `GridView.builder`, `CustomScrollView`) for lists of unknown or large length. Never use `ListView(children: items.map(...).toList())` for long lists.\\n- **`RepaintBoundary`:** Wrap expensive, independently-animating subtrees in `RepaintBoundary` to isolate repaints.\\n- **Avoid rebuilding with `AnimatedBuilder`:** Pass only the widget that changes as the `child` parameter — it is cached and not rebuilt on animation ticks.\\n- **`ImageCache` and `precacheImage`:** Precache images likely to be shown soon. Prefer `AssetImage`/`NetworkImage` with proper caching over `Image.memory` for large images.\\n- **Avoid work in `build`:** Never compute values, parse data, or make network calls inside `build()`. Compute in `initState`, `didUpdateWidget`, or a state management layer.\"\n    },\n    {\n      \"heading\": \"5. Layout\",\n      \"body\": \"- **Prefer `Column`/`Row` over `Stack` where possible:** `Stack` is for overlay scenarios; prefer linear layouts for sequential content.\\n- **`Expanded` and `Flexible`:** Use `Expanded` to fill remaining space. Use `Flexible` when you want to allow shrinking. Never use `SizedBox.expand()` inside `Column`/`Row` — use `Expanded`.\\n- **`SizedBox` for spacing:** Prefer `SizedBox(height: 8)` over `Padding(padding: EdgeInsets.only(top: 8))` for simple fixed spacing.\\n- **`MediaQuery` and `LayoutBuilder`:** Use `LayoutBuilder` for widget-local responsive constraints. Use `MediaQuery.of(context)` sparingly — changes trigger full rebuilds.\\n- **Avoid unbounded height/width:** `Column` inside a `ListView` without a bounded height will throw. Use `shrinkWrap: true` or wrap in `SizedBox` with an explicit dimension when nesting scrollables.\"\n    },\n    {\n      \"heading\": \"6. Navigation and Routing\",\n      \"body\": \"- **Named routes or `go_router`:** Prefer declarative routing (`go_router`, `auto_route`) over imperative `Navigator.push` for deep linking and complex flows.\\n- **`WillPopScope` / `PopScope`:** Handle back navigation explicitly when there is unsaved state or a multi-step flow.\\n- **Pass data via constructor, not `arguments`:** Prefer typed constructor injection over `ModalRoute.of(context)!.settings.arguments`. Use route parameters with `go_router`.\"\n    },\n    {\n      \"heading\": \"7. Theming and Styling\",\n      \"body\": \"- **Use `Theme.of(context)`:** Never hardcode colors or text styles. Always derive from the active `ThemeData`.\\n- **`TextTheme`:** Use `Theme.of(context).textTheme.bodyLarge` etc. — do not define ad-hoc `TextStyle` objects everywhere.\\n- **`ColorScheme`:** Use Material 3's `ColorScheme` roles (`primary`, `surface`, `onPrimary`) rather than direct `Color` values.\\n- **`ThemeExtension`:** Define custom theme data in a `ThemeExtension<T>` instead of global constants.\"\n    },\n    {\n      \"heading\": \"8. Testing\",\n      \"body\": \"- **Widget tests over unit tests for UI:** Test widget behavior with `flutter_test`'s `WidgetTester`. Avoid screenshot testing as the primary regression tool.\\n- **`find.byType`, `find.byKey`:** Prefer `find.byKey(ValueKey('submit'))` for interactive elements. Use `find.byType` for presence checks.\\n- **`pumpAndSettle`:** Use after triggering async operations. Use `pump(Duration)` for animations you control explicitly.\\n- **Golden tests:** Use sparingly — they are brittle across platforms. Gate on a single reference platform in CI.\"\n    },\n    {\n      \"heading\": \"9. Naming and File Structure\",\n      \"body\": \"- **One widget per file:** Each top-level `Widget` subclass lives in its own file.\\n- **File names match class names:** `UserProfileCard` → `user_profile_card.dart`.\\n- **Feature-first folder structure:** Group by feature (`lib/features/auth/`, `lib/features/home/`), not by type (`lib/widgets/`, `lib/models/`).\\n- **`_` prefix for private widgets:** Private helper widgets within a file are prefixed with `_`.\\n\\n*Sources: [Flutter Docs](https://docs.flutter.dev) · [Effective Dart](https://dart.dev/effective-dart) · [Flutter Performance Best Practices](https://docs.flutter.dev/perf/best-practices)*\"\n    }\n  ],\n  \"languages\": [\n    \"flutter\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"flutter-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`dart format`:** All Flutter code must be formatted with `dart format`. Run via `flutter format .`.\"\n    },\n    {\n      \"id\": \"flutter-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`flutter analyze`:** Treat all analyzer warnings as errors. Enforce in CI.\"\n    },\n    {\n      \"id\": \"flutter-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Trailing commas:** Always add trailing commas after the last argument in multi-line widget trees — this keeps `dart format` from collapsing widget hierarchies to one line.\"\n    },\n    {\n      \"id\": \"flutter-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Import ordering:** `dart:` → `package:flutter/` → other `package:` → relative imports.\"\n    },\n    {\n      \"id\": \"flutter-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`StatelessWidget` by default:** Reach for `StatelessWidget` first. Only use `StatefulWidget` when local mutable state is genuinely needed.\"\n    },\n    {\n      \"id\": \"flutter-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Small, composable widgets:** Prefer extracting reusable pieces into their own widgets over large `build` methods. A build method over ~50 lines is a sign it should be split.\"\n    },\n    {\n      \"id\": \"flutter-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`const` constructors everywhere:** Define `const` constructors on all widgets. Use `const` at call sites to skip unnecessary rebuilds.\"\n    },\n    {\n      \"id\": \"flutter-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Key` usage:** Pass `Key` to the parent `Widget` constructor, not to child widgets inside `build`. Use `ValueKey`, `ObjectKey`, or `UniqueKey` — not `GlobalKey` unless absolutely required (e.g., form validation, imperative scroll).\"\n    },\n    {\n      \"id\": \"flutter-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Widget` over helper methods:** Extract sub-trees into separate `Widget` classes rather than private `_buildXxx()` methods. Widget classes participate in the element tree and optimize rebuilds; helper methods do not.\"\n    },\n    {\n      \"id\": \"flutter-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Lift state up:** Keep state as low in the tree as needed and no lower. Share state by lifting to a common ancestor.\"\n    },\n    {\n      \"id\": \"flutter-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`setState` scope:** Call `setState` with the minimum code needed. Only pass the mutation, not the full rebuild computation.\"\n    },\n    {\n      \"id\": \"flutter-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid storing `BuildContext` across async gaps:** After an `await`, check `mounted` before using `context`. Never store `context` in a field for later use.\"\n    },\n    {\n      \"id\": \"flutter-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Inherited widgets / providers:** Use `InheritedWidget`, `Provider`, `Riverpod`, or `Bloc` for state that many widgets need. Do not prop-drill through 3+ layers.\"\n    },\n    {\n      \"id\": \"flutter-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`initState` / `dispose`:** Always pair resource acquisition (`initState`, `addListener`) with release (`dispose`, `removeListener`). Never call `setState` in `initState`.\"\n    },\n    {\n      \"id\": \"flutter-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`didChangeDependencies`:** Use for operations that depend on `InheritedWidget` values, not `initState`.\"\n    },\n    {\n      \"id\": \"flutter-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`const` widgets:** Mark widgets and their constructors `const` whenever possible — this is the single highest-ROI optimization in Flutter.\"\n    },\n    {\n      \"id\": \"flutter-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`ListView.builder`:** Use `ListView.builder` (or `GridView.builder`, `CustomScrollView`) for lists of unknown or large length. Never use `ListView(children: items.map(...).toList())` for long lists.\"\n    },\n    {\n      \"id\": \"flutter-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`RepaintBoundary`:** Wrap expensive, independently-animating subtrees in `RepaintBoundary` to isolate repaints.\"\n    },\n    {\n      \"id\": \"flutter-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid rebuilding with `AnimatedBuilder`:** Pass only the widget that changes as the `child` parameter — it is cached and not rebuilt on animation ticks.\"\n    },\n    {\n      \"id\": \"flutter-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`ImageCache` and `precacheImage`:** Precache images likely to be shown soon. Prefer `AssetImage`/`NetworkImage` with proper caching over `Image.memory` for large images.\"\n    },\n    {\n      \"id\": \"flutter-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid work in `build`:** Never compute values, parse data, or make network calls inside `build()`. Compute in `initState`, `didUpdateWidget`, or a state management layer.\"\n    },\n    {\n      \"id\": \"flutter-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Prefer `Column`/`Row` over `Stack` where possible:** `Stack` is for overlay scenarios; prefer linear layouts for sequential content.\"\n    },\n    {\n      \"id\": \"flutter-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Expanded` and `Flexible`:** Use `Expanded` to fill remaining space. Use `Flexible` when you want to allow shrinking. Never use `SizedBox.expand()` inside `Column`/`Row` — use `Expanded`.\"\n    },\n    {\n      \"id\": \"flutter-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`SizedBox` for spacing:** Prefer `SizedBox(height: 8)` over `Padding(padding: EdgeInsets.only(top: 8))` for simple fixed spacing.\"\n    },\n    {\n      \"id\": \"flutter-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`MediaQuery` and `LayoutBuilder`:** Use `LayoutBuilder` for widget-local responsive constraints. Use `MediaQuery.of(context)` sparingly — changes trigger full rebuilds.\"\n    },\n    {\n      \"id\": \"flutter-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid unbounded height/width:** `Column` inside a `ListView` without a bounded height will throw. Use `shrinkWrap: true` or wrap in `SizedBox` with an explicit dimension when nesting scrollables.\"\n    },\n    {\n      \"id\": \"flutter-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Named routes or `go_router`:** Prefer declarative routing (`go_router`, `auto_route`) over imperative `Navigator.push` for deep linking and complex flows.\"\n    },\n    {\n      \"id\": \"flutter-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`WillPopScope` / `PopScope`:** Handle back navigation explicitly when there is unsaved state or a multi-step flow.\"\n    },\n    {\n      \"id\": \"flutter-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Pass data via constructor, not `arguments`:** Prefer typed constructor injection over `ModalRoute.of(context)!.settings.arguments`. Use route parameters with `go_router`.\"\n    },\n    {\n      \"id\": \"flutter-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Use `Theme.of(context)`:** Never hardcode colors or text styles. Always derive from the active `ThemeData`.\"\n    },\n    {\n      \"id\": \"flutter-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`TextTheme`:** Use `Theme.of(context).textTheme.bodyLarge` etc. — do not define ad-hoc `TextStyle` objects everywhere.\"\n    },\n    {\n      \"id\": \"flutter-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`ColorScheme`:** Use Material 3's `ColorScheme` roles (`primary`, `surface`, `onPrimary`) rather than direct `Color` values.\"\n    },\n    {\n      \"id\": \"flutter-rule-33\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`ThemeExtension`:** Define custom theme data in a `ThemeExtension<T>` instead of global constants.\"\n    },\n    {\n      \"id\": \"flutter-rule-34\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Widget tests over unit tests for UI:** Test widget behavior with `flutter_test`'s `WidgetTester`. Avoid screenshot testing as the primary regression tool.\"\n    },\n    {\n      \"id\": \"flutter-rule-35\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`find.byType`, `find.byKey`:** Prefer `find.byKey(ValueKey('submit'))` for interactive elements. Use `find.byType` for presence checks.\"\n    },\n    {\n      \"id\": \"flutter-rule-36\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`pumpAndSettle`:** Use after triggering async operations. Use `pump(Duration)` for animations you control explicitly.\"\n    },\n    {\n      \"id\": \"flutter-rule-37\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Golden tests:** Use sparingly — they are brittle across platforms. Gate on a single reference platform in CI.\"\n    },\n    {\n      \"id\": \"flutter-rule-38\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**One widget per file:** Each top-level `Widget` subclass lives in its own file.\"\n    },\n    {\n      \"id\": \"flutter-rule-39\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**File names match class names:** `UserProfileCard` → `user_profile_card.dart`.\"\n    },\n    {\n      \"id\": \"flutter-rule-40\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Feature-first folder structure:** Group by feature (`lib/features/auth/`, `lib/features/home/`), not by type (`lib/widgets/`, `lib/models/`).\"\n    },\n    {\n      \"id\": \"flutter-rule-41\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`_` prefix for private widgets:** Private helper widgets within a file are prefixed with `_`.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/general.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"general\",\n  \"title\": \"General Code Style Principles\",\n  \"summary\": \"This document outlines general coding principles that apply across all languages and frameworks used in this project.\",\n  \"sections\": [\n    {\n      \"heading\": \"Readability\",\n      \"body\": \"- Code should be easy to read and understand by humans.\\n- Avoid overly clever or obscure constructs.\"\n    },\n    {\n      \"heading\": \"Consistency\",\n      \"body\": \"- Follow existing patterns in the codebase.\\n- Maintain consistent formatting, naming, and structure.\"\n    },\n    {\n      \"heading\": \"Simplicity\",\n      \"body\": \"- Prefer simple solutions over complex ones.\\n- Break down complex problems into smaller, manageable parts.\"\n    },\n    {\n      \"heading\": \"Maintainability\",\n      \"body\": \"- Write code that is easy to modify and extend.\\n- Minimize dependencies and coupling.\"\n    },\n    {\n      \"heading\": \"Documentation\",\n      \"body\": \"- Document *why* something is done, not just *what*.\\n- Keep documentation up-to-date with code changes.\"\n    }\n  ],\n  \"languages\": [],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"general-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Code should be easy to read and understand by humans.\"\n    },\n    {\n      \"id\": \"general-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Avoid overly clever or obscure constructs.\"\n    },\n    {\n      \"id\": \"general-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Follow existing patterns in the codebase.\"\n    },\n    {\n      \"id\": \"general-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Maintain consistent formatting, naming, and structure.\"\n    },\n    {\n      \"id\": \"general-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Prefer simple solutions over complex ones.\"\n    },\n    {\n      \"id\": \"general-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Break down complex problems into smaller, manageable parts.\"\n    },\n    {\n      \"id\": \"general-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Write code that is easy to modify and extend.\"\n    },\n    {\n      \"id\": \"general-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Minimize dependencies and coupling.\"\n    },\n    {\n      \"id\": \"general-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Document *why* something is done, not just *what*.\"\n    },\n    {\n      \"id\": \"general-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Keep documentation up-to-date with code changes.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/go.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"go\",\n  \"title\": \"Effective Go Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the official \\\"Effective Go\\\" guide for writing idiomatic Go code.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting\",\n      \"body\": \"- **`gofmt`:** All Go code **must** be formatted with `gofmt` (or `go fmt`). This is a non-negotiable, automated standard.\\n- **Indentation:** Use tabs for indentation (`gofmt` handles this).\\n- **Line Length:** Go has no strict line length limit. Let `gofmt` handle line wrapping.\"\n    },\n    {\n      \"heading\": \"2. Naming\",\n      \"body\": \"- **`MixedCaps`:** Use `MixedCaps` or `mixedCaps` for multi-word names. Do not use underscores.\\n- **Exported vs. Unexported:** Names starting with an uppercase letter are exported (public). Names starting with a lowercase letter are not exported (private).\\n- **Package Names:** Short, concise, single-word, lowercase names.\\n- **Getters:** Do not name getters with a `Get` prefix. A getter for a field named `owner` should be named `Owner()`.\\n- **Interface Names:** One-method interfaces are named by the method name plus an `-er` suffix (e.g., `Reader`, `Writer`).\"\n    },\n    {\n      \"heading\": \"3. Control Structures\",\n      \"body\": \"- **`if`:** No parentheses around the condition. Braces are mandatory. Can include an initialization statement (e.g., `if err := file.Chmod(0664); err != nil`).\\n- **`for`:** Go's only looping construct. Unifies `for` and `while`. Use `for...range` to iterate over slices, maps, strings, and channels.\\n- **`switch`:** More general than in C. Cases do not fall through by default (use `fallthrough` explicitly). Can be used without an expression to function as a cleaner `if-else-if` chain.\"\n    },\n    {\n      \"heading\": \"4. Functions\",\n      \"body\": \"- **Multiple Returns:** Functions can return multiple values. This is the standard way to return a result and an error (e.g., `value, err`).\\n- **Named Result Parameters:** Return parameters can be named. This can make code clearer and more concise.\\n- **`defer`:** Schedules a function call to be run immediately before the function executing `defer` returns. Use it for cleanup tasks like closing files.\"\n    },\n    {\n      \"heading\": \"5. Data\",\n      \"body\": \"- **`new` vs. `make`:**\\n  - `new(T)`: Allocates memory for a new item of type `T`, zeroes it, and returns a pointer (`*T`).\\n  - `make(T, ...)`: Creates and initializes slices, maps, and channels only. Returns an initialized value of type `T` (not a pointer).\\n- **Slices:** The preferred way to work with sequences. They are more flexible than arrays.\\n- **Maps:** Use the \\\"comma ok\\\" idiom to check for the existence of a key: `value, ok := myMap[key]`.\"\n    },\n    {\n      \"heading\": \"6. Interfaces\",\n      \"body\": \"- **Implicit Implementation:** A type implements an interface by implementing its methods. No `implements` keyword is needed.\\n- **Small Interfaces:** Prefer many small interfaces over one large one. The standard library is full of single-method interfaces (e.g., `io.Reader`).\"\n    },\n    {\n      \"heading\": \"7. Concurrency\",\n      \"body\": \"- **Share Memory By Communicating:** This is the core philosophy. Do not communicate by sharing memory; instead, share memory by communicating.\\n- **Goroutines:** Lightweight, concurrently executing functions. Start one with the `go` keyword.\\n- **Channels:** Typed conduits for communication between goroutines. Use `make` to create them.\"\n    },\n    {\n      \"heading\": \"8. Errors\",\n      \"body\": \"- **`error` type:** The built-in `error` interface is the standard way to handle errors.\\n- **Explicit Error Handling:** Do not discard errors with the blank identifier (`_`). Check for errors explicitly.\\n- **`panic`:** Reserved for truly exceptional, unrecoverable situations. Generally, libraries should not panic.\\n\\n*Source: [Effective Go](https://go.dev/doc/effective_go)*\"\n    }\n  ],\n  \"languages\": [\n    \"go\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"go-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`gofmt`:** All Go code **must** be formatted with `gofmt` (or `go fmt`). This is a non-negotiable, automated standard.\"\n    },\n    {\n      \"id\": \"go-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** Use tabs for indentation (`gofmt` handles this).\"\n    },\n    {\n      \"id\": \"go-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Line Length:** Go has no strict line length limit. Let `gofmt` handle line wrapping.\"\n    },\n    {\n      \"id\": \"go-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`MixedCaps`:** Use `MixedCaps` or `mixedCaps` for multi-word names. Do not use underscores.\"\n    },\n    {\n      \"id\": \"go-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Exported vs. Unexported:** Names starting with an uppercase letter are exported (public). Names starting with a lowercase letter are not exported (private).\"\n    },\n    {\n      \"id\": \"go-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Package Names:** Short, concise, single-word, lowercase names.\"\n    },\n    {\n      \"id\": \"go-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Getters:** Do not name getters with a `Get` prefix. A getter for a field named `owner` should be named `Owner()`.\"\n    },\n    {\n      \"id\": \"go-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Interface Names:** One-method interfaces are named by the method name plus an `-er` suffix (e.g., `Reader`, `Writer`).\"\n    },\n    {\n      \"id\": \"go-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`if`:** No parentheses around the condition. Braces are mandatory. Can include an initialization statement (e.g., `if err := file.Chmod(0664); err != nil`).\"\n    },\n    {\n      \"id\": \"go-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`for`:** Go's only looping construct. Unifies `for` and `while`. Use `for...range` to iterate over slices, maps, strings, and channels.\"\n    },\n    {\n      \"id\": \"go-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`switch`:** More general than in C. Cases do not fall through by default (use `fallthrough` explicitly). Can be used without an expression to function as a cleaner `if-else-if` chain.\"\n    },\n    {\n      \"id\": \"go-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Multiple Returns:** Functions can return multiple values. This is the standard way to return a result and an error (e.g., `value, err`).\"\n    },\n    {\n      \"id\": \"go-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Named Result Parameters:** Return parameters can be named. This can make code clearer and more concise.\"\n    },\n    {\n      \"id\": \"go-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`defer`:** Schedules a function call to be run immediately before the function executing `defer` returns. Use it for cleanup tasks like closing files.\"\n    },\n    {\n      \"id\": \"go-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`new` vs. `make`:**\"\n    },\n    {\n      \"id\": \"go-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`new(T)`: Allocates memory for a new item of type `T`, zeroes it, and returns a pointer (`*T`).\"\n    },\n    {\n      \"id\": \"go-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`make(T, ...)`: Creates and initializes slices, maps, and channels only. Returns an initialized value of type `T` (not a pointer).\"\n    },\n    {\n      \"id\": \"go-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Slices:** The preferred way to work with sequences. They are more flexible than arrays.\"\n    },\n    {\n      \"id\": \"go-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Maps:** Use the \\\"comma ok\\\" idiom to check for the existence of a key: `value, ok := myMap[key]`.\"\n    },\n    {\n      \"id\": \"go-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Implicit Implementation:** A type implements an interface by implementing its methods. No `implements` keyword is needed.\"\n    },\n    {\n      \"id\": \"go-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Small Interfaces:** Prefer many small interfaces over one large one. The standard library is full of single-method interfaces (e.g., `io.Reader`).\"\n    },\n    {\n      \"id\": \"go-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Share Memory By Communicating:** This is the core philosophy. Do not communicate by sharing memory; instead, share memory by communicating.\"\n    },\n    {\n      \"id\": \"go-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Goroutines:** Lightweight, concurrently executing functions. Start one with the `go` keyword.\"\n    },\n    {\n      \"id\": \"go-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Channels:** Typed conduits for communication between goroutines. Use `make` to create them.\"\n    },\n    {\n      \"id\": \"go-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`error` type:** The built-in `error` interface is the standard way to handle errors.\"\n    },\n    {\n      \"id\": \"go-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Explicit Error Handling:** Do not discard errors with the blank identifier (`_`). Check for errors explicitly.\"\n    },\n    {\n      \"id\": \"go-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`panic`:** Reserved for truly exceptional, unrecoverable situations. Generally, libraries should not panic.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/html-css.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"html-css\",\n  \"title\": \"Google HTML/CSS Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the Google HTML/CSS Style Guide.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. General Rules\",\n      \"body\": \"- **Protocol:** Use HTTPS for all embedded resources.\\n- **Indentation:** Indent by 2 spaces. Do not use tabs.\\n- **Capitalization:** Use only lowercase for all code (element names, attributes, selectors, properties).\\n- **Trailing Whitespace:** Remove all trailing whitespace.\\n- **Encoding:** Use UTF-8 (without a BOM). Specify `<meta charset=\\\"utf-8\\\">` in HTML.\"\n    },\n    {\n      \"heading\": \"2. HTML Style Rules\",\n      \"body\": \"- **Document Type:** Use `<!doctype html>`.\\n- **HTML Validity:** Use valid HTML.\\n- **Semantics:** Use HTML elements according to their intended purpose (e.g., use `<p>` for paragraphs, not for spacing).\\n- **Multimedia Fallback:** Provide `alt` text for images and transcripts/captions for audio/video.\\n- **Separation of Concerns:** Strictly separate structure (HTML), presentation (CSS), and behavior (JavaScript). Link to CSS and JS from external files.\\n- **`type` Attributes:** Omit `type` attributes for stylesheets (`<link>`) and scripts (`<script>`).\"\n    },\n    {\n      \"heading\": \"3. HTML Formatting Rules\",\n      \"body\": \"- **General:** Use a new line for every block, list, or table element, and indent its children.\\n- **Quotation Marks:** Use double quotation marks (`\\\"\\\"`) for attribute values.\"\n    },\n    {\n      \"heading\": \"4. CSS Style Rules\",\n      \"body\": \"- **CSS Validity:** Use valid CSS.\\n- **Class Naming:** Use meaningful, generic names. Separate words with a hyphen (`-`).\\n  - **Good:** `.video-player`, `.site-navigation`\\n  - **Bad:** `.vid`, `.red-text`\\n- **ID Selectors:** Avoid using ID selectors for styling. Prefer class selectors.\\n- **Shorthand Properties:** Use shorthand properties where possible (e.g., `padding`, `font`).\\n- **`0` and Units:** Omit units for `0` values (e.g., `margin: 0;`).\\n- **Leading `0`s:** Always include leading `0`s for decimal values (e.g., `font-size: 0.8em;`).\\n- **Hexadecimal Notation:** Use 3-character hex notation where possible (e.g., `#fff`).\\n- **`!important`:** Avoid using `!important`.\"\n    },\n    {\n      \"heading\": \"5. CSS Formatting Rules\",\n      \"body\": \"- **Declaration Order:** Alphabetize declarations within a rule.\\n- **Indentation:** Indent all block content.\\n- **Semicolons:** Use a semicolon after every declaration.\\n- **Spacing:**\\n  - Use a space after a property name's colon (`font-weight: bold;`).\\n  - Use a space between the last selector and the opening brace (`.foo {`).\\n  - Start a new line for each selector and declaration.\\n- **Rule Separation:** Separate rules with a new line.\\n- **Quotation Marks:** Use single quotes (`''`) for attribute selectors and property values (e.g., `[type='text']`).\\n\\n**BE CONSISTENT.** When editing code, match the existing style.\\n\\n*Source: [Google HTML/CSS Style Guide](https://google.github.io/styleguide/htmlcssguide.html)*\"\n    }\n  ],\n  \"languages\": [\n    \"html-css\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"html-css-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Protocol:** Use HTTPS for all embedded resources.\"\n    },\n    {\n      \"id\": \"html-css-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** Indent by 2 spaces. Do not use tabs.\"\n    },\n    {\n      \"id\": \"html-css-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Capitalization:** Use only lowercase for all code (element names, attributes, selectors, properties).\"\n    },\n    {\n      \"id\": \"html-css-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Trailing Whitespace:** Remove all trailing whitespace.\"\n    },\n    {\n      \"id\": \"html-css-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Encoding:** Use UTF-8 (without a BOM). Specify `<meta charset=\\\"utf-8\\\">` in HTML.\"\n    },\n    {\n      \"id\": \"html-css-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Document Type:** Use `<!doctype html>`.\"\n    },\n    {\n      \"id\": \"html-css-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**HTML Validity:** Use valid HTML.\"\n    },\n    {\n      \"id\": \"html-css-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Semantics:** Use HTML elements according to their intended purpose (e.g., use `<p>` for paragraphs, not for spacing).\"\n    },\n    {\n      \"id\": \"html-css-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Multimedia Fallback:** Provide `alt` text for images and transcripts/captions for audio/video.\"\n    },\n    {\n      \"id\": \"html-css-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Separation of Concerns:** Strictly separate structure (HTML), presentation (CSS), and behavior (JavaScript). Link to CSS and JS from external files.\"\n    },\n    {\n      \"id\": \"html-css-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`type` Attributes:** Omit `type` attributes for stylesheets (`<link>`) and scripts (`<script>`).\"\n    },\n    {\n      \"id\": \"html-css-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**General:** Use a new line for every block, list, or table element, and indent its children.\"\n    },\n    {\n      \"id\": \"html-css-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Quotation Marks:** Use double quotation marks (`\\\"\\\"`) for attribute values.\"\n    },\n    {\n      \"id\": \"html-css-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**CSS Validity:** Use valid CSS.\"\n    },\n    {\n      \"id\": \"html-css-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Class Naming:** Use meaningful, generic names. Separate words with a hyphen (`-`).\"\n    },\n    {\n      \"id\": \"html-css-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Good:** `.video-player`, `.site-navigation`\"\n    },\n    {\n      \"id\": \"html-css-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Bad:** `.vid`, `.red-text`\"\n    },\n    {\n      \"id\": \"html-css-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**ID Selectors:** Avoid using ID selectors for styling. Prefer class selectors.\"\n    },\n    {\n      \"id\": \"html-css-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Shorthand Properties:** Use shorthand properties where possible (e.g., `padding`, `font`).\"\n    },\n    {\n      \"id\": \"html-css-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`0` and Units:** Omit units for `0` values (e.g., `margin: 0;`).\"\n    },\n    {\n      \"id\": \"html-css-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Leading `0`s:** Always include leading `0`s for decimal values (e.g., `font-size: 0.8em;`).\"\n    },\n    {\n      \"id\": \"html-css-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Hexadecimal Notation:** Use 3-character hex notation where possible (e.g., `#fff`).\"\n    },\n    {\n      \"id\": \"html-css-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`!important`:** Avoid using `!important`.\"\n    },\n    {\n      \"id\": \"html-css-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Declaration Order:** Alphabetize declarations within a rule.\"\n    },\n    {\n      \"id\": \"html-css-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** Indent all block content.\"\n    },\n    {\n      \"id\": \"html-css-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Semicolons:** Use a semicolon after every declaration.\"\n    },\n    {\n      \"id\": \"html-css-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Spacing:**\"\n    },\n    {\n      \"id\": \"html-css-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Use a space after a property name's colon (`font-weight: bold;`).\"\n    },\n    {\n      \"id\": \"html-css-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Use a space between the last selector and the opening brace (`.foo {`).\"\n    },\n    {\n      \"id\": \"html-css-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Start a new line for each selector and declaration.\"\n    },\n    {\n      \"id\": \"html-css-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Rule Separation:** Separate rules with a new line.\"\n    },\n    {\n      \"id\": \"html-css-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Quotation Marks:** Use single quotes (`''`) for attribute selectors and property values (e.g., `[type='text']`).\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/javascript.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"javascript\",\n  \"title\": \"Google JavaScript Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the Google JavaScript Style Guide.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Source File Basics\",\n      \"body\": \"- **File Naming:** All lowercase, with underscores (`_`) or dashes (`-`). Extension must be `.js`.\\n- **File Encoding:** UTF-8.\\n- **Whitespace:** Use only ASCII horizontal spaces (0x20). Tabs are forbidden for indentation.\"\n    },\n    {\n      \"heading\": \"2. Source File Structure\",\n      \"body\": \"- New files should be ES modules (`import`/`export`).\\n- **Exports:** Use named exports (`export {MyClass};`). **Do not use default exports.**\\n- **Imports:** Do not use line-wrapped imports. The `.js` extension in import paths is mandatory.\"\n    },\n    {\n      \"heading\": \"3. Formatting\",\n      \"body\": \"- **Braces:** Required for all control structures (`if`, `for`, `while`, etc.), even single-line blocks. Use K&R style (\\\"Egyptian brackets\\\").\\n- **Indentation:** +2 spaces for each new block.\\n- **Semicolons:** Every statement must be terminated with a semicolon.\\n- **Column Limit:** 80 characters.\\n- **Line-wrapping:** Indent continuation lines at least +4 spaces.\\n- **Whitespace:** Use single blank lines between methods. No trailing whitespace.\"\n    },\n    {\n      \"heading\": \"4. Language Features\",\n      \"body\": \"- **Variable Declarations:** Use `const` by default, `let` if reassignment is needed. **`var` is forbidden.**\\n- **Array Literals:** Use trailing commas. Do not use the `Array` constructor.\\n- **Object Literals:** Use trailing commas and shorthand properties. Do not use the `Object` constructor.\\n- **Classes:** Do not use JavaScript getter/setter properties (`get name()`). Provide ordinary methods instead.\\n- **Functions:** Prefer arrow functions for nested functions to preserve `this` context.\\n- **String Literals:** Use single quotes (`'`). Use template literals (`` ` ``) for multi-line strings or complex interpolation.\\n- **Control Structures:** Prefer `for-of` loops. `for-in` loops should only be used on dict-style objects.\\n- **`this`:** Only use `this` in class constructors, methods, or in arrow functions defined within them.\\n- **Equality Checks:** Always use identity operators (`===` / `!==`).\"\n    },\n    {\n      \"heading\": \"5. Disallowed Features\",\n      \"body\": \"- `with` keyword.\\n- `eval()` or `Function(...string)`.\\n- Automatic Semicolon Insertion.\\n- Modifying builtin objects (`Array.prototype.foo = ...`).\"\n    },\n    {\n      \"heading\": \"6. Naming\",\n      \"body\": \"- **Classes:** `UpperCamelCase`.\\n- **Methods & Functions:** `lowerCamelCase`.\\n- **Constants:** `CONSTANT_CASE` (all uppercase with underscores).\\n- **Non-constant Fields & Variables:** `lowerCamelCase`.\"\n    },\n    {\n      \"heading\": \"7. JSDoc\",\n      \"body\": \"- JSDoc is used on all classes, fields, and methods.\\n- Use `@param`, `@return`, `@override`, `@deprecated`.\\n- Type annotations are enclosed in braces (e.g., `/** @param {string} userName */`).\\n\\n*Source: [Google JavaScript Style Guide](https://google.github.io/styleguide/jsguide.html)*\"\n    }\n  ],\n  \"languages\": [\n    \"javascript\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"javascript-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**File Naming:** All lowercase, with underscores (`_`) or dashes (`-`). Extension must be `.js`.\"\n    },\n    {\n      \"id\": \"javascript-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**File Encoding:** UTF-8.\"\n    },\n    {\n      \"id\": \"javascript-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Whitespace:** Use only ASCII horizontal spaces (0x20). Tabs are forbidden for indentation.\"\n    },\n    {\n      \"id\": \"javascript-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"New files should be ES modules (`import`/`export`).\"\n    },\n    {\n      \"id\": \"javascript-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Exports:** Use named exports (`export {MyClass};`). **Do not use default exports.**\"\n    },\n    {\n      \"id\": \"javascript-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Imports:** Do not use line-wrapped imports. The `.js` extension in import paths is mandatory.\"\n    },\n    {\n      \"id\": \"javascript-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Braces:** Required for all control structures (`if`, `for`, `while`, etc.), even single-line blocks. Use K&R style (\\\"Egyptian brackets\\\").\"\n    },\n    {\n      \"id\": \"javascript-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** +2 spaces for each new block.\"\n    },\n    {\n      \"id\": \"javascript-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Semicolons:** Every statement must be terminated with a semicolon.\"\n    },\n    {\n      \"id\": \"javascript-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Column Limit:** 80 characters.\"\n    },\n    {\n      \"id\": \"javascript-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Line-wrapping:** Indent continuation lines at least +4 spaces.\"\n    },\n    {\n      \"id\": \"javascript-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Whitespace:** Use single blank lines between methods. No trailing whitespace.\"\n    },\n    {\n      \"id\": \"javascript-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Variable Declarations:** Use `const` by default, `let` if reassignment is needed. **`var` is forbidden.**\"\n    },\n    {\n      \"id\": \"javascript-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Array Literals:** Use trailing commas. Do not use the `Array` constructor.\"\n    },\n    {\n      \"id\": \"javascript-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Object Literals:** Use trailing commas and shorthand properties. Do not use the `Object` constructor.\"\n    },\n    {\n      \"id\": \"javascript-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Classes:** Do not use JavaScript getter/setter properties (`get name()`). Provide ordinary methods instead.\"\n    },\n    {\n      \"id\": \"javascript-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Functions:** Prefer arrow functions for nested functions to preserve `this` context.\"\n    },\n    {\n      \"id\": \"javascript-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**String Literals:** Use single quotes (`'`). Use template literals (`` ` ``) for multi-line strings or complex interpolation.\"\n    },\n    {\n      \"id\": \"javascript-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Control Structures:** Prefer `for-of` loops. `for-in` loops should only be used on dict-style objects.\"\n    },\n    {\n      \"id\": \"javascript-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`this`:** Only use `this` in class constructors, methods, or in arrow functions defined within them.\"\n    },\n    {\n      \"id\": \"javascript-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Equality Checks:** Always use identity operators (`===` / `!==`).\"\n    },\n    {\n      \"id\": \"javascript-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`with` keyword.\"\n    },\n    {\n      \"id\": \"javascript-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`eval()` or `Function(...string)`.\"\n    },\n    {\n      \"id\": \"javascript-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Automatic Semicolon Insertion.\"\n    },\n    {\n      \"id\": \"javascript-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Modifying builtin objects (`Array.prototype.foo = ...`).\"\n    },\n    {\n      \"id\": \"javascript-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Classes:** `UpperCamelCase`.\"\n    },\n    {\n      \"id\": \"javascript-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Methods & Functions:** `lowerCamelCase`.\"\n    },\n    {\n      \"id\": \"javascript-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Constants:** `CONSTANT_CASE` (all uppercase with underscores).\"\n    },\n    {\n      \"id\": \"javascript-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Non-constant Fields & Variables:** `lowerCamelCase`.\"\n    },\n    {\n      \"id\": \"javascript-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"JSDoc is used on all classes, fields, and methods.\"\n    },\n    {\n      \"id\": \"javascript-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Use `@param`, `@return`, `@override`, `@deprecated`.\"\n    },\n    {\n      \"id\": \"javascript-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Type annotations are enclosed in braces (e.g., `/** @param {string} userName */`).\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/kotlin.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"kotlin\",\n  \"title\": \"Kotlin Coding Conventions Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the official Kotlin Coding Conventions and JetBrains style guide for writing idiomatic, concise, and safe Kotlin code.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting\",\n      \"body\": \"- **ktlint / detekt:** Format with `ktlint` and lint with `detekt`. Enforce both in CI.\\n- **Indentation:** 4 spaces. No tabs. Continuation lines: 4 spaces (not 8).\\n- **Line Length:** 120 characters maximum (Kotlin convention; some projects use 100).\\n- **Trailing lambdas:** When the last parameter is a lambda, move it outside the parentheses. Omit the parentheses entirely if the lambda is the only parameter.\"\n    },\n    {\n      \"heading\": \"2. Naming\",\n      \"body\": \"- **`UpperCamelCase`:** For classes, objects, interfaces, and enum entries.\\n- **`lowerCamelCase`:** For functions, properties, local variables, and parameters.\\n- **`SCREAMING_SNAKE_CASE`:** For `const val` top-level and companion object constants only.\\n- **`_` prefix:** Do not use. Kotlin backing properties use `_name` as a convention for private backing fields exposed via a public property — this is the single accepted use.\\n- **Acronyms:** Use `UpperCamelCase` even for acronyms: `XmlParser`, `HttpClient`, not `XMLParser`.\\n- **Boolean properties:** Name as `isEnabled`, `hasError`, `canClose`. Prefix with `is`, `has`, or `can`.\\n- **Test functions:** Use backtick names for test readability: `` `should throw when user is null`() ``.\"\n    },\n    {\n      \"heading\": \"3. Null Safety\",\n      \"body\": \"- **Non-nullable by default:** Prefer non-nullable types. Add `?` only when `null` is a genuinely valid value.\\n- **Avoid `!!`:** The `!!` operator is a code smell. It bypasses null safety and throws `NullPointerException`. Use `?.`, `?:`, `let`, `also`, or explicit null checks instead.\\n- **`?.let { }`:** Use for null-conditional execution. Prefer `if (x != null)` when the block is long and `x` is used multiple times (smart cast).\\n- **Elvis operator `?:`:** Use for default values: `val name = user?.name ?: \\\"Anonymous\\\"`.\\n- **`requireNotNull` / `checkNotNull`:** Use to assert non-nullability at runtime with a clear message: `requireNotNull(user) { \\\"User must not be null\\\" }`.\"\n    },\n    {\n      \"heading\": \"4. Classes and Objects\",\n      \"body\": \"- **`data class`:** Use for classes that hold data. They get `equals`, `hashCode`, `toString`, `copy`, and `componentN` for free.\\n- **`sealed class`:** Use for closed type hierarchies (discriminated unions). Prefer `sealed class` over `enum class` when variants carry different data.\\n- **`object`:** Use for singletons and companion objects. Do not use a class with a private constructor + static `getInstance()`.\\n- **`companion object`:** Use for factory methods and constants logically associated with a class. Name the companion only if it will be referenced by name.\\n- **`val` over `var`:** Prefer immutable `val` properties. Use `var` only when mutation is necessary.\\n- **`init` blocks:** Minimize logic in `init` blocks. Prefer constructor parameter defaults and `lazy` properties.\\n- **`lazy`:** Use `by lazy { }` for properties that are expensive to compute and not always needed.\"\n    },\n    {\n      \"heading\": \"5. Functions\",\n      \"body\": \"- **Expression bodies:** Use `= expression` for single-expression functions. Omit braces and `return`.\\n- **Default parameter values:** Use default values instead of overloaded functions.\\n- **Named arguments:** Use named arguments when calling functions with many parameters or when the parameter type is a primitive and meaning is unclear.\\n- **Extension functions:** Use extension functions to add behavior to existing types without subclassing. Place extensions in a file named `TypeExtensions.kt`.\\n- **Scope functions (`let`, `run`, `with`, `apply`, `also`):**\\n  - `apply`: Configure an object — use when returning the object. (`builder.apply { ... }`)\\n  - `also`: Perform side effects — use when returning the object. (`list.also { log(it) }`)\\n  - `let`: Transform a value, especially with nullability. (`str?.let { process(it) }`)\\n  - `run`/`with`: Group a block of operations on an object. Prefer `run` for nullable receivers, `with` for non-null.\\n  - **Do not nest scope functions more than 2 levels deep.**\\n- **`operator fun`:** Only implement operator overloads when the semantics are universally clear (e.g., `+` for numeric types, `get`/`set` for collections).\"\n    },\n    {\n      \"heading\": \"6. Collections\",\n      \"body\": \"- **Immutable collections:** Prefer `listOf`, `mapOf`, `setOf`. Use `mutableListOf` etc. only when mutation is needed.\\n- **`Sequence` for lazy pipelines:** Use `asSequence()` for long transformation chains on large collections to avoid creating intermediate lists.\\n- **Avoid `null` in collections:** Prefer `emptyList()` over `null` for absent collections. Use `filterNotNull()` to clean mixed lists.\\n- **`associate`, `groupBy`, `partition`:** Use the stdlib collection transforms instead of manual loops.\\n- **Destructuring:** Use destructuring declarations in `for` loops and `map` operations: `for ((key, value) in map)`.\"\n    },\n    {\n      \"heading\": \"7. Coroutines\",\n      \"body\": \"- **`suspend` functions:** Mark any function that calls another `suspend` function or performs async I/O as `suspend`. Do not block inside `suspend` functions — use `withContext(Dispatchers.IO)`.\\n- **Structured concurrency:** Always launch coroutines in a `CoroutineScope`. Never use `GlobalScope` in application code.\\n- **`Dispatchers.Main` / `.IO` / `.Default`:** Use the correct dispatcher — `Main` for UI, `IO` for blocking I/O, `Default` for CPU work.\\n- **`Flow`:** Use `Flow<T>` for streams of values. Use `StateFlow`/`SharedFlow` for hot streams. Call `.collect {}` in a lifecycle-aware scope.\\n- **Error handling:** Use `try/catch` inside coroutines. Use `CoroutineExceptionHandler` for unhandled exceptions in `launch`. `async`/`await` propagates exceptions at the `await` site.\\n- **`viewModelScope` / `lifecycleScope`:** Use these scopes in Android (see Android guide) to tie coroutine lifetime to the component.\"\n    },\n    {\n      \"heading\": \"8. Error Handling\",\n      \"body\": \"- **Exceptions for exceptional cases:** Do not use exceptions for normal control flow. Return `Result<T>`, `sealed class`, or nullable values for expected failure paths.\\n- **`runCatching`:** Use `runCatching { }` for concise try-catch with `Result<T>` return.\\n- **Avoid catching `Throwable`:** Catch specific exception types. Catching `Throwable` also catches `Error` (e.g., `OutOfMemoryError`).\"\n    },\n    {\n      \"heading\": \"9. Documentation\",\n      \"body\": \"- **KDoc (`/** */`):** Use KDoc for all public API documentation.\\n- **`@param`, `@return`, `@throws`:** Include these tags for public functions with non-obvious parameters or return values.\\n- **`@since`:** Mark API additions with `@since <version>`.\\n- **Avoid redundant comments:** Do not restate the function name in the doc. Add information about behavior, edge cases, or constraints.\\n\\n*Source: [Kotlin Coding Conventions](https://kotlinlang.org/docs/coding-conventions.html)*\"\n    }\n  ],\n  \"languages\": [\n    \"kotlin\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"kotlin-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**ktlint / detekt:** Format with `ktlint` and lint with `detekt`. Enforce both in CI.\"\n    },\n    {\n      \"id\": \"kotlin-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** 4 spaces. No tabs. Continuation lines: 4 spaces (not 8).\"\n    },\n    {\n      \"id\": \"kotlin-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Line Length:** 120 characters maximum (Kotlin convention; some projects use 100).\"\n    },\n    {\n      \"id\": \"kotlin-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Trailing lambdas:** When the last parameter is a lambda, move it outside the parentheses. Omit the parentheses entirely if the lambda is the only parameter.\"\n    },\n    {\n      \"id\": \"kotlin-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`UpperCamelCase`:** For classes, objects, interfaces, and enum entries.\"\n    },\n    {\n      \"id\": \"kotlin-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`lowerCamelCase`:** For functions, properties, local variables, and parameters.\"\n    },\n    {\n      \"id\": \"kotlin-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`SCREAMING_SNAKE_CASE`:** For `const val` top-level and companion object constants only.\"\n    },\n    {\n      \"id\": \"kotlin-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`_` prefix:** Do not use. Kotlin backing properties use `_name` as a convention for private backing fields exposed via a public property — this is the single accepted use.\"\n    },\n    {\n      \"id\": \"kotlin-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Acronyms:** Use `UpperCamelCase` even for acronyms: `XmlParser`, `HttpClient`, not `XMLParser`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Boolean properties:** Name as `isEnabled`, `hasError`, `canClose`. Prefix with `is`, `has`, or `can`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Test functions:** Use backtick names for test readability: `` `should throw when user is null`() ``.\"\n    },\n    {\n      \"id\": \"kotlin-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Non-nullable by default:** Prefer non-nullable types. Add `?` only when `null` is a genuinely valid value.\"\n    },\n    {\n      \"id\": \"kotlin-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `!!`:** The `!!` operator is a code smell. It bypasses null safety and throws `NullPointerException`. Use `?.`, `?:`, `let`, `also`, or explicit null checks instead.\"\n    },\n    {\n      \"id\": \"kotlin-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`?.let { }`:** Use for null-conditional execution. Prefer `if (x != null)` when the block is long and `x` is used multiple times (smart cast).\"\n    },\n    {\n      \"id\": \"kotlin-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Elvis operator `?:`:** Use for default values: `val name = user?.name ?: \\\"Anonymous\\\"`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`requireNotNull` / `checkNotNull`:** Use to assert non-nullability at runtime with a clear message: `requireNotNull(user) { \\\"User must not be null\\\" }`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`data class`:** Use for classes that hold data. They get `equals`, `hashCode`, `toString`, `copy`, and `componentN` for free.\"\n    },\n    {\n      \"id\": \"kotlin-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`sealed class`:** Use for closed type hierarchies (discriminated unions). Prefer `sealed class` over `enum class` when variants carry different data.\"\n    },\n    {\n      \"id\": \"kotlin-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`object`:** Use for singletons and companion objects. Do not use a class with a private constructor + static `getInstance()`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`companion object`:** Use for factory methods and constants logically associated with a class. Name the companion only if it will be referenced by name.\"\n    },\n    {\n      \"id\": \"kotlin-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`val` over `var`:** Prefer immutable `val` properties. Use `var` only when mutation is necessary.\"\n    },\n    {\n      \"id\": \"kotlin-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`init` blocks:** Minimize logic in `init` blocks. Prefer constructor parameter defaults and `lazy` properties.\"\n    },\n    {\n      \"id\": \"kotlin-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`lazy`:** Use `by lazy { }` for properties that are expensive to compute and not always needed.\"\n    },\n    {\n      \"id\": \"kotlin-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Expression bodies:** Use `= expression` for single-expression functions. Omit braces and `return`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Default parameter values:** Use default values instead of overloaded functions.\"\n    },\n    {\n      \"id\": \"kotlin-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Named arguments:** Use named arguments when calling functions with many parameters or when the parameter type is a primitive and meaning is unclear.\"\n    },\n    {\n      \"id\": \"kotlin-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Extension functions:** Use extension functions to add behavior to existing types without subclassing. Place extensions in a file named `TypeExtensions.kt`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Scope functions (`let`, `run`, `with`, `apply`, `also`):**\"\n    },\n    {\n      \"id\": \"kotlin-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`apply`: Configure an object — use when returning the object. (`builder.apply { ... }`)\"\n    },\n    {\n      \"id\": \"kotlin-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`also`: Perform side effects — use when returning the object. (`list.also { log(it) }`)\"\n    },\n    {\n      \"id\": \"kotlin-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`let`: Transform a value, especially with nullability. (`str?.let { process(it) }`)\"\n    },\n    {\n      \"id\": \"kotlin-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`run`/`with`: Group a block of operations on an object. Prefer `run` for nullable receivers, `with` for non-null.\"\n    },\n    {\n      \"id\": \"kotlin-rule-33\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Do not nest scope functions more than 2 levels deep.**\"\n    },\n    {\n      \"id\": \"kotlin-rule-34\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`operator fun`:** Only implement operator overloads when the semantics are universally clear (e.g., `+` for numeric types, `get`/`set` for collections).\"\n    },\n    {\n      \"id\": \"kotlin-rule-35\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Immutable collections:** Prefer `listOf`, `mapOf`, `setOf`. Use `mutableListOf` etc. only when mutation is needed.\"\n    },\n    {\n      \"id\": \"kotlin-rule-36\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Sequence` for lazy pipelines:** Use `asSequence()` for long transformation chains on large collections to avoid creating intermediate lists.\"\n    },\n    {\n      \"id\": \"kotlin-rule-37\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `null` in collections:** Prefer `emptyList()` over `null` for absent collections. Use `filterNotNull()` to clean mixed lists.\"\n    },\n    {\n      \"id\": \"kotlin-rule-38\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`associate`, `groupBy`, `partition`:** Use the stdlib collection transforms instead of manual loops.\"\n    },\n    {\n      \"id\": \"kotlin-rule-39\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Destructuring:** Use destructuring declarations in `for` loops and `map` operations: `for ((key, value) in map)`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-40\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`suspend` functions:** Mark any function that calls another `suspend` function or performs async I/O as `suspend`. Do not block inside `suspend` functions — use `withContext(Dispatchers.IO)`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-41\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Structured concurrency:** Always launch coroutines in a `CoroutineScope`. Never use `GlobalScope` in application code.\"\n    },\n    {\n      \"id\": \"kotlin-rule-42\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Dispatchers.Main` / `.IO` / `.Default`:** Use the correct dispatcher — `Main` for UI, `IO` for blocking I/O, `Default` for CPU work.\"\n    },\n    {\n      \"id\": \"kotlin-rule-43\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Flow`:** Use `Flow<T>` for streams of values. Use `StateFlow`/`SharedFlow` for hot streams. Call `.collect {}` in a lifecycle-aware scope.\"\n    },\n    {\n      \"id\": \"kotlin-rule-44\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Error handling:** Use `try/catch` inside coroutines. Use `CoroutineExceptionHandler` for unhandled exceptions in `launch`. `async`/`await` propagates exceptions at the `await` site.\"\n    },\n    {\n      \"id\": \"kotlin-rule-45\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`viewModelScope` / `lifecycleScope`:** Use these scopes in Android (see Android guide) to tie coroutine lifetime to the component.\"\n    },\n    {\n      \"id\": \"kotlin-rule-46\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Exceptions for exceptional cases:** Do not use exceptions for normal control flow. Return `Result<T>`, `sealed class`, or nullable values for expected failure paths.\"\n    },\n    {\n      \"id\": \"kotlin-rule-47\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`runCatching`:** Use `runCatching { }` for concise try-catch with `Result<T>` return.\"\n    },\n    {\n      \"id\": \"kotlin-rule-48\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid catching `Throwable`:** Catch specific exception types. Catching `Throwable` also catches `Error` (e.g., `OutOfMemoryError`).\"\n    },\n    {\n      \"id\": \"kotlin-rule-49\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**KDoc (`/** */`):** Use KDoc for all public API documentation.\"\n    },\n    {\n      \"id\": \"kotlin-rule-50\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@param`, `@return`, `@throws`:** Include these tags for public functions with non-obvious parameters or return values.\"\n    },\n    {\n      \"id\": \"kotlin-rule-51\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@since`:** Mark API additions with `@since <version>`.\"\n    },\n    {\n      \"id\": \"kotlin-rule-52\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid redundant comments:** Do not restate the function name in the doc. Add information about behavior, edge cases, or constraints.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/python.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"python\",\n  \"title\": \"Google Python Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the Google Python Style Guide.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Python Language Rules\",\n      \"body\": \"- **Linting:** Run `pylint` on your code to catch bugs and style issues.\\n- **Imports:** Use `import x` for packages/modules. Use `from x import y` only when `y` is a submodule.\\n- **Exceptions:** Use built-in exception classes. Do not use bare `except:` clauses.\\n- **Global State:** Avoid mutable global state. Module-level constants are okay and should be `ALL_CAPS_WITH_UNDERSCORES`.\\n- **Comprehensions:** Use for simple cases. Avoid for complex logic where a full loop is more readable.\\n- **Default Argument Values:** Do not use mutable objects (like `[]` or `{}`) as default values.\\n- **True/False Evaluations:** Use implicit false (e.g., `if not my_list:`). Use `if foo is None:` to check for `None`.\\n- **Type Annotations:** Strongly encouraged for all public APIs.\"\n    },\n    {\n      \"heading\": \"2. Python Style Rules\",\n      \"body\": \"- **Line Length:** Maximum 80 characters.\\n- **Indentation:** 4 spaces per indentation level. Never use tabs.\\n- **Blank Lines:** Two blank lines between top-level definitions (classes, functions). One blank line between method definitions.\\n- **Whitespace:** Avoid extraneous whitespace. Surround binary operators with single spaces.\\n- **Docstrings:** Use `\\\"\\\"\\\"triple double quotes\\\"\\\"\\\"`. Every public module, function, class, and method must have a docstring.\\n  - **Format:** Start with a one-line summary. Include `Args:`, `Returns:`, and `Raises:` sections.\\n- **Strings:** Use f-strings for formatting. Be consistent with single (`'`) or double (`\\\"`) quotes.\\n- **`TODO` Comments:** Use `TODO(username): Fix this.` format.\\n- **Imports Formatting:** Imports should be on separate lines and grouped: standard library, third-party, and your own application's imports.\"\n    },\n    {\n      \"heading\": \"3. Naming\",\n      \"body\": \"- **General:** `snake_case` for modules, functions, methods, and variables.\\n- **Classes:** `PascalCase`.\\n- **Constants:** `ALL_CAPS_WITH_UNDERSCORES`.\\n- **Internal Use:** Use a single leading underscore (`_internal_variable`) for internal module/class members.\"\n    },\n    {\n      \"heading\": \"4. Main\",\n      \"body\": \"- All executable files should have a `main()` function that contains the main logic, called from a `if __name__ == '__main__':` block.\\n\\n**BE CONSISTENT.** When editing code, match the existing style.\\n\\n*Source: [Google Python Style Guide](https://google.github.io/styleguide/pyguide.html)*\"\n    }\n  ],\n  \"languages\": [\n    \"python\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"python-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Linting:** Run `pylint` on your code to catch bugs and style issues.\"\n    },\n    {\n      \"id\": \"python-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Imports:** Use `import x` for packages/modules. Use `from x import y` only when `y` is a submodule.\"\n    },\n    {\n      \"id\": \"python-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Exceptions:** Use built-in exception classes. Do not use bare `except:` clauses.\"\n    },\n    {\n      \"id\": \"python-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Global State:** Avoid mutable global state. Module-level constants are okay and should be `ALL_CAPS_WITH_UNDERSCORES`.\"\n    },\n    {\n      \"id\": \"python-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Comprehensions:** Use for simple cases. Avoid for complex logic where a full loop is more readable.\"\n    },\n    {\n      \"id\": \"python-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Default Argument Values:** Do not use mutable objects (like `[]` or `{}`) as default values.\"\n    },\n    {\n      \"id\": \"python-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**True/False Evaluations:** Use implicit false (e.g., `if not my_list:`). Use `if foo is None:` to check for `None`.\"\n    },\n    {\n      \"id\": \"python-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Type Annotations:** Strongly encouraged for all public APIs.\"\n    },\n    {\n      \"id\": \"python-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Line Length:** Maximum 80 characters.\"\n    },\n    {\n      \"id\": \"python-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** 4 spaces per indentation level. Never use tabs.\"\n    },\n    {\n      \"id\": \"python-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Blank Lines:** Two blank lines between top-level definitions (classes, functions). One blank line between method definitions.\"\n    },\n    {\n      \"id\": \"python-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Whitespace:** Avoid extraneous whitespace. Surround binary operators with single spaces.\"\n    },\n    {\n      \"id\": \"python-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Docstrings:** Use `\\\"\\\"\\\"triple double quotes\\\"\\\"\\\"`. Every public module, function, class, and method must have a docstring.\"\n    },\n    {\n      \"id\": \"python-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Format:** Start with a one-line summary. Include `Args:`, `Returns:`, and `Raises:` sections.\"\n    },\n    {\n      \"id\": \"python-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Strings:** Use f-strings for formatting. Be consistent with single (`'`) or double (`\\\"`) quotes.\"\n    },\n    {\n      \"id\": \"python-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`TODO` Comments:** Use `TODO(username): Fix this.` format.\"\n    },\n    {\n      \"id\": \"python-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Imports Formatting:** Imports should be on separate lines and grouped: standard library, third-party, and your own application's imports.\"\n    },\n    {\n      \"id\": \"python-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**General:** `snake_case` for modules, functions, methods, and variables.\"\n    },\n    {\n      \"id\": \"python-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Classes:** `PascalCase`.\"\n    },\n    {\n      \"id\": \"python-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Constants:** `ALL_CAPS_WITH_UNDERSCORES`.\"\n    },\n    {\n      \"id\": \"python-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Internal Use:** Use a single leading underscore (`_internal_variable`) for internal module/class members.\"\n    },\n    {\n      \"id\": \"python-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"All executable files should have a `main()` function that contains the main logic, called from a `if __name__ == '__main__':` block.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/rust.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"rust\",\n  \"title\": \"Effective Rust Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the official Rust API Guidelines, Rust Style Guide, and idiomatic Rust conventions for writing safe, performant, and maintainable Rust code.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting\",\n      \"body\": \"- **`rustfmt`:** All Rust code **must** be formatted with `rustfmt`. This is non-negotiable. Run via `cargo fmt`.\\n- **`clippy`:** Run `cargo clippy` and fix all warnings before review. Treat lints as errors in CI: `RUSTFLAGS=\\\"-D warnings\\\"`.\\n- **Indentation:** 4 spaces. No tabs.\\n- **Line Length:** 100 characters maximum (rustfmt default).\"\n    },\n    {\n      \"heading\": \"2. Naming\",\n      \"body\": \"- **`snake_case`:** For functions, methods, variables, modules, and crates.\\n- **`UpperCamelCase`:** For types, traits, enums, and struct names.\\n- **`SCREAMING_SNAKE_CASE`:** For constants and statics.\\n- **`snake_case`:** For crate names (hyphens in `Cargo.toml` map to underscores in code).\\n- **Acronyms:** Treat acronyms as words — `HttpClient`, not `HTTPClient`; `JsonParser`, not `JSONParser`.\\n- **Getters:** Do not prefix with `get_`. A getter for `name` is `name()`, not `get_name()`.\\n- **Setters:** Use `set_name()`.\\n- **Builders:** Use the `Builder` suffix and return `Self` or `Result<T>` from `build()`.\\n- **Iterator adaptors:** Name the struct for its iterator, e.g., `Lines` for a `lines()` method.\"\n    },\n    {\n      \"heading\": \"3. Ownership and Borrowing\",\n      \"body\": \"- **Prefer borrowing over cloning:** Pass `&T` or `&mut T` unless ownership transfer is required.\\n- **Prefer `&str` over `&String`:** Accept `&str` in function arguments; callers with `String` can deref. Same for `&[T]` over `&Vec<T>` and `&Path` over `&PathBuf`.\\n- **Minimize `clone()`:** A `clone()` call is a code smell if it can be avoided with a borrow.\\n- **Avoid `unwrap()` in library code:** Use `?` to propagate errors. Reserve `unwrap()` for prototypes, tests, or genuinely infallible paths (document why).\\n- **Use `expect()` over `unwrap()`** when a panic is intended: `expect(\\\"config file must exist\\\")` produces better diagnostics.\"\n    },\n    {\n      \"heading\": \"4. Error Handling\",\n      \"body\": \"- **`Result<T, E>` is the standard:** Use it for all fallible functions. Never silently discard errors.\\n- **`?` operator:** Use `?` to propagate errors up the call stack. Do not write `match` blocks for simple propagation.\\n- **Error types:** In libraries, define a custom error type implementing `std::error::Error`. Use `thiserror` crate. In applications, use `anyhow` for ergonomic error context.\\n- **`panic!`:** Reserved for unrecoverable programming errors (invariant violations). Libraries must not panic in normal operation.\\n- **`unwrap_or_else`, `map`, `and_then`:** Chain `Result`/`Option` combinators for concise, readable error paths.\"\n    },\n    {\n      \"heading\": \"5. Types and Traits\",\n      \"body\": \"- **Newtype pattern:** Wrap primitive types in newtypes to enforce invariants: `struct Meters(f64);`.\\n- **Derive common traits:** Derive `Debug`, `Clone`, `PartialEq`, `Eq`, `Hash`, `Default` where appropriate. Always derive `Debug`.\\n- **`Default` trait:** Implement `Default` for types that have a sensible zero/empty value. Use `#[derive(Default)]` when possible.\\n- **Trait objects vs. generics:** Prefer generics (`fn foo<T: Trait>(x: T)`) for performance. Use `dyn Trait` only when type erasure is needed (heterogeneous collections, dynamic dispatch).\\n- **`impl Trait` in return position:** Use for simple cases to hide concrete return types without boxing.\\n- **Sealed traits:** Use the sealed-trait pattern to prevent external implementations of traits not designed for extension.\\n- **Iterator:** Implement `Iterator` for custom collections. Return `impl Iterator` from methods rather than concrete types.\"\n    },\n    {\n      \"heading\": \"6. Enums and Pattern Matching\",\n      \"body\": \"- **Enums over booleans:** Prefer `enum Direction { Left, Right }` over `bool` for clarity.\\n- **`match` is exhaustive:** Always handle all variants. Prefer explicit arms over `_ =>` wildcards when variants may be added.\\n- **`if let` / `while let`:** Use for single-pattern matching instead of a full `match` block.\\n- **`Option` instead of null:** Never model absence with sentinel values. Use `Option<T>`.\\n- **Avoid nested `match`:** Flatten with combinators (`map`, `and_then`, `unwrap_or`) or `?`.\"\n    },\n    {\n      \"heading\": \"7. Concurrency\",\n      \"body\": \"- **Fearless concurrency:** Let the compiler enforce thread safety via `Send` and `Sync` bounds.\\n- **Prefer channels over shared state:** Use `std::sync::mpsc` or `tokio::sync` channels.\\n- **`Arc<Mutex<T>>`:** Standard pattern for shared mutable state. Lock for the minimum scope needed.\\n- **`async`/`await`:** Use for I/O-bound concurrency. Use `tokio` or `async-std` as the runtime. Do not block inside async functions — use `tokio::task::spawn_blocking` for CPU-bound work.\\n- **`rayon`:** Use for data-parallel CPU-bound work instead of manual threading.\"\n    },\n    {\n      \"heading\": \"8. Modules and Visibility\",\n      \"body\": \"- **`pub` is an API contract:** Only `pub` what is intended to be part of the public API. Default to private.\\n- **`pub(crate)`:** Use for items shared within a crate but not externally.\\n- **`mod.rs` vs. inline modules:** Prefer `module/mod.rs` for multi-file modules. Prefer inline `mod` for small, single-file submodules.\\n- **`use` declarations:** Group imports: std → external crates → local. Use `rustfmt` to sort.\\n- **Re-exports:** Re-export public types at the crate root to flatten the API surface.\"\n    },\n    {\n      \"heading\": \"9. Performance\",\n      \"body\": \"- **Zero-cost abstractions:** Trust iterators, closures, and generics — they compile to the same code as manual loops.\\n- **Avoid unnecessary allocation:** Prefer stack types and slices. Profile before switching to `Box`, `Vec`, or `Arc`.\\n- **`Cow<str>` / `Cow<[T]>`:** Use when a function sometimes needs to allocate and sometimes doesn't.\\n- **Inline:** Use `#[inline]` on small hot functions. Use `#[inline(always)]` sparingly.\"\n    },\n    {\n      \"heading\": \"10. Documentation\",\n      \"body\": \"- **`///` doc comments:** All public items **must** have doc comments. Include an example in `# Examples` for non-trivial functions.\\n- **`//!` module docs:** Add module-level documentation explaining purpose and usage.\\n- **`# Panics` / `# Errors` / `# Safety`:** Document these sections on any function that panics, returns `Result`, or is `unsafe`.\\n- **`cargo doc --open`:** Verify documentation renders correctly before publishing.\\n- **`unsafe` blocks:** Every `unsafe` block **must** have a `// SAFETY:` comment explaining why the invariants are upheld.\\n\\n*Sources: [Rust API Guidelines](https://rust-lang.github.io/api-guidelines/) · [Rust Style Guide](https://doc.rust-lang.org/nightly/style-guide/) · [The Rust Book](https://doc.rust-lang.org/book/)*\"\n    }\n  ],\n  \"languages\": [\n    \"rust\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"rust-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`rustfmt`:** All Rust code **must** be formatted with `rustfmt`. This is non-negotiable. Run via `cargo fmt`.\"\n    },\n    {\n      \"id\": \"rust-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`clippy`:** Run `cargo clippy` and fix all warnings before review. Treat lints as errors in CI: `RUSTFLAGS=\\\"-D warnings\\\"`.\"\n    },\n    {\n      \"id\": \"rust-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** 4 spaces. No tabs.\"\n    },\n    {\n      \"id\": \"rust-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Line Length:** 100 characters maximum (rustfmt default).\"\n    },\n    {\n      \"id\": \"rust-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`snake_case`:** For functions, methods, variables, modules, and crates.\"\n    },\n    {\n      \"id\": \"rust-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`UpperCamelCase`:** For types, traits, enums, and struct names.\"\n    },\n    {\n      \"id\": \"rust-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`SCREAMING_SNAKE_CASE`:** For constants and statics.\"\n    },\n    {\n      \"id\": \"rust-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`snake_case`:** For crate names (hyphens in `Cargo.toml` map to underscores in code).\"\n    },\n    {\n      \"id\": \"rust-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Acronyms:** Treat acronyms as words — `HttpClient`, not `HTTPClient`; `JsonParser`, not `JSONParser`.\"\n    },\n    {\n      \"id\": \"rust-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Getters:** Do not prefix with `get_`. A getter for `name` is `name()`, not `get_name()`.\"\n    },\n    {\n      \"id\": \"rust-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Setters:** Use `set_name()`.\"\n    },\n    {\n      \"id\": \"rust-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Builders:** Use the `Builder` suffix and return `Self` or `Result<T>` from `build()`.\"\n    },\n    {\n      \"id\": \"rust-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Iterator adaptors:** Name the struct for its iterator, e.g., `Lines` for a `lines()` method.\"\n    },\n    {\n      \"id\": \"rust-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Prefer borrowing over cloning:** Pass `&T` or `&mut T` unless ownership transfer is required.\"\n    },\n    {\n      \"id\": \"rust-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Prefer `&str` over `&String`:** Accept `&str` in function arguments; callers with `String` can deref. Same for `&[T]` over `&Vec<T>` and `&Path` over `&PathBuf`.\"\n    },\n    {\n      \"id\": \"rust-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Minimize `clone()`:** A `clone()` call is a code smell if it can be avoided with a borrow.\"\n    },\n    {\n      \"id\": \"rust-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `unwrap()` in library code:** Use `?` to propagate errors. Reserve `unwrap()` for prototypes, tests, or genuinely infallible paths (document why).\"\n    },\n    {\n      \"id\": \"rust-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Use `expect()` over `unwrap()`** when a panic is intended: `expect(\\\"config file must exist\\\")` produces better diagnostics.\"\n    },\n    {\n      \"id\": \"rust-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Result<T, E>` is the standard:** Use it for all fallible functions. Never silently discard errors.\"\n    },\n    {\n      \"id\": \"rust-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`?` operator:** Use `?` to propagate errors up the call stack. Do not write `match` blocks for simple propagation.\"\n    },\n    {\n      \"id\": \"rust-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Error types:** In libraries, define a custom error type implementing `std::error::Error`. Use `thiserror` crate. In applications, use `anyhow` for ergonomic error context.\"\n    },\n    {\n      \"id\": \"rust-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`panic!`:** Reserved for unrecoverable programming errors (invariant violations). Libraries must not panic in normal operation.\"\n    },\n    {\n      \"id\": \"rust-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`unwrap_or_else`, `map`, `and_then`:** Chain `Result`/`Option` combinators for concise, readable error paths.\"\n    },\n    {\n      \"id\": \"rust-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Newtype pattern:** Wrap primitive types in newtypes to enforce invariants: `struct Meters(f64);`.\"\n    },\n    {\n      \"id\": \"rust-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Derive common traits:** Derive `Debug`, `Clone`, `PartialEq`, `Eq`, `Hash`, `Default` where appropriate. Always derive `Debug`.\"\n    },\n    {\n      \"id\": \"rust-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Default` trait:** Implement `Default` for types that have a sensible zero/empty value. Use `#[derive(Default)]` when possible.\"\n    },\n    {\n      \"id\": \"rust-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Trait objects vs. generics:** Prefer generics (`fn foo<T: Trait>(x: T)`) for performance. Use `dyn Trait` only when type erasure is needed (heterogeneous collections, dynamic dispatch).\"\n    },\n    {\n      \"id\": \"rust-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`impl Trait` in return position:** Use for simple cases to hide concrete return types without boxing.\"\n    },\n    {\n      \"id\": \"rust-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Sealed traits:** Use the sealed-trait pattern to prevent external implementations of traits not designed for extension.\"\n    },\n    {\n      \"id\": \"rust-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Iterator:** Implement `Iterator` for custom collections. Return `impl Iterator` from methods rather than concrete types.\"\n    },\n    {\n      \"id\": \"rust-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Enums over booleans:** Prefer `enum Direction { Left, Right }` over `bool` for clarity.\"\n    },\n    {\n      \"id\": \"rust-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`match` is exhaustive:** Always handle all variants. Prefer explicit arms over `_ =>` wildcards when variants may be added.\"\n    },\n    {\n      \"id\": \"rust-rule-33\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`if let` / `while let`:** Use for single-pattern matching instead of a full `match` block.\"\n    },\n    {\n      \"id\": \"rust-rule-34\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Option` instead of null:** Never model absence with sentinel values. Use `Option<T>`.\"\n    },\n    {\n      \"id\": \"rust-rule-35\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid nested `match`:** Flatten with combinators (`map`, `and_then`, `unwrap_or`) or `?`.\"\n    },\n    {\n      \"id\": \"rust-rule-36\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Fearless concurrency:** Let the compiler enforce thread safety via `Send` and `Sync` bounds.\"\n    },\n    {\n      \"id\": \"rust-rule-37\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Prefer channels over shared state:** Use `std::sync::mpsc` or `tokio::sync` channels.\"\n    },\n    {\n      \"id\": \"rust-rule-38\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Arc<Mutex<T>>`:** Standard pattern for shared mutable state. Lock for the minimum scope needed.\"\n    },\n    {\n      \"id\": \"rust-rule-39\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`async`/`await`:** Use for I/O-bound concurrency. Use `tokio` or `async-std` as the runtime. Do not block inside async functions — use `tokio::task::spawn_blocking` for CPU-bound work.\"\n    },\n    {\n      \"id\": \"rust-rule-40\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`rayon`:** Use for data-parallel CPU-bound work instead of manual threading.\"\n    },\n    {\n      \"id\": \"rust-rule-41\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`pub` is an API contract:** Only `pub` what is intended to be part of the public API. Default to private.\"\n    },\n    {\n      \"id\": \"rust-rule-42\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`pub(crate)`:** Use for items shared within a crate but not externally.\"\n    },\n    {\n      \"id\": \"rust-rule-43\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`mod.rs` vs. inline modules:** Prefer `module/mod.rs` for multi-file modules. Prefer inline `mod` for small, single-file submodules.\"\n    },\n    {\n      \"id\": \"rust-rule-44\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`use` declarations:** Group imports: std → external crates → local. Use `rustfmt` to sort.\"\n    },\n    {\n      \"id\": \"rust-rule-45\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Re-exports:** Re-export public types at the crate root to flatten the API surface.\"\n    },\n    {\n      \"id\": \"rust-rule-46\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Zero-cost abstractions:** Trust iterators, closures, and generics — they compile to the same code as manual loops.\"\n    },\n    {\n      \"id\": \"rust-rule-47\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid unnecessary allocation:** Prefer stack types and slices. Profile before switching to `Box`, `Vec`, or `Arc`.\"\n    },\n    {\n      \"id\": \"rust-rule-48\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Cow<str>` / `Cow<[T]>`:** Use when a function sometimes needs to allocate and sometimes doesn't.\"\n    },\n    {\n      \"id\": \"rust-rule-49\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Inline:** Use `#[inline]` on small hot functions. Use `#[inline(always)]` sparingly.\"\n    },\n    {\n      \"id\": \"rust-rule-50\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`///` doc comments:** All public items **must** have doc comments. Include an example in `# Examples` for non-trivial functions.\"\n    },\n    {\n      \"id\": \"rust-rule-51\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`//!` module docs:** Add module-level documentation explaining purpose and usage.\"\n    },\n    {\n      \"id\": \"rust-rule-52\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`# Panics` / `# Errors` / `# Safety`:** Document these sections on any function that panics, returns `Result`, or is `unsafe`.\"\n    },\n    {\n      \"id\": \"rust-rule-53\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`cargo doc --open`:** Verify documentation renders correctly before publishing.\"\n    },\n    {\n      \"id\": \"rust-rule-54\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`unsafe` blocks:** Every `unsafe` block **must** have a `// SAFETY:` comment explaining why the invariants are upheld.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/swift.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"swift\",\n  \"title\": \"Swift API Design Guidelines Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the Swift API Design Guidelines and Swift.org style conventions for writing idiomatic, safe, and expressive Swift code.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting\",\n      \"body\": \"- **SwiftFormat / SwiftLint:** Format with `swiftformat` and lint with `swiftlint`. Enforce both in CI. Include `.swiftlint.yml` in the repository root.\\n- **Indentation:** 4 spaces. No tabs.\\n- **Line Length:** 120 characters maximum (SwiftLint default; adjust to team preference).\\n- **Blank lines:** One blank line between declarations. Two blank lines between major sections (e.g., before `// MARK:` sections).\"\n    },\n    {\n      \"heading\": \"2. Naming\",\n      \"body\": \"- **`UpperCamelCase`:** For types (class, struct, enum, protocol, typealias) and enum cases.\\n- **`lowerCamelCase`:** For functions, methods, properties, parameters, and local variables.\\n- **Clarity at the call site:** Name things so usage reads like prose: `list.insert(element, at: index)`, not `list.insert(element, position: index)`.\\n- **Omit needless words:** Do not repeat the type in the name. `backgroundColor`, not `backgroundColorOfView`. `users.remove(at: index)`, not `users.removeElement(at: index)`.\\n- **Boolean properties:** Start with `is`, `has`, `can`, or `should`: `isEnabled`, `hasChildren`, `canClose`.\\n- **Protocol naming:**\\n  - Protocols describing what something *is*: noun (`Collection`, `Sequence`).\\n  - Protocols describing a *capability*: `-able`, `-ible`, `-ing` suffix (`Hashable`, `Codable`, `Equatable`).\\n- **Avoid abbreviations:** Use full words. `destinationURL` not `destURL`, `error` not `err`, `index` not `idx`. Well-known abbreviations (`URL`, `ID`, `JSON`) are fine.\\n- **First argument label:** Omit when it is obviously part of the function name: `x.addSubview(y)` not `x.addSubview(view: y)`. Include when it adds clarity: `a.move(from: b, to: c)`.\"\n    },\n    {\n      \"heading\": \"3. Types\",\n      \"body\": \"- **`struct` by default:** Prefer value types (`struct`, `enum`) over reference types (`class`). Use `class` only when identity semantics, inheritance, or reference sharing is required.\\n- **`final` by default:** Mark classes `final` unless designed for subclassing. Subclassable classes are a design decision, not a default.\\n- **`enum` for closed sets:** Use `enum` for a fixed set of related values. Use `CaseIterable` when you need to enumerate cases.\\n- **Associated values:** Use `enum` with associated values as discriminated unions: `enum Result<T> { case success(T); case failure(Error) }`.\\n- **`typealias`:** Use to clarify the role of complex types: `typealias Completion = (Result<User, Error>) -> Void`.\\n- **`extension` over subclass:** Prefer extending a type to add behavior over subclassing. Extensions cannot store state — use composition for that.\"\n    },\n    {\n      \"heading\": \"4. Optionals\",\n      \"body\": \"- **Avoid `!` force-unwrap:** Force-unwrap is a crash waiting to happen. Use `guard let`, `if let`, or `??` instead.\\n- **`guard let` for early exit:** Use `guard let` to unwrap optionals at the top of a function when `nil` means \\\"cannot proceed\\\". This reduces nesting.\\n- **`if let` for optional use:** Use `if let` when the `nil` case is a legitimate alternate path.\\n- **Optional chaining (`?.`):** Use for accessing properties/methods on optionals where `nil` propagation is correct.\\n- **`??` for defaults:** Use the nil-coalescing operator for fallback values: `let name = user?.name ?? \\\"Unknown\\\"`.\\n- **Implicit optionals (`!` type):** Use `@IBOutlet weak var button: UIButton!` and similarly initialized-before-use patterns — keep to `@IBOutlet` and injection patterns only. Never use `!` types for general logic.\"\n    },\n    {\n      \"heading\": \"5. Functions and Closures\",\n      \"body\": \"- **Single responsibility:** Each function does one thing. Break down large functions.\\n- **`@discardableResult`:** Add only when ignoring the result is a common and correct pattern. Do not add it to silence warnings mechanically.\\n- **Trailing closure syntax:** Use trailing closure syntax when the closure is the last parameter and its role is clear: `UIView.animate(withDuration: 0.3) { ... }`.\\n- **`@escaping`:** Mark closures `@escaping` when they outlive the function call. Document the lifecycle implications.\\n- **`inout`:** Use `inout` sparingly. Prefer returning a new value over mutating in place unless performance is critical.\\n- **`throws` / `async throws`:** Use `throws` for functions that can fail with typed errors. Use `async` for asynchronous work. Combine as `async throws` for async-fallible functions.\"\n    },\n    {\n      \"heading\": \"6. Error Handling\",\n      \"body\": \"- **`throws` + `try`:** Use Swift's typed error handling for recoverable errors. Functions that can fail return via `throw`, not via optional or sentinel values.\\n- **`do / catch`:** Handle errors at the appropriate layer. Do not `try!` unless a failure is genuinely impossible and you can prove it.\\n- **`Result<T, Error>`:** Use `Result` for asynchronous completions that predate `async/await`, or when you need to store a result for later.\\n- **Custom error types:** Define `enum MyError: Error, LocalizedError` with associated values for structured error information. Implement `errorDescription` for user-facing messages.\\n- **`try?`:** Use when `nil` on failure is acceptable and the error is irrelevant to the caller.\"\n    },\n    {\n      \"heading\": \"7. Concurrency (Swift Concurrency)\",\n      \"body\": \"- **`async`/`await`:** Prefer Swift Concurrency over completion handlers, Combine, or DispatchQueue-based async code for new code.\\n- **`Task { }`:** Use to bridge from synchronous to async context. Use `Task.detached` only when the task must not inherit actor context.\\n- **`@MainActor`:** Annotate types or functions that must run on the main thread. Use `@MainActor` on ViewModels/Controllers rather than `DispatchQueue.main.async`.\\n- **`actor`:** Use `actor` types for shared mutable state. Actors serialize access and eliminate data races.\\n- **`Sendable`:** Mark types crossing actor boundaries as `Sendable`. Do not suppress `Sendable` warnings with `@unchecked Sendable` without justification.\\n- **`async let`:** Use `async let` for independent concurrent work within a function: `async let user = fetchUser(); async let feed = fetchFeed(); let (u, f) = try await (user, feed)`.\"\n    },\n    {\n      \"heading\": \"8. Memory Management\",\n      \"body\": \"- **ARC is automatic:** Do not call `retain`/`release`. ARC handles memory.\\n- **`weak` for delegates and callbacks:** Mark delegate properties `weak var` to avoid retain cycles. Capture `[weak self]` in closures that outlive the object.\\n- **`unowned` only when guaranteed:** Use `unowned` instead of `weak` only when the referenced object is guaranteed to outlive the closure/property. Incorrect `unowned` crashes; incorrect `weak` just produces `nil`.\\n- **`[weak self]` + guard:** Pattern: `{ [weak self] in guard let self = self else { return } ... }` (or `[weak self] in self?.doWork()`).\"\n    },\n    {\n      \"heading\": \"9. Collections and Functional Patterns\",\n      \"body\": \"- **`map`, `filter`, `compactMap`, `reduce`:** Use functional transforms over `for` loops for collection transformations.\\n- **`compactMap`:** Use to filter `nil` from a sequence of optionals: `names.compactMap { user(id: $0)?.name }`.\\n- **`flatMap` on sequences:** Use to flatten sequences: `[[1,2],[3,4]].flatMap { $0 }` → `[1,2,3,4]`.\\n- **`lazy`:** Use `collection.lazy.filter { }.map { }` to avoid allocating intermediate arrays for long pipelines.\"\n    },\n    {\n      \"heading\": \"10. Documentation\",\n      \"body\": \"- **`///` doc comments:** Use triple-slash comments for all public API. Xcode renders these in Quick Help.\\n- **Summary + discussion:** First line is the summary. Add a blank line then the discussion for longer explanations.\\n- **`- Parameter`, `- Returns`, `- Throws`:** Document parameters, return values, and thrown errors using these list items.\\n- **`- Note:`, `- Important:`, `- Warning:`:** Use callout keywords for additional reader guidance.\\n- **Code examples:** Include brief usage examples in doc comments for complex APIs using fenced code blocks.\\n\\n*Sources: [Swift API Design Guidelines](https://swift.org/documentation/api-design-guidelines/) · [Swift.org Style Guide](https://google.github.io/swift/) · [Swift Concurrency](https://docs.swift.org/swift-book/documentation/the-swift-programming-language/concurrency/)*\"\n    }\n  ],\n  \"languages\": [\n    \"swift\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"swift-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**SwiftFormat / SwiftLint:** Format with `swiftformat` and lint with `swiftlint`. Enforce both in CI. Include `.swiftlint.yml` in the repository root.\"\n    },\n    {\n      \"id\": \"swift-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Indentation:** 4 spaces. No tabs.\"\n    },\n    {\n      \"id\": \"swift-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Line Length:** 120 characters maximum (SwiftLint default; adjust to team preference).\"\n    },\n    {\n      \"id\": \"swift-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Blank lines:** One blank line between declarations. Two blank lines between major sections (e.g., before `// MARK:` sections).\"\n    },\n    {\n      \"id\": \"swift-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`UpperCamelCase`:** For types (class, struct, enum, protocol, typealias) and enum cases.\"\n    },\n    {\n      \"id\": \"swift-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`lowerCamelCase`:** For functions, methods, properties, parameters, and local variables.\"\n    },\n    {\n      \"id\": \"swift-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Clarity at the call site:** Name things so usage reads like prose: `list.insert(element, at: index)`, not `list.insert(element, position: index)`.\"\n    },\n    {\n      \"id\": \"swift-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Omit needless words:** Do not repeat the type in the name. `backgroundColor`, not `backgroundColorOfView`. `users.remove(at: index)`, not `users.removeElement(at: index)`.\"\n    },\n    {\n      \"id\": \"swift-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Boolean properties:** Start with `is`, `has`, `can`, or `should`: `isEnabled`, `hasChildren`, `canClose`.\"\n    },\n    {\n      \"id\": \"swift-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Protocol naming:**\"\n    },\n    {\n      \"id\": \"swift-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Protocols describing what something *is*: noun (`Collection`, `Sequence`).\"\n    },\n    {\n      \"id\": \"swift-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Protocols describing a *capability*: `-able`, `-ible`, `-ing` suffix (`Hashable`, `Codable`, `Equatable`).\"\n    },\n    {\n      \"id\": \"swift-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid abbreviations:** Use full words. `destinationURL` not `destURL`, `error` not `err`, `index` not `idx`. Well-known abbreviations (`URL`, `ID`, `JSON`) are fine.\"\n    },\n    {\n      \"id\": \"swift-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**First argument label:** Omit when it is obviously part of the function name: `x.addSubview(y)` not `x.addSubview(view: y)`. Include when it adds clarity: `a.move(from: b, to: c)`.\"\n    },\n    {\n      \"id\": \"swift-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`struct` by default:** Prefer value types (`struct`, `enum`) over reference types (`class`). Use `class` only when identity semantics, inheritance, or reference sharing is required.\"\n    },\n    {\n      \"id\": \"swift-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`final` by default:** Mark classes `final` unless designed for subclassing. Subclassable classes are a design decision, not a default.\"\n    },\n    {\n      \"id\": \"swift-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`enum` for closed sets:** Use `enum` for a fixed set of related values. Use `CaseIterable` when you need to enumerate cases.\"\n    },\n    {\n      \"id\": \"swift-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Associated values:** Use `enum` with associated values as discriminated unions: `enum Result<T> { case success(T); case failure(Error) }`.\"\n    },\n    {\n      \"id\": \"swift-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`typealias`:** Use to clarify the role of complex types: `typealias Completion = (Result<User, Error>) -> Void`.\"\n    },\n    {\n      \"id\": \"swift-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`extension` over subclass:** Prefer extending a type to add behavior over subclassing. Extensions cannot store state — use composition for that.\"\n    },\n    {\n      \"id\": \"swift-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `!` force-unwrap:** Force-unwrap is a crash waiting to happen. Use `guard let`, `if let`, or `??` instead.\"\n    },\n    {\n      \"id\": \"swift-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`guard let` for early exit:** Use `guard let` to unwrap optionals at the top of a function when `nil` means \\\"cannot proceed\\\". This reduces nesting.\"\n    },\n    {\n      \"id\": \"swift-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`if let` for optional use:** Use `if let` when the `nil` case is a legitimate alternate path.\"\n    },\n    {\n      \"id\": \"swift-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Optional chaining (`?.`):** Use for accessing properties/methods on optionals where `nil` propagation is correct.\"\n    },\n    {\n      \"id\": \"swift-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`??` for defaults:** Use the nil-coalescing operator for fallback values: `let name = user?.name ?? \\\"Unknown\\\"`.\"\n    },\n    {\n      \"id\": \"swift-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Implicit optionals (`!` type):** Use `@IBOutlet weak var button: UIButton!` and similarly initialized-before-use patterns — keep to `@IBOutlet` and injection patterns only. Never use `!` types for general logic.\"\n    },\n    {\n      \"id\": \"swift-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Single responsibility:** Each function does one thing. Break down large functions.\"\n    },\n    {\n      \"id\": \"swift-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@discardableResult`:** Add only when ignoring the result is a common and correct pattern. Do not add it to silence warnings mechanically.\"\n    },\n    {\n      \"id\": \"swift-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Trailing closure syntax:** Use trailing closure syntax when the closure is the last parameter and its role is clear: `UIView.animate(withDuration: 0.3) { ... }`.\"\n    },\n    {\n      \"id\": \"swift-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@escaping`:** Mark closures `@escaping` when they outlive the function call. Document the lifecycle implications.\"\n    },\n    {\n      \"id\": \"swift-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`inout`:** Use `inout` sparingly. Prefer returning a new value over mutating in place unless performance is critical.\"\n    },\n    {\n      \"id\": \"swift-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`throws` / `async throws`:** Use `throws` for functions that can fail with typed errors. Use `async` for asynchronous work. Combine as `async throws` for async-fallible functions.\"\n    },\n    {\n      \"id\": \"swift-rule-33\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`throws` + `try`:** Use Swift's typed error handling for recoverable errors. Functions that can fail return via `throw`, not via optional or sentinel values.\"\n    },\n    {\n      \"id\": \"swift-rule-34\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`do / catch`:** Handle errors at the appropriate layer. Do not `try!` unless a failure is genuinely impossible and you can prove it.\"\n    },\n    {\n      \"id\": \"swift-rule-35\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Result<T, Error>`:** Use `Result` for asynchronous completions that predate `async/await`, or when you need to store a result for later.\"\n    },\n    {\n      \"id\": \"swift-rule-36\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Custom error types:** Define `enum MyError: Error, LocalizedError` with associated values for structured error information. Implement `errorDescription` for user-facing messages.\"\n    },\n    {\n      \"id\": \"swift-rule-37\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`try?`:** Use when `nil` on failure is acceptable and the error is irrelevant to the caller.\"\n    },\n    {\n      \"id\": \"swift-rule-38\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`async`/`await`:** Prefer Swift Concurrency over completion handlers, Combine, or DispatchQueue-based async code for new code.\"\n    },\n    {\n      \"id\": \"swift-rule-39\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Task { }`:** Use to bridge from synchronous to async context. Use `Task.detached` only when the task must not inherit actor context.\"\n    },\n    {\n      \"id\": \"swift-rule-40\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@MainActor`:** Annotate types or functions that must run on the main thread. Use `@MainActor` on ViewModels/Controllers rather than `DispatchQueue.main.async`.\"\n    },\n    {\n      \"id\": \"swift-rule-41\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`actor`:** Use `actor` types for shared mutable state. Actors serialize access and eliminate data races.\"\n    },\n    {\n      \"id\": \"swift-rule-42\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Sendable`:** Mark types crossing actor boundaries as `Sendable`. Do not suppress `Sendable` warnings with `@unchecked Sendable` without justification.\"\n    },\n    {\n      \"id\": \"swift-rule-43\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`async let`:** Use `async let` for independent concurrent work within a function: `async let user = fetchUser(); async let feed = fetchFeed(); let (u, f) = try await (user, feed)`.\"\n    },\n    {\n      \"id\": \"swift-rule-44\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**ARC is automatic:** Do not call `retain`/`release`. ARC handles memory.\"\n    },\n    {\n      \"id\": \"swift-rule-45\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`weak` for delegates and callbacks:** Mark delegate properties `weak var` to avoid retain cycles. Capture `[weak self]` in closures that outlive the object.\"\n    },\n    {\n      \"id\": \"swift-rule-46\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`unowned` only when guaranteed:** Use `unowned` instead of `weak` only when the referenced object is guaranteed to outlive the closure/property. Incorrect `unowned` crashes; incorrect `weak` just produces `nil`.\"\n    },\n    {\n      \"id\": \"swift-rule-47\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`[weak self]` + guard:** Pattern: `{ [weak self] in guard let self = self else { return } ... }` (or `[weak self] in self?.doWork()`).\"\n    },\n    {\n      \"id\": \"swift-rule-48\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`map`, `filter`, `compactMap`, `reduce`:** Use functional transforms over `for` loops for collection transformations.\"\n    },\n    {\n      \"id\": \"swift-rule-49\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`compactMap`:** Use to filter `nil` from a sequence of optionals: `names.compactMap { user(id: $0)?.name }`.\"\n    },\n    {\n      \"id\": \"swift-rule-50\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`flatMap` on sequences:** Use to flatten sequences: `[[1,2],[3,4]].flatMap { $0 }` → `[1,2,3,4]`.\"\n    },\n    {\n      \"id\": \"swift-rule-51\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`lazy`:** Use `collection.lazy.filter { }.map { }` to avoid allocating intermediate arrays for long pipelines.\"\n    },\n    {\n      \"id\": \"swift-rule-52\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`///` doc comments:** Use triple-slash comments for all public API. Xcode renders these in Quick Help.\"\n    },\n    {\n      \"id\": \"swift-rule-53\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Summary + discussion:** First line is the summary. Add a blank line then the discussion for longer explanations.\"\n    },\n    {\n      \"id\": \"swift-rule-54\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`- Parameter`, `- Returns`, `- Throws`:** Document parameters, return values, and thrown errors using these list items.\"\n    },\n    {\n      \"id\": \"swift-rule-55\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`- Note:`, `- Important:`, `- Warning:`:** Use callout keywords for additional reader guidance.\"\n    },\n    {\n      \"id\": \"swift-rule-56\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Code examples:** Include brief usage examples in doc comments for complex APIs using fenced code blocks.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/swiftui.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"swiftui\",\n  \"title\": \"Effective SwiftUI Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices for writing idiomatic SwiftUI code, building on Swift API Design Guidelines with SwiftUI-specific view, state, and composition conventions.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Formatting and Tooling\",\n      \"body\": \"- **SwiftFormat:** Format all SwiftUI code with `swiftformat`. Add `.swiftformat` config to the repo.\\n- **SwiftLint:** Enforce with `.swiftlint.yml`. Treat all errors as CI failures.\\n- **Trailing closures and `ViewBuilder`:** Use trailing closure syntax for all `ViewBuilder` content parameters — this is the SwiftUI idiom.\\n- **`// MARK:` sections:** Organize view files with `// MARK: - Body`, `// MARK: - Subviews`, `// MARK: - View Model` sections for long files.\"\n    },\n    {\n      \"heading\": \"2. View Design\",\n      \"body\": \"- **Small, composable views:** Decompose complex `body` implementations into private child views or separate `View` structs. A `body` over ~30 lines should be split.\\n- **`some View` return type:** Always use `some View` as the return type of `body`. Do not erase to `AnyView` without a concrete reason.\\n- **`AnyView` avoidance:** `AnyView` disables SwiftUI's diff engine. Use conditional views, `Group`, or `@ViewBuilder` functions instead. If `AnyView` is unavoidable, contain it to a single call site.\\n- **`@ViewBuilder` functions:** Extract reusable sub-trees into `@ViewBuilder` private methods or standalone `View` structs. Prefer structs for views reused across files.\\n- **Previews:** Every view **must** have a `#Preview` (or `PreviewProvider`). Include multiple preview variants: light/dark, different data states (empty, loaded, error), and different device sizes.\"\n    },\n    {\n      \"heading\": \"3. State Management\",\n      \"body\": \"- **Choose the right property wrapper:**\\n  - `@State`: Local, view-owned, value-type state. Keep it minimal and view-specific.\\n  - `@Binding`: Pass mutable state down from a parent. Never derive a `@Binding` from a computed property.\\n  - `@StateObject`: Own a reference-type observable object for the lifetime of this view subtree.\\n  - `@ObservedObject`: Receive an observable object from a parent. The parent owns its lifetime.\\n  - `@EnvironmentObject`: Inject a shared object through the view hierarchy. Use for app-wide state (user session, theme, router).\\n  - `@Environment`: Read system values (`\\\\.colorScheme`, `\\\\.dismiss`, `\\\\.locale`) and custom environment keys.\\n- **Observation (`@Observable`):** In iOS 17+, prefer `@Observable` macro over `ObservableObject`/`@Published` — it is more efficient (only the accessed properties trigger redraws).\\n- **Minimize `@State`:** Lift state up to the smallest common ancestor that needs it. Do not put everything in the root view.\\n- **No logic in `body`:** Move business logic, data transformations, and side effects into the ViewModel or a computed property. `body` should be pure view construction.\"\n    },\n    {\n      \"heading\": \"4. ViewModel Pattern\",\n      \"body\": \"- **`@Observable` / `ObservableObject` ViewModel:** Each screen has a ViewModel that is `@Observable` (iOS 17+) or `ObservableObject`. The view is a thin rendering layer.\\n- **`@StateObject` for injection:** Create the ViewModel in the view that owns it with `@StateObject var vm = MyViewModel()`. Pass to children via `@ObservedObject` or `@EnvironmentObject`.\\n- **`@MainActor` on ViewModels:** Annotate the ViewModel class with `@MainActor` to ensure all published property updates occur on the main thread.\\n- **Async tasks via `Task`:** Trigger async operations from `.task { }` modifier (cancelled automatically on view disappearance) or `Task { }` in response to user actions.\\n- **`.onAppear` vs `.task`:** Prefer `.task { }` for async work (auto-cancels). Use `.onAppear` only for synchronous side effects.\"\n    },\n    {\n      \"heading\": \"5. Layout and Composition\",\n      \"body\": \"- **Use modifiers in the right order:** Modifier order matters in SwiftUI. Background and border go before padding; padding before frame.\\n  ```swift\\n  Text(\\\"Hello\\\")\\n      .background(Color.blue)  // applies to text bounds\\n      .padding()               // then pads\\n      .border(Color.red)       // border around padded area\\n  ```\\n- **`Group` for applying modifiers to multiple views:** Use `Group { }` to apply the same modifier to several views without a container.\\n- **`ViewThatFits`:** Use for adaptive layouts that switch between compact and expanded presentations.\\n- **`LazyVStack` / `LazyHStack` / `LazyVGrid`:** Use lazy containers for long or dynamically-loaded lists. Use eager `VStack`/`HStack` only for short, fixed-count content.\\n- **`GeometryReader` sparingly:** `GeometryReader` takes all available space and can break layouts. Prefer `containerRelativeFrame` (iOS 17+), `frame(maxWidth: .infinity)`, or `Layout` protocol instead.\\n- **`Spacer()` vs fixed spacing:** Use `Spacer()` to push views apart in a stack. Use `.padding()` or `.frame(height: 8)` for fixed spacing.\"\n    },\n    {\n      \"heading\": \"6. Navigation\",\n      \"body\": \"- **`NavigationStack`:** Use `NavigationStack` (iOS 16+) over `NavigationView`. Use path-based navigation for programmatic control: `NavigationStack(path: $path)`.\\n- **`navigationDestination(for:)`:** Register destinations with `.navigationDestination(for: MyType.self)` and drive navigation by appending to the path.\\n- **`NavigationPath`:** Use for heterogeneous navigation stacks. Use typed `[MyRoute]` arrays when all destinations are the same type.\\n- **`.sheet`, `.fullScreenCover`, `.popover`:** Control presentation with `@State var isPresented = false` and `item:`-based variants for associated data.\\n- **Avoid `NavigationLink(isActive:)`:** Deprecated in iOS 16+. Use path-based navigation instead.\"\n    },\n    {\n      \"heading\": \"7. Animation\",\n      \"body\": \"- **`withAnimation { }`:** Wrap state changes that should animate in `withAnimation`. Prefer this over `.animation(_:value:)` on views.\\n- **`.animation(_:value:)`:** Use on a view when only that view should animate in response to a specific state value changing.\\n- **Explicit animations over implicit:** Prefer `withAnimation(.spring())` triggered by state changes over `.animation(.spring())` on views — explicit animations are more predictable.\\n- **`matchedGeometryEffect`:** Use for hero animations between views. Always pair with a shared `@Namespace`.\\n- **`Animatable` and `AnimatableModifier`:** Implement for custom animations on non-standard properties.\"\n    },\n    {\n      \"heading\": \"8. Performance\",\n      \"body\": \"- **`Equatable` views:** Make views `Equatable` and use `.equatable()` to skip redraws when input is unchanged.\\n- **Stable identities in `ForEach`:** Pass stable `Identifiable` models or explicit `id:` parameters. Avoid `ForEach(items, id: \\\\.self)` for complex objects — use a proper `id` property.\\n- **Avoid heavy computation in `body`:** Any expensive computation in `body` runs on every redraw. Move to a stored property or the ViewModel.\\n- **`@Observable` over `ObservableObject`:** `@Observable` tracks property access precisely — only views that read a changed property redraw. `@Published` triggers redraws for all observers of any property change.\\n- **`task(id:)`:** Use `task(id: someValue) { }` to restart async work when a value changes, instead of `onChange(of:) { Task { ... } }`.\"\n    },\n    {\n      \"heading\": \"9. Accessibility\",\n      \"body\": \"- **`.accessibilityLabel`:** Every interactive element and image must have an accessibility label.\\n- **`.accessibilityHint`:** Add hints for non-obvious actions: `.accessibilityHint(\\\"Double-tap to expand details\\\")`.\\n- **`.accessibilityElement(children: .combine)`:** Group visually related elements into a single accessible element.\\n- **Dynamic Type:** Use semantic text styles (`.font(.headline)`) not fixed sizes. Test with the largest and smallest Dynamic Type sizes.\\n- **Color contrast:** Do not convey information with color alone. Use labels, icons, or patterns as secondary indicators.\"\n    },\n    {\n      \"heading\": \"10. File and Code Organization\",\n      \"body\": \"- **One view per file:** Each top-level `View` struct lives in its own file.\\n- **File naming:** File name matches the type name: `UserProfileView.swift`.\\n- **Extension for subviews:** Place private subviews in an `extension` at the bottom of the same file:\\n  ```swift\\n  // MARK: - Subviews\\n  private extension UserProfileView {\\n      var headerSection: some View { ... }\\n  }\\n  ```\\n- **Feature folder structure:** `Features/Auth/`, `Features/Home/` — group screens, ViewModels, and models by feature.\\n- **Shared components:** Place reusable design-system components in `Components/` or a dedicated Swift package.\\n\\n*Sources: [SwiftUI Documentation](https://developer.apple.com/documentation/swiftui) · [Swift API Design Guidelines](https://swift.org/documentation/api-design-guidelines/) · [Observation framework](https://developer.apple.com/documentation/observation)*\"\n    }\n  ],\n  \"languages\": [\n    \"swiftui\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"swiftui-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**SwiftFormat:** Format all SwiftUI code with `swiftformat`. Add `.swiftformat` config to the repo.\"\n    },\n    {\n      \"id\": \"swiftui-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**SwiftLint:** Enforce with `.swiftlint.yml`. Treat all errors as CI failures.\"\n    },\n    {\n      \"id\": \"swiftui-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Trailing closures and `ViewBuilder`:** Use trailing closure syntax for all `ViewBuilder` content parameters — this is the SwiftUI idiom.\"\n    },\n    {\n      \"id\": \"swiftui-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`// MARK:` sections:** Organize view files with `// MARK: - Body`, `// MARK: - Subviews`, `// MARK: - View Model` sections for long files.\"\n    },\n    {\n      \"id\": \"swiftui-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Small, composable views:** Decompose complex `body` implementations into private child views or separate `View` structs. A `body` over ~30 lines should be split.\"\n    },\n    {\n      \"id\": \"swiftui-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`some View` return type:** Always use `some View` as the return type of `body`. Do not erase to `AnyView` without a concrete reason.\"\n    },\n    {\n      \"id\": \"swiftui-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`AnyView` avoidance:** `AnyView` disables SwiftUI's diff engine. Use conditional views, `Group`, or `@ViewBuilder` functions instead. If `AnyView` is unavoidable, contain it to a single call site.\"\n    },\n    {\n      \"id\": \"swiftui-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@ViewBuilder` functions:** Extract reusable sub-trees into `@ViewBuilder` private methods or standalone `View` structs. Prefer structs for views reused across files.\"\n    },\n    {\n      \"id\": \"swiftui-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Previews:** Every view **must** have a `#Preview` (or `PreviewProvider`). Include multiple preview variants: light/dark, different data states (empty, loaded, error), and different device sizes.\"\n    },\n    {\n      \"id\": \"swiftui-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Choose the right property wrapper:**\"\n    },\n    {\n      \"id\": \"swiftui-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`@State`: Local, view-owned, value-type state. Keep it minimal and view-specific.\"\n    },\n    {\n      \"id\": \"swiftui-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`@Binding`: Pass mutable state down from a parent. Never derive a `@Binding` from a computed property.\"\n    },\n    {\n      \"id\": \"swiftui-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`@StateObject`: Own a reference-type observable object for the lifetime of this view subtree.\"\n    },\n    {\n      \"id\": \"swiftui-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`@ObservedObject`: Receive an observable object from a parent. The parent owns its lifetime.\"\n    },\n    {\n      \"id\": \"swiftui-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`@EnvironmentObject`: Inject a shared object through the view hierarchy. Use for app-wide state (user session, theme, router).\"\n    },\n    {\n      \"id\": \"swiftui-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"`@Environment`: Read system values (`\\\\.colorScheme`, `\\\\.dismiss`, `\\\\.locale`) and custom environment keys.\"\n    },\n    {\n      \"id\": \"swiftui-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Observation (`@Observable`):** In iOS 17+, prefer `@Observable` macro over `ObservableObject`/`@Published` — it is more efficient (only the accessed properties trigger redraws).\"\n    },\n    {\n      \"id\": \"swiftui-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Minimize `@State`:** Lift state up to the smallest common ancestor that needs it. Do not put everything in the root view.\"\n    },\n    {\n      \"id\": \"swiftui-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**No logic in `body`:** Move business logic, data transformations, and side effects into the ViewModel or a computed property. `body` should be pure view construction.\"\n    },\n    {\n      \"id\": \"swiftui-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@Observable` / `ObservableObject` ViewModel:** Each screen has a ViewModel that is `@Observable` (iOS 17+) or `ObservableObject`. The view is a thin rendering layer.\"\n    },\n    {\n      \"id\": \"swiftui-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@StateObject` for injection:** Create the ViewModel in the view that owns it with `@StateObject var vm = MyViewModel()`. Pass to children via `@ObservedObject` or `@EnvironmentObject`.\"\n    },\n    {\n      \"id\": \"swiftui-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@MainActor` on ViewModels:** Annotate the ViewModel class with `@MainActor` to ensure all published property updates occur on the main thread.\"\n    },\n    {\n      \"id\": \"swiftui-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Async tasks via `Task`:** Trigger async operations from `.task { }` modifier (cancelled automatically on view disappearance) or `Task { }` in response to user actions.\"\n    },\n    {\n      \"id\": \"swiftui-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`.onAppear` vs `.task`:** Prefer `.task { }` for async work (auto-cancels). Use `.onAppear` only for synchronous side effects.\"\n    },\n    {\n      \"id\": \"swiftui-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Use modifiers in the right order:** Modifier order matters in SwiftUI. Background and border go before padding; padding before frame.\"\n    },\n    {\n      \"id\": \"swiftui-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Group` for applying modifiers to multiple views:** Use `Group { }` to apply the same modifier to several views without a container.\"\n    },\n    {\n      \"id\": \"swiftui-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`ViewThatFits`:** Use for adaptive layouts that switch between compact and expanded presentations.\"\n    },\n    {\n      \"id\": \"swiftui-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`LazyVStack` / `LazyHStack` / `LazyVGrid`:** Use lazy containers for long or dynamically-loaded lists. Use eager `VStack`/`HStack` only for short, fixed-count content.\"\n    },\n    {\n      \"id\": \"swiftui-rule-29\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`GeometryReader` sparingly:** `GeometryReader` takes all available space and can break layouts. Prefer `containerRelativeFrame` (iOS 17+), `frame(maxWidth: .infinity)`, or `Layout` protocol instead.\"\n    },\n    {\n      \"id\": \"swiftui-rule-30\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Spacer()` vs fixed spacing:** Use `Spacer()` to push views apart in a stack. Use `.padding()` or `.frame(height: 8)` for fixed spacing.\"\n    },\n    {\n      \"id\": \"swiftui-rule-31\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`NavigationStack`:** Use `NavigationStack` (iOS 16+) over `NavigationView`. Use path-based navigation for programmatic control: `NavigationStack(path: $path)`.\"\n    },\n    {\n      \"id\": \"swiftui-rule-32\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`navigationDestination(for:)`:** Register destinations with `.navigationDestination(for: MyType.self)` and drive navigation by appending to the path.\"\n    },\n    {\n      \"id\": \"swiftui-rule-33\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`NavigationPath`:** Use for heterogeneous navigation stacks. Use typed `[MyRoute]` arrays when all destinations are the same type.\"\n    },\n    {\n      \"id\": \"swiftui-rule-34\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`.sheet`, `.fullScreenCover`, `.popover`:** Control presentation with `@State var isPresented = false` and `item:`-based variants for associated data.\"\n    },\n    {\n      \"id\": \"swiftui-rule-35\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid `NavigationLink(isActive:)`:** Deprecated in iOS 16+. Use path-based navigation instead.\"\n    },\n    {\n      \"id\": \"swiftui-rule-36\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`withAnimation { }`:** Wrap state changes that should animate in `withAnimation`. Prefer this over `.animation(_:value:)` on views.\"\n    },\n    {\n      \"id\": \"swiftui-rule-37\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`.animation(_:value:)`:** Use on a view when only that view should animate in response to a specific state value changing.\"\n    },\n    {\n      \"id\": \"swiftui-rule-38\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Explicit animations over implicit:** Prefer `withAnimation(.spring())` triggered by state changes over `.animation(.spring())` on views — explicit animations are more predictable.\"\n    },\n    {\n      \"id\": \"swiftui-rule-39\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`matchedGeometryEffect`:** Use for hero animations between views. Always pair with a shared `@Namespace`.\"\n    },\n    {\n      \"id\": \"swiftui-rule-40\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Animatable` and `AnimatableModifier`:** Implement for custom animations on non-standard properties.\"\n    },\n    {\n      \"id\": \"swiftui-rule-41\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Equatable` views:** Make views `Equatable` and use `.equatable()` to skip redraws when input is unchanged.\"\n    },\n    {\n      \"id\": \"swiftui-rule-42\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Stable identities in `ForEach`:** Pass stable `Identifiable` models or explicit `id:` parameters. Avoid `ForEach(items, id: \\\\.self)` for complex objects — use a proper `id` property.\"\n    },\n    {\n      \"id\": \"swiftui-rule-43\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Avoid heavy computation in `body`:** Any expensive computation in `body` runs on every redraw. Move to a stored property or the ViewModel.\"\n    },\n    {\n      \"id\": \"swiftui-rule-44\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`@Observable` over `ObservableObject`:** `@Observable` tracks property access precisely — only views that read a changed property redraw. `@Published` triggers redraws for all observers of any property change.\"\n    },\n    {\n      \"id\": \"swiftui-rule-45\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`task(id:)`:** Use `task(id: someValue) { }` to restart async work when a value changes, instead of `onChange(of:) { Task { ... } }`.\"\n    },\n    {\n      \"id\": \"swiftui-rule-46\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`.accessibilityLabel`:** Every interactive element and image must have an accessibility label.\"\n    },\n    {\n      \"id\": \"swiftui-rule-47\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`.accessibilityHint`:** Add hints for non-obvious actions: `.accessibilityHint(\\\"Double-tap to expand details\\\")`.\"\n    },\n    {\n      \"id\": \"swiftui-rule-48\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`.accessibilityElement(children: .combine)`:** Group visually related elements into a single accessible element.\"\n    },\n    {\n      \"id\": \"swiftui-rule-49\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Dynamic Type:** Use semantic text styles (`.font(.headline)`) not fixed sizes. Test with the largest and smallest Dynamic Type sizes.\"\n    },\n    {\n      \"id\": \"swiftui-rule-50\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Color contrast:** Do not convey information with color alone. Use labels, icons, or patterns as secondary indicators.\"\n    },\n    {\n      \"id\": \"swiftui-rule-51\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**One view per file:** Each top-level `View` struct lives in its own file.\"\n    },\n    {\n      \"id\": \"swiftui-rule-52\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**File naming:** File name matches the type name: `UserProfileView.swift`.\"\n    },\n    {\n      \"id\": \"swiftui-rule-53\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Extension for subviews:** Place private subviews in an `extension` at the bottom of the same file:\"\n    },\n    {\n      \"id\": \"swiftui-rule-54\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Feature folder structure:** `Features/Auth/`, `Features/Home/` — group screens, ViewModels, and models by feature.\"\n    },\n    {\n      \"id\": \"swiftui-rule-55\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Shared components:** Place reusable design-system components in `Components/` or a dedicated Swift package.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","styleguides/typescript.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.styleguide.v1\",\n  \"kind\": \"styleguide\",\n  \"id\": \"typescript\",\n  \"title\": \"Google TypeScript Style Guide Summary\",\n  \"summary\": \"This document summarizes key rules and best practices from the Google TypeScript Style Guide, which is enforced by the `gts` tool.\",\n  \"sections\": [\n    {\n      \"heading\": \"1. Language Features\",\n      \"body\": \"- **Variable Declarations:** Always use `const` or `let`. **`var` is forbidden.** Use `const` by default.\\n- **Modules:** Use ES6 modules (`import`/`export`). **Do not use `namespace`.**\\n- **Exports:** Use named exports (`export {MyClass};`). **Do not use default exports.**\\n- **Classes:**\\n  - **Do not use `#private` fields.** Use TypeScript's `private` visibility modifier.\\n  - Mark properties never reassigned outside the constructor with `readonly`.\\n  - **Never use the `public` modifier** (it's the default). Restrict visibility with `private` or `protected` where possible.\\n- **Functions:** Prefer function declarations for named functions. Use arrow functions for anonymous functions/callbacks.\\n- **String Literals:** Use single quotes (`'`). Use template literals (`` ` ``) for interpolation and multi-line strings.\\n- **Equality Checks:** Always use triple equals (`===`) and not equals (`!==`).\\n- **Type Assertions:** **Avoid type assertions (`x as SomeType`) and non-nullability assertions (`y!`)**. If you must use them, provide a clear justification.\"\n    },\n    {\n      \"heading\": \"2. Disallowed Features\",\n      \"body\": \"- **`any` Type:** **Avoid `any`**. Prefer `unknown` or a more specific type.\\n- **Wrapper Objects:** Do not instantiate `String`, `Boolean`, or `Number` wrapper classes.\\n- **Automatic Semicolon Insertion (ASI):** Do not rely on it. **Explicitly end all statements with a semicolon.**\\n- **`const enum`:** Do not use `const enum`. Use plain `enum` instead.\\n- **`eval()` and `Function(...string)`:** Forbidden.\"\n    },\n    {\n      \"heading\": \"3. Naming\",\n      \"body\": \"- **`UpperCamelCase`:** For classes, interfaces, types, enums, and decorators.\\n- **`lowerCamelCase`:** For variables, parameters, functions, methods, and properties.\\n- **`CONSTANT_CASE`:** For global constant values, including enum values.\\n- **`_` Prefix/Suffix:** **Do not use `_` as a prefix or suffix** for identifiers, including for private properties.\"\n    },\n    {\n      \"heading\": \"4. Type System\",\n      \"body\": \"- **Type Inference:** Rely on type inference for simple, obvious types. Be explicit for complex types.\\n- **`undefined` and `null`:** Both are supported. Be consistent within your project.\\n- **Optional vs. `|undefined`:** Prefer optional parameters and fields (`?`) over adding `|undefined` to the type.\\n- **`Array<T>` Type:** Use `T[]` for simple types. Use `Array<T>` for more complex union types (e.g., `Array<string | number>`).\\n- **`{}` Type:** **Do not use `{}`**. Prefer `unknown`, `Record<string, unknown>`, or `object`.\"\n    },\n    {\n      \"heading\": \"5. Comments and Documentation\",\n      \"body\": \"- **JSDoc:** Use `/** JSDoc */` for documentation, `//` for implementation comments.\\n- **Redundancy:** **Do not declare types in `@param` or `@return` blocks** (e.g., `/** @param {string} user */`). This is redundant in TypeScript.\\n- **Add Information:** Comments must add information, not just restate the code.\\n\\n*Source: [Google TypeScript Style Guide](https://google.github.io/styleguide/tsguide.html)*\"\n    }\n  ],\n  \"languages\": [\n    \"typescript\"\n  ],\n  \"frameworks\": [],\n  \"file_patterns\": [],\n  \"applies_to\": [\n    \"planning\",\n    \"implementation\",\n    \"review\",\n    \"tests\"\n  ],\n  \"rules\": [\n    {\n      \"id\": \"typescript-rule-1\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Variable Declarations:** Always use `const` or `let`. **`var` is forbidden.** Use `const` by default.\"\n    },\n    {\n      \"id\": \"typescript-rule-2\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Modules:** Use ES6 modules (`import`/`export`). **Do not use `namespace`.**\"\n    },\n    {\n      \"id\": \"typescript-rule-3\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Exports:** Use named exports (`export {MyClass};`). **Do not use default exports.**\"\n    },\n    {\n      \"id\": \"typescript-rule-4\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Classes:**\"\n    },\n    {\n      \"id\": \"typescript-rule-5\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Do not use `#private` fields.** Use TypeScript's `private` visibility modifier.\"\n    },\n    {\n      \"id\": \"typescript-rule-6\",\n      \"severity\": \"guidance\",\n      \"summary\": \"Mark properties never reassigned outside the constructor with `readonly`.\"\n    },\n    {\n      \"id\": \"typescript-rule-7\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Never use the `public` modifier** (it's the default). Restrict visibility with `private` or `protected` where possible.\"\n    },\n    {\n      \"id\": \"typescript-rule-8\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Functions:** Prefer function declarations for named functions. Use arrow functions for anonymous functions/callbacks.\"\n    },\n    {\n      \"id\": \"typescript-rule-9\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**String Literals:** Use single quotes (`'`). Use template literals (`` ` ``) for interpolation and multi-line strings.\"\n    },\n    {\n      \"id\": \"typescript-rule-10\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Equality Checks:** Always use triple equals (`===`) and not equals (`!==`).\"\n    },\n    {\n      \"id\": \"typescript-rule-11\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Type Assertions:** **Avoid type assertions (`x as SomeType`) and non-nullability assertions (`y!`)**. If you must use them, provide a clear justification.\"\n    },\n    {\n      \"id\": \"typescript-rule-12\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`any` Type:** **Avoid `any`**. Prefer `unknown` or a more specific type.\"\n    },\n    {\n      \"id\": \"typescript-rule-13\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Wrapper Objects:** Do not instantiate `String`, `Boolean`, or `Number` wrapper classes.\"\n    },\n    {\n      \"id\": \"typescript-rule-14\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Automatic Semicolon Insertion (ASI):** Do not rely on it. **Explicitly end all statements with a semicolon.**\"\n    },\n    {\n      \"id\": \"typescript-rule-15\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`const enum`:** Do not use `const enum`. Use plain `enum` instead.\"\n    },\n    {\n      \"id\": \"typescript-rule-16\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`eval()` and `Function(...string)`:** Forbidden.\"\n    },\n    {\n      \"id\": \"typescript-rule-17\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`UpperCamelCase`:** For classes, interfaces, types, enums, and decorators.\"\n    },\n    {\n      \"id\": \"typescript-rule-18\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`lowerCamelCase`:** For variables, parameters, functions, methods, and properties.\"\n    },\n    {\n      \"id\": \"typescript-rule-19\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`CONSTANT_CASE`:** For global constant values, including enum values.\"\n    },\n    {\n      \"id\": \"typescript-rule-20\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`_` Prefix/Suffix:** **Do not use `_` as a prefix or suffix** for identifiers, including for private properties.\"\n    },\n    {\n      \"id\": \"typescript-rule-21\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Type Inference:** Rely on type inference for simple, obvious types. Be explicit for complex types.\"\n    },\n    {\n      \"id\": \"typescript-rule-22\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`undefined` and `null`:** Both are supported. Be consistent within your project.\"\n    },\n    {\n      \"id\": \"typescript-rule-23\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Optional vs. `|undefined`:** Prefer optional parameters and fields (`?`) over adding `|undefined` to the type.\"\n    },\n    {\n      \"id\": \"typescript-rule-24\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`Array<T>` Type:** Use `T[]` for simple types. Use `Array<T>` for more complex union types (e.g., `Array<string | number>`).\"\n    },\n    {\n      \"id\": \"typescript-rule-25\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**`{}` Type:** **Do not use `{}`**. Prefer `unknown`, `Record<string, unknown>`, or `object`.\"\n    },\n    {\n      \"id\": \"typescript-rule-26\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**JSDoc:** Use `/** JSDoc */` for documentation, `//` for implementation comments.\"\n    },\n    {\n      \"id\": \"typescript-rule-27\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Redundancy:** **Do not declare types in `@param` or `@return` blocks** (e.g., `/** @param {string} user */`). This is redundant in TypeScript.\"\n    },\n    {\n      \"id\": \"typescript-rule-28\",\n      \"severity\": \"guidance\",\n      \"summary\": \"**Add Information:** Comments must add information, not just restate the code.\"\n    }\n  ],\n  \"source\": \"bundled_template\"\n}\n","workflow.json":"{\n  \"version\": 1,\n  \"schema\": \"cadre.workflow.v1\",\n  \"kind\": \"workflow\",\n  \"title\": \"Project Workflow\",\n  \"summary\": \"\",\n  \"sections\": [\n    {\n      \"heading\": \"Guiding Principles\",\n      \"body\": \"1. **Cadre State Is Packet-Owned:** Cadre records progress through MCP\\n   task-completion, mutation, workflow, review, native event/message, and validation packets.\\n2. **The JSON Plan Is Authoritative:** `plan.json` describes the work. `plan.md`\\n   is generated for human review only and must not drive runtime behavior.\\n3. **The Tech Stack Is Deliberate:** Changes to the tech stack must be reflected\\n   in Cadre context through the appropriate packet before implementation relies on\\n   them.\\n4. **Test-Driven Development:** Write tests before implementing behavior whenever\\n   the task is testable.\\n5. **High Code Coverage:** Aim for more than 80 percent coverage for new or\\n   changed modules unless the packet records an approved exception.\\n6. **User Experience First:** Product decisions should prioritize the user's\\n   workflow, clarity, and failure recovery.\\n7. **Non-Interactive and CI-Aware:** Prefer one-shot project commands for tests,\\n   builds, linters, and formatters.\"\n    },\n    {\n      \"heading\": \"Task Lifecycle\",\n      \"body\": \"All Cadre orchestration uses MCP packets. If a required packet returns `ok:false`,\\nhalt and report the packet error instead of recreating Cadre state logic outside\\nMCP.\\n\\n1. **Select Work:** Use Cadre status or implementation-prep packets to select the\\n   next available track, phase, or task.\\n2. **Claim Work:** Use the implementation workflow packet or mutation packet to\\n   claim ownership before product edits begin.\\n3. **Red Phase:** Add or update product tests that express the expected behavior.\\n   Run the relevant project test command and confirm the new test fails for the\\n   intended reason when practical.\\n4. **Green Phase:** Implement the smallest product change that satisfies the test\\n   and acceptance criteria.\\n5. **Refactor:** Improve clarity, duplication, performance, and accessibility\\n   while preserving behavior.\\n6. **Verify:** Run targeted tests first, then broader project checks according to\\n   risk. Include coverage when the project has coverage tooling.\\n7. **Commit Product Work:** Make clear product commits for the completed unit of\\n   work according to the team's normal commit policy.\\n8. **Record Completion:** Call the Cadre task-completion packet with test,\\n   coverage, commit, and summary evidence. The packet updates plan state, metadata,\\n   journals, events, and indexes.\"\n    },\n    {\n      \"heading\": \"Commit Discipline\",\n      \"body\": \"- Prefer one product commit per completed task or coherent implementation\\n  checkpoint.\\n- For larger tasks, commit after each significant tested change instead of\\n  batching an entire track into one commit.\\n- Keep commit messages task-aware by mentioning the track id, phase/task intent,\\n  or user-visible behavior changed.\\n- After each task-level commit, call the Cadre task-completion packet with the\\n  commit SHA, tests run, coverage when available, files changed, and a concise\\n  implementation note. Cadre records that evidence in track metadata, journals,\\n  and events.\\n- Do not mark a task complete until its commit evidence and verification summary\\n  have been recorded through Cadre.\"\n    },\n    {\n      \"heading\": \"Polyrepo Notes\",\n      \"body\": \"In polyrepo mode, Cadre packets identify the repo, branch, worktree, task, and\\ncommit evidence for each product repository. Product commands run in the\\nappropriate repo worktree. Cadre control-plane synchronization, cross-repo review\\nlinks, fleet status, and merge-train metadata are packet-owned.\\n\\nIn monorepo mode, the same packet lifecycle applies with a single product repo.\"\n    },\n    {\n      \"heading\": \"Quality Gates\",\n      \"body\": \"Before marking any task complete through Cadre packets, verify:\\n\\n- Tests for the changed behavior pass.\\n- Coverage meets the project's threshold or the packet records an approved reason.\\n- Code follows the relevant style guide in `styleguides/*.json`; Markdown under\\n  `code_styleguides/` is generated for human readability only.\\n- Public APIs are documented when the project convention requires it.\\n- Type checks, lint checks, and static analysis pass when configured.\\n- User-facing changes have appropriate accessibility and responsive checks.\\n- Documentation is updated when behavior, setup, or operations changed.\\n- Security-sensitive changes receive explicit review evidence.\"\n    },\n    {\n      \"heading\": \"Phase Completion\",\n      \"body\": \"When a task completes a phase, use Cadre workflow and completion packets to gather\\nphase evidence, validate coverage, record the checkpoint, and refresh indexes.\\nManual verification plans may be summarized for the user, but Cadre checkpoint\\nstate is recorded only through packets.\"\n    },\n    {\n      \"heading\": \"Development Commands\",\n      \"body\": \"Project setup, tests, builds, linters, formatters, local servers, and product\\ncommits remain normal product work. Use the commands configured by the project and\\nkeep them non-interactive. These commands must not mutate Cadre control-plane,\\nprovider, index, review, archive, handoff, event, message, or validation state directly.\"\n    }\n  ]\n}\n"}};
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// src/cadre-job-runner.ts
var import_node_path40 = __toESM(require("node:path"));

// src/cadre-core.ts
var cadre_core_exports = {};
__export(cadre_core_exports, {
  appendCadreEvent: () => appendCadreEvent,
  appendCadreMessage: () => appendCadreMessage,
  artifactCatalog: () => artifactCatalog,
  artifactPacket: () => artifactPacket,
  artifactRender: () => artifactRender,
  artifactSchema: () => artifactSchema,
  artifactSync: () => artifactSync,
  availableWork: () => availableWork,
  claimTrack: () => claimTrack,
  collisionScan: () => collisionScan,
  completeTask: () => completeTask,
  dependencyGraph: () => dependencyGraph,
  doctor: () => doctor,
  ensureNativeState: () => ensureNativeState,
  fleetStatus: () => fleetStatus,
  heartbeatTrack: () => heartbeatTrack,
  implementationPrep: () => implementationPrep,
  integrationInventory: () => integrationInventory,
  isCadreProjectRoot: () => isCadreProjectRoot,
  liveStatus: () => liveStatus,
  loadTopology: () => loadTopology,
  lspConfigStatus: () => lspConfigStatus,
  lspImpact: () => lspImpact,
  lspReview: () => lspReview,
  lspSetup: () => lspSetup,
  mcpReadiness: () => mcpReadiness,
  metadataPatch: () => metadataPatch,
  nativeStateSummary: () => nativeStateSummary,
  parallelWorkflow: () => parallelWorkflow,
  parsePlanFile: () => parsePlanFile,
  parsePlanJson: () => parsePlanJson,
  phaseSchedule: () => phaseSchedule,
  planAssist: () => planAssist,
  planIntegrity: () => planIntegrity,
  polyrepoPreflight: () => polyrepoPreflight,
  prCiStatus: () => prCiStatus,
  providerEvidence: () => providerEvidence,
  readCadreEvents: () => readCadreEvents,
  readCadreMessages: () => readCadreMessages,
  recordParallelWorker: () => recordParallelWorker,
  recordReview: () => recordReview,
  recordTaskResult: () => recordTaskResult,
  regenIndex: () => regenIndex,
  repoMap: () => repoMap,
  reviewAssist: () => reviewAssist,
  reviewEvidence: () => reviewEvidence,
  reviewGate: () => reviewGate,
  reviewMachineGate: () => reviewMachineGate,
  setTrackStatus: () => setTrackStatus,
  syncControlPlane: () => syncControlPlane,
  teamBoard: () => teamBoard,
  teamStatus: () => teamStatus,
  techStackSummary: () => techStackSummary,
  testCoverage: () => testCoverage,
  testImpact: () => testImpact,
  trackContext: () => trackContext,
  workflowFormula: () => workflowFormula,
  workflowPacket: () => workflowPacket,
  workspaceDiagnostics: () => workspaceDiagnostics,
  workspaceHealth: () => workspaceHealth,
  worktreePlan: () => worktreePlan
});

// src/core/application/runtime/artifact-actions.ts
var import_node_fs15 = __toESM(require("node:fs"));
var import_node_path27 = __toESM(require("node:path"));

// src/guards.ts
function isRecord(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function asJsonObject(value) {
  return isRecord(value) ? value : {};
}
function asString(value, fallback = "") {
  return typeof value === "string" ? value : fallback;
}
function asOptionalString(value) {
  return typeof value === "string" ? value : void 0;
}
function asNumber(value, fallback = 0) {
  return typeof value === "number" && Number.isFinite(value) ? value : fallback;
}
function asOptionalNumber(value) {
  return typeof value === "number" && Number.isFinite(value) ? value : void 0;
}
function asBoolean(value, fallback = false) {
  return typeof value === "boolean" ? value : fallback;
}
function asStringArray(value) {
  return Array.isArray(value) ? value.filter((item) => typeof item === "string") : [];
}
function errorMessage(error) {
  return error instanceof Error ? error.message : String(error);
}
function errorCode(error) {
  return isRecord(error) && typeof error.code === "string" ? error.code : void 0;
}

// src/core/application/runtime/artifact-catalog.ts
var import_node_fs14 = __toESM(require("node:fs"));
var import_node_path26 = __toESM(require("node:path"));

// src/core/infrastructure/runtime/json-store.ts
var import_node_fs2 = __toESM(require("node:fs"));
var import_node_path3 = __toESM(require("node:path"));
var import_node_crypto = __toESM(require("node:crypto"));

// src/core/infrastructure/runtime/locking.ts
var import_node_fs = __toESM(require("node:fs"));
var import_node_path2 = __toESM(require("node:path"));
var import_node_os = __toESM(require("node:os"));

// src/core/domain/lease-policy.ts
var STALE_LEASE_MS = 30 * 60 * 1e3;
var LOCK_STALE_MS = STALE_LEASE_MS;

// src/core/infrastructure/runtime/system.ts
var import_node_path = __toESM(require("node:path"));
var import_node_child_process = require("node:child_process");
var commandExistsCache = /* @__PURE__ */ new Map();
function isCadreProjectRoot(root) {
  const cadreDir = import_node_path.default.join(root, "cadre");
  if (!fileExists(cadreDir)) return false;
  return [
    "tracks.json",
    "setup_state.json",
    "product.json",
    "tech-stack.json",
    "workflow.json",
    "config.json",
    "repos.json"
  ].some((name) => fileExists(import_node_path.default.join(cadreDir, name))) || fileExists(import_node_path.default.join(cadreDir, "tracks"));
}
function gitIdentity(root) {
  for (const key of ["user.email", "user.name"]) {
    const result = (0, import_node_child_process.spawnSync)("git", ["config", key], {
      cwd: root,
      encoding: "utf8"
    });
    if (result.status === 0 && result.stdout.trim()) {
      return result.stdout.trim();
    }
  }
  return null;
}
function runCommand(command, args, options = {}) {
  const result = (0, import_node_child_process.spawnSync)(command, args, {
    cwd: options.cwd,
    encoding: "utf8",
    shell: options.shell === true,
    timeout: options.timeoutMs,
    maxBuffer: options.maxBuffer || 10 * 1024 * 1024
  });
  const commandResult = {
    ok: result.status === 0,
    status: result.status,
    signal: result.signal || null,
    stdout: result.stdout || "",
    stderr: result.stderr || "",
    command: options.shell === true ? command : [command, ...args].join(" "),
    args
  };
  if (options.cwd !== void 0) commandResult.cwd = options.cwd;
  return commandResult;
}
function plannedGitAction(id, kind, repo, cwd, args, description) {
  return {
    id,
    kind,
    repo,
    cwd,
    command: "git",
    args,
    description
  };
}
function runPlannedGitActions(actions) {
  return actions.map((action) => runCommand(action.command, action.args, { cwd: action.cwd }));
}
function actionResultsOk(results) {
  return results.every((result) => result.ok);
}
function hasProviderEvidence(args = {}) {
  return Boolean(args.evidence || args.providerEvidence || args.provider_evidence);
}
function workflowPhaseState(args, blocked, pendingProvider = false) {
  if (blocked) return "blocked";
  if (pendingProvider) return "pending_provider";
  return args.execute === true ? "executed" : "ready";
}
function continuationToken(workflow, trackId, actions) {
  return textHash(JSON.stringify({ workflow, trackId, actions })).slice(0, 24);
}
function parsePorcelainFiles(text) {
  return String(text || "").split(/\r?\n/).filter(Boolean).flatMap((line) => {
    const raw = line.slice(3).trim();
    if (!raw) return [];
    if (raw.includes(" -> ")) return [raw.split(" -> ").pop() ?? ""];
    return [raw.replace(/^"|"$/g, "")];
  }).filter(Boolean);
}
function isControlPlaneFile(file) {
  const normalized = String(file || "").replace(/\\/g, "/").replace(/^\.\//, "");
  if (!normalized) return true;
  if (normalized.startsWith("cadre/")) return true;
  if (normalized === ".gitattributes" || normalized === ".gitmodules") return true;
  if (normalized === "cadre-merge-train.gitlab-ci.yml") return true;
  if (normalized === ".gitlab-ci.yml") return true;
  if (normalized.startsWith(".github/workflows/cadre-")) return true;
  return false;
}
function controlPlaneSyncSafety(root, mode2, remote, branch) {
  const status = runCommand("git", ["status", "--porcelain"], { cwd: root });
  const dirtyFiles = parsePorcelainFiles(status.stdout);
  const unsafeDirtyFiles = dirtyFiles.filter((file) => !isControlPlaneFile(file));
  const safety = {
    ok: true,
    mode: mode2,
    remote,
    branch,
    dirty_files: dirtyFiles,
    unsafe_dirty_files: unsafeDirtyFiles,
    ahead_files: [],
    unsafe_ahead_files: [],
    warnings: []
  };
  if (!status.ok) {
    return { ...safety, ok: false, reason: "Unable to inspect git status", status };
  }
  if (unsafeDirtyFiles.length > 0) {
    return {
      ...safety,
      ok: false,
      reason: "Working tree has non-control-plane changes; refusing control-plane sync"
    };
  }
  if (mode2 !== "post") return safety;
  const remoteRef = `${remote}/${branch}`;
  const fetch = runCommand("git", ["fetch", "--quiet", remote, branch], { cwd: root });
  const rev = runCommand("git", ["rev-parse", "--verify", remoteRef], { cwd: root });
  let diff;
  if (fetch.ok && rev.ok) {
    diff = runCommand("git", ["diff", "--name-only", `${remoteRef}..HEAD`], { cwd: root });
  } else {
    return {
      ...safety,
      ok: false,
      reason: `Unable to verify ${remoteRef}; refusing control-plane post-sync rather than classifying only the last commit`,
      fetch,
      rev
    };
  }
  const aheadFiles = diff.stdout.split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
  const unsafeAheadFiles = aheadFiles.filter((file) => !isControlPlaneFile(file));
  safety.ahead_files = aheadFiles;
  safety.unsafe_ahead_files = unsafeAheadFiles;
  if (!diff.ok) {
    return { ...safety, ok: false, reason: "Unable to classify unpushed commits", diff };
  }
  if (unsafeAheadFiles.length > 0) {
    return {
      ...safety,
      ok: false,
      reason: "Unpushed commits include non-control-plane files; refusing control-plane push"
    };
  }
  return safety;
}
function commandExists(command, cwd) {
  const key = `${process.env.PATH || ""}\0${cwd}\0${command}`;
  if (commandExistsCache.has(key)) return commandExistsCache.get(key) === true;
  const result = (0, import_node_child_process.spawnSync)("sh", ["-lc", `command -v '${String(command).replace(/'/g, "'\\''")}'`], {
    cwd,
    encoding: "utf8"
  });
  const exists = result.status === 0;
  commandExistsCache.set(key, exists);
  return exists;
}

// src/core/infrastructure/runtime/locking.ts
function lockRoot(root) {
  return import_node_path2.default.join(root, "cadre", ".locks");
}
function processAlive(pid) {
  const numeric = Number(pid);
  if (!Number.isInteger(numeric) || numeric <= 0) return false;
  try {
    process.kill(numeric, 0);
    return true;
  } catch (error) {
    return errorCode(error) === "EPERM";
  }
}
function readLockInfo(lockDir) {
  return readJson(import_node_path2.default.join(lockDir, "owner.json"), null) ?? {};
}
function lockIsStale(info, nowMs = Date.now()) {
  const stamp = Date.parse(info.updated_at || info.acquired_at || "");
  if (Number.isFinite(stamp) && nowMs - stamp > LOCK_STALE_MS) return true;
  if (info.pid && !processAlive(info.pid)) return true;
  return false;
}
function acquireLock(root, name, options = {}) {
  const now = utcNow();
  const dir = import_node_path2.default.join(lockRoot(root), `${safeName(name)}.lock`);
  import_node_fs.default.mkdirSync(import_node_path2.default.dirname(dir), { recursive: true });
  for (let attempt = 1; attempt <= Number(options.retries || 3); attempt += 1) {
    try {
      import_node_fs.default.mkdirSync(dir);
      const info = {
        name,
        pid: process.pid,
        owner: options.owner || gitIdentity(root) || null,
        acquired_at: now,
        updated_at: now,
        hostname: import_node_os.default.hostname()
      };
      writeJson(import_node_path2.default.join(dir, "owner.json"), info);
      return { ok: true, dir, info, attempts: attempt };
    } catch (error) {
      if (errorCode(error) !== "EEXIST") {
        return { ok: false, dir, error: errorMessage(error), attempts: attempt };
      }
      const current = readLockInfo(dir);
      if (lockIsStale(current)) {
        try {
          import_node_fs.default.rmSync(dir, { recursive: true, force: true });
          continue;
        } catch (removeError) {
          return {
            ok: false,
            dir,
            conflict: true,
            stale: true,
            holder: current,
            error: errorMessage(removeError),
            attempts: attempt
          };
        }
      }
      return {
        ok: false,
        dir,
        conflict: true,
        stale: false,
        holder: current,
        error: `Lock already held: ${name}`,
        attempts: attempt
      };
    }
  }
  return { ok: false, dir, conflict: true, error: `Unable to acquire lock: ${name}` };
}
function releaseLock(lock) {
  if (!lock || !lock.ok || !lock.dir) return { ok: true, skipped: true };
  try {
    import_node_fs.default.rmSync(lock.dir, { recursive: true, force: true });
    return { ok: true };
  } catch (error) {
    return { ok: false, error: errorMessage(error) };
  }
}
function withLock(root, name, fn, options = {}) {
  const lock = acquireLock(root, name, options);
  if (!lock.ok) return { ok: false, stage: "lock", lock };
  try {
    const value = fn(lock);
    if (value && typeof value === "object" && !Array.isArray(value)) {
      return Object.prototype.hasOwnProperty.call(value, "ok") ? { ...value, lock } : { ok: true, value, lock };
    }
    return { ok: true, value, lock };
  } catch (error) {
    return { ok: false, stage: "locked_operation", error: errorMessage(error), lock };
  } finally {
    releaseLock(lock);
  }
}
function trackLockName(trackId) {
  return `track:${trackId}`;
}
function withTrackLock(root, trackId, fn, options = {}) {
  return withLock(root, trackLockName(trackId), fn, options);
}

// src/core/infrastructure/runtime/json-store.ts
function readJson(file, fallback) {
  try {
    return JSON.parse(import_node_fs2.default.readFileSync(file, "utf8"));
  } catch {
    return fallback;
  }
}
function writeJson(file, value) {
  const tmp = `${file}.${process.pid}.${Date.now()}.tmp`;
  import_node_fs2.default.writeFileSync(tmp, `${JSON.stringify(value, null, 2)}
`);
  import_node_fs2.default.renameSync(tmp, file);
}
function safeName(value) {
  return String(value || "lock").replace(/[^A-Za-z0-9_.-]+/g, "_").replace(/^_+|_+$/g, "").slice(0, 160) || "lock";
}
function textHash(text) {
  return import_node_crypto.default.createHash("sha256").update(String(text || "")).digest("hex");
}
function patchJsonFileUnlocked(file, patcher, options = {}) {
  const retries = Number(options.retries || 5);
  for (let attempt = 1; attempt <= retries; attempt += 1) {
    let beforeText;
    try {
      beforeText = import_node_fs2.default.readFileSync(file, "utf8");
    } catch (error) {
      return { ok: false, file, error: `Unable to read JSON file: ${errorMessage(error)}` };
    }
    let before;
    try {
      before = asJsonObject(JSON.parse(beforeText || "{}"));
    } catch (error) {
      return { ok: false, file, error: `Invalid JSON: ${errorMessage(error)}` };
    }
    const beforeHash = textHash(beforeText);
    let next;
    try {
      next = patcher({ ...before }, before);
    } catch (error) {
      return { ok: false, file, error: `JSON patcher failed: ${errorMessage(error)}` };
    }
    if (!next || typeof next !== "object") {
      return { ok: false, file, error: "JSON patcher must return an object" };
    }
    let latestText;
    try {
      latestText = import_node_fs2.default.readFileSync(file, "utf8");
    } catch (error) {
      return { ok: false, file, error: `Unable to re-read JSON file: ${errorMessage(error)}` };
    }
    if (textHash(latestText) !== beforeHash) continue;
    writeJson(file, next);
    return {
      ok: true,
      file,
      attempts: attempt,
      before_hash: beforeHash,
      after_hash: textHash(`${JSON.stringify(next, null, 2)}
`),
      value: next
    };
  }
  return {
    ok: false,
    file,
    error: `JSON file changed during patch after ${retries} retries`,
    conflict: true
  };
}
function patchJsonFile(file, patcher, options = {}) {
  if (options.root && options.lockName && options.lock !== false) {
    return withLock(options.root, options.lockName, () => patchJsonFileUnlocked(file, patcher, { ...options, lock: false }), options.lockOptions || {});
  }
  return patchJsonFileUnlocked(file, patcher, options);
}
function utcNow() {
  return (/* @__PURE__ */ new Date()).toISOString().replace(/\.\d{3}Z$/, "Z");
}
function fileExists(file) {
  try {
    return import_node_fs2.default.existsSync(file);
  } catch {
    return false;
  }
}
function ensureParent(file) {
  import_node_fs2.default.mkdirSync(import_node_path3.default.dirname(file), { recursive: true });
}
function writeJsonEnsured(file, value) {
  ensureParent(file);
  writeJson(file, value);
}
function appendJsonl(file, value) {
  ensureParent(file);
  import_node_fs2.default.appendFileSync(file, `${JSON.stringify(value)}
`);
}

// src/core/application/runtime/plan-docs.ts
var import_node_path25 = __toESM(require("node:path"));

// src/core/application/runtime/status.ts
var import_node_path24 = __toESM(require("node:path"));

// src/core/infrastructure/runtime/project-config.ts
var import_node_path4 = __toESM(require("node:path"));

// src/core/domain/provider-policy.ts
var PROVIDER_MODES = /* @__PURE__ */ new Set(["local", "github", "gitlab"]);

// src/core/infrastructure/runtime/project-config.ts
function loadTopology(root) {
  const reposPath = import_node_path4.default.join(root, "cadre", "repos.json");
  const configPath = import_node_path4.default.join(root, "cadre", "config.json");
  const repos = readJson(reposPath, null);
  const config = readJson(configPath, {});
  const polyrepo = Boolean(repos && repos.mode === "polyrepo");
  return {
    polyrepo,
    repos: asJsonObject(repos || {}),
    config,
    defaultRepo: polyrepo ? asString(repos?.default_repo, ".") : "."
  };
}
function loadPackageJson(root) {
  return readJson(import_node_path4.default.join(root, "package.json"), null);
}
function normalizeProviderMode(value) {
  const raw = String(value || "").trim().toLowerCase();
  if (!raw) return null;
  if (["none", "no", "off", "local-only", "local_only"].includes(raw)) return "local";
  return PROVIDER_MODES.has(raw) ? raw : null;
}
function gitRemoteUrls(root) {
  const result = runCommand("git", ["remote", "-v"], { cwd: root });
  if (!result.ok) return [];
  return Array.from(new Set(result.stdout.split(/\r?\n/).map((line) => line.trim().split(/\s+/)[1] || "").filter(Boolean))).sort();
}
function remoteHost(url) {
  try {
    return new URL(url).hostname.toLowerCase();
  } catch {
    const ssh = url.match(/^[^@]+@([^:/]+)[:/]/);
    if (ssh?.[1]) return ssh[1].toLowerCase();
    const schemeLess = url.match(/^([^:/]+)[:/]/);
    return schemeLess?.[1] ? schemeLess[1].toLowerCase() : null;
  }
}
function providerModeForHost(host) {
  if (!host) return null;
  if (host.includes("github")) return "github";
  if (host.includes("gitlab")) return "gitlab";
  return null;
}
function detectedProviderFromRemotes(root) {
  const remotes = gitRemoteUrls(root).map((url) => {
    const host = remoteHost(url);
    return { url, host, provider_mode: providerModeForHost(host) };
  });
  const providerModes = Array.from(new Set(remotes.map((remote) => remote.provider_mode).filter(Boolean))).sort();
  const hosts = Array.from(new Set(remotes.map((remote) => remote.host).filter(Boolean))).sort();
  const hasRemotes = remotes.length > 0;
  const ambiguous = providerModes.length > 1 || hasRemotes && providerModes.length === 0;
  const providerMode = providerModes.length === 1 ? providerModes[0] : !hasRemotes ? "local" : null;
  return {
    ok: true,
    provider_mode: providerMode,
    remote_host: hosts.length === 1 ? hosts[0] : null,
    remote_hosts: hosts,
    remotes,
    ambiguous,
    source: !hasRemotes ? "no_remote" : providerModes.length === 0 ? "unknown_remote" : "git_remote"
  };
}
function configuredProvider(root, args = {}) {
  const config = loadTopology(root).config || {};
  const detected = detectedProviderFromRemotes(root);
  const explicit = normalizeProviderMode(args.providerMode || args.provider_mode || args.provider);
  const configured = normalizeProviderMode(config.provider_mode) || normalizeProviderMode(config.pr_provider);
  const providerMode = explicit || configured || (detected.ambiguous ? null : normalizeProviderMode(detected.provider_mode));
  const remoteHostValue = args.remoteHost || args.remote_host || config.remote_host || detected.remote_host || null;
  const mode2 = providerMode || null;
  return {
    ok: Boolean(mode2),
    provider_mode: mode2,
    provider_mcp_required: mode2 === "github" || mode2 === "gitlab",
    remote_host: remoteHostValue,
    detected,
    source: explicit ? "argument" : configured ? "config" : "detected",
    requires_confirmation: !mode2 && detected.ambiguous === true
  };
}
function providerMcpAvailability(root, args = {}) {
  const provider = configuredProvider(root, args);
  const mode2 = asOptionalString(provider.provider_mode) || "local";
  if (mode2 === "local") {
    return { ...provider, available: true, skipped: true, reason: "provider_mode is local" };
  }
  const explicit = args.provider_mcp_available ?? args.providerMcpAvailable;
  const modeSpecific = mode2 === "github" ? args.githubMcpAvailable : args.gitlabMcpAvailable;
  const available = typeof modeSpecific === "boolean" ? modeSpecific : typeof explicit === "boolean" ? explicit : null;
  return {
    ...provider,
    available,
    availability_source: available == null ? "not_verifiable_by_cadre_runtime" : "caller",
    required_provider_mcp: {
      provider: mode2,
      server: mode2,
      purpose: "Fetch PR/MR metadata, reviews, CI/check status, and discussion evidence."
    }
  };
}
function configuredCoverageCommand(root, args = {}, workingRoot = root) {
  if (args.command) return String(args.command);
  const topology = loadTopology(root);
  const config = topology.config || {};
  for (const key of ["coverage_command", "test_coverage_command", "test_command"]) {
    const value = config[key];
    if (typeof value === "string" && value.trim()) return value.trim();
  }
  const pkg = loadPackageJson(workingRoot);
  const scripts = isRecord(pkg?.scripts) ? pkg.scripts : null;
  if (scripts) {
    for (const name of ["coverage", "test:coverage", "test:cov", "test"]) {
      if (scripts[name]) {
        if (fileExists(import_node_path4.default.join(workingRoot, "pnpm-lock.yaml"))) return `pnpm ${name}`;
        if (fileExists(import_node_path4.default.join(workingRoot, "yarn.lock"))) return `yarn ${name}`;
        return `npm run ${name}`;
      }
    }
  }
  if (fileExists(import_node_path4.default.join(workingRoot, "pyproject.toml")) || fileExists(import_node_path4.default.join(workingRoot, "pytest.ini"))) {
    return "pytest --cov --cov-report=term";
  }
  if (fileExists(import_node_path4.default.join(workingRoot, "go.mod"))) return "go test ./...";
  return null;
}
function parseCoveragePercent(text) {
  const source = String(text || "");
  const patterns = [
    /All files[^|\n]*(?:\|[^|\n]*){3,}\|\s*([0-9]+(?:\.[0-9]+)?)\s*(?:\||$)/i,
    /\bStatements\s*:\s*([0-9]+(?:\.[0-9]+)?)%/i,
    /\bLines\s*:\s*([0-9]+(?:\.[0-9]+)?)%/i,
    /\bTOTAL\b[^\n%]*\s([0-9]+(?:\.[0-9]+)?)%/i,
    /\bcoverage[^0-9%]{0,40}([0-9]+(?:\.[0-9]+)?)%/i
  ];
  for (const pattern of patterns) {
    const match = source.match(pattern);
    if (match?.[1]) return Number(match[1]);
  }
  return null;
}

// src/core/application/runtime/track-schedule.ts
var import_node_fs13 = __toESM(require("node:fs"));
var import_node_path23 = __toESM(require("node:path"));

// src/core/infrastructure/runtime/coverage.ts
var import_node_fs3 = __toESM(require("node:fs"));
var import_node_path5 = __toESM(require("node:path"));
function parseLcovCoverage(root) {
  const candidates = [
    import_node_path5.default.join(root, "coverage", "lcov.info"),
    import_node_path5.default.join(root, "lcov.info")
  ];
  for (const file of candidates) {
    if (!fileExists(file)) continue;
    const text = import_node_fs3.default.readFileSync(file, "utf8");
    let found = 0;
    let hit = 0;
    for (const line of text.split(/\r?\n/)) {
      if (line.startsWith("LF:")) found += Number(line.slice(3)) || 0;
      if (line.startsWith("LH:")) hit += Number(line.slice(3)) || 0;
    }
    if (found > 0) return Math.round(hit / found * 1e4) / 100;
  }
  return null;
}
function coverageThreshold(root) {
  const topology = loadTopology(root);
  const config = topology.config || {};
  for (const key of ["coverage_threshold", "minimum_coverage", "min_coverage"]) {
    const value = Number(config[key]);
    if (Number.isFinite(value) && value >= 0) return value;
  }
  const workflowPath = import_node_path5.default.join(root, "cadre", "workflow.json");
  const workflow = readJson(workflowPath, null);
  if (workflow) {
    const text = JSON.stringify(workflow);
    const match = text.match(/(?:coverage|test coverage)[^%]{0,120}?([0-9]+(?:\.[0-9]+)?)\s*%/i);
    if (match?.[1]) return Number(match[1]);
  }
  return 80;
}
function runCoverage(root, args = {}, workingRoot = root) {
  const command = configuredCoverageCommand(root, args, workingRoot);
  if (!command) {
    return {
      ok: false,
      available: false,
      command: null,
      coverage: null,
      reason: "No coverage/test command configured or detected",
      hints: [
        "Set cadre/config.json coverage_command",
        "Add package.json scripts.coverage or scripts.test:coverage",
        "Pass { command } explicitly to cadre_complete_task"
      ]
    };
  }
  const timeoutMs = Number(args.timeoutMs || 10 * 60 * 1e3);
  const result = runCommand(command, [], {
    cwd: workingRoot,
    shell: true,
    timeoutMs,
    maxBuffer: 30 * 1024 * 1024
  });
  const combined = `${result.stdout}
${result.stderr}`;
  const parsed = parseCoveragePercent(combined);
  const lcov = parsed == null ? parseLcovCoverage(workingRoot) : null;
  const coverage = parsed == null ? lcov : parsed;
  return {
    ok: result.ok,
    available: true,
    command,
    cwd: workingRoot,
    status: result.status,
    signal: result.signal,
    coverage,
    coverage_source: parsed == null && lcov != null ? "lcov" : parsed != null ? "output" : null,
    timed_out: result.signal === "SIGTERM" || result.signal === "SIGKILL",
    stdout_tail: result.stdout.slice(-4e3),
    stderr_tail: result.stderr.slice(-4e3),
    result
  };
}
function parseIsoTime(value) {
  if (typeof value !== "string" || value.trim() === "") return null;
  const time = Date.parse(value);
  return Number.isFinite(time) ? time : null;
}
function staleInfo(value, now = Date.now()) {
  const time = parseIsoTime(value);
  if (!time) return { stale: false, age_minutes: null };
  const ageMs = Math.max(0, now - time);
  return {
    stale: ageMs > STALE_LEASE_MS,
    age_minutes: Math.floor(ageMs / 6e4)
  };
}

// src/core/application/runtime/repo-resolution.ts
var import_node_path22 = __toESM(require("node:path"));

// src/core/application/runtime/planning.ts
var import_node_path19 = __toESM(require("node:path"));

// src/lsp/language-registry.ts
var import_node_fs4 = __toESM(require("node:fs"));
var import_node_path7 = __toESM(require("node:path"));
var import_node_child_process2 = require("node:child_process");

// src/lsp/ignore-policy.ts
var import_node_path6 = __toESM(require("node:path"));
var DEFAULT_IGNORES = /* @__PURE__ */ new Set([
  ".git",
  ".hg",
  ".svn",
  ".worktrees",
  ".agents",
  ".claude",
  ".cache",
  ".codex",
  ".dart_tool",
  ".gradle",
  ".mypy_cache",
  ".pytest_cache",
  ".ruff_cache",
  ".serverless",
  "node_modules",
  "vendor",
  "dist",
  "build",
  "coverage",
  "out",
  "target",
  ".next",
  ".nuxt",
  ".parcel-cache",
  ".svelte-kit",
  ".turbo",
  ".vite",
  ".venv",
  "venv",
  "__pycache__",
  "__generated__",
  "generated",
  "gen",
  "tmp",
  "temp",
  "logs",
  "Pods",
  "DerivedData",
  ".idea",
  ".vscode"
]);
var DEFAULT_IGNORE_PATHS = [
  "plugins/cadre",
  "plugins/cadre-claude"
];
function normalizeRel(file) {
  return file.split(import_node_path6.default.sep).join("/");
}
function shouldIgnore(root, fullPath, name) {
  if (DEFAULT_IGNORES.has(name)) return true;
  const rel = normalizeRel(import_node_path6.default.relative(root, fullPath));
  return DEFAULT_IGNORE_PATHS.some(
    (ignored) => rel === ignored || rel.startsWith(`${ignored}/`)
  );
}
function isIgnoredFile(root, file) {
  const rel = normalizeRel(file);
  if (rel.split("/").some((part) => DEFAULT_IGNORES.has(part))) return true;
  return DEFAULT_IGNORE_PATHS.some(
    (ignored) => rel === ignored || rel.startsWith(`${ignored}/`)
  );
}

// src/lsp/language-registry.ts
var EXTENSION_LANGUAGE_IDS = {
  ".js": "javascript",
  ".jsx": "javascript",
  ".ts": "typescript",
  ".tsx": "typescript",
  ".py": "python",
  ".go": "go",
  ".rs": "rust",
  ".java": "java",
  ".kt": "kotlin",
  ".kts": "kotlin",
  ".swift": "swift",
  ".rb": "ruby",
  ".php": "php",
  ".dart": "dart",
  ".html": "html",
  ".htm": "html",
  ".css": "css",
  ".scss": "css",
  ".sass": "css",
  ".less": "css",
  ".json": "json",
  ".jsonc": "json",
  ".yaml": "yaml",
  ".yml": "yaml",
  ".md": "markdown",
  ".mdx": "markdown",
  ".toml": "toml",
  ".lua": "lua",
  ".sh": "shell",
  ".bash": "shell",
  ".zsh": "shell",
  ".ksh": "shell",
  ".tf": "terraform",
  ".tfvars": "terraform",
  ".hcl": "terraform",
  ".ex": "elixir",
  ".exs": "elixir",
  ".scala": "scala",
  ".sc": "scala",
  ".clj": "clojure",
  ".cljs": "clojure",
  ".cljc": "clojure",
  ".hs": "haskell",
  ".lhs": "haskell",
  ".ml": "ocaml",
  ".mli": "ocaml",
  ".zig": "zig",
  ".nix": "nix",
  ".elm": "elm",
  ".vue": "vue",
  ".svelte": "svelte",
  ".xml": "xml",
  ".xsd": "xml",
  ".xsl": "xml",
  ".xslt": "xml",
  ".graphql": "graphql",
  ".gql": "graphql",
  ".prisma": "prisma",
  ".proto": "protobuf",
  ".c": "c-cpp",
  ".h": "c-cpp",
  ".cc": "c-cpp",
  ".cpp": "c-cpp",
  ".cxx": "c-cpp",
  ".hpp": "c-cpp",
  ".m": "c-cpp",
  ".mm": "c-cpp",
  ".cs": "csharp"
};
var FILE_LANGUAGE_IDS = {
  Dockerfile: "dockerfile",
  Containerfile: "dockerfile"
};
var LANGUAGE_RULES = [
  {
    id: "typescript",
    label: "TypeScript / JavaScript",
    extensions: [".ts", ".tsx", ".js", ".jsx", ".mjs", ".cjs"],
    command: "typescript-language-server",
    args: ["--stdio"],
    install: "npm install -g typescript-language-server typescript"
  },
  {
    id: "python",
    label: "Python",
    extensions: [".py", ".pyi"],
    command: "pyright-langserver",
    args: ["--stdio"],
    install: "npm install -g pyright"
  },
  { id: "go", label: "Go", extensions: [".go"], command: "gopls", args: [], install: "go install golang.org/x/tools/gopls@latest" },
  { id: "rust", label: "Rust", extensions: [".rs"], command: "rust-analyzer", args: [], install: "rustup component add rust-analyzer" },
  { id: "java", label: "Java", extensions: [".java"], command: "jdtls", args: [], install: "brew install jdtls  # or install Eclipse JDT LS for your platform" },
  { id: "kotlin", label: "Kotlin", extensions: [".kt", ".kts"], command: "kotlin-language-server", args: [], install: "brew install fwcd/kotlin-language-server/kotlin-language-server" },
  { id: "swift", label: "Swift", extensions: [".swift"], command: "sourcekit-lsp", args: [], install: "Install Xcode or Swift toolchain; sourcekit-lsp ships with it" },
  { id: "c-cpp", label: "C / C++ / Objective-C", extensions: [".c", ".h", ".cc", ".cpp", ".cxx", ".hpp", ".m", ".mm"], command: "clangd", args: [], install: "brew install llvm  # or install clangd for your platform" },
  { id: "csharp", label: "C#", extensions: [".cs"], command: "csharp-ls", args: [], install: "dotnet tool install --global csharp-ls" },
  { id: "php", label: "PHP", extensions: [".php"], command: "intelephense", args: ["--stdio"], install: "npm install -g intelephense" },
  { id: "ruby", label: "Ruby", extensions: [".rb"], command: "ruby-lsp", args: [], install: "gem install ruby-lsp" },
  { id: "dart", label: "Dart / Flutter", extensions: [".dart"], command: "dart", args: ["language-server", "--protocol=lsp"], install: "Install the Dart or Flutter SDK and ensure `dart` is on PATH" },
  { id: "html", label: "HTML", extensions: [".html", ".htm"], command: "vscode-html-language-server", args: ["--stdio"], install: "npm install -g vscode-langservers-extracted" },
  { id: "css", label: "CSS / Sass / Less", extensions: [".css", ".scss", ".sass", ".less"], command: "vscode-css-language-server", args: ["--stdio"], install: "npm install -g vscode-langservers-extracted" },
  { id: "json", label: "JSON", extensions: [".json", ".jsonc"], command: "vscode-json-language-server", args: ["--stdio"], install: "npm install -g vscode-langservers-extracted" },
  { id: "yaml", label: "YAML", extensions: [".yaml", ".yml"], command: "yaml-language-server", args: ["--stdio"], install: "npm install -g yaml-language-server" },
  { id: "markdown", label: "Markdown", extensions: [".md", ".mdx"], command: "marksman", args: ["server"], install: "brew install marksman  # or download Marksman for your platform" },
  { id: "toml", label: "TOML", extensions: [".toml"], command: "taplo", args: ["lsp", "stdio"], install: "cargo install taplo-cli --locked" },
  { id: "lua", label: "Lua", extensions: [".lua"], command: "lua-language-server", args: [], install: "brew install lua-language-server  # or install LuaLS for your platform" },
  { id: "shell", label: "Shell", extensions: [".sh", ".bash", ".zsh", ".ksh"], command: "bash-language-server", args: ["start"], install: "npm install -g bash-language-server" },
  { id: "terraform", label: "Terraform / HCL", extensions: [".tf", ".tfvars", ".hcl"], command: "terraform-ls", args: ["serve"], install: "brew install hashicorp/tap/terraform-ls" },
  { id: "elixir", label: "Elixir", extensions: [".ex", ".exs"], command: "elixir-ls", args: [], install: "Install ElixirLS and ensure `elixir-ls` is on PATH" },
  { id: "scala", label: "Scala", extensions: [".scala", ".sc"], command: "metals", args: [], install: "coursier install metals" },
  { id: "clojure", label: "Clojure", extensions: [".clj", ".cljs", ".cljc", ".edn"], command: "clojure-lsp", args: [], install: "brew install clojure-lsp/brew/clojure-lsp-native" },
  { id: "haskell", label: "Haskell", extensions: [".hs", ".lhs"], command: "haskell-language-server-wrapper", args: ["--lsp"], install: "ghcup install hls" },
  { id: "ocaml", label: "OCaml", extensions: [".ml", ".mli"], command: "ocamllsp", args: [], install: "opam install ocaml-lsp-server" },
  { id: "zig", label: "Zig", extensions: [".zig"], command: "zls", args: [], install: "Install zls and ensure it is on PATH" },
  { id: "nix", label: "Nix", extensions: [".nix"], command: "nil", args: [], install: "nix profile install nixpkgs#nil" },
  { id: "elm", label: "Elm", extensions: [".elm"], command: "elm-language-server", args: ["--stdio"], install: "npm install -g @elm-tooling/elm-language-server" },
  { id: "vue", label: "Vue", extensions: [".vue"], command: "vue-language-server", args: ["--stdio"], install: "npm install -g @vue/language-server" },
  { id: "svelte", label: "Svelte", extensions: [".svelte"], command: "svelteserver", args: ["--stdio"], install: "npm install -g svelte-language-server" },
  { id: "dockerfile", label: "Dockerfile", extensions: [], filenames: ["Dockerfile", "Containerfile"], command: "docker-langserver", args: ["--stdio"], install: "npm install -g dockerfile-language-server-nodejs", languageIds: { Dockerfile: "dockerfile", Containerfile: "dockerfile" } },
  { id: "xml", label: "XML", extensions: [".xml", ".xsd", ".xsl", ".xslt"], command: "lemminx", args: [], install: "Install Eclipse LemMinX and ensure `lemminx` is on PATH" },
  { id: "graphql", label: "GraphQL", extensions: [".graphql", ".gql"], command: "graphql-lsp", args: ["server", "-m", "stream"], install: "npm install -g graphql-language-service-cli" },
  { id: "prisma", label: "Prisma", extensions: [".prisma"], command: "prisma-language-server", args: ["--stdio"], install: "npm install -g @prisma/language-server" },
  { id: "protobuf", label: "Protocol Buffers", extensions: [".proto"], command: "buf", args: ["beta", "lsp", "--timeout=0"], install: "brew install bufbuild/buf/buf" }
];
function fileLanguageId(file) {
  return FILE_LANGUAGE_IDS[import_node_path7.default.basename(file)] || EXTENSION_LANGUAGE_IDS[import_node_path7.default.extname(file).toLowerCase()] || null;
}
function languageForFile(file) {
  return fileLanguageId(file);
}
function normalizeWorkspaceFile(root, rel) {
  const normalized = rel.replace(/\r/g, "").trim();
  if (!normalized) return null;
  const full = import_node_path7.default.join(root, normalized);
  if (shouldIgnore(root, full, import_node_path7.default.basename(normalized))) return null;
  return normalized;
}
function gitWorkspaceFiles(root) {
  const result = (0, import_node_child_process2.spawnSync)("git", ["ls-files", "-co", "--exclude-standard", "-z"], {
    cwd: root,
    encoding: "buffer",
    maxBuffer: 30 * 1024 * 1024
  });
  if (result.status !== 0 || !result.stdout || result.stdout.length === 0) return [];
  return result.stdout.toString("utf8").split("\0").map((item) => normalizeWorkspaceFile(root, item)).filter((item) => Boolean(item)).sort();
}
function walkWorkspaceFiles(root) {
  const files = [];
  const visit = (dir) => {
    let entries;
    try {
      entries = import_node_fs4.default.readdirSync(dir, { withFileTypes: true });
    } catch {
      return;
    }
    for (const entry of entries) {
      const full = import_node_path7.default.join(dir, entry.name);
      if (shouldIgnore(root, full, entry.name)) continue;
      if (entry.isDirectory()) {
        visit(full);
        continue;
      }
      if (entry.isFile()) files.push(import_node_path7.default.relative(root, full).split(import_node_path7.default.sep).join("/"));
    }
  };
  visit(root);
  return files.sort();
}
function listWorkspaceFiles(root) {
  const gitFiles = gitWorkspaceFiles(root);
  return gitFiles.length > 0 ? gitFiles : walkWorkspaceFiles(root);
}
function scanWorkspaceFiles(root) {
  const counts = /* @__PURE__ */ new Map();
  const samples = /* @__PURE__ */ new Map();
  const filenameCounts = /* @__PURE__ */ new Map();
  const filenameSamples = /* @__PURE__ */ new Map();
  for (const rel of listWorkspaceFiles(root)) {
    const full = import_node_path7.default.join(root, rel);
    const name = import_node_path7.default.basename(rel);
    if (shouldIgnore(root, full, name)) continue;
    const ext = import_node_path7.default.extname(name).toLowerCase();
    if (ext) {
      counts.set(ext, (counts.get(ext) ?? 0) + 1);
      const extSamples = samples.get(ext) ?? [];
      if (extSamples.length < 5) extSamples.push(rel);
      samples.set(ext, extSamples);
    }
    const lower = name.toLowerCase();
    filenameCounts.set(lower, (filenameCounts.get(lower) ?? 0) + 1);
    const nameSamples = filenameSamples.get(lower) ?? [];
    if (nameSamples.length < 5) nameSamples.push(rel);
    filenameSamples.set(lower, nameSamples);
  }
  return { counts, samples, filenameCounts, filenameSamples };
}

// src/core/application/runtime/track-context.ts
var import_node_path8 = __toESM(require("node:path"));
function findTrack(root, trackId) {
  return listTracks(root).find((item) => item.track_id === trackId) || null;
}
function trackContext(root, trackId) {
  const track = findTrack(root, trackId);
  if (!track) return { ok: false, error: `Track not found: ${trackId}` };
  const topology = loadTopology(root);
  const plan = parsePlanFile(track.plan_path);
  const hold = holdInfo(track);
  const worktrees = [];
  if (track.metadata.worktree_path) {
    const abs = import_node_path8.default.resolve(root, track.metadata.worktree_path);
    worktrees.push({
      repo: ".",
      path: track.metadata.worktree_path,
      exists: fileExists(abs),
      git_branch: track.metadata.git_branch || `track/${track.track_id}`
    });
  }
  const reposMetadata = isRecord(track.metadata.repos) ? track.metadata.repos : null;
  if (reposMetadata) {
    for (const [repo, rawInfo] of Object.entries(reposMetadata)) {
      const info = asJsonObject(rawInfo);
      const submodulePath = info.submodule_path || "";
      const worktreePath = info.worktree_path || "";
      worktrees.push({
        repo,
        submodule_path: submodulePath,
        path: worktreePath,
        exists: worktreePath ? fileExists(import_node_path8.default.resolve(root, worktreePath)) : false,
        git_branch: info.git_branch || `track/${track.track_id}`,
        base_branch: info.base_branch || "main"
      });
    }
  }
  return {
    ok: true,
    root,
    topology: {
      polyrepo: topology.polyrepo,
      default_repo: topology.defaultRepo,
      sync_mode: topology.config.sync_mode || "local"
    },
    track: {
      track_id: track.track_id,
      status: track.metadata.status || "new",
      name: track.metadata.name || track.metadata.description || track.track_id,
      priority: track.metadata.priority || "medium",
      owner: track.metadata.owner || null,
      reviewer: track.metadata.reviewer || null,
      git_branch: track.metadata.git_branch || `track/${track.track_id}`,
      metadata_path: import_node_path8.default.relative(root, track.metadata_path || import_node_path8.default.join(track.dir, "metadata.json")),
      plan_path: import_node_path8.default.relative(root, track.plan_path),
      spec_path: import_node_path8.default.relative(root, track.spec_path),
      tags: asStringArray(track.metadata.tags),
      review: track.metadata.review || null,
      last_coverage: track.metadata.last_coverage ?? null
    },
    hold,
    task_counts: taskCounts(plan),
    plan,
    worktrees
  };
}

// src/core/application/runtime/review-records.ts
var import_node_path16 = __toESM(require("node:path"));

// src/core/application/runtime/quality-gates.ts
var import_node_fs10 = __toESM(require("node:fs"));
var import_node_path15 = __toESM(require("node:path"));

// src/core/application/runtime/mcp-readiness.ts
var OPTIONAL_CATEGORIES = [
  {
    kind: "code_search",
    label: "Code search",
    aliases: ["code_search", "codeSearch", "sourcegraph", "search"],
    purpose: "Cross-repo semantic or indexed code search for repo maps, impact analysis, and claim suggestions.",
    required: false,
    recommended: true
  },
  {
    kind: "issue_tracker",
    label: "Issue tracker",
    aliases: ["issue_tracker", "issueTracker", "jira", "linear", "github_issues", "gitlab_issues"],
    purpose: "Link Cadre tracks, native workflow state, and provider issues without forcing agents through local CLI fallbacks.",
    required: false,
    recommended: true
  },
  {
    kind: "ci",
    label: "CI/checks",
    aliases: ["ci", "checks", "github_actions", "gitlab_ci", "buildkite", "circleci"],
    purpose: "Fetch check, pipeline, and workflow evidence for review/ship gates.",
    required: false,
    recommended: true
  },
  {
    kind: "observability",
    label: "Observability",
    aliases: ["observability", "logging", "telemetry", "sentry", "datadog", "honeycomb"],
    purpose: "Pull production error, log, and telemetry context for review and release readiness.",
    required: false,
    recommended: true
  },
  {
    kind: "knowledge_base",
    label: "Knowledge base",
    aliases: ["knowledge_base", "knowledgeBase", "kb", "docs", "confluence", "notion"],
    purpose: "Retrieve team documentation and runbooks without embedding large docs in packet responses.",
    required: false,
    recommended: true
  }
];
function capabilityEvidence(args) {
  const raw = args.mcpCapabilities || args.mcp_capabilities || null;
  return isRecord(raw) ? asJsonObject(raw) : {};
}
function valueAvailable(value) {
  if (typeof value === "boolean") return value;
  if (typeof value === "string") {
    const lower = value.trim().toLowerCase();
    if (!lower) return null;
    if (["false", "0", "no", "off", "missing", "unavailable", "disabled"].includes(lower)) return false;
    return true;
  }
  if (!isRecord(value)) return null;
  const object = asJsonObject(value);
  if (typeof object.available === "boolean") return object.available;
  if (typeof object.enabled === "boolean") return object.enabled;
  if (typeof object.configured === "boolean") return object.configured;
  if (object.server || object.tool || object.tools || object.capability) return true;
  return null;
}
function probeNestedMap(map, aliases, source) {
  for (const alias of aliases) {
    if (!Object.prototype.hasOwnProperty.call(map, alias)) continue;
    const value = map[alias];
    return {
      available: valueAvailable(value),
      source: `${source}.${alias}`,
      evidence: isRecord(value) ? asJsonObject(value) : { value },
      missing: []
    };
  }
  return null;
}
function probeTools(evidence, aliases) {
  const tools = Array.isArray(evidence.tools) ? evidence.tools : [];
  const servers = Array.isArray(evidence.servers) ? evidence.servers : [];
  const values = [...tools, ...servers].map((item) => {
    if (typeof item === "string") return item;
    if (isRecord(item)) {
      const object = asJsonObject(item);
      return asOptionalString(object.name) || asOptionalString(object.id) || asOptionalString(object.server) || "";
    }
    return "";
  }).filter(Boolean);
  const found = values.find((value) => aliases.some((alias) => value.toLowerCase().includes(alias.toLowerCase())));
  if (!found) return null;
  return {
    available: true,
    source: "mcpCapabilities.tools",
    evidence: { match: found },
    missing: []
  };
}
function probeCapability(args, aliases, argFallbacks = []) {
  for (const fallback of argFallbacks) {
    const value = args[fallback];
    if (typeof value === "boolean") {
      return {
        available: value,
        source: `args.${fallback}`,
        evidence: { [fallback]: value },
        missing: []
      };
    }
  }
  const evidence = capabilityEvidence(args);
  if (Object.keys(evidence).length === 0) {
    return {
      available: null,
      source: "not_provided",
      evidence: null,
      missing: ["mcpCapabilities"]
    };
  }
  const direct = probeNestedMap(evidence, aliases, "mcpCapabilities");
  if (direct) return direct;
  for (const containerName of ["servers", "mcpServers", "capabilities", "apps", "connectors"]) {
    const container = evidence[containerName];
    if (isRecord(container)) {
      const nested = probeNestedMap(asJsonObject(container), aliases, `mcpCapabilities.${containerName}`);
      if (nested) return nested;
    }
  }
  const tools = probeTools(evidence, aliases);
  if (tools) return tools;
  return {
    available: null,
    source: "not_detected",
    evidence: null,
    missing: aliases.map((alias) => `mcpCapabilities.${alias}`)
  };
}
function configConfigured(root, aliases) {
  const config = loadTopology(root).config || {};
  const integrations2 = isRecord(config.integrations) ? asJsonObject(config.integrations) : {};
  return aliases.some(
    (alias) => Object.prototype.hasOwnProperty.call(config, alias) || Object.prototype.hasOwnProperty.call(integrations2, alias)
  );
}
function readinessEntry(root, category, args) {
  const probe = probeCapability(args, category.aliases);
  return {
    kind: category.kind,
    label: category.label,
    required: category.required,
    recommended: category.recommended,
    configured: configConfigured(root, category.aliases),
    available: probe.available,
    evidence_source: probe.source,
    detected_agent_capability_evidence: probe.evidence,
    missing_evidence_fields: probe.available == null ? probe.missing : [],
    purpose: category.purpose
  };
}
function providerReadiness(root, args = {}) {
  const provider = providerMcpAvailability(root, args);
  const mode2 = asOptionalString(provider.provider_mode) || "local";
  if (mode2 === "local") {
    return {
      kind: "provider",
      label: "Provider MCP",
      provider_mode: "local",
      required: false,
      recommended: false,
      available: true,
      evidence_source: "provider_mode_local",
      detected_agent_capability_evidence: null,
      missing_evidence_fields: [],
      required_provider_mcp: null,
      exact_write_back_packet: null,
      reason: "provider_mode is local; provider MCP evidence is not required"
    };
  }
  const probe = probeCapability(args, [mode2, `${mode2}_mcp`, `${mode2}Mcp`, "provider", "provider_mcp"], [
    "providerMcpAvailable",
    "provider_mcp_available",
    mode2 === "github" ? "githubMcpAvailable" : "gitlabMcpAvailable"
  ]);
  const available = probe.available ?? (typeof provider.available === "boolean" ? provider.available : null);
  return {
    kind: "provider",
    label: "Provider MCP",
    provider_mode: mode2,
    required: true,
    recommended: true,
    available,
    evidence_source: probe.available == null && provider.available != null ? asOptionalString(provider.availability_source) || "caller" : probe.source,
    detected_agent_capability_evidence: probe.evidence,
    missing_evidence_fields: available == null ? probe.missing : [],
    required_provider_mcp: isRecord(provider.required_provider_mcp) ? asJsonObject(provider.required_provider_mcp) : {
      provider: mode2,
      server: mode2,
      purpose: "Fetch PR/MR metadata, reviews, CI/check status, and discussion evidence."
    },
    exact_write_back_packet: {
      tool: "cadre_review",
      action: "provider_evidence",
      required_fields: ["root", "trackId", "providerEvidence"]
    },
    reason: available == null ? `${mode2} provider mode requires packet-owned evidence that the agent can access the ${mode2} MCP` : null
  };
}
function mcpReadiness(root, args = {}) {
  const provider = providerReadiness(root, args);
  const optional = OPTIONAL_CATEGORIES.map((category) => readinessEntry(root, category, args));
  const all = [provider, ...optional];
  const missingRequired = all.filter((entry) => entry.required === true && entry.available !== true);
  const optionalAvailable = optional.filter((entry) => entry.available === true).length;
  return {
    ok: missingRequired.length === 0,
    root,
    provider,
    optional_mcps: optional,
    summary: {
      required_count: all.filter((entry) => entry.required === true).length,
      missing_required_count: missingRequired.length,
      optional_recommended_count: optional.length,
      optional_available_count: optionalAvailable,
      optional_configured_count: optional.filter((entry) => entry.configured === true).length,
      packet_owned_evidence_only: true
    },
    recommendations: optional.filter((entry) => entry.available !== true).map((entry) => ({
      kind: entry.kind,
      label: entry.label,
      required: false,
      reason: "Optional MCP improves team-scale visibility but is not mandatory."
    }))
  };
}

// src/core/application/runtime/project-maintenance.ts
var import_node_fs6 = __toESM(require("node:fs"));
var import_node_path10 = __toESM(require("node:path"));
var import_node_child_process3 = require("node:child_process");

// src/runtime-paths.ts
var import_node_fs5 = __toESM(require("node:fs"));
var import_node_path9 = __toESM(require("node:path"));
function unique(values) {
  return Array.from(new Set(values));
}
function fileExists2(file) {
  try {
    return import_node_fs5.default.statSync(file).isFile();
  } catch {
    return false;
  }
}
function mcpServerPathCandidates(root) {
  const candidates = [];
  let dir = __dirname;
  for (let depth = 0; depth < 8; depth += 1) {
    candidates.push(import_node_path9.default.join(dir, "cadre-server.js"));
    candidates.push(import_node_path9.default.join(dir, "mcp", "cadre-server.js"));
    candidates.push(import_node_path9.default.join(dir, "scripts", "mcp", "cadre-server.js"));
    const parent = import_node_path9.default.dirname(dir);
    if (parent === dir) break;
    dir = parent;
  }
  if (root) candidates.push(import_node_path9.default.join(root, "cadre", "scripts", "mcp", "cadre-server.js"));
  return unique(candidates);
}
function currentMcpServerPath(root) {
  return mcpServerPathCandidates(root).find(fileExists2) || null;
}
function mcpRuntimeRoot(serverPath) {
  return import_node_path9.default.resolve(import_node_path9.default.dirname(serverPath), "..", "..");
}

// src/core/application/runtime/project-maintenance.ts
function lspReview(root, args = {}) {
  const candidates = mcpServerPathCandidates(root);
  const helper = candidates.find(fileExists);
  if (!helper) return { available: false, reason: "No Cadre MCP runtime found for LSP review", checked: candidates };
  const commandArgs = [helper, "--cadre-lsp-review", "--base", args.base || "main", "--head", args.head || "HEAD", "--json"];
  if (args.config) commandArgs.push("--config", args.config);
  const result = runCommand("node", commandArgs, { cwd: root });
  if (!result.ok) {
    return { available: false, reason: "LSP review helper failed", helper, result };
  }
  try {
    return { helper, ...asJsonObject(JSON.parse(result.stdout || "{}")) };
  } catch {
    return { available: false, reason: "LSP review helper returned invalid JSON", helper, result };
  }
}
function polyrepoPreflight(root) {
  const topology = loadTopology(root);
  if (!topology.polyrepo) {
    return { ok: true, polyrepo: false, checks: ["monorepo mode"] };
  }
  const checks = [];
  const errors = [];
  const gitmodules = import_node_path10.default.join(root, ".gitmodules");
  for (const repo of topology.repos.repos || []) {
    if (repo.enabled === false) continue;
    const repoPath = import_node_path10.default.join(root, repo.submodule_path || "");
    if (!repo.name) errors.push("repo entry missing name");
    if (!repo.submodule_path) errors.push(`repo ${repo.name || "?"} missing submodule_path`);
    if (repo.submodule_path && !fileExists(repoPath)) {
      errors.push(`repo ${repo.name} path is missing: ${repo.submodule_path}`);
    }
    if (fileExists(gitmodules) && repo.name) {
      const result = (0, import_node_child_process3.spawnSync)(
        "git",
        ["config", "-f", ".gitmodules", "--get", `submodule.${repo.name}.path`],
        { cwd: root, encoding: "utf8" }
      );
      if (result.status === 0 && result.stdout.trim() !== repo.submodule_path) {
        errors.push(
          `repo ${repo.name} submodule_path mismatch: repos.json=${repo.submodule_path}, .gitmodules=${result.stdout.trim()}`
        );
      }
    }
    if (repo.name) checks.push(repo.name);
  }
  return { ok: errors.length === 0, polyrepo: true, checks, errors };
}
function regenIndex(root, options = {}) {
  if (options.lock !== false) {
    return withLock(root, "tracks-index", () => regenIndex(root, { ...options, lock: false }));
  }
  const tracksFile = import_node_path10.default.join(root, "cadre", "tracks.json");
  const legacyMarkdownFile = import_node_path10.default.join(root, "cadre", "tracks.md");
  const tracks = listTracks(root).sort((a, b) => a.track_id.localeCompare(b.track_id));
  const payload = trackIndexPayload(root, tracks);
  import_node_fs6.default.mkdirSync(import_node_path10.default.dirname(tracksFile), { recursive: true });
  writeJson(tracksFile, payload);
  let removedLegacyMarkdown = null;
  if (fileExists(legacyMarkdownFile)) {
    const legacy = import_node_fs6.default.readFileSync(legacyMarkdownFile, "utf8");
    if (hasGeneratedMarker(legacy) || legacy.includes("<!-- cadre:index:start -->") || legacy.includes("<!-- cadre:index:end -->")) {
      import_node_fs6.default.rmSync(legacyMarkdownFile, { force: true });
      removedLegacyMarkdown = import_node_path10.default.relative(root, legacyMarkdownFile);
    }
  }
  return {
    ok: true,
    tracks_file: tracksFile,
    tracks: tracks.length,
    removed_legacy_markdown: removedLegacyMarkdown,
    stdout: `Regenerated ${tracksFile} index from ${tracks.length} tracks' metadata.
`,
    stderr: ""
  };
}

// src/core/application/runtime/repo-map.ts
var import_node_fs7 = __toESM(require("node:fs"));
var import_node_path12 = __toESM(require("node:path"));

// src/core/application/runtime/workspace-cache.ts
var import_node_child_process4 = require("node:child_process");
var import_node_path11 = __toESM(require("node:path"));
var CACHE = /* @__PURE__ */ new Map();
var DEFAULT_TTL_MS = 3e4;
function gitHead(root) {
  const result = (0, import_node_child_process4.spawnSync)("git", ["rev-parse", "HEAD"], {
    cwd: root,
    encoding: "utf8",
    maxBuffer: 1024 * 1024
  });
  return result.status === 0 ? result.stdout.trim() || "unknown" : "nogit";
}
function workspaceCacheKey(root, scope, discriminator = "") {
  return JSON.stringify({
    root: import_node_path11.default.resolve(root),
    head: gitHead(root),
    scope,
    discriminator
  });
}
function cachedWorkspaceValue(root, scope, discriminator, producer, ttlMs = DEFAULT_TTL_MS) {
  const key = workspaceCacheKey(root, scope, discriminator);
  const now = Date.now();
  const existing = CACHE.get(key);
  if (existing && now - existing.createdAt <= ttlMs) return existing.value;
  const value = producer();
  CACHE.set(key, { value, createdAt: now });
  if (CACHE.size > 200) {
    const oldest = Array.from(CACHE.entries()).sort((a, b) => a[1].createdAt - b[1].createdAt).slice(0, 40);
    for (const [oldKey] of oldest) CACHE.delete(oldKey);
  }
  return value;
}

// src/core/application/runtime/repo-map.ts
function isIgnoredRepoMapFile(file) {
  const normalized = String(file || "").replace(/\\/g, "/");
  if (!normalized) return true;
  if (normalized.startsWith(".agents/")) return true;
  if (normalized.startsWith(".claude/")) return true;
  if (normalized.startsWith(".claude-plugin/")) return true;
  if (normalized.startsWith("plugins/cadre/")) return true;
  if (normalized.startsWith("plugins/cadre-claude/")) return true;
  return normalized.split("/").some((part) => [".git", "node_modules", "dist", "build", "coverage"].includes(part));
}
function selectedRepoNames(args = {}) {
  const values = [
    asOptionalString(args.repo),
    ...asStringArray(args.repos)
  ].filter((value) => Boolean(value));
  return values.length > 0 ? new Set(values) : null;
}
function intelRepoRoots(root, args = {}) {
  const selected = selectedRepoNames(args);
  const topology = loadTopology(root);
  const control = {
    repo: ".",
    root,
    path: ".",
    source: "control-root"
  };
  if (!topology.polyrepo) return selected && !selected.has(".") ? [] : [control];
  const entries = [control];
  for (const raw of Array.isArray(topology.repos.repos) ? topology.repos.repos : []) {
    const repo = asJsonObject(raw);
    if (repo.enabled === false) continue;
    const name = asOptionalString(repo.name);
    const rel = asOptionalString(repo.submodule_path);
    if (!name || !rel) continue;
    entries.push({
      repo: name,
      root: import_node_path12.default.resolve(root, rel),
      path: rel,
      source: "repos.json"
    });
  }
  return selected ? entries.filter((entry) => selected.has(entry.repo)) : entries;
}
function combineLanguageCounts(entries) {
  const counts = {};
  for (const entry of entries) {
    const languages = asJsonObject(entry.by_language);
    for (const [language, count] of Object.entries(languages)) {
      counts[language] = (counts[language] || 0) + Number(count || 0);
    }
  }
  return Object.fromEntries(Object.entries(counts).sort());
}
var GENERIC_SYMBOL_PATTERNS = [
  /\b(?:export\s+)?(?:async\s+)?function\s+([A-Za-z_$][\w$]*)\b/g,
  /\b(?:export\s+)?(?:class|interface|type|enum|struct|record|trait)\s+([A-Za-z_$][\w$]*)\b/g,
  /\bexport\s+(?:const|let|var)\s+([A-Za-z_$][\w$]*)\s*[:=]/g
];
var LANGUAGE_SYMBOL_PATTERNS = {
  python: [/^\s*(?:async\s+)?def\s+([A-Za-z_][\w]*)\b/gm, /^\s*class\s+([A-Za-z_][\w]*)\b/gm],
  go: [/^\s*func\s+(?:\([^)]*\)\s*)?([A-Za-z_][\w]*)\b/gm, /^\s*type\s+([A-Za-z_][\w]*)\s+/gm],
  rust: [/^\s*(?:pub\s+)?(?:async\s+)?fn\s+([A-Za-z_][\w]*)\b/gm, /^\s*(?:pub\s+)?(?:struct|enum|trait|type)\s+([A-Za-z_][\w]*)\b/gm],
  java: [/^\s*(?:public|private|protected|static|final|abstract|sealed|non-sealed|\s)*(?:class|interface|enum|record)\s+([A-Za-z_][\w]*)\b/gm, /^\s*(?:public|private|protected|static|final|abstract|synchronized|native|\s)*[\w<>\[\].?,\s]+\s+([A-Za-z_][\w]*)\s*\(/gm],
  kotlin: [/^\s*(?:public|private|protected|internal|open|final|abstract|data|sealed|\s)*(?:class|interface|object|enum|typealias)\s+([A-Za-z_][\w]*)\b/gm, /^\s*(?:public|private|protected|internal|suspend|inline|tailrec|operator|infix|fun|\s)*fun\s+([A-Za-z_][\w]*)\b/gm],
  swift: [/^\s*(?:public|private|internal|open|fileprivate|static|final|mutating|nonmutating|\s)*(?:func|class|struct|enum|protocol)\s+([A-Za-z_][\w]*)\b/gm],
  csharp: [/^\s*(?:public|private|protected|internal|static|partial|sealed|abstract|virtual|override|\s)*(?:class|interface|record|struct|enum)\s+([A-Za-z_][\w]*)\b/gm, /^\s*(?:public|private|protected|internal|static|async|\s)*[\w<>\[\].?,\s]+\s+([A-Za-z_][\w]*)\s*\(/gm],
  ruby: [/^\s*(?:class|module|def)\s+([A-Za-z_][\w!?=]*)\b/gm],
  elixir: [/^\s*(?:defmodule|defp?|defmacro)\s+([A-Za-z_][\w!?]*)\b/gm],
  lua: [/^\s*(?:local\s+)?function\s+([A-Za-z_][\w.]*)\b/gm],
  terraform: [/^\s*(?:resource|module|variable|output|data)\s+"?([A-Za-z0-9_.-]+)"?/gim],
  sql: [/^\s*(?:CREATE\s+(?:OR\s+REPLACE\s+)?)?(?:TABLE|VIEW|FUNCTION|PROCEDURE|TYPE)\s+([A-Za-z_][\w."]*)\b/gim],
  shell: [/^\s*function\s+([A-Za-z_][\w-]*)\b/gm, /^\s*(?:local\s+)?([A-Za-z_][\w-]*)\s*\(\)\s*\{/gm]
};
function symbolPatternsForLanguage(language) {
  return [...GENERIC_SYMBOL_PATTERNS, ...LANGUAGE_SYMBOL_PATTERNS[language] || []].map(
    (pattern) => new RegExp(pattern.source, pattern.flags)
  );
}
function extractRepoSymbols(root, file, limitPerFile = 40) {
  const abs = import_node_path12.default.join(root, file);
  if (!fileExists(abs)) return [];
  let stat;
  try {
    stat = import_node_fs7.default.statSync(abs);
  } catch {
    return [];
  }
  if (stat.size > 1024 * 1024) return [];
  const language = languageForFile(file);
  if (!language) return [];
  const text = import_node_fs7.default.readFileSync(abs, "utf8");
  const patterns = symbolPatternsForLanguage(language);
  const symbols = [];
  for (const pattern of patterns) {
    let match;
    while ((match = pattern.exec(text)) && symbols.length < limitPerFile) {
      const prefix = text.slice(0, match.index);
      const line = prefix.split(/\r?\n/).length;
      const name = match[1];
      if (name) symbols.push({ name, file, line, language });
    }
  }
  return symbols;
}
function repoMap(root, args = {}) {
  const limit = Number(args.limit || 200);
  const symbol = args.symbol ? String(args.symbol) : null;
  const repos = intelRepoRoots(root, args);
  if (symbol) {
    const repoResults = repos.map((entry) => {
      const result = runCommand("git", ["grep", "-n", "-w", "--", symbol], { cwd: entry.root, maxBuffer: 10 * 1024 * 1024 });
      const matches2 = result.stdout.split(/\r?\n/).filter(Boolean).filter((line) => !isIgnoredRepoMapFile(line.split(":")[0] || "")).slice(0, limit).map((line) => {
        const [file, lineNo, ...rest] = line.split(":");
        return { repo: entry.repo, file: file || "", line: Number(lineNo), snippet: rest.join(":").trim().slice(0, 180) };
      });
      return { repo: entry.repo, root: entry.root, path: entry.path, ok: result.ok || matches2.length > 0, matches: matches2, truncated: matches2.length >= limit };
    });
    const matches = repoResults.flatMap((entry) => asArray(entry.matches)).slice(0, limit);
    return { ok: repoResults.some((entry) => entry.ok) || matches.length > 0, root, symbol, matches, repos: repoResults, truncated: matches.length >= limit };
  }
  return cachedWorkspaceValue(root, "repo-map", JSON.stringify({ limit, repos: repos.map((entry) => entry.repo) }), () => {
    const repoResults = repos.map((entry) => {
      const files2 = listWorkspaceFiles(entry.root).filter((file) => !isIgnoredRepoMapFile(file));
      const byLanguage = {};
      const symbols2 = [];
      for (const file of files2) {
        const language = languageForFile(file);
        if (language) byLanguage[language] = (byLanguage[language] || 0) + 1;
        if (symbols2.length < limit) symbols2.push(...extractRepoSymbols(entry.root, file, 12).map((symbolEntry) => ({
          ...symbolEntry,
          repo: entry.repo
        })));
        if (symbols2.length > limit) symbols2.length = limit;
      }
      return {
        ok: true,
        repo: entry.repo,
        root: entry.root,
        path: entry.path,
        files: files2.length,
        by_language: Object.fromEntries(Object.entries(byLanguage).sort()),
        symbols: symbols2,
        truncated: symbols2.length >= limit
      };
    });
    const files = repoResults.reduce((sum, entry) => sum + Number(entry.files || 0), 0);
    const symbols = repoResults.flatMap((entry) => asArray(entry.symbols)).slice(0, limit);
    return {
      ok: true,
      root,
      files,
      by_language: combineLanguageCounts(repoResults),
      symbols,
      repos: repoResults,
      truncated: symbols.length >= limit
    };
  });
}

// src/core/application/runtime/track-mutations.ts
var import_node_fs9 = __toESM(require("node:fs"));
var import_node_path14 = __toESM(require("node:path"));

// src/core/domain/track-status.ts
var STATUS_MARKERS = {
  new: "[ ]",
  in_progress: "[~]",
  completed: "[x]",
  blocked: "[!]",
  skipped: "[-]"
};
var VALID_STATUSES = new Set(Object.keys(STATUS_MARKERS));

// src/core/application/runtime/native-state.ts
var import_node_fs8 = __toESM(require("node:fs"));
var import_node_path13 = __toESM(require("node:path"));
function nativeStatePaths(root) {
  return {
    events: import_node_path13.default.join(root, "cadre", "events.jsonl"),
    formulas: import_node_path13.default.join(root, "cadre", "formulas"),
    operations: import_node_path13.default.join(root, "cadre", "operations"),
    messages: import_node_path13.default.join(root, "cadre", "messages"),
    inbox: import_node_path13.default.join(root, "cadre", "messages", "inbox.jsonl"),
    outbox: import_node_path13.default.join(root, "cadre", "messages", "outbox.jsonl"),
    wisps: import_node_path13.default.join(root, "cadre", "local", "wisps")
  };
}
function compactStamp(value) {
  return value.replace(/[^0-9A-Za-z]+/g, "").slice(0, 16);
}
function nativeId(prefix, payload) {
  const recordedAt = asOptionalString(payload.recorded_at) || utcNow();
  return `${prefix}_${compactStamp(recordedAt)}_${textHash(JSON.stringify(payload)).slice(0, 12)}`;
}
function readJsonlObjects(file, limit = 200) {
  if (!fileExists(file)) return [];
  const lines = import_node_fs8.default.readFileSync(file, "utf8").split(/\r?\n/).filter(Boolean);
  const selected = limit > 0 ? lines.slice(-limit) : lines;
  const out = [];
  for (const line of selected) {
    try {
      out.push(asJsonObject(JSON.parse(line)));
    } catch {
    }
  }
  return out;
}
function countJsonlLines(file) {
  if (!fileExists(file)) return 0;
  return import_node_fs8.default.readFileSync(file, "utf8").split(/\r?\n/).filter(Boolean).length;
}
function ensureCadreLocalIgnored(root) {
  const cadreDir = import_node_path13.default.join(root, "cadre");
  const ignorePath = import_node_path13.default.join(cadreDir, ".gitignore");
  import_node_fs8.default.mkdirSync(cadreDir, { recursive: true });
  const current = fileExists(ignorePath) ? import_node_fs8.default.readFileSync(ignorePath, "utf8") : "";
  const lines = current.split(/\r?\n/).filter(Boolean);
  if (!lines.includes("/local/")) {
    lines.push("/local/");
    import_node_fs8.default.writeFileSync(ignorePath, `${lines.join("\n")}
`);
    return import_node_path13.default.relative(root, ignorePath);
  }
  return null;
}
function ensureNativeState(root) {
  const paths = nativeStatePaths(root);
  for (const dir of [
    asOptionalString(paths.formulas),
    asOptionalString(paths.operations),
    asOptionalString(paths.messages),
    asOptionalString(paths.wisps)
  ].filter((value) => Boolean(value))) {
    import_node_fs8.default.mkdirSync(dir, { recursive: true });
  }
  const ignore_path = ensureCadreLocalIgnored(root);
  return {
    ok: true,
    paths: {
      events: import_node_path13.default.relative(root, String(paths.events)),
      formulas: import_node_path13.default.relative(root, String(paths.formulas)),
      operations: import_node_path13.default.relative(root, String(paths.operations)),
      messages: import_node_path13.default.relative(root, String(paths.messages)),
      wisps: import_node_path13.default.relative(root, String(paths.wisps))
    },
    ignore_path
  };
}
function appendCadreEvent(root, event) {
  ensureNativeState(root);
  const recorded_at = asOptionalString(event.recorded_at) || utcNow();
  const kind = asOptionalString(event.kind || event.type) || "event";
  const actor = asOptionalString(event.actor) || gitIdentity(root) || null;
  const entry = {
    ...event,
    version: 1,
    schema: "cadre.event.v1",
    id: asOptionalString(event.id) || nativeId("evt", { ...event, recorded_at, kind }),
    kind,
    recorded_at,
    actor
  };
  const file = import_node_path13.default.join(root, "cadre", "events.jsonl");
  appendJsonl(file, entry);
  return { ok: true, path: import_node_path13.default.relative(root, file), event: entry };
}
function readCadreEvents(root, limit = 50) {
  return readJsonlObjects(import_node_path13.default.join(root, "cadre", "events.jsonl"), limit);
}
function appendCadreMessage(root, box, message) {
  ensureNativeState(root);
  const recorded_at = asOptionalString(message.recorded_at) || utcNow();
  const direction = box === "outbox" ? "outgoing" : "incoming";
  const entry = {
    ...message,
    version: 1,
    schema: "cadre.message.v1",
    id: asOptionalString(message.id) || nativeId(`msg_${box}`, { ...message, recorded_at, direction }),
    box,
    direction,
    status: asOptionalString(message.status) || "pending",
    recorded_at,
    from: asOptionalString(message.from) || gitIdentity(root) || null
  };
  const file = import_node_path13.default.join(root, "cadre", "messages", `${box}.jsonl`);
  appendJsonl(file, entry);
  return { ok: true, path: import_node_path13.default.relative(root, file), message: entry };
}
function readCadreMessages(root, box, limit = 50) {
  return readJsonlObjects(import_node_path13.default.join(root, "cadre", "messages", `${box}.jsonl`), limit);
}
function nativeStateSummary(root) {
  const paths = nativeStatePaths(root);
  return {
    ok: true,
    paths: {
      events: import_node_path13.default.relative(root, String(paths.events)),
      inbox: import_node_path13.default.relative(root, String(paths.inbox)),
      outbox: import_node_path13.default.relative(root, String(paths.outbox)),
      operations: import_node_path13.default.relative(root, String(paths.operations)),
      formulas: import_node_path13.default.relative(root, String(paths.formulas)),
      wisps: import_node_path13.default.relative(root, String(paths.wisps))
    },
    counts: {
      events: countJsonlLines(String(paths.events)),
      inbox: countJsonlLines(String(paths.inbox)),
      outbox: countJsonlLines(String(paths.outbox))
    },
    recent_events: readCadreEvents(root, 8),
    recent_inbox: readCadreMessages(root, "inbox", 8),
    recent_outbox: readCadreMessages(root, "outbox", 8)
  };
}

// src/core/application/runtime/track-mutations.ts
function reviewGate(root, trackId, options = {}) {
  const track = listTracks(root).find((item) => item.track_id === trackId);
  if (!track) {
    return { ok: false, track_id: trackId, reasons: [`Track not found: ${trackId}`] };
  }
  const config = loadTopology(root).config || {};
  const review = track.metadata.review || null;
  const reasons = [];
  const warnings = [];
  if (!review) {
    if (config.allow_unreviewed_ship === true) {
      warnings.push("No recorded review verdict; allowed by config.allow_unreviewed_ship");
    } else {
      reasons.push("No recorded review verdict");
    }
  } else {
    if (review.verdict !== "approved") {
      reasons.push(`Review verdict is ${review.verdict || "absent"}`);
    }
    if ((review.blocking_count || 0) > 0) {
      reasons.push(`Review has ${review.blocking_count} blocking finding(s)`);
    }
    if (config.require_second_reviewer === true && review.self_reviewed === true) {
      reasons.push("Self-review is not sufficient when require_second_reviewer is true");
    }
    const hasPinnedReview = Boolean(review.reviewed_sha) || Boolean(review.reviewed_shas && Object.values(review.reviewed_shas).some(Boolean));
    if (!hasPinnedReview) {
      if (config.allow_unpinned_review_ship === true) {
        warnings.push("Review does not record reviewed_sha/reviewed_shas; allowed by config.allow_unpinned_review_ship");
      } else {
        reasons.push("Review does not record reviewed_sha/reviewed_shas");
      }
    } else if (review.reviewed_sha && options.headSha && options.headSha !== review.reviewed_sha) {
      reasons.push(`Head ${options.headSha} differs from reviewed_sha ${review.reviewed_sha}; re-review required`);
    }
    if (review.reviewed_shas && options.headShas && typeof options.headShas === "object") {
      for (const [repo, reviewedSha] of Object.entries(review.reviewed_shas)) {
        const headSha = options.headShas[repo];
        if (typeof reviewedSha === "string" && typeof headSha === "string" && headSha !== reviewedSha) {
          reasons.push(`Repo ${repo} head ${headSha} differs from reviewed_shas.${repo} ${reviewedSha}; re-review required`);
        }
      }
    }
  }
  return {
    ok: reasons.length === 0,
    track_id: track.track_id,
    review,
    reasons,
    warnings
  };
}
function metadataPatch(root, args = {}) {
  const track = findTrack(root, args.trackId || args.track_id);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId || args.track_id}` };
  const patch = args.patch && typeof args.patch === "object" ? args.patch : null;
  if (!patch) return { ok: false, error: "patch object is required" };
  const result = patchJsonFile(track.metadata_path, (metadata) => ({ ...metadata, ...patch }), {
    root,
    lockName: trackLockName(track.track_id)
  });
  return {
    ok: result.ok,
    track_id: track.track_id,
    metadata_path: import_node_path14.default.relative(root, track.metadata_path),
    patch_keys: Object.keys(patch).sort(),
    result,
    event: result.ok ? appendCadreEvent(root, {
      kind: "metadata_updated",
      workflow: "metadata_patch",
      track_id: track.track_id,
      patch_keys: Object.keys(patch).sort(),
      tags: asStringArray(asJsonObject(patch).tags),
      depends_on: asStringArray(asJsonObject(patch).depends_on)
    }) : null
  };
}
function heartbeatTrack(root, args = {}) {
  const track = findTrack(root, args.trackId || args.track_id);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId || args.track_id}` };
  return withTrackLock(root, track.track_id, () => heartbeatTrackUnlocked(root, track, args));
}
function heartbeatTrackUnlocked(root, track, args = {}) {
  const identity = args.identity || gitIdentity(root) || track.metadata.owner || null;
  const now = asOptionalString(args.now) || utcNow();
  const topology = loadTopology(root);
  const metadataResult = patchJsonFile(track.metadata_path, (metadata) => {
    if (topology.config.sync_mode === "shared") {
      const existingLease = asJsonObject(metadata.lease);
      metadata.lease = {
        ...existingLease,
        owner: identity,
        acquired_at: asOptionalString(existingLease.acquired_at) || now,
        heartbeat_at: now
      };
    }
    metadata.owner = metadata.owner || identity;
    metadata.updated_at = now;
    return metadata;
  }, { lock: false });
  const statePath = import_node_path14.default.join(track.dir, "implement_state.json");
  let stateResult = null;
  if (fileExists(statePath)) {
    stateResult = patchJsonFile(statePath, (state) => ({
      ...state,
      owner: state.owner || identity,
      last_updated: now
    }), { lock: false });
  }
  return {
    ok: metadataResult.ok && (!stateResult || stateResult.ok),
    track_id: track.track_id,
    owner: identity,
    heartbeat_at: now,
    metadata: metadataResult,
    state: stateResult
  };
}
function claimTrack(root, trackId, options = {}) {
  const track = findTrack(root, trackId);
  if (!track) return { ok: false, error: `Track not found: ${trackId}` };
  return withTrackLock(root, track.track_id, () => claimTrackUnlocked(root, track, options));
}
function claimTrackUnlocked(root, track, options = {}) {
  const identity = options.identity || gitIdentity(root);
  if (!identity) return { ok: false, error: "No git identity found for claim" };
  const now = utcNow();
  const hold = holdInfo(track);
  const heldBy = hold.lease_owner || hold.owner;
  const stale = hold.lease_stale || hold.state_stale || !heldBy || heldBy === identity;
  if (heldBy && heldBy !== identity && !stale && options.takeover !== true) {
    return { ok: false, claimed: false, reason: "foreign-held", held_by: heldBy, hold };
  }
  const topology = loadTopology(root);
  const metadataResult = patchJsonFile(track.metadata_path, (metadata) => {
    metadata.owner = identity;
    metadata.updated_at = now;
    if (topology.config.sync_mode === "shared") {
      const existingLease = asJsonObject(metadata.lease);
      metadata.lease = {
        ...existingLease,
        owner: identity,
        acquired_at: existingLease.owner === identity ? asOptionalString(existingLease.acquired_at) || now : now,
        heartbeat_at: now
      };
    }
    return metadata;
  }, { lock: false });
  if (!metadataResult.ok) return { ok: false, claimed: false, error: "Metadata claim patch failed", metadata: metadataResult };
  const statePath = import_node_path14.default.join(track.dir, "implement_state.json");
  writeJson(statePath, {
    status: "starting",
    owner: identity,
    track_id: track.track_id,
    last_updated: now
  });
  const event = appendCadreEvent(root, {
    kind: "track_claimed",
    workflow: "claim",
    track_id: track.track_id,
    owner: identity,
    previous_owner: heldBy || null
  });
  return {
    ok: true,
    claimed: true,
    track_id: track.track_id,
    owner: identity,
    previous_hold: hold,
    metadata: metadataResult,
    event
  };
}
function setTrackStatus(root, trackId, status) {
  if (!VALID_STATUSES.has(status)) {
    return {
      ok: false,
      error: `Invalid status: ${status}`,
      valid_statuses: Array.from(VALID_STATUSES)
    };
  }
  const track = listTracks(root).find((item) => item.track_id === trackId);
  if (!track) {
    return { ok: false, error: `Track not found: ${trackId}` };
  }
  return withTrackLock(root, trackId, () => {
    const metadata = patchJsonFile(track.metadata_path, (current) => ({
      ...current,
      status
    }), { lock: false });
    if (!metadata.ok) {
      return { ok: false, track_id: trackId, status, stage: "metadata_patch", metadata };
    }
    const regen = regenIndex(root);
    const event = appendCadreEvent(root, {
      kind: "status_changed",
      workflow: "set_status",
      track_id: trackId,
      status
    });
    return {
      ok: Boolean(regen.ok),
      track_id: trackId,
      status,
      metadata,
      regen,
      event
    };
  });
}
function recordTaskResultUnlocked(root, args = {}) {
  const track = findTrack(root, args.trackId);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId}` };
  const plan = parsePlanFile(track.plan_path);
  const phaseIndex = Number(args.phaseIndex);
  const taskIndex = Number(args.taskIndex);
  const phase = (plan.phases || []).find((item) => item.phase_index === phaseIndex);
  const task = phase && (phase.tasks || []).find((item) => item.task_index === taskIndex);
  if (!task) return { ok: false, error: `Task not found: phase ${phaseIndex} task ${taskIndex}` };
  const planJsonPath = trackPlanJsonPath(track);
  const commitSha = args.commitSha ? String(args.commitSha).trim() : "";
  const recordedAt = utcNow();
  const lastTaskResult = {
    phase_index: phaseIndex,
    task_index: taskIndex,
    task_key: task.task_key,
    status: args.status || "completed",
    commit_sha: commitSha || null,
    repo: args.repo || task.repo || null,
    working_root: args.workingRoot || null,
    recorded_at: recordedAt
  };
  const metadata = patchJsonFile(track.metadata_path, (current) => {
    if (typeof args.coverage === "number") current.last_coverage = args.coverage;
    if (args.lastTestRun && typeof args.lastTestRun === "object") current.last_test_run = args.lastTestRun;
    if (isRecord(args.manualVerificationEvidence)) current.last_manual_verification_result = asJsonObject(args.manualVerificationEvidence);
    current.last_task_result = lastTaskResult;
    return current;
  });
  if (!metadata.ok) {
    return { ok: false, track_id: track.track_id, stage: "metadata_patch", metadata };
  }
  if (fileExists(planJsonPath)) {
    const planPatch = patchJsonFile(planJsonPath, (current) => {
      const phases = asArray(current.phases).map((rawPhase) => {
        const currentPhase = asJsonObject(rawPhase);
        if (Number(currentPhase.phase_index || currentPhase.index) !== phaseIndex) return currentPhase;
        return {
          ...currentPhase,
          tasks: asArray(currentPhase.tasks).map((rawTask) => {
            const currentTask = asJsonObject(rawTask);
            if (Number(currentTask.task_index || currentTask.index) !== taskIndex) return currentTask;
            const commitShas = Array.from(/* @__PURE__ */ new Set([
              ...asStringArray(currentTask.commit_shas),
              ...commitSha ? [commitSha.slice(0, 12)] : []
            ]));
            return {
              ...currentTask,
              status: args.status || "completed",
              commit_shas: commitShas,
              completion_evidence: {
                ...asJsonObject(currentTask.completion_evidence),
                commit_sha: commitSha || null,
                repo: args.repo || task.repo || null,
                working_root: args.workingRoot || null,
                coverage: typeof args.coverage === "number" ? args.coverage : null,
                recorded_at: recordedAt,
                ...isRecord(args.manualVerificationEvidence) ? { manual_verification: asJsonObject(args.manualVerificationEvidence) } : {}
              }
            };
          })
        };
      });
      return { ...current, phases, updated_at: recordedAt };
    });
    if (!planPatch.ok) {
      return { ok: false, track_id: track.track_id, stage: "plan_json_patch", metadata, plan_json: planPatch };
    }
    const nextPlan = asJsonObject(planPatch.value);
    import_node_fs9.default.writeFileSync(
      track.plan_path,
      withGeneratedMarker(import_node_path14.default.relative(root, planJsonPath), "cadre.plan.v1", renderPlanMarkdown(nextPlan))
    );
    return {
      ok: true,
      track_id: track.track_id,
      task_key: task.task_key,
      line: task.line,
      status: args.status || "completed",
      commit_sha: commitSha || null,
      event: appendCadreEvent(root, {
        kind: "task_result_recorded",
        workflow: "record_task_result",
        track_id: track.track_id,
        phase_index: phaseIndex,
        task_index: taskIndex,
        task_key: task.task_key,
        status: args.status || "completed",
        commit_sha: commitSha || null,
        repo: args.repo || task.repo || null
      }),
      metadata,
      plan_json: planPatch
    };
  }
  return {
    ok: false,
    track_id: track.track_id,
    stage: "missing_plan_json",
    error: `Missing canonical plan JSON: ${planJsonPath}`,
    metadata
  };
}
function recordTaskResult(root, args = {}) {
  const track = findTrack(root, args.trackId);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId}` };
  if (args.lock === false) return recordTaskResultUnlocked(root, args);
  return withTrackLock(root, track.track_id, () => recordTaskResultUnlocked(root, { ...args, lock: false }));
}

// src/core/application/runtime/quality-gates.ts
function testCoverage(root, args = {}) {
  let track = null;
  let task = null;
  let workingRoot = {
    repo: args.repo || ".",
    path: args.workingRoot ? import_node_path15.default.resolve(root, args.workingRoot) : root,
    source: args.workingRoot ? "argument.workingRoot" : "project-root"
  };
  if (args.trackId) {
    track = findTrack(root, args.trackId);
    if (!track) {
      return { ok: false, available: false, error: `Track not found: ${args.trackId}` };
    }
    if (args.phaseIndex != null && args.taskIndex != null) {
      const plan = parsePlanFile(track.plan_path);
      const phase = (plan.phases || []).find((item) => item.phase_index === Number(args.phaseIndex));
      task = phase?.tasks.find((item) => item.task_index === Number(args.taskIndex)) || null;
    }
    workingRoot = resolveTaskWorkingRoot(root, track, task, args);
    if (isWorkingRootError(workingRoot)) {
      return {
        ok: false,
        available: false,
        stage: "polyrepo_repo_resolution",
        working_root: workingRoot,
        reason: workingRoot.error
      };
    }
  }
  const coverageRun = runCoverage(root, args, workingRoot.path);
  if (!coverageRun.available) return coverageRun;
  const { command, coverage } = coverageRun;
  let task_result = null;
  let metadata = null;
  if (track) {
    const writeCoverage = () => {
      metadata = patchJsonFile(track.metadata_path, (current) => {
        current.last_test_run = {
          command,
          cwd: coverageRun.cwd || workingRoot.path,
          ok: coverageRun.ok,
          status: coverageRun.status,
          signal: coverageRun.signal,
          coverage,
          measured_at: utcNow()
        };
        if (typeof coverage === "number") current.last_coverage = coverage;
        return current;
      }, { lock: false });
      return metadata;
    };
    const metadataWrite = withTrackLock(root, track.track_id, writeCoverage);
    if (!metadataWrite.ok) return { ok: false, available: true, command, coverage, working_root: workingRoot, stage: "metadata_lock", metadata: metadataWrite };
    const metadataResult = metadata ?? metadataWrite;
    if (!metadataResult.ok) {
      return { ok: false, available: true, command, coverage, working_root: workingRoot, stage: "metadata_patch", metadata: metadataResult };
    }
    if (args.phaseIndex != null && args.taskIndex != null) {
      task_result = recordTaskResult(root, {
        trackId: args.trackId,
        phaseIndex: args.phaseIndex,
        taskIndex: args.taskIndex,
        status: args.status || (coverageRun.ok ? "completed" : "blocked"),
        commitSha: args.commitSha,
        coverage,
        repo: workingRoot.repo,
        workingRoot: import_node_path15.default.relative(root, workingRoot.path) || "."
      });
    }
  }
  return {
    ok: coverageRun.ok,
    available: true,
    command,
    cwd: coverageRun.cwd,
    status: coverageRun.status,
    signal: coverageRun.signal,
    coverage,
    coverage_source: coverageRun.coverage_source,
    timed_out: coverageRun.signal === "SIGTERM" || coverageRun.signal === "SIGKILL",
    stdout_tail: coverageRun.stdout_tail,
    stderr_tail: coverageRun.stderr_tail,
    working_root: workingRoot,
    metadata,
    task_result
  };
}
function configuredMachineGateCommand(root, args = {}, workingRoot = root) {
  const explicit = args.machineCommand || args.machine_command || args.command;
  if (explicit) return String(explicit);
  const config = loadTopology(root).config || {};
  for (const key of [
    "review_machine_gate_command",
    "machine_gate_command",
    "review_check_command",
    "typecheck_command",
    "build_command",
    "check_command"
  ]) {
    if (typeof config[key] === "string" && config[key].trim()) return config[key].trim();
  }
  const pkg = loadPackageJson(workingRoot);
  const scripts = pkg ? asJsonObject(pkg.scripts) : {};
  if (Object.keys(scripts).length > 0) {
    for (const name of ["typecheck", "check", "build", "lint"]) {
      if (scripts[name]) {
        if (fileExists(import_node_path15.default.join(workingRoot, "pnpm-lock.yaml"))) return `pnpm ${name}`;
        if (fileExists(import_node_path15.default.join(workingRoot, "yarn.lock"))) return `yarn ${name}`;
        return `npm run ${name}`;
      }
    }
  }
  return null;
}
function runMachineGate(root, args = {}, workingRoot = root) {
  const command = configuredMachineGateCommand(root, args, workingRoot);
  if (!command) {
    return {
      ok: true,
      available: false,
      reason: "No review machine-gate command configured or discovered",
      hints: [
        "Pass { machineCommand } explicitly",
        "Set cadre/config.json review_machine_gate_command",
        "Add a package script named typecheck, check, build, or lint"
      ]
    };
  }
  const timeoutMs = Number(args.timeoutMs || 10 * 60 * 1e3);
  const result = runCommand(command, [], {
    cwd: workingRoot,
    shell: true,
    timeoutMs,
    maxBuffer: 30 * 1024 * 1024
  });
  return {
    ok: result.ok,
    available: true,
    command,
    cwd: workingRoot,
    status: result.status,
    signal: result.signal,
    timed_out: result.signal === "SIGTERM" || result.signal === "SIGKILL",
    stdout_tail: result.stdout.slice(-4e3),
    stderr_tail: result.stderr.slice(-4e3)
  };
}
function reviewMachineGate(root, args = {}) {
  const trackId = args.trackId || args.track_id || null;
  const track = trackId ? findTrack(root, trackId) : null;
  if (trackId && !track) return { ok: false, error: `Track not found: ${trackId}` };
  if (track) {
    const repoError = repoEntriesError(root, track, args);
    if (repoError) return repoError;
  }
  const entries = track ? repoEntriesForTrack(root, track, args) : [{
    repo: args.repo || ".",
    root: args.workingRoot ? import_node_path15.default.resolve(root, args.workingRoot) : root,
    path: args.workingRoot || ".",
    source: args.workingRoot ? "argument.workingRoot" : "project-root"
  }];
  const results = entries.map((entry) => {
    const gate = runMachineGate(root, args, entry.root);
    return {
      repo: entry.repo,
      path: entry.path,
      source: entry.source,
      ...gate
    };
  });
  const blocking = results.filter((result) => result.available === true && !result.ok);
  return {
    ok: blocking.length === 0,
    available: results.some((result) => result.available === true),
    track_id: trackId,
    results,
    blocking_count: blocking.length
  };
}
function providerFromConfig(root, args = {}) {
  return asOptionalString(configuredProvider(root, args).provider_mode) || "local";
}
function providerEvidenceRequirement(root, args = {}) {
  const providerInfo = configuredProvider(root, args);
  const provider = asOptionalString(providerInfo.provider_mode) || "local";
  const readiness = providerReadiness(root, args);
  const track = args.trackId ? findTrack(root, args.trackId) : null;
  const branch = args.branch || track && (track.metadata.git_branch || `track/${track.track_id}`) || null;
  const target = args.pr || args.prNumber || args.mr || branch || null;
  const kind = provider === "gitlab" ? "gitlab_merge_request_status" : "github_pull_request_status";
  const minimumFields = provider === "gitlab" ? ["url", "state", "source_branch", "target_branch", "head_sha", "approvals", "pipeline_status", "discussions"] : ["url", "state", "head_ref", "base_ref", "head_sha", "review_decision", "status_checks", "workflow_runs", "comments"];
  return {
    ok: false,
    available: false,
    provider,
    target,
    branch,
    provider_mode: provider,
    required_provider_mcp: provider === "local" ? null : {
      provider,
      server: provider,
      purpose: "Fetch provider evidence through the installed provider MCP. CLI fallback is intentionally disabled."
    },
    mcp_readiness: readiness,
    detected_agent_capability_evidence: readiness.detected_agent_capability_evidence || null,
    missing_evidence_fields: readiness.missing_evidence_fields || [],
    required_evidence: provider === "local" ? null : {
      kind,
      provider,
      target,
      branch,
      minimum_fields: minimumFields,
      write_back: {
        tool: "cadre_review",
        action: "provider_evidence",
        trackId: args.trackId || args.track_id || null
      }
    },
    exact_write_back_packet: provider === "local" ? null : {
      tool: "cadre_review",
      arguments: {
        root,
        action: "provider_evidence",
        trackId: args.trackId || args.track_id || null,
        providerEvidence: `<${provider}-mcp-evidence>`
      }
    },
    next_actions: provider === "local" ? [] : [
      `Use the installed ${provider} MCP to fetch PR/MR metadata, reviews, checks or pipeline status, and discussion evidence for the target.`,
      "Call cadre_review with action provider_evidence and the fetched evidence before recording review or shipping."
    ],
    reason: provider === "local" ? "provider_mode is local; provider evidence is not required" : `${provider} provider evidence must come from the ${provider} MCP; CLI fallback is disabled`,
    unsupported_reason: provider === "local" ? null : `provider_mode ${provider} requires ${provider} MCP evidence; Cadre workflow packets do not use provider CLI fallback`
  };
}
function prCiStatus(root, args = {}) {
  const provider = providerFromConfig(root, args);
  const evidence = args.evidence || args.providerEvidence || args.provider_evidence || null;
  if (provider === "local") {
    return {
      ok: true,
      available: false,
      skipped: true,
      provider,
      provider_mode: "local",
      reason: "provider_mode is local; no provider MCP evidence required"
    };
  }
  if (provider !== "github" && provider !== "gitlab") {
    return { ok: false, available: false, provider, reason: `Unsupported provider_mode: ${provider}` };
  }
  if (evidence) {
    return {
      ok: true,
      available: true,
      provider,
      provider_mode: provider,
      evidence_source: `${provider}_mcp`,
      evidence: asJsonObject(evidence)
    };
  }
  return providerEvidenceRequirement(root, args);
}
function diffSurface(root, base, head) {
  const range = `${base}...${head}`;
  const stat = runCommand("git", ["diff", "--stat", range], { cwd: root, maxBuffer: 10 * 1024 * 1024 });
  const names = runCommand("git", ["diff", "--name-only", range], { cwd: root, maxBuffer: 10 * 1024 * 1024 });
  return {
    ok: stat.ok || names.ok,
    base,
    head,
    stat: stat.stdout.trim(),
    files: names.stdout.split(/\r?\n/).map((line) => line.trim()).filter(Boolean),
    errors: [stat.stderr, names.stderr].filter(Boolean).join("\n").trim()
  };
}
function scanReviewTodos(root, files, limit = 100) {
  const findings = [];
  const patterns = [
    /\bTODO\b/i,
    /\bFIXME\b/i,
    /\bstub\b/i,
    /throw new Error\(["']not implemented/i
  ];
  for (const file of files || []) {
    if (isIgnoredRepoMapFile(file)) continue;
    const abs = import_node_path15.default.join(root, file);
    if (!fileExists(abs)) continue;
    let stat;
    try {
      stat = import_node_fs10.default.statSync(abs);
    } catch {
      continue;
    }
    if (stat.size > 1024 * 1024) continue;
    const lines = import_node_fs10.default.readFileSync(abs, "utf8").split(/\r?\n/);
    for (let index = 0; index < lines.length && findings.length < limit; index += 1) {
      const line = lines[index] || "";
      if (patterns.some((pattern) => pattern.test(line))) {
        findings.push({ file, line: index + 1, snippet: line.trim().slice(0, 180) });
      }
    }
  }
  return findings;
}
function reviewAssist(root, args = {}) {
  const trackId = args.trackId || args.track_id;
  if (!trackId) return { ok: false, error: "trackId is required" };
  const context = trackContext(root, trackId);
  if (!context.ok) return context;
  const track = findTrack(root, trackId);
  if (!track) return { ok: false, error: `Track not found: ${trackId}` };
  const repoError = repoEntriesError(root, track, args);
  if (repoError) return repoError;
  const plan = parsePlanFile(track.plan_path);
  const base = args.base || "main";
  const head = args.head || track.metadata.git_branch || "HEAD";
  const repoEntries = repoEntriesForTrack(root, track, args);
  const repoDiffs = repoEntries.map((entry) => ({
    repo: entry.repo,
    path: entry.path,
    cwd: entry.root,
    source: entry.source,
    ...diffSurface(entry.root, entry.base || base, entry.head || head)
  }));
  const diff = repoDiffs.find((entry) => entry.repo === ".") || diffSurface(root, base, head);
  const incompleteTasks = [];
  for (const phase of plan.phases) {
    for (const task of phase.tasks) {
      if (task.marker !== "x" && task.marker !== "-") {
        incompleteTasks.push({
          phase: phase.phase_index,
          task: task.task_index,
          task_key: task.task_key,
          title: task.title,
          marker: task.marker,
          repo: task.repo || null
        });
      }
    }
  }
  const todoLimit = Number(args.todoLimit || 100);
  const repoTodos = repoDiffs.map((entry) => ({
    repo: entry.repo,
    path: entry.path,
    cwd: entry.cwd,
    todos: scanReviewTodos(entry.cwd || root, entry.files, todoLimit)
  }));
  const todos = repoTodos.flatMap((entry) => entry.todos.map((todo) => ({ ...todo, repo: entry.repo }))).slice(0, todoLimit);
  const lsp = args.includeLsp === false ? null : args.lspResult || args.lsp_result || lspReview(root, { base, head, config: args.config });
  const machineGate = args.includeMachine === false ? null : reviewMachineGate(root, args);
  const lspObject = asJsonObject(lsp);
  const machineGateObject = asJsonObject(machineGate);
  const blocking = [];
  if (incompleteTasks.length > 0) blocking.push(`${incompleteTasks.length} plan task(s) are not completed or skipped`);
  if (todos.length > 0) blocking.push(`${todos.length} TODO/FIXME/stub marker(s) found in changed files`);
  if (track.metadata.last_coverage == null) blocking.push("No measured coverage recorded on the track");
  if (lsp && lspObject.available !== false && Array.isArray(lspObject.findings)) {
    const lspBlocking = asArray(lspObject.findings).filter((finding) => finding.severity === "blocking" || finding.blocking === true);
    if (lspBlocking.length > 0) blocking.push(`${lspBlocking.length} blocking LSP/code-intelligence finding(s)`);
  }
  const machineBlockingCount = asNumber(machineGateObject.blocking_count);
  if (machineGate && machineBlockingCount > 0) {
    blocking.push(`${machineBlockingCount} machine gate check(s) failed`);
  }
  const result = {
    ok: true,
    root,
    track_id: trackId,
    base,
    head,
    diff,
    repo_diffs: repoDiffs,
    task_counts: context.task_counts,
    incomplete_tasks: incompleteTasks,
    coverage: track.metadata.last_coverage ?? null,
    todos,
    repo_todos: repoTodos,
    lsp,
    machine_gate: machineGate,
    suggested_verdict: blocking.length === 0 ? "approved" : "changes_requested",
    blocking_reasons: blocking
  };
  const mode2 = (asOptionalString(args.responseMode || args.response_mode || (args.detail === true ? "detail" : null)) || "compact").toLowerCase();
  if (["detail", "detailed", "full", "verbose"].includes(mode2)) return result;
  const lspSummary = asJsonObject(result.lsp);
  const gateSummary = asJsonObject(result.machine_gate);
  const diffSummary = asJsonObject(diff);
  const diffFiles = Array.isArray(diffSummary.files) ? diffSummary.files : [];
  return {
    ok: true,
    root,
    track_id: trackId,
    base,
    head,
    task_counts: context.task_counts,
    diff: {
      repo: diffSummary.repo || ".",
      file_count: diffFiles.length,
      stat: asOptionalString(diffSummary.stat) || ""
    },
    repo_diffs: repoDiffs.slice(0, 20).map((entry) => ({
      repo: entry.repo,
      path: entry.path,
      files: Array.isArray(entry.files) ? entry.files.slice(0, 50) : [],
      file_count: Array.isArray(entry.files) ? entry.files.length : 0,
      stat: entry.stat
    })),
    repo_diffs_count: repoDiffs.length,
    incomplete_tasks: incompleteTasks.slice(0, 20),
    incomplete_tasks_count: incompleteTasks.length,
    coverage: track.metadata.last_coverage ?? null,
    todos: todos.slice(0, 20),
    todos_count: todos.length,
    lsp: lsp ? {
      available: lspSummary.available !== false,
      degraded: lspSummary.degraded === true || lspSummary.fallback === "text_scan",
      findings_count: Array.isArray(lspSummary.findings) ? lspSummary.findings.length : 0,
      server_count: Array.isArray(lspSummary.servers) ? lspSummary.servers.length : 0,
      polyrepo: lspSummary.polyrepo === true
    } : null,
    machine_gate: machineGate ? {
      ok: gateSummary.ok !== false,
      available: gateSummary.available !== false,
      blocking_count: Number(gateSummary.blocking_count || 0),
      result_count: Array.isArray(gateSummary.results) ? gateSummary.results.length : 0
    } : null,
    suggested_verdict: result.suggested_verdict,
    blocking_reasons: blocking,
    detail_available: true
  };
}

// src/core/application/runtime/review-records.ts
function recordReview(root, args = {}) {
  const track = findTrack(root, args.trackId);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId}` };
  return withTrackLock(root, track.track_id, () => recordReviewUnlocked(root, track, args));
}
function recordReviewUnlocked(root, track, args = {}) {
  const verdict = args.verdict || "";
  if (!["approved", "changes_requested"].includes(verdict)) {
    return { ok: false, error: `Invalid review verdict: ${verdict}` };
  }
  const reviewer = args.reviewer || gitIdentity(root);
  const pins = reviewedShasForTrack(root, track, args);
  if (pins.ok === false) return pins;
  const metadata = patchJsonFile(track.metadata_path, (current) => {
    const existing = asJsonObject(current.review);
    const existingReviewer = asOptionalString(existing.reviewer);
    const existingVerdict = asOptionalString(existing.verdict);
    const existingBlockingCount = Number(existing.blocking_count || 0);
    if (verdict === "approved" && args.allowOverride !== true && existingReviewer && existingReviewer !== reviewer && (existingVerdict === "changes_requested" || existingBlockingCount > 0)) {
      throw new Error("Approval would override another reviewer's open changes_requested verdict");
    }
    current.review = {
      verdict,
      blocking_count: Number(args.blockingCount || 0),
      date: args.date || utcNow(),
      reviewer: reviewer || null,
      coverage: args.coverage ?? current.last_coverage ?? null,
      self_reviewed: Boolean(reviewer && current.owner && reviewer === current.owner),
      reviewed_sha: asOptionalString(pins.reviewed_sha) || null,
      reviewed_shas: asJsonObject(pins.reviewed_shas),
      review_seq: Number(existing.review_seq || 0) + 1
    };
    return current;
  }, { lock: false });
  if (!metadata.ok) {
    return {
      ok: false,
      track_id: track.track_id,
      stage: "metadata_patch",
      error: metadata.error,
      requires_override: /override another reviewer/.test(metadata.error || ""),
      metadata
    };
  }
  const gate = reviewGate(root, track.track_id, args);
  return { ok: true, track_id: track.track_id, review: asJsonObject(metadata.value).review, metadata, gate };
}
function reviewEvidencePath(track) {
  return import_node_path16.default.join(track.dir, "review-evidence.json");
}
function reviewEvidence(root, trackId) {
  const track = findTrack(root, trackId);
  if (!track) return { ok: false, error: `Track not found: ${trackId}` };
  const evidencePath = reviewEvidencePath(track);
  const evidence = readJson(evidencePath, {
    track_id: track.track_id,
    entries: []
  });
  return {
    ok: true,
    track_id: track.track_id,
    path: import_node_path16.default.relative(root, evidencePath),
    evidence
  };
}
function providerEvidence(root, args = {}) {
  const track = findTrack(root, args.trackId || args.track_id);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId || args.track_id}` };
  return withTrackLock(root, track.track_id, () => providerEvidenceUnlocked(root, track, args));
}
function providerEvidenceUnlocked(root, track, args = {}) {
  const evidencePath = reviewEvidencePath(track);
  const existing = readJson(evidencePath, {
    track_id: track.track_id,
    entries: []
  });
  const entries = Array.isArray(existing.entries) ? existing.entries.map(asJsonObject) : [];
  const findings = Array.isArray(args.findings) ? args.findings.map(asJsonObject) : [];
  const blockingCount = findings.filter(
    (finding) => finding.blocking === true || asString(finding.severity) === "blocking"
  ).length;
  const provider = args.provider || providerFromConfig(root, args);
  const fetched = args.evidence || args.providerEvidence || args.provider_evidence || null;
  const providerStatus = fetched ? asJsonObject(fetched) : null;
  if (!providerStatus && args.fetch !== false) {
    return {
      ok: false,
      track_id: track.track_id,
      stage: "provider_mcp_evidence_required",
      provider,
      requirement: providerEvidenceRequirement(root, { ...args, trackId: track.track_id })
    };
  }
  const entry = {
    id: `review-${entries.length + 1}`,
    recorded_at: utcNow(),
    provider,
    reviewer: args.reviewer || gitIdentity(root) || null,
    reviewed_sha: args.reviewedSha || args.reviewed_sha || gitRevParse(root, "HEAD"),
    blocking_count: Number(args.blockingCount ?? blockingCount),
    verdict: args.verdict || null,
    findings,
    evidence: providerStatus,
    notes: asOptionalString(args.notes) || null
  };
  const next = {
    ...existing,
    track_id: track.track_id,
    entries: [...entries, entry],
    updated_at: entry.recorded_at
  };
  writeJson(evidencePath, next);
  appendJsonl(import_node_path16.default.join(track.dir, "review-evidence.jsonl"), entry);
  const metadata = patchJsonFile(track.metadata_path, (current) => {
    current.review_evidence = {
      path: import_node_path16.default.relative(root, evidencePath),
      entries: asArray(next.entries).length || entries.length + 1,
      latest_id: entry.id,
      latest_recorded_at: entry.recorded_at,
      provider,
      blocking_count: entry.blocking_count
    };
    return current;
  }, { lock: false });
  if (!metadata.ok) return { ok: false, track_id: track.track_id, stage: "metadata_patch", metadata };
  return {
    ok: true,
    track_id: track.track_id,
    path: import_node_path16.default.relative(root, evidencePath),
    entry,
    metadata
  };
}
function syncControlPlane(root, args = {}) {
  const topology = loadTopology(root);
  if (topology.config.sync_mode !== "shared") {
    return { ok: true, skipped: true, reason: "sync_mode is not shared", commands: [] };
  }
  const mode2 = args.mode || "pre";
  const remote = asOptionalString(topology.config.control_remote) || "origin";
  const branch = asOptionalString(topology.config.control_branch) || "main";
  const commands = [];
  commands.push(runCommand("git", ["config", "merge.ours.driver", "true"], { cwd: root }));
  const safety = controlPlaneSyncSafety(root, mode2, remote, branch);
  if (!safety.ok) {
    return { ok: false, mode: mode2, remote, branch, safety, commands };
  }
  if (mode2 === "pre") {
    commands.push(runCommand("git", ["pull", "--rebase", remote, branch], { cwd: root }));
  } else if (mode2 === "post") {
    commands.push(runCommand("git", ["push", remote, branch], { cwd: root }));
  } else {
    return { ok: false, error: `Invalid sync mode: ${mode2}`, commands };
  }
  return { ok: commands.every((cmd) => cmd.ok), mode: mode2, remote, branch, safety, commands };
}

// src/core/application/runtime/text-utils.ts
function compactLines(value, limit = 1200) {
  return String(value || "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean).join("\n").slice(0, limit);
}

// src/core/application/runtime/spec-docs.ts
function normalizedSpecHeading(heading) {
  return heading.toLowerCase().replace(/&/g, " and ").replace(/[^a-z0-9]+/g, " ").trim();
}
function specSectionKind(heading) {
  const normalized = normalizedSpecHeading(heading);
  if (["description", "summary", "overview", "goal", "goals", "objective", "objectives", "context", "problem"].includes(normalized)) {
    return "description";
  }
  if ([
    "functional requirements",
    "functional requirement",
    "fr",
    "requirements",
    "requirement",
    "user facing behavior",
    "user facing behaviours",
    "user behavior",
    "user behaviour",
    "behavior",
    "behaviour"
  ].includes(normalized)) {
    return "functional_requirements";
  }
  if ([
    "non functional requirements",
    "non functional requirement",
    "nonfunctional requirements",
    "nonfunctional requirement",
    "nfr",
    "constraints",
    "constraint",
    "quality attributes",
    "quality attribute",
    "performance requirements",
    "security requirements"
  ].includes(normalized)) {
    return "non_functional_requirements";
  }
  if (["acceptance criteria", "acceptance criterion", "acceptance", "success criteria", "definition of done", "done"].includes(normalized)) {
    return "acceptance_criteria";
  }
  if (["out of scope", "out scope", "out of scopes", "non goals", "non goal", "nongoals", "exclusions", "exclusion"].includes(normalized)) {
    return "out_of_scope";
  }
  return null;
}
function cleanSpecItem(value) {
  const heading = compactLines(asOptionalString(value.heading || value.title || value.name) || "", 240);
  const body = normalizedText(asOptionalString(value.body || value.description || value.text) || "").trim();
  if (!heading && !body) return null;
  return { heading: heading || "Item", body };
}
function parseSpecListItem(line) {
  const text = line.trim();
  const bold = text.match(/^\*\*(.+?)\*\*:?\s*(.*)$/);
  const keyed = !bold ? text.match(/^(.+?)\s*(?::|\s+-\s+|\s+--\s+|\s+—\s+)\s*(.+)$/) : null;
  if (bold?.[1]) return { heading: bold[1].trim(), body: (bold[2] || "").trim() };
  if (keyed?.[1]) return { heading: keyed[1].trim(), body: (keyed[2] || "").trim() };
  return { heading: text, body: "" };
}
function parseSpecListItems(body, fallbackHeading) {
  const lines = normalizedText(body).split("\n");
  const items = [];
  let current = null;
  const pushCurrent = () => {
    if (!current) return;
    const cleaned = cleanSpecItem(current);
    if (cleaned) items.push(cleaned);
    current = null;
  };
  for (const rawLine of lines) {
    const bullet = rawLine.match(/^\s*(?:[-*+]|\d+[.)])\s+(.+)$/);
    if (bullet?.[1]) {
      pushCurrent();
      current = parseSpecListItem(bullet[1]);
      continue;
    }
    const text = rawLine.trim();
    if (!text) continue;
    if (!current) {
      current = { heading: fallbackHeading, body: text };
      continue;
    }
    const existing = asOptionalString(current.body) || "";
    current.body = existing ? `${existing}
${text}` : text;
  }
  pushCurrent();
  if (items.length > 0) return items;
  const fallbackBody = normalizedText(body).trim();
  return fallbackBody ? [{ heading: fallbackHeading, body: fallbackBody }] : [];
}
function specItemsFromRaw(value) {
  return asArray(value).map((entry) => {
    if (typeof entry === "string") return cleanSpecItem({ heading: entry, body: "" });
    return cleanSpecItem(asJsonObject(entry));
  }).filter((entry) => Boolean(entry));
}
function specJsonFromText(trackId, text) {
  const parsed = splitMarkdownSections(text || `# Spec: ${trackId}
`);
  const descriptionParts = [parsed.body].filter(Boolean);
  const functionalRequirements = [];
  const nonFunctionalRequirements = [];
  const acceptanceCriteria = [];
  const outOfScope = [];
  for (const rawSection of parsed.sections) {
    const section = asJsonObject(rawSection);
    const heading = asOptionalString(section.heading) || "";
    const body = asOptionalString(section.body) || "";
    const kind = specSectionKind(heading);
    if (kind === "description") descriptionParts.push(body);
    else if (kind === "functional_requirements") functionalRequirements.push(...parseSpecListItems(body, heading));
    else if (kind === "non_functional_requirements") nonFunctionalRequirements.push(...parseSpecListItems(body, heading));
    else if (kind === "acceptance_criteria") acceptanceCriteria.push(...parseSpecListItems(body, heading));
    else if (kind === "out_of_scope") outOfScope.push(...parseSpecListItems(body, heading));
    else if (body) descriptionParts.push(`### ${heading}

${body}`);
  }
  return {
    version: 1,
    schema: "cadre.spec.v1",
    kind: "spec",
    track_id: trackId,
    title: parsed.title,
    description: descriptionParts.join("\n\n").trim(),
    functional_requirements: functionalRequirements,
    non_functional_requirements: nonFunctionalRequirements,
    acceptance_criteria: acceptanceCriteria,
    out_of_scope: outOfScope,
    updated_at: utcNow()
  };
}
function normalizedSpecFromRaw(raw) {
  if (asOptionalString(raw.description) || asArray(raw.functional_requirements).length > 0 || asArray(raw.non_functional_requirements).length > 0 || asArray(raw.acceptance_criteria).length > 0 || asArray(raw.out_of_scope).length > 0) {
    return {
      ...raw,
      description: asOptionalString(raw.description || raw.summary) || "",
      functional_requirements: specItemsFromRaw(raw.functional_requirements),
      non_functional_requirements: specItemsFromRaw(raw.non_functional_requirements),
      acceptance_criteria: specItemsFromRaw(raw.acceptance_criteria),
      out_of_scope: specItemsFromRaw(raw.out_of_scope)
    };
  }
  const title = asOptionalString(raw.title) || `Spec: ${asOptionalString(raw.track_id) || "track"}`;
  const parts = [`# ${title}`, ""];
  const summary = asOptionalString(raw.summary);
  if (summary) parts.push(summary, "");
  for (const rawSection of asArray(raw.sections)) {
    const section = asJsonObject(rawSection);
    const heading = asOptionalString(section.heading);
    const body = asOptionalString(section.body);
    if (heading && body) parts.push(`## ${heading}`, "", body, "");
  }
  return specJsonFromText(asOptionalString(raw.track_id) || "track", parts.join("\n"));
}
function appendSpecItemSection(parts, heading, items) {
  if (items.length === 0) return;
  parts.push(`## ${heading}`, "");
  for (const item of items) {
    const itemHeading = asOptionalString(item.heading) || "Item";
    const body = normalizedText(asOptionalString(item.body) || "").trim();
    if (!body) {
      parts.push(`- ${itemHeading}`);
      continue;
    }
    if (!body.includes("\n")) {
      parts.push(`- **${itemHeading}**: ${body}`);
      continue;
    }
    parts.push(`- **${itemHeading}**`);
    for (const line of body.split("\n")) {
      parts.push(line.trim() ? `  ${line}` : "");
    }
  }
  parts.push("");
}
function renderSpecMarkdown(raw) {
  const spec = normalizedSpecFromRaw(raw);
  const title = asOptionalString(spec.title) || `Spec: ${asOptionalString(spec.track_id) || "track"}`;
  const parts = [`# ${title}`, ""];
  const description = asOptionalString(spec.description);
  if (description) parts.push("## Description", "", description, "");
  appendSpecItemSection(parts, "Functional Requirements", specItemsFromRaw(spec.functional_requirements));
  appendSpecItemSection(parts, "Non-Functional Requirements", specItemsFromRaw(spec.non_functional_requirements));
  appendSpecItemSection(parts, "Acceptance Criteria", specItemsFromRaw(spec.acceptance_criteria));
  appendSpecItemSection(parts, "Out Of Scope", specItemsFromRaw(spec.out_of_scope));
  return normalizedText(parts.join("\n"));
}
function renderStyleGuideMarkdown(raw) {
  const id = asOptionalString(raw.id) || "styleguide";
  const title = asOptionalString(raw.title) || id;
  const parts = [`# ${title}`, ""];
  const summary = asOptionalString(raw.summary);
  if (summary) parts.push(summary, "");
  const rules = asArray(raw.rules);
  if (rules.length > 0) {
    parts.push("## Rules", "");
    for (const rawRule of rules) {
      const rule = asJsonObject(rawRule);
      const severity = asOptionalString(rule.severity);
      const suffix = severity ? ` (${severity})` : "";
      parts.push(`- ${asOptionalString(rule.summary) || asOptionalString(rule.id) || "Rule"}${suffix}`);
      const rationale = asOptionalString(rule.rationale);
      if (rationale) parts.push(`  Rationale: ${rationale}`);
    }
    parts.push("");
  }
  for (const rawSection of asArray(raw.sections)) {
    const section = asJsonObject(rawSection);
    const heading = asOptionalString(section.heading);
    const body = asOptionalString(section.body);
    if (heading && body) parts.push(`## ${heading}`, "", body, "");
  }
  return normalizedText(parts.join("\n"));
}

// src/core/application/runtime/packaged-assets.ts
var import_node_fs11 = __toESM(require("node:fs"));
var import_node_path17 = __toESM(require("node:path"));
var SEARCH_DEPTH = 8;
function embeddedAssets() {
  return typeof __CADRE_EMBEDDED_ASSETS__ !== "undefined" ? __CADRE_EMBEDDED_ASSETS__ : null;
}
function normalizeRelativePath(value) {
  return value.replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+/g, "/");
}
function unique2(values) {
  return Array.from(new Set(values));
}
function isFile(file) {
  try {
    return import_node_fs11.default.statSync(file).isFile();
  } catch {
    return false;
  }
}
function isDir(dir) {
  try {
    return import_node_fs11.default.statSync(dir).isDirectory();
  } catch {
    return false;
  }
}
function readJsonFile(file) {
  try {
    return asJsonObject(JSON.parse(import_node_fs11.default.readFileSync(file, "utf8")));
  } catch {
    return null;
  }
}
function walkingCandidates(factory) {
  const candidates = [];
  let dir = __dirname;
  for (let depth = 0; depth < SEARCH_DEPTH; depth += 1) {
    candidates.push(...factory(dir));
    const parent = import_node_path17.default.dirname(dir);
    if (parent === dir) break;
    dir = parent;
  }
  return unique2(candidates);
}
function findFile(candidates) {
  return candidates.find(isFile) || null;
}
function jsonFiles(dir) {
  try {
    return import_node_fs11.default.readdirSync(dir).filter((file) => file.endsWith(".json")).map((file) => import_node_path17.default.join(dir, file)).filter(isFile).sort();
  } catch {
    return [];
  }
}
function walkTemplateFiles(dir, base = dir) {
  const files = [];
  for (const entry of import_node_fs11.default.readdirSync(dir, { withFileTypes: true })) {
    const full = import_node_path17.default.join(dir, entry.name);
    if (entry.isDirectory()) {
      files.push(...walkTemplateFiles(full, base));
    } else if (entry.isFile()) {
      files.push(import_node_path17.default.relative(base, full).split(import_node_path17.default.sep).join("/"));
    }
  }
  return files;
}
function referenceDirs() {
  return walkingCandidates((dir) => [
    import_node_path17.default.join(dir, "assets", "cadre", "references"),
    import_node_path17.default.join(dir, "references"),
    import_node_path17.default.join(dir, "scripts", "agent-refs"),
    import_node_path17.default.join(dir, "skills", "cadre", "references")
  ]).filter(isDir);
}
function protocolDirs() {
  return walkingCandidates((dir) => [
    import_node_path17.default.join(dir, "assets", "cadre", "protocols"),
    import_node_path17.default.join(dir, "skills", "cadre", "protocols"),
    import_node_path17.default.join(dir, "protocols")
  ]).filter(isDir);
}
function packagedSkillContract() {
  const embedded = embeddedAssets()?.skill;
  if (embedded) return embedded;
  const file = findFile(walkingCandidates((dir) => [
    import_node_path17.default.join(dir, "assets", "cadre", "skill.json"),
    import_node_path17.default.join(dir, "skills", "cadre", "skill.json"),
    import_node_path17.default.join(dir, "skill.json")
  ]));
  return file ? readJsonFile(file) : null;
}
function packagedWorkflowProtocols() {
  const embedded = embeddedAssets()?.protocols;
  if (embedded) {
    return Object.values(embedded).map(asJsonObject).sort((left, right) => String(left.workflow || left.id || "").localeCompare(String(right.workflow || right.id || "")));
  }
  const protocols = protocolDirs().flatMap(jsonFiles).map(readJsonFile).filter((protocol) => protocol !== null);
  return protocols.sort((left, right) => String(left.workflow || left.id || "").localeCompare(String(right.workflow || right.id || "")));
}
function packagedWorkflowProtocol(workflow) {
  const wanted = asOptionalString(workflow)?.trim();
  if (!wanted) return null;
  return packagedWorkflowProtocols().find((protocol) => {
    const id = asOptionalString(protocol.id) || "";
    const protocolWorkflow = asOptionalString(protocol.workflow) || "";
    return protocolWorkflow === wanted || id === wanted || id === `cadre-${wanted}`;
  }) || null;
}
function packagedAgentReferences() {
  const embedded = embeddedAssets()?.references;
  if (embedded) {
    return Object.values(embedded).map(asJsonObject).sort((left, right) => String(left.id || "").localeCompare(String(right.id || "")));
  }
  const seen = /* @__PURE__ */ new Set();
  const references = [];
  for (const file of referenceDirs().flatMap(jsonFiles)) {
    const reference = readJsonFile(file);
    const id = asOptionalString(reference?.id) || import_node_path17.default.basename(file, ".json");
    if (!reference || seen.has(id)) continue;
    seen.add(id);
    references.push(reference);
  }
  return references.sort((left, right) => String(left.id || "").localeCompare(String(right.id || "")));
}
function packagedAgentReference(id) {
  const wanted = asOptionalString(id)?.trim();
  if (!wanted) return null;
  const embedded = embeddedAssets()?.references?.[wanted];
  if (embedded) return embedded;
  const file = findFile(walkingCandidates((dir) => [
    import_node_path17.default.join(dir, "assets", "cadre", "references", `${wanted}.json`),
    import_node_path17.default.join(dir, "references", `${wanted}.json`),
    import_node_path17.default.join(dir, "scripts", "agent-refs", `${wanted}.json`),
    import_node_path17.default.join(dir, "skills", "cadre", "references", `${wanted}.json`)
  ]));
  return file ? readJsonFile(file) : null;
}
function packagedTemplatePath(relativePath) {
  const normalized = normalizeRelativePath(relativePath);
  return findFile(walkingCandidates((dir) => [
    import_node_path17.default.join(dir, "assets", "cadre", "templates", normalized),
    import_node_path17.default.join(dir, "templates", normalized),
    import_node_path17.default.join(dir, "skills", "cadre", "templates", normalized)
  ]));
}
function packagedTemplateText(relativePath) {
  const normalized = normalizeRelativePath(relativePath);
  const embedded = embeddedAssets()?.templates?.[normalized];
  if (typeof embedded === "string") return embedded;
  const file = packagedTemplatePath(normalized);
  return file ? import_node_fs11.default.readFileSync(file, "utf8") : null;
}
function packagedTemplateJson(relativePath) {
  const text = packagedTemplateText(relativePath);
  if (text === null) return null;
  try {
    return asJsonObject(JSON.parse(text));
  } catch {
    return null;
  }
}
function packagedTemplatePaths(prefix = "") {
  const normalizedPrefix = normalizeRelativePath(prefix).replace(/\/$/, "");
  const embedded = embeddedAssets()?.templates;
  if (embedded) {
    return Object.keys(embedded).filter((file) => !normalizedPrefix || file === normalizedPrefix || file.startsWith(`${normalizedPrefix}/`)).sort();
  }
  const manifest = packagedTemplatePath("manifest.json");
  if (!manifest) return [];
  const root = import_node_path17.default.dirname(manifest);
  return walkTemplateFiles(root).filter((file) => !normalizedPrefix || file === normalizedPrefix || file.startsWith(`${normalizedPrefix}/`)).sort();
}
function packagedTemplateSource(relativePath) {
  const normalized = normalizeRelativePath(relativePath);
  if (typeof embeddedAssets()?.templates?.[normalized] === "string") return `embedded:${normalized}`;
  return packagedTemplatePath(normalized);
}

// src/core/application/runtime/response-compaction.ts
function jsonByteLength(value) {
  return Buffer.byteLength(JSON.stringify(value ?? null), "utf8");
}
function compactTemplateManifest(value) {
  const manifest = asJsonObject(value);
  const templates = Array.isArray(manifest.templates) ? manifest.templates.map(asJsonObject) : [];
  return {
    ...manifest,
    templates: templates.map((template) => ({
      id: asOptionalString(template.id) || null,
      path: asOptionalString(template.path) || null,
      scope: asOptionalString(template.scope) || null,
      kind: asOptionalString(template.kind) || null
    })),
    template_count: templates.length,
    content_in_response: false,
    resource_uri: "cadre://template-inventory",
    detail_bytes: jsonByteLength(value)
  };
}
function compactStyleGuides(value) {
  const guides = asJsonObject(value);
  return {
    ok: guides.ok !== false,
    valid: guides.valid !== false,
    source: asOptionalString(guides.source) || null,
    detected: asStringArray(guides.detected),
    requested: asStringArray(guides.requested),
    selected: asStringArray(guides.selected),
    missing: asStringArray(guides.missing),
    warnings: asStringArray(guides.warnings),
    written: asStringArray(guides.written),
    skipped: asStringArray(guides.skipped),
    selected_count: asStringArray(guides.selected).length,
    content_in_response: false,
    detail_bytes: jsonByteLength(value),
    resource_uri: "cadre://styleguide-selection"
  };
}
function compactReviewArtifacts(value) {
  const artifacts = Array.isArray(value) ? value.map(asJsonObject) : [];
  return {
    count: artifacts.length,
    total_bytes: artifacts.reduce((sum, artifact) => sum + Number(artifact.bytes || 0), 0),
    files: artifacts.slice(0, 30).map((artifact) => ({
      path: asOptionalString(artifact.path) || null,
      title: asOptionalString(artifact.title) || null,
      kind: asOptionalString(artifact.kind) || null,
      source: asOptionalString(artifact.source) || null,
      bytes: Number(artifact.bytes || 0),
      missing: artifact.missing === true
    })),
    truncated: artifacts.length > 30,
    content_in_response: false
  };
}
function compactReviewBundle(value) {
  if (!value || !isRecord(value)) return null;
  const bundle = asJsonObject(value);
  const files = Array.isArray(bundle.files) ? bundle.files.map(asJsonObject) : [];
  return {
    directory: asOptionalString(bundle.directory) || null,
    manifest_path: asOptionalString(bundle.manifest_path) || null,
    content_in_response: false,
    warnings: asStringArray(bundle.warnings),
    file_count: files.length,
    total_bytes: files.reduce((sum, file) => sum + Number(file.bytes || 0), 0),
    files: files.slice(0, 30).map((file) => ({
      path: asOptionalString(file.path) || null,
      review_path: asOptionalString(file.review_path) || null,
      title: asOptionalString(file.title) || null,
      kind: asOptionalString(file.kind) || null,
      bytes: Number(file.bytes || 0),
      missing: file.missing === true
    })),
    truncated: files.length > 30
  };
}
function compactProvider(value) {
  if (!value || !isRecord(value)) return null;
  const provider = asJsonObject(value);
  return {
    ok: provider.ok !== false,
    provider_mode: asOptionalString(provider.provider_mode) || asOptionalString(provider.provider) || "local",
    available: typeof provider.available === "boolean" ? provider.available : null,
    required_provider_mcp: provider.required_provider_mcp || null,
    required_evidence: provider.required_evidence || null,
    missing_evidence_fields: provider.missing_evidence_fields || null,
    source: asOptionalString(provider.source) || null,
    remote_host: asOptionalString(provider.remote_host) || null,
    detected: isRecord(provider.detected) ? {
      source: asOptionalString(asJsonObject(provider.detected).source) || null,
      remote_host: asOptionalString(asJsonObject(provider.detected).remote_host) || null,
      remote_hosts: Array.isArray(asJsonObject(provider.detected).remote_hosts) ? asJsonObject(provider.detected).remote_hosts : [],
      ambiguous: asJsonObject(provider.detected).ambiguous === true
    } : null,
    requires_confirmation: provider.requires_confirmation === true,
    reason: asOptionalString(provider.reason) || null
  };
}
function compactWorkspaceHealth(value) {
  if (!value || !isRecord(value)) return null;
  const health = asJsonObject(value);
  return {
    ok: health.ok !== false,
    response_mode: asOptionalString(health.response_mode) || "compact",
    detail_available: health.detail_available !== false,
    topology: health.topology,
    workspace: health.workspace,
    dependency_graph: health.dependency_graph,
    parallel: health.parallel,
    languages: health.languages,
    lsp: health.lsp,
    integrations: isRecord(health.integrations) ? {
      summary: asJsonObject(health.integrations).summary || null,
      provider: asJsonObject(health.integrations).provider || null,
      optional_mcps: Array.isArray(asJsonObject(health.integrations).optional_mcps) ? asJsonObject(health.integrations).optional_mcps : []
    } : null,
    detail_resources: Array.isArray(health.detail_resources) ? health.detail_resources : []
  };
}
function compactLspSetup(value) {
  if (!value || !isRecord(value)) return null;
  const setup = asJsonObject(value);
  return {
    ok: setup.ok !== false,
    available: setup.available !== false,
    execute: setup.execute === true,
    dry_run: setup.dry_run !== false,
    written: setup.written === true,
    added: asStringArray(setup.added),
    added_count: Array.isArray(setup.added) ? setup.added.length : Number(setup.added_count || 0),
    missing_from_config_count: Array.isArray(setup.missingFromConfig) ? setup.missingFromConfig.length : Number(setup.missing_from_config_count || 0),
    missing_commands_count: Array.isArray(setup.missingCommands) ? setup.missingCommands.length : Number(setup.missing_commands_count || 0),
    recommended_count: Array.isArray(setup.recommended) ? setup.recommended.length : Number(setup.recommended_count || 0)
  };
}
function compactSetupResponse(result) {
  const styleGuides = compactStyleGuides(result.styleGuides);
  const reviewBundle = compactReviewBundle(result.review_bundle);
  const reviewArtifacts = compactReviewArtifacts(result.review_artifacts);
  const reviewBundleSummary = reviewBundle ? {
    directory: reviewBundle.directory,
    manifest_path: reviewBundle.manifest_path,
    content_in_response: false,
    warnings: reviewBundle.warnings,
    file_count: reviewBundle.file_count,
    total_bytes: reviewBundle.total_bytes,
    truncated: reviewBundle.truncated
  } : null;
  const humanReview = isRecord(result.human_review) ? {
    required: asJsonObject(result.human_review).required !== false,
    confirmed: asJsonObject(result.human_review).confirmed === true,
    workflow: asOptionalString(asJsonObject(result.human_review).workflow) || null,
    confirm_argument: asOptionalString(asJsonObject(result.human_review).confirm_argument) || "humanConfirmed",
    artifact_count: reviewArtifacts.count,
    review_bundle_path: reviewBundleSummary?.manifest_path || null
  } : null;
  return {
    ok: result.ok,
    root: result.root,
    workflow: result.workflow,
    packet_only: result.packet_only,
    execute: result.execute,
    dry_run: result.dry_run,
    phase_state: result.phase_state,
    stage: result.stage,
    error: result.error,
    response_mode: result.response_mode,
    detail_available: true,
    identity: result.identity,
    generated_at: result.generated_at,
    provider: compactProvider(result.provider),
    required_payload: Array.isArray(result.required_payload) ? result.required_payload : [],
    missing_payload: Array.isArray(result.missing_payload) ? result.missing_payload : void 0,
    sync_mode: result.sync_mode,
    sync_recommendation: result.sync_recommendation,
    topology: result.topology,
    workspace_health: compactWorkspaceHealth(result.workspace_health),
    workspace: result.workspace,
    dependency_graph: result.dependency_graph,
    lsp: result.lsp,
    lsp_setup: compactLspSetup(result.lsp_setup),
    integrations: isRecord(result.integrations) ? {
      summary: asJsonObject(result.integrations).summary || null,
      provider: asJsonObject(result.integrations).provider || null,
      optional_mcps: Array.isArray(asJsonObject(result.integrations).optional_mcps) ? asJsonObject(result.integrations).optional_mcps : []
    } : null,
    styleGuides,
    styleguide_ids: asStringArray(styleGuides.selected),
    templates: compactTemplateManifest(result.templates),
    techStackSummary: isRecord(result.techStackSummary) ? {
      ok: asJsonObject(result.techStackSummary).ok !== false,
      path: asJsonObject(result.techStackSummary).path,
      styleGuideIds: asJsonObject(result.techStackSummary).styleGuideIds,
      summary: asJsonObject(result.techStackSummary).summary
    } : result.techStackSummary,
    human_review: humanReview,
    review_artifacts: reviewArtifacts.files,
    review_bundle: reviewBundleSummary,
    review_bundle_path: reviewBundleSummary?.manifest_path || null,
    warnings: Array.isArray(result.warnings) ? result.warnings : [],
    next_actions: Array.isArray(result.next_actions) ? result.next_actions : [],
    packet_notes: Array.isArray(result.packet_notes) ? result.packet_notes : [],
    detail_resources: Array.isArray(result.detail_resources) ? result.detail_resources : [],
    resource_uris: Array.isArray(result.resource_uris) ? result.resource_uris : [],
    scaffolded: result.scaffolded,
    written: Array.isArray(result.written) ? result.written : void 0,
    skipped: Array.isArray(result.skipped) ? result.skipped : void 0,
    gitattributes: result.gitattributes,
    ci_setup: result.ci_setup,
    polyrepo_setup: result.polyrepo_setup,
    force: result.force,
    doctor_summary: isRecord(result.doctor) ? {
      ok: asJsonObject(result.doctor).ok !== false,
      warnings: Array.isArray(asJsonObject(result.doctor).warnings) ? asJsonObject(result.doctor).warnings : []
    } : null,
    doctor_after_summary: isRecord(result.doctor_after) ? {
      ok: asJsonObject(result.doctor_after).ok !== false,
      warnings: Array.isArray(asJsonObject(result.doctor_after).warnings) ? asJsonObject(result.doctor_after).warnings : []
    } : null
  };
}
function planSummary(value) {
  if (!value || !isRecord(value)) return null;
  const plan = asJsonObject(value);
  return {
    ok: plan.ok !== false,
    phases: Array.isArray(plan.phases) ? plan.phases.length : Number(plan.phases || 0),
    tasks: Array.isArray(plan.tasks) ? plan.tasks.length : Number(plan.tasks || 0),
    warnings: Array.isArray(plan.warnings) ? plan.warnings.length : Number(plan.warnings || 0),
    errors: Array.isArray(plan.errors) ? plan.errors.length : Number(plan.errors || 0)
  };
}
function compactTrackContext(value) {
  if (!value || !isRecord(value)) return null;
  const context = asJsonObject(value);
  const track = asJsonObject(context.track);
  return {
    ok: context.ok !== false,
    track: {
      track_id: asOptionalString(track.track_id) || asOptionalString(context.track_id) || null,
      status: asOptionalString(track.status) || null,
      owner: asOptionalString(track.owner) || null,
      reviewer: asOptionalString(track.reviewer) || null,
      git_branch: asOptionalString(track.git_branch) || null
    },
    task_counts: context.task_counts || null,
    topology: context.topology || null,
    plan: planSummary(context.plan),
    resource_uri: context.track_id ? `cadre://track-context?trackId=${encodeURIComponent(String(context.track_id))}` : null
  };
}
function compactReviewAssist(value) {
  if (!value || !isRecord(value)) return null;
  const assist = asJsonObject(value);
  const repoDiffs = Array.isArray(assist.repo_diffs) ? assist.repo_diffs.map(asJsonObject) : [];
  const todos = Array.isArray(assist.todos) ? assist.todos.map(asJsonObject) : [];
  const incomplete = Array.isArray(assist.incomplete_tasks) ? assist.incomplete_tasks.map(asJsonObject) : [];
  const lsp = asJsonObject(assist.lsp);
  const machine = asJsonObject(assist.machine_gate);
  const diff = asJsonObject(assist.diff);
  const diffFiles = Array.isArray(diff.files) ? diff.files : [];
  return {
    ok: assist.ok !== false,
    root: assist.root,
    track_id: assist.track_id,
    base: assist.base,
    head: assist.head,
    diff: isRecord(assist.diff) ? {
      repo: diff.repo || ".",
      file_count: diffFiles.length,
      stat: asOptionalString(diff.stat) || ""
    } : null,
    repo_diffs: repoDiffs.slice(0, 20).map((entry) => ({
      repo: entry.repo || ".",
      path: entry.path || ".",
      files: Array.isArray(entry.files) ? entry.files.slice(0, 50) : [],
      file_count: Array.isArray(entry.files) ? entry.files.length : 0,
      stat: asOptionalString(entry.stat) || "",
      ok: entry.ok !== false
    })),
    repo_diffs_count: repoDiffs.length,
    task_counts: assist.task_counts,
    incomplete_tasks: incomplete.slice(0, 20),
    incomplete_tasks_count: incomplete.length,
    coverage: assist.coverage,
    todos: todos.slice(0, 20),
    todos_count: todos.length,
    lsp: assist.lsp ? {
      available: lsp.available !== false,
      degraded: lsp.degraded === true || lsp.fallback === "text_scan",
      findings_count: Array.isArray(lsp.findings) ? lsp.findings.length : 0,
      server_count: Array.isArray(lsp.servers) ? lsp.servers.length : 0,
      polyrepo: lsp.polyrepo === true
    } : null,
    machine_gate: assist.machine_gate ? {
      ok: machine.ok !== false,
      available: machine.available !== false,
      blocking_count: Number(machine.blocking_count || 0),
      result_count: Array.isArray(machine.results) ? machine.results.length : 0
    } : null,
    suggested_verdict: assist.suggested_verdict,
    blocking_reasons: Array.isArray(assist.blocking_reasons) ? assist.blocking_reasons : [],
    detail_resources: assist.track_id ? [
      `cadre://quality-gate?trackId=${encodeURIComponent(String(assist.track_id))}`,
      `cadre://review-evidence?trackId=${encodeURIComponent(String(assist.track_id))}`
    ] : []
  };
}
function compactReviewResponse(result) {
  return {
    ok: result.ok,
    root: result.root,
    workflow: result.workflow,
    packet_only: result.packet_only,
    execute: result.execute,
    phase_state: result.phase_state,
    response_mode: result.response_mode,
    detail_available: true,
    identity: result.identity,
    generated_at: result.generated_at,
    track_context: compactTrackContext(result.track_context),
    review_assist: compactReviewAssist(result.review_assist),
    gate: result.gate,
    provider: compactProvider(result.provider),
    required_provider_mcp: result.required_provider_mcp,
    required_evidence: result.required_evidence,
    unsupported_reason: result.unsupported_reason,
    next_actions: Array.isArray(result.next_actions) ? result.next_actions : [],
    resource_uris: Array.isArray(result.resource_uris) ? result.resource_uris : []
  };
}
function compactWorkflowResponse(workflow, result) {
  if (workflow === "setup" || workflow === "setup_assist" || workflow === "setup_scaffold") {
    return compactSetupResponse(result);
  }
  if (workflow === "review") return compactReviewResponse(result);
  return null;
}

// src/core/application/runtime/workflow-response.ts
function workflowResponseMode(args = {}) {
  const raw = asOptionalString(args.responseMode || args.response_mode || (args.detail === true ? "detail" : null) || (args.compact === true ? "compact" : null))?.trim().toLowerCase();
  if (raw && ["detail", "detailed", "full", "verbose"].includes(raw)) return "detail";
  return "compact";
}
function workflowSummary(root, workflow, args = {}) {
  const identity = gitIdentity(root);
  return {
    root,
    workflow,
    packet_only: true,
    execute: args.execute === true,
    phase_state: args.execute === true ? "executed" : "dry_run",
    response_mode: workflowResponseMode(args),
    detail_available: true,
    identity,
    generated_at: utcNow()
  };
}
function resultOk(value) {
  return !value || value.ok !== false;
}
function withSharedControlPlaneSync(root, args = {}, operation, fn) {
  const topology = loadTopology(root);
  if (args.execute !== true || topology.config.sync_mode !== "shared" || args.skipSync === true) {
    return fn();
  }
  const syncPre = syncControlPlane(root, { mode: "pre" });
  if (syncPre.ok === false) {
    return {
      ok: false,
      phase_state: "blocked",
      stage: "sync_pre",
      operation,
      sync_pre: syncPre
    };
  }
  const result = fn();
  if (result.ok === false) {
    return {
      ...result,
      sync_pre: syncPre,
      sync_post: null
    };
  }
  const syncPost = syncControlPlane(root, { mode: "post" });
  return {
    ...result,
    ok: resultOk(result) && syncPost.ok !== false,
    phase_state: syncPost.ok === false ? "recovery_required" : result.phase_state,
    sync_pre: syncPre,
    sync_post: syncPost
  };
}
function compactObject(value, depth = 0) {
  if (value == null || typeof value !== "object") return value;
  if (Array.isArray(value)) {
    const limit = depth === 0 ? 80 : 25;
    return value.slice(0, limit).map((item) => compactObject(item, depth + 1));
  }
  const source = asJsonObject(value);
  const out = {};
  for (const [key, entry] of Object.entries(source)) {
    if (key === "content" && typeof entry === "string" && entry.length > 800) {
      out[key] = `${entry.slice(0, 800)}
...[truncated; request responseMode:"detail" for full content]`;
      continue;
    }
    if (key === "plan" && isRecord(entry)) {
      const plan = asJsonObject(entry);
      out[key] = {
        ok: plan.ok,
        phases: Array.isArray(plan.phases) ? plan.phases.length : 0,
        tasks: Array.isArray(plan.tasks) ? plan.tasks.length : 0,
        warnings: Array.isArray(plan.warnings) ? plan.warnings.length : 0,
        errors: Array.isArray(plan.errors) ? plan.errors.length : 0
      };
      continue;
    }
    if (["stdout", "stderr"].includes(key) && typeof entry === "string" && entry.length > 1200) {
      out[`${key}_tail`] = entry.slice(-1200);
      out[`${key}_truncated`] = true;
      continue;
    }
    if (["repo_diffs", "repo_todos", "commands", "results"].includes(key) && Array.isArray(entry)) {
      out[key] = entry.slice(0, 20).map((item) => compactObject(item, depth + 1));
      out[`${key}_count`] = entry.length;
      out[`${key}_truncated`] = entry.length > 20;
      continue;
    }
    out[key] = depth > 5 ? "[depth-limit]" : compactObject(entry, depth + 1);
  }
  return out;
}
function workflowResourceUris(root, workflow, result) {
  const encodedRoot = encodeURIComponent(root);
  const trackId = asOptionalString(result.track_id) || asOptionalString(asJsonObject(result.track || {}).track_id) || asOptionalString(asJsonObject(asJsonObject(result.track_context).track).track_id);
  const uris = [
    `cadre://workspace-health?root=${encodedRoot}`,
    `cadre://mcp-readiness?root=${encodedRoot}`,
    `cadre://team-board?root=${encodedRoot}`,
    `cadre://quality-gate?root=${encodedRoot}${trackId ? `&trackId=${encodeURIComponent(trackId)}` : ""}`
  ];
  if (trackId) {
    uris.push(`cadre://track-context?root=${encodedRoot}&trackId=${encodeURIComponent(trackId)}`);
    uris.push(`cadre://parallel-state?root=${encodedRoot}&trackId=${encodeURIComponent(trackId)}`);
  }
  if (workflow === "ship" && trackId) uris.push(`cadre://ship-plan?root=${encodedRoot}&trackId=${encodeURIComponent(trackId)}`);
  if (workflow === "land" && trackId) uris.push(`cadre://land-plan?root=${encodedRoot}&trackId=${encodeURIComponent(trackId)}`);
  if (workflow === "release") uris.push(`cadre://release-plan?root=${encodedRoot}`);
  if (workflow === "artifacts" || workflow === "artifact_sync") {
    const scope = asOptionalString(result.scope || result.artifact_scope) || "all";
    uris.push(`cadre://artifact-catalog?root=${encodedRoot}`);
    uris.push(`cadre://artifact-sync-plan?root=${encodedRoot}&scope=${encodeURIComponent(scope)}`);
    if (scope.startsWith("track:")) {
      const scopedTrackId = scope.slice("track:".length);
      uris.push(`cadre://track-spec?root=${encodedRoot}&trackId=${encodeURIComponent(scopedTrackId)}`);
      uris.push(`cadre://track-plan?root=${encodedRoot}&trackId=${encodeURIComponent(scopedTrackId)}`);
    }
  }
  return Array.from(new Set(uris));
}
function shapeWorkflowResponse(root, workflow, args, result) {
  const mode2 = workflowResponseMode(args);
  const enriched = {
    ...result,
    response_mode: mode2,
    detail_available: true,
    resource_uris: workflowResourceUris(root, workflow, result)
  };
  if (mode2 === "detail") return enriched;
  const workflowSpecific = compactWorkflowResponse(workflow, enriched);
  if (workflowSpecific) return workflowSpecific;
  return compactObject(enriched);
}
function templateText(relativePath, fallback) {
  return packagedTemplateText(relativePath) ?? fallback;
}
function templateSourceLabel(relativePath) {
  return packagedTemplateSource(relativePath);
}
function templateRelativePaths(prefix = "") {
  return packagedTemplatePaths(prefix);
}
var MARKDOWN_PAYLOAD_FIELDS = [
  "productText",
  "productGuidelinesText",
  "product_guidelines_text",
  "workflowText",
  "specText",
  "planText",
  "planPath"
];
function markdownPayloadError(args = {}) {
  const raw = args;
  const provided = MARKDOWN_PAYLOAD_FIELDS.filter((field) => raw[field] !== void 0);
  if (provided.length === 0) return null;
  return {
    ok: false,
    error: `Markdown payload fields are not supported: ${provided.join(", ")}. Use structured JSON fields instead.`,
    unsupported_fields: provided,
    expected_fields: ["product", "productGuidelines", "workflowPolicy", "spec", "plan"]
  };
}
function normalizeProjectDoc(kind, raw, templateFile, fallbackTitle, notesHeading) {
  const template = templateJson(templateFile, {
    version: 1,
    schema: `cadre.${kind}.v1`,
    kind,
    title: fallbackTitle,
    summary: "",
    sections: []
  });
  const provided = isRecord(raw) ? asJsonObject(raw) : {};
  const templateSections = asArray(template.sections).map(asJsonObject);
  const providedSections = asArray(provided.sections).map(asJsonObject);
  const providedSummary = asOptionalString(provided.summary || provided.description || provided.notes);
  const sections = providedSections.length > 0 ? providedSections : [
    ...templateSections,
    ...providedSummary && isRecord(raw) ? [{ heading: notesHeading, body: providedSummary }] : []
  ];
  return {
    ...template,
    ...provided,
    version: 1,
    schema: `cadre.${kind}.v1`,
    kind,
    title: asOptionalString(provided.title) || asOptionalString(template.title) || fallbackTitle,
    summary: asOptionalString(provided.summary || provided.description) || asOptionalString(template.summary) || "",
    sections,
    updated_at: utcNow()
  };
}
function normalizeSpecJson(trackId, raw) {
  const spec = normalizedSpecFromRaw({
    version: 1,
    schema: "cadre.spec.v1",
    kind: "spec",
    track_id: trackId,
    title: `Spec: ${trackId}`,
    ...asJsonObject(raw)
  });
  return {
    ...spec,
    version: 1,
    schema: "cadre.spec.v1",
    kind: "spec",
    track_id: trackId,
    updated_at: utcNow()
  };
}
function normalizePlanJson(trackId, raw, specJson) {
  const plan = {
    version: 1,
    schema: "cadre.plan.v1",
    track_id: trackId,
    title: `Plan: ${trackId}`,
    ...asJsonObject(raw)
  };
  return normalizePlanManualVerification({ ...plan, track_id: trackId }, specJson);
}
function templateJson(relativePath, fallback) {
  return packagedTemplateJson(relativePath) || fallback;
}
function templateManifest() {
  return templateJson("manifest.json", { templates: [] });
}

// src/core/application/runtime/workspace-intel.ts
var import_node_path18 = __toESM(require("node:path"));
function lspImpact(root, args = {}) {
  const limit = Number(args.limit || 50);
  const symbols = Array.isArray(args.symbols) ? args.symbols : args.symbol ? [args.symbol] : [];
  const files = Array.isArray(args.files) ? args.files : [];
  const symbolResults = {};
  for (const symbol of symbols.filter(Boolean)) {
    symbolResults[symbol] = repoMap(root, { symbol, limit });
  }
  const repoEntries = intelRepoRoots(root, args);
  const repoFileSymbols = repoEntries.map((entry) => {
    const fileSymbols2 = {};
    for (const file of files) {
      if (isIgnoredRepoMapFile(file)) continue;
      fileSymbols2[file] = extractRepoSymbols(entry.root, file, limit).map((symbolEntry) => ({
        ...symbolEntry,
        repo: entry.repo
      }));
    }
    return { repo: entry.repo, root: entry.root, path: entry.path, files: fileSymbols2 };
  });
  const fileSymbols = {};
  for (const entry of repoFileSymbols) {
    for (const [file, symbolsForFile] of Object.entries(asJsonObject(entry.files))) {
      const key = entry.repo === "." ? file : `${entry.repo}:${file}`;
      fileSymbols[key] = asArray(symbolsForFile);
    }
  }
  const review = args.lspResult || args.lsp_result ? args.lspResult || args.lsp_result : args.base || args.head ? lspReview(root, { base: args.base || "main", head: args.head || "HEAD", config: args.config }) : null;
  return {
    ok: true,
    root,
    symbols: symbolResults,
    files: fileSymbols,
    repos: repoFileSymbols,
    review
  };
}
function shellCommandPlan(command, cwd, adapter) {
  return { adapter, command, cwd };
}
function detectWorkspaceAdapters(root) {
  const adapters = [];
  const pkg = loadPackageJson(root);
  if (pkg) {
    const scripts = asJsonObject(pkg.scripts);
    const runner = fileExists(import_node_path18.default.join(root, "pnpm-lock.yaml")) ? "pnpm" : fileExists(import_node_path18.default.join(root, "yarn.lock")) ? "yarn" : "npm run";
    const scriptCommands = ["typecheck", "check", "test", "build", "lint"].filter((script) => scripts[script]).map((script) => runner === "npm run" ? `npm run ${script}` : `${runner} ${script}`);
    adapters.push({
      id: "node",
      ecosystem: "javascript",
      manifest: "package.json",
      available: commandExists(runner.split(" ")[0] || "npm", root),
      commands: scriptCommands
    });
    if (fileExists(import_node_path18.default.join(root, "nx.json")) || asJsonObject(pkg.devDependencies).nx || asJsonObject(pkg.dependencies).nx) {
      adapters.push({
        id: "nx",
        ecosystem: "javascript",
        manifest: fileExists(import_node_path18.default.join(root, "nx.json")) ? "nx.json" : "package.json",
        available: commandExists("nx", root) || commandExists("pnpm", root) || commandExists("npx", root),
        commands: ["nx affected -t test", "nx affected -t build"]
      });
    }
  }
  if (["pyproject.toml", "pytest.ini", "setup.cfg"].some((file) => fileExists(import_node_path18.default.join(root, file)))) {
    adapters.push({ id: "pytest", ecosystem: "python", manifest: "pyproject.toml", available: commandExists("pytest", root), commands: ["pytest"] });
  }
  if (fileExists(import_node_path18.default.join(root, "go.mod"))) {
    adapters.push({ id: "go", ecosystem: "go", manifest: "go.mod", available: commandExists("go", root), commands: ["go test ./..."] });
  }
  if (fileExists(import_node_path18.default.join(root, "Cargo.toml"))) {
    adapters.push({ id: "cargo", ecosystem: "rust", manifest: "Cargo.toml", available: commandExists("cargo", root), commands: ["cargo test"] });
  }
  if (fileExists(import_node_path18.default.join(root, "pom.xml"))) {
    adapters.push({ id: "maven", ecosystem: "java", manifest: "pom.xml", available: commandExists("mvn", root), commands: ["mvn test"] });
  }
  const gradleManifest = ["build.gradle", "build.gradle.kts"].find((file) => fileExists(import_node_path18.default.join(root, file)));
  if (gradleManifest) {
    const gradlew = fileExists(import_node_path18.default.join(root, "gradlew")) ? "./gradlew" : "gradle";
    adapters.push({ id: "gradle", ecosystem: "jvm", manifest: gradleManifest, available: gradlew === "./gradlew" || commandExists("gradle", root), commands: [`${gradlew} test`] });
  }
  if (["MODULE.bazel", "WORKSPACE", "WORKSPACE.bazel"].some((file) => fileExists(import_node_path18.default.join(root, file)))) {
    adapters.push({ id: "bazel", ecosystem: "polyglot", manifest: "MODULE.bazel", available: commandExists("bazel", root), commands: ["bazel test //..."] });
  }
  return adapters;
}
function workspaceDiagnostics(root, args = {}) {
  if (args.execute !== true && args.cacheBypass !== true) {
    return cachedWorkspaceValue(root, "workspace-diagnostics", JSON.stringify({
      repos: asStringArray(args.repos).concat(asOptionalString(args.repo) || []).filter(Boolean)
    }), () => workspaceDiagnostics(root, { ...args, includeHeavy: false, cacheBypass: true }));
  }
  const repoEntries = intelRepoRoots(root, args);
  const repoDiagnostics = repoEntries.map((entry) => {
    const adapters2 = detectWorkspaceAdapters(entry.root).map((rawAdapter) => {
      const adapter = asJsonObject(rawAdapter);
      return {
        ...adapter,
        repo: entry.repo,
        cwd: entry.root,
        path: entry.path
      };
    });
    const commands2 = adapters2.flatMap(
      (adapter) => asStringArray(adapter.commands).map((command) => ({
        ...shellCommandPlan(command, entry.root, asString(adapter.id)),
        repo: entry.repo,
        path: entry.path
      }))
    );
    return { repo: entry.repo, root: entry.root, path: entry.path, adapters: adapters2, commands: commands2 };
  });
  const adapters = repoDiagnostics.flatMap((entry) => asArray(entry.adapters));
  const commands = repoDiagnostics.flatMap((entry) => asArray(entry.commands));
  const execute = args.execute === true;
  return {
    ok: true,
    root,
    execute,
    dry_run: !execute,
    adapters,
    commands,
    repos: repoDiagnostics,
    results: execute ? commands.map((entry) => runCommand(asString(entry.command), [], {
      cwd: asString(entry.cwd, root),
      shell: true,
      timeoutMs: Number(args.timeoutMs || 10 * 60 * 1e3),
      maxBuffer: 30 * 1024 * 1024
    })) : []
  };
}
function impactedFiles(root, args = {}) {
  if (Array.isArray(args.files) && args.files.length > 0) return args.files.map(normalizeClaimPath).filter(Boolean);
  if (args.base || args.head) {
    return diffSurface(root, args.base || "main", args.head || "HEAD").files;
  }
  return [];
}
function testImpact(root, args = {}) {
  const repoEntries = intelRepoRoots(root, args);
  const repoImpacts = repoEntries.map((entry) => {
    const files2 = impactedFiles(entry.root, args);
    const likelyTests = Object.fromEntries(files2.map((file) => [file, likelyTestCandidatesForFile(entry.root, file)]));
    const manifests = /* @__PURE__ */ new Set();
    for (const file of files2) {
      let dir = import_node_path18.default.dirname(import_node_path18.default.join(entry.root, file));
      while (dir.startsWith(entry.root)) {
        for (const manifest of ["package.json", "pyproject.toml", "go.mod", "Cargo.toml", "pom.xml", "build.gradle", "build.gradle.kts", "MODULE.bazel", "nx.json"]) {
          const candidate = import_node_path18.default.join(dir, manifest);
          if (fileExists(candidate)) manifests.add(normalizeClaimPath(import_node_path18.default.relative(entry.root, candidate)));
        }
        if (dir === entry.root) break;
        dir = import_node_path18.default.dirname(dir);
      }
    }
    return {
      repo: entry.repo,
      root: entry.root,
      path: entry.path,
      files: files2,
      likely_tests: likelyTests,
      manifests: Array.from(manifests).sort(),
      adapters: detectWorkspaceAdapters(entry.root)
    };
  });
  const primary = repoImpacts[0] || { files: [], likely_tests: {}, manifests: [], adapters: [] };
  const files = asStringArray(primary.files);
  return {
    ok: true,
    root,
    files,
    likely_tests: asJsonObject(primary.likely_tests),
    manifests: asStringArray(primary.manifests),
    adapters: asArray(primary.adapters),
    repos: repoImpacts
  };
}
function dependencyGraph(root, args = {}) {
  if (args.cacheBypass !== true) {
    return cachedWorkspaceValue(root, "dependency-graph", JSON.stringify({
      repos: asStringArray(args.repos).concat(asOptionalString(args.repo) || []).filter(Boolean)
    }), () => dependencyGraph(root, { ...args, cacheBypass: true }));
  }
  const repoEntries = intelRepoRoots(root, args);
  const manifestPatterns = /* @__PURE__ */ new Set([
    "package.json",
    "pyproject.toml",
    "go.mod",
    "Cargo.toml",
    "pom.xml",
    "build.gradle",
    "build.gradle.kts",
    "MODULE.bazel",
    "WORKSPACE",
    "WORKSPACE.bazel",
    "nx.json"
  ]);
  const repoGraphs = repoEntries.map((entry) => {
    const files = listWorkspaceFiles(entry.root).filter((file) => !isIgnoredRepoMapFile(file));
    const manifests2 = files.filter((file) => manifestPatterns.has(import_node_path18.default.basename(file))).map((file) => ({ repo: entry.repo, file, dir: normalizeClaimPath(import_node_path18.default.dirname(file)), kind: import_node_path18.default.basename(file) }));
    return {
      repo: entry.repo,
      root: entry.root,
      path: entry.path,
      manifests: manifests2,
      adapters: detectWorkspaceAdapters(entry.root),
      edges: manifests2.map((manifest) => ({
        repo: entry.repo,
        from: manifest.file,
        to: manifest.dir || ".",
        kind: "workspace_manifest"
      }))
    };
  });
  const manifests = repoGraphs.flatMap((entry) => asArray(entry.manifests));
  const edges = repoGraphs.flatMap((entry) => asArray(entry.edges));
  return {
    ok: true,
    root,
    manifests,
    adapters: repoGraphs.flatMap((entry) => asArray(entry.adapters)),
    edges,
    repos: repoGraphs
  };
}

// src/core/application/runtime/planning.ts
function likelyTestCandidatesForFile(root, file) {
  const normalized = normalizeClaimPath(file);
  if (!normalized) return [];
  const parsed = import_node_path19.default.parse(normalized);
  const candidates = [
    import_node_path19.default.join(parsed.dir, `${parsed.name}.test${parsed.ext}`),
    import_node_path19.default.join(parsed.dir, `${parsed.name}.spec${parsed.ext}`),
    import_node_path19.default.join(parsed.dir, `${parsed.name}_test${parsed.ext}`),
    import_node_path19.default.join("test", normalized),
    import_node_path19.default.join("tests", normalized)
  ].map((candidate) => normalizeClaimPath(candidate));
  return Array.from(new Set(candidates.filter((candidate) => fileExists(import_node_path19.default.join(root, candidate)))));
}
function taskSearchTokens(task) {
  return Array.from(new Set(String(task.title || "").toLowerCase().split(/[^a-z0-9]+/).filter((token) => token.length >= 4).slice(0, 8)));
}
function suggestedRepoForFile(topology, file) {
  if (!topology.polyrepo) return ".";
  const repos = Array.isArray(topology.repos.repos) ? topology.repos.repos : [];
  const match = repos.find((repo) => {
    const name = asOptionalString(repo.name);
    const submodule = asOptionalString(repo.submodule_path);
    return Boolean(submodule && file.startsWith(`${normalizeClaimPath(submodule)}/`) || name && file.startsWith(`${name}/`));
  });
  return asOptionalString(match?.name) || asOptionalString(topology.repos.default_repo) || null;
}
function missingClaimSuggestions(root, topology, tasks, limit) {
  const workspaceFiles = listWorkspaceFiles(root).filter((file) => languageForFile(file) !== "markdown").slice(0, 5e3);
  return tasks.filter((task) => asStringArray(task.files).length === 0 || topology.polyrepo && !task.repo && !topology.repos.default_repo).map((task) => {
    const tokens = taskSearchTokens(task);
    const proposedFiles = workspaceFiles.filter((file) => {
      const lower = file.toLowerCase();
      return tokens.some((token) => lower.includes(token));
    }).slice(0, limit);
    return {
      phase_index: task.phase_index,
      task_index: task.task_index,
      task_key: task.task_key,
      title: task.title,
      missing_files: asStringArray(task.files).length === 0,
      missing_repo: topology.polyrepo && !task.repo && !topology.repos.default_repo,
      proposed_repo: task.repo || (proposedFiles[0] ? suggestedRepoForFile(topology, proposedFiles[0]) : asOptionalString(topology.repos.default_repo) || null),
      proposed_files: proposedFiles,
      likely_tests: proposedFiles.flatMap((file) => likelyTestCandidatesForFile(root, file)).slice(0, limit),
      evidence: proposedFiles.length > 0 ? "workspace_file_name_match" : "no_workspace_filename_match"
    };
  });
}
function planAssist(root, args = {}) {
  const markdownError = markdownPayloadError(args);
  if (markdownError) return markdownError;
  const trackId = args.trackId || args.track_id || null;
  const track = trackId ? findTrack(root, trackId) : null;
  if (trackId && !track && !args.plan) return { ok: false, error: `Track not found: ${trackId}` };
  const topology = loadTopology(root);
  const plan = args.plan ? parsePlanJson(normalizePlanJson(String(trackId || asOptionalString(args.plan.track_id) || "draft"), args.plan)) : track ? parsePlanFile(track.plan_path) : null;
  if (!plan) return { ok: false, error: "trackId or plan is required" };
  const repoErrors = track ? unresolvedPlanRepos(root, track, args) : [];
  const claims = (plan.tasks || []).map((task) => {
    const repo = topology.polyrepo ? task.repo || topology.defaultRepo : ".";
    return {
      phase_index: task.phase_index,
      task_index: task.task_index,
      task_key: task.task_key,
      title: task.title,
      repo,
      files: task.files || [],
      depends: task.depends || [],
      likely_tests: (task.files || []).flatMap((file) => likelyTestCandidatesForFile(root, file))
    };
  });
  const fileClaims = {};
  for (const claim of claims) {
    const repo = asString(claim.repo, ".");
    if (!fileClaims[repo]) fileClaims[repo] = [];
    fileClaims[repo].push(...asStringArray(claim.files));
  }
  const rawFileClaims = Object.fromEntries(Object.entries(fileClaims).map(([repo, files2]) => [repo, [...files2]]));
  for (const repo of Object.keys(fileClaims)) {
    const files2 = fileClaims[repo] || [];
    fileClaims[repo] = Array.from(new Set(files2.map(normalizeClaimPath).filter(Boolean))).sort();
  }
  const duplicateClaims = Object.entries(rawFileClaims).flatMap(([repo, files2]) => {
    const counts = /* @__PURE__ */ new Map();
    for (const file of files2) counts.set(file, (counts.get(file) || 0) + 1);
    return Array.from(counts.entries()).filter(([, count]) => count > 1).map(([file, count]) => ({ repo, file, count }));
  });
  const phases = (plan.phases || []).map((phase) => {
    const phaseClaims = claims.filter((claim) => claim.phase_index === phase.phase_index);
    const phaseFiles = phaseClaims.flatMap((claim) => asStringArray(claim.files).map((file) => `${claim.repo}:${normalizeClaimPath(file)}`));
    return {
      phase_index: phase.phase_index,
      title: phase.title,
      execution: phase.annotations.execution || "sequential",
      tasks: phase.tasks.length,
      parallel_candidate: phaseClaims.length > 1 && new Set(phaseFiles).size === phaseFiles.length
    };
  });
  const files = Array.from(new Set(Object.values(fileClaims).flat())).slice(0, Number(args.limit || 50));
  const semanticImpact = files.length > 0 ? lspImpact(root, { files, limit: args.limit || 50 }) : null;
  const schedule = track ? phaseSchedule(root, { ...args, trackId: track.track_id }) : null;
  const limit = Number(args.limit || 50);
  return {
    ok: repoErrors.length === 0 && plan.ok !== false,
    root,
    track_id: track?.track_id || trackId,
    topology: {
      polyrepo: topology.polyrepo,
      default_repo: topology.defaultRepo
    },
    claims,
    file_claims: fileClaims,
    duplicate_claims: duplicateClaims,
    likely_tests: Array.from(new Set(claims.flatMap((claim) => asStringArray(claim.likely_tests)))).sort(),
    phases,
    schedule,
    semantic_impact: semanticImpact,
    missing_claim_suggestions: missingClaimSuggestions(root, topology, plan.tasks || [], Math.min(limit, 25)),
    errors: [...plan.errors || [], ...repoErrors],
    warnings: plan.warnings || []
  };
}
function worktreePlan(root, args = {}) {
  const track = findTrack(root, args.trackId || args.track_id);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId || args.track_id}` };
  const repoError = repoEntriesError(root, track, args);
  if (repoError) return repoError;
  const topology = loadTopology(root);
  const branch = args.branch || track.metadata.git_branch || `track/${track.track_id}`;
  const entries = topology.polyrepo ? repoEntriesForTrack(root, track, args) : [{
    repo: ".",
    root,
    path: ".",
    source: "project-root",
    base: args.base || "main",
    head: branch
  }];
  const plans = entries.map((entry) => {
    const repo = asString(entry.repo, ".");
    const repoBranch = args.branch || entry.head || branch;
    const base = args.base || entry.base || "main";
    const relWorktree = topology.polyrepo ? `.worktrees/${track.track_id}/${safeName(repo)}` : asOptionalString(track.metadata.worktree_path) || `.worktrees/${track.track_id}`;
    const absWorktree = import_node_path19.default.resolve(root, relWorktree);
    return {
      repo,
      source_root: entry.root,
      source_path: entry.path,
      worktree_path: relWorktree,
      branch: repoBranch,
      base,
      exists: fileExists(absWorktree),
      commands: [
        {
          command: "git",
          args: ["worktree", "add", "-B", repoBranch, absWorktree, base],
          cwd: entry.root
        }
      ]
    };
  });
  return {
    ok: true,
    root,
    track_id: track.track_id,
    execute: false,
    topology: topology.polyrepo ? "polyrepo" : "monorepo",
    plans
  };
}
function planIntegrity(root, trackId = null) {
  const topology = loadTopology(root);
  const foundTrack = trackId ? findTrack(root, trackId) : null;
  const tracks = trackId ? foundTrack ? [foundTrack] : [] : listTracks(root);
  if (trackId && tracks.length === 0) return { ok: false, error: `Track not found: ${trackId}` };
  const errors = [];
  const warnings = [];
  for (const track of tracks) {
    const plan = parsePlanFile(track.plan_path);
    const seenKeys = /* @__PURE__ */ new Set();
    for (const phase of plan.phases) {
      const execution = phase.annotations.execution || "sequential";
      const claimedFiles = /* @__PURE__ */ new Set();
      for (const task of phase.tasks) {
        if (seenKeys.has(task.task_key)) {
          errors.push({ track_id: track.track_id, line: task.line, message: `Duplicate task key ${task.task_key}` });
        }
        seenKeys.add(task.task_key);
        if (!task.files || task.files.length === 0) {
          warnings.push({ track_id: track.track_id, line: task.line, task_key: task.task_key, message: "Missing <!-- files: ... --> annotation" });
        }
        if (topology.polyrepo && !task.repo && !topology.defaultRepo) {
          errors.push({ track_id: track.track_id, line: task.line, task_key: task.task_key, message: "Task has no repo annotation and repos.json has no default_repo" });
        }
        for (const dep of task.depends || []) {
          if (!/^task\d+$|^phase\d+_task\d+$/.test(dep)) {
            warnings.push({ track_id: track.track_id, line: task.line, task_key: task.task_key, message: `Unrecognized dependency reference ${dep}` });
          }
        }
        if (execution === "parallel") {
          for (const file of task.files || []) {
            const normalized = `${task.repo || topology.defaultRepo || "."}:${normalizeClaimPath(file)}`;
            if (claimedFiles.has(normalized)) {
              warnings.push({ track_id: track.track_id, line: task.line, task_key: task.task_key, message: `Parallel phase repeats file claim ${normalized}` });
            }
            claimedFiles.add(normalized);
          }
        }
      }
    }
    errors.push(...unresolvedPlanRepos(root, track));
  }
  return { ok: errors.length === 0, root, checked_tracks: tracks.length, errors, warnings };
}

// src/core/application/runtime/review-bundles.ts
var import_node_fs12 = __toESM(require("node:fs"));
var import_node_path21 = __toESM(require("node:path"));
var import_node_crypto2 = __toESM(require("node:crypto"));
var import_node_os2 = __toESM(require("node:os"));

// src/core/application/runtime/tech-stack.ts
var import_node_path20 = __toESM(require("node:path"));
function availableStyleGuideIds() {
  return templateRelativePaths("styleguides").filter((file) => file.endsWith(".json")).map((file) => import_node_path20.default.basename(file, ".json")).sort();
}
function normalizeStyleGuideId(value) {
  return value.trim().replace(/\\/g, "/").replace(/^.*code_styleguides\//, "").replace(/^.*styleguides\//, "").replace(/\.(md|json)$/i, "").toLowerCase();
}
function requestedStyleGuideIds(value) {
  const raw = typeof value === "string" ? value.split(/[,\s]+/) : asStringArray(value);
  return Array.from(new Set(raw.map(normalizeStyleGuideId).filter(Boolean))).sort();
}
function collectTechStackTokens(value) {
  if (typeof value === "string") return [value];
  if (Array.isArray(value)) return value.flatMap(collectTechStackTokens);
  if (!isRecord(value)) return [];
  return Object.entries(value).flatMap(([key, entry]) => [key, ...collectTechStackTokens(entry)]);
}
function techStackStyleGuideOverrides(techStack) {
  return requestedStyleGuideIds(
    techStack.styleGuideIds || techStack.style_guides || techStack.codeStyleGuides || techStack.code_style_guides
  );
}
function styleGuideIdsForTechStack(techStack) {
  const tokens = collectTechStackTokens(techStack).map((item) => item.toLowerCase().replace(/[^a-z0-9+#.-]+/g, " ").trim()).filter(Boolean);
  const tokenText = ` ${tokens.join(" ")} `;
  const detected = /* @__PURE__ */ new Set();
  const add = (id) => {
    detected.add(id);
  };
  const has = (pattern) => pattern.test(tokenText);
  if (has(/\b(?:typescript|tsx|ts)\b/)) add("typescript");
  if (!detected.has("typescript") && has(/\b(?:javascript|node\.?js|js|jsx)\b/)) add("javascript");
  if (has(/\b(?:html|css|scss|sass|less|tailwind|frontend|web)\b/)) add("html-css");
  if (has(/\b(?:python|pytest|django|flask|fastapi)\b/)) add("python");
  if (has(/\b(?:go|golang)\b/)) add("go");
  if (has(/\b(?:rust|cargo)\b/)) add("rust");
  if (has(/\b(?:dart|flutter)\b/)) {
    add("dart");
    if (has(/\bflutter\b/)) add("flutter");
  }
  if (has(/\b(?:kotlin|gradle|jvm)\b/)) add("kotlin");
  if (has(/\b(?:android|jetpack compose)\b/)) add("android");
  if (has(/\b(?:compose multiplatform|kotlin multiplatform|kmp)\b/)) {
    add("compose-multiplatform");
    add("kotlin");
  }
  if (has(/\b(?:swift|swiftui|ios|macos)\b/)) {
    add("swift");
    if (has(/\bswiftui\b/)) add("swiftui");
  }
  return Array.from(/* @__PURE__ */ new Set([...detected, ...techStackStyleGuideOverrides(techStack)])).sort();
}
function techStackFromArgs(args = {}) {
  return isRecord(args.techStack) ? asJsonObject(args.techStack) : null;
}
function loadTechStack(root) {
  return readJson(import_node_path20.default.join(root, "cadre", "tech-stack.json"), null);
}
function techStackForPacket(root, args = {}) {
  return techStackFromArgs(args) || loadTechStack(root);
}
function summarizeList(label, value) {
  const values = collectTechStackTokens(value).filter((item) => item !== label).slice(0, 12);
  return values.length > 0 ? `${label}: ${values.join(", ")}` : null;
}
function techStackSummary(root, args = {}) {
  const techStack = techStackForPacket(root, args);
  if (!techStack) {
    return {
      ok: false,
      root,
      path: import_node_path20.default.relative(root, import_node_path20.default.join(root, "cadre", "tech-stack.json")),
      error: "Missing structured tech stack: cadre/tech-stack.json"
    };
  }
  const lines = [
    summarizeList("languages", techStack.languages),
    summarizeList("frameworks", techStack.frameworks),
    summarizeList("runtimes", techStack.runtimes),
    summarizeList("platforms", techStack.platforms),
    summarizeList("packageManagers", techStack.packageManagers || techStack.package_managers),
    summarizeList("build", techStack.build),
    summarizeList("test", techStack.test),
    summarizeList("datastores", techStack.datastores),
    summarizeList("services", techStack.services),
    summarizeList("styleGuideIds", techStackStyleGuideOverrides(techStack))
  ].filter((line) => Boolean(line));
  return {
    ok: true,
    root,
    path: import_node_path20.default.relative(root, import_node_path20.default.join(root, "cadre", "tech-stack.json")),
    techStack,
    styleGuideIds: styleGuideIdsForTechStack(techStack),
    summary: lines.length > 0 ? lines.join("\n") : "No tech stack details recorded."
  };
}
function setupStyleGuides(root, args = {}) {
  const available = new Set(availableStyleGuideIds());
  const techStack = techStackForPacket(root, args) || {};
  const detected = styleGuideIdsForTechStack(techStack).filter((id) => available.has(id));
  const requested = requestedStyleGuideIds(args.styleGuideIds);
  const missing = requested.filter((id) => !available.has(id));
  const selected = Array.from(/* @__PURE__ */ new Set([
    ...available.has("general") ? ["general"] : [],
    ...detected,
    ...requested.filter((id) => available.has(id))
  ])).sort();
  return {
    ok: true,
    valid: missing.length === 0,
    detected,
    requested,
    selected,
    written: [],
    skipped: [],
    missing,
    warnings: missing.length > 0 ? [`Unknown setup style guide id(s) ignored: ${missing.join(", ")}`] : [],
    source: "tech-stack.json"
  };
}
function humanReviewConfirmed(args = {}) {
  const rawArgs = args;
  return rawArgs.humanConfirmed === true || rawArgs.human_confirmed === true || rawArgs.userConfirmed === true || rawArgs.user_confirmed === true || rawArgs.confirmed === true;
}

// src/core/application/runtime/review-bundles.ts
function reviewStats(text) {
  const normalized = text.replace(/\n*$/, "\n");
  return {
    bytes: Buffer.byteLength(normalized, "utf8"),
    lines: normalized.split("\n").length - 1,
    sha256: import_node_crypto2.default.createHash("sha256").update(normalized).digest("hex")
  };
}
function textReviewFile(relativePath, title, source, text) {
  return {
    path: relativePath,
    title,
    kind: "markdown",
    source,
    content: text.replace(/\n*$/, "\n")
  };
}
function plainReviewFile(relativePath, title, source, text) {
  return {
    path: relativePath,
    title,
    kind: "text",
    source,
    content: text.replace(/\n*$/, "\n")
  };
}
function jsonReviewFile(relativePath, title, source, value) {
  const text = value ? `${JSON.stringify(value, null, 2)}
` : "";
  return {
    path: relativePath,
    title,
    kind: "json",
    source,
    missing: value == null,
    content: text
  };
}
function setupLspWriteRequested(args = {}) {
  const rawArgs = args;
  return rawArgs.lsp === true || args.setupLsp === true || args.setup_lsp === true || args.writeLsp === true || args.write_lsp === true;
}
function setupLspWriteDisabled(args = {}) {
  const rawArgs = args;
  return rawArgs.lsp === false || args.setupLsp === false || args.setup_lsp === false || args.writeLsp === false || args.write_lsp === false;
}
function setupShouldWriteLsp(args, lspRecommendations) {
  if (setupLspWriteRequested(args)) return true;
  if (setupLspWriteDisabled(args)) return false;
  return Array.isArray(lspRecommendations.recommended) && lspRecommendations.recommended.length > 0;
}
function setupReviewFiles(root, args, styleGuides, polyrepoRequested) {
  const rawArgs = args;
  const techStack = techStackForPacket(root, args);
  const productJson = normalizeProjectDoc("product", rawArgs.product, "product.json", "Product Context", "Project-Specific Product Notes");
  const productGuidelinesJson = normalizeProjectDoc(
    "product_guidelines",
    rawArgs.productGuidelines || rawArgs.product_guidelines,
    "product_guidelines.json",
    "Product Guidelines",
    "Project-Specific Product Guideline Notes"
  );
  const workflowJson = normalizeProjectDoc("workflow", rawArgs.workflowPolicy || rawArgs.workflow_policy, "workflow.json", "Project Workflow", "Project-Specific Workflow Notes");
  const patternsSeed = templateJson("patterns_seed.json", { id: "initial", kind: "patterns_seed", text: "# Codebase Patterns\n\nLast refreshed: YYYY-MM-DD\n" });
  const patternsText = asOptionalString(patternsSeed.text) || "# Codebase Patterns\n\nLast refreshed: YYYY-MM-DD\n";
  const patternsEntry = {
    ...patternsSeed,
    id: "initial",
    kind: "patterns_seed",
    recorded_at: utcNow(),
    text: patternsText
  };
  const selectedStyleGuides = asStringArray(styleGuides.selected);
  const styleGuideIndex = {
    version: 1,
    schema: "cadre.styleguide_index.v1",
    selected: selectedStyleGuides,
    generated_at: utcNow()
  };
  const files = [
    jsonReviewFile("cadre/product.json", "Product context canonical", "product", productJson),
    textReviewFile("cadre/product.md", "Product context", "cadre/product.json", withGeneratedMarker("cadre/product.json", "cadre.product.v1", renderMarkdownDoc(productJson, "Product Context"))),
    jsonReviewFile("cadre/product_guidelines.json", "Product guidelines canonical", "productGuidelines", productGuidelinesJson),
    textReviewFile("cadre/product_guidelines.md", "Product guidelines", "cadre/product_guidelines.json", withGeneratedMarker("cadre/product_guidelines.json", "cadre.product_guidelines.v1", renderMarkdownDoc(productGuidelinesJson, "Product Guidelines"))),
    jsonReviewFile("cadre/tech-stack.json", "Structured tech stack", "techStack", techStack),
    jsonReviewFile("cadre/workflow.json", "Workflow policy canonical", "workflowPolicy", workflowJson),
    textReviewFile("cadre/workflow.md", "Workflow policy", "cadre/workflow.json", withGeneratedMarker("cadre/workflow.json", "cadre.workflow.v1", renderMarkdownDoc(workflowJson, "Project Workflow"))),
    plainReviewFile("cadre/patterns.jsonl", "Project patterns canonical", "template:patterns_seed.json", `${JSON.stringify(patternsEntry)}
`),
    textReviewFile("cadre/patterns.md", "Project patterns", "cadre/patterns.jsonl", withGeneratedMarker("cadre/patterns.jsonl", "cadre.patterns.v1", patternsText)),
    jsonReviewFile("cadre/styleguides/index.json", "Style guide catalog canonical", "tech-stack.json/styleGuideIds", styleGuideIndex),
    textReviewFile("cadre/code_styleguides/README.md", "Style guide catalog", "cadre/styleguides/index.json", withGeneratedMarker("cadre/styleguides/index.json", "cadre.styleguide_index.v1", renderJsonCodeblock("Style guide catalog", styleGuideIndex))),
    ...selectedStyleGuides.flatMap((guideId) => {
      const guideJson = templateJson(`styleguides/${guideId}.json`, {
        version: 1,
        schema: "cadre.styleguide.v1",
        id: guideId,
        title: guideId,
        rules: [],
        source: "bundled_template"
      });
      return [
        jsonReviewFile(`cadre/styleguides/${guideId}.json`, `Code style guide canonical: ${guideId}`, "tech-stack.json/styleGuideIds", guideJson),
        textReviewFile(
          `cadre/code_styleguides/${guideId}.md`,
          `Code style guide: ${guideId}`,
          `cadre/styleguides/${guideId}.json`,
          withGeneratedMarker(`cadre/styleguides/${guideId}.json`, "cadre.styleguide.v1", renderStyleGuideMarkdown(guideJson))
        )
      ];
    })
  ];
  if (polyrepoRequested) {
    files.push(jsonReviewFile("cadre/repos.json", "Polyrepo topology", "repos", isRecord(rawArgs.repos) ? asJsonObject(rawArgs.repos) : null));
  }
  return files;
}
function reviewArtifactsFromFiles(reviewFiles) {
  return reviewFiles.map((file) => ({
    path: file.path,
    title: file.title,
    kind: file.kind,
    source: file.source,
    missing: file.missing === true,
    ...reviewStats(file.content)
  }));
}
function setupReviewArtifacts(reviewFiles, styleGuides) {
  const artifacts = [
    ...reviewArtifactsFromFiles(reviewFiles),
    {
      path: "cadre/styleguides/*.json",
      title: "Selected code style guides",
      kind: "selection",
      source: "tech-stack.json/styleGuideIds",
      selected: asStringArray(styleGuides.selected),
      missing: asStringArray(styleGuides.missing),
      warnings: asStringArray(styleGuides.warnings)
    }
  ];
  return artifacts;
}
function shellQuote(value) {
  return `'${value.replace(/'/g, "'\\''")}'`;
}
function workflowReviewBundle(root, workflow, args, reviewFiles, manifestExtras = {}) {
  const rawArgs = args;
  if (reviewFiles.length === 0) return null;
  if (args.execute === true && humanReviewConfirmed(args) || rawArgs.reviewBundle === false || rawArgs.reviewFiles === false) return null;
  const explicitDir = asOptionalString(rawArgs.reviewBundleDir || rawArgs.review_bundle_dir || rawArgs.reviewDir || rawArgs.review_dir);
  const rootHash = import_node_crypto2.default.createHash("sha256").update(root).digest("hex").slice(0, 12);
  const defaultDirectory = import_node_path21.default.join(import_node_os2.default.tmpdir(), `cadre-${safeName(workflow)}-review-${safeName(import_node_path21.default.basename(root))}-${rootHash}`);
  let directory = explicitDir ? import_node_path21.default.resolve(root, explicitDir) : defaultDirectory;
  const resolvedRoot = import_node_path21.default.resolve(root);
  const resolvedCadre = import_node_path21.default.join(resolvedRoot, "cadre");
  const relativeToCadre = import_node_path21.default.relative(resolvedCadre, directory);
  const insideCadre = relativeToCadre === "" || !relativeToCadre.startsWith("..") && !import_node_path21.default.isAbsolute(relativeToCadre);
  const unsafeExplicitDir = Boolean(explicitDir) && (directory === resolvedRoot || insideCadre);
  const warnings = unsafeExplicitDir ? [`Ignored unsafe reviewBundleDir ${explicitDir}; using a temp review bundle outside the project control plane.`] : [];
  if (unsafeExplicitDir) directory = defaultDirectory;
  if (!explicitDir || unsafeExplicitDir) import_node_fs12.default.rmSync(directory, { recursive: true, force: true });
  import_node_fs12.default.mkdirSync(directory, { recursive: true });
  const files = reviewFiles.map((file) => {
    const reviewPath = import_node_path21.default.join(directory, file.path);
    import_node_fs12.default.mkdirSync(import_node_path21.default.dirname(reviewPath), { recursive: true });
    import_node_fs12.default.writeFileSync(reviewPath, file.content);
    return {
      path: file.path,
      review_path: reviewPath,
      title: file.title,
      kind: file.kind,
      source: file.source,
      missing: file.missing === true,
      ...reviewStats(file.content)
    };
  });
  const manifestPath = import_node_path21.default.join(directory, "manifest.json");
  const relativeReviewPaths = files.map((file) => shellQuote(String(file.path)));
  const openCommand = relativeReviewPaths.length > 0 ? `cd ${shellQuote(directory)} && \${VISUAL:-\${EDITOR:-vi}} ${relativeReviewPaths.join(" ")}` : `cd ${shellQuote(directory)} && \${VISUAL:-\${EDITOR:-vi}} ${shellQuote("manifest.json")}`;
  const printCommand = relativeReviewPaths.length > 0 ? `cd ${shellQuote(directory)} && for file in ${relativeReviewPaths.join(" ")}; do printf '\\n### %s\\n' "$file"; sed -n '1,240p' "$file"; done` : `sed -n '1,240p' ${shellQuote(manifestPath)}`;
  const commands = {
    list: `find ${shellQuote(directory)} -type f | sort`,
    open_editor: openCommand,
    print: printCommand
  };
  const manifest = {
    version: 1,
    kind: `cadre_${safeName(workflow)}_review`,
    workflow,
    root,
    generated_at: utcNow(),
    content_in_response: false,
    warnings,
    files,
    ...manifestExtras,
    commands
  };
  writeJson(manifestPath, manifest);
  return {
    directory,
    manifest_path: manifestPath,
    content_in_response: false,
    warnings,
    files,
    commands
  };
}
function setupReviewBundle(root, args, reviewFiles, styleGuides) {
  return workflowReviewBundle(root, "setup", args, reviewFiles, {
    styleGuides: {
      selected: asStringArray(styleGuides.selected),
      missing: asStringArray(styleGuides.missing),
      warnings: asStringArray(styleGuides.warnings)
    }
  });
}
function setupLspReviewArtifacts(args = {}, writeRequested = setupLspWriteRequested(args)) {
  if (writeRequested) {
    return [
      {
        path: "cadre/lsp.json",
        title: "LSP configuration",
        kind: "json",
        source: "lsp_setup",
        write_requested: true
      }
    ];
  }
  return [];
}
function appendLspReviewArtifacts(artifacts, args = {}, writeRequested = setupLspWriteRequested(args)) {
  artifacts.push(...setupLspReviewArtifacts(args, writeRequested));
  return artifacts;
}
function humanReviewState(workflow, args, artifacts, reviewBundle = null) {
  return {
    required: true,
    confirmed: humanReviewConfirmed(args),
    workflow,
    confirm_argument: "humanConfirmed",
    artifacts,
    review_bundle: reviewBundle
  };
}
function packetReviewArtifact(title, source, fields = {}) {
  return {
    title,
    kind: "packet",
    source,
    content_in_response: false,
    ...fields
  };
}
function trackLearningsText(trackId) {
  const seed = templateJson("learnings_seed.json", {
    text: "# Track Learnings: {{track_id}}\n\nPatterns, gotchas, and context discovered during implementation.\n"
  });
  return (asOptionalString(seed.text) || "# Track Learnings: {{track_id}}\n\n").replace(/\{\{track_id\}\}/g, trackId).replace(/\n*$/, "\n");
}
function installedStyleGuideIds(root) {
  const dir = import_node_path21.default.join(root, "cadre", "styleguides");
  if (!fileExists(dir)) return [];
  try {
    return import_node_fs12.default.readdirSync(dir).filter((file) => file.endsWith(".json") && file !== "index.json").map((file) => import_node_path21.default.basename(file, ".json")).sort();
  } catch {
    return [];
  }
}
function styleGuideIdsForFiles(files) {
  const ids = /* @__PURE__ */ new Set();
  for (const rawFile of files) {
    const file = normalizeClaimPath(rawFile);
    const ext = import_node_path21.default.extname(file).toLowerCase();
    const base = import_node_path21.default.basename(file).toLowerCase();
    if ([".ts", ".tsx"].includes(ext)) ids.add("typescript");
    if ([".js", ".jsx", ".mjs", ".cjs"].includes(ext)) ids.add("javascript");
    if ([".html", ".css", ".scss", ".sass", ".less", ".vue", ".svelte"].includes(ext)) ids.add("html-css");
    if (ext === ".py") ids.add("python");
    if (ext === ".go") ids.add("go");
    if (ext === ".rs") ids.add("rust");
    if ([".kt", ".kts"].includes(ext)) ids.add("kotlin");
    if (ext === ".swift") ids.add("swift");
    if (ext === ".dart") ids.add("dart");
    if (base === "androidmanifest.xml" || file.includes("/android/")) ids.add("android");
    if (file.includes("compose")) ids.add("compose-multiplatform");
    if (file.includes("swiftui")) ids.add("swiftui");
    if (ext === ".dart" && (file.startsWith("lib/") || file.includes("/flutter/"))) ids.add("flutter");
  }
  return Array.from(ids).sort();
}
function implementationStyleGuides(root, trackId, args = {}) {
  const installed = installedStyleGuideIds(root);
  if (installed.length === 0) {
    return {
      ok: true,
      available: false,
      source: "cadre/styleguides",
      installed: [],
      selected: [],
      guides: [],
      warning: "No installed Cadre code style guides found"
    };
  }
  const installedSet = new Set(installed);
  const track = findTrack(root, trackId);
  const plan = track ? parsePlanFile(track.plan_path) : { tasks: [] };
  const taskFiles = asArray(plan.tasks).flatMap((task) => asStringArray(asJsonObject(task).files)).map(normalizeClaimPath).filter(Boolean);
  const taskFileIds = styleGuideIdsForFiles(taskFiles).filter((id) => installedSet.has(id));
  const techStack = techStackForPacket(root, args);
  if (!techStack) {
    return {
      ok: false,
      available: false,
      source: "cadre/tech-stack.json",
      installed,
      selected: [],
      guides: [],
      error: "Missing structured tech stack: cadre/tech-stack.json"
    };
  }
  const techStackIds = styleGuideIdsForTechStack(techStack).filter((id) => installedSet.has(id));
  const requested = requestedStyleGuideIds(args.styleGuideIds);
  const missing = requested.filter((id) => !installedSet.has(id));
  const selected = Array.from(/* @__PURE__ */ new Set([
    ...installedSet.has("general") ? ["general"] : [],
    ...techStackIds,
    ...requested.filter((id) => installedSet.has(id))
  ])).sort();
  const maxChars = Math.max(1e3, Math.min(Number(args.styleGuideMaxChars || 6e3), 2e4));
  const guides = selected.map((id) => {
    const file = import_node_path21.default.join(root, "cadre", "styleguides", `${id}.json`);
    const guide = readJson(file, null) || {};
    const text = JSON.stringify(guide, null, 2);
    return {
      id,
      path: import_node_path21.default.relative(root, file),
      guide,
      content: text.slice(0, maxChars),
      truncated: text.length > maxChars,
      bytes: Buffer.byteLength(text, "utf8"),
      reasons: [
        id === "general" ? "general" : null,
        techStackIds.includes(id) ? "tech_stack" : null,
        requested.includes(id) ? "explicit" : null
      ].filter(Boolean)
    };
  });
  return {
    ok: missing.length === 0,
    available: true,
    source: "cadre/styleguides",
    tech_stack_source: "cadre/tech-stack.json",
    installed,
    selected,
    tech_stack_ids: techStackIds,
    task_file_ids: taskFileIds,
    task_files: taskFiles,
    missing,
    max_chars_per_guide: maxChars,
    guides
  };
}

// src/core/application/runtime/repo-resolution.ts
function topologyRepoEntries(topology) {
  const entries = {};
  for (const raw of Array.isArray(topology.repos.repos) ? topology.repos.repos : []) {
    const repo = asJsonObject(raw);
    const name = asOptionalString(repo.name);
    if (!name) continue;
    entries[name] = {
      submodule_path: asOptionalString(repo.submodule_path) || "",
      base_branch: asOptionalString(repo.default_branch) || asOptionalString(repo.base_branch) || "main"
    };
  }
  return entries;
}
function trackRepoEntries(root, track) {
  const topology = loadTopology(root);
  const entries = topologyRepoEntries(topology);
  const reposMetadata = isRecord(track.metadata.repos) ? track.metadata.repos : null;
  if (reposMetadata) {
    for (const [repo, rawInfo] of Object.entries(reposMetadata)) {
      entries[repo] = { ...entries[repo], ...asJsonObject(rawInfo) };
    }
  }
  return entries;
}
function availableRepoNames(root, track) {
  return Object.keys(trackRepoEntries(root, track)).sort();
}
function unresolvedWorkingRoot(root, track, repo, task = null) {
  return {
    ok: false,
    repo,
    path: "",
    source: "polyrepo-unresolved-repo",
    error: `Unknown polyrepo task repo "${repo}" for track ${track.track_id}`,
    unresolved_repo: repo,
    available_repos: availableRepoNames(root, track),
    track_id: track.track_id,
    task_key: task?.task_key
  };
}
function isWorkingRootError(value) {
  return value.ok === false;
}
function unresolvedPlanRepos(root, track, args = {}) {
  const topology = loadTopology(root);
  if (!topology.polyrepo) return [];
  const entries = trackRepoEntries(root, track);
  const known = new Set(Object.keys(entries));
  const plan = parsePlanFile(track.plan_path);
  const errors = [];
  const seen = /* @__PURE__ */ new Set();
  for (const task of plan.tasks || []) {
    const repo = asOptionalString(args.repo) || task.repo || topology.defaultRepo;
    if (repo && known.has(repo)) continue;
    const key = `${repo || ""}:${task.task_key}`;
    if (seen.has(key)) continue;
    seen.add(key);
    errors.push({
      track_id: track.track_id,
      task_key: task.task_key,
      line: task.line,
      repo: repo || null,
      message: repo ? `Unknown polyrepo task repo "${repo}"` : "Task has no repo annotation and repos.json has no default_repo",
      available_repos: Array.from(known).sort()
    });
  }
  return errors;
}
function repoEntriesError(root, track, args = {}) {
  const topology = loadTopology(root);
  if (!topology.polyrepo) return null;
  const entries = trackRepoEntries(root, track);
  const requested = asOptionalString(args.repo);
  const missing = requested ? entries[requested] ? [] : [{ repo: requested, message: `Unknown polyrepo repo "${requested}"` }] : unresolvedPlanRepos(root, track, args);
  if (missing.length === 0) return null;
  return {
    ok: false,
    stage: "polyrepo_repo_resolution",
    track_id: track.track_id,
    errors: missing,
    available_repos: Object.keys(entries).sort()
  };
}
function resolveTaskWorkingRoot(root, track, task = null, args = {}) {
  if (args.workingRoot) {
    const candidate = import_node_path22.default.isAbsolute(args.workingRoot) ? args.workingRoot : import_node_path22.default.resolve(root, args.workingRoot);
    return { repo: args.repo || task?.repo || ".", path: candidate, source: "argument.workingRoot" };
  }
  const topology = loadTopology(root);
  if (topology.polyrepo) {
    const repo = args.repo || task?.repo || topology.defaultRepo;
    const info = typeof repo === "string" ? trackRepoEntries(root, track)[repo] || {} : {};
    if (Object.keys(info).length > 0) {
      const rel = info.worktree_path || info.submodule_path || "";
      return {
        repo,
        path: rel ? import_node_path22.default.resolve(root, rel) : root,
        source: info.worktree_path ? "metadata.repos.worktree_path" : "metadata.repos.submodule_path"
      };
    }
    return unresolvedWorkingRoot(root, track, String(repo || ""), task);
  }
  if (track.metadata.worktree_path) {
    const candidate = import_node_path22.default.resolve(root, track.metadata.worktree_path);
    if (fileExists(candidate)) {
      return { repo: ".", path: candidate, source: "metadata.worktree_path" };
    }
  }
  return { repo: ".", path: root, source: "project-root" };
}
function repoEntriesForTrack(root, track, args = {}) {
  const topology = loadTopology(root);
  if (topology.polyrepo) {
    const repos = trackRepoEntries(root, track);
    return Object.entries(repos).filter(([repo]) => !args.repo || args.repo === repo).map(([repo, rawInfo]) => {
      const info = asJsonObject(rawInfo);
      const rel = info.worktree_path || info.submodule_path || "";
      return {
        repo,
        root: rel ? import_node_path22.default.resolve(root, rel) : root,
        path: rel,
        base: args.base || info.base_branch || "main",
        head: args.head || info.git_branch || track.metadata.git_branch || `track/${track.track_id}`,
        source: info.worktree_path ? "metadata.repos.worktree_path" : "metadata.repos.submodule_path"
      };
    });
  }
  return [{
    repo: args.repo || ".",
    root: args.workingRoot ? import_node_path22.default.resolve(root, args.workingRoot) : root,
    path: args.workingRoot || ".",
    base: args.base || "main",
    head: args.head || track.metadata.git_branch || `track/${track.track_id}`,
    source: args.workingRoot ? "argument.workingRoot" : "project-root"
  }];
}
function gitRevParse(root, ref) {
  if (!ref) return null;
  const result = runCommand("git", ["rev-parse", ref], { cwd: root });
  return result.ok ? result.stdout.trim() || null : null;
}
function reviewedShasForTrack(root, track, args = {}) {
  const repoError = repoEntriesError(root, track, args);
  if (repoError) return repoError;
  const supplied = args.reviewedShas || args.reviewed_shas || null;
  const entries = repoEntriesForTrack(root, track, args);
  const reviewedShas = {};
  const controlHead = supplied && asOptionalString(supplied["."]) || args.reviewedSha || args.reviewed_sha || gitRevParse(root, track.metadata.git_branch || `track/${track.track_id}`) || gitRevParse(root, "HEAD");
  if (controlHead) reviewedShas["."] = controlHead;
  for (const entry of entries) {
    const repo = asString(entry.repo, ".");
    reviewedShas[repo] = supplied && asOptionalString(supplied[repo]) || gitRevParse(asString(entry.root, root), asString(entry.head, "HEAD")) || gitRevParse(asString(entry.root, root), "HEAD");
  }
  return {
    reviewed_sha: controlHead || null,
    reviewed_shas: reviewedShas
  };
}
function implementationPrep(root, args = {}) {
  const identity = args.identity || gitIdentity(root);
  const team = teamStatus(root);
  const available = availableWork(root);
  let trackId = args.trackId || args.track_id || null;
  const warnings = [];
  const availableTracks = asArray(available.available);
  if (!trackId && availableTracks.length > 0) {
    trackId = asOptionalString(availableTracks[0]?.track_id) || null;
  }
  if (!trackId) {
    const teamTracks = asArray(team.tracks);
    const mine = teamTracks.find((track) => track.status === "in_progress" && (!track.owner || track.owner === identity));
    const anyOpen = teamTracks.find((track) => ["new", "in_progress", "blocked"].includes(asString(track.status)));
    trackId = asOptionalString((mine || anyOpen || {}).track_id) || null;
  }
  if (!trackId) {
    return {
      ok: false,
      root,
      identity,
      reason: "No available or incomplete track found",
      team,
      available
    };
  }
  let claim = null;
  if (args.claim === true) {
    claim = claimTrack(root, trackId, { identity, takeover: args.takeover === true });
    if (!claim.ok) {
      return { ok: false, root, identity, selected_track: trackId, claim, team, available };
    }
  }
  const context = trackContext(root, trackId);
  const styleGuides = implementationStyleGuides(root, trackId, args);
  const collisions = collisionScan(root);
  const selectedCollisions = asArray(collisions.collisions).filter(
    (collision) => asStringArray(collision.track_ids).includes(trackId)
  );
  const integrity = planIntegrity(root, trackId);
  const foreignCollisions = selectedCollisions.filter(
    (collision) => asStringArray(collision.owners).some((owner) => owner && owner !== identity)
  );
  if (foreignCollisions.length > 0) {
    warnings.push(`${foreignCollisions.length} cross-owner file collision(s) involve the selected track`);
  }
  const contextHold = asJsonObject(context.hold);
  if (context.ok && contextHold.owner && identity && contextHold.owner !== identity) {
    warnings.push(`Selected track is held by ${contextHold.owner}`);
  }
  return {
    ok: context.ok && integrity.ok,
    root,
    identity,
    selected_track: trackId,
    claim,
    context,
    styleGuides,
    team_summary: {
      total_tracks: team.total_tracks,
      by_status: team.by_status,
      by_owner: team.by_owner
    },
    available,
    collisions: selectedCollisions,
    integrity,
    warnings
  };
}

// src/core/application/runtime/track-schedule.ts
function workStateForTrack(track) {
  const statePath = import_node_path23.default.join(track.dir, "implement_state.json");
  return readJson(statePath, null);
}
function holdInfo(track, now = Date.now()) {
  const state = workStateForTrack(track);
  const lease = track.metadata.lease || null;
  const stateOwner = state?.owner || null;
  const owner = stateOwner || track.metadata.owner || null;
  const leaseOwner = lease?.owner || null;
  const leaseTime = lease && (asOptionalString(lease.heartbeat_at) || lease.acquired_at);
  const stateTime = state && (state.last_updated || state.last_handoff);
  const leaseStale = staleInfo(leaseTime, now);
  const stateStale = staleInfo(stateTime, now);
  return {
    owner,
    metadata_owner: track.metadata.owner || null,
    state_owner: stateOwner,
    lease_owner: leaseOwner,
    lease_heartbeat_at: leaseTime || null,
    lease_stale: lease ? leaseStale.stale : false,
    lease_age_minutes: leaseStale.age_minutes,
    state_last_updated: stateTime || null,
    state_stale: state ? stateStale.stale : false,
    state_age_minutes: stateStale.age_minutes
  };
}
function taskCounts(plan) {
  const counts = { total: 0, pending: 0, in_progress: 0, completed: 0, blocked: 0, skipped: 0, percent: 0 };
  for (const phase of plan.phases || []) {
    for (const task of phase.tasks || []) {
      counts.total += 1;
      if (task.marker === "x") counts.completed += 1;
      else if (task.marker === "~") counts.in_progress += 1;
      else if (task.marker === "!") counts.blocked += 1;
      else if (task.marker === "-") counts.skipped += 1;
      else counts.pending += 1;
    }
  }
  counts.percent = counts.total === 0 ? 0 : Math.round(counts.completed / counts.total * 100);
  return counts;
}
function listTrackDirs(root) {
  const tracksDir = import_node_path23.default.join(root, "cadre", "tracks");
  if (!fileExists(tracksDir)) return [];
  return import_node_fs13.default.readdirSync(tracksDir, { withFileTypes: true }).filter((entry) => entry.isDirectory()).map((entry) => import_node_path23.default.join(tracksDir, entry.name)).sort();
}
function listTracks(root) {
  const tracks = [];
  for (const dir of listTrackDirs(root)) {
    const metadataPath = import_node_path23.default.join(dir, "metadata.json");
    const metadata = readJson(metadataPath, null);
    if (!metadata) continue;
    const trackId = metadata.track_id || import_node_path23.default.basename(dir);
    tracks.push({
      track_id: trackId,
      dir,
      metadata_path: metadataPath,
      plan_path: import_node_path23.default.join(dir, "plan.md"),
      spec_path: import_node_path23.default.join(dir, "spec.md"),
      plan_json_path: import_node_path23.default.join(dir, "plan.json"),
      spec_json_path: import_node_path23.default.join(dir, "spec.json"),
      learnings_jsonl_path: import_node_path23.default.join(dir, "learnings.jsonl"),
      handoff_json_path: import_node_path23.default.join(dir, "handoff.json"),
      metadata
    });
  }
  return tracks;
}
function parsePlanJson(raw) {
  return planJsonToParsedPlan(asJsonObject(raw));
}
function parsePlanFile(file) {
  const planJsonPath = planJsonPathForPlanPath(file);
  if (fileExists(planJsonPath)) {
    const raw = readJson(planJsonPath, null);
    if (raw) return parsePlanJson(raw);
  }
  return {
    ok: false,
    phases: [],
    tasks: [],
    warnings: [],
    errors: [`Missing canonical plan JSON: ${planJsonPath}`]
  };
}
function planClaims(root, track, topology = loadTopology(root)) {
  const plan = parsePlanFile(track.plan_path);
  const claims = [];
  for (const phase of plan.phases) {
    for (const task of phase.tasks) {
      const repo = topology.polyrepo ? task.repo || topology.defaultRepo : ".";
      for (const file of task.files) {
        claims.push({
          track_id: track.track_id,
          owner: track.metadata.owner || null,
          repo,
          file,
          phase: phase.title,
          task: task.title,
          task_line: task.line
        });
      }
    }
  }
  return claims;
}
function phaseAliases(phase) {
  const title = String(phase.title || "").trim().toLowerCase();
  const simpleTitle = title.replace(/^phase\s+\d+\s*:\s*/, "").trim();
  return Array.from(new Set([
    `phase${phase.phase_index}`,
    `phase ${phase.phase_index}`,
    String(phase.phase_index),
    title,
    simpleTitle
  ].filter(Boolean)));
}
function resolvePhaseDependency(value, aliasMap) {
  const key = String(value || "").trim().toLowerCase();
  if (!key) return null;
  if (aliasMap.has(key)) return aliasMap.get(key) ?? null;
  const compact = key.replace(/\s+/g, "");
  if (aliasMap.has(compact)) return aliasMap.get(compact) ?? null;
  const phaseNumber = key.match(/^phase\s*(\d+)$/i) || key.match(/^(\d+)$/);
  if (phaseNumber?.[1] && aliasMap.has(`phase${phaseNumber[1]}`)) return aliasMap.get(`phase${phaseNumber[1]}`) ?? null;
  return null;
}
function phaseDependencyIds(phase, previousPhase, aliasMap) {
  if (!Object.prototype.hasOwnProperty.call(phase.annotations || {}, "depends")) {
    return previousPhase ? [`phase${previousPhase.phase_index ?? ""}`] : [];
  }
  const raw = String((phase.annotations || {}).depends || "").trim();
  if (!raw) return [];
  return raw.split(",").map((item) => resolvePhaseDependency(item, aliasMap)).filter((item) => item !== null).map((item) => `phase${item.phase_index ?? ""}`);
}
function phaseStatus(phase) {
  const tasks = phase.tasks || [];
  if (tasks.length === 0) return "completed";
  if (tasks.every((task) => task.marker === "x" || task.marker === "-")) return "completed";
  if (tasks.some((task) => task.marker === "!")) return "blocked";
  if (tasks.some((task) => task.marker === "~" || task.marker === "x" || task.marker === "-")) return "in_progress";
  return "pending";
}
function claimsForPhase(root, phase, topology = loadTopology(root)) {
  const claims = [];
  for (const task of phase.tasks || []) {
    const repo = topology.polyrepo ? task.repo || topology.defaultRepo : ".";
    for (const file of task.files || []) {
      claims.push({
        phase_id: `phase${phase.phase_index}`,
        phase_index: phase.phase_index ?? 0,
        phase_title: phase.title,
        task_key: task.task_key ?? "",
        task_title: task.title,
        repo,
        file: normalizeClaimPath(file)
      });
    }
  }
  return claims;
}
function phaseConflict(left, right) {
  const conflicts = [];
  for (const leftClaim of left.claims || []) {
    for (const rightClaim of right.claims || []) {
      if (leftClaim.repo !== rightClaim.repo) continue;
      if (!claimsOverlap(leftClaim.file, rightClaim.file)) continue;
      conflicts.push({ left: leftClaim, right: rightClaim });
    }
  }
  return conflicts;
}
function groupReadyPhases(readyPhases) {
  const groups = [];
  const conflicts = [];
  for (const phase of readyPhases) {
    let placed = false;
    for (const group of groups) {
      const groupConflicts = group.flatMap((existing) => phaseConflict(existing, phase));
      if (groupConflicts.length === 0) {
        group.push(phase);
        placed = true;
        break;
      }
      conflicts.push(...groupConflicts);
    }
    if (!placed) groups.push([phase]);
  }
  return { groups, conflicts };
}
function detectPhaseCycles(phaseNodes) {
  const byId = new Map(phaseNodes.map((phase) => [phase.phase_id, phase]));
  const visiting = /* @__PURE__ */ new Set();
  const visited = /* @__PURE__ */ new Set();
  const cycles = [];
  const stack = [];
  function visit(id) {
    if (visited.has(id)) return;
    if (visiting.has(id)) {
      const start = stack.indexOf(id);
      cycles.push(stack.slice(start).concat(id));
      return;
    }
    visiting.add(id);
    stack.push(id);
    const node = byId.get(id);
    for (const dep of node && node.depends_on || []) visit(dep);
    stack.pop();
    visiting.delete(id);
    visited.add(id);
  }
  for (const phase of phaseNodes) visit(phase.phase_id);
  return cycles;
}
function topologicalPhaseWaves(phaseNodes) {
  const remaining = new Map(phaseNodes.map((phase) => [phase.phase_id, { ...phase }]));
  const completed = /* @__PURE__ */ new Set();
  const waves = [];
  while (remaining.size > 0) {
    const wave = Array.from(remaining.values()).filter((phase) => phase.depends_on.every((dep) => completed.has(dep))).sort((a, b) => a.phase_index - b.phase_index);
    if (wave.length === 0) break;
    waves.push(wave.map((phase) => phase.phase_id));
    for (const phase of wave) {
      completed.add(phase.phase_id);
      remaining.delete(phase.phase_id);
    }
  }
  return waves;
}
function phaseSchedule(root, args = {}) {
  const track = findTrack(root, args.trackId || args.track_id);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId || args.track_id}` };
  const plan = parsePlanFile(track.plan_path);
  const topology = loadTopology(root);
  const aliasMap = /* @__PURE__ */ new Map();
  for (const phase of plan.phases || []) {
    for (const alias of phaseAliases(phase)) aliasMap.set(alias, phase);
  }
  const errors = [];
  const phases = (plan.phases || []).map((phase, index, all) => {
    const rawDepends = Object.prototype.hasOwnProperty.call(phase.annotations || {}, "depends") ? String((phase.annotations || {}).depends || "").trim() : null;
    const unknownDepends = rawDepends == null || rawDepends === "" ? [] : rawDepends.split(",").map((item) => item.trim()).filter(Boolean).filter((item) => !resolvePhaseDependency(item, aliasMap));
    for (const dep of unknownDepends) {
      errors.push({ phase_id: `phase${phase.phase_index}`, message: `Unknown phase dependency: ${dep}` });
    }
    const dependsOn = phaseDependencyIds(phase, all[index - 1], aliasMap);
    const claims = claimsForPhase(root, phase, topology);
    return {
      phase_id: `phase${phase.phase_index}`,
      phase_index: phase.phase_index ?? index + 1,
      title: phase.title,
      execution: asString(phase.annotations?.execution, "sequential"),
      depends_on: dependsOn,
      status: phaseStatus(phase),
      task_counts: taskCounts({ phases: [phase] }),
      claims,
      tasks: (phase.tasks || []).map((task) => ({
        task_key: task.task_key ?? "",
        task_index: task.task_index ?? 0,
        title: task.title,
        marker: task.marker,
        repo: task.repo || (topology.polyrepo ? topology.defaultRepo : "."),
        files: task.files || [],
        depends: task.depends || [],
        labels: task.labels || []
      }))
    };
  });
  const cycles = detectPhaseCycles(phases);
  for (const cycle of cycles) {
    errors.push({ phase_id: cycle[0] || null, message: `Phase dependency cycle: ${cycle.join(" -> ")}` });
  }
  errors.push(...unresolvedPlanRepos(root, track, args));
  const completed = new Set(phases.filter((phase) => phase.status === "completed").map((phase) => phase.phase_id));
  const ready = errors.length === 0 ? phases.filter((phase) => !["completed", "blocked"].includes(phase.status)).filter((phase) => phase.depends_on.every((dep) => completed.has(dep))) : [];
  const { groups, conflicts } = groupReadyPhases(ready);
  return {
    ok: errors.length === 0,
    track_id: track.track_id,
    phases,
    topological_waves: topologicalPhaseWaves(phases),
    ready_phases: ready.map((phase) => phase.phase_id),
    ready_groups: groups.map((group) => group.map((phase) => phase.phase_id)),
    conflict_splits: conflicts.map((conflict) => ({
      repo: conflict.left.repo,
      file: conflict.left.file === conflict.right.file ? conflict.left.file : `${conflict.left.file} <-> ${conflict.right.file}`,
      left_phase: conflict.left.phase_id,
      right_phase: conflict.right.phase_id,
      left_task: conflict.left.task_key,
      right_task: conflict.right.task_key
    })),
    errors
  };
}

// src/core/application/runtime/collision.ts
function normalizeClaimPath(file) {
  return String(file || "").trim().replace(/^["']|["']$/g, "").replace(/\\/g, "/").replace(/\/+/g, "/").replace(/^\.\//, "").replace(/\/$/, "");
}
function globToRegExp(glob) {
  const normalized = normalizeClaimPath(glob);
  let out = "^";
  for (let i = 0; i < normalized.length; i += 1) {
    const char = normalized[i];
    const next = normalized[i + 1];
    if (char === "*" && next === "*") {
      out += ".*";
      i += 1;
    } else if (char === "*") {
      out += "[^/]*";
    } else if (char === "?") {
      out += "[^/]";
    } else if (char && "\\^$+?.()|{}[]".includes(char)) {
      out += `\\${char}`;
    } else {
      out += char;
    }
  }
  return new RegExp(`${out}$`);
}
function isGlobClaim(file) {
  return /[*?]/.test(file);
}
function claimsOverlap(leftFile, rightFile) {
  const left = normalizeClaimPath(leftFile);
  const right = normalizeClaimPath(rightFile);
  if (!left || !right) return false;
  if (left === right) return true;
  if (right.startsWith(`${left}/`) || left.startsWith(`${right}/`)) return true;
  if (isGlobClaim(left) && globToRegExp(left).test(right)) return true;
  if (isGlobClaim(right) && globToRegExp(right).test(left)) return true;
  return false;
}
function collisionScan(root) {
  const topology = loadTopology(root);
  const active = listTracks(root).filter(
    (track) => ["new", "in_progress", "blocked"].includes(track.metadata.status || "new")
  );
  const claims = [];
  for (const track of active) {
    for (const claim of planClaims(root, track, topology)) {
      claims.push({
        ...claim,
        file: normalizeClaimPath(claim.file)
      });
    }
  }
  const collisions = [];
  const seen = /* @__PURE__ */ new Set();
  for (let i = 0; i < claims.length; i += 1) {
    for (let j = i + 1; j < claims.length; j += 1) {
      const left = claims[i];
      const right = claims[j];
      if (!left || !right) continue;
      if (left.track_id === right.track_id) continue;
      if (left.repo !== right.repo) continue;
      if (!claimsOverlap(left.file, right.file)) continue;
      const trackIds = [left.track_id, right.track_id].sort();
      const files = [left.file, right.file].sort();
      const key = `${left.repo}\0${trackIds.join("\0")}\0${files.join("\0")}`;
      if (seen.has(key)) continue;
      seen.add(key);
      const collisionClaims = [left, right];
      collisions.push({
        repo: left.repo,
        file: left.file === right.file ? left.file : `${left.file} <-> ${right.file}`,
        kind: left.file === right.file ? "exact" : "overlap",
        claims: collisionClaims,
        track_ids: trackIds,
        owners: Array.from(new Set(collisionClaims.map((claim) => claim.owner).filter(Boolean))).sort()
      });
    }
  }
  collisions.sort((a, b) => `${asString(a.repo)}${asString(a.file)}`.localeCompare(`${asString(b.repo)}${asString(b.file)}`));
  return {
    root,
    active_tracks: active.length,
    collisions
  };
}

// src/core/application/runtime/status.ts
var TRACKS_INDEX_SCHEMA = "cadre.tracks_index.v1";
function liveStatus(root) {
  const tracks = listTracks(root);
  const identity = gitIdentity(root);
  const byStatus = /* @__PURE__ */ new Map();
  const activeTracks = [];
  for (const track of tracks) {
    const status = track.metadata.status || "new";
    byStatus.set(status, (byStatus.get(status) || 0) + 1);
    if (status === "in_progress") {
      const plan = parsePlanFile(track.plan_path);
      activeTracks.push({
        track_id: track.track_id,
        name: track.metadata.name || track.metadata.description || track.track_id,
        owner: track.metadata.owner || null,
        git_branch: track.metadata.git_branch || `track/${track.track_id}`,
        task_counts: taskCounts(plan)
      });
    }
  }
  return {
    root,
    identity,
    total_tracks: tracks.length,
    by_status: Object.fromEntries(Array.from(byStatus.entries()).sort()),
    active_tracks: activeTracks
  };
}
function teamStatus(root) {
  const tracks = listTracks(root);
  const byOwner = /* @__PURE__ */ new Map();
  const byStatus = /* @__PURE__ */ new Map();
  for (const track of tracks) {
    const owner = track.metadata.owner || "(unowned)";
    const status = track.metadata.status || "new";
    byOwner.set(owner, (byOwner.get(owner) || 0) + 1);
    byStatus.set(status, (byStatus.get(status) || 0) + 1);
  }
  return {
    root,
    identity: gitIdentity(root),
    total_tracks: tracks.length,
    by_owner: Object.fromEntries(Array.from(byOwner.entries()).sort()),
    by_status: Object.fromEntries(Array.from(byStatus.entries()).sort()),
    tracks: tracks.map((track) => ({
      track_id: track.track_id,
      status: track.metadata.status || "new",
      name: track.metadata.name || track.metadata.description || track.track_id,
      priority: track.metadata.priority || "medium",
      owner: track.metadata.owner || null,
      reviewer: track.metadata.reviewer || null,
      review_verdict: track.metadata.review ? track.metadata.review.verdict : null
    }))
  };
}
function asArray(value) {
  if (Array.isArray(value)) return value.filter(isRecord);
  if (isRecord(value) && Array.isArray(value.items)) return value.items.filter(isRecord).map(asJsonObject);
  if (isRecord(value) && Array.isArray(value.issues)) return value.issues.filter(isRecord).map(asJsonObject);
  if (isRecord(value) && Array.isArray(value.data)) return value.data.filter(isRecord).map(asJsonObject);
  return [];
}
function taskMarkerName(marker) {
  const names = {
    "~": "in_progress",
    "!": "blocked",
    "x": "completed",
    "-": "skipped",
    " ": "pending"
  };
  return names[marker] || "pending";
}
function metadataTrackSummary(track) {
  return {
    track_id: track.track_id,
    name: track.metadata.name || track.metadata.description || track.track_id,
    status: track.metadata.status || "new",
    priority: track.metadata.priority || "medium",
    owner: track.metadata.owner || null,
    reviewer: track.metadata.reviewer || null,
    tags: asStringArray(track.metadata.tags),
    review: track.metadata.review ? asJsonObject(track.metadata.review) : null
  };
}
function lspRuntimeSummary(root) {
  const configPath = import_node_path24.default.join(root, "cadre", "lsp.json");
  const config = readJson(configPath, null);
  const servers = isRecord(config) && Array.isArray(config.servers) ? config.servers.map((server) => asJsonObject(server)) : [];
  const entries = servers.map((server) => {
    const command = asOptionalString(server.command);
    return {
      id: asOptionalString(server.id) || command || "unknown",
      command: command || null,
      available: command ? commandExists(command, root) : false
    };
  });
  return {
    configured: Boolean(config),
    path: import_node_path24.default.relative(root, configPath),
    server_count: entries.length,
    available_count: entries.filter((entry) => entry.available === true).length,
    missing_count: entries.filter((entry) => entry.available !== true).length,
    missing: entries.filter((entry) => entry.available !== true).map((entry) => entry.id),
    daemon: {
      status_packet: "cadre_intel action lsp_daemon_status",
      shutdown_packet: "cadre_intel action lsp_daemon_shutdown",
      max_clients_default: 8,
      idle_eviction_ms_default: 6e5
    }
  };
}
function trackIndexPayload(root, tracks = listTracks(root)) {
  const counts = {
    new: 0,
    in_progress: 0,
    completed: 0,
    blocked: 0,
    skipped: 0
  };
  const entries = tracks.sort((a, b) => a.track_id.localeCompare(b.track_id)).map((track) => {
    const summary = metadataTrackSummary(track);
    const status = summary.status || "new";
    counts[status] = (counts[status] || 0) + 1;
    return {
      ...summary,
      metadata_path: import_node_path24.default.relative(root, track.metadata_path),
      spec_path: import_node_path24.default.relative(root, trackSpecJsonPath(track)),
      plan_path: import_node_path24.default.relative(root, trackPlanJsonPath(track))
    };
  });
  return {
    version: 1,
    schema: TRACKS_INDEX_SCHEMA,
    generated_at: utcNow(),
    counts,
    tracks: entries
  };
}
function teamBoard(root, args = {}) {
  const tracks = listTracks(root);
  const identity = gitIdentity(root);
  const scope = args.mine === true ? "mine" : "all";
  const byId = new Map(tracks.map((track) => [track.track_id, track]));
  const wip = [];
  const reviewQueue = [];
  const blockers = [];
  const handoffs = [];
  const outgoingHandoffs = [];
  for (const track of tracks) {
    const summary = metadataTrackSummary(track);
    const hold = holdInfo(track);
    if (summary.status === "in_progress" || summary.status === "blocked" || hold.owner || hold.lease_owner) {
      if (scope !== "mine" || summary.owner === identity || hold.owner === identity || hold.lease_owner === identity) {
        wip.push({ ...summary, hold });
      }
    }
    if (summary.review && (summary.review.verdict === "changes_requested" || Number(summary.review.blocking_count || 0) > 0)) {
      reviewQueue.push({ ...summary, review_state: "changes_requested" });
    } else if (summary.review && summary.review.verdict === "approved") {
      reviewQueue.push({ ...summary, review_state: "ready_to_ship" });
    }
    const deps = Array.isArray(track.metadata.depends_on) ? track.metadata.depends_on.filter((dep) => typeof dep === "string") : [];
    for (const dep of deps) {
      const depTrack = byId.get(dep);
      if (!depTrack || depTrack.metadata.status !== "completed") {
        blockers.push({
          kind: "track_dependency",
          track_id: track.track_id,
          blocked_on: dep,
          blocked_on_status: depTrack ? depTrack.metadata.status || "new" : "missing"
        });
      }
    }
    const plan = parsePlanFile(track.plan_path);
    for (const phase of plan.phases || []) {
      for (const task of phase.tasks || []) {
        if (task.marker === "!" || task.marker === "~") {
          blockers.push({
            kind: taskMarkerName(task.marker),
            track_id: track.track_id,
            phase: phase.phase_index,
            task: task.task_index,
            task_key: task.task_key,
            title: task.title
          });
        }
      }
    }
  }
  for (const message of readCadreMessages(root, "inbox", 200)) {
    if (asOptionalString(message.kind) !== "handoff") continue;
    const recipient = asOptionalString(message.to || message.recipient) || null;
    if (scope === "mine" && recipient && recipient !== identity) continue;
    handoffs.push({
      source: "message_inbox",
      message_id: asOptionalString(message.id) || null,
      track_id: asOptionalString(message.track_id) || null,
      from: asOptionalString(message.from) || null,
      to: recipient,
      subject: asOptionalString(message.subject) || null,
      status: asOptionalString(message.status) || "pending",
      recorded_at: asOptionalString(message.recorded_at) || null
    });
  }
  for (const message of readCadreMessages(root, "outbox", 200)) {
    if (asOptionalString(message.kind) !== "handoff") continue;
    const sender = asOptionalString(message.from) || null;
    if (scope === "mine" && sender && sender !== identity) continue;
    outgoingHandoffs.push({
      source: "message_outbox",
      message_id: asOptionalString(message.id) || null,
      track_id: asOptionalString(message.track_id) || null,
      from: sender,
      to: asOptionalString(message.to || message.recipient) || null,
      subject: asOptionalString(message.subject) || null,
      status: asOptionalString(message.status) || "pending",
      recorded_at: asOptionalString(message.recorded_at) || null
    });
  }
  const dedupReview = /* @__PURE__ */ new Map();
  for (const item of reviewQueue) {
    const key = `${item.track_id}:${item.review_state || ""}`;
    if (!dedupReview.has(key)) dedupReview.set(key, item);
  }
  return {
    ok: true,
    root,
    identity,
    scope,
    generated_at: utcNow(),
    summary: teamStatus(root),
    wip,
    incoming_handoffs: handoffs,
    outgoing_handoffs: outgoingHandoffs,
    review_queue: Array.from(dedupReview.values()),
    blockers,
    native_state: nativeStateSummary(root),
    lsp: lspRuntimeSummary(root)
  };
}
function gitSummary(root) {
  if (!fileExists(root)) return { ok: false, exists: false };
  const branch = runCommand("git", ["rev-parse", "--abbrev-ref", "HEAD"], { cwd: root });
  const head = runCommand("git", ["rev-parse", "--short", "HEAD"], { cwd: root });
  const status = runCommand("git", ["status", "--porcelain"], { cwd: root });
  return {
    ok: branch.ok || head.ok || status.ok,
    exists: true,
    branch: branch.ok ? branch.stdout.trim() : null,
    head: head.ok ? head.stdout.trim() : null,
    dirty_files: status.ok ? status.stdout.split(/\r?\n/).filter(Boolean).length : null,
    errors: [branch.stderr, head.stderr, status.stderr].filter(Boolean).join("\n").trim()
  };
}
function fleetStatus(root, args = {}) {
  const topology = loadTopology(root);
  const repos = [{
    name: ".",
    role: "control",
    path: ".",
    root,
    ...gitSummary(root)
  }];
  if (topology.polyrepo) {
    for (const raw of Array.isArray(topology.repos.repos) ? topology.repos.repos : []) {
      const repo = asJsonObject(raw);
      const name = asOptionalString(repo.name) || asOptionalString(repo.submodule_path) || "unknown";
      const rel = asOptionalString(repo.submodule_path) || "";
      const repoRoot = rel ? import_node_path24.default.resolve(root, rel) : root;
      repos.push({
        name,
        role: "product",
        path: rel,
        root: repoRoot,
        enabled: repo.enabled !== false,
        ...gitSummary(repoRoot)
      });
    }
  }
  return {
    ok: true,
    root,
    topology: topology.polyrepo ? "polyrepo" : "monorepo",
    repos,
    provider: providerMcpAvailability(root, args),
    lsp: lspRuntimeSummary(root),
    collisions: args.includeCollisions === false ? null : collisionScan(root)
  };
}
function availableWork(root) {
  const tracks = listTracks(root);
  const byId = new Map(tracks.map((track) => [track.track_id, track]));
  const available = [];
  const reclaimable = [];
  const now = Date.now();
  for (const track of tracks) {
    const status = track.metadata.status || "new";
    const owner = track.metadata.owner || null;
    const hold = holdInfo(track, now);
    const deps = Array.isArray(track.metadata.depends_on) ? track.metadata.depends_on.filter((dep) => typeof dep === "string") : [];
    const depsMet = deps.every((dep) => {
      const depTrack = byId.get(dep);
      return depTrack && depTrack.metadata.status === "completed";
    });
    if (status === "new" && !owner && depsMet) {
      available.push({
        track_id: track.track_id,
        priority: track.metadata.priority || "medium",
        description: track.metadata.description || track.metadata.name || track.track_id
      });
    }
    const heldBy = hold.lease_owner || hold.owner;
    const stale = hold.lease_stale || hold.state_stale;
    if (depsMet && heldBy && stale && ["new", "in_progress", "blocked"].includes(status)) {
      reclaimable.push({
        track_id: track.track_id,
        status,
        priority: track.metadata.priority || "medium",
        description: track.metadata.description || track.metadata.name || track.track_id,
        held_by: heldBy,
        lease_age_minutes: hold.lease_age_minutes,
        state_age_minutes: hold.state_age_minutes
      });
    }
  }
  return { root, available, reclaimable };
}
function activeTrackId(root, identity = gitIdentity(root)) {
  const tracks = listTracks(root);
  const topology = loadTopology(root);
  const active = tracks.filter((track) => (track.metadata.status || "new") === "in_progress");
  if (topology.config.sync_mode === "shared" && identity) {
    const mine = active.find((track) => track.metadata.owner === identity);
    if (mine) return mine.track_id;
  }
  return active[0]?.track_id || null;
}
function selectedTrackId(root, args = {}) {
  return args.trackId || args.track_id || activeTrackId(root);
}

// src/core/application/runtime/markdown-docs.ts
function normalizedText(text) {
  return text.replace(/\r\n/g, "\n").replace(/\n*$/, "\n");
}
function generatedMarker(source, schema, body) {
  return `<!-- cadre:generated from="${source}" schema="${schema}" hash="${textHash(body).slice(0, 16)}" -->`;
}
function withGeneratedMarker(source, schema, body) {
  const normalized = normalizedText(body);
  return `${generatedMarker(source, schema, normalized)}
${normalized}`;
}
function hasGeneratedMarker(text) {
  return /<!--\s*cadre:generated\b/.test(text);
}
function splitMarkdownSections(text) {
  const lines = normalizedText(text).split("\n");
  const firstHeadingIndex = lines.findIndex((line) => /^#\s+/.test(line));
  const title = firstHeadingIndex >= 0 ? lines[firstHeadingIndex]?.replace(/^#\s+/, "").trim() || "Untitled" : "Untitled";
  const bodyStart = firstHeadingIndex >= 0 ? firstHeadingIndex + 1 : 0;
  const bodyLines = [];
  const sections = [];
  let current = null;
  for (const line of lines.slice(bodyStart)) {
    const section = line.match(/^##+\s+(.+?)\s*$/);
    if (section?.[1]) {
      if (current) sections.push(current);
      current = { heading: section[1].trim(), body: "" };
      continue;
    }
    if (current) {
      current.body = `${asOptionalString(current.body) || ""}${line}
`;
    } else {
      bodyLines.push(line);
    }
  }
  if (current) sections.push(current);
  return {
    title,
    body: normalizedText(bodyLines.join("\n")).trim(),
    sections: sections.map((section) => ({
      heading: asOptionalString(section.heading) || "",
      body: normalizedText(asOptionalString(section.body) || "").trim()
    }))
  };
}
function markdownDocJson(kind, markdown, extras = {}) {
  const parsed = splitMarkdownSections(markdown);
  return {
    version: 1,
    schema: `cadre.${kind}.v1`,
    kind,
    title: parsed.title,
    summary: parsed.body,
    sections: parsed.sections,
    updated_at: utcNow(),
    ...extras
  };
}
function renderMarkdownDoc(value, fallbackTitle) {
  const title = asOptionalString(value.title) || fallbackTitle;
  const parts = [`# ${title}`, ""];
  const summary = asOptionalString(value.summary);
  if (summary) parts.push(summary, "");
  for (const rawSection of asArray(value.sections)) {
    const section = asJsonObject(rawSection);
    const heading = asOptionalString(section.heading);
    if (!heading) continue;
    parts.push(`## ${heading}`, "");
    const body = asOptionalString(section.body);
    if (body) parts.push(body, "");
  }
  return normalizedText(parts.join("\n"));
}
function markerForPlanStatus(status) {
  const normalized = String(status || "pending");
  if (normalized === "completed") return "x";
  if (normalized === "in_progress") return "~";
  if (normalized === "blocked") return "!";
  if (normalized === "skipped") return "-";
  return " ";
}

// src/core/application/runtime/plan-docs.ts
var MANUAL_VERIFICATION_TASK_TYPE = "user_manual_verification";
function trackPlanJsonPath(track) {
  return track.plan_json_path || import_node_path25.default.join(track.dir, "plan.json");
}
function trackSpecJsonPath(track) {
  return track.spec_json_path || import_node_path25.default.join(track.dir, "spec.json");
}
function trackLearningsJsonlPath(track) {
  return track.learnings_jsonl_path || import_node_path25.default.join(track.dir, "learnings.jsonl");
}
function trackHandoffJsonPath(track) {
  return track.handoff_json_path || import_node_path25.default.join(track.dir, "handoff.json");
}
function planJsonPathForPlanPath(file) {
  if (file.endsWith(".json")) return file;
  return import_node_path25.default.join(import_node_path25.default.dirname(file), "plan.json");
}
function manualVerificationScope(value) {
  const task = asJsonObject(value);
  const manual = asJsonObject(task.manual_verification);
  const annotations = asJsonObject(task.annotations);
  return asOptionalString(manual.scope) || asOptionalString(annotations["manual-verification-scope"]) || asOptionalString(annotations["manual-verification"]) || null;
}
function isManualVerificationTaskObject(value, scope) {
  const task = asJsonObject(value);
  const annotations = asJsonObject(task.annotations);
  const type = asOptionalString(task.task_type) || asOptionalString(annotations["task-type"]);
  const taskScope = manualVerificationScope(task);
  const key = asOptionalString(task.task_key || task.key) || "";
  const title = asOptionalString(task.title) || "";
  const matchesManual = type === MANUAL_VERIFICATION_TASK_TYPE || title.toLowerCase().includes("user manual verification") || key.endsWith("_manual_verification") || key === "track_manual_verification";
  if (!matchesManual) return false;
  if (!scope) return true;
  if (taskScope) return taskScope === scope;
  return scope === "track" ? key === "track_manual_verification" : key !== "track_manual_verification";
}
function manualVerificationCheck(id, heading, body, source) {
  return { id, heading, body, source };
}
function phaseManualVerificationChecks(phase, tasks) {
  const phaseIndex = Number(phase.phase_index || phase.index || 1);
  const title = asOptionalString(phase.title) || `Phase ${phaseIndex}`;
  const taskTitles = tasks.map((task) => asOptionalString(task.title)).filter(Boolean).map((value) => String(value).replace(/^Task\s+\d+:\s*/i, ""));
  const files = Array.from(new Set(tasks.flatMap((task) => asStringArray(task.files))));
  return [
    manualVerificationCheck(
      `phase${phaseIndex}-check-1`,
      "Exercise changed behavior",
      taskTitles.length > 0 ? `Manually exercise the behavior delivered by ${title}: ${taskTitles.join("; ")}.` : `Manually exercise the behavior delivered by ${title}.`,
      "phase"
    ),
    ...files.length > 0 ? [manualVerificationCheck(
      `phase${phaseIndex}-check-2`,
      "Inspect affected surfaces",
      `Review the user-visible behavior around affected files: ${files.slice(0, 12).join(", ")}.`,
      "phase"
    )] : []
  ];
}
function trackManualVerificationChecks(specJson) {
  const spec = asJsonObject(specJson);
  const groups = [
    { key: "functional_requirements", label: "Functional", source: "functional_requirements" },
    { key: "non_functional_requirements", label: "Non-functional", source: "non_functional_requirements" },
    { key: "acceptance_criteria", label: "Acceptance", source: "acceptance_criteria" },
    { key: "out_of_scope", label: "Out of scope guardrail", source: "out_of_scope" }
  ];
  const checks = [];
  for (const group of groups) {
    const items = specItemsFromRaw(spec[group.key]);
    items.forEach((item, index) => {
      const heading = asOptionalString(item.heading) || `${group.label} ${index + 1}`;
      const body = asOptionalString(item.body) || heading;
      checks.push(manualVerificationCheck(
        `track-${group.source.replace(/_/g, "-")}-${index + 1}`,
        `${group.label}: ${heading}`,
        body,
        group.source
      ));
    });
  }
  if (checks.length > 0) return checks;
  return [
    manualVerificationCheck(
      "track-check-1",
      "Verify delivered outcome",
      "Manually verify that the completed track delivers the intended behavior from the spec.",
      "track"
    )
  ];
}
function normalizePlanManualVerification(plan, specJson) {
  const rawPhases = asArray(plan.phases).map(asJsonObject);
  const existingTrackPhase = rawPhases.find((phase) => {
    const title = (asOptionalString(phase.title) || "").toLowerCase();
    const tasks = asArray(phase.tasks);
    return title.includes("user manual verification") || tasks.some((task) => isManualVerificationTaskObject(task, "track"));
  });
  const implementationPhases = rawPhases.filter((phase) => phase !== existingTrackPhase);
  const phaseManualKeys = [];
  const normalizedPhases = implementationPhases.map((rawPhase, phaseOffset) => {
    const phaseIndex = phaseOffset + 1;
    const tasks = asArray(rawPhase.tasks).map(asJsonObject);
    const existingManual = tasks.find((task) => isManualVerificationTaskObject(task, "phase"));
    const implementationTasks = tasks.filter((task) => !isManualVerificationTaskObject(task, "phase") && !isManualVerificationTaskObject(task, "track"));
    const normalizedTasks = implementationTasks.map((task, taskOffset) => ({
      ...task,
      task_index: taskOffset + 1,
      task_key: asOptionalString(task.task_key) || `phase${phaseIndex}_task${taskOffset + 1}`,
      depends_on: asStringArray(task.depends_on || task.depends),
      files: asStringArray(task.files),
      commit_shas: asStringArray(task.commit_shas),
      repo_shas: asJsonObject(task.repo_shas)
    }));
    const manualTaskKey = `phase${phaseIndex}_manual_verification`;
    phaseManualKeys.push(manualTaskKey);
    const manualTask = {
      ...existingManual || {},
      task_index: normalizedTasks.length + 1,
      task_key: manualTaskKey,
      title: "User Manual Verification",
      status: asOptionalString(asJsonObject(existingManual).status) || "pending",
      task_type: MANUAL_VERIFICATION_TASK_TYPE,
      files: [],
      depends_on: normalizedTasks.map((task) => asOptionalString(task.task_key)).filter((value) => Boolean(value)),
      repo: null,
      annotations: {
        ...asJsonObject(asJsonObject(existingManual).annotations),
        "task-type": MANUAL_VERIFICATION_TASK_TYPE,
        "manual-verification-scope": "phase"
      },
      commit_shas: asStringArray(asJsonObject(existingManual).commit_shas),
      repo_shas: asJsonObject(asJsonObject(existingManual).repo_shas),
      manual_verification: {
        ...asJsonObject(asJsonObject(existingManual).manual_verification),
        scope: "phase",
        suggested_checks: phaseManualVerificationChecks({ ...rawPhase, phase_index: phaseIndex }, normalizedTasks)
      },
      completion_evidence: asJsonObject(asJsonObject(existingManual).completion_evidence)
    };
    return {
      ...rawPhase,
      phase_index: phaseIndex,
      title: asOptionalString(rawPhase.title) || `Phase ${phaseIndex}`,
      execution_mode: asOptionalString(rawPhase.execution_mode) || asOptionalString(asJsonObject(rawPhase.annotations).execution) || "sequential",
      depends_on: asStringArray(rawPhase.depends_on),
      annotations: asJsonObject(rawPhase.annotations),
      tasks: [...normalizedTasks, manualTask]
    };
  });
  const trackPhaseIndex = normalizedPhases.length + 1;
  const trackPhaseDepends = normalizedPhases.map((phase) => `phase${phase.phase_index}`);
  const existingTrackTask = asArray(asJsonObject(existingTrackPhase).tasks).map(asJsonObject).find((task) => isManualVerificationTaskObject(task, "track"));
  const trackManualTask = {
    ...existingTrackTask || {},
    task_index: 1,
    task_key: "track_manual_verification",
    title: "Track-Level User Manual Verification",
    status: asOptionalString(asJsonObject(existingTrackTask).status) || "pending",
    task_type: MANUAL_VERIFICATION_TASK_TYPE,
    files: [],
    depends_on: phaseManualKeys,
    repo: null,
    annotations: {
      ...asJsonObject(asJsonObject(existingTrackTask).annotations),
      "task-type": MANUAL_VERIFICATION_TASK_TYPE,
      "manual-verification-scope": "track"
    },
    commit_shas: asStringArray(asJsonObject(existingTrackTask).commit_shas),
    repo_shas: asJsonObject(asJsonObject(existingTrackTask).repo_shas),
    manual_verification: {
      ...asJsonObject(asJsonObject(existingTrackTask).manual_verification),
      scope: "track",
      suggested_checks: trackManualVerificationChecks(specJson)
    },
    completion_evidence: asJsonObject(asJsonObject(existingTrackTask).completion_evidence)
  };
  return {
    ...plan,
    phases: [
      ...normalizedPhases,
      {
        ...existingTrackPhase || {},
        phase_index: trackPhaseIndex,
        title: `Phase ${trackPhaseIndex}: User Manual Verification`,
        execution_mode: "sequential",
        depends_on: trackPhaseDepends,
        annotations: {
          ...asJsonObject(asJsonObject(existingTrackPhase).annotations),
          execution: "sequential",
          depends: trackPhaseDepends.join(",")
        },
        tasks: [trackManualTask]
      }
    ]
  };
}
function planJsonToParsedPlan(raw) {
  const phases = asArray(raw.phases).map((rawPhase, phaseOffset) => {
    const phase = asJsonObject(rawPhase);
    const phaseIndex = Number(phase.phase_index || phase.index || phaseOffset + 1);
    const phaseDepends = asStringArray(phase.depends_on);
    const hasExplicitPhaseDepends = Object.prototype.hasOwnProperty.call(phase, "depends_on");
    const annotations = {
      ...asJsonObject(phase.annotations),
      ...asOptionalString(phase.execution_mode) ? { execution: asOptionalString(phase.execution_mode) } : {},
      ...hasExplicitPhaseDepends ? { depends: phaseDepends.join(",") } : {}
    };
    const tasks = asArray(phase.tasks).map((rawTask, taskOffset) => {
      const task = asJsonObject(rawTask);
      const taskIndex = Number(task.task_index || task.index || taskOffset + 1);
      const taskKey = asOptionalString(task.task_key) || `phase${phaseIndex}_task${taskIndex}`;
      const files = asStringArray(task.files);
      const depends = asStringArray(task.depends_on || task.depends);
      const labels = asStringArray(task.labels);
      const taskAnnotations = {
        ...asJsonObject(task.annotations),
        ...files.length > 0 ? { files: files.join(", ") } : {},
        ...depends.length > 0 ? { depends: depends.join(",") } : {},
        ...labels.length > 0 ? { labels: labels.join(",") } : {},
        ...asOptionalString(task.repo) ? { repo: asOptionalString(task.repo) } : {}
      };
      return {
        task_index: taskIndex,
        task_key: taskKey,
        title: asOptionalString(task.title) || `Task ${taskIndex}`,
        marker: markerForPlanStatus(task.status),
        annotations: taskAnnotations,
        files,
        depends,
        labels,
        repo: asOptionalString(task.repo) || null,
        line: Number(task.line || phaseIndex * 100 + taskIndex),
        phase_index: phaseIndex,
        commit_shas: asStringArray(task.commit_shas),
        repo_shas: asJsonObject(task.repo_shas),
        task_type: asOptionalString(task.task_type || taskAnnotations["task-type"]) || null,
        manual_verification: isRecord(task.manual_verification) ? asJsonObject(task.manual_verification) : asOptionalString(taskAnnotations["manual-verification-scope"]) ? { scope: asOptionalString(taskAnnotations["manual-verification-scope"]) } : null,
        completion_evidence: isRecord(task.completion_evidence) ? asJsonObject(task.completion_evidence) : null
      };
    });
    return {
      phase_index: phaseIndex,
      title: asOptionalString(phase.title) || `Phase ${phaseIndex}`,
      annotations,
      tasks,
      line: Number(phase.line || phaseIndex * 100)
    };
  });
  return { ok: true, phases, tasks: phases.flatMap((phase) => phase.tasks), warnings: [], errors: [] };
}
function renderPlanMarkdown(raw) {
  const trackId = asOptionalString(raw.track_id) || "track";
  const parts = [`# Plan: ${trackId}`, ""];
  const parsed = planJsonToParsedPlan(raw);
  for (const phase of parsed.phases) {
    parts.push(`## Phase ${phase.phase_index}: ${phase.title.replace(/^Phase\s+\d+:\s*/i, "")}`);
    if (phase.annotations.execution) parts.push(`<!-- execution: ${phase.annotations.execution} -->`);
    if (Object.prototype.hasOwnProperty.call(phase.annotations, "depends")) {
      parts.push(`<!-- depends: ${phase.annotations.depends || ""} -->`);
    }
    parts.push("");
    for (const task of phase.tasks) {
      const commit = task.commit_shas && task.commit_shas.length > 0 ? ` (${task.commit_shas[task.commit_shas.length - 1]})` : "";
      parts.push(`- [${task.marker}] Task ${task.task_index}: ${task.title.replace(/^Task\s+\d+:\s*/i, "")}${commit}`);
      if (task.repo) parts.push(`  <!-- repo: ${task.repo} -->`);
      if (task.files.length > 0) parts.push(`  <!-- files: ${task.files.join(", ")} -->`);
      if (task.depends.length > 0) parts.push(`  <!-- depends: ${task.depends.join(", ")} -->`);
      if (task.labels && task.labels.length > 0) parts.push(`  <!-- labels: ${task.labels.join(", ")} -->`);
      if (task.commit_shas && task.commit_shas.length > 0) parts.push(`  <!-- commits: ${task.commit_shas.join(", ")} -->`);
      if (task.task_type) parts.push(`  <!-- task-type: ${task.task_type} -->`);
      if (task.manual_verification) {
        const manual = asJsonObject(task.manual_verification);
        const scope = asOptionalString(manual.scope);
        if (scope) parts.push(`  <!-- manual-verification-scope: ${scope} -->`);
        const checks = asArray(manual.suggested_checks);
        if (checks.length > 0) parts.push(`  <!-- manual-verification-checks: ${checks.length} suggested -->`);
      }
      parts.push("");
    }
  }
  return normalizedText(parts.join("\n"));
}

// src/core/application/runtime/artifact-catalog.ts
function safeReadDir(dir) {
  try {
    return import_node_fs14.default.readdirSync(dir).sort();
  } catch {
    return [];
  }
}
function artifactSchema(artifact) {
  const id = String(artifact || "catalog").toLowerCase();
  const objectSchema = (required, properties) => ({
    type: "object",
    required,
    additionalProperties: true,
    properties
  });
  const specListItemSchema = objectSchema(["heading"], {
    heading: { type: "string" },
    body: { type: "string" }
  });
  const schemas = {
    spec: objectSchema(["track_id", "title"], {
      track_id: { type: "string" },
      title: { type: "string" },
      description: { type: "string" },
      functional_requirements: { type: "array", items: specListItemSchema },
      non_functional_requirements: { type: "array", items: specListItemSchema },
      acceptance_criteria: { type: "array", items: specListItemSchema },
      out_of_scope: { type: "array", items: specListItemSchema }
    }),
    plan: objectSchema(["track_id", "phases"], {
      track_id: { type: "string" },
      phases: { type: "array" },
      tasks: { type: "array" },
      execution_mode: { type: "string" },
      dependencies: { type: "array" },
      files: { type: "array" },
      repo: { type: "string" },
      status: { type: "string" },
      commit_shas: { type: "array", items: { type: "string" } },
      test_expectations: { type: "array" },
      completion_evidence: { type: "object" }
    }),
    styleguide: objectSchema(["id", "title", "rules"], {
      id: { type: "string" },
      title: { type: "string" },
      languages: { type: "array", items: { type: "string" } },
      frameworks: { type: "array", items: { type: "string" } },
      file_patterns: { type: "array", items: { type: "string" } },
      applies_to: { type: "array", items: { type: "string" } },
      rules: { type: "array" },
      examples: { type: "array" },
      anti_examples: { type: "array" },
      severity: { type: "string" },
      source: { type: "string" },
      version: { type: "number" }
    }),
    metadata: objectSchema(["track_id"], {
      track_id: { type: "string" },
      status: { type: "string" },
      owner: { type: "string" },
      reviewer: { type: "string" },
      review: { type: "object" },
      worktree_path: { type: "string" }
    }),
    release: objectSchema(["version", "completed_tracks"], {
      version: { type: "string" },
      generated_at: { type: "string" },
      completed_tracks: { type: "array" },
      notes: { type: "array" }
    }),
    journal: objectSchema(["track_id", "events"], {
      track_id: { type: "string" },
      events: { type: "array" },
      event: { type: "string" },
      recorded_at: { type: "string" }
    }),
    evidence: objectSchema(["entries"], {
      entries: { type: "array" },
      provider: { type: "string" },
      findings: { type: "array" },
      blocking_count: { type: "number" },
      recorded_at: { type: "string" }
    }),
    project_doc: objectSchema(["title", "sections"], {
      title: { type: "string" },
      summary: { type: "string" },
      sections: { type: "array" }
    }),
    artifact_sync_result: objectSchema(["ok", "dry_run", "artifacts"], {
      ok: { type: "boolean" },
      dry_run: { type: "boolean" },
      artifacts: { type: "array" },
      review_bundle: { type: "object" },
      written: { type: "array", items: { type: "string" } },
      skipped: { type: "array", items: { type: "string" } },
      warnings: { type: "array", items: { type: "string" } },
      errors: { type: "array", items: { type: "string" } }
    })
  };
  return {
    ok: true,
    artifact: id,
    schema: schemas[id] || (["review-evidence", "review_evidence", "provider-evidence", "provider_evidence"].includes(id) ? schemas.evidence : void 0) || (["completion-journal", "completion_journal"].includes(id) ? schemas.journal : void 0) || schemas.project_doc,
    dialect: "https://json-schema.org/draft/2020-12/schema"
  };
}
function artifactDefinitions(root, args = {}) {
  const defs = [
    { id: "product", title: "Product context", canonical: "cadre/product.json", projection: "cadre/product.md", schema: "cadre.product.v1", scope: "project", sourceFormat: "json", projectionFormat: "markdown" },
    { id: "product-guidelines", title: "Product guidelines", canonical: "cadre/product_guidelines.json", projection: "cadre/product_guidelines.md", schema: "cadre.product_guidelines.v1", scope: "project", sourceFormat: "json", projectionFormat: "markdown" },
    { id: "workflow", title: "Workflow policy", canonical: "cadre/workflow.json", projection: "cadre/workflow.md", schema: "cadre.workflow.v1", scope: "project", sourceFormat: "json", projectionFormat: "markdown" },
    { id: "patterns", title: "Project patterns", canonical: "cadre/patterns.jsonl", projection: "cadre/patterns.md", schema: "cadre.patterns.v1", scope: "project", sourceFormat: "jsonl", projectionFormat: "markdown" },
    { id: "tracks-index", title: "Track index", canonical: "cadre/tracks.json", schema: TRACKS_INDEX_SCHEMA, scope: "project", sourceFormat: "json", projectionFormat: "none" },
    { id: "tech-stack", title: "Tech stack", canonical: "cadre/tech-stack.json", schema: "cadre.tech_stack.v1", scope: "project", sourceFormat: "json", projectionFormat: "none" },
    { id: "config", title: "Cadre config", canonical: "cadre/config.json", schema: "cadre.config.v1", scope: "project", sourceFormat: "json", projectionFormat: "none" },
    { id: "setup-state", title: "Setup state", canonical: "cadre/setup_state.json", schema: "cadre.setup_state.v1", scope: "project", sourceFormat: "json", projectionFormat: "none" }
  ];
  if (fileExists(import_node_path26.default.join(root, "cadre", "repos.json")) || fileExists(import_node_path26.default.join(root, "cadre", "repos.md"))) {
    defs.push({ id: "repos", title: "Repository topology", canonical: "cadre/repos.json", projection: "cadre/repos.md", schema: "cadre.repos.v1", scope: "project", sourceFormat: "json", projectionFormat: "markdown" });
  }
  if (fileExists(import_node_path26.default.join(root, "cadre", "lsp.json"))) {
    defs.push({ id: "lsp-config", title: "LSP config", canonical: "cadre/lsp.json", schema: "cadre.lsp.v1", scope: "project", sourceFormat: "json", projectionFormat: "none" });
  }
  const styleJsonDir = import_node_path26.default.join(root, "cadre", "styleguides");
  const styleMdDir = import_node_path26.default.join(root, "cadre", "code_styleguides");
  const styleIds = /* @__PURE__ */ new Set();
  for (const file of safeReadDir(styleJsonDir)) {
    if (file.endsWith(".json") && file !== "index.json") styleIds.add(import_node_path26.default.basename(file, ".json"));
  }
  for (const file of safeReadDir(styleMdDir)) {
    if (file.endsWith(".md") && file !== "README.md") styleIds.add(import_node_path26.default.basename(file, ".md"));
  }
  if (styleIds.size > 0) {
    defs.push({ id: "styleguides-index", title: "Style guide catalog", canonical: "cadre/styleguides/index.json", projection: "cadre/code_styleguides/README.md", schema: "cadre.styleguide_index.v1", scope: "styleguide", sourceFormat: "json", projectionFormat: "markdown" });
  }
  for (const id of Array.from(styleIds).sort()) {
    defs.push({ id: `styleguide:${id}`, title: `Style guide: ${id}`, canonical: `cadre/styleguides/${id}.json`, projection: `cadre/code_styleguides/${id}.md`, schema: "cadre.styleguide.v1", scope: "styleguide", sourceFormat: "json", projectionFormat: "markdown" });
  }
  for (const track of listTracks(root)) {
    defs.push(
      { id: `track:${track.track_id}:metadata`, title: `Metadata: ${track.track_id}`, canonical: import_node_path26.default.relative(root, track.metadata_path), schema: "cadre.metadata.v1", scope: "track", sourceFormat: "json", projectionFormat: "none" },
      { id: `track:${track.track_id}:spec`, title: `Spec: ${track.track_id}`, canonical: import_node_path26.default.relative(root, trackSpecJsonPath(track)), projection: import_node_path26.default.relative(root, track.spec_path), schema: "cadre.spec.v1", scope: "track", sourceFormat: "json", projectionFormat: "markdown" },
      { id: `track:${track.track_id}:plan`, title: `Plan: ${track.track_id}`, canonical: import_node_path26.default.relative(root, trackPlanJsonPath(track)), projection: import_node_path26.default.relative(root, track.plan_path), schema: "cadre.plan.v1", scope: "track", sourceFormat: "json", projectionFormat: "markdown" },
      { id: `track:${track.track_id}:learnings`, title: `Learnings: ${track.track_id}`, canonical: import_node_path26.default.relative(root, trackLearningsJsonlPath(track)), projection: import_node_path26.default.relative(root, track.learnings_path || import_node_path26.default.join(track.dir, "learnings.md")), schema: "cadre.learnings.v1", scope: "track", sourceFormat: "jsonl", projectionFormat: "markdown" },
      { id: `track:${track.track_id}:handoff`, title: `Handoff: ${track.track_id}`, canonical: import_node_path26.default.relative(root, trackHandoffJsonPath(track)), projection: import_node_path26.default.relative(root, import_node_path26.default.join(track.dir, "HANDOFF.md")), schema: "cadre.handoff.v1", scope: "track", sourceFormat: "json", projectionFormat: "markdown" }
    );
    const reviewEvidenceJsonl = import_node_path26.default.join(track.dir, "review-evidence.jsonl");
    const reviewEvidenceJson = import_node_path26.default.join(track.dir, "review-evidence.json");
    const completionJournal = import_node_path26.default.join(track.dir, "completion_journal.jsonl");
    const parallelState = import_node_path26.default.join(track.dir, "parallel_state.json");
    const implementState = import_node_path26.default.join(track.dir, "implement_state.json");
    if (fileExists(reviewEvidenceJsonl)) {
      defs.push({ id: `track:${track.track_id}:review-evidence`, title: `Review evidence: ${track.track_id}`, canonical: import_node_path26.default.relative(root, reviewEvidenceJsonl), schema: "cadre.review_evidence.v1", scope: "track", sourceFormat: "jsonl", projectionFormat: "none" });
    }
    if (fileExists(reviewEvidenceJson)) {
      defs.push({ id: `track:${track.track_id}:review-evidence-summary`, title: `Review evidence summary: ${track.track_id}`, canonical: import_node_path26.default.relative(root, reviewEvidenceJson), schema: "cadre.review_evidence_summary.v1", scope: "track", sourceFormat: "json", projectionFormat: "none" });
    }
    if (fileExists(completionJournal)) {
      defs.push({ id: `track:${track.track_id}:completion-journal`, title: `Completion journal: ${track.track_id}`, canonical: import_node_path26.default.relative(root, completionJournal), schema: "cadre.completion_journal.v1", scope: "track", sourceFormat: "jsonl", projectionFormat: "none" });
    }
    if (fileExists(parallelState)) {
      defs.push({ id: `track:${track.track_id}:parallel-state`, title: `Parallel state: ${track.track_id}`, canonical: import_node_path26.default.relative(root, parallelState), schema: "cadre.parallel_state.v1", scope: "track", sourceFormat: "json", projectionFormat: "none" });
    }
    if (fileExists(implementState)) {
      defs.push({ id: `track:${track.track_id}:implement-state`, title: `Implementation state: ${track.track_id}`, canonical: import_node_path26.default.relative(root, implementState), schema: "cadre.implement_state.v1", scope: "track", sourceFormat: "json", projectionFormat: "none" });
    }
  }
  const releasesDir = import_node_path26.default.join(root, "cadre", "releases");
  for (const file of safeReadDir(releasesDir)) {
    if (!file.endsWith(".json")) continue;
    const version = import_node_path26.default.basename(file, ".json");
    defs.push({ id: `release:${version}`, title: `Release - ${version}`, canonical: `cadre/releases/${file}`, projection: `cadre/releases/${version}.md`, schema: "cadre.release.v1", scope: "release", sourceFormat: "json", projectionFormat: "markdown" });
  }
  const jobsDir = import_node_path26.default.join(root, "cadre", "jobs");
  for (const file of safeReadDir(jobsDir)) {
    if (!file.endsWith(".json")) continue;
    const jobId = import_node_path26.default.basename(file, ".json");
    defs.push({ id: `job:${jobId}`, title: `Job ${jobId}`, canonical: `cadre/jobs/${file}`, schema: "cadre.job.v1", scope: "external", sourceFormat: "json", projectionFormat: "none" });
  }
  if (args.includeArchive === true || args.include_archive === true) {
    const archiveDir = import_node_path26.default.join(root, "cadre", "archive");
    for (const trackId of safeReadDir(archiveDir)) {
      const dir = import_node_path26.default.join(archiveDir, trackId);
      if (!fileExists(import_node_path26.default.join(dir, "metadata.json"))) continue;
      defs.push(
        { id: `archive:${trackId}:spec`, title: `Archived spec: ${trackId}`, canonical: `cadre/archive/${trackId}/spec.json`, projection: `cadre/archive/${trackId}/spec.md`, schema: "cadre.spec.v1", scope: "track", sourceFormat: "json", projectionFormat: "markdown" },
        { id: `archive:${trackId}:plan`, title: `Archived plan: ${trackId}`, canonical: `cadre/archive/${trackId}/plan.json`, projection: `cadre/archive/${trackId}/plan.md`, schema: "cadre.plan.v1", scope: "track", sourceFormat: "json", projectionFormat: "markdown" }
      );
    }
  }
  return defs;
}
function artifactMatches(def, args = {}) {
  const artifact = asOptionalString(args.artifact || args.id);
  if (artifact && def.id !== artifact && !def.id.endsWith(`:${artifact}`)) return false;
  const scope = asOptionalString(args.scope || args.view || "all") || "all";
  if (scope === "all") return true;
  if (scope === "project") return def.scope === "project";
  if (scope === "tracks") return def.scope === "track" || def.id === "tracks-index";
  if (scope === "styleguides" || scope === "styleguide") return def.scope === "styleguide";
  if (scope === "release") return def.scope === "release";
  if (scope === "spec") return def.id.endsWith(":spec");
  if (scope === "plan") return def.id.endsWith(":plan");
  if (scope.startsWith("track:")) return def.id.startsWith(`${scope}:`) || def.id === "tracks-index";
  return def.id === scope || def.scope === scope;
}

// src/core/application/runtime/artifact-actions.ts
function artifactCatalog(root, args = {}) {
  const artifacts = artifactDefinitions(root, args).filter((def) => artifactMatches(def, args)).map((def) => ({
    ...def,
    canonical_exists: def.canonical === "cadre/tracks" ? fileExists(import_node_path27.default.join(root, "cadre", "tracks")) : fileExists(import_node_path27.default.join(root, def.canonical)),
    projection_exists: def.projection ? fileExists(import_node_path27.default.join(root, def.projection)) : false
  }));
  return { ok: true, root, artifacts };
}
function renderJsonCodeblock(title, value) {
  return normalizedText(`# ${title}

\`\`\`json
${JSON.stringify(value, null, 2)}
\`\`\`
`);
}
function readJsonl(file) {
  if (!fileExists(file)) return [];
  return import_node_fs15.default.readFileSync(file, "utf8").split(/\r?\n/).map((line) => line.trim()).filter(Boolean).map((line) => {
    try {
      return asJsonObject(JSON.parse(line));
    } catch {
      return { text: line };
    }
  });
}
function renderJsonlMarkdown(title, entries) {
  if (entries.length === 1) {
    const text = asOptionalString(entries[0]?.text || entries[0]?.summary || entries[0]?.body);
    if (text && /^#\s+/m.test(text.trimStart())) return normalizedText(text);
  }
  const parts = [`# ${title}`, ""];
  for (const entry of entries) {
    const heading = asOptionalString(entry.title || entry.kind || entry.id);
    if (heading) parts.push(`## ${heading}`, "");
    const text = asOptionalString(entry.text || entry.summary || entry.body);
    if (text) parts.push(text, "");
  }
  return normalizedText(parts.join("\n"));
}
function renderArtifact(root, def, args = {}) {
  const canonicalPath = import_node_path27.default.join(root, def.canonical);
  const projectionPath = def.projection ? import_node_path27.default.join(root, def.projection) : void 0;
  let raw = null;
  let body = "";
  let missingCanonical = false;
  if (def.sourceFormat === "jsonl") {
    const entries = readJsonl(canonicalPath);
    if (entries.length === 0) missingCanonical = true;
    const title = def.title;
    body = renderJsonlMarkdown(title, entries);
  } else if (fileExists(canonicalPath)) {
    raw = readJson(canonicalPath, null);
    if (!raw) return { ok: false, artifact_id: def.id, canonical_path: def.canonical, projection_path: def.projection, error: "Invalid canonical JSON" };
    if (def.schema === "cadre.plan.v1") body = renderPlanMarkdown(raw);
    else if (def.schema === "cadre.spec.v1") body = renderSpecMarkdown(raw);
    else if (def.schema === "cadre.styleguide.v1") body = renderStyleGuideMarkdown(raw);
    else if (def.schema === "cadre.styleguide_index.v1") body = renderJsonCodeblock(def.title, raw);
    else if (def.schema === "cadre.release.v1") body = releaseMarkdownFromMetadata(raw);
    else if (["cadre.product.v1", "cadre.product_guidelines.v1", "cadre.workflow.v1", "cadre.handoff.v1"].includes(def.schema)) body = renderMarkdownDoc(raw, def.title);
    else body = renderJsonCodeblock(def.title, raw);
  } else {
    missingCanonical = true;
  }
  if (!body) return { ok: false, artifact_id: def.id, canonical_path: def.canonical, projection_path: def.projection, missing_canonical: missingCanonical };
  const content = withGeneratedMarker(def.canonical, def.schema, body);
  const existing = projectionPath && fileExists(projectionPath) ? import_node_fs15.default.readFileSync(projectionPath, "utf8") : "";
  return {
    ok: true,
    artifact_id: def.id,
    canonical_path: def.canonical,
    projection_path: def.projection,
    content,
    changed: projectionPath ? normalizedText(existing) !== normalizedText(content) : false,
    missing_canonical: missingCanonical,
    legacy_import_available: false
  };
}
function releaseMarkdownFromMetadata(metadata) {
  const version = asOptionalString(metadata.version) || "release";
  const parts = [`# Release - ${version}`, "", `Generated: ${asOptionalString(metadata.generated_at) || utcNow()}`, "", "## Completed Tracks", ""];
  for (const rawTrack of asArray(metadata.completed_tracks)) {
    const track = asJsonObject(rawTrack);
    parts.push(`- ${asOptionalString(track.track_id) || "track"}: ${asOptionalString(track.name || track.status) || ""}`.trim());
  }
  parts.push("");
  return normalizedText(parts.join("\n"));
}
function artifactRender(root, args = {}) {
  const artifact = asOptionalString(args.artifact || args.id);
  if (!artifact) return { ok: false, error: "artifact is required" };
  const def = artifactDefinitions(root, args).find((item) => item.id === artifact || item.id.endsWith(`:${artifact}`));
  if (!def) return { ok: false, error: `Unknown artifact: ${artifact}` };
  return renderArtifact(root, def, args);
}
function artifactValidate(root, args = {}) {
  const artifacts = artifactDefinitions(root, args).filter((def) => artifactMatches(def, args));
  const results = artifacts.map((def) => {
    const file = import_node_path27.default.join(root, def.canonical);
    if (!fileExists(file)) return { artifact_id: def.id, ok: false, missing: true, canonical_path: def.canonical };
    if (def.sourceFormat === "jsonl") return { artifact_id: def.id, ok: readJsonl(file).length >= 0, canonical_path: def.canonical };
    const value = readJson(file, null);
    return { artifact_id: def.id, ok: Boolean(value), canonical_path: def.canonical };
  });
  return { ok: results.every((result) => result.ok !== false), root, results };
}
function artifactDiff(root, args = {}) {
  const artifacts = artifactDefinitions(root, args).filter((def) => artifactMatches(def, args));
  const diffs = artifacts.map((def) => {
    const rendered = renderArtifact(root, def, args);
    return {
      artifact_id: def.id,
      projection_path: def.projection,
      changed: rendered.changed === true,
      missing_canonical: rendered.missing_canonical === true,
      legacy_import_available: rendered.legacy_import_available === true
    };
  });
  return { ok: true, root, diffs, changed: diffs.filter((diff) => diff.changed).length };
}
function artifactSync(root, args = {}) {
  const execute = args.execute === true;
  const force = args.force === true;
  if (args.importLegacy !== void 0 || args.import_legacy !== void 0) {
    return {
      ok: false,
      error: "Legacy Markdown import is not supported. Create canonical JSON/JSONL artifacts and rerun artifact sync.",
      unsupported_fields: ["importLegacy", "import_legacy"].filter((field) => args[field] !== void 0)
    };
  }
  const defs = artifactDefinitions(root, args).filter((def) => artifactMatches(def, args));
  const reviewFiles = [];
  const artifacts = [];
  const written = [];
  const skipped = [];
  const warnings = [];
  const errors = [];
  for (const def of defs) {
    const rendered = renderArtifact(root, def, args);
    artifacts.push({
      artifact_id: def.id,
      canonical_path: def.canonical,
      projection_path: def.projection || null,
      changed: rendered.changed === true,
      missing_canonical: rendered.missing_canonical === true,
      legacy_import_available: rendered.legacy_import_available === true
    });
    if (rendered.ok === false || !rendered.content || !def.projection) {
      if (rendered.missing_canonical) warnings.push(`Missing canonical for ${def.id}`);
      continue;
    }
    reviewFiles.push(textReviewFile(def.projection, def.title, def.canonical, rendered.content));
    if (!execute) continue;
    if (!humanReviewConfirmed(args)) continue;
    const projectionFile = import_node_path27.default.join(root, def.projection);
    const existing = fileExists(projectionFile) ? import_node_fs15.default.readFileSync(projectionFile, "utf8") : "";
    if (existing && !hasGeneratedMarker(existing) && !force) {
      skipped.push(def.projection);
      warnings.push(`Skipped unmarked projection ${def.projection}; pass force:true or import first.`);
      continue;
    }
    ensureParent(projectionFile);
    import_node_fs15.default.writeFileSync(projectionFile, rendered.content);
    written.push(def.projection);
  }
  const reviewBundle = workflowReviewBundle(root, "artifacts", args, reviewFiles, {
    scope: args.scope || "all",
    artifact: args.artifact || null
  });
  const humanReview = humanReviewState("artifacts", args, reviewArtifactsFromFiles(reviewFiles), reviewBundle);
  if (execute && !humanReviewConfirmed(args)) {
    return {
      ok: false,
      dry_run: true,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      artifacts,
      human_review: humanReview,
      review_bundle: reviewBundle,
      warnings,
      errors: ["Human confirmation is required before syncing artifacts"],
      error: "Human confirmation is required before syncing artifacts"
    };
  }
  return {
    ok: errors.length === 0,
    dry_run: !execute,
    phase_state: execute ? "executed" : "dry_run",
    artifacts,
    review_bundle: reviewBundle,
    human_review: humanReview,
    written,
    skipped,
    warnings,
    errors
  };
}
function artifactImport(root, args = {}) {
  return {
    ok: false,
    error: "Legacy Markdown import is not supported. Create canonical JSON/JSONL artifacts and rerun artifact sync.",
    action: asOptionalString(args.action) || "import"
  };
}
function artifactPacket(root, args = {}) {
  const markdownError = markdownPayloadError(args);
  if (markdownError) return markdownError;
  const action = asOptionalString(args.action) || "catalog";
  if (action === "catalog") return artifactCatalog(root, args);
  if (action === "schema") return artifactSchema(args.artifact || args.id || args.scope);
  if (action === "validate") return artifactValidate(root, args);
  if (action === "render") return artifactRender(root, args);
  if (action === "diff") return artifactDiff(root, args);
  if (action === "sync") return artifactSync(root, args);
  if (action === "import") return artifactImport(root, args);
  return { ok: false, error: `Unknown artifact action: ${action}` };
}

// src/core/application/runtime/setup-infrastructure.ts
var import_node_fs16 = __toESM(require("node:fs"));
var import_node_path28 = __toESM(require("node:path"));
function configuredCiProvider(root, args = {}) {
  const raw = asOptionalString(args.ciProvider || args.ci_provider) || asOptionalString(args.providerMode || args.provider_mode || args.provider) || asOptionalString(loadTopology(root).config.provider_mode);
  const provider = normalizeProviderMode(raw);
  return provider === "github" || provider === "gitlab" ? provider : null;
}
function setupGitattributes(root) {
  const file = import_node_path28.default.join(root, ".gitattributes");
  const required = [
    "cadre/tracks/**/parallel_state.json merge=ours"
  ];
  const existing = fileExists(file) ? import_node_fs16.default.readFileSync(file, "utf8") : "";
  const lines = existing.split(/\r?\n/).filter(Boolean);
  let changed = false;
  for (const line of required) {
    if (!lines.includes(line)) {
      lines.push(line);
      changed = true;
    }
  }
  if (changed || !fileExists(file)) {
    import_node_fs16.default.writeFileSync(file, `${lines.join("\n")}
`);
  }
  const mergeDriver = runCommand("git", ["config", "merge.ours.driver", "true"], { cwd: root });
  return {
    ok: mergeDriver.ok,
    path: import_node_path28.default.relative(root, file),
    changed,
    merge_driver: mergeDriver
  };
}
function setupCiTemplates(root, provider, args = {}) {
  if (!provider) return { ok: true, skipped: true, reason: "No hosted provider selected" };
  if (args.writeCi === false || args.write_ci === false) {
    return { ok: true, skipped: true, reason: "writeCi=false" };
  }
  const topology = asOptionalString(args.topology)?.toLowerCase();
  const polyrepo = topology === "polyrepo" || asJsonObject(args.repos).mode === "polyrepo" || args.polyrepo === true;
  const template = polyrepo ? provider === "github" ? "ci/cadre-merge-train.github.yml" : "ci/cadre-merge-train.gitlab.yml" : provider === "github" ? "ci/cadre-monorepo-check.github.yml" : "ci/cadre-monorepo-check.gitlab.yml";
  const sourceText = templateText(template, "");
  const source = templateSourceLabel(template) || template;
  if (!sourceText) return { ok: false, error: `Missing CI template ${template}` };
  const target = provider === "github" ? import_node_path28.default.join(root, ".github", "workflows", polyrepo ? "cadre-merge-train.yml" : "cadre-monorepo-check.yml") : import_node_path28.default.join(root, ".gitlab-ci.yml");
  if (fileExists(target) && args.force !== true) {
    return { ok: true, skipped: true, provider, source, path: import_node_path28.default.relative(root, target) };
  }
  import_node_fs16.default.mkdirSync(import_node_path28.default.dirname(target), { recursive: true });
  import_node_fs16.default.writeFileSync(target, sourceText);
  return { ok: true, provider, source, path: import_node_path28.default.relative(root, target), written: true };
}
function setupSubmodulePlan(root, repos, args = {}) {
  const entries = Array.isArray(repos.repos) ? repos.repos.map(asJsonObject) : [];
  const commands = [];
  for (const repo of entries) {
    const name = asOptionalString(repo.name);
    const url = asOptionalString(repo.url);
    const submodulePath = asOptionalString(repo.submodule_path);
    if (!name || !url || !submodulePath || repo.enabled === false) continue;
    if (fileExists(import_node_path28.default.join(root, submodulePath))) continue;
    commands.push(plannedGitAction(
      `submodule-${safeName(name)}`,
      "submodule_add",
      name,
      root,
      ["submodule", "add", url, submodulePath],
      `Register ${name} as a product submodule`
    ));
  }
  const execute = args.addSubmodules === true || args.add_submodules === true || args.executeSubmodules === true || args.execute_submodules === true;
  const results = execute ? runPlannedGitActions(commands) : [];
  return {
    ok: !execute || actionResultsOk(results),
    execute,
    dry_run: !execute,
    commands,
    results
  };
}
function lspSetupHelperCandidates(root) {
  return mcpServerPathCandidates(root);
}
function redactRuntimeHelperPaths(text) {
  return text.replace(/[^\s"'`]*cadre-lsp-(?:setup|review|daemon)\.js/g, "<cadre-lsp-helper>").replace(/[^\s"'`]*cadre-server\.js/g, "<cadre-mcp-server>");
}
function summarizeRuntimeCommandResult(result) {
  return {
    ok: result.ok,
    status: result.status,
    signal: result.signal || null,
    timed_out: result.timed_out === true,
    stdout: redactRuntimeHelperPaths(result.stdout || "").slice(0, 4e3),
    stderr: redactRuntimeHelperPaths(result.stderr || "").slice(0, 4e3),
    error: result.error ? redactRuntimeHelperPaths(result.error) : void 0
  };
}
function lspSetup(root, args = {}) {
  const helper = lspSetupHelperCandidates(root).find(fileExists);
  if (!helper) {
    return {
      ok: false,
      available: false,
      reason: "Cadre MCP runtime was not found for LSP setup",
      checked_count: lspSetupHelperCandidates(root).length
    };
  }
  const config = asOptionalString(args.config) || "cadre/lsp.json";
  const commandArgs = [helper, "--cadre-lsp-setup", "--root", root, "--config", config, "--json"];
  if (args.execute === true) commandArgs.push("--write");
  const result = runCommand("node", commandArgs, { cwd: root, maxBuffer: 20 * 1024 * 1024 });
  if (!result.ok) {
    return { ok: false, available: true, result: summarizeRuntimeCommandResult(result), reason: "LSP setup helper failed" };
  }
  try {
    return {
      ok: true,
      available: true,
      execute: args.execute === true,
      dry_run: args.execute !== true,
      ...asJsonObject(JSON.parse(result.stdout || "{}"))
    };
  } catch {
    return { ok: false, available: true, result: summarizeRuntimeCommandResult(result), reason: "LSP setup helper returned invalid JSON" };
  }
}

// src/core/application/runtime/workspace-health.ts
var import_node_path29 = __toESM(require("node:path"));
function workspaceHealth(root, args = {}) {
  const mode2 = workflowResponseMode(args);
  const topology = loadTopology(root);
  const techStack = techStackSummary(root, args);
  const workspace = workspaceDiagnostics(root, { execute: false });
  const dependencyGraphResult = dependencyGraph(root);
  const lspCoverage = summarizeLspCoverage(root, args);
  const availableWorkResult = availableWork(root);
  const integrations2 = integrationInventory(root, { ...args, responseMode: mode2 });
  const compactIntegrations = {
    ok: integrations2.ok !== false,
    provider: asJsonObject(integrations2.provider),
    mcp_readiness: isRecord(integrations2.mcp_readiness) ? {
      ok: asJsonObject(integrations2.mcp_readiness).ok !== false,
      provider: asJsonObject(asJsonObject(integrations2.mcp_readiness).provider),
      summary: asJsonObject(asJsonObject(integrations2.mcp_readiness).summary)
    } : null,
    optional_mcps: Array.isArray(integrations2.optional_mcps) ? integrations2.optional_mcps : [],
    summary: asJsonObject(integrations2.summary)
  };
  const detailResources = workspaceHealthDetailResources(root);
  if (mode2 === "detail") {
    return {
      ok: true,
      root,
      response_mode: mode2,
      detail_available: true,
      topology: {
        polyrepo: topology.polyrepo,
        default_repo: topology.defaultRepo || null,
        sync_mode: topology.config.sync_mode || "local",
        repos: topology.repos
      },
      tech_stack: techStack,
      workspace,
      dependency_graph: dependencyGraphResult,
      parallel: availableWorkResult,
      languages: {
        detected: lspCoverage.recommended,
        configured: lspCoverage.configured
      },
      lsp: {
        coverage: lspCoverage,
        status: lspConfigStatus(root),
        setup: lspSetup(root, { ...args, execute: false })
      },
      integrations: integrations2,
      detail_resources: detailResources
    };
  }
  return {
    ok: true,
    root,
    response_mode: mode2,
    detail_available: true,
    topology: {
      polyrepo: topology.polyrepo,
      default_repo: topology.defaultRepo || null,
      sync_mode: topology.config.sync_mode || "local",
      repo_count: countRecords(asJsonObject(topology.repos).repos)
    },
    tech_stack: techStack.ok === false ? { ok: false, error: techStack.error || "Missing tech stack" } : {
      ok: true,
      path: techStack.path,
      summary: techStack.summary,
      styleGuideIds: techStack.styleGuideIds
    },
    workspace: summarizeWorkspaceDiagnosticsResult(workspace),
    dependency_graph: summarizeDependencyGraphResult(dependencyGraphResult),
    parallel: availableWorkResult.ok === false ? { ok: false, error: availableWorkResult.error || "Available work unavailable" } : {
      ok: true,
      available_count: countRecords(availableWorkResult.available),
      reclaimable_count: countRecords(availableWorkResult.reclaimable),
      available: Array.isArray(availableWorkResult.available) ? availableWorkResult.available.slice(0, 5) : [],
      reclaimable: Array.isArray(availableWorkResult.reclaimable) ? availableWorkResult.reclaimable.slice(0, 5) : []
    },
    languages: {
      detected: lspCoverage.recommended,
      configured: lspCoverage.configured
    },
    lsp: lspCoverage,
    integrations: compactIntegrations,
    detail_resources: detailResources
  };
}
function lspConfigStatus(root) {
  const configPath = import_node_path29.default.join(root, "cadre", "lsp.json");
  const config = readJson(configPath, null);
  if (!config) {
    return {
      configured: false,
      path: import_node_path29.default.relative(root, configPath),
      servers: [],
      missing: [],
      daemon: {
        status_packet: "cadre_intel action lsp_daemon_status",
        shutdown_packet: "cadre_intel action lsp_daemon_shutdown",
        max_clients_default: 8,
        idle_eviction_ms_default: 6e5
      }
    };
  }
  const configObject = asJsonObject(config);
  const servers = Array.isArray(configObject.servers) ? configObject.servers.map((server) => asJsonObject(server)) : [];
  return {
    configured: true,
    path: import_node_path29.default.relative(root, configPath),
    servers: servers.map((server) => {
      const command = asOptionalString(server.command);
      return {
        id: asOptionalString(server.id) || command || "unknown",
        command: command || null,
        available: command ? commandExists(command, root) : false
      };
    }),
    missing: servers.filter((server) => {
      const command = asOptionalString(server.command);
      return !command || !commandExists(command, root);
    }).map((server) => asOptionalString(server.id) || asOptionalString(server.command) || "unknown"),
    daemon: {
      status_packet: "cadre_intel action lsp_daemon_status",
      shutdown_packet: "cadre_intel action lsp_daemon_shutdown",
      max_clients_default: 8,
      idle_eviction_ms_default: 6e5
    }
  };
}
function mergeDriverStatus(root) {
  const result = runCommand("git", ["config", "merge.ours.driver"], { cwd: root });
  return {
    configured: result.ok && result.stdout.trim() !== "",
    value: result.stdout.trim() || null
  };
}
function doctor(root, options = {}) {
  const candidateRoot = import_node_path29.default.resolve(root || process.cwd());
  const generatedCheck = import_node_path29.default.join(candidateRoot, "scripts", "generate-skills.sh");
  const lspStatus = lspConfigStatus(candidateRoot);
  const lspMissing = asStringArray(lspStatus.missing);
  const checks = {
    mcp_runtime: { ok: true, server: "cadre" },
    cadre_project: {
      ok: Boolean(options.hasCadreProject || isCadreProjectRoot(candidateRoot)),
      root: candidateRoot,
      markers: [
        "cadre/tracks.json",
        "cadre/setup_state.json",
        "cadre/product.json",
        "cadre/config.json",
        "cadre/lsp.json"
      ].filter((name) => fileExists(import_node_path29.default.join(candidateRoot, name)))
    },
    git: {
      available: commandExists("git", candidateRoot),
      identity: gitIdentity(candidateRoot),
      merge_ours: mergeDriverStatus(candidateRoot)
    },
    lsp: lspStatus,
    provider: providerMcpAvailability(candidateRoot, options),
    generated_bundles: {
      check_available: fileExists(generatedCheck),
      command: fileExists(generatedCheck) ? "bash scripts/generate-skills.sh --check" : null
    }
  };
  const warnings = [];
  if (!checks.cadre_project.ok) {
    warnings.push("No Cadre project markers found. This is fine for the Cadre harness/source repo, but project-scoped Cadre workflows need setup first.");
  }
  if (checks.lsp.configured && lspMissing.length > 0) {
    warnings.push(`LSP config exists but missing server commands: ${lspMissing.join(", ")}`);
  }
  return {
    ok: warnings.length === 0,
    root: candidateRoot,
    checks,
    warnings
  };
}

// src/core/application/runtime/health-summaries.ts
function countRecords(records) {
  return Array.isArray(records) ? records.length : 0;
}
function workspaceHealthDetailResources(root) {
  const encodedRoot = encodeURIComponent(root);
  return [
    `cadre://workspace-health?root=${encodedRoot}&responseMode=detail`,
    `cadre://workspace-diagnostics?root=${encodedRoot}`,
    `cadre://repo-topology?root=${encodedRoot}`,
    `cadre://repo-map?root=${encodedRoot}`,
    `cadre://lsp-status?root=${encodedRoot}`,
    `cadre://integrations?root=${encodedRoot}`,
    `cadre://mcp-readiness?root=${encodedRoot}`
  ];
}
function summarizeWorkspaceDiagnosticsResult(result) {
  return {
    ok: result.ok !== false,
    repo_count: countRecords(result.repos),
    adapter_count: countRecords(result.adapters),
    command_count: countRecords(result.commands),
    result_count: countRecords(result.results)
  };
}
function summarizeDependencyGraphResult(result) {
  return {
    ok: result.ok !== false,
    repo_count: countRecords(result.repos),
    manifest_count: countRecords(result.manifests),
    edge_count: countRecords(result.edges)
  };
}
function summarizeLspSetupResult(result) {
  return {
    ok: result.ok !== false,
    available: result.available !== false,
    execute: result.execute === true,
    dry_run: result.dry_run !== false,
    written: result.written === true,
    added: Array.isArray(result.added) ? result.added.slice(0, 10) : [],
    added_count: countRecords(result.added),
    missing_from_config_count: countRecords(result.missingFromConfig),
    missing_commands_count: countRecords(result.missingCommands)
  };
}
function summarizeLspCoverage(root, args = {}) {
  const status = asJsonObject(lspConfigStatus(root));
  const setup = asJsonObject(lspSetup(root, { ...args, execute: false }));
  const configured = Array.isArray(status.servers) ? status.servers.map((server) => asJsonObject(server).id || null).filter((id) => typeof id === "string" && id.length > 0) : [];
  const recommended = Array.isArray(setup.recommended) ? setup.recommended.map((entry) => asJsonObject(entry).id || null).filter((id) => typeof id === "string" && id.length > 0) : [];
  const missing = recommended.filter((id) => !configured.includes(id));
  const covered = recommended.filter((id) => configured.includes(id));
  return {
    ok: status.configured !== false && setup.ok !== false,
    status_configured: status.configured !== false,
    configured_count: configured.length,
    recommended_count: recommended.length,
    covered_count: covered.length,
    missing_count: missing.length,
    coverage: recommended.length > 0 ? Math.round(covered.length / recommended.length * 100) : null,
    configured: configured.slice(0, 10),
    recommended: recommended.slice(0, 10),
    missing: missing.slice(0, 10)
  };
}

// src/core/application/runtime/integrations.ts
function normalizeIntegrationValue(value) {
  if (value == null) {
    return { configured: false, available: null };
  }
  if (typeof value === "boolean") {
    return { configured: true, available: value };
  }
  if (typeof value === "string") {
    const text = value.trim();
    if (!text) return { configured: false, available: null };
    const lower = text.toLowerCase();
    if (["false", "0", "no", "off", "disabled"].includes(lower)) {
      return { configured: true, available: false, label: text };
    }
    return { configured: true, available: true, label: text };
  }
  if (isRecord(value)) {
    const entry = asJsonObject(value);
    const configured = Object.keys(entry).length > 0;
    const available = typeof entry.available === "boolean" ? entry.available : typeof entry.enabled === "boolean" ? entry.enabled : typeof entry.configured === "boolean" ? entry.configured : entry.command || entry.server || entry.url ? true : null;
    return {
      configured,
      available,
      label: asOptionalString(entry.label) || asOptionalString(entry.name),
      command: asOptionalString(entry.command),
      server: asOptionalString(entry.server),
      url: asOptionalString(entry.url),
      provider: asOptionalString(entry.provider),
      platform: asOptionalString(entry.platform),
      kind: asOptionalString(entry.kind)
    };
  }
  return { configured: false, available: null };
}
function pickIntegrationCandidate(scopes, keys) {
  for (const { source, scope } of scopes) {
    if (!isRecord(scope)) continue;
    for (const key of keys) {
      if (Object.prototype.hasOwnProperty.call(scope, key)) {
        const value = asJsonObject(scope)[key];
        if (value !== void 0 && value !== null) return { source: `${source}.${key}`, value };
      }
    }
  }
  return null;
}
function integrationStatus(root, args, kind, label, keys, mode2) {
  const topology = loadTopology(root);
  const config = asJsonObject(topology.config || {});
  const configIntegrations = isRecord(config.integrations) ? asJsonObject(config.integrations) : {};
  const candidate = pickIntegrationCandidate([
    { source: "config.integrations", scope: configIntegrations },
    { source: "config", scope: config },
    { source: "args", scope: args }
  ], keys);
  const normalized = normalizeIntegrationValue(candidate?.value);
  const status = {
    kind,
    label,
    configured: normalized.configured === true || candidate != null,
    available: normalized.available,
    source: candidate?.source || "not_configured"
  };
  if (normalized.label) status.value = normalized.label;
  if (normalized.command) status.command = normalized.command;
  if (normalized.server) status.server = normalized.server;
  if (normalized.url) status.url = normalized.url;
  if (normalized.provider) status.provider = normalized.provider;
  if (normalized.platform) status.platform = normalized.platform;
  if (normalized.kind) status.integration_kind = normalized.kind;
  if (mode2 === "detail") {
    status.candidates = keys;
  }
  return status;
}
function integrationInventory(root, args = {}) {
  const mode2 = workflowResponseMode(args);
  const provider = providerMcpAvailability(root, args);
  const readiness = mcpReadiness(root, args);
  const lsp = summarizeLspCoverage(root, args);
  const optionalMcps = [
    integrationStatus(root, args, "code_search", "Code search", ["code_search", "codeSearch", "sourcegraph", "sourcegraph_mcp", "sourcegraphMcp", "search"], mode2),
    integrationStatus(root, args, "issue_tracker", "Issue tracker", ["issue_tracker", "issueTracker", "jira", "jira_mcp", "jiraMcp", "linear", "linear_mcp", "linearMcp"], mode2),
    integrationStatus(root, args, "ci", "CI", ["ci", "ci_provider", "ciProvider", "ci_mcp", "ciMcp", "ci_mcp_available", "ciMcpAvailable"], mode2),
    integrationStatus(root, args, "logging", "Logging", ["logging", "observability", "telemetry", "sentry", "sentry_mcp", "datadog", "datadog_mcp", "honeycomb", "honeycomb_mcp"], mode2),
    integrationStatus(root, args, "knowledge_base", "Knowledge base", ["knowledge_base", "knowledgeBase", "kb", "docs", "confluence", "notion", "knowledge_base_mcp", "knowledgeBaseMcp"], mode2)
  ];
  const configuredOptionalCount = optionalMcps.filter((entry) => entry.configured === true).length;
  const availableOptionalCount = optionalMcps.filter((entry) => entry.available === true).length;
  const unavailableOptionalCount = optionalMcps.filter((entry) => entry.configured === true && entry.available === false).length;
  const detailResources = workspaceHealthDetailResources(root);
  const summary = {
    provider_mode: asOptionalString(provider.provider_mode) || "local",
    provider_available: provider.available ?? null,
    optional_configured_count: configuredOptionalCount,
    optional_available_count: availableOptionalCount,
    optional_unavailable_count: unavailableOptionalCount,
    lsp_configured_count: asOptionalNumber(lsp.configured_count),
    lsp_recommended_count: asOptionalNumber(lsp.recommended_count),
    lsp_covered_count: asOptionalNumber(lsp.covered_count),
    lsp_missing_count: asOptionalNumber(lsp.missing_count),
    lsp_coverage: asOptionalNumber(lsp.coverage)
  };
  if (mode2 === "detail") {
    return {
      ok: true,
      root,
      response_mode: mode2,
      detail_available: true,
      provider,
      mcp_readiness: readiness,
      optional_mcps: optionalMcps,
      lsp: {
        coverage: lsp,
        status: lspConfigStatus(root),
        setup: lspSetup(root, { ...args, execute: false })
      },
      summary,
      detail_resources: detailResources
    };
  }
  return {
    ok: true,
    root,
    response_mode: mode2,
    detail_available: true,
    provider: {
      ok: provider.ok !== false,
      provider_mode: provider.provider_mode || "local",
      available: provider.available ?? null,
      required_provider_mcp: provider.required_provider_mcp || null,
      source: provider.source || null,
      remote_host: provider.remote_host || null,
      requires_confirmation: provider.requires_confirmation === true
    },
    mcp_readiness: {
      ok: readiness.ok !== false,
      provider: asJsonObject(readiness.provider),
      summary: asJsonObject(readiness.summary),
      recommendations: Array.isArray(readiness.recommendations) ? readiness.recommendations : []
    },
    optional_mcps: optionalMcps.map((entry) => ({
      kind: entry.kind,
      label: entry.label,
      configured: entry.configured,
      available: entry.available,
      source: entry.source
    })),
    lsp,
    summary,
    detail_resources: detailResources
  };
}

// src/core/application/runtime/formula-workflow.ts
var import_node_fs18 = __toESM(require("node:fs"));
var import_node_path31 = __toESM(require("node:path"));

// src/core/application/runtime/workflow-new-track.ts
var import_node_fs17 = __toESM(require("node:fs"));
var import_node_path30 = __toESM(require("node:path"));
function newTrackReviewFiles(trackId, spec, plan, metadata) {
  const safeTrack = safeName(trackId);
  const specJson = normalizeSpecJson(trackId, spec);
  const planJson = normalizePlanJson(trackId, plan, specJson);
  const learningsEntry = {
    ...templateJson("learnings_seed.json", { id: "initial", kind: "learnings_seed" }),
    id: "initial",
    kind: "learnings_seed",
    track_id: trackId,
    recorded_at: utcNow(),
    text: trackLearningsText(trackId)
  };
  return [
    jsonReviewFile(
      `cadre/tracks/${safeTrack}/spec.json`,
      "Track spec canonical",
      "spec",
      specJson
    ),
    textReviewFile(
      `cadre/tracks/${safeTrack}/spec.md`,
      "Track spec",
      "spec.json",
      withGeneratedMarker(`cadre/tracks/${safeTrack}/spec.json`, "cadre.spec.v1", renderSpecMarkdown(specJson))
    ),
    jsonReviewFile(
      `cadre/tracks/${safeTrack}/plan.json`,
      "Track plan canonical",
      "plan",
      planJson
    ),
    textReviewFile(
      `cadre/tracks/${safeTrack}/plan.md`,
      "Track plan",
      "plan.json",
      withGeneratedMarker(`cadre/tracks/${safeTrack}/plan.json`, "cadre.plan.v1", renderPlanMarkdown(planJson))
    ),
    jsonReviewFile(
      `cadre/tracks/${safeTrack}/metadata.json`,
      "Track metadata",
      "metadata",
      metadata
    ),
    plainReviewFile(
      `cadre/tracks/${safeTrack}/learnings.jsonl`,
      "Track learnings canonical",
      "template:learnings_seed.json",
      `${JSON.stringify(learningsEntry)}
`
    ),
    textReviewFile(
      `cadre/tracks/${safeTrack}/learnings.md`,
      "Track learnings",
      "learnings.jsonl",
      withGeneratedMarker(`cadre/tracks/${safeTrack}/learnings.jsonl`, "cadre.learnings.v1", trackLearningsText(trackId))
    )
  ];
}
function workflowNewTrack(root, args = {}) {
  const summary = workflowSummary(root, "newtrack", args);
  const markdownError = markdownPayloadError(args);
  if (markdownError) return { ...summary, ...markdownError };
  const trackId = args.trackId || args.track_id;
  if (!trackId) return { ...summary, ok: false, error: "trackId is required" };
  if (!isRecord(args.plan)) return { ...summary, ok: false, error: "plan is required" };
  const specJson = normalizeSpecJson(String(trackId), args.spec || { title: `Spec: ${trackId}`, description: asOptionalString(args.description) || String(trackId) });
  const planJson = normalizePlanJson(String(trackId), args.plan, specJson);
  const metadata = {
    track_id: trackId,
    type: "feature",
    status: "new",
    priority: "medium",
    depends_on: [],
    description: asOptionalString(args.description) || trackId,
    owner: gitIdentity(root) || null,
    reviewer: null,
    git_branch: `track/${trackId}`,
    worktree_path: `.worktrees/${trackId}`,
    ...args.metadata && typeof args.metadata === "object" ? args.metadata : {}
  };
  const reviewFiles = newTrackReviewFiles(String(trackId), specJson, planJson, metadata);
  const reviewArtifacts = reviewArtifactsFromFiles(reviewFiles);
  const reviewBundle = workflowReviewBundle(root, "newtrack", args, reviewFiles, { track_id: String(trackId) });
  const humanReview = humanReviewState("newtrack", args, reviewArtifacts, reviewBundle);
  const warnings = asStringArray(asJsonObject(reviewBundle).warnings);
  const dryRun = args.execute !== true;
  const assist = planAssist(root, { ...args, plan: planJson, trackId });
  if (dryRun) {
    return {
      ...summary,
      ok: assist.ok !== false,
      dry_run: true,
      track_id: trackId,
      metadata,
      plan_assist: assist,
      human_review: humanReview,
      review_artifacts: reviewArtifacts,
      review_bundle: reviewBundle,
      warnings
    };
  }
  if (findTrack(root, trackId)) {
    return { ...summary, ok: false, track_id: trackId, error: "Track already exists" };
  }
  if (!humanReviewConfirmed(args)) {
    return {
      ...summary,
      ok: false,
      dry_run: true,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      track_id: trackId,
      metadata,
      plan_assist: assist,
      human_review: humanReview,
      review_artifacts: reviewArtifacts,
      review_bundle: reviewBundle,
      warnings,
      error: "Human confirmation is required before creating track artifacts"
    };
  }
  const dir = import_node_path30.default.join(root, "cadre", "tracks", safeName(trackId));
  const learningsEntry = {
    ...templateJson("learnings_seed.json", { id: "initial", kind: "learnings_seed" }),
    id: "initial",
    kind: "learnings_seed",
    track_id: String(trackId),
    recorded_at: utcNow(),
    text: trackLearningsText(String(trackId))
  };
  import_node_fs17.default.mkdirSync(dir, { recursive: true });
  writeJson(import_node_path30.default.join(dir, "metadata.json"), metadata);
  writeJson(import_node_path30.default.join(dir, "spec.json"), specJson);
  writeJson(import_node_path30.default.join(dir, "plan.json"), planJson);
  import_node_fs17.default.writeFileSync(import_node_path30.default.join(dir, "spec.md"), withGeneratedMarker(`cadre/tracks/${safeName(trackId)}/spec.json`, "cadre.spec.v1", renderSpecMarkdown(specJson)));
  import_node_fs17.default.writeFileSync(import_node_path30.default.join(dir, "plan.md"), withGeneratedMarker(`cadre/tracks/${safeName(trackId)}/plan.json`, "cadre.plan.v1", renderPlanMarkdown(planJson)));
  import_node_fs17.default.writeFileSync(import_node_path30.default.join(dir, "learnings.jsonl"), `${JSON.stringify(learningsEntry)}
`);
  import_node_fs17.default.writeFileSync(import_node_path30.default.join(dir, "learnings.md"), withGeneratedMarker(`cadre/tracks/${safeName(trackId)}/learnings.jsonl`, "cadre.learnings.v1", trackLearningsText(String(trackId))));
  const regen = regenIndex(root);
  const event = appendCadreEvent(root, {
    kind: "track_created",
    workflow: "newtrack",
    track_id: String(trackId),
    status: metadata.status,
    tags: metadata.tags || []
  });
  return {
    ...summary,
    ok: regen.ok !== false,
    dry_run: false,
    track_id: trackId,
    metadata_path: import_node_path30.default.relative(root, import_node_path30.default.join(dir, "metadata.json")),
    regen,
    event,
    human_review: humanReview,
    worktree_plan: worktreePlan(root, { trackId })
  };
}

// src/core/application/runtime/formula-workflow.ts
var READ_ACTIONS = /* @__PURE__ */ new Set(["list", "show", "cook", "wisp_list", "pour"]);
function formulaAction(args) {
  const rawArgs = args;
  const raw = asOptionalString(rawArgs.operation) || asOptionalString(rawArgs.mode) || asOptionalString(rawArgs.formulaAction || rawArgs.formula_action) || (asOptionalString(args.action) && asOptionalString(args.action) !== "formula" ? asOptionalString(args.action) : null) || "list";
  const normalized = raw.replace(/^formula_/, "").replace(/-/g, "_");
  if (normalized === "wisp_update") return "wisp_update_step";
  if (normalized === "squash") return "wisp_squash";
  if (normalized === "burn") return "wisp_burn";
  if (normalized === "create_wisp") return "wisp_create";
  if (normalized === "list_wisps") return "wisp_list";
  if (normalized === "update_step") return "wisp_update_step";
  return [
    "list",
    "show",
    "cook",
    "wisp_create",
    "wisp_list",
    "wisp_update_step",
    "wisp_squash",
    "wisp_burn",
    "pour"
  ].includes(normalized) ? normalized : "list";
}
function formulaDir(root) {
  return import_node_path31.default.join(root, "cadre", "formulas");
}
function wispDir(root) {
  return import_node_path31.default.join(root, "cadre", "local", "wisps");
}
function formulaId(args) {
  const rawArgs = args;
  return asOptionalString(args.id) || asOptionalString(rawArgs.formulaId || rawArgs.formula_id) || asOptionalString(args.name) || null;
}
function wispId(args) {
  const rawArgs = args;
  return asOptionalString(args.id) || asOptionalString(rawArgs.wispId || rawArgs.wisp_id) || null;
}
function formulaPath(root, id) {
  return import_node_path31.default.join(formulaDir(root), `${safeName(id).replace(/\.json$/i, "")}.json`);
}
function wispPath(root, id) {
  return import_node_path31.default.join(wispDir(root), `${safeName(id).replace(/\.json$/i, "")}.json`);
}
function loadFormula(root, id) {
  if (!id) return { ok: false, error: "formula id is required" };
  const file = formulaPath(root, id);
  const formula = readJson(file, null);
  if (!formula) return { ok: false, id, error: `Formula not found: ${id}`, path: import_node_path31.default.relative(root, file) };
  return { ok: true, id: asOptionalString(formula.id) || id, path: import_node_path31.default.relative(root, file), formula };
}
function variablesFromArgs(formula, args) {
  const rawArgs = args;
  return {
    ...asJsonObject(formula.defaults),
    ...asJsonObject(rawArgs.variables),
    ...asJsonObject(rawArgs.vars)
  };
}
function variableValue(variables, key) {
  const value = key.split(".").reduce((current, part) => isRecord(current) ? asJsonObject(current)[part] : void 0, variables);
  if (value == null) return "";
  return typeof value === "string" ? value : JSON.stringify(value);
}
function renderTemplateValue(value, variables) {
  if (typeof value === "string") {
    return value.replace(/\{\{\s*([A-Za-z0-9_.-]+)\s*\}\}/g, (_match, key) => variableValue(variables, String(key)));
  }
  if (Array.isArray(value)) return value.map((item) => renderTemplateValue(item, variables));
  if (isRecord(value)) {
    return Object.fromEntries(Object.entries(asJsonObject(value)).map(([key, entry]) => [key, renderTemplateValue(entry, variables)]));
  }
  return value;
}
function stepsFromFormula(formula) {
  return Array.isArray(formula.steps) ? formula.steps.map(asJsonObject) : [];
}
function cookedPlan(trackId, formula, rendered) {
  if (isRecord(rendered.plan)) return asJsonObject(rendered.plan);
  const steps = stepsFromFormula(rendered);
  const phaseTitle = asOptionalString(rendered.phase_title) || asOptionalString(rendered.recommended_phase) || "Formula";
  return {
    version: 1,
    schema: "cadre.plan.v1",
    track_id: trackId,
    title: `Plan: ${trackId}`,
    phases: [{
      phase_index: 1,
      title: phaseTitle,
      execution_mode: asOptionalString(rendered.execution_mode) || "sequential",
      depends_on: asStringArray(rendered.dependencies),
      tasks: steps.map((step, index) => ({
        task_index: index + 1,
        task_key: asOptionalString(step.id || step.key) || `formula_step_${index + 1}`,
        title: asOptionalString(step.title) || asOptionalString(step.description) || `Formula step ${index + 1}`,
        status: asOptionalString(step.status) || "pending",
        labels: asStringArray(step.labels),
        depends_on: asStringArray(step.depends_on || step.depends),
        files: asStringArray(step.files),
        repo: asOptionalString(step.repo) || null,
        task_type: asOptionalString(step.task_type) || null,
        description: asOptionalString(step.description) || null
      }))
    }]
  };
}
function cookedSpec(trackId, formula, rendered) {
  if (isRecord(rendered.spec)) return asJsonObject(rendered.spec);
  return {
    version: 1,
    schema: "cadre.spec.v1",
    track_id: trackId,
    title: asOptionalString(rendered.title) || `Spec: ${trackId}`,
    description: asOptionalString(rendered.description) || asOptionalString(formula.description) || asOptionalString(formula.title) || trackId,
    acceptance: asStringArray(rendered.acceptance)
  };
}
function cookFormula(root, args) {
  const loaded = loadFormula(root, formulaId(args));
  if (loaded.ok === false) return loaded;
  const formula = asJsonObject(loaded.formula);
  const variables = variablesFromArgs(formula, args);
  const rendered = asJsonObject(renderTemplateValue(formula, variables));
  const trackId = asOptionalString(args.trackId || args.track_id) || asOptionalString(args.trackName || args.track_name) || `${asOptionalString(formula.id) || loaded.id}-draft`;
  return {
    ok: true,
    formula_id: loaded.id,
    title: asOptionalString(formula.title) || loaded.id,
    variables,
    recommended_phase: asOptionalString(formula.recommended_phase) || null,
    dependencies: asStringArray(formula.dependencies),
    spec: cookedSpec(trackId, formula, rendered),
    plan: cookedPlan(trackId, formula, rendered)
  };
}
function listFormulas(root) {
  ensureNativeState(root);
  const dir = formulaDir(root);
  const items = fileExists(dir) ? import_node_fs18.default.readdirSync(dir).filter((name) => name.endsWith(".json")).map((name) => {
    const file = import_node_path31.default.join(dir, name);
    const formula = readJson(file, {});
    return {
      id: asOptionalString(formula.id) || name.replace(/\.json$/i, ""),
      title: asOptionalString(formula.title) || asOptionalString(formula.id) || name,
      recommended_phase: asOptionalString(formula.recommended_phase) || null,
      path: import_node_path31.default.relative(root, file)
    };
  }) : [];
  return { ok: true, formulas: items, count: items.length };
}
function wispSteps(cooked) {
  const plan = asJsonObject(cooked.plan);
  const phases = Array.isArray(plan.phases) ? plan.phases.map(asJsonObject) : [];
  return phases.flatMap((phase) => {
    const phaseIndex = Number(phase.phase_index || phase.index || 1);
    return (Array.isArray(phase.tasks) ? phase.tasks.map(asJsonObject) : []).map((task, index) => ({
      step_id: asOptionalString(task.task_key || task.id || task.key) || `step_${phaseIndex}_${index + 1}`,
      phase_index: phaseIndex,
      task_index: Number(task.task_index || task.index || index + 1),
      title: asOptionalString(task.title) || `Step ${index + 1}`,
      status: "pending",
      evidence: null,
      updated_at: null
    }));
  });
}
function createWisp(root, args) {
  const cooked = cookFormula(root, args);
  if (cooked.ok === false) return cooked;
  const formula_id = asOptionalString(cooked.formula_id) || "formula";
  const now = utcNow();
  const id = asOptionalString(args.wispId || args.wisp_id) || `wisp_${safeName(formula_id)}_${textHash(`${now}:${JSON.stringify(cooked.variables || {})}`).slice(0, 10)}`;
  const wisp = {
    version: 1,
    schema: "cadre.wisp.v1",
    id,
    formula_id,
    status: "active",
    owner: asOptionalString(args.identity) || null,
    ttl: asOptionalString(args.ttl) || null,
    created_at: now,
    updated_at: now,
    variables: asJsonObject(cooked.variables),
    spec: asJsonObject(cooked.spec),
    plan: asJsonObject(cooked.plan),
    steps: wispSteps(asJsonObject(cooked)),
    evidence: []
  };
  const file = wispPath(root, id);
  writeJsonEnsured(file, wisp);
  const event = appendCadreEvent(root, { kind: "wisp_created", workflow: "formula", formula_id, wisp_id: id });
  return { ok: true, wisp_id: id, path: import_node_path31.default.relative(root, file), wisp, event };
}
function listWisps(root) {
  ensureNativeState(root);
  const dir = wispDir(root);
  const wisps = fileExists(dir) ? import_node_fs18.default.readdirSync(dir).filter((name) => name.endsWith(".json")).map((name) => {
    const file = import_node_path31.default.join(dir, name);
    const wisp = readJson(file, {});
    return {
      id: asOptionalString(wisp.id) || name.replace(/\.json$/i, ""),
      formula_id: asOptionalString(wisp.formula_id) || null,
      status: asOptionalString(wisp.status) || null,
      owner: asOptionalString(wisp.owner) || null,
      updated_at: asOptionalString(wisp.updated_at) || null,
      path: import_node_path31.default.relative(root, file)
    };
  }) : [];
  return { ok: true, wisps, count: wisps.length };
}
function updateWispStep(root, args) {
  const id = wispId(args);
  if (!id) return { ok: false, error: "wisp id is required" };
  const file = wispPath(root, id);
  const wisp = readJson(file, null);
  if (!wisp) return { ok: false, error: `Wisp not found: ${id}`, path: import_node_path31.default.relative(root, file) };
  const rawArgs = args;
  const stepId = asOptionalString(rawArgs.stepId || rawArgs.step_id || rawArgs.taskKey || rawArgs.task_key);
  const stepIndex = Number(rawArgs.stepIndex || rawArgs.step_index || args.taskIndex || 0);
  const steps = Array.isArray(wisp.steps) ? wisp.steps.map(asJsonObject) : [];
  const index = steps.findIndex(
    (step, offset) => stepId && asOptionalString(step.step_id) === stepId || stepIndex > 0 && offset + 1 === stepIndex
  );
  if (index < 0) return { ok: false, error: "Wisp step not found", wisp_id: id, step_id: stepId || null, step_index: stepIndex || null };
  const now = utcNow();
  const status = asOptionalString(args.status) || "completed";
  const currentStep = steps[index] || {};
  steps[index] = {
    ...currentStep,
    status,
    evidence: isRecord(args.evidence) ? asJsonObject(args.evidence) : asOptionalString(args.evidence) || asJsonObject(currentStep.evidence),
    summary: asOptionalString(args.summary) || asOptionalString(currentStep.summary) || null,
    updated_at: now
  };
  const next = { ...wisp, steps, status: asOptionalString(wisp.status) || "active", updated_at: now };
  writeJsonEnsured(file, next);
  const event = appendCadreEvent(root, { kind: "wisp_step_updated", workflow: "formula", wisp_id: id, step_id: asOptionalString(steps[index].step_id) || null, status });
  return { ok: true, wisp_id: id, path: import_node_path31.default.relative(root, file), step: steps[index], wisp: next, event };
}
function squashWisp(root, args) {
  const id = wispId(args);
  if (!id) return { ok: false, error: "wisp id is required" };
  const file = wispPath(root, id);
  const wisp = readJson(file, null);
  if (!wisp) return { ok: false, error: `Wisp not found: ${id}`, path: import_node_path31.default.relative(root, file) };
  const digest = {
    version: 1,
    schema: "cadre.wisp_digest.v1",
    id: `digest_${safeName(id)}_${textHash(`${utcNow()}:${JSON.stringify(wisp)}`).slice(0, 10)}`,
    wisp_id: id,
    formula_id: asOptionalString(wisp.formula_id) || null,
    status: asOptionalString(args.status) || asOptionalString(wisp.status) || "squashed",
    summary: asOptionalString(args.summary) || null,
    recorded_at: utcNow(),
    steps: Array.isArray(wisp.steps) ? wisp.steps.map(asJsonObject) : [],
    evidence: Array.isArray(wisp.evidence) ? wisp.evidence.map(asJsonObject) : []
  };
  const digestPath = import_node_path31.default.join(root, "cadre", "operations", "wisp-digests.jsonl");
  appendJsonl(digestPath, digest);
  const event = appendCadreEvent(root, { kind: "wisp_squashed", workflow: "formula", wisp_id: id, digest_id: digest.id });
  return { ok: true, wisp_id: id, digest, path: import_node_path31.default.relative(root, digestPath), event };
}
function burnWisp(root, args) {
  const id = wispId(args);
  if (!id) return { ok: false, error: "wisp id is required" };
  const file = wispPath(root, id);
  const existed = fileExists(file);
  if (existed) import_node_fs18.default.rmSync(file, { force: true });
  const event = appendCadreEvent(root, { kind: "wisp_burned", workflow: "formula", wisp_id: id, existed });
  return { ok: true, wisp_id: id, existed, path: import_node_path31.default.relative(root, file), event };
}
function pourFormula(root, args) {
  const id = wispId(args);
  const wisp = id ? readJson(wispPath(root, id), null) : null;
  const cooked = wisp ? { ok: true, formula_id: asOptionalString(wisp.formula_id), spec: asJsonObject(wisp.spec), plan: asJsonObject(wisp.plan), variables: asJsonObject(wisp.variables) } : cookFormula(root, args);
  if (cooked.ok === false) return cooked;
  const formula_id = asOptionalString(cooked.formula_id) || formulaId(args) || "formula";
  const trackId = asOptionalString(args.trackId || args.track_id) || asOptionalString(args.outputTrackId || args.output_track_id) || `${safeName(formula_id)}-${textHash(JSON.stringify(cooked.variables || {})).slice(0, 8)}`;
  const tags = Array.from(/* @__PURE__ */ new Set([
    `formula:${formula_id}`,
    ...asStringArray(asJsonObject(args.metadata).tags)
  ]));
  const result = workflowNewTrack(root, {
    ...args,
    workflow: "newtrack",
    action: "newtrack",
    trackId,
    spec: asJsonObject(cooked.spec),
    plan: asJsonObject(cooked.plan),
    description: asOptionalString(args.description) || `Formula output: ${formula_id}`,
    metadata: {
      ...asJsonObject(args.metadata),
      tags
    }
  });
  const event = result.ok === false ? null : appendCadreEvent(root, { kind: "formula_poured", workflow: "formula", formula_id, wisp_id: id || null, track_id: trackId });
  return { ...result, formula_id, wisp_id: id || null, pour_event: event };
}
function workflowFormula(root, args = {}) {
  const action = formulaAction(args);
  const summary = workflowSummary(root, "formula", args);
  ensureNativeState(root);
  if (action === "list") return { ...summary, action, ...listFormulas(root) };
  if (action === "show") return { ...summary, action, ...loadFormula(root, formulaId(args)) };
  if (action === "cook") return { ...summary, action, ...cookFormula(root, args) };
  if (action === "wisp_list") return { ...summary, action, ...listWisps(root) };
  if (args.execute !== true && !READ_ACTIONS.has(action)) {
    return {
      ...summary,
      ok: false,
      action,
      dry_run: true,
      phase_state: "awaiting_execute",
      error: `Formula action ${action} requires execute:true`
    };
  }
  if (action === "wisp_create") return { ...summary, action, ...createWisp(root, args) };
  if (action === "wisp_update_step") return { ...summary, action, ...updateWispStep(root, args) };
  if (action === "wisp_squash") return { ...summary, action, ...squashWisp(root, args) };
  if (action === "wisp_burn") return { ...summary, action, ...burnWisp(root, args) };
  if (action === "pour") return { ...summary, action, ...pourFormula(root, args) };
  return { ...summary, ok: false, action, error: `Unknown formula action: ${action}` };
}

// src/core/application/runtime/parallel-state.ts
var import_node_path34 = __toESM(require("node:path"));

// src/core/application/runtime/task-completion.ts
var import_node_path33 = __toESM(require("node:path"));

// src/core/application/runtime/manual-verification.ts
var import_node_path32 = __toESM(require("node:path"));
function completionJournalPath(track) {
  return import_node_path32.default.join(track.dir, "completion_journal.json");
}
function readCompletionJournal(track) {
  const value = readJson(completionJournalPath(track), { entries: {} });
  if (!isRecord(value)) return { entries: {} };
  const entries = isRecord(value.entries) ? value.entries : {};
  return {
    ...asJsonObject(value),
    entries: Object.fromEntries(Object.entries(entries).map(([key, entry]) => [key, asJsonObject(entry)]))
  };
}
function writeCompletionJournal(track, journal) {
  writeJson(completionJournalPath(track), journal);
}
function patchCompletionJournal(track, key, patcher) {
  const journal = readCompletionJournal(track);
  const before = journal.entries[key] || {};
  journal.entries[key] = patcher({ ...before }, journal);
  journal.updated_at = utcNow();
  writeCompletionJournal(track, journal);
  appendJsonl(import_node_path32.default.join(track.dir, "completion_journal.jsonl"), {
    key,
    recorded_at: journal.updated_at,
    entry: journal.entries[key]
  });
  return journal.entries[key];
}
function manualVerificationChecksFromInput(value) {
  if (Array.isArray(value)) {
    return value.map((entry, index) => {
      if (typeof entry === "string") return { id: `check-${index + 1}`, heading: entry, status: "reported" };
      return asJsonObject(entry);
    }).filter((entry) => Object.keys(entry).length > 0);
  }
  if (typeof value === "string" && value.trim()) {
    return value.split(/\r?\n/).map((line) => line.trim()).filter(Boolean).map((line, index) => ({ id: `check-${index + 1}`, heading: line, status: "reported" }));
  }
  const object = asJsonObject(value);
  if (Array.isArray(object.checks)) return manualVerificationChecksFromInput(object.checks);
  return Object.keys(object).length > 0 ? [object] : [];
}
function manualVerificationResultObject(value) {
  if (typeof value === "string" && value.trim()) return { summary: value.trim() };
  return asJsonObject(value);
}
function manualVerificationInputResult(args) {
  return manualVerificationResultObject(args.manualVerificationResult || args.manual_verification_result);
}
function manualVerificationInputSummary(args, result) {
  return asOptionalString(args.manualVerificationSummary || args.manual_verification_summary) || asOptionalString(result.summary) || asOptionalString(result.message) || "";
}
function manualVerificationInputChecks(args, result) {
  return manualVerificationChecksFromInput(args.manualVerificationChecks || args.manual_verification_checks || result.checks);
}
function manualVerificationCommand(args) {
  return asOptionalString(args.manualVerificationCommand || args.manual_verification_command) || null;
}
function commandResultEvidence(result) {
  return {
    ok: result.ok,
    status: result.status,
    signal: result.signal || null,
    command: result.command,
    cwd: result.cwd || null,
    stdout_tail: result.stdout.slice(-4e3),
    stderr_tail: result.stderr.slice(-4e3)
  };
}
function prepareManualVerificationCompletion(root, track, task, args, workingRoot) {
  const mode2 = asOptionalString(args.manualVerificationMode || args.manual_verification_mode) || (manualVerificationCommand(args) ? "autorun" : "offline");
  const command = manualVerificationCommand(args);
  const suggestedChecks = asArray(asJsonObject(task.manual_verification).suggested_checks).map(asJsonObject);
  const inputResult = manualVerificationInputResult(args);
  const confirmed = humanReviewConfirmed(args);
  if (mode2 === "autorun" && !confirmed) {
    if (!command) {
      return {
        ok: false,
        blocked: true,
        stage: "manual_verification",
        reason: "manualVerificationCommand is required for autorun manual verification"
      };
    }
    const result = runCommand(command, [], {
      cwd: workingRoot.path,
      shell: true,
      timeoutMs: Number(args.timeoutMs || 10 * 60 * 1e3),
      maxBuffer: 30 * 1024 * 1024
    });
    const evidence2 = {
      mode: mode2,
      approved: false,
      task_key: task.task_key,
      scope: manualVerificationScope(task),
      summary: result.ok ? "Autorun manual verification command completed successfully." : "Autorun manual verification command failed.",
      suggested_checks: suggestedChecks,
      checks: manualVerificationInputChecks(args, inputResult),
      command,
      result: commandResultEvidence(result),
      recorded_at: utcNow()
    };
    return {
      ok: false,
      dry_run: true,
      blocked: true,
      phase_state: "awaiting_human_review",
      stage: "manual_verification_approval",
      track_id: track.track_id,
      task_key: task.task_key,
      manual_verification: evidence2,
      reason: "Human approval is required before marking manual verification complete"
    };
  }
  if (!confirmed) {
    return {
      ok: false,
      dry_run: true,
      blocked: true,
      phase_state: "awaiting_human_review",
      stage: "manual_verification_approval",
      track_id: track.track_id,
      task_key: task.task_key,
      manual_verification: {
        mode: mode2,
        approved: false,
        task_key: task.task_key,
        scope: manualVerificationScope(task),
        suggested_checks: suggestedChecks
      },
      reason: "Human approval is required before marking manual verification complete"
    };
  }
  let commandEvidence = isRecord(inputResult.result) ? asJsonObject(inputResult.result) : null;
  if (mode2 === "autorun" && command && !commandEvidence) {
    const result = runCommand(command, [], {
      cwd: workingRoot.path,
      shell: true,
      timeoutMs: Number(args.timeoutMs || 10 * 60 * 1e3),
      maxBuffer: 30 * 1024 * 1024
    });
    commandEvidence = commandResultEvidence(result);
  }
  const summary = manualVerificationInputSummary(args, inputResult) || (commandEvidence ? commandEvidence.ok === true ? "Approved autorun manual verification result." : "Approved autorun manual verification result with failures noted." : "");
  if (!summary.trim()) {
    return {
      ok: false,
      blocked: true,
      stage: "manual_verification_summary",
      track_id: track.track_id,
      task_key: task.task_key,
      reason: "manualVerificationSummary or manualVerificationResult.summary is required"
    };
  }
  const evidence = {
    mode: mode2,
    approved: true,
    approved_at: utcNow(),
    approved_by: gitIdentity(root) || null,
    task_key: task.task_key,
    scope: manualVerificationScope(task),
    summary: summary.trim(),
    suggested_checks: suggestedChecks,
    checks: manualVerificationInputChecks(args, inputResult),
    ...command ? { command } : {},
    ...commandEvidence ? { result: commandEvidence } : {},
    recorded_at: utcNow()
  };
  return { ok: true, evidence };
}

// src/core/application/runtime/task-completion.ts
function completeTask(root, args = {}) {
  return withSharedControlPlaneSync(root, args, "complete_task", () => completeTaskInner(root, { ...args, skipSync: true }));
}
function completeTaskInner(root, args = {}) {
  const track = findTrack(root, args.trackId);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId}` };
  const phaseIndex = Number(args.phaseIndex);
  const taskIndex = Number(args.taskIndex);
  const plan = parsePlanFile(track.plan_path);
  const phase = (plan.phases || []).find((item) => item.phase_index === phaseIndex);
  const task = phase && (phase.tasks || []).find((item) => item.task_index === taskIndex);
  if (!task) return { ok: false, error: `Task not found: phase ${phaseIndex} task ${taskIndex}` };
  const workingRoot = resolveTaskWorkingRoot(root, track, task, args);
  if (isWorkingRootError(workingRoot)) {
    return {
      ok: false,
      stage: "polyrepo_repo_resolution",
      blocked: true,
      working_root: workingRoot,
      reason: workingRoot.error
    };
  }
  const manualVerificationTask = isManualVerificationTaskObject(task);
  const manualVerificationCompletion = manualVerificationTask ? prepareManualVerificationCompletion(root, track, task, args, workingRoot) : null;
  if (manualVerificationCompletion && manualVerificationCompletion.ok === false) return manualVerificationCompletion;
  const manualVerificationEvidence = manualVerificationCompletion && isRecord(manualVerificationCompletion.evidence) ? asJsonObject(manualVerificationCompletion.evidence) : null;
  const coverage = manualVerificationTask ? {
    ok: true,
    available: false,
    command: null,
    coverage: null,
    reason: "Manual verification task uses structured human-approved evidence instead of coverage."
  } : runCoverage(root, args, workingRoot.path);
  const threshold = Number(args.coverageThreshold ?? coverageThreshold(root));
  const allowMissingCoverage = args.allowMissingCoverage === true;
  const allowLowCoverage = args.allowLowCoverage === true;
  if (!manualVerificationTask && !coverage.available && !allowMissingCoverage) {
    return {
      ok: false,
      stage: "coverage",
      blocked: true,
      threshold,
      coverage,
      reason: coverage.reason || "Coverage command unavailable"
    };
  }
  if (!manualVerificationTask && coverage.available && !coverage.ok) {
    return {
      ok: false,
      stage: "coverage",
      blocked: true,
      threshold,
      coverage,
      reason: "Coverage/test command failed; task was not marked complete"
    };
  }
  if (!manualVerificationTask && coverage.available && typeof coverage.coverage === "number" && coverage.coverage < threshold && !allowLowCoverage) {
    return {
      ok: false,
      stage: "coverage",
      blocked: true,
      threshold,
      coverage,
      reason: `Coverage ${coverage.coverage}% is below required ${threshold}%; task was not marked complete`
    };
  }
  const lastTestRun = manualVerificationTask ? null : {
    command: coverage.command,
    cwd: coverage.cwd || workingRoot.path,
    ok: coverage.available ? coverage.ok : null,
    status: coverage.available ? coverage.status : null,
    signal: coverage.available ? coverage.signal : null,
    coverage: coverage.coverage,
    threshold,
    measured_at: utcNow(),
    allow_missing_coverage: allowMissingCoverage,
    allow_low_coverage: allowLowCoverage
  };
  const sha = args.commitSha ? String(args.commitSha).slice(0, 12) : "unknown";
  const dedupKey = `key: ${track.track_id}:p${phaseIndex}:t${taskIndex}:${sha.slice(0, 7)}`;
  const journalKey = `${phaseIndex}:${taskIndex}:${sha}`;
  const recordState = () => {
    const entry = patchCompletionJournal(track, journalKey, (current) => ({
      ...current,
      stage: current.stage || "started",
      track_id: track.track_id,
      phase_index: phaseIndex,
      task_index: taskIndex,
      task_key: task.task_key,
      commit_sha: sha,
      dedup_key: dedupKey,
      started_at: current.started_at || utcNow()
    }));
    const taskResult = recordTaskResultUnlocked(root, {
      trackId: args.trackId,
      phaseIndex,
      taskIndex,
      status: args.status || "completed",
      commitSha: args.commitSha,
      coverage: coverage.coverage,
      repo: workingRoot.repo,
      workingRoot: import_node_path33.default.relative(root, workingRoot.path) || ".",
      ...lastTestRun ? { lastTestRun } : {},
      ...manualVerificationEvidence ? { manualVerificationEvidence } : {}
    });
    if (!taskResult.ok) {
      patchCompletionJournal(track, journalKey, (current) => ({
        ...current,
        stage: "record_task_result_failed",
        error: taskResult.error || asOptionalString(taskResult.stage) || "record task result failed"
      }));
      return { ok: false, stage: "record_task_result", threshold, working_root: workingRoot, coverage, task_result: taskResult, journal: entry };
    }
    const taskResultJson = asJsonObject(taskResult);
    const recordedEntry = patchCompletionJournal(track, journalKey, (current) => ({
      ...current,
      stage: "state_recorded",
      state_recorded_at: utcNow(),
      task_result: {
        task_key: taskResultJson.task_key,
        commit_sha: taskResultJson.commit_sha,
        line: taskResultJson.line
      }
    }));
    return { ok: true, task_result: taskResult, journal: recordedEntry };
  };
  const stateResult = args.lock === false ? recordState() : withTrackLock(root, track.track_id, recordState);
  if (!stateResult.ok) return { ...stateResult, threshold, working_root: workingRoot, coverage };
  const stateTaskResult = asJsonObject(stateResult.task_result);
  const markComplete = () => patchCompletionJournal(track, journalKey, (current) => ({
    ...current,
    stage: "completed",
    completed_at: current.completed_at || utcNow()
  }));
  const completedJournal = args.lock === false ? markComplete() : withTrackLock(root, track.track_id, markComplete);
  const event = appendCadreEvent(root, {
    kind: "task_completed",
    workflow: "complete_task",
    track_id: track.track_id,
    phase_index: phaseIndex,
    task_index: taskIndex,
    task_key: stateTaskResult.task_key,
    status: args.status || "completed",
    commit_sha: sha,
    coverage: coverage.coverage ?? null,
    summary: args.summary || null,
    journal_key: journalKey
  });
  return {
    ok: true,
    track_id: track.track_id,
    task_key: stateTaskResult.task_key,
    working_root: workingRoot,
    threshold,
    coverage,
    task_result: stateTaskResult,
    event,
    journal: completedJournal.ok === false ? completedJournal : completedJournal.value || completedJournal
  };
}

// src/core/application/runtime/parallel-state.ts
function recordParallelWorker(root, args = {}) {
  return withSharedControlPlaneSync(root, args, "record_parallel_worker", () => recordParallelWorkerInner(root, args));
}
function recordParallelWorkerInner(root, args = {}) {
  const track = findTrack(root, args.trackId);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId}` };
  return withTrackLock(root, track.track_id, () => recordParallelWorkerUnlocked(root, track, args));
}
function recordParallelWorkerUnlocked(root, track, args = {}) {
  const workerId = args.workerId || args.worker_id;
  if (!workerId) return { ok: false, error: "workerId is required" };
  const status = args.status || "awaiting_merge";
  const valid = /* @__PURE__ */ new Set(["in_progress", "awaiting_merge", "merged", "conflict", "failed"]);
  if (!valid.has(status)) return { ok: false, error: `Invalid parallel worker status: ${status}` };
  if (status === "awaiting_merge" && !args.commitSha && !args.commit && args.allowNoCommit !== true) {
    return { ok: false, error: "commitSha is required before a parallel worker can move to awaiting_merge" };
  }
  const statePath = import_node_path34.default.join(track.dir, "parallel_state.json");
  const existing = readJson(statePath, {
    track_id: track.track_id,
    execution_mode: "parallel",
    started_at: utcNow(),
    workers: []
  });
  const existingObject = isRecord(existing) ? asJsonObject(existing) : {};
  const state = {
    ...existingObject,
    track_id: asOptionalString(existingObject.track_id) || track.track_id,
    execution_mode: asOptionalString(existingObject.execution_mode) || "parallel",
    started_at: asOptionalString(existingObject.started_at) || utcNow(),
    workers: Array.isArray(existingObject.workers) ? existingObject.workers.map((worker) => asJsonObject(worker)) : []
  };
  const now = utcNow();
  const index = state.workers.findIndex((worker) => worker.worker_id === workerId);
  const existingWorker = index >= 0 ? state.workers[index] : void 0;
  const nextWorker = {
    ...existingWorker || {},
    worker_id: workerId,
    status,
    phase_index: args.phaseIndex ?? existingWorker?.phase_index ?? null,
    task_index: args.taskIndex ?? existingWorker?.task_index ?? null,
    task_key: args.phaseIndex && args.taskIndex ? `phase${args.phaseIndex}_task${args.taskIndex}` : existingWorker?.task_key ?? null,
    repo: args.repo || existingWorker?.repo || null,
    worktree: args.worktree || existingWorker?.worktree || null,
    branch: args.branch || existingWorker?.branch || null,
    commit_sha: args.commitSha || existingWorker?.commit_sha || null,
    coverage: typeof args.coverage === "number" ? args.coverage : existingWorker?.coverage ?? null,
    evidence: args.evidence || existingWorker?.evidence || null,
    files_changed: asStringArray(args.filesChanged || args.files_changed || existingWorker?.files_changed),
    tests: Array.isArray(args.tests) ? args.tests.map((test) => asJsonObject(test)) : Array.isArray(existingWorker?.tests) ? existingWorker.tests : [],
    summary: asOptionalString(args.summary) || asOptionalString(existingWorker?.summary) || null,
    blockers: asStringArray(args.blockers || existingWorker?.blockers),
    updated_at: now
  };
  if (status === "awaiting_merge" && !nextWorker.completed_at) nextWorker.completed_at = now;
  if (status === "merged") nextWorker.merged_at = now;
  if (status === "conflict") nextWorker.conflict_at = now;
  if (index >= 0) state.workers[index] = nextWorker;
  else state.workers.push(nextWorker);
  state.completed_workers = state.workers.filter((worker) => ["awaiting_merge", "merged"].includes(worker.status)).length;
  state.merged_workers = state.workers.filter((worker) => worker.status === "merged").length;
  state.conflict_workers = state.workers.filter((worker) => worker.status === "conflict").length;
  state.updated_at = now;
  let completion = null;
  if (args.completeTask === true) {
    completion = completeTask(root, {
      trackId: track.track_id,
      phaseIndex: args.phaseIndex,
      taskIndex: args.taskIndex,
      commitSha: args.commitSha,
      command: args.command,
      timeoutMs: args.timeoutMs,
      coverageThreshold: args.coverageThreshold,
      allowMissingCoverage: args.allowMissingCoverage,
      allowLowCoverage: args.allowLowCoverage,
      summary: args.summary || `parallel worker ${workerId}`,
      reason: args.reason || `merged ${workerId}`,
      repo: nextWorker.repo || args.repo,
      workingRoot: args.workingRoot || nextWorker.worktree || args.worktree,
      lock: false
    });
    if (!completion.ok) return { ok: false, stage: "complete_task", state_path: statePath, worker: nextWorker, completion };
  }
  writeJson(statePath, state);
  return {
    ok: true,
    track_id: track.track_id,
    state_path: import_node_path34.default.relative(root, statePath),
    worker: nextWorker,
    completion,
    summary: {
      total_workers: state.workers.length,
      completed_workers: state.completed_workers,
      merged_workers: state.merged_workers,
      conflict_workers: state.conflict_workers
    }
  };
}
function parallelStatePath(track) {
  return import_node_path34.default.join(track.dir, "parallel_state.json");
}
function readParallelState(track) {
  const existing = readJson(parallelStatePath(track), {
    track_id: track.track_id,
    execution_mode: "parallel",
    started_at: utcNow(),
    workers: []
  });
  const existingObject = isRecord(existing) ? asJsonObject(existing) : {};
  return {
    ...existingObject,
    track_id: asOptionalString(existingObject.track_id) || track.track_id,
    execution_mode: asOptionalString(existingObject.execution_mode) || "parallel",
    started_at: asOptionalString(existingObject.started_at) || utcNow(),
    workers: Array.isArray(existingObject.workers) ? existingObject.workers.map((worker) => asJsonObject(worker)) : []
  };
}

// src/core/application/runtime/parallel-workflow.ts
var import_node_path35 = __toESM(require("node:path"));
function positiveInt(value, fallback, max = 20) {
  const parsed = Number(value);
  if (!Number.isFinite(parsed) || parsed <= 0) return fallback;
  return Math.max(1, Math.min(max, Math.floor(parsed)));
}
function humanConfirmed(args) {
  return args.humanConfirmed === true || args.human_confirmed === true;
}
function changedFilesFromArgs(args) {
  return asStringArray(args.filesChanged || args.files_changed || args.files);
}
function isManifestChange(file) {
  const base = import_node_path35.default.basename(file).toLowerCase();
  return [
    "package.json",
    "package-lock.json",
    "pnpm-lock.yaml",
    "yarn.lock",
    "bun.lockb",
    "pyproject.toml",
    "poetry.lock",
    "requirements.txt",
    "cargo.toml",
    "cargo.lock",
    "go.mod",
    "go.sum",
    "pom.xml",
    "build.gradle",
    "settings.gradle",
    "tsconfig.json"
  ].includes(base);
}
function fileStem(file) {
  return import_node_path35.default.basename(file).replace(/\.(test|spec|_test)\.[^.]+$/i, "").replace(/\.[^.]+$/, "");
}
function isNarrowTestChange(file, ownedFiles) {
  const normalized = normalizeClaimPath(file);
  if (!/\b(test|tests|spec|specs|__tests__)\b|[._-](test|spec)\.[^.]+$/i.test(normalized)) return false;
  const stem = fileStem(normalized);
  return ownedFiles.some((owned) => {
    const ownedStem = fileStem(normalizeClaimPath(owned));
    return stem === ownedStem || stem.includes(ownedStem) || ownedStem.includes(stem);
  });
}
function planTaskForWorker(track, args) {
  const plan = parsePlanFile(track.plan_path);
  const phaseIndex = Number(args.phaseIndex ?? args.phase_index ?? 0);
  const taskIndex = Number(args.taskIndex ?? args.task_index ?? 0);
  const workerId = asOptionalString(args.workerId || args.worker_id);
  for (const phase of plan.phases) {
    for (const task of phase.tasks) {
      if (phaseIndex && taskIndex && phase.phase_index === phaseIndex && task.task_index === taskIndex) return task;
      if (workerId && `${track.track_id}_${task.task_key}` === workerId) return task;
      if (workerId && task.task_key === workerId) return task;
    }
  }
  return null;
}
function validateWorkerFinishEvidence(root, track, args = {}) {
  const changed = changedFilesFromArgs(args).map(normalizeClaimPath).filter(Boolean);
  if (changed.length === 0) {
    return {
      ok: true,
      checked: false,
      reason: "filesChanged was not supplied; compatibility mode accepted the worker record without file evidence validation"
    };
  }
  const task = planTaskForWorker(track, args);
  const ownedFiles = asStringArray(task?.files).map(normalizeClaimPath).filter(Boolean);
  const likelyTests = Array.from(new Set(ownedFiles.flatMap((file) => likelyTestCandidatesForFile(root, file)).map(normalizeClaimPath)));
  const allowed = changed.filter(
    (file) => ownedFiles.some((owned) => claimsOverlap(file, owned)) || likelyTests.includes(file) || isNarrowTestChange(file, ownedFiles) || isManifestChange(file)
  );
  const violations = changed.filter((file) => !allowed.includes(file));
  const forceAccepted = violations.length > 0 && args.force === true && humanConfirmed(args);
  return {
    ok: violations.length === 0 || forceAccepted,
    checked: true,
    force_accepted: forceAccepted,
    worker_id: args.workerId || args.worker_id || null,
    task_key: task?.task_key || null,
    owned_files: ownedFiles,
    likely_tests: likelyTests,
    files_changed: changed,
    allowed_files_changed: allowed,
    unowned_files_changed: violations,
    reason: violations.length === 0 ? "All changed files are owned, likely related tests, or narrow manifest changes" : forceAccepted ? "Unowned changed files accepted because force and humanConfirmed were supplied" : "Changed files include paths outside the worker ownership claim"
  };
}
function parallelWorkersForWave(root, track, args = {}) {
  const schedule = phaseSchedule(root, { ...args, trackId: track.track_id });
  if (schedule.ok === false) return schedule;
  const readyGroups = Array.isArray(schedule.ready_groups) ? schedule.ready_groups : [];
  const groupIndex = Number(args.groupIndex || 0);
  const phaseIds = asStringArray(readyGroups[groupIndex]);
  const plan = parsePlanFile(track.plan_path);
  const state = readParallelState(track);
  const activeWorkers = state.workers.filter(
    (worker) => ["in_progress", "awaiting_merge", "merged"].includes(worker.status)
  );
  const maxWorkers = positiveInt(args.maxWorkers || args.limit, 8);
  const activeSlotCount = activeWorkers.filter((worker) => ["in_progress", "awaiting_merge"].includes(worker.status)).length;
  const availableSlots = Math.max(0, maxWorkers - activeSlotCount);
  const activeTaskKeys = new Set(activeWorkers.map((worker) => asOptionalString(worker.task_key)).filter(Boolean));
  const completeTaskKeys = new Set(
    plan.tasks.filter((task) => ["x", "-"].includes(task.marker)).map((task) => task.task_key).concat(activeWorkers.filter((worker) => ["awaiting_merge", "merged"].includes(worker.status)).map((worker) => asString(worker.task_key)).filter(Boolean))
  );
  const activeClaims = activeWorkers.flatMap((worker) => {
    const phase = plan.phases.find((item) => item.phase_index === worker.phase_index);
    const task = phase?.tasks.find((item) => item.task_index === worker.task_index || item.task_key === worker.task_key);
    return (task?.files || []).map((file) => ({
      repo: asOptionalString(worker.repo) || task?.repo || ".",
      file: normalizeClaimPath(file),
      worker_id: worker.worker_id,
      task_key: worker.task_key
    }));
  });
  const normalizeTaskDependency = (phase, dep) => {
    const taskMatch = dep.match(/^task(\d+)$/i);
    if (taskMatch?.[1]) return `phase${phase.phase_index}_task${taskMatch[1]}`;
    const phaseTaskMatch = dep.match(/^phase(\d+)_task(\d+)$/i);
    if (phaseTaskMatch?.[1] && phaseTaskMatch[2]) return `phase${phaseTaskMatch[1]}_task${phaseTaskMatch[2]}`;
    return dep;
  };
  const taskIsReady = (phase, task) => {
    if (["x", "-", "!", "~"].includes(task.marker)) return false;
    if (activeTaskKeys.has(task.task_key)) return false;
    const dependencies = (task.depends || []).map((dep) => normalizeTaskDependency(phase, dep));
    if (dependencies.some((dep) => !completeTaskKeys.has(dep))) return false;
    const taskClaims = (task.files || []).map((file) => ({
      repo: task.repo || loadTopology(root).defaultRepo || ".",
      file: normalizeClaimPath(file)
    }));
    return taskClaims.every(
      (claim) => activeClaims.every((active) => claim.repo !== active.repo || !claimsOverlap(claim.file, active.file))
    );
  };
  const readyTasksForPhase = (phase) => {
    const execution = asString(phase.annotations.execution, "sequential");
    if (execution === "parallel") return phase.tasks.filter((task) => taskIsReady(phase, task));
    const firstOpen = phase.tasks.find((task) => !["x", "-"].includes(task.marker));
    return firstOpen && taskIsReady(phase, firstOpen) ? [firstOpen] : [];
  };
  const phases = plan.phases.filter((phase) => phaseIds.includes(`phase${phase.phase_index}`));
  const candidateWorkers = phases.flatMap((phase) => readyTasksForPhase(phase).map((task) => ({
    worker_id: `${track.track_id}_${asString(task.task_key)}`,
    phase_id: `phase${phase.phase_index}`,
    phase_index: phase.phase_index,
    task_index: task.task_index,
    task_key: asString(task.task_key),
    title: asString(task.title),
    marker: asString(task.marker),
    repo: asString(task.repo, loadTopology(root).defaultRepo || "."),
    files: asStringArray(task.files),
    branch: `${track.metadata.git_branch || `track/${track.track_id}`}-${safeName(task.task_key)}`
  }))).filter((worker) => !["x", "-"].includes(worker.marker));
  const workers = candidateWorkers.slice(0, availableSlots);
  return {
    ok: true,
    track_id: track.track_id,
    schedule,
    state,
    group_index: groupIndex,
    phase_ids: phaseIds,
    max_workers: maxWorkers,
    active_worker_slots: activeSlotCount,
    available_worker_slots: availableSlots,
    candidate_workers_count: candidateWorkers.length,
    limited: workers.length < candidateWorkers.length,
    workers
  };
}
function plannedCommand(command, args, cwd) {
  return { command, args, cwd };
}
function runPlannedCommands(commands) {
  return commands.map((entry) => runCommand(asString(entry.command), asStringArray(entry.args), { cwd: asString(entry.cwd) }));
}
function workerDispatchPayload(root, track, worker, worktree, sourceRoot, agentIdentifier) {
  const workerId = asString(worker.worker_id);
  const taskKey = asString(worker.task_key);
  const repo = asString(worker.repo, ".");
  const ownedFiles = asStringArray(worker.files);
  const selectedDispatch = agentIdentifier === "claude" ? { agent_identifier: "claude", mechanism: "Task", instruction: "Call the Task tool once for this prompt and dispatch payload. Return the result JSON to the coordinator." } : { agent_identifier: "codex", mechanism: "multi_agent_v1.spawn_agent", instruction: "Use tool discovery for multi_agent_v1.spawn_agent, pass this prompt and dispatch payload, then wait for completion." };
  const prompt = [
    `You are a Cadre parallel worker for track ${track.track_id}.`,
    `Worker: ${workerId}`,
    `Task: ${taskKey} - ${asString(worker.title)}`,
    `Repo: ${repo}`,
    `Source root: ${sourceRoot}`,
    `Worker worktree: ${worktree}`,
    ownedFiles.length > 0 ? `Owned files: ${ownedFiles.join(", ")}` : "Owned files: none declared; inspect the task plan before editing.",
    "Use only Cadre packets for Cadre, provider, index, and worker-state mutations.",
    "Change only the assigned product files unless the task requires a narrowly related test or manifest update.",
    "Run the smallest relevant tests first, then the configured project gate when practical.",
    "Commit the worker worktree changes and return the structured result JSON."
  ].join("\n");
  const recordFinishArguments = {
    root,
    action: "record_finish",
    trackId: track.track_id,
    workerId,
    status: "awaiting_merge",
    phaseIndex: worker.phase_index,
    taskIndex: worker.task_index,
    repo,
    commitSha: "<commit-sha>",
    coverage: "<coverage-number-or-null>",
    filesChanged: ["<changed-file>"],
    tests: [{ command: "<test-command>", cwd: worktree, ok: true, status: 0 }],
    summary: "<worker-summary>",
    blockers: []
  };
  return {
    prompt,
    canonical_worker_contract: "cadre_parallel.dispatch.v1",
    repo,
    worktree,
    source_root: sourceRoot,
    owned_files: ownedFiles,
    agent_identifier: agentIdentifier,
    selected_dispatch: selectedDispatch,
    expected_result_schema: {
      type: "object",
      required: ["worker_id", "task_key", "repo", "status", "summary", "files_changed", "tests", "commit_sha"],
      properties: {
        worker_id: { type: "string" },
        task_key: { type: "string" },
        repo: { type: "string" },
        status: { type: "string", enum: ["awaiting_merge", "blocked"] },
        summary: { type: "string" },
        files_changed: { type: "array", items: { type: "string" } },
        tests: { type: "array", items: { type: "object" } },
        coverage: { type: ["number", "null"] },
        commit_sha: { type: ["string", "null"] },
        blockers: { type: "array", items: { type: "string" } }
      }
    },
    evidence_requirements: {
      commit: "Required unless blocked before code changes; record the commit SHA in record_finish.",
      tests: "Include every command run, cwd, exit status, and relevant stdout/stderr tail.",
      coverage: "Include parsed coverage when available or a reason coverage was not produced."
    },
    record_finish_packet: {
      tool: "cadre_parallel",
      arguments: recordFinishArguments
    },
    finish_evidence_fields: Object.keys(recordFinishArguments)
  };
}
function parallelSetupWorkers(root, track, args = {}) {
  const agentIdentifier = asOptionalString(args.agentIdentifier);
  if (agentIdentifier !== "claude" && agentIdentifier !== "codex") return { ok: false, action: "setup_workers", error: 'cadre_parallel setup_workers requires agentIdentifier "claude" or "codex"', accepted_agent_identifiers: ["claude", "codex"] };
  const wave = parallelWorkersForWave(root, track, args);
  if (wave.ok === false) return wave;
  const topology = loadTopology(root);
  const entries = new Map(repoEntriesForTrack(root, track, args).map((entry) => [entry.repo, entry]));
  const commands = [];
  const workers = asArray(wave.workers).map((rawWorker) => {
    const worker = asJsonObject(rawWorker);
    const repo = asString(worker.repo, ".");
    const entry = entries.get(repo) || { root, base: args.base || "main" };
    const worktree = import_node_path35.default.resolve(root, ".worktrees", track.track_id, safeName(repo), safeName(worker.task_key));
    const sourceRoot = asString(entry.root || root);
    commands.push(plannedCommand(
      "git",
      ["worktree", "add", "-B", asString(worker.branch), worktree, asString(entry.base || args.base || "main")],
      sourceRoot
    ));
    return {
      ...worker,
      worktree,
      source_root: sourceRoot,
      dispatch: workerDispatchPayload(root, track, worker, worktree, sourceRoot, agentIdentifier)
    };
  });
  const execute = args.execute === true;
  const results = execute ? runPlannedCommands(commands) : [];
  const stateRecords = [];
  if (execute) {
    workers.forEach((worker, index) => {
      const commandResult = results[index];
      if (commandResult && commandResult.ok) {
        stateRecords.push(recordParallelWorker(root, {
          ...args,
          skipSync: true,
          trackId: track.track_id,
          workerId: asString(worker.worker_id),
          status: "in_progress",
          phaseIndex: asNumber(worker.phase_index),
          taskIndex: asNumber(worker.task_index),
          repo: asString(worker.repo, "."),
          worktree: asString(worker.worktree),
          branch: asString(worker.branch)
        }));
      }
    });
  }
  return {
    ok: results.every((result) => result.ok) && stateRecords.every((record) => record.ok !== false),
    track_id: track.track_id,
    action: "setup_workers",
    execute,
    dry_run: !execute,
    topology: topology.polyrepo ? "polyrepo" : "monorepo",
    workers,
    commands,
    results,
    state_records: stateRecords
  };
}
function workerRepoRoot(root, track, worker, args = {}) {
  const repo = asOptionalString(worker.repo) || asOptionalString(args.repo) || ".";
  const entry = repoEntriesForTrack(root, track, { ...args, repo }).find((item) => item.repo === repo);
  return asString(entry?.root, root);
}
function parallelMergeBack(root, track, args = {}) {
  const state = readParallelState(track);
  const force = args.force === true;
  const workers = state.workers.filter((worker) => !args.workerId || worker.worker_id === args.workerId).filter((worker) => force || worker.status === "awaiting_merge");
  const skipped = state.workers.filter((worker) => !args.workerId || worker.worker_id === args.workerId).filter((worker) => !workers.includes(worker)).map((worker) => ({ worker_id: worker.worker_id, status: worker.status, reason: "worker is not awaiting_merge" }));
  const commands = workers.filter((worker) => worker.branch || worker.commit_sha).map((worker) => plannedCommand("git", ["merge", "--no-ff", asString(worker.commit_sha || worker.branch)], workerRepoRoot(root, track, worker, args)));
  const execute = args.execute === true;
  const results = execute ? runPlannedCommands(commands) : [];
  const stateRecords = [];
  if (execute) {
    workers.forEach((worker, index) => {
      const result = results[index];
      if (result && result.ok) {
        const recordArgs = {
          ...args,
          skipSync: true,
          trackId: track.track_id,
          workerId: worker.worker_id,
          status: "merged"
        };
        if (worker.phase_index != null) recordArgs.phaseIndex = worker.phase_index;
        if (worker.task_index != null) recordArgs.taskIndex = worker.task_index;
        if (worker.repo) recordArgs.repo = worker.repo;
        if (worker.worktree) recordArgs.worktree = worker.worktree;
        if (worker.branch) recordArgs.branch = worker.branch;
        if (worker.commit_sha) recordArgs.commitSha = worker.commit_sha;
        stateRecords.push(recordParallelWorker(root, recordArgs));
      }
    });
  }
  return {
    ok: results.every((result) => result.ok) && stateRecords.every((record) => record.ok !== false),
    track_id: track.track_id,
    action: "merge_back",
    execute,
    dry_run: !execute,
    workers,
    skipped,
    commands,
    results,
    state_records: stateRecords
  };
}
function parallelCleanup(root, track, args = {}) {
  const state = readParallelState(track);
  const force = args.force === true;
  const workers = state.workers.filter((worker) => worker.worktree && (force || worker.status === "merged"));
  const skipped = state.workers.filter((worker) => worker.worktree && !workers.includes(worker)).map((worker) => ({ worker_id: worker.worker_id, status: worker.status, reason: "worker is not merged" }));
  const commands = workers.map((worker) => plannedCommand("git", ["worktree", "remove", asString(worker.worktree)], workerRepoRoot(root, track, worker, args)));
  const execute = args.execute === true;
  const results = execute ? runPlannedCommands(commands) : [];
  return {
    ok: results.every((result) => result.ok),
    track_id: track.track_id,
    action: "cleanup",
    execute,
    dry_run: !execute,
    workers,
    skipped,
    commands,
    results
  };
}
function parallelWorkflow(root, args = {}) {
  const action = args.action || "plan";
  const mutating = ["setup_workers", "record_finish", "merge_back", "cleanup"].includes(action);
  if (mutating && args.execute === true && args.skipSync !== true) {
    return withSharedControlPlaneSync(
      root,
      args,
      `parallel:${action}`,
      () => parallelWorkflow(root, { ...args, skipSync: true })
    );
  }
  const track = findTrack(root, args.trackId || args.track_id);
  if (!track) return { ok: false, error: `Track not found: ${args.trackId || args.track_id}` };
  const repoError = repoEntriesError(root, track, args);
  if (repoError) return repoError;
  if (action === "plan") {
    const schedule = phaseSchedule(root, { ...args, trackId: track.track_id });
    return { ok: schedule.ok !== false, track_id: track.track_id, schedule, state: readParallelState(track) };
  }
  if (action === "next_wave") return parallelWorkersForWave(root, track, args);
  if (action === "setup_workers") return parallelSetupWorkers(root, track, args);
  if (action === "record_finish") {
    const evidenceValidation = validateWorkerFinishEvidence(root, track, args);
    if (args.execute !== true) {
      return {
        ok: true,
        track_id: track.track_id,
        action,
        dry_run: true,
        evidence_validation: evidenceValidation,
        planned_record: {
          worker_id: args.workerId || args.worker_id,
          status: args.status || "awaiting_merge",
          phase_index: args.phaseIndex ?? null,
          task_index: args.taskIndex ?? null,
          commit_sha: args.commitSha || null,
          files_changed: changedFilesFromArgs(args),
          tests: Array.isArray(args.tests) ? args.tests : [],
          summary: asOptionalString(args.summary) || null,
          blockers: asStringArray(args.blockers)
        }
      };
    }
    if (evidenceValidation.ok === false) return evidenceValidation;
    return recordParallelWorker(root, { ...args, trackId: track.track_id, status: args.status || "awaiting_merge" });
  }
  if (action === "merge_back") return parallelMergeBack(root, track, args);
  if (action === "cleanup") return parallelCleanup(root, track, args);
  return { ok: false, error: `Unknown parallel action: ${action}` };
}

// src/core/application/runtime/workflow-basic.ts
var import_node_fs19 = __toESM(require("node:fs"));
var import_node_path36 = __toESM(require("node:path"));
function workflowImplement(root, args = {}) {
  const prep = implementationPrep(root, {
    ...args,
    claim: args.claim === true || args.execute === true
  });
  const trackId = asOptionalString(prep.selected_track) || args.trackId || args.track_id || null;
  return {
    ...workflowSummary(root, "implement", args),
    ok: prep.ok !== false,
    prepare_implementation: prep,
    phase_schedule: trackId ? phaseSchedule(root, { ...args, trackId }) : null
  };
}
function workflowStatus(root, args = {}) {
  const mode2 = args.mode || args.view || args.status || "live";
  const summary = workflowSummary(root, "status", args);
  if (mode2 === "team" || args.mine === true) return { ...summary, ok: true, status: teamBoard(root, { ...args, mine: args.mine === true }) };
  if (mode2 === "fleet" || mode2 === "repos") return { ...summary, ok: true, status: fleetStatus(root, args) };
  if (mode2 === "available") return { ...summary, ok: true, status: availableWork(root) };
  if (mode2 === "collisions") return { ...summary, ok: true, status: collisionScan(root) };
  if (mode2 === "doctor") return { ...summary, ok: true, status: doctor(root, { hasCadreProject: true }) };
  return { ...summary, ok: true, status: liveStatus(root) };
}
function workflowReview(root, args = {}) {
  const trackId = selectedTrackId(root, args);
  const summary = workflowSummary(root, "review", args);
  if (!trackId) return { ...summary, ok: false, error: "trackId is required" };
  const context = trackContext(root, trackId);
  const review = reviewAssist(root, { ...args, trackId });
  const gate = reviewGate(root, trackId, args);
  const provider = args.includeProvider === false ? null : prCiStatus(root, { ...args, trackId });
  const pendingProvider = Boolean(provider && provider.ok === false);
  return {
    ...summary,
    ok: review.ok !== false,
    phase_state: pendingProvider ? "pending_provider" : summary.phase_state,
    track_context: context,
    review_assist: review,
    gate,
    provider,
    required_provider_mcp: provider && provider.ok === false ? provider.required_provider_mcp || null : null,
    required_evidence: provider && provider.ok === false ? provider.required_evidence || null : null,
    unsupported_reason: provider && provider.ok === false ? provider.unsupported_reason || provider.reason || null : null,
    next_actions: provider && Array.isArray(provider.next_actions) ? provider.next_actions : []
  };
}
function workflowValidate(root, args = {}) {
  const summary = workflowSummary(root, "validate", args);
  return {
    ...summary,
    ok: true,
    doctor: doctor(root, { hasCadreProject: true }),
    team: teamStatus(root),
    integrity: planIntegrity(root, args.trackId || args.track_id || null),
    collisions: collisionScan(root),
    fleet: fleetStatus(root, { includeCollisions: false }),
    native_state: nativeStateSummary(root)
  };
}
function workflowArchive(root, args = {}) {
  const summary = workflowSummary(root, "archive", args);
  const tracks = listTracks(root).filter(
    (track) => args.trackId || args.track_id ? track.track_id === (args.trackId || args.track_id) : (track.metadata.status || "new") === "completed"
  );
  if (tracks.length === 0) return { ...summary, ok: false, error: "No completed or selected track found" };
  const reviewArtifacts = [
    packetReviewArtifact("Archive scope", "workflow:archive", {
      track_count: tracks.length,
      tracks: tracks.map((track) => asJsonObject(metadataTrackSummary(track)))
    })
  ];
  const humanReview = humanReviewState("archive", args, reviewArtifacts);
  if (args.execute !== true) {
    return {
      ...summary,
      ok: true,
      dry_run: true,
      tracks: tracks.map((track) => asJsonObject(metadataTrackSummary(track))),
      human_review: humanReview,
      review_artifacts: reviewArtifacts
    };
  }
  if (!humanReviewConfirmed(args)) {
    return {
      ...summary,
      ok: false,
      dry_run: true,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      tracks: tracks.map((track) => asJsonObject(metadataTrackSummary(track))),
      human_review: humanReview,
      review_artifacts: reviewArtifacts,
      error: "Human confirmation is required before archiving tracks"
    };
  }
  const syncPre = syncControlPlane(root, { mode: "pre" });
  if (syncPre.ok === false) return { ...summary, ok: false, phase_state: "blocked", stage: "sync_pre", sync_pre: syncPre };
  const archived = [];
  const archiveRoot = import_node_path36.default.join(root, "cadre", "archive");
  import_node_fs19.default.mkdirSync(archiveRoot, { recursive: true });
  for (const track of tracks) {
    const target = import_node_path36.default.join(archiveRoot, track.track_id);
    if (fileExists(target)) {
      archived.push({ track_id: track.track_id, ok: false, error: "Archive target already exists" });
      continue;
    }
    import_node_fs19.default.renameSync(track.dir, target);
    archived.push({ track_id: track.track_id, ok: true, path: import_node_path36.default.relative(root, target) });
  }
  const regen = regenIndex(root);
  const syncPost = syncControlPlane(root, { mode: "post" });
  return {
    ...summary,
    ok: archived.every((item) => item.ok !== false) && regen.ok !== false && syncPost.ok !== false,
    phase_state: syncPost.ok === false ? "recovery_required" : "executed",
    dry_run: false,
    archived,
    regen,
    sync_pre: syncPre,
    sync_post: syncPost
  };
}
function workflowHandoff(root, args = {}) {
  const trackId = selectedTrackId(root, args);
  const summary = workflowSummary(root, "handoff", args);
  if (!trackId) return { ...summary, ok: false, error: "trackId is required" };
  const context = trackContext(root, trackId);
  if (context.ok === false) return { ...summary, ok: false, track_context: context };
  const track = findTrack(root, trackId);
  if (!track) return { ...summary, ok: false, track_context: context, error: `Track not found: ${trackId}` };
  const text = asOptionalString(args.handoffText) || [
    `# Handoff: ${trackId}`,
    "",
    `Updated: ${utcNow()}`,
    "",
    "Resume from the packet context returned by Cadre MCP."
  ].join("\n");
  const handoffPath = import_node_path36.default.join(track.dir, "HANDOFF.md");
  const handoffJsonPath = trackHandoffJsonPath(track);
  const handoffJson = markdownDocJson("handoff", text, { track_id: trackId });
  const reviewFiles = [
    jsonReviewFile(import_node_path36.default.relative(root, handoffJsonPath), "Track handoff canonical", "handoffText", handoffJson),
    textReviewFile(
      import_node_path36.default.relative(root, handoffPath),
      "Track handoff",
      "handoff.json",
      withGeneratedMarker(import_node_path36.default.relative(root, handoffJsonPath), "cadre.handoff.v1", renderMarkdownDoc(handoffJson, `Handoff: ${trackId}`))
    )
  ];
  const reviewArtifacts = reviewArtifactsFromFiles(reviewFiles);
  const reviewBundle = workflowReviewBundle(root, "handoff", args, reviewFiles, { track_id: trackId });
  const humanReview = humanReviewState("handoff", args, reviewArtifacts, reviewBundle);
  const warnings = asStringArray(asJsonObject(reviewBundle).warnings);
  const base = {
    ...summary,
    track_id: trackId,
    track_context: context,
    handoff_path: import_node_path36.default.relative(root, handoffPath),
    human_review: humanReview,
    review_artifacts: reviewArtifacts,
    review_bundle: reviewBundle,
    warnings
  };
  if (args.execute !== true) {
    return {
      ...base,
      ok: true,
      dry_run: true,
      phase_state: "dry_run"
    };
  }
  if (!humanReviewConfirmed(args)) {
    return {
      ...base,
      ok: false,
      dry_run: true,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      error: "Human confirmation is required before writing handoff artifacts"
    };
  }
  if (args.execute === true) {
    writeJsonEnsured(handoffJsonPath, handoffJson);
    import_node_fs19.default.writeFileSync(handoffPath, withGeneratedMarker(import_node_path36.default.relative(root, handoffJsonPath), "cadre.handoff.v1", renderMarkdownDoc(handoffJson, `Handoff: ${trackId}`)));
  }
  const recipient = asOptionalString(args.to || args.assignee || track.metadata.reviewer) || null;
  const subject = asOptionalString(args.subject) || `Handoff: ${trackId}`;
  const message = appendCadreMessage(root, "outbox", {
    kind: "handoff",
    workflow: "handoff",
    track_id: trackId,
    to: recipient,
    subject,
    body: asOptionalString(args.body) || text,
    handoff_path: import_node_path36.default.relative(root, handoffPath),
    handoff_json_path: import_node_path36.default.relative(root, handoffJsonPath)
  });
  const event = appendCadreEvent(root, {
    kind: "handoff_created",
    workflow: "handoff",
    track_id: trackId,
    to: recipient,
    subject,
    handoff_path: import_node_path36.default.relative(root, handoffPath)
  });
  return {
    ...base,
    ok: true,
    dry_run: args.execute !== true,
    phase_state: "executed",
    message,
    event
  };
}

// src/core/application/runtime/workflow-refresh-revert.ts
var import_node_fs20 = __toESM(require("node:fs"));
var import_node_path37 = __toESM(require("node:path"));
function refreshedPatternsText(text, now = utcNow()) {
  const stamp = `Last refreshed: ${now.slice(0, 10)}`;
  const next = /Last refreshed:\s*.*/.test(text) ? text.replace(/Last refreshed:\s*.*/, stamp) : `${text.replace(/\n*$/, "\n\n")}${stamp}
`;
  return { text: next, stamp };
}
function refreshedPatternsArtifacts(root) {
  const jsonlPath = import_node_path37.default.join(root, "cadre", "patterns.jsonl");
  if (!fileExists(jsonlPath)) return null;
  const entries = readJsonl(jsonlPath);
  const seed = entries[0] || templateJson("patterns_seed.json", { id: "initial", kind: "patterns_seed", text: "# Codebase Patterns\n\nLast refreshed: YYYY-MM-DD\n" });
  const currentText = asOptionalString(seed.text) || "# Codebase Patterns\n\nLast refreshed: YYYY-MM-DD\n";
  const next = refreshedPatternsText(currentText);
  const nextEntries = [{ ...seed, text: next.text, refreshed_at: utcNow() }, ...entries.slice(1)];
  const jsonl = nextEntries.map((entry) => JSON.stringify(entry)).join("\n").replace(/\n*$/, "\n");
  const projection = withGeneratedMarker("cadre/patterns.jsonl", "cadre.patterns.v1", renderJsonlMarkdown("Project patterns", nextEntries));
  const projectionPath = import_node_path37.default.join(root, "cadre", "patterns.md");
  return {
    files: [
      plainReviewFile(import_node_path37.default.relative(root, jsonlPath), "Refreshed project patterns canonical", "refresh:patterns", jsonl),
      textReviewFile(import_node_path37.default.relative(root, projectionPath), "Refreshed project patterns projection", "cadre/patterns.jsonl", projection)
    ],
    jsonlPath,
    projectionPath,
    jsonl,
    projection,
    stamp: next.stamp
  };
}
function refreshReviewFiles(root) {
  return refreshedPatternsArtifacts(root)?.files || [];
}
function revertGitActions(root, track, args = {}) {
  const plan = parsePlanFile(track.plan_path);
  const requestedCommit = asOptionalString(args.commitSha || args.commit);
  const commits = requestedCommit ? [requestedCommit] : Array.from(new Set(plan.tasks.flatMap((task) => asStringArray(task.commit_shas || []).concat(task.commit ? [task.commit] : [])))).filter(Boolean);
  const topology = loadTopology(root);
  return commits.reverse().map((commit, index) => {
    const repo = asOptionalString(args.repo) || (topology.polyrepo ? topology.defaultRepo : ".");
    const entry = repoEntriesForTrack(root, track, { ...args, repo }).find((item) => item.repo === repo);
    return plannedGitAction(
      `revert-${index + 1}`,
      "revert_commit",
      repo,
      entry ? asString(entry.root, root) : root,
      ["revert", "--no-edit", commit],
      `Revert ${commit} in ${repo}`
    );
  });
}
function workflowRevert(root, args = {}) {
  const trackId = selectedTrackId(root, args);
  const summary = workflowSummary(root, "revert", args);
  if (!trackId) return { ...summary, ok: false, phase_state: "blocked", error: "trackId is required" };
  const track = findTrack(root, trackId);
  if (!track) return { ...summary, ok: false, phase_state: "blocked", error: `Track not found: ${trackId}` };
  const repoError = repoEntriesError(root, track, args);
  if (repoError) return { ...summary, ok: false, phase_state: "blocked", stage: "polyrepo_repo_resolution", repo_error: repoError };
  const gitActions = revertGitActions(root, track, args);
  if (gitActions.length === 0) {
    return {
      ...summary,
      ok: false,
      phase_state: "blocked",
      track_context: trackContext(root, trackId),
      reason: "No commit evidence found to revert; pass commitSha or record task commits first",
      git_actions: gitActions
    };
  }
  const reviewArtifacts = [
    packetReviewArtifact("Revert scope", "workflow:revert", {
      track_id: trackId,
      git_actions: gitActions,
      reason: args.reason || null
    })
  ];
  const humanReview = humanReviewState("revert", args, reviewArtifacts);
  if (args.execute === true && !humanReviewConfirmed(args)) {
    return {
      ...summary,
      ok: false,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      dry_run: true,
      track_context: trackContext(root, trackId),
      git_actions: gitActions,
      human_review: humanReview,
      review_artifacts: reviewArtifacts,
      error: "Human confirmation is required before reverting tracked commits"
    };
  }
  const gitResults = args.execute === true ? runPlannedGitActions(gitActions) : [];
  const gitOk = actionResultsOk(gitResults);
  const statusResult = args.execute === true && gitOk ? metadataPatch(root, {
    trackId,
    patch: {
      status: "in_progress",
      last_revert: {
        reverted_at: utcNow(),
        commits: gitActions.map((action) => action.args[action.args.length - 1]).filter((commit) => typeof commit === "string"),
        reason: args.reason || null
      }
    }
  }) : null;
  return {
    ...summary,
    ok: args.execute === true ? gitOk && (!statusResult || statusResult.ok !== false) : true,
    phase_state: args.execute !== true ? "dry_run" : gitOk ? "executed" : "recovery_required",
    dry_run: args.execute !== true,
    track_context: trackContext(root, trackId),
    git_actions: gitActions,
    git_results: gitResults,
    metadata_patch: statusResult,
    human_review: humanReview,
    review_artifacts: reviewArtifacts
  };
}
function workflowRefresh(root, args = {}) {
  const summary = workflowSummary(root, "refresh", args);
  const reviewFiles = refreshReviewFiles(root);
  const reviewArtifacts = reviewArtifactsFromFiles(reviewFiles);
  if (setupLspWriteRequested(args)) reviewArtifacts.push(...setupLspReviewArtifacts(args));
  const reviewBundle = workflowReviewBundle(root, "refresh", args, reviewFiles);
  const humanReview = reviewArtifacts.length > 0 ? humanReviewState("refresh", args, reviewArtifacts, reviewBundle) : null;
  const warnings = asStringArray(asJsonObject(reviewBundle).warnings);
  const awaitingDocumentReview = args.execute === true && reviewFiles.length > 0 && !humanReviewConfirmed(args);
  const lspRequested = args.execute === true && !awaitingDocumentReview && setupLspWriteRequested(args);
  const lsp = lspRequested ? lspSetup(root, { ...args, execute: true }) : lspSetup(root, { ...args, execute: false });
  if (awaitingDocumentReview) {
    return {
      ...summary,
      ok: false,
      dry_run: true,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      doctor: doctor(root, { hasCadreProject: true }),
      workspace: workspaceDiagnostics(root, { execute: false }),
      dependency_graph: dependencyGraph(root),
      lsp: lspConfigStatus(root),
      lsp_setup: lsp,
      human_review: humanReview,
      review_artifacts: reviewArtifacts,
      review_bundle: reviewBundle,
      warnings,
      error: "Human confirmation is required before refreshing Cadre context documents"
    };
  }
  const regen = args.execute === true ? regenIndex(root) : null;
  let patterns = null;
  if (args.execute === true) {
    const refreshed = refreshedPatternsArtifacts(root);
    if (refreshed) {
      import_node_fs20.default.writeFileSync(refreshed.jsonlPath, refreshed.jsonl);
      import_node_fs20.default.writeFileSync(refreshed.projectionPath, refreshed.projection);
      patterns = {
        ok: true,
        path: import_node_path37.default.relative(root, refreshed.jsonlPath),
        projection: import_node_path37.default.relative(root, refreshed.projectionPath),
        refreshed_at: refreshed.stamp
      };
    }
  }
  return {
    ...summary,
    ok: (!regen || regen.ok !== false) && lsp.ok !== false,
    phase_state: args.execute === true ? "executed" : "dry_run",
    doctor: doctor(root, { hasCadreProject: true }),
    workspace: workspaceDiagnostics(root, { execute: false }),
    dependency_graph: dependencyGraph(root),
    lsp: lspConfigStatus(root),
    lsp_setup: lsp,
    regen,
    patterns,
    human_review: humanReview,
    review_artifacts: reviewArtifacts,
    review_bundle: reviewBundle,
    warnings
  };
}

// src/core/application/runtime/workflow-release-revise.ts
var import_node_fs21 = __toESM(require("node:fs"));
var import_node_path38 = __toESM(require("node:path"));
function releaseArtifactPlan(root, args = {}) {
  const completed = listTracks(root).filter((track) => (track.metadata.status || "new") === "completed").map((track) => asJsonObject(metadataTrackSummary(track)));
  const rawArgs = args;
  const version = String(args.releaseVersion || args.release_version || args.bump || args.mode || `release-${utcNow().slice(0, 10)}`);
  const generatedAt = asOptionalString(rawArgs.generatedAt || rawArgs.generated_at) || utcNow();
  const releaseDir = import_node_path38.default.join(root, "cadre", "releases");
  const releaseSlug = safeName(version);
  const releaseMd = import_node_path38.default.join(releaseDir, `${releaseSlug}.md`);
  const releaseJson = import_node_path38.default.join(releaseDir, `${releaseSlug}.json`);
  const notes = asOptionalString(args.releaseNotes || args.release_notes) || [
    `# Release - ${version}`,
    "",
    `Generated: ${generatedAt}`,
    "",
    "## Completed Tracks",
    "",
    ...completed.map((track) => `- ${track.track_id}: ${track.name}`),
    ""
  ].join("\n");
  const metadata = {
    version,
    generated_at: generatedAt,
    completed_tracks: completed.map((track) => ({
      track_id: track.track_id,
      name: track.name,
      status: track.status,
      priority: track.priority,
      owner: track.owner,
      reviewer: track.reviewer,
      tags: track.tags || [],
      review: track.review
    }))
  };
  const gitActions = rawArgs.createTag === true || rawArgs.create_tag === true || rawArgs.tag === true ? [plannedGitAction("release-tag", "tag_release", ".", root, ["tag", "-a", version, "-m", `Cadre release ${version}`], `Create release tag ${version}`)] : [];
  return { version, generatedAt, completed, releaseDir, releaseMd, releaseJson, notes, metadata, gitActions };
}
function releaseReviewFiles(root, plan) {
  return [
    textReviewFile(
      import_node_path38.default.relative(root, plan.releaseMd),
      "Release notes",
      "releaseNotes",
      plan.notes.endsWith("\n") ? plan.notes : `${plan.notes}
`
    ),
    jsonReviewFile(
      import_node_path38.default.relative(root, plan.releaseJson),
      "Release metadata",
      "releaseMetadata",
      plan.metadata
    )
  ];
}
function workflowRelease(root, args = {}) {
  const summary = workflowSummary(root, "release", args);
  const plan = releaseArtifactPlan(root, args);
  const reviewFiles = releaseReviewFiles(root, plan);
  const reviewArtifacts = reviewArtifactsFromFiles(reviewFiles);
  if (plan.gitActions.length > 0) {
    reviewArtifacts.push(packetReviewArtifact("Release git actions", "workflow:release", {
      git_actions: plan.gitActions
    }));
  }
  const reviewBundle = workflowReviewBundle(root, "release", args, reviewFiles, { release_version: plan.version });
  const humanReview = humanReviewState("release", args, reviewArtifacts, reviewBundle);
  const warnings = asStringArray(asJsonObject(reviewBundle).warnings);
  const base = {
    ...summary,
    release_version: plan.version,
    completed_tracks: plan.completed,
    release_artifacts: [import_node_path38.default.relative(root, plan.releaseMd), import_node_path38.default.relative(root, plan.releaseJson)],
    git_actions: plan.gitActions,
    human_review: humanReview,
    review_artifacts: reviewArtifacts,
    review_bundle: reviewBundle,
    warnings
  };
  if (args.execute !== true) {
    return {
      ...base,
      ok: true,
      phase_state: "dry_run",
      dry_run: true
    };
  }
  if (!humanReviewConfirmed(args)) {
    return {
      ...base,
      ok: false,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      dry_run: true,
      error: "Human confirmation is required before writing release artifacts"
    };
  }
  import_node_fs21.default.mkdirSync(plan.releaseDir, { recursive: true });
  import_node_fs21.default.writeFileSync(plan.releaseMd, plan.notes.endsWith("\n") ? plan.notes : `${plan.notes}
`);
  writeJson(plan.releaseJson, plan.metadata);
  const indexPatch = patchJsonFile(import_node_path38.default.join(root, "cadre", "setup_state.json"), (current) => {
    current.last_release = {
      version: plan.version,
      path: import_node_path38.default.relative(root, plan.releaseMd),
      metadata: import_node_path38.default.relative(root, plan.releaseJson),
      completed_tracks: plan.completed.length,
      released_at: plan.generatedAt
    };
    current.updated_at = utcNow();
    return current;
  }, { lock: false });
  const gitResults = runPlannedGitActions(plan.gitActions);
  const gitOk = actionResultsOk(gitResults);
  return {
    ...base,
    ok: indexPatch.ok !== false && gitOk,
    phase_state: gitOk ? "executed" : "recovery_required",
    dry_run: args.execute !== true,
    bump: args.bump || args.mode || "patch",
    setup_state: indexPatch,
    git_results: gitResults
  };
}
function workflowRevise(root, args = {}) {
  const trackId = selectedTrackId(root, args);
  const summary = workflowSummary(root, "revise", args);
  const markdownError = markdownPayloadError(args);
  if (markdownError) return { ...summary, ...markdownError };
  if (!trackId) return { ...summary, ok: false, error: "trackId is required" };
  const track = findTrack(root, trackId);
  const context = trackContext(root, trackId);
  const impact = lspImpact(root, args);
  const reviewFiles = [];
  const existingSpec = track ? readJson(trackSpecJsonPath(track), null) : null;
  const revisedSpec = isRecord(args.spec) ? normalizeSpecJson(trackId, args.spec) : null;
  const revisedPlan = isRecord(args.plan) ? normalizePlanJson(trackId, args.plan, revisedSpec || existingSpec) : null;
  const revisionRequested = Boolean(revisedSpec || revisedPlan);
  if (revisionRequested && !track) {
    return {
      ...summary,
      ok: false,
      track_context: context,
      impact,
      error: `Track not found: ${trackId}`
    };
  }
  if (track && revisedSpec) {
    reviewFiles.push(jsonReviewFile(import_node_path38.default.relative(root, trackSpecJsonPath(track)), "Revised track spec canonical", "spec", revisedSpec));
    reviewFiles.push(textReviewFile(
      import_node_path38.default.relative(root, track.spec_path),
      "Revised track spec",
      "spec.json",
      withGeneratedMarker(import_node_path38.default.relative(root, trackSpecJsonPath(track)), "cadre.spec.v1", renderSpecMarkdown(revisedSpec))
    ));
  }
  if (track && revisedPlan) {
    reviewFiles.push(jsonReviewFile(import_node_path38.default.relative(root, trackPlanJsonPath(track)), "Revised track plan canonical", "plan", revisedPlan));
    reviewFiles.push(textReviewFile(
      import_node_path38.default.relative(root, track.plan_path),
      "Revised track plan",
      "plan.json",
      withGeneratedMarker(import_node_path38.default.relative(root, trackPlanJsonPath(track)), "cadre.plan.v1", renderPlanMarkdown(revisedPlan))
    ));
  }
  const reviewArtifacts = reviewArtifactsFromFiles(reviewFiles);
  const reviewBundle = workflowReviewBundle(root, "revise", args, reviewFiles, { track_id: trackId });
  const humanReview = reviewFiles.length > 0 ? humanReviewState("revise", args, reviewArtifacts, reviewBundle) : null;
  const warnings = asStringArray(asJsonObject(reviewBundle).warnings);
  if (reviewFiles.length === 0) {
    return {
      ...summary,
      ok: true,
      track_context: context,
      impact
    };
  }
  if (!track) {
    return {
      ...summary,
      ok: false,
      track_context: context,
      impact,
      error: `Track not found: ${trackId}`
    };
  }
  const base = {
    ...summary,
    track_id: trackId,
    track_context: context,
    impact,
    human_review: humanReview,
    review_artifacts: reviewArtifacts,
    review_bundle: reviewBundle,
    warnings
  };
  if (args.execute !== true) {
    return {
      ...base,
      ok: true,
      dry_run: true,
      phase_state: "dry_run"
    };
  }
  if (!humanReviewConfirmed(args)) {
    return {
      ...base,
      ok: false,
      dry_run: true,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      error: "Human confirmation is required before revising track artifacts"
    };
  }
  const writeResult = withTrackLock(root, track.track_id, () => {
    const written = [];
    if (revisedSpec) {
      writeJsonEnsured(trackSpecJsonPath(track), revisedSpec);
      import_node_fs21.default.writeFileSync(track.spec_path, withGeneratedMarker(import_node_path38.default.relative(root, trackSpecJsonPath(track)), "cadre.spec.v1", renderSpecMarkdown(revisedSpec)));
      written.push(import_node_path38.default.relative(root, trackSpecJsonPath(track)));
      written.push(import_node_path38.default.relative(root, track.spec_path));
    }
    if (revisedPlan) {
      writeJsonEnsured(trackPlanJsonPath(track), revisedPlan);
      import_node_fs21.default.writeFileSync(track.plan_path, withGeneratedMarker(import_node_path38.default.relative(root, trackPlanJsonPath(track)), "cadre.plan.v1", renderPlanMarkdown(revisedPlan)));
      written.push(import_node_path38.default.relative(root, trackPlanJsonPath(track)));
      written.push(import_node_path38.default.relative(root, track.plan_path));
    }
    return { ok: true, written, revised_at: utcNow() };
  });
  const regen = writeResult.ok !== false ? regenIndex(root) : null;
  return {
    ...base,
    ok: writeResult.ok !== false && (!regen || regen.ok !== false),
    dry_run: false,
    phase_state: writeResult.ok === false || regen && regen.ok === false ? "recovery_required" : "executed",
    write: writeResult,
    regen
  };
}

// src/core/application/runtime/workflow-setup.ts
var import_node_fs22 = __toESM(require("node:fs"));
var import_node_path39 = __toESM(require("node:path"));
function workflowSetup(root, args = {}) {
  const summary = workflowSummary(root, "setup", args);
  const markdownError = markdownPayloadError(args);
  if (markdownError) return { ...summary, ...markdownError };
  const rawArgs = args;
  const requestedTopology = asOptionalString(rawArgs.topology)?.toLowerCase();
  const reposPayload = isRecord(rawArgs.repos) ? asJsonObject(rawArgs.repos) : null;
  const polyrepoRequested = Boolean(reposPayload && reposPayload.mode === "polyrepo") || requestedTopology === "polyrepo" || rawArgs.polyrepo === true;
  const styleGuides = setupStyleGuides(root, args);
  const provider = configuredProvider(root, args);
  const providerMode = asOptionalString(provider.provider_mode);
  const lspRecommendations = lspSetup(root, { ...args, execute: false });
  const lspWriteRequested = setupShouldWriteLsp(args, lspRecommendations);
  const detailMode = workflowResponseMode(args) === "detail";
  const workspaceHealthResult = workspaceHealth(root, { ...args, responseMode: detailMode ? "detail" : "compact" });
  const configOverrides = asJsonObject(rawArgs.config);
  const requestedSyncMode = asOptionalString(rawArgs.syncMode || rawArgs.sync_mode || configOverrides.sync_mode);
  const teamSize = Number(rawArgs.teamSize || rawArgs.team_size || 0);
  const syncModeRecommendation = requestedSyncMode || (teamSize >= 2 ? "shared" : "local");
  const reviewFiles = setupReviewFiles(root, args, styleGuides, polyrepoRequested);
  const reviewBundle = setupReviewBundle(root, args, reviewFiles, styleGuides);
  const reviewArtifacts = appendLspReviewArtifacts(setupReviewArtifacts(reviewFiles, styleGuides), args, lspWriteRequested);
  const humanReview = humanReviewState("setup", args, reviewArtifacts, reviewBundle);
  const warnings = [
    ...asStringArray(styleGuides.warnings),
    ...asStringArray(asJsonObject(reviewBundle).warnings)
  ];
  const result = {
    ...summary,
    ok: true,
    doctor: doctor(root, { hasCadreProject: isCadreProjectRoot(root) }),
    workspace_health: workspaceHealthResult,
    workspace: workspaceHealthResult.workspace,
    dependency_graph: workspaceHealthResult.dependency_graph,
    lsp: workspaceHealthResult.lsp,
    lsp_setup: detailMode ? lspRecommendations : summarizeLspSetupResult(lspRecommendations),
    integrations: workspaceHealthResult.integrations,
    detail_resources: workspaceHealthResult.detail_resources,
    provider,
    sync_mode: syncModeRecommendation,
    sync_recommendation: teamSize >= 2 && syncModeRecommendation !== "shared" ? "Team setup detected; use syncMode/shared sync for 10-20 person coordination." : null,
    styleGuides,
    templates: templateManifest(),
    techStackSummary: techStackSummary(root, args),
    human_review: humanReview,
    review_artifacts: reviewArtifacts,
    review_bundle: reviewBundle,
    warnings,
    required_payload: args.execute === true ? ["product", "techStack"].concat(provider.requires_confirmation === true ? ["providerMode"] : []).concat(polyrepoRequested && !reposPayload ? ["repos"] : []) : [],
    next_actions: [
      ...provider.requires_confirmation === true ? ["Choose providerMode: local, github, or gitlab before setup writes cadre/config.json."] : [],
      "Review setup artifacts with the user; call setup_scaffold with execute:true and humanConfirmed:true only after explicit approval."
    ],
    packet_notes: [
      "cadre-setup is packet-only: agents gather user intent, then pass confirmed structured JSON payloads to this packet.",
      "Setup writes are human-in-loop: mutating setup packets require humanConfirmed:true after artifact review.",
      "Project mutation must be performed by MCP packets; clients must not recreate Cadre setup writes themselves.",
      "Provider evidence is direct-MCP only: GitHub/GitLab modes require the matching provider MCP, local mode requires none."
    ]
  };
  if (args.execute !== true) return result;
  const cadreDir = import_node_path39.default.join(root, "cadre");
  const force = asBoolean(rawArgs.force, false);
  const missingPayload = [
    ...!isRecord(rawArgs.product) ? ["product"] : [],
    ...!techStackFromArgs(args) ? ["techStack"] : [],
    ...provider.requires_confirmation === true || !providerMode ? ["providerMode"] : [],
    ...polyrepoRequested && !reposPayload ? ["repos"] : []
  ];
  if (missingPayload.length > 0) {
    return {
      ...result,
      ok: false,
      error: `Missing setup payload: ${missingPayload.join(", ")}`,
      missing_payload: missingPayload
    };
  }
  if (!humanReviewConfirmed(args)) {
    return {
      ...result,
      ok: false,
      phase_state: "awaiting_human_review",
      stage: "human_review",
      error: "Human confirmation is required before writing setup artifacts"
    };
  }
  const written = [];
  const skipped = [];
  const writeText = (relativePath, text) => {
    const file = import_node_path39.default.join(cadreDir, relativePath);
    if (fileExists(file) && !force) {
      skipped.push(import_node_path39.default.relative(root, file));
      return;
    }
    import_node_fs22.default.mkdirSync(import_node_path39.default.dirname(file), { recursive: true });
    import_node_fs22.default.writeFileSync(file, text);
    written.push(import_node_path39.default.relative(root, file));
  };
  const writeSetupJson = (relativePath, value) => {
    const file = import_node_path39.default.join(cadreDir, relativePath);
    if (fileExists(file) && !force) {
      skipped.push(import_node_path39.default.relative(root, file));
      return;
    }
    import_node_fs22.default.mkdirSync(import_node_path39.default.dirname(file), { recursive: true });
    writeJson(file, value);
    written.push(import_node_path39.default.relative(root, file));
  };
  const writeSetupJsonlEntry = (relativePath, value) => {
    const file = import_node_path39.default.join(cadreDir, relativePath);
    if (fileExists(file) && !force) {
      skipped.push(import_node_path39.default.relative(root, file));
      return;
    }
    appendJsonl(file, value);
    written.push(import_node_path39.default.relative(root, file));
  };
  const writeProjectDoc = (relativePath, kind, value, title) => {
    const jsonPath = relativePath.replace(/\.md$/, ".json");
    writeSetupJson(jsonPath, value);
    writeText(relativePath, withGeneratedMarker(`cadre/${jsonPath}`, `cadre.${kind}.v1`, renderMarkdownDoc(value, title)));
  };
  import_node_fs22.default.mkdirSync(import_node_path39.default.join(cadreDir, "tracks"), { recursive: true });
  import_node_fs22.default.mkdirSync(import_node_path39.default.join(cadreDir, "archive"), { recursive: true });
  const nativeState = ensureNativeState(root);
  const nativeIgnorePath = asOptionalString(nativeState.ignore_path);
  if (nativeIgnorePath) written.push(nativeIgnorePath);
  writeProjectDoc(
    "product.md",
    "product",
    normalizeProjectDoc("product", rawArgs.product, "product.json", "Product Context", "Project-Specific Product Notes"),
    "Product Context"
  );
  writeProjectDoc(
    "product_guidelines.md",
    "product_guidelines",
    normalizeProjectDoc(
      "product_guidelines",
      rawArgs.productGuidelines || rawArgs.product_guidelines,
      "product_guidelines.json",
      "Product Guidelines",
      "Project-Specific Product Guideline Notes"
    ),
    "Product Guidelines"
  );
  writeSetupJson("tech-stack.json", techStackFromArgs(args) || {});
  writeProjectDoc(
    "workflow.md",
    "workflow",
    normalizeProjectDoc("workflow", rawArgs.workflowPolicy || rawArgs.workflow_policy, "workflow.json", "Project Workflow", "Project-Specific Workflow Notes"),
    "Project Workflow"
  );
  writeSetupJson("tracks.json", trackIndexPayload(root, []));
  const patternsSeed = templateJson("patterns_seed.json", { id: "initial", kind: "patterns_seed", text: "# Codebase Patterns\n\nLast refreshed: YYYY-MM-DD\n" });
  const patternsText = asOptionalString(patternsSeed.text) || "# Codebase Patterns\n\nLast refreshed: YYYY-MM-DD\n";
  writeSetupJsonlEntry("patterns.jsonl", {
    ...patternsSeed,
    id: "initial",
    kind: "patterns_seed",
    recorded_at: utcNow(),
    text: patternsText
  });
  writeText("patterns.md", withGeneratedMarker("cadre/patterns.jsonl", "cadre.patterns.v1", patternsText));
  const beforeStyleWritten = written.length;
  const beforeStyleSkipped = skipped.length;
  for (const guideId of asStringArray(styleGuides.selected)) {
    const guideJson = templateJson(`styleguides/${guideId}.json`, {
      version: 1,
      schema: "cadre.styleguide.v1",
      id: guideId,
      title: guideId,
      source: "bundled_template",
      rules: []
    });
    writeSetupJson(`styleguides/${guideId}.json`, guideJson);
    writeText(
      `code_styleguides/${guideId}.md`,
      withGeneratedMarker(`cadre/styleguides/${guideId}.json`, "cadre.styleguide.v1", renderStyleGuideMarkdown(guideJson))
    );
  }
  const styleGuideIndex = {
    version: 1,
    schema: "cadre.styleguide_index.v1",
    selected: asStringArray(styleGuides.selected),
    generated_at: utcNow()
  };
  writeSetupJson("styleguides/index.json", styleGuideIndex);
  writeText(
    "code_styleguides/README.md",
    withGeneratedMarker("cadre/styleguides/index.json", "cadre.styleguide_index.v1", renderJsonCodeblock("Style guide catalog", styleGuideIndex))
  );
  writeSetupJson("setup_state.json", {
    version: 1,
    packet_only: true,
    topology: polyrepoRequested ? "polyrepo" : "monorepo",
    initialized_at: utcNow(),
    updated_at: utcNow()
  });
  const configPayload = {
    ...templateJson("config.json", { sync_mode: "local", auto_open: false }),
    packet_only: true,
    sync_mode: syncModeRecommendation,
    provider_mode: providerMode || "local",
    provider_mcp_required: providerMode === "github" || providerMode === "gitlab",
    ...asOptionalString(provider.remote_host) ? { remote_host: asOptionalString(provider.remote_host) } : {},
    ...isRecord(rawArgs.integrations) ? { integrations: asJsonObject(rawArgs.integrations) } : {},
    ...configOverrides
  };
  writeSetupJson("config.json", configPayload);
  let repos = null;
  if (reposPayload) {
    repos = reposPayload;
    writeSetupJson("repos.json", reposPayload);
  }
  const lspSetupResult = lspWriteRequested ? lspSetup(root, { ...args, execute: true }) : lspRecommendations;
  const gitattributesNeeded = polyrepoRequested || configPayload.sync_mode === "shared" || rawArgs.writeGitattributes === true || rawArgs.write_gitattributes === true;
  const gitattributes = gitattributesNeeded ? setupGitattributes(root) : null;
  const ciSetup = setupCiTemplates(
    root,
    configuredCiProvider(root, args) || (providerMode === "github" || providerMode === "gitlab" ? providerMode : null),
    { ...args, topology: polyrepoRequested ? "polyrepo" : "monorepo" }
  );
  const polyrepoSetup = polyrepoRequested && repos ? {
    gitattributes,
    ci: ciSetup,
    submodules: setupSubmodulePlan(root, repos, args)
  } : null;
  const setupEvent = appendCadreEvent(root, {
    kind: "setup_completed",
    workflow: "setup",
    topology: polyrepoRequested ? "polyrepo" : "monorepo",
    sync_mode: syncModeRecommendation,
    provider_mode: providerMode || "local",
    written_count: written.length,
    skipped_count: skipped.length
  });
  return {
    ...result,
    ok: true,
    scaffolded: true,
    phase_state: "executed",
    topology: polyrepoRequested ? "polyrepo" : "monorepo",
    written,
    skipped,
    styleGuides: {
      ...styleGuides,
      written: written.slice(beforeStyleWritten),
      skipped: skipped.slice(beforeStyleSkipped)
    },
    lsp_setup: lspSetupResult,
    native_state: nativeState,
    event: setupEvent,
    gitattributes,
    ci_setup: ciSetup,
    polyrepo_setup: polyrepoSetup,
    force,
    doctor_after: doctor(root, { hasCadreProject: true })
  };
}

// src/core/application/runtime/workflow-ship-land.ts
function providerActionKind(workflow, provider) {
  if (provider === "gitlab") return workflow === "land" ? "open_merge_request_group" : "open_merge_request";
  return workflow === "land" ? "open_pull_request_group" : "open_pull_request";
}
function providerActionsForTrack(root, workflow, track, args = {}) {
  const provider = providerFromConfig(root, args);
  if (provider === "local") return [];
  const entries = workflow === "land" ? repoEntriesForTrack(root, track, args) : repoEntriesForTrack(root, track, { ...args, repo: args.repo || "." }).filter((entry) => entry.repo === "." || !loadTopology(root).polyrepo);
  const label = `cadre-track:${track.track_id}`;
  return entries.map((entry) => {
    const repo = asString(entry.repo, ".");
    const target = entry.head || track.metadata.git_branch || `track/${track.track_id}`;
    return {
      id: `${workflow}-${safeName(repo)}`,
      provider,
      kind: providerActionKind(workflow, provider),
      repo,
      track_id: track.track_id,
      title: `${track.track_id}: ${track.metadata.description || track.metadata.name || track.track_id}`,
      source_branch: target,
      target_branch: entry.base || args.base || "main",
      body: [
        `Cadre track: ${track.track_id}`,
        `Repo: ${repo}`,
        "Provider evidence must be fetched through the installed provider MCP and written back to Cadre."
      ].join("\n"),
      labels: [label],
      evidence_key: `${provider}:${workflow}:${track.track_id}:${repo}`,
      required_evidence: asJsonObject(providerEvidenceRequirement(root, { ...args, trackId: track.track_id }))
    };
  });
}
function shipGitActions(root, track, args = {}) {
  const remote = args.remote || "origin";
  const base = args.base || "main";
  const branch = args.branch || track.metadata.git_branch || `track/${track.track_id}`;
  return [
    plannedGitAction("ship-fetch", "fetch_base", ".", root, ["fetch", String(remote), String(base)], `Fetch ${remote}/${base}`),
    plannedGitAction("ship-rebase", "rebase_base", ".", root, ["rebase", `${remote}/${base}`], `Rebase ${branch} onto ${remote}/${base}`),
    plannedGitAction("ship-push", "push_branch", ".", root, ["push", "-u", String(remote), String(branch)], `Push ${branch}`)
  ];
}
function landGitActions(root, track, args = {}) {
  const remote = args.remote || "origin";
  const actions = [];
  for (const entry of repoEntriesForTrack(root, track, args)) {
    const repo = asString(entry.repo, ".");
    const branch = asString(entry.head || track.metadata.git_branch || `track/${track.track_id}`);
    actions.push(plannedGitAction(
      `land-push-${safeName(repo)}`,
      "push_repo_branch",
      repo,
      asString(entry.root, root),
      ["push", "-u", String(remote), branch],
      `Push ${repo} branch ${branch}`
    ));
  }
  actions.push(plannedGitAction(
    "land-push-control",
    "push_control_branch",
    ".",
    root,
    ["push", String(remote), args.branch || "HEAD"],
    "Push control-plane branch"
  ));
  return actions;
}
function persistProviderEvidenceIfSupplied(root, track, args = {}) {
  const evidence = args.evidence || args.providerEvidence || args.provider_evidence;
  if (!evidence || args.execute !== true) return null;
  return providerEvidence(root, {
    ...args,
    trackId: track.track_id,
    evidence
  });
}
function workflowShip(root, args = {}) {
  const trackId = selectedTrackId(root, args);
  const summary = workflowSummary(root, "ship", args);
  if (!trackId) return { ...summary, ok: false, error: "trackId is required" };
  const track = findTrack(root, trackId);
  if (!track) return { ...summary, ok: false, error: `Track not found: ${trackId}` };
  const gate = reviewGate(root, trackId, args);
  const provider = args.includeProvider === false ? null : prCiStatus(root, { ...args, trackId });
  const providerActions = providerActionsForTrack(root, "ship", track, args);
  const gitActions = shipGitActions(root, track, args);
  const evidenceSupplied = hasProviderEvidence(args);
  const pendingProvider = Boolean(provider && provider.ok === false && providerActions.length > 0 && !evidenceSupplied);
  const blocked = gate.ok === false;
  const evidenceWrite = persistProviderEvidenceIfSupplied(root, track, args);
  const canExecuteGit = args.execute === true && !blocked && !evidenceSupplied && (!evidenceWrite || evidenceWrite.ok !== false);
  const gitResults = canExecuteGit ? runPlannedGitActions(gitActions) : [];
  const executionFailed = canExecuteGit && !actionResultsOk(gitResults);
  const phaseState = executionFailed ? "recovery_required" : workflowPhaseState(args, blocked || Boolean(evidenceWrite && evidenceWrite.ok === false), pendingProvider);
  return {
    ...summary,
    ok: gate.ok !== false && (!evidenceWrite || evidenceWrite.ok !== false) && !executionFailed,
    phase_state: phaseState,
    gate,
    provider,
    pr_ci_status: provider,
    provider_actions: providerActions,
    git_actions: gitActions,
    git_results: gitResults,
    git_action_state: canExecuteGit ? "executed" : evidenceSupplied ? "skipped_provider_evidence_continuation" : "pending_execute",
    provider_evidence_write: evidenceWrite,
    continuation_token: continuationToken("ship", trackId, [...providerActions, ...gitActions]),
    required_provider_mcp: provider && provider.ok === false ? provider.required_provider_mcp || null : null,
    required_evidence: provider && provider.ok === false ? provider.required_evidence || null : null,
    unsupported_reason: provider && provider.ok === false ? provider.unsupported_reason || provider.reason || null : null,
    next_actions: provider && Array.isArray(provider.next_actions) ? provider.next_actions : []
  };
}
function workflowLand(root, args = {}) {
  const trackId = selectedTrackId(root, args);
  const summary = workflowSummary(root, "land", args);
  if (!trackId) return { ...summary, ok: false, error: "trackId is required" };
  const track = findTrack(root, trackId);
  if (!track) return { ...summary, ok: false, error: `Track not found: ${trackId}` };
  const topology = loadTopology(root);
  const preflight = polyrepoPreflight(root);
  const gate = reviewGate(root, trackId, args);
  const provider = args.includeProvider === false ? null : prCiStatus(root, { ...args, trackId });
  const providerActions = topology.polyrepo && preflight.ok !== false ? providerActionsForTrack(root, "land", track, args) : [];
  const gitActions = topology.polyrepo && preflight.ok !== false ? landGitActions(root, track, args) : [];
  const evidenceSupplied = hasProviderEvidence(args);
  const pendingProvider = Boolean(provider && provider.ok === false && providerActions.length > 0 && !evidenceSupplied);
  const blocked = !topology.polyrepo || preflight.ok === false || gate.ok === false;
  const evidenceWrite = persistProviderEvidenceIfSupplied(root, track, args);
  const canExecuteGit = args.execute === true && !blocked && !evidenceSupplied && (!evidenceWrite || evidenceWrite.ok !== false);
  const gitResults = canExecuteGit ? runPlannedGitActions(gitActions) : [];
  const executionFailed = canExecuteGit && !actionResultsOk(gitResults);
  const phaseState = executionFailed ? "recovery_required" : workflowPhaseState(args, blocked || Boolean(evidenceWrite && evidenceWrite.ok === false), pendingProvider);
  return {
    ...summary,
    ok: topology.polyrepo && preflight.ok !== false && gate.ok !== false && (!evidenceWrite || evidenceWrite.ok !== false) && !executionFailed,
    phase_state: phaseState,
    topology: topology.polyrepo ? "polyrepo" : "monorepo",
    preflight,
    gate,
    provider,
    provider_actions: providerActions,
    git_actions: gitActions,
    git_results: gitResults,
    git_action_state: canExecuteGit ? "executed" : evidenceSupplied ? "skipped_provider_evidence_continuation" : "pending_execute",
    provider_evidence_write: evidenceWrite,
    continuation_token: continuationToken("land", trackId, [...providerActions, ...gitActions]),
    required_provider_mcp: provider && provider.ok === false ? provider.required_provider_mcp || null : null,
    required_evidence: provider && provider.ok === false ? provider.required_evidence || null : null,
    unsupported_reason: provider && provider.ok === false ? provider.unsupported_reason || provider.reason || null : null,
    next_actions: provider && Array.isArray(provider.next_actions) ? provider.next_actions : [],
    fleet: fleetStatus(root, { includeCollisions: false })
  };
}

// src/core/application/runtime/workflow-packet.ts
function workflowPacket(root, args = {}) {
  const workflow = asOptionalString(args.workflow) || asOptionalString(args.action) || "status";
  const markdownError = markdownPayloadError(args);
  if (markdownError) return shapeWorkflowResponse(root, workflow, args, { ...workflowSummary(root, workflow, args), ...markdownError });
  const mutating = args.execute === true && [
    "newtrack",
    "new_track",
    "handoff",
    "release",
    "revise",
    "refresh",
    "flag",
    "revert",
    "formula",
    "artifacts",
    "artifact_sync"
  ].includes(workflow);
  if (mutating && args.skipSync !== true) {
    const result2 = withSharedControlPlaneSync(
      root,
      args,
      `workflow:${workflow}`,
      () => workflowPacket(root, { ...args, skipSync: true })
    );
    return shapeWorkflowResponse(root, workflow, args, result2);
  }
  const result = (() => {
    switch (workflow) {
      case "setup":
      case "setup_assist":
      case "setup_scaffold":
        return workflowSetup(root, args);
      case "newtrack":
      case "new_track":
        return workflowNewTrack(root, args);
      case "implement":
        return workflowImplement(root, args);
      case "status":
        return workflowStatus(root, args);
      case "review":
        return workflowReview(root, args);
      case "validate":
        return workflowValidate(root, args);
      case "archive":
        return workflowArchive(root, args);
      case "handoff":
        return workflowHandoff(root, args);
      case "ship":
        return workflowShip(root, args);
      case "land":
        return workflowLand(root, args);
      case "release":
        return workflowRelease(root, args);
      case "revise":
        return workflowRevise(root, args);
      case "refresh":
        return workflowRefresh(root, args);
      case "artifacts":
      case "artifact_sync": {
        const artifactAction = asOptionalString(args.artifactAction || args.artifact_action) || asOptionalString(args.mode) || "sync";
        return {
          ...workflowSummary(root, workflow, args),
          artifact_action: artifactAction,
          artifact_scope: asOptionalString(args.scope || args.view) || "all",
          ...artifactPacket(root, { ...args, action: artifactAction })
        };
      }
      case "flag": {
        const trackId = selectedTrackId(root, args);
        const summary = workflowSummary(root, "flag", args);
        if (!trackId) return { ...summary, ok: false, error: "trackId is required" };
        const status = asOptionalString(args.status) || "blocked";
        const reason = asOptionalString(args.reason) || asOptionalString(args.note) || null;
        const context = trackContext(root, trackId);
        const reviewArtifacts = [
          packetReviewArtifact("Flag status change", "workflow:flag", {
            track_id: trackId,
            proposed_status: status,
            reason
          })
        ];
        const humanReview = humanReviewState("flag", args, reviewArtifacts);
        if (args.execute !== true) {
          return {
            ...summary,
            ok: context.ok !== false,
            dry_run: true,
            track_context: context,
            proposed_status: status,
            reason,
            human_review: humanReview,
            review_artifacts: reviewArtifacts
          };
        }
        if (!humanReviewConfirmed(args)) {
          return {
            ...summary,
            ok: false,
            dry_run: true,
            phase_state: "awaiting_human_review",
            stage: "human_review",
            track_context: context,
            proposed_status: status,
            reason,
            human_review: humanReview,
            review_artifacts: reviewArtifacts,
            error: "Human confirmation is required before flagging track status"
          };
        }
        const statusResult = setTrackStatus(root, trackId, status);
        if (statusResult.ok === false) return { ...summary, ok: false, track_context: context, status_result: statusResult };
        const patch = metadataPatch(root, {
          trackId,
          patch: {
            last_status_reason: reason,
            last_status_at: utcNow()
          }
        });
        const event = appendCadreEvent(root, {
          kind: "status_changed",
          workflow: "flag",
          track_id: trackId,
          status,
          reason
        });
        return {
          ...summary,
          ok: patch.ok !== false,
          dry_run: false,
          track_context: context,
          status_result: statusResult,
          metadata_patch: patch,
          event,
          human_review: humanReview,
          review_artifacts: reviewArtifacts
        };
      }
      case "revert":
        return workflowRevert(root, args);
      case "formula":
        return workflowFormula(root, args);
      default:
        return {
          ...workflowSummary(root, workflow, args),
          ok: false,
          error: `Unknown Cadre workflow packet: ${workflow}`
        };
    }
  })();
  return shapeWorkflowResponse(root, workflow, args, result);
}

// src/cadre-job-runner.ts
function readStdin() {
  return new Promise((resolve, reject) => {
    let text = "";
    process.stdin.setEncoding("utf8");
    process.stdin.on("data", (chunk) => {
      text += chunk;
    });
    process.stdin.on("end", () => resolve(text));
    process.stdin.on("error", reject);
  });
}
function runJob(payload) {
  const root = payload.root || process.cwd();
  const args = asJsonObject(payload.args);
  switch (asOptionalString(payload.type)) {
    case "complete_task":
      return completeTask(root, args);
    case "coverage":
      return testCoverage(root, args);
    case "machine_gate":
      return reviewMachineGate(root, args);
    case "review_assist":
      return reviewAssist(root, args);
    case "lsp_review":
      return lspReview(root, args);
    case "lsp_impact":
      return lspImpact(root, args);
    default:
      return { ok: false, error: `Unsupported job type: ${payload.type}` };
  }
}
async function runJobRunner() {
  const input = await readStdin();
  const payload = asJsonObject(JSON.parse(input || "{}"));
  const result = runJob(payload);
  process.stdout.write(`${JSON.stringify(result, null, 2)}
`);
}
if (["cadre-job-runner.js", "cadre-job-runner.ts"].includes(import_node_path40.default.basename(process.argv[1] || ""))) {
  runJobRunner().catch((error) => {
    process.stdout.write(`${JSON.stringify({ ok: false, error: errorMessage(error), stack: error instanceof Error ? error.stack : void 0 }, null, 2)}
`);
    process.exit(1);
  });
}

// src/cadre-lsp-daemon.ts
var import_node_path47 = __toESM(require("node:path"));
var import_node_readline = __toESM(require("node:readline"));

// src/cadre-lsp-review.ts
var import_node_path46 = __toESM(require("node:path"));

// src/lsp/review/client.ts
var import_node_fs23 = __toESM(require("node:fs"));
var import_node_path42 = __toESM(require("node:path"));
var import_node_child_process5 = require("node:child_process");
var import_node_url = require("node:url");

// src/lsp/review/constants.ts
var DEFAULT_STARTUP_TIMEOUT_MS = 1e4;
var DEFAULT_REQUEST_TIMEOUT_MS = 8e3;
var DEFAULT_SHUTDOWN_TIMEOUT_MS = 2e3;
var MAX_TEXT_REFERENCE_RESULTS = 50;
var MAX_SCAN_FILE_BYTES = 1024 * 1024;

// src/lsp/review/language-ids.ts
var import_node_path41 = __toESM(require("node:path"));
var DEFAULT_LANGUAGE_IDS = {
  ".bash": "shellscript",
  ".c": "c",
  ".cc": "cpp",
  ".clj": "clojure",
  ".cljc": "clojure",
  ".cljs": "clojure",
  ".cpp": "cpp",
  ".cs": "csharp",
  ".css": "css",
  ".cxx": "cpp",
  ".dart": "dart",
  ".edn": "clojure",
  ".elm": "elm",
  ".ex": "elixir",
  ".exs": "elixir",
  ".go": "go",
  ".gql": "graphql",
  ".graphql": "graphql",
  ".h": "c",
  ".hcl": "terraform",
  ".hpp": "cpp",
  ".hs": "haskell",
  ".html": "html",
  ".htm": "html",
  ".java": "java",
  ".js": "javascript",
  ".json": "json",
  ".jsonc": "jsonc",
  ".jsx": "javascriptreact",
  ".kt": "kotlin",
  ".kts": "kotlin",
  ".less": "less",
  ".lhs": "haskell",
  ".lua": "lua",
  ".m": "objective-c",
  ".md": "markdown",
  ".mdx": "markdown",
  ".ml": "ocaml",
  ".mli": "ocaml",
  ".mm": "objective-cpp",
  ".nix": "nix",
  ".php": "php",
  ".prisma": "prisma",
  ".proto": "proto",
  ".py": "python",
  ".pyi": "python",
  ".rb": "ruby",
  ".rs": "rust",
  ".sass": "sass",
  ".scala": "scala",
  ".sc": "scala",
  ".scss": "scss",
  ".sh": "shellscript",
  ".svelte": "svelte",
  ".swift": "swift",
  ".tf": "terraform",
  ".tfvars": "terraform",
  ".toml": "toml",
  ".ts": "typescript",
  ".tsx": "typescriptreact",
  ".vue": "vue",
  ".xml": "xml",
  ".xsd": "xml",
  ".xsl": "xml",
  ".xslt": "xml",
  ".yaml": "yaml",
  ".yml": "yaml",
  ".zig": "zig",
  ".zsh": "shellscript",
  "containerfile": "dockerfile",
  "dockerfile": "dockerfile"
};
function languageId(file, server) {
  const ext = import_node_path41.default.extname(file);
  const basename = import_node_path41.default.basename(file).toLowerCase();
  const overrides = server ? asJsonObject(server.languageIds) : {};
  return asOptionalString(overrides[ext]) || asOptionalString(overrides[basename]) || DEFAULT_LANGUAGE_IDS[ext] || DEFAULT_LANGUAGE_IDS[basename] || "plaintext";
}

// src/lsp/review/utils.ts
function positiveInt2(value, fallback) {
  const number = Number(value);
  return Number.isFinite(number) && number > 0 ? number : fallback;
}
function withTimeout(promise, timeoutMs, message) {
  let timer;
  return Promise.race([
    promise,
    new Promise((_, reject) => {
      timer = setTimeout(() => reject(new Error(message)), timeoutMs);
    })
  ]).finally(() => clearTimeout(timer));
}

// src/lsp/review/client.ts
var LspClient = class {
  root;
  server;
  requestTimeoutMs;
  nextId;
  pending;
  buffer;
  opened;
  publishedDiagnostics;
  proc = null;
  constructor(root, server) {
    this.root = root;
    this.server = server;
    this.requestTimeoutMs = positiveInt2(server.requestTimeoutMs, DEFAULT_REQUEST_TIMEOUT_MS);
    this.nextId = 1;
    this.pending = /* @__PURE__ */ new Map();
    this.buffer = Buffer.alloc(0);
    this.opened = /* @__PURE__ */ new Map();
    this.publishedDiagnostics = /* @__PURE__ */ new Map();
  }
  start() {
    return new Promise((resolve, reject) => {
      let settled = false;
      const finishResolve = () => {
        if (settled) return;
        settled = true;
        resolve();
      };
      const finishReject = (error) => {
        if (settled) return;
        settled = true;
        reject(error);
      };
      this.proc = (0, import_node_child_process5.spawn)(this.server.command, this.server.args || [], {
        cwd: this.root,
        stdio: ["pipe", "pipe", "pipe"]
      });
      this.proc.once("spawn", finishResolve);
      this.proc.once("error", (error) => {
        finishReject(new Error(`Unable to start ${this.server.command}: ${errorMessage(error)}`));
      });
      this.proc.stdout.on("data", (chunk) => this.read(chunk));
      this.proc.stderr.on("data", () => {
      });
      this.proc.on("exit", (code, signal) => {
        const message = signal ? `LSP server exited with signal ${signal}` : `LSP server exited with code ${code}`;
        finishReject(new Error(message));
        for (const pending of this.pending.values()) {
          clearTimeout(pending.timer);
          pending.reject(new Error(message));
        }
        this.pending.clear();
      });
    });
  }
  read(chunk) {
    this.buffer = Buffer.concat([this.buffer, chunk]);
    while (true) {
      const headerEnd = this.buffer.indexOf("\r\n\r\n");
      if (headerEnd === -1) return;
      const header = this.buffer.slice(0, headerEnd).toString("utf8");
      const match = header.match(/Content-Length:\s*(\d+)/i);
      if (!match) {
        this.buffer = this.buffer.slice(headerEnd + 4);
        continue;
      }
      const length = Number(match[1]);
      const bodyStart = headerEnd + 4;
      const bodyEnd = bodyStart + length;
      if (this.buffer.length < bodyEnd) return;
      const body = this.buffer.slice(bodyStart, bodyEnd).toString("utf8");
      this.buffer = this.buffer.slice(bodyEnd);
      let message;
      try {
        message = asJsonObject(JSON.parse(body));
      } catch {
        continue;
      }
      const messageId = typeof message.id === "number" ? message.id : null;
      if (messageId != null && this.pending.has(messageId)) {
        const pending = this.pending.get(messageId);
        if (!pending) continue;
        this.pending.delete(messageId);
        clearTimeout(pending.timer);
        const error = asJsonObject(message.error);
        if (Object.keys(error).length > 0) pending.reject(new Error(asOptionalString(error.message) || "LSP request failed"));
        else pending.resolve(message.result);
      } else if (message.method === "textDocument/publishDiagnostics") {
        const params = asJsonObject(message.params);
        const uri = asOptionalString(params.uri);
        if (uri) {
          const diagnostics = Array.isArray(params.diagnostics) ? params.diagnostics.map((diagnostic) => asJsonObject(diagnostic)) : [];
          this.publishedDiagnostics.set(uri, diagnostics);
        }
      }
    }
  }
  write(message) {
    if (!this.proc) throw new Error("LSP process is not running");
    const body = JSON.stringify(message);
    this.proc.stdin.write(`Content-Length: ${Buffer.byteLength(body, "utf8")}\r
\r
${body}`);
  }
  request(method, params) {
    const id = this.nextId++;
    return new Promise((resolve, reject) => {
      const timer = setTimeout(() => {
        this.pending.delete(id);
        reject(new Error(`${method} timed out after ${this.requestTimeoutMs}ms`));
      }, this.requestTimeoutMs);
      this.pending.set(id, { resolve, reject, timer });
      try {
        this.write({ jsonrpc: "2.0", id, method, params });
      } catch (error) {
        clearTimeout(timer);
        this.pending.delete(id);
        reject(error);
      }
    });
  }
  notify(method, params) {
    this.write({ jsonrpc: "2.0", method, params });
  }
  async initialize() {
    await this.request("initialize", {
      processId: process.pid,
      rootUri: (0, import_node_url.pathToFileURL)(this.root).href,
      capabilities: {
        textDocument: {
          documentSymbol: { hierarchicalDocumentSymbolSupport: true },
          references: {}
        },
        workspace: { workspaceFolders: true }
      },
      workspaceFolders: [{ uri: (0, import_node_url.pathToFileURL)(this.root).href, name: import_node_path42.default.basename(this.root) }]
    });
    this.notify("initialized", {});
  }
  open(file) {
    const abs = import_node_path42.default.join(this.root, file);
    const uri = (0, import_node_url.pathToFileURL)(abs).href;
    const text = import_node_fs23.default.readFileSync(abs, "utf8");
    const version = (this.opened.get(file) || 0) + 1;
    const alreadyOpen = this.opened.has(file);
    this.opened.set(file, version);
    if (alreadyOpen) {
      this.notify("textDocument/didChange", {
        textDocument: { uri, version },
        contentChanges: [{ text }]
      });
    } else {
      this.notify("textDocument/didOpen", {
        textDocument: {
          uri,
          languageId: languageId(file, this.server),
          version,
          text
        }
      });
    }
  }
  async documentSymbols(file) {
    return this.request("textDocument/documentSymbol", {
      textDocument: { uri: (0, import_node_url.pathToFileURL)(import_node_path42.default.join(this.root, file)).href }
    });
  }
  async references(file, position) {
    return this.request("textDocument/references", {
      textDocument: { uri: (0, import_node_url.pathToFileURL)(import_node_path42.default.join(this.root, file)).href },
      position,
      context: { includeDeclaration: false }
    });
  }
  async definition(file, position) {
    return this.request("textDocument/definition", {
      textDocument: { uri: (0, import_node_url.pathToFileURL)(import_node_path42.default.join(this.root, file)).href },
      position
    });
  }
  async typeDefinition(file, position) {
    return this.request("textDocument/typeDefinition", {
      textDocument: { uri: (0, import_node_url.pathToFileURL)(import_node_path42.default.join(this.root, file)).href },
      position
    });
  }
  async implementation(file, position) {
    return this.request("textDocument/implementation", {
      textDocument: { uri: (0, import_node_url.pathToFileURL)(import_node_path42.default.join(this.root, file)).href },
      position
    });
  }
  diagnostics(file) {
    return this.publishedDiagnostics.get((0, import_node_url.pathToFileURL)(import_node_path42.default.join(this.root, file)).href) || [];
  }
  async shutdown() {
    try {
      await withTimeout(
        this.request("shutdown", null),
        DEFAULT_SHUTDOWN_TIMEOUT_MS,
        `shutdown timed out after ${DEFAULT_SHUTDOWN_TIMEOUT_MS}ms`
      );
      this.notify("exit", {});
    } catch {
    }
    if (this.proc && !this.proc.killed) this.proc.kill();
  }
};

// src/lsp/review/command-availability.ts
var import_node_child_process6 = require("node:child_process");
function shellQuote2(value) {
  return `'${String(value).replace(/'/g, "'\\''")}'`;
}
function commandAvailability(command) {
  if (!command || typeof command !== "string") {
    return {
      state: "invalid",
      command: typeof command === "string" ? command : null,
      message: "Server command is missing or invalid"
    };
  }
  const result = (0, import_node_child_process6.spawnSync)("sh", ["-lc", `command -v ${shellQuote2(command)}`], {
    encoding: "utf8"
  });
  if (result.status === 0) {
    return {
      state: "available",
      command,
      path: result.stdout.trim().split(/\r?\n/)[0] || command
    };
  }
  return {
    state: "missing",
    command,
    message: (result.stderr || result.stdout || "Command not found on PATH").trim()
  };
}

// src/lsp/review/run-review.ts
var import_node_fs26 = __toESM(require("node:fs"));
var import_node_path45 = __toESM(require("node:path"));

// src/lsp/review/git-diff.ts
var import_node_fs24 = __toESM(require("node:fs"));
var import_node_path43 = __toESM(require("node:path"));
var import_node_child_process7 = require("node:child_process");
function runGit(root, args) {
  const result = (0, import_node_child_process7.spawnSync)("git", args, { cwd: root, encoding: "utf8" });
  if (result.status !== 0) {
    throw new Error((result.stderr || result.stdout || `git ${args.join(" ")} failed`).trim());
  }
  return result.stdout;
}
function changedEntries(root, base, head) {
  return runGit(root, ["diff", "--name-status", "--find-renames", `${base}...${head}`]).split(/\r?\n/).map((line) => line.trim()).filter(Boolean).map((line) => {
    const parts = line.split(/\t+/);
    const status = parts[0] || "M";
    const code = status[0] || "M";
    const oldPath = (code === "R" || code === "C" ? parts[1] : null) || null;
    const file = code === "R" || code === "C" ? parts[2] : parts[1];
    const kind = {
      A: "added",
      C: "copied",
      D: "deleted",
      M: "modified",
      R: "renamed",
      T: "type_changed",
      U: "unmerged",
      X: "unknown"
    }[code] || "modified";
    return {
      status,
      kind,
      path: file || "",
      oldPath,
      exists: file ? import_node_fs24.default.existsSync(import_node_path43.default.join(root, file)) : false
    };
  }).filter((entry) => Boolean(entry.path) && !isIgnoredFile(root, entry.path));
}
function changedSymbolCandidates(root, base, head, entry) {
  const paths = Array.from(new Set([entry.oldPath, entry.path].filter((item) => typeof item === "string" && item.length > 0)));
  const diff = runGit(root, [
    "diff",
    "--unified=0",
    "--find-renames",
    `${base}...${head}`,
    "--",
    ...paths
  ]);
  const byName = /* @__PURE__ */ new Map();
  const patterns = [
    /^[+-]\s*(?:export\s+)?(?:async\s+)?(?:function|def)\s+([A-Za-z_$][\w$]*)\b/,
    /^[+-]\s*func\s+(?:\([^)]*\)\s*)?([A-Za-z_$][\w$]*)\b/,
    /^[+-]\s*(?:export\s+)?(?:class|interface|type|enum|struct|module|namespace)\s+([A-Za-z_$][\w$]*)\b/,
    /^[+-]\s*(?:export\s+)?(?:const|let|var)\s+([A-Za-z_$][\w$]*)\s*[:=]/,
    /^[+-]\s*(?:public|private|protected|internal|static|final|open|override|async|\s)*(?:fun|func)\s+([A-Za-z_$][\w$]*)\b/,
    /^[+-]\s*(?:public|private|protected|static|async|\s)*([A-Za-z_$][\w$]*)\s*\([^)]*\)\s*[:{]/,
    /^[+-]\s*(?:local\s+)?function\s+([A-Za-z_][\w.]*)\b/,
    /^[+-]\s*(?:defp?|defmacro)\s+([A-Za-z_][\w!?]*)\b/,
    /^[+-]\s*(?:function\s+)?([A-Za-z_][\w-]*)\s*\(\)\s*\{?/,
    /^[+-]\s*(?:CREATE\s+(?:OR\s+REPLACE\s+)?)?(?:TABLE|VIEW|FUNCTION|PROCEDURE|TYPE)\s+([A-Za-z_][\w."]*)\b/i,
    /^[+-]\s*(?:resource|module|variable|output|data)\s+"?([A-Za-z0-9_.-]+)"?/
  ];
  for (const line of diff.split(/\r?\n/)) {
    if (!/^[+-]/.test(line) || /^(\+\+\+|---)/.test(line)) continue;
    const direction = line[0] === "-" ? "removed" : "added";
    for (const pattern of patterns) {
      const match = line.match(pattern);
      if (!match) continue;
      const name = match[1];
      if (!name) continue;
      if (!byName.has(name)) {
        byName.set(name, {
          name,
          added: false,
          removed: false,
          changeType: "changed",
          changedFile: entry.path,
          oldPath: entry.oldPath,
          status: entry.kind,
          evidence: []
        });
      }
      const candidate = byName.get(name);
      if (!candidate) continue;
      if (direction === "removed") candidate.removed = true;
      else candidate.added = true;
      if (candidate.evidence.length < 4) {
        candidate.evidence.push({
          direction,
          text: line.slice(1).trim().slice(0, 160)
        });
      }
    }
  }
  return Array.from(byName.values()).map((candidate) => ({
    ...candidate,
    changeType: candidate.removed ? candidate.added ? "changed" : "removed" : "added"
  })).filter((candidate) => candidate.changeType !== "added").sort((a, b) => a.name.localeCompare(b.name));
}

// src/lsp/review/scanners.ts
var import_node_fs25 = __toESM(require("node:fs"));
var import_node_path44 = __toESM(require("node:path"));
var import_node_url2 = require("node:url");
function flattenSymbols(symbols, out = []) {
  if (!Array.isArray(symbols)) return out;
  for (const symbol of symbols) {
    const candidate = asJsonObject(symbol);
    if (candidate.name && candidate.selectionRange) out.push(candidate);
    if (candidate.children) flattenSymbols(candidate.children, out);
  }
  return out;
}
function escapeRegExp(value) {
  return String(value).replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function serverFileMatch(file, server) {
  const extensionSet = new Set(server.extensions || []);
  const filenameSet = new Set((server.filenames || []).map((name) => name.toLowerCase()));
  const ext = import_node_path44.default.extname(file);
  const basename = import_node_path44.default.basename(file).toLowerCase();
  return extensionSet.has(ext) || filenameSet.has(basename);
}
function scanTextReferences(root, symbol, changedPathSet, server) {
  const results = [];
  const pattern = new RegExp(
    `(^|[^A-Za-z0-9_$])${escapeRegExp(symbol)}([^A-Za-z0-9_$]|$)`
  );
  function visit(dir) {
    let entries;
    try {
      entries = import_node_fs25.default.readdirSync(dir, { withFileTypes: true });
    } catch {
      return;
    }
    for (const entry of entries) {
      const full = import_node_path44.default.join(dir, entry.name);
      if (shouldIgnore(root, full, entry.name)) continue;
      if (entry.isDirectory()) {
        visit(full);
        if (results.length >= MAX_TEXT_REFERENCE_RESULTS) return;
        continue;
      }
      if (!entry.isFile()) continue;
      if (!serverFileMatch(full, server)) continue;
      if (changedPathSet.has(import_node_path44.default.resolve(full))) continue;
      let stat;
      try {
        stat = import_node_fs25.default.statSync(full);
      } catch {
        continue;
      }
      if (stat.size > MAX_SCAN_FILE_BYTES) continue;
      let text;
      try {
        text = import_node_fs25.default.readFileSync(full, "utf8");
      } catch {
        continue;
      }
      const lines = text.split(/\r?\n/);
      for (let i = 0; i < lines.length; i += 1) {
        const line = lines[i] || "";
        if (!pattern.test(line)) continue;
        results.push({
          file: full,
          relativeFile: normalizeRel(import_node_path44.default.relative(root, full)),
          line: i + 1,
          snippet: line.trim().slice(0, 160)
        });
        if (results.length >= MAX_TEXT_REFERENCE_RESULTS) return;
      }
    }
  }
  visit(root);
  return results;
}
function externalReferenceFinding(server, candidate, refs, engine) {
  const removed = candidate.changeType === "removed";
  return {
    severity: removed ? "blocking" : "warning",
    type: "external_reference",
    code: removed ? "external_reference_to_removed_symbol" : "external_reference_to_changed_symbol",
    server: server.id || server.command,
    engine,
    symbol: {
      name: candidate.name,
      changeType: candidate.changeType,
      status: candidate.status,
      changedFile: candidate.changedFile,
      oldPath: candidate.oldPath
    },
    changedFile: candidate.changedFile,
    externalReferences: refs,
    message: `${candidate.name} has references outside the track diff after being ${removed ? "removed" : "changed"}.`
  };
}
function skipFinding(server, code, message, extra = {}) {
  return {
    severity: "info",
    type: "skip",
    code,
    server: server.id || server.command || "unknown",
    message,
    ...extra || {}
  };
}
function lspRefToLocation(root, ref) {
  const location = asJsonObject(ref);
  const uri = location.uri || location.targetUri;
  if (!uri) return null;
  try {
    const file = (0, import_node_url2.fileURLToPath)(uri);
    const range = location.range || location.targetSelectionRange || location.targetRange;
    return {
      file,
      relativeFile: normalizeRel(import_node_path44.default.relative(root, file)),
      line: (range?.start ? range.start.line : 0) + 1
    };
  } catch {
    return null;
  }
}
function locationsFromResult(root, value) {
  const items = Array.isArray(value) ? value : value ? [value] : [];
  return items.map((item) => {
    const object = asJsonObject(item);
    const ref = object.targetUri ? { uri: object.targetUri, range: object.targetSelectionRange || object.targetRange } : item;
    return lspRefToLocation(root, ref);
  }).filter((location) => location !== null).slice(0, 20);
}
async function optionalLocations(client, kind, file, position, root) {
  try {
    if (kind === "definition") return locationsFromResult(root, await client.definition(file, position));
    if (kind === "typeDefinition") return locationsFromResult(root, await client.typeDefinition(file, position));
    if (kind === "implementation") return locationsFromResult(root, await client.implementation(file, position));
  } catch {
    return [];
  }
  return [];
}
function diagnosticFinding(server, file, diagnostic) {
  const severity = diagnostic.severity === 1 ? "blocking" : "warning";
  return {
    severity,
    type: "diagnostic",
    code: diagnostic.code || "lsp_diagnostic",
    server: server.id || server.command,
    file,
    line: diagnostic.range && diagnostic.range.start ? diagnostic.range.start.line + 1 : null,
    message: diagnostic.message || "LSP diagnostic"
  };
}
function nearbyFileHints(root, files) {
  const manifests = /* @__PURE__ */ new Set();
  const tests = /* @__PURE__ */ new Set();
  const manifestNames = /* @__PURE__ */ new Set([
    "package.json",
    "pyproject.toml",
    "go.mod",
    "Cargo.toml",
    "pom.xml",
    "build.gradle",
    "build.gradle.kts"
  ]);
  for (const file of files || []) {
    let dir = import_node_path44.default.dirname(import_node_path44.default.join(root, file));
    while (dir.startsWith(root)) {
      for (const name of manifestNames) {
        const candidate = import_node_path44.default.join(dir, name);
        if (import_node_fs25.default.existsSync(candidate)) manifests.add(normalizeRel(import_node_path44.default.relative(root, candidate)));
      }
      if (dir === root) break;
      dir = import_node_path44.default.dirname(dir);
    }
    const parsed = import_node_path44.default.parse(file);
    const candidates = [
      import_node_path44.default.join(parsed.dir, `${parsed.name}.test${parsed.ext}`),
      import_node_path44.default.join(parsed.dir, `${parsed.name}.spec${parsed.ext}`),
      import_node_path44.default.join(parsed.dir, `${parsed.name}_test${parsed.ext}`),
      import_node_path44.default.join("test", file),
      import_node_path44.default.join("tests", file)
    ];
    for (const candidate of candidates) {
      if (import_node_fs25.default.existsSync(import_node_path44.default.join(root, candidate))) tests.add(normalizeRel(candidate));
    }
  }
  return {
    package_manifests: Array.from(manifests).slice(0, 20),
    likely_tests: Array.from(tests).slice(0, 30)
  };
}

// src/lsp/review/run-review.ts
async function runReview(options = {}) {
  const args = {
    base: options.base || "main",
    head: options.head || "HEAD",
    config: options.config || "cadre/lsp.json"
  };
  const root = options.root || process.cwd();
  const clientPool = options.clientPool || null;
  const configPath = import_node_path45.default.resolve(root, args.config);
  if (!import_node_fs26.default.existsSync(configPath)) {
    return {
      available: false,
      reason: `No LSP config found at ${args.config}`,
      changedFiles: [],
      changedEntries: [],
      servers: [],
      findings: []
    };
  }
  const entries = changedEntries(root, args.base, args.head);
  const files = entries.map((entry) => entry.path);
  const config = asJsonObject(JSON.parse(import_node_fs26.default.readFileSync(configPath, "utf8")));
  const servers = Array.isArray(config.servers) ? config.servers.map((server) => asJsonObject(server)).map((server) => ({
    ...server,
    id: asOptionalString(server.id),
    command: asOptionalString(server.command) || "",
    args: asStringArray(server.args),
    extensions: asStringArray(server.extensions),
    filenames: asStringArray(server.filenames),
    languageIds: asJsonObject(server.languageIds),
    requestTimeoutMs: asNumber(server.requestTimeoutMs, DEFAULT_REQUEST_TIMEOUT_MS),
    startupTimeoutMs: asNumber(server.startupTimeoutMs, DEFAULT_STARTUP_TIMEOUT_MS),
    diagnosticsDelayMs: asNumber(server.diagnosticsDelayMs, 250)
  })) : [];
  const findings = [];
  const serverReports = [];
  const changedSet = /* @__PURE__ */ new Set();
  for (const entry of entries) {
    if (entry.path) changedSet.add(import_node_path45.default.resolve(root, entry.path));
    if (entry.oldPath) changedSet.add(import_node_path45.default.resolve(root, entry.oldPath));
  }
  for (const server of servers) {
    const serverEntries = entries.filter((entry) => {
      return serverFileMatch(entry.path, server) || (entry.oldPath ? serverFileMatch(entry.oldPath, server) : false);
    });
    if (serverEntries.length === 0) continue;
    const availability = commandAvailability(server.command);
    const serverReport = {
      id: server.id || server.command || "unknown",
      command: server.command || null,
      availability,
      files: serverEntries.map((entry) => ({
        path: entry.path,
        oldPath: entry.oldPath,
        status: entry.status,
        kind: entry.kind,
        exists: entry.exists
      })),
      candidates: [],
      skipped: false
    };
    serverReports.push(serverReport);
    const allCandidates = /* @__PURE__ */ new Map();
    for (const entry of serverEntries) {
      for (const candidate of changedSymbolCandidates(root, args.base, args.head, entry)) {
        const key = `${candidate.name}\0${candidate.changedFile}\0${candidate.oldPath || ""}`;
        allCandidates.set(key, candidate);
      }
    }
    serverReport.candidates = Array.from(allCandidates.values()).map((candidate) => ({
      name: candidate.name,
      changeType: candidate.changeType,
      status: candidate.status,
      changedFile: candidate.changedFile,
      oldPath: candidate.oldPath
    }));
    if (availability.state !== "available") {
      serverReport.skipped = true;
      serverReport.degraded = true;
      serverReport.fallback = "text_scan";
      findings.push(skipFinding(
        server,
        availability.state === "invalid" ? "server_invalid" : "server_missing",
        availability.message || `LSP server command ${server.command} is unavailable`,
        { availability }
      ));
      for (const candidate of allCandidates.values()) {
        if (candidate.changeType !== "removed") continue;
        const refs = scanTextReferences(root, candidate.name, changedSet, server);
        if (refs.length > 0) {
          findings.push(externalReferenceFinding(server, candidate, refs, "text"));
        }
      }
      continue;
    }
    let pooled = null;
    let client = null;
    try {
      pooled = clientPool ? await clientPool.get(root, server) : null;
      client = pooled ? pooled.client : new LspClient(root, server);
      if (!pooled) {
        const startupTimeoutMs = positiveInt2(server.startupTimeoutMs, DEFAULT_STARTUP_TIMEOUT_MS);
        await withTimeout(
          client.start(),
          startupTimeoutMs,
          `${server.command} did not spawn within ${startupTimeoutMs}ms`
        );
        await withTimeout(
          client.initialize(),
          startupTimeoutMs,
          `${server.command} did not initialize within ${startupTimeoutMs}ms`
        );
      }
      serverReport.warm = Boolean(pooled);
      const openFiles = Array.from(new Set(
        serverEntries.filter((entry) => entry.exists).map((entry) => entry.path)
      ));
      for (const file of openFiles) client.open(file);
      await new Promise((resolve) => setTimeout(resolve, positiveInt2(server.diagnosticsDelayMs, 250)));
      serverReport.diagnostics = [];
      serverReport.symbolEvidence = [];
      for (const file of openFiles) {
        for (const diagnostic of client.diagnostics(file)) {
          const item = {
            file,
            line: diagnostic.range && diagnostic.range.start ? diagnostic.range.start.line + 1 : null,
            severity: diagnostic.severity || null,
            code: diagnostic.code || null,
            message: diagnostic.message || ""
          };
          serverReport.diagnostics.push(item);
          if (diagnostic.severity === 1 || diagnostic.severity === 2) {
            findings.push(diagnosticFinding(server, file, diagnostic));
          }
        }
      }
      for (const file of openFiles) {
        const candidates = Array.from(allCandidates.values()).filter((candidate) => candidate.changedFile === file);
        if (candidates.length === 0) continue;
        const symbols = flattenSymbols(await client.documentSymbols(file));
        for (const candidate of candidates) {
          const symbol = symbols.find((item) => item.name === candidate.name);
          if (!symbol) {
            const refs2 = scanTextReferences(root, candidate.name, changedSet, server);
            if (refs2.length > 0) {
              findings.push(externalReferenceFinding(server, candidate, refs2, "text"));
            }
            continue;
          }
          const rawRefs = await client.references(file, symbol.selectionRange.start);
          const refs = Array.isArray(rawRefs) ? rawRefs : [];
          const definitions = await optionalLocations(client, "definition", file, symbol.selectionRange.start, root);
          const typeDefinitions = await optionalLocations(client, "typeDefinition", file, symbol.selectionRange.start, root);
          const implementations = await optionalLocations(client, "implementation", file, symbol.selectionRange.start, root);
          serverReport.symbolEvidence.push({
            symbol: candidate.name,
            file,
            definitions,
            typeDefinitions,
            implementations
          });
          const externalRefs = refs.map((ref) => lspRefToLocation(root, ref)).filter((ref) => ref !== null).filter((ref) => !changedSet.has(import_node_path45.default.resolve(ref.file))).filter((ref) => !isIgnoredFile(root, ref.relativeFile));
          if (externalRefs.length > 0) {
            findings.push(externalReferenceFinding(server, candidate, externalRefs, "lsp"));
          }
        }
      }
      for (const candidate of allCandidates.values()) {
        if (candidate.changeType !== "removed") continue;
        if (candidate.changedFile && import_node_fs26.default.existsSync(import_node_path45.default.join(root, candidate.changedFile))) continue;
        const refs = scanTextReferences(root, candidate.name, changedSet, server);
        if (refs.length > 0) {
          findings.push(externalReferenceFinding(server, candidate, refs, "text"));
        }
      }
    } catch (error) {
      serverReport.skipped = true;
      serverReport.degraded = true;
      serverReport.fallback = "text_scan";
      findings.push(skipFinding(server, "server_unavailable", `LSP scan skipped: ${errorMessage(error)}`));
      if (clientPool && pooled) await clientPool.drop(root, server);
      for (const candidate of allCandidates.values()) {
        if (candidate.changeType !== "removed") continue;
        const refs = scanTextReferences(root, candidate.name, changedSet, server);
        if (refs.length > 0) {
          findings.push(externalReferenceFinding(server, candidate, refs, "text"));
        }
      }
    } finally {
      if (!clientPool && client) await client.shutdown();
    }
  }
  return {
    available: true,
    base: args.base,
    head: args.head,
    config: args.config,
    degraded: serverReports.some((report) => report.degraded === true),
    fallback: serverReports.some((report) => report.fallback === "text_scan") ? "text_scan" : null,
    changedFiles: files,
    changedEntries: entries,
    fileHints: nearbyFileHints(root, files),
    servers: serverReports,
    findings
  };
}

// src/lsp/review/cli.ts
function usage() {
  console.log(`Usage: node <cadre-lsp-review.js> [--base main] [--head HEAD] [--config cadre/lsp.json] [--json]

Runs a best-effort LSP reference scan for changed/removed symbols. If no
cadre/lsp.json exists, exits successfully with available=false.

Example cadre/lsp.json:
{
  "servers": [
    {
      "id": "typescript",
      "command": "typescript-language-server",
      "args": ["--stdio"],
      "extensions": [".ts", ".tsx", ".js", ".jsx"]
    },
    {
      "id": "python",
      "command": "pyright-langserver",
      "args": ["--stdio"],
      "extensions": [".py", ".pyi"]
    }
  ]
}`);
}
function parseArgs(argv) {
  const args = {
    base: "main",
    head: "HEAD",
    config: "cadre/lsp.json",
    json: false
  };
  for (let i = 2; i < argv.length; i += 1) {
    const arg = argv[i];
    if (arg === "--help" || arg === "-h") {
      usage();
      process.exit(0);
    } else if (arg === "--json") {
      args.json = true;
    } else if (arg === "--base" || arg === "--head" || arg === "--config") {
      const value = argv[++i];
      if (!value) throw new Error(`${arg} requires a value`);
      if (arg === "--base") args.base = value;
      else if (arg === "--head") args.head = value;
      else args.config = value;
    } else {
      throw new Error(`Unknown argument: ${arg}`);
    }
  }
  return args;
}
async function runCli() {
  const args = parseArgs(process.argv);
  const result = await runReview({ ...args, root: process.cwd() });
  if (args.json) {
    console.log(JSON.stringify(result, null, 2));
  } else if (!result.available) {
    console.log(`LSP review helper unavailable: ${asOptionalString(result.reason) || "unknown reason"}`);
  } else if (result.findings.length === 0) {
    console.log("LSP review helper found no external reference risks.");
  } else {
    for (const finding of result.findings) {
      console.log(JSON.stringify(finding));
    }
  }
}

// src/cadre-lsp-review.ts
if (["cadre-lsp-review.js", "cadre-lsp-review.ts"].includes(import_node_path46.default.basename(process.argv[1] || ""))) {
  runCli().catch((error) => {
    console.error(errorMessage(error));
    process.exit(1);
  });
}

// src/cadre-lsp-daemon.ts
var ClientPool = class {
  clients;
  maxClients;
  idleEvictionMs;
  constructor(options = {}) {
    this.clients = /* @__PURE__ */ new Map();
    this.maxClients = Math.max(1, Math.min(32, Number(options.maxClients || process.env.CADRE_LSP_MAX_CLIENTS || 8)));
    this.idleEvictionMs = Math.max(3e4, Number(options.idleEvictionMs || process.env.CADRE_LSP_IDLE_EVICTION_MS || 10 * 60 * 1e3));
  }
  key(root, server) {
    return JSON.stringify({
      root,
      id: server.id || null,
      command: server.command,
      args: server.args || []
    });
  }
  async get(root, server) {
    await this.evictIdle();
    const key = this.key(root, server);
    const existing = this.clients.get(key);
    if (existing) {
      existing.last_used_at = (/* @__PURE__ */ new Date()).toISOString();
      existing.uses += 1;
      return existing;
    }
    await this.evictOverflow(this.maxClients - 1);
    const client = new LspClient(root, server);
    await client.start();
    await client.initialize();
    const entry = {
      key,
      root,
      server_id: server.id || server.command || "unknown",
      command: server.command,
      client,
      started_at: (/* @__PURE__ */ new Date()).toISOString(),
      last_used_at: (/* @__PURE__ */ new Date()).toISOString(),
      uses: 1
    };
    this.clients.set(key, entry);
    return entry;
  }
  async evictIdle(now = Date.now()) {
    const stale = Array.from(this.clients.values()).filter((entry) => {
      const lastUsed = Date.parse(entry.last_used_at);
      return Number.isFinite(lastUsed) && now - lastUsed > this.idleEvictionMs;
    });
    for (const entry of stale) {
      this.clients.delete(entry.key);
      await entry.client.shutdown();
    }
    return stale.length;
  }
  async evictOverflow(maxRemaining) {
    const entries = Array.from(this.clients.values()).sort((left, right) => Date.parse(left.last_used_at) - Date.parse(right.last_used_at));
    let evicted = 0;
    while (this.clients.size > maxRemaining && entries.length > 0) {
      const entry = entries.shift();
      if (!entry) break;
      this.clients.delete(entry.key);
      await entry.client.shutdown();
      evicted += 1;
    }
    return evicted;
  }
  async drop(root, server) {
    const key = this.key(root, server);
    const existing = this.clients.get(key);
    if (!existing) return false;
    this.clients.delete(key);
    await existing.client.shutdown();
    return true;
  }
  status() {
    return {
      client_count: this.clients.size,
      max_clients: this.maxClients,
      idle_eviction_ms: this.idleEvictionMs,
      servers: Array.from(this.clients.values()).map((entry) => ({
        root: entry.root,
        server_id: entry.server_id,
        command: entry.command,
        started_at: entry.started_at,
        last_used_at: entry.last_used_at,
        uses: entry.uses,
        open_documents: entry.client.opened ? entry.client.opened.size : 0
      }))
    };
  }
  async shutdownAll() {
    const entries = Array.from(this.clients.values());
    this.clients.clear();
    await Promise.all(entries.map((entry) => entry.client.shutdown()));
    return entries.length;
  }
};
var pool = new ClientPool();
function send(id, result, error = null) {
  process.stdout.write(`${JSON.stringify({ id, result, error })}
`);
}
async function handleDaemonMessage(message) {
  if (message.method === "status") {
    const evicted_idle = await pool.evictIdle();
    return { ok: true, evicted_idle, ...pool.status() };
  }
  if (message.method === "review") {
    const params = asJsonObject(message.params);
    return runReview({ ...params, clientPool: pool });
  }
  if (message.method === "shutdown") {
    const stopped = await pool.shutdownAll();
    setImmediate(() => process.exit(0));
    return { ok: true, stopped };
  }
  throw new Error(`Unknown LSP daemon method: ${message.method}`);
}
function runLspDaemon() {
  const rl = import_node_readline.default.createInterface({ input: process.stdin });
  rl.on("line", async (line) => {
    if (!line.trim()) return;
    let message = null;
    try {
      message = asJsonObject(JSON.parse(line));
      const result = await handleDaemonMessage(message);
      send(message.id || null, result);
    } catch (error) {
      send(message && message.id ? message.id : null, null, {
        message: errorMessage(error),
        stack: error instanceof Error ? error.stack : void 0
      });
    }
  });
  process.on("SIGTERM", () => {
    pool.shutdownAll().finally(() => process.exit(0));
  });
}
if (["cadre-lsp-daemon.js", "cadre-lsp-daemon.ts"].includes(import_node_path47.default.basename(process.argv[1] || ""))) {
  runLspDaemon();
}

// src/cadre-lsp-setup.ts
var import_node_path49 = __toESM(require("node:path"));

// src/lsp/setup-recommender.ts
var import_node_fs27 = __toESM(require("node:fs"));
var import_node_path48 = __toESM(require("node:path"));
var import_node_child_process8 = require("node:child_process");
function usage2() {
  console.log(`Usage: node <cadre-lsp-setup.js> [--root DIR] [--config cadre/lsp.json] [--write] [--json]

Scans the codebase, recommends language servers, detects whether the server
commands are installed, and optionally appends missing server entries to
cadre/lsp.json.`);
}
function parseArgs2(argv) {
  const args = {
    root: process.cwd(),
    config: "cadre/lsp.json",
    write: false,
    json: false
  };
  for (let i = 2; i < argv.length; i += 1) {
    const arg = argv[i] ?? "";
    if (arg === "--help" || arg === "-h") {
      usage2();
      process.exit(0);
    } else if (arg === "--write") {
      args.write = true;
    } else if (arg === "--json") {
      args.json = true;
    } else if (arg === "--root") {
      args.root = argv[++i] ?? args.root;
    } else if (arg === "--config") {
      args.config = argv[++i] ?? args.config;
    } else {
      throw new Error(`Unknown argument: ${arg}`);
    }
  }
  const root = import_node_path48.default.resolve(args.root);
  return { ...args, root, configPath: import_node_path48.default.resolve(root, args.config) };
}
function shellQuote3(value) {
  return `'${value.replace(/'/g, "'\\''")}'`;
}
function commandAvailability2(command) {
  const result = (0, import_node_child_process8.spawnSync)("sh", ["-lc", `command -v ${shellQuote3(command)}`], {
    encoding: "utf8"
  });
  if (result.status === 0) {
    return {
      state: "available",
      command,
      path: result.stdout.trim().split(/\r?\n/)[0] || command
    };
  }
  return {
    state: "missing",
    command,
    message: (result.stderr || result.stdout || "Command not found on PATH").trim()
  };
}
function scanFiles(root) {
  return scanWorkspaceFiles(root);
}
function normalizeServer(value) {
  if (!isRecord(value)) return null;
  return asJsonObject(value);
}
function loadConfig(configPath) {
  try {
    const parsed = JSON.parse(import_node_fs27.default.readFileSync(configPath, "utf8"));
    const config = asJsonObject(parsed);
    const servers = Array.isArray(config.servers) ? config.servers.map(normalizeServer).filter((server) => server !== null) : [];
    return { ...config, servers };
  } catch {
    return { servers: [] };
  }
}
function saveConfig(configPath, config) {
  import_node_fs27.default.mkdirSync(import_node_path48.default.dirname(configPath), { recursive: true });
  import_node_fs27.default.writeFileSync(configPath, `${JSON.stringify(config, null, 2)}
`);
}
function recommend(root) {
  const scan = scanFiles(root);
  return LANGUAGE_RULES.flatMap((rule) => {
    const extensionFiles = rule.extensions.reduce(
      (sum, ext) => sum + (scan.counts.get(ext) ?? 0),
      0
    );
    const filenameFiles = (rule.filenames ?? []).reduce(
      (sum, filename) => sum + (scan.filenameCounts.get(filename.toLowerCase()) ?? 0),
      0
    );
    const files = extensionFiles + filenameFiles;
    if (files === 0) return [];
    const sampleFiles = [
      ...rule.extensions.flatMap((ext) => scan.samples.get(ext) ?? []),
      ...(rule.filenames ?? []).flatMap((filename) => scan.filenameSamples.get(filename.toLowerCase()) ?? [])
    ];
    const availability = commandAvailability2(rule.command);
    return [{
      ...rule,
      files,
      samples: sampleFiles.slice(0, 8),
      available: availability.state === "available",
      availability
    }];
  });
}
function serverKey(server) {
  const id = typeof server.id === "string" ? server.id : "";
  const command = typeof server.command === "string" ? server.command : "";
  return id || command;
}
function workspaceFolders(root) {
  const folders = [{ name: ".", path: "." }];
  const reposPath = import_node_path48.default.join(root, "cadre", "repos.json");
  let repos = {};
  try {
    repos = asJsonObject(JSON.parse(import_node_fs27.default.readFileSync(reposPath, "utf8")));
  } catch {
    return folders;
  }
  if (repos.mode !== "polyrepo" || !Array.isArray(repos.repos)) return folders;
  for (const raw of repos.repos) {
    const repo = asJsonObject(raw);
    if (repo.enabled === false || typeof repo.name !== "string" || typeof repo.submodule_path !== "string") continue;
    folders.push({ name: repo.name, path: repo.submodule_path });
  }
  return folders;
}
function mergeConfig(config, recommendations, root) {
  const servers = Array.isArray(config.servers) ? [...config.servers] : [];
  const next = {
    ...config,
    servers,
    workspaceFolders: workspaceFolders(root)
  };
  const existing = new Set(servers.map(serverKey).filter(Boolean));
  const added = [];
  for (const rec of recommendations) {
    if (existing.has(rec.id) || existing.has(rec.command)) continue;
    servers.push({
      id: rec.id,
      command: rec.command,
      args: rec.args,
      extensions: rec.extensions,
      ...rec.filenames ? { filenames: rec.filenames } : {},
      ...rec.languageIds ? { languageIds: rec.languageIds } : {}
    });
    existing.add(rec.id);
    added.push(rec.id);
  }
  return { config: next, added };
}
function runCli2() {
  const args = parseArgs2(process.argv);
  const config = loadConfig(args.configPath);
  const recommendations = recommend(args.root);
  const existingIds = new Set((config.servers ?? []).map(serverKey).filter(Boolean));
  const missingFromConfig = recommendations.filter(
    (rec) => !existingIds.has(rec.id) && !existingIds.has(rec.command)
  );
  const missingCommands = recommendations.filter((rec) => !rec.available);
  let written = false;
  let added = [];
  if (args.write) {
    const merged = mergeConfig(config, recommendations, args.root);
    saveConfig(args.configPath, merged.config);
    written = true;
    added = merged.added;
  }
  const result = {
    root: args.root,
    config: import_node_path48.default.relative(args.root, args.configPath),
    recommended: recommendations,
    missingFromConfig: missingFromConfig.map((rec) => rec.id),
    missingCommands: missingCommands.map((rec) => ({
      id: rec.id,
      command: rec.command,
      availability: rec.availability,
      install: rec.install
    })),
    workspaceFolders: workspaceFolders(args.root),
    written,
    added
  };
  if (args.json) {
    console.log(JSON.stringify(result, null, 2));
    return;
  }
  if (recommendations.length === 0) {
    console.log("No LSP recommendations found from source file extensions.");
    return;
  }
  console.log("Cadre LSP recommendations:");
  for (const rec of recommendations) {
    const status = rec.available ? "available" : "missing";
    const configured = existingIds.has(rec.id) || existingIds.has(rec.command) ? "configured" : "not configured";
    console.log(`- ${rec.label}: ${rec.command} (${status}, ${configured}, ${rec.files} files)`);
    if (!rec.available) console.log(`  install: ${rec.install}`);
  }
  if (written) {
    console.log(`Updated ${import_node_path48.default.relative(args.root, args.configPath)}; added: ${added.join(", ") || "none"}.`);
  } else if (missingFromConfig.length > 0) {
    console.log("Run with --write to append missing server entries to cadre/lsp.json.");
  }
}

// src/cadre-lsp-setup.ts
if (["cadre-lsp-setup.js", "cadre-lsp-setup.ts"].includes(import_node_path49.default.basename(process.argv[1] || ""))) {
  try {
    runCli2();
  } catch (error) {
    console.error(error instanceof Error ? error.message : String(error));
    process.exit(1);
  }
}

// src/mcp/application/envelope.ts
function asTextJson(value) {
  return {
    content: [{ type: "text", text: JSON.stringify(value, null, 2) }]
  };
}
function envelope(value) {
  const object = asJsonObject(value);
  const ok = Object.prototype.hasOwnProperty.call(object, "ok") ? Boolean(object.ok) : true;
  const warnings = Array.isArray(object.warnings) ? object.warnings : [];
  const reason = object.error || object.reason || object.stage;
  const errors = ok ? [] : [typeof reason === "string" ? reason : "Cadre operation failed"];
  const out = { ok, data: value || null, warnings, errors };
  if (Object.prototype.hasOwnProperty.call(object, "commands")) out.commands = object.commands;
  if (Object.prototype.hasOwnProperty.call(object, "job")) out.job = object.job;
  return out;
}
function syncedEnvelope(root, operation, fn) {
  const syncPre = syncControlPlane(root, { mode: "pre" });
  if (syncPre.ok === false) {
    return envelope({
      ok: false,
      phase_state: "blocked",
      stage: "sync_pre",
      operation,
      sync_pre: syncPre
    });
  }
  const value = asJsonObject(fn());
  const valueOk = value.ok === false ? false : true;
  if (!valueOk) {
    return envelope({ ...value, sync_pre: syncPre, sync_post: null });
  }
  const syncPost = syncControlPlane(root, { mode: "post" });
  return envelope({
    ...value,
    ok: valueOk && syncPost.ok !== false,
    phase_state: syncPost.ok === false ? "recovery_required" : value.phase_state,
    sync_pre: syncPre,
    sync_post: syncPost
  });
}

// src/mcp/domain/resource-catalog.ts
var RESOURCE_DEFINITIONS = [
  { uri: "cadre://skill-contract", name: "Cadre skill contract", description: "Packaged cadre.skill.v1 contract. Read without a project root." },
  { uri: "cadre://workflow-protocols", name: "Cadre workflow protocols", description: "Packaged workflow protocol catalog. Read without a project root." },
  { uri: "cadre://workflow-protocol", name: "Cadre workflow protocol", description: "Packaged cadre.protocol.v1 body. Read with ?workflow=<name>." },
  { uri: "cadre://agent-references", name: "Cadre agent references", description: "Packaged Cadre agent reference catalog. Read without a project root." },
  { uri: "cadre://agent-reference", name: "Cadre agent reference", description: "Packaged Cadre agent reference JSON. Read with ?name=<reference-id>." },
  { uri: "cadre://template-inventory", name: "Cadre template inventory", description: "Packaged target-project template manifest. Read without a project root." },
  { uri: "cadre://team-board", name: "Cadre team board", description: "Rich team board. Read with ?root=/path/to/project." },
  { uri: "cadre://fleet-board", name: "Cadre fleet board", description: "Mono/polyrepo fleet status. Read with ?root=/path." },
  { uri: "cadre://workspace-health", name: "Cadre workspace health", description: "Compact topology, tech stack, LSP, dependency, and integration health snapshot. Read with ?root=/path." },
  { uri: "cadre://integrations", name: "Cadre integrations", description: "Optional MCP inventory and LSP coverage. Read with ?root=/path." },
  { uri: "cadre://mcp-readiness", name: "Cadre MCP readiness", description: "Provider and optional MCP capability evidence. Read with ?root=/path." },
  { uri: "cadre://track-context", name: "Cadre track context", description: "Track context. Read with ?root=/path&trackId=<id>." },
  { uri: "cadre://review-evidence", name: "Cadre review evidence", description: "Review evidence artifact. Read with ?root=/path&trackId=<id>." },
  { uri: "cadre://collisions", name: "Cadre collisions", description: "File collision scan. Read with ?root=/path." },
  { uri: "cadre://repo-map", name: "Cadre repo map", description: "Symbol map. Read with ?root=/path and optional &symbol=<name>." },
  { uri: "cadre://workspace-diagnostics", name: "Cadre workspace diagnostics", description: "Detected build/test adapters. Read with ?root=/path." },
  { uri: "cadre://lsp-status", name: "Cadre LSP status", description: "Configured LSP servers plus setup recommendations. Read with ?root=/path." },
  { uri: "cadre://repo-topology", name: "Cadre repo topology", description: "Mono/polyrepo topology. Read with ?root=/path." },
  { uri: "cadre://provider-actions", name: "Cadre provider actions", description: "Provider action queue from ship/land packets. Read with ?root=/path&trackId=<id>&workflow=ship|land." },
  { uri: "cadre://ship-plan", name: "Cadre ship plan", description: "Ship workflow dry-run plan. Read with ?root=/path&trackId=<id>." },
  { uri: "cadre://land-plan", name: "Cadre land plan", description: "Land workflow dry-run plan. Read with ?root=/path&trackId=<id>." },
  { uri: "cadre://release-plan", name: "Cadre release plan", description: "Release workflow dry-run plan. Read with ?root=/path." },
  { uri: "cadre://my-next-actions", name: "Cadre next actions", description: "Mine/available/action queue. Read with ?root=/path." },
  { uri: "cadre://review-queue", name: "Cadre review queue", description: "Bounded tracks needing review/ship attention. Read with ?root=/path." },
  { uri: "cadre://handoff-inbox", name: "Cadre handoff inbox", description: "Incoming handoffs from native Cadre state. Read with ?root=/path." },
  { uri: "cadre://parallel-state", name: "Cadre parallel state", description: "Track parallel worker state. Read with ?root=/path&trackId=<id>." },
  { uri: "cadre://quality-gate", name: "Cadre quality gate", description: "Review and integrity gate summary. Read with ?root=/path&trackId=<id>." },
  { uri: "cadre://test-impact", name: "Cadre test impact", description: "Impacted tests/manifests. Read with ?root=/path&files=a,b." },
  { uri: "cadre://track-plan", name: "Cadre track plan", description: "Parsed track plan. Read with ?root=/path&trackId=<id>." },
  { uri: "cadre://job-result", name: "Cadre job result", description: "Persisted async job result. Read with ?root=/path&jobId=<id>." },
  { uri: "cadre://artifact-catalog", name: "Cadre artifact catalog", description: "Canonical/projection artifact catalog. Read with ?root=/path." },
  { uri: "cadre://artifact-schema", name: "Cadre artifact schema", description: "Artifact JSON schema. Read with ?root=/path&artifact=spec|plan|styleguide." },
  { uri: "cadre://artifact-preview", name: "Cadre artifact preview", description: "Rendered artifact projection preview. Read with ?root=/path&artifact=<id>." },
  { uri: "cadre://artifact-sync-plan", name: "Cadre artifact sync plan", description: "Dry-run artifact sync plan. Read with ?root=/path&scope=<scope>." },
  { uri: "cadre://track-spec", name: "Cadre track spec", description: "Canonical track spec and projection preview. Read with ?root=/path&trackId=<id>." },
  { uri: "cadre://styleguide-selection", name: "Cadre styleguide selection", description: "Selected style guidance for a track or file list. Read with ?root=/path&trackId=<id>&files=a,b." }
];
var RESOURCE_CONTRACTS = {
  "cadre://skill-contract": { required: [] },
  "cadre://workflow-protocols": { required: [] },
  "cadre://workflow-protocol": { required: ["workflow"] },
  "cadre://agent-references": { required: [] },
  "cadre://agent-reference": { required: ["name"] },
  "cadre://template-inventory": { required: [] },
  "cadre://team-board": { required: ["root"] },
  "cadre://fleet-board": { required: ["root"] },
  "cadre://workspace-health": { required: ["root"], optional: ["responseMode", "detail", "compact"] },
  "cadre://integrations": { required: ["root"], optional: ["responseMode", "detail", "compact"] },
  "cadre://mcp-readiness": { required: ["root"] },
  "cadre://track-context": { required: ["root", "trackId"] },
  "cadre://review-evidence": { required: ["root", "trackId"] },
  "cadre://collisions": { required: ["root"] },
  "cadre://repo-map": { required: ["root"], optional: ["symbol"] },
  "cadre://workspace-diagnostics": { required: ["root"] },
  "cadre://lsp-status": { required: ["root"] },
  "cadre://repo-topology": { required: ["root"] },
  "cadre://provider-actions": { required: ["root", "trackId", "workflow"] },
  "cadre://ship-plan": { required: ["root", "trackId"] },
  "cadre://land-plan": { required: ["root", "trackId"] },
  "cadre://release-plan": { required: ["root"] },
  "cadre://my-next-actions": { required: ["root"] },
  "cadre://review-queue": { required: ["root"] },
  "cadre://handoff-inbox": { required: ["root"] },
  "cadre://parallel-state": { required: ["root", "trackId"] },
  "cadre://quality-gate": { required: ["root", "trackId"] },
  "cadre://test-impact": { required: ["root"], requiredAny: [["files"], ["base", "head"]] },
  "cadre://track-plan": { required: ["root", "trackId"] },
  "cadre://job-result": { required: ["root", "jobId"] },
  "cadre://artifact-catalog": { required: ["root"], optional: ["scope", "artifact", "includeArchive"] },
  "cadre://artifact-schema": { required: ["root", "artifact"] },
  "cadre://artifact-preview": { required: ["root", "artifact"], optional: ["scope"] },
  "cadre://artifact-sync-plan": { required: ["root"], optional: ["scope", "artifact", "includeArchive"] },
  "cadre://track-spec": { required: ["root", "trackId"] },
  "cadre://styleguide-selection": { required: ["root"], optional: ["trackId", "files"] }
};
function contractQueryParams(contract) {
  return Array.from(/* @__PURE__ */ new Set([
    ...contract.required,
    ...contract.optional || [],
    ...(contract.requiredAny || []).flat()
  ]));
}
function resourceList() {
  return {
    resources: RESOURCE_DEFINITIONS.map((resource) => ({ ...resource, mimeType: "application/json" }))
  };
}
function resourceTemplatesList() {
  const resources = RESOURCE_DEFINITIONS;
  const templates = resources.map((resource) => {
    const contract = RESOURCE_CONTRACTS[resource.uri] || { required: ["root"] };
    const queryParams = contractQueryParams(contract).filter(Boolean);
    return {
      uriTemplate: queryParams.length > 0 ? `${resource.uri}{?${queryParams.join(",")}}` : resource.uri,
      name: resource.name,
      description: resource.description,
      mimeType: "application/json",
      ...contract
    };
  });
  return { resourceTemplates: templates };
}
function parseResourceUri(uri) {
  const [rawBase, query = ""] = uri.split("?");
  const base = rawBase || "";
  const params = new URLSearchParams(query);
  return {
    base,
    root: params.get("root"),
    trackId: params.get("trackId"),
    symbol: params.get("symbol"),
    workflow: params.get("workflow"),
    name: params.get("name"),
    artifact: params.get("artifact"),
    scope: params.get("scope"),
    jobId: params.get("jobId"),
    baseRef: params.get("base"),
    headRef: params.get("head"),
    files: (params.get("files") || "").split(",").map((item) => item.trim()).filter(Boolean),
    responseMode: params.get("responseMode"),
    response_mode: params.get("response_mode"),
    detail: params.has("detail") ? params.get("detail") !== "false" : null,
    compact: params.has("compact") ? params.get("compact") !== "false" : null,
    includeArchive: params.has("includeArchive") ? params.get("includeArchive") !== "false" : null
  };
}

// src/mcp/domain/tool-catalog.ts
var PROTOCOL_VERSION = "2025-06-18";
var SERVER_INSTRUCTIONS = [
  "Cadre MCP is the packet-owned runtime for Cadre workflows. Pass an explicit root on every project-scoped call; setup packets may use a root candidate before cadre/ exists.",
  "Prefer compact responses and Cadre resources for dashboards, review queues, quality gates, workspace health, repo maps, job results, and status views.",
  "Do not mutate cadre/, provider state, indexes, worker state, or merge/cleanup state outside Cadre packets."
].join(" ");
var PROPS = {
  root: { type: "string", description: "Absolute project root or a path inside it." },
  action: { type: "string" },
  workflow: { type: "string" },
  uri: { type: "string", description: "Cadre MCP resource URI to read through a tool fallback." },
  execute: { type: "boolean", description: "Run a mutating packet when true; omitted or false is a dry-run where supported." },
  async: { type: "boolean" },
  trackId: { type: "string" },
  track_id: { type: "string" },
  phaseIndex: { type: "number" },
  taskIndex: { type: "number" },
  status: { type: "string" },
  patch: { type: "object" },
  identity: { type: "string" },
  takeover: { type: "boolean" },
  base: { type: "string" },
  head: { type: "string" },
  config: { type: "string" },
  operation: { type: "string" },
  id: { type: "string" },
  formulaId: { type: "string" },
  formula_id: { type: "string" },
  variables: { type: "object" },
  vars: { type: "object" },
  wispId: { type: "string" },
  wisp_id: { type: "string" },
  stepId: { type: "string" },
  step_id: { type: "string" },
  stepIndex: { type: "number" },
  step_index: { type: "number" },
  command: { type: "string" },
  machineCommand: { type: "string" },
  timeoutMs: { type: "number" },
  limit: { type: "number" },
  maxWorkers: { type: "number" },
  includeHeavy: { type: "boolean" },
  agentIdentifier: { type: "string", enum: ["claude", "codex"], description: "Calling agent platform used to select the worker dispatch adapter." },
  provider: { type: "string" },
  providerMode: { type: "string", enum: ["local", "github", "gitlab"] },
  provider_mode: { type: "string", enum: ["local", "github", "gitlab"] },
  providerEvidence: { oneOf: [{ type: "object" }, { type: "string" }] },
  provider_evidence: { oneOf: [{ type: "object" }, { type: "string" }] },
  evidence: { oneOf: [{ type: "object" }, { type: "string" }] },
  mcpCapabilities: { type: "object" },
  mcp_capabilities: { type: "object" },
  continuationToken: { type: "string" },
  continuation_token: { type: "string" },
  responseMode: { type: "string", enum: ["compact", "detail", "detailed", "full", "verbose"] },
  response_mode: { type: "string", enum: ["compact", "detail", "detailed", "full", "verbose"] },
  detail: { type: "boolean" },
  compact: { type: "boolean" },
  symbol: { type: "string" },
  symbols: { type: "array", items: { type: "string" } },
  files: { type: "array", items: { type: "string" } },
  filesChanged: { type: "array", items: { type: "string" } },
  files_changed: { type: "array", items: { type: "string" } },
  repo: { type: "string" },
  repos: { type: "array", items: { type: "string" } },
  workerId: { type: "string" },
  worker_id: { type: "string" },
  commitSha: { type: "string" },
  coverage: { type: "number" },
  tests: { type: "array" },
  summary: { type: "string" },
  blockers: { type: "array", items: { type: "string" } },
  force: { type: "boolean" },
  allowNoCommit: { type: "boolean" },
  completeTask: { type: "boolean" },
  styleGuideIds: { oneOf: [{ type: "array", items: { type: "string" } }, { type: "string" }] },
  styleGuideMaxChars: { type: "number" },
  techStack: { type: "object" },
  product: { type: "object" },
  productGuidelines: { type: "object" },
  product_guidelines: { type: "object" },
  workflowPolicy: { type: "object" },
  workflow_policy: { type: "object" },
  humanConfirmed: { type: "boolean" },
  human_confirmed: { type: "boolean" },
  manualVerificationMode: { type: "string", enum: ["offline", "autorun"] },
  manual_verification_mode: { type: "string", enum: ["offline", "autorun"] },
  manualVerificationSummary: { type: "string" },
  manual_verification_summary: { type: "string" },
  manualVerificationChecks: { oneOf: [{ type: "array" }, { type: "object" }, { type: "string" }] },
  manual_verification_checks: { oneOf: [{ type: "array" }, { type: "object" }, { type: "string" }] },
  manualVerificationCommand: { type: "string" },
  manual_verification_command: { type: "string" },
  manualVerificationResult: { oneOf: [{ type: "object" }, { type: "string" }] },
  manual_verification_result: { oneOf: [{ type: "object" }, { type: "string" }] },
  reviewBundle: { type: "boolean" },
  reviewFiles: { type: "boolean" },
  reviewBundleDir: { type: "string" },
  review_bundle_dir: { type: "string" },
  spec: { type: "object" },
  plan: { type: "object" },
  args: { type: "object" },
  type: { type: "string" },
  jobId: { type: "string" },
  artifact: { type: "string" },
  scope: { type: "string" },
  artifactAction: { type: "string" },
  artifact_action: { type: "string" },
  includeArchive: { type: "boolean" },
  include_archive: { type: "boolean" },
  view: { type: "string" },
  description: { type: "string" },
  handoffText: { type: "string" },
  bump: { type: "string" },
  includeProvider: { type: "boolean" },
  includeLsp: { type: "boolean" },
  includeMachine: { type: "boolean" }
};
function props(names, actionEnum, workflowEnum) {
  const picked = {};
  for (const name of names) picked[name] = PROPS[name];
  if (actionEnum && picked.action) picked.action = { ...PROPS.action, enum: actionEnum };
  if (workflowEnum && picked.workflow) picked.workflow = { ...PROPS.workflow, enum: workflowEnum };
  return picked;
}
function trackIdAnyOf() {
  return [{ required: ["trackId"] }, { required: ["track_id"] }];
}
function requireTrackForActions(actions) {
  return {
    if: { properties: { action: { enum: actions } }, required: ["action"] },
    then: { anyOf: trackIdAnyOf() }
  };
}
function requireRootForActions(actions) {
  return {
    if: { properties: { action: { enum: actions } }, required: ["action"] },
    then: { required: ["root"] }
  };
}
function packetSchema(options) {
  const fieldNames = Array.from(/* @__PURE__ */ new Set(["root", ...options.fields || []]));
  return {
    name: options.name,
    description: options.description,
    inputSchema: {
      type: "object",
      properties: props(fieldNames, options.actionEnum, options.workflowEnum),
      required: options.required || [],
      ...options.anyOf ? { anyOf: options.anyOf } : {},
      allOf: [...options.allOf || [], forbidMarkdownPayloadFields()],
      additionalProperties: true
    }
  };
}
function forbidMarkdownPayloadFields() {
  return {
    not: {
      anyOf: [
        "productText",
        "productGuidelinesText",
        "workflowText",
        "specText",
        "planText",
        "planPath",
        "importLegacy",
        "import_legacy"
      ].map((name) => ({ required: [name] }))
    }
  };
}
var WORKFLOWS = [
  "setup",
  "setup_assist",
  "setup_scaffold",
  "newtrack",
  "new_track",
  "implement",
  "status",
  "review",
  "validate",
  "archive",
  "handoff",
  "ship",
  "land",
  "release",
  "revise",
  "refresh",
  "flag",
  "revert",
  "formula",
  "artifacts",
  "artifact_sync"
];
var FORMULA_ACTIONS = [
  "list",
  "show",
  "cook",
  "wisp_create",
  "wisp_list",
  "wisp_update_step",
  "wisp_squash",
  "wisp_burn",
  "pour"
];
var TOOLS = [
  packetSchema({
    name: "cadre_resource",
    description: "Tool fallback for Cadre MCP resources/read. Prefer resources when available; use this when a client exposes tools more reliably than resources.",
    fields: ["uri", "responseMode", "response_mode", "detail", "compact"],
    required: ["uri"]
  }),
  packetSchema({
    name: "cadre_workflow",
    description: "Packet-only Cadre workflow coordinator for setup, newtrack, implement, status, review, validate, archive, handoff, ship, land, release, refresh, flag, revert, revise, formula, and artifact sync flows.",
    workflowEnum: WORKFLOWS,
    actionEnum: [...WORKFLOWS, ...FORMULA_ACTIONS],
    fields: ["workflow", "action", "operation", "id", "formulaId", "formula_id", "variables", "vars", "wispId", "wisp_id", "stepId", "step_id", "stepIndex", "step_index", "execute", "humanConfirmed", "human_confirmed", "trackId", "track_id", "responseMode", "response_mode", "detail", "compact", "limit", "includeHeavy", "providerMode", "provider_mode", "providerEvidence", "provider_evidence", "mcpCapabilities", "mcp_capabilities", "continuationToken", "continuation_token", "product", "productGuidelines", "product_guidelines", "workflowPolicy", "workflow_policy", "techStack", "styleGuideIds", "reviewBundle", "reviewFiles", "reviewBundleDir", "review_bundle_dir", "spec", "plan", "description", "artifact", "artifactAction", "artifact_action", "scope", "force", "includeArchive", "include_archive", "status", "summary", "evidence"],
    required: ["root"],
    anyOf: [{ required: ["workflow"] }, { required: ["action"] }],
    allOf: [{
      if: { properties: { workflow: { enum: ["implement", "review", "ship", "land", "archive", "handoff", "flag", "revert", "revise"] } }, required: ["workflow"] },
      then: { anyOf: trackIdAnyOf() }
    }]
  }),
  packetSchema({
    name: "cadre_project",
    description: "Cadre project packet: ping, doctor, root, topology/config, tech-stack summary, integrations, sync, and polyrepo preflight.",
    actionEnum: ["ping", "doctor", "root", "topology", "tech_stack_summary", "integrations", "sync_control_plane", "polyrepo_preflight"],
    fields: ["action", "execute", "responseMode", "response_mode", "detail", "compact"],
    required: ["action"],
    allOf: [requireRootForActions(["root", "topology", "tech_stack_summary", "integrations", "sync_control_plane", "polyrepo_preflight"])]
  }),
  packetSchema({
    name: "cadre_status",
    description: "Cadre status packet: live, team, mine, available, collisions, fleet, and team board.",
    actionEnum: ["live", "team", "mine", "available", "collisions", "board", "fleet"],
    fields: ["action", "identity", "view", "limit", "includeHeavy", "responseMode", "response_mode", "detail", "compact"],
    required: ["root", "action"]
  }),
  packetSchema({
    name: "cadre_track",
    description: "Cadre track packet: context, JSON plan parsing, integrity, phase scheduling, implementation prep, planning evidence, and worktree planning.",
    actionEnum: ["context", "parse_plan", "integrity", "phase_schedule", "prepare_implementation", "plan_assist", "worktree_plan"],
    fields: ["action", "trackId", "track_id", "plan", "execute", "identity", "takeover", "base", "head", "limit", "includeHeavy", "styleGuideIds", "styleGuideMaxChars", "responseMode", "response_mode", "detail", "compact"],
    required: ["root", "action"],
    allOf: [
      requireTrackForActions(["context", "phase_schedule", "prepare_implementation", "worktree_plan"]),
      { if: { properties: { action: { enum: ["parse_plan"] } }, required: ["action"] }, then: { anyOf: [{ required: ["plan"] }, { required: ["trackId"] }, { required: ["track_id"] }] } }
    ]
  }),
  packetSchema({
    name: "cadre_parallel",
    description: "Cadre parallel packet: plan worker waves, setup workers, record finishes, merge back, and cleanup.",
    actionEnum: ["plan", "next_wave", "setup_workers", "record_finish", "merge_back", "cleanup"],
    fields: ["action", "trackId", "track_id", "execute", "phaseIndex", "taskIndex", "workerId", "worker_id", "status", "commitSha", "repo", "command", "timeoutMs", "limit", "maxWorkers", "includeHeavy", "agentIdentifier", "filesChanged", "files_changed", "tests", "summary", "blockers", "force", "allowNoCommit", "completeTask", "humanConfirmed", "human_confirmed", "responseMode", "response_mode", "detail", "compact"],
    required: ["root", "action"],
    anyOf: trackIdAnyOf(),
    allOf: [{ if: { properties: { action: { enum: ["setup_workers"] } }, required: ["action"] }, then: { required: ["agentIdentifier"] } }]
  }),
  packetSchema({
    name: "cadre_mutate",
    description: "Cadre mutation packet: claim, heartbeat, status, metadata, review, worker, task-result, and index writes.",
    actionEnum: ["claim", "heartbeat", "set_status", "metadata_patch", "record_review", "record_worker", "record_task_result", "regen_index"],
    fields: ["action", "trackId", "track_id", "execute", "status", "patch", "identity", "workerId", "worker_id", "phaseIndex", "taskIndex", "commitSha", "repo", "coverage", "command", "timeoutMs", "force"],
    required: ["root", "action"],
    allOf: [requireTrackForActions(["claim", "heartbeat", "set_status", "metadata_patch", "record_review", "record_worker", "record_task_result"])]
  }),
  packetSchema({
    name: "cadre_complete_task",
    description: "Journaled task completion: coverage gate plus locked plan, metadata, and completion journal writes.",
    fields: ["trackId", "track_id", "phaseIndex", "taskIndex", "commitSha", "repo", "command", "timeoutMs", "coverage", "force", "allowNoCommit", "humanConfirmed", "human_confirmed", "manualVerificationMode", "manual_verification_mode", "manualVerificationSummary", "manual_verification_summary", "manualVerificationChecks", "manual_verification_checks", "manualVerificationCommand", "manual_verification_command", "manualVerificationResult", "manual_verification_result", "async"],
    required: ["root", "phaseIndex", "taskIndex"],
    anyOf: trackIdAnyOf()
  }),
  packetSchema({
    name: "cadre_job",
    description: "Cadre job packet: start, status, result, cancel, and list process-local or persisted long-running jobs.",
    actionEnum: ["start", "status", "result", "cancel", "list"],
    fields: ["action", "type", "jobId", "id", "args", "timeoutMs"],
    required: ["action"],
    allOf: [
      requireRootForActions(["start"]),
      { if: { properties: { action: { enum: ["status", "result", "cancel"] } }, required: ["action"] }, then: { anyOf: [{ required: ["jobId"] }, { required: ["id"] }] } }
    ]
  }),
  packetSchema({
    name: "cadre_review",
    description: "Cadre review packet: review assist, machine gate, review gate, provider evidence, and PR/MR/CI status.",
    actionEnum: ["assist", "machine_gate", "gate", "pr_ci_status", "provider_evidence"],
    fields: ["action", "trackId", "track_id", "base", "head", "config", "machineCommand", "command", "providerEvidence", "provider_evidence", "mcpCapabilities", "mcp_capabilities", "includeLsp", "includeMachine", "async", "timeoutMs", "limit", "includeHeavy", "responseMode", "response_mode", "detail", "compact"],
    required: ["root", "action"],
    allOf: [requireTrackForActions(["assist", "gate", "provider_evidence"])]
  }),
  packetSchema({
    name: "cadre_intel",
    description: "Cadre code intelligence packet: repo map, LSP setup/impact/review, workspace diagnostics, test impact, dependency graph, and daemon lifecycle.",
    actionEnum: ["repo_map", "lsp_setup", "lsp_impact", "lsp_review", "lsp_warm_review", "lsp_daemon_status", "lsp_daemon_shutdown", "workspace_diagnostics", "test_impact", "dependency_graph", "mcp_readiness"],
    fields: ["action", "trackId", "track_id", "base", "head", "config", "files", "symbol", "symbols", "repo", "repos", "execute", "async", "timeoutMs", "limit", "includeHeavy", "mcpCapabilities", "mcp_capabilities", "responseMode", "response_mode", "detail", "compact"],
    required: ["action"],
    allOf: [requireRootForActions(["repo_map", "lsp_setup", "lsp_impact", "lsp_review", "lsp_warm_review", "workspace_diagnostics", "test_impact", "dependency_graph", "mcp_readiness"])]
  }),
  packetSchema({
    name: "cadre_artifact",
    description: "Cadre artifact packet: catalog, schema, validate JSON canonicals, render human projections, diff, and sync generated artifacts.",
    actionEnum: ["catalog", "schema", "validate", "render", "diff", "sync"],
    fields: ["action", "artifact", "id", "scope", "trackId", "track_id", "execute", "humanConfirmed", "human_confirmed", "force", "includeArchive", "include_archive", "reviewBundle", "reviewFiles", "reviewBundleDir", "review_bundle_dir"],
    required: ["root", "action"]
  })
];

// src/mcp/application/resources-service.ts
var import_node_path50 = __toESM(require("node:path"));
function normalizeResourceArgs(resource) {
  const args = {
    base: resource.base,
    files: resource.files
  };
  if (resource.root != null) args.root = resource.root;
  if (resource.trackId != null) args.trackId = resource.trackId;
  if (resource.symbol != null) args.symbol = resource.symbol;
  if (resource.workflow != null) args.workflow = resource.workflow;
  if (resource.artifact != null) args.artifact = resource.artifact;
  if (resource.scope != null) args.scope = resource.scope;
  if (resource.jobId != null) args.jobId = resource.jobId;
  if (resource.baseRef != null) args.baseRef = resource.baseRef;
  if (resource.headRef != null) args.headRef = resource.headRef;
  const responseMode = resource.responseMode ?? resource.response_mode;
  if (responseMode != null) {
    args.responseMode = responseMode;
    args.response_mode = responseMode;
  }
  if (resource.detail != null) args.detail = resource.detail;
  if (resource.compact != null) args.compact = resource.compact;
  if (resource.includeArchive != null) args.includeArchive = resource.includeArchive;
  return args;
}
function workspaceHealth2(root, args = {}) {
  return asJsonObject(workspaceHealth(root, args));
}
function integrations(root, args = {}) {
  return asJsonObject(integrationInventory(root, args));
}
function safeAssetName(name) {
  const normalized = name.trim();
  return /^[a-z0-9-]+$/.test(normalized) ? normalized : null;
}
function skillContract() {
  const skill = packagedSkillContract();
  return skill ? { ok: true, skill } : { ok: false, error: "Skill contract not found" };
}
function workflowProtocolCatalog() {
  const skill = packagedSkillContract() || {};
  const skillWorkflows = asJsonObject(skill.workflows);
  const workflows = packagedWorkflowProtocols().map((protocol) => {
    const workflow = asOptionalString(protocol.workflow) || asOptionalString(protocol.id)?.replace(/^cadre-/, "") || "";
    const skillWorkflow = asJsonObject(skillWorkflows[workflow]);
    return {
      workflow,
      id: asOptionalString(protocol.id) || `cadre-${workflow}`,
      title: asOptionalString(protocol.title) || asOptionalString(skillWorkflow.intent) || workflow,
      uri: `cadre://workflow-protocol?workflow=${encodeURIComponent(workflow)}`
    };
  });
  return { ok: true, workflows };
}
function workflowProtocol(workflow) {
  const protocol = packagedWorkflowProtocol(workflow);
  if (!protocol) return { ok: false, error: "workflow is required and must match a packaged protocol" };
  return { ok: true, protocol };
}
function agentReferenceCatalog() {
  const references = packagedAgentReferences().map((reference) => {
    const id = asOptionalString(reference.id) || "unknown";
    return {
      id,
      title: asOptionalString(reference.title) || id,
      uri: `cadre://agent-reference?name=${encodeURIComponent(id)}`
    };
  });
  return { ok: true, references };
}
function agentReference(name) {
  const safeName2 = name ? safeAssetName(name) : null;
  if (!safeName2) return { ok: false, error: "name is required and must be a reference id" };
  const reference = packagedAgentReference(safeName2);
  if (!reference) return { ok: false, error: `Unknown agent reference: ${safeName2}` };
  return { ok: true, reference };
}
function templateInventory() {
  const templates = packagedTemplateJson("manifest.json");
  if (!templates) return { ok: false, error: "Template manifest not found" };
  return { ok: true, templates };
}
function resourceRead(uri, deps) {
  const resource = parseResourceUri(uri);
  const normalizedResource = normalizeResourceArgs(resource);
  let value;
  if (resource.base === "cadre://skill-contract") value = skillContract();
  else if (resource.base === "cadre://workflow-protocols") value = workflowProtocolCatalog();
  else if (resource.base === "cadre://workflow-protocol") value = workflowProtocol(resource.workflow);
  else if (resource.base === "cadre://agent-references") value = agentReferenceCatalog();
  else if (resource.base === "cadre://agent-reference") value = agentReference(resource.name);
  else if (resource.base === "cadre://template-inventory") value = templateInventory();
  else {
    const root = deps.rootResolver.requireCadreRoot(resource.root ? { root: resource.root } : {});
    if (resource.base === "cadre://team-board") value = deps.core.teamBoard(root);
    else if (resource.base === "cadre://fleet-board") value = deps.core.fleetStatus(root);
    else if (resource.base === "cadre://workspace-health") value = workspaceHealth2(root, normalizedResource);
    else if (resource.base === "cadre://integrations") value = integrations(root, normalizedResource);
    else if (resource.base === "cadre://mcp-readiness") value = deps.core.mcpReadiness(root, normalizedResource);
    else if (resource.base === "cadre://track-context") value = deps.core.trackContext(root, resource.trackId);
    else if (resource.base === "cadre://review-evidence") value = deps.core.reviewEvidence(root, resource.trackId);
    else if (resource.base === "cadre://collisions") value = deps.core.collisionScan(root);
    else if (resource.base === "cadre://repo-map") value = deps.core.repoMap(root, resource.symbol ? { symbol: resource.symbol } : {});
    else if (resource.base === "cadre://workspace-diagnostics") value = deps.core.workspaceDiagnostics(root);
    else if (resource.base === "cadre://lsp-status") value = { ok: true, status: deps.core.lspConfigStatus(root), setup: deps.core.lspSetup(root, { execute: false }) };
    else if (resource.base === "cadre://repo-topology") value = { ok: true, root, topology: deps.core.loadTopology(root) };
    else if (resource.base === "cadre://ship-plan") value = deps.core.workflowPacket(root, { workflow: "ship", trackId: resource.trackId || void 0 });
    else if (resource.base === "cadre://land-plan") value = deps.core.workflowPacket(root, { workflow: "land", trackId: resource.trackId || void 0 });
    else if (resource.base === "cadre://release-plan") value = deps.core.workflowPacket(root, { workflow: "release" });
    else if (resource.base === "cadre://my-next-actions") {
      const mine = deps.core.teamBoard(root, { mine: true });
      const available = deps.core.availableWork(root);
      value = {
        ok: mine.ok !== false && available.ok !== false,
        mine: {
          wip: Array.isArray(mine.wip) ? mine.wip : [],
          incoming_handoffs: Array.isArray(mine.incoming_handoffs) ? mine.incoming_handoffs : [],
          review_queue: Array.isArray(mine.review_queue) ? mine.review_queue : []
        },
        available: Array.isArray(available.available) ? available.available : [],
        reclaimable: Array.isArray(available.reclaimable) ? available.reclaimable : []
      };
    } else if (resource.base === "cadre://review-queue") {
      const board = deps.core.teamBoard(root);
      value = { ok: board.ok !== false, review_queue: Array.isArray(board.review_queue) ? board.review_queue : [] };
    } else if (resource.base === "cadre://handoff-inbox") {
      const board = deps.core.teamBoard(root, { mine: true });
      value = { ok: board.ok !== false, incoming_handoffs: Array.isArray(board.incoming_handoffs) ? board.incoming_handoffs : [] };
    } else if (resource.base === "cadre://parallel-state") value = deps.core.parallelWorkflow(root, { action: "plan", trackId: resource.trackId || void 0 });
    else if (resource.base === "cadre://quality-gate") {
      if (!resource.trackId) value = { ok: false, error: "trackId is required" };
      else value = {
        ok: true,
        track_id: resource.trackId,
        integrity: deps.core.planIntegrity(root, resource.trackId),
        review_gate: deps.core.reviewGate(root, resource.trackId, {}),
        collisions: deps.core.collisionScan(root)
      };
    } else if (resource.base === "cadre://test-impact") value = deps.core.testImpact(root, {
      files: resource.files,
      base: resource.baseRef || void 0,
      head: resource.headRef || void 0
    });
    else if (resource.base === "cadre://track-plan") {
      const context = deps.core.trackContext(root, resource.trackId);
      const track = asJsonObject(asJsonObject(context).track);
      const planPath = asOptionalString(track.plan_json_path);
      value = context.ok === false || !planPath ? context : deps.core.parsePlanFile(import_node_path50.default.resolve(root, planPath));
    } else if (resource.base === "cadre://track-spec") {
      value = resource.trackId ? deps.core.artifactRender(root, { artifact: `track:${resource.trackId}:spec` }) : { ok: false, error: "trackId is required" };
    } else if (resource.base === "cadre://job-result") {
      const persisted = deps.jobs.loadPersisted(root, resource.jobId);
      value = persisted || { ok: false, error: `Job not found: ${resource.jobId}` };
    } else if (resource.base === "cadre://provider-actions") {
      const workflow = resource.workflow === "land" ? "land" : "ship";
      const plan = asJsonObject(deps.core.workflowPacket(root, { workflow, trackId: resource.trackId || void 0 }));
      value = {
        ok: plan.ok !== false,
        workflow,
        track_id: resource.trackId,
        phase_state: plan.phase_state,
        provider_actions: Array.isArray(plan.provider_actions) ? plan.provider_actions : [],
        required_provider_mcp: plan.required_provider_mcp || null,
        required_evidence: plan.required_evidence || null,
        continuation_token: plan.continuation_token || null
      };
    } else if (resource.base === "cadre://artifact-catalog") {
      value = deps.core.artifactCatalog(root, normalizedResource);
    } else if (resource.base === "cadre://artifact-schema") {
      value = deps.core.artifactSchema(resource.artifact || normalizedResource.artifact || "catalog");
    } else if (resource.base === "cadre://artifact-preview") {
      value = resource.artifact ? deps.core.artifactRender(root, normalizedResource) : { ok: false, error: "artifact is required" };
    } else if (resource.base === "cadre://artifact-sync-plan") {
      value = deps.core.artifactSync(root, { ...normalizedResource, execute: false });
    } else if (resource.base === "cadre://styleguide-selection") {
      value = {
        ok: true,
        track_id: resource.trackId,
        files: resource.files,
        catalog: deps.core.artifactCatalog(root, { ...normalizedResource, scope: "styleguides" })
      };
    } else throw Object.assign(new Error(`Unknown resource: ${uri}`), { code: -32602 });
  }
  return { contents: [{ uri, mimeType: "application/json", text: JSON.stringify(envelope(value), null, 2) }] };
}

// src/mcp/application/review-support.ts
var import_node_fs28 = __toESM(require("node:fs"));
var import_node_path51 = __toESM(require("node:path"));
function selectedRepos(args) {
  const values = [
    asOptionalString(args.repo),
    ...Array.isArray(args.repos) ? args.repos.filter((item) => typeof item === "string") : []
  ].filter((item) => Boolean(item));
  return values.length > 0 ? new Set(values) : null;
}
function repoReviewTargets(deps, root, args) {
  const trackId = asOptionalString(args.trackId) || asOptionalString(args.track_id);
  const selected = selectedRepos(args);
  if (!trackId) return [{ repo: ".", path: ".", cwd: root, base: args.base || "main", head: args.head || "HEAD", source: "project-root" }];
  const context = asJsonObject(deps.core.trackContext(root, trackId));
  const topology = asJsonObject(context.topology);
  if (topology.polyrepo !== true) {
    return [{ repo: ".", path: ".", cwd: root, base: args.base || "main", head: args.head || "HEAD", source: "project-root" }];
  }
  const track = asJsonObject(context.track);
  const fromContext = Array.isArray(context.worktrees) ? context.worktrees.map((entry) => asJsonObject(entry)) : [];
  const topologyInfo = asJsonObject(deps.core.loadTopology(root));
  const topologyRepos = asJsonObject(topologyInfo.repos);
  const fromTopology = Array.isArray(topologyRepos.repos) ? topologyRepos.repos.map((entry) => asJsonObject(entry)) : [];
  const rawTargets = fromContext.length > 0 ? fromContext : fromTopology;
  const seen = /* @__PURE__ */ new Set();
  return rawTargets.map((entry) => {
    const repo = asOptionalString(entry.repo) || asOptionalString(entry.name) || ".";
    if (selected && !selected.has(repo)) return null;
    const rel = asOptionalString(entry.path) || asOptionalString(entry.worktree_path) || asOptionalString(entry.submodule_path) || ".";
    const key = `${repo}:${rel}`;
    if (seen.has(key)) return null;
    seen.add(key);
    return {
      repo,
      path: rel,
      cwd: import_node_path51.default.isAbsolute(rel) ? rel : import_node_path51.default.resolve(root, rel),
      base: args.base || asOptionalString(entry.base_branch) || asOptionalString(entry.default_branch) || "main",
      head: args.head || asOptionalString(entry.git_branch) || asOptionalString(track.git_branch) || `track/${trackId}`,
      source: asOptionalString(entry.source) || (entry.worktree_path ? "metadata.repos.worktree_path" : "repos.json")
    };
  }).filter((entry) => entry !== null);
}
function annotateRepoFindings(result, target) {
  const findings = Array.isArray(result.findings) ? result.findings.map((finding) => asJsonObject(finding)) : [];
  const repoPath = asOptionalString(target.path) || ".";
  const cwd = asOptionalString(target.cwd) || "";
  return findings.map((finding) => ({
    ...finding,
    repo: target.repo,
    path: asOptionalString(finding.path) || asOptionalString(finding.file) || repoPath,
    cwd,
    repo_path: repoPath
  }));
}
async function mapWithConcurrency(items, limit, fn) {
  const results = new Array(items.length);
  let next = 0;
  const workers = Array.from({ length: Math.max(1, Math.min(limit, items.length)) }, async () => {
    while (next < items.length) {
      const index = next;
      next += 1;
      results[index] = await fn(items[index]);
    }
  });
  await Promise.all(workers);
  return results;
}
async function warmLspReview(deps, root, args) {
  const targets = repoReviewTargets(deps, root, args);
  const timeoutMs = Number(args.timeoutMs || 12e4);
  const maxWorkers = Math.max(1, Math.min(12, Number(args.maxWorkers || args.limit || 4)));
  const rawConfig = asOptionalString(args.config) || import_node_path51.default.join(root, "cadre", "lsp.json");
  const config = import_node_path51.default.isAbsolute(rawConfig) ? rawConfig : import_node_path51.default.resolve(root, rawConfig);
  if (targets.length <= 1 && targets[0]?.repo === ".") {
    return asJsonObject(await deps.lspDaemon.request(
      "review",
      { ...args, root, base: args.base || "main", head: args.head || "HEAD", config },
      timeoutMs
    ).catch((error) => ({ available: false, reason: errorMessage(error), findings: [] })));
  }
  const repos = await mapWithConcurrency(targets, maxWorkers, async (target) => {
    const cwd = asOptionalString(target.cwd) || root;
    const repo = asOptionalString(target.repo) || ".";
    if (!import_node_fs28.default.existsSync(cwd)) {
      const result2 = {
        available: false,
        reason: `Repo working root is missing: ${cwd}`,
        findings: []
      };
      return { ...target, result: result2, findings: [] };
    }
    const result = asJsonObject(await deps.lspDaemon.request(
      "review",
      {
        ...args,
        root: cwd,
        base: asOptionalString(target.base) || args.base || "main",
        head: asOptionalString(target.head) || args.head || "HEAD",
        config
      },
      timeoutMs
    ).catch((error) => ({ available: false, reason: errorMessage(error), findings: [] })));
    const findings2 = annotateRepoFindings(result, target);
    return {
      ...target,
      repo,
      result: { ...result, findings: findings2 },
      findings: findings2
    };
  });
  const findings = repos.flatMap((entry) => Array.isArray(entry.findings) ? entry.findings : []);
  return {
    available: repos.some((entry) => asJsonObject(entry.result).available !== false),
    polyrepo: true,
    max_workers: maxWorkers,
    target_count: targets.length,
    bounded_concurrency: true,
    config,
    repos,
    findings
  };
}

// src/mcp/application/packets/workflow.ts
async function workflowPacket2(deps, args) {
  const workflow = args.workflow || args.action || "status";
  const setupWorkflows = /* @__PURE__ */ new Set(["setup", "setup_assist", "setup_scaffold"]);
  if (setupWorkflows.has(workflow)) {
    const info = deps.rootResolver.rootFromCandidate(args.root || process.cwd());
    return envelope(deps.core.workflowPacket(info ? info.root : process.cwd(), { ...args, workflow }));
  }
  const root = deps.rootResolver.requireCadreRoot(args);
  if ((workflow === "review" || workflow === "revise") && args.includeLsp !== false) {
    const lspResult = await warmLspReview(deps, root, args);
    return envelope(deps.core.workflowPacket(root, { ...args, workflow, lspResult }));
  }
  return envelope(deps.core.workflowPacket(root, { ...args, workflow }));
}

// src/mcp/application/packets/project.ts
async function projectPacket(deps, args) {
  const action = args.action || "ping";
  if (action === "ping") {
    return envelope({
      ok: true,
      protocolVersion: PROTOCOL_VERSION,
      server: "cadre",
      rootContract: "project-scoped tools require { root } per call"
    });
  }
  if (action === "doctor") {
    const info = deps.rootResolver.rootFromCandidate(args.root || process.cwd());
    return envelope(deps.core.doctor(info ? info.root : process.cwd(), { hasCadreProject: Boolean(info && info.has_cadre) }));
  }
  if (action === "root") {
    const root2 = deps.rootResolver.requireCadreRoot(args);
    return envelope({ ok: true, root: root2, source: "argument.root" });
  }
  const root = deps.rootResolver.requireCadreRoot(args);
  if (action === "topology") return envelope({ ok: true, root, topology: deps.core.loadTopology(root) });
  if (action === "tech_stack_summary") return envelope(deps.core.techStackSummary(root, args));
  if (action === "integrations") return envelope(deps.core.integrationInventory(root, args));
  if (action === "sync_control_plane") return envelope(deps.core.syncControlPlane(root, args));
  if (action === "polyrepo_preflight") return envelope(deps.core.polyrepoPreflight(root));
  return envelope({ ok: false, error: `Unknown cadre_project action: ${action}` });
}

// src/mcp/application/packets/status.ts
function statusPacket(deps, args) {
  const root = deps.rootResolver.requireCadreRoot(args);
  const action = args.action || "live";
  if (action === "live") return envelope(deps.core.liveStatus(root));
  if (action === "team") return envelope(deps.core.teamStatus(root));
  if (action === "mine") return envelope(deps.core.teamBoard(root, { mine: true }));
  if (action === "available") return envelope(deps.core.availableWork(root));
  if (action === "collisions") return envelope(deps.core.collisionScan(root));
  if (action === "board") return envelope(deps.core.teamBoard(root, args));
  if (action === "fleet") return envelope(deps.core.fleetStatus(root, args));
  return envelope({ ok: false, error: `Unknown cadre_status action: ${action}` });
}

// src/mcp/application/packets/track.ts
var import_node_path52 = __toESM(require("node:path"));
function trackPacket(deps, args) {
  const root = deps.rootResolver.requireCadreRoot(args);
  const action = args.action || "context";
  if (action === "context") return envelope(deps.core.trackContext(root, args.trackId || args.track_id));
  if (action === "parse_plan") {
    if (args.plan && typeof args.plan === "object") return envelope(deps.core.parsePlanJson(args.plan));
    const trackId = args.trackId || args.track_id;
    if (!trackId) return envelope({ ok: false, error: "plan or trackId is required" });
    const context = deps.core.trackContext(root, trackId);
    const track = context && typeof context === "object" ? context.track : void 0;
    if (!track?.plan_json_path) return envelope({ ok: false, error: `Track not found: ${trackId}` });
    return envelope(deps.core.parsePlanFile(import_node_path52.default.resolve(root, track.plan_json_path)));
  }
  if (action === "integrity") return envelope(deps.core.planIntegrity(root, args.trackId || args.track_id || null));
  if (action === "phase_schedule") return envelope(deps.core.phaseSchedule(root, args));
  if (action === "prepare_implementation") return envelope(deps.core.implementationPrep(root, args));
  if (action === "plan_assist") return envelope(deps.core.planAssist(root, args));
  if (action === "worktree_plan") return envelope(deps.core.worktreePlan(root, args));
  return envelope({ ok: false, error: `Unknown cadre_track action: ${action}` });
}

// src/mcp/application/packets/mutate.ts
function mutatePacket(deps, args) {
  const root = deps.rootResolver.requireCadreRoot(args);
  const action = args.action;
  if (action === "claim") {
    const trackId = args.trackId || args.track_id;
    if (!trackId) return envelope({ ok: false, error: "trackId is required" });
    return syncedEnvelope(root, "mutate:claim", () => deps.core.claimTrack(root, trackId, args));
  }
  if (action === "heartbeat") return syncedEnvelope(root, "mutate:heartbeat", () => deps.core.heartbeatTrack(root, args));
  if (action === "set_status") {
    const trackId = args.trackId || args.track_id;
    if (!trackId || !args.status) return envelope({ ok: false, error: "trackId and status are required" });
    return syncedEnvelope(root, "mutate:set_status", () => deps.core.setTrackStatus(root, String(trackId), String(args.status)));
  }
  if (action === "metadata_patch") return syncedEnvelope(root, "mutate:metadata_patch", () => deps.core.metadataPatch(root, args));
  if (action === "record_review") return syncedEnvelope(root, "mutate:record_review", () => deps.core.recordReview(root, args));
  if (action === "record_worker") return syncedEnvelope(root, "mutate:record_worker", () => deps.core.recordParallelWorker(root, { ...args, execute: false }));
  if (action === "record_task_result") return syncedEnvelope(root, "mutate:record_task_result", () => deps.core.recordTaskResult(root, args));
  if (action === "regen_index") return syncedEnvelope(root, "mutate:regen_index", () => deps.core.regenIndex(root));
  return envelope({ ok: false, error: `Unknown cadre_mutate action: ${action}` });
}

// src/mcp/application/packets/parallel.ts
function parallelPacket(deps, args) {
  const root = deps.rootResolver.requireCadreRoot(args);
  return envelope(deps.core.parallelWorkflow(root, args));
}

// src/mcp/application/job-support.ts
function jobTypeForPacket(name, args) {
  if (name === "cadre_complete_task") return "complete_task";
  if (name === "cadre_review" && args.action === "assist") return "review_assist";
  if (name === "cadre_review" && args.action === "machine_gate") return "machine_gate";
  if (name === "cadre_intel" && args.action === "lsp_review") return "lsp_review";
  if (name === "cadre_intel" && args.action === "lsp_impact") return "lsp_impact";
  return args.type || null;
}
function jobEnvelope(type, root, args, deps) {
  if (!type) return envelope({ ok: false, error: "job type is required" });
  return envelope({ ok: true, job: deps.jobs.start(type, root, args) });
}

// src/mcp/application/packets/review.ts
async function reviewPacket(deps, args) {
  const root = deps.rootResolver.requireCadreRoot(args);
  const action = args.action || "assist";
  if (args.async === true) return jobEnvelope(jobTypeForPacket("cadre_review", args), root, args, deps);
  if (action === "assist") {
    let lspResult = null;
    if (args.includeLsp !== false) {
      lspResult = await warmLspReview(deps, root, args);
    }
    const reviewArgs = { ...args };
    if (lspResult) reviewArgs.lspResult = lspResult;
    return envelope(deps.core.reviewAssist(root, reviewArgs));
  }
  if (action === "machine_gate") return envelope(deps.core.reviewMachineGate(root, args));
  if (action === "gate") {
    const trackId = args.trackId || args.track_id;
    if (!trackId) return envelope({ ok: false, error: "trackId is required" });
    return envelope(deps.core.reviewGate(root, trackId, args));
  }
  if (action === "pr_ci_status") return envelope(deps.core.prCiStatus(root, args));
  if (action === "provider_evidence") return syncedEnvelope(root, "review:provider_evidence", () => deps.core.providerEvidence(root, args));
  return envelope({ ok: false, error: `Unknown cadre_review action: ${action}` });
}

// src/mcp/application/packets/intel.ts
async function intelPacket(deps, args) {
  const daemonRoot = args.root ? deps.rootResolver.rootFromCandidate(args.root) : null;
  const root = args.action && args.action.startsWith("lsp_daemon") ? daemonRoot ? daemonRoot.root : process.cwd() : deps.rootResolver.requireCadreRoot(args);
  const action = args.action || "repo_map";
  if (args.async === true) return jobEnvelope(jobTypeForPacket("cadre_intel", args), root, args, deps);
  if (action === "repo_map") return envelope(deps.core.repoMap(root, args));
  if (action === "lsp_setup") return envelope(deps.core.lspSetup(root, args));
  if (action === "workspace_diagnostics") return envelope(deps.core.workspaceDiagnostics(root));
  if (action === "test_impact") return envelope(deps.core.testImpact(root, args));
  if (action === "dependency_graph") return envelope(deps.core.dependencyGraph(root));
  if (action === "mcp_readiness") return envelope(deps.core.mcpReadiness(root, args));
  if (action === "lsp_impact") {
    let lspResult = null;
    if ((args.base || args.head) && args.includeLsp !== false) {
      lspResult = await warmLspReview(deps, root, args);
    }
    const impactArgs = { ...args };
    if (lspResult) impactArgs.lspResult = lspResult;
    return envelope(deps.core.lspImpact(root, impactArgs));
  }
  if (action === "lsp_review") return envelope(deps.core.lspReview(root, args));
  if (action === "lsp_warm_review") {
    return envelope(await warmLspReview(deps, root, args));
  }
  if (action === "lsp_daemon_status") return envelope(await deps.lspDaemon.request("status", {}, 5e3));
  if (action === "lsp_daemon_shutdown") return envelope(await deps.lspDaemon.shutdown());
  return envelope({ ok: false, error: `Unknown cadre_intel action: ${action}` });
}

// src/mcp/application/packets/job.ts
function jobPacket(deps, args) {
  const action = args.action || "status";
  if (action === "start") {
    const root = deps.rootResolver.requireCadreRoot(args);
    const type = args.type;
    if (!type) return envelope({ ok: false, error: "type is required for cadre_job start" });
    return jobEnvelope(type, root, args.args || args, deps);
  }
  if (action === "status") {
    const job = deps.jobs.get(args.jobId || args.id);
    if (job) return envelope({ ok: true, job: deps.jobs.summary(job) });
    const info = args.root ? deps.rootResolver.rootFromCandidate(args.root) : null;
    const persisted = info ? deps.jobs.loadPersisted(info.root, args.jobId || args.id) : null;
    return envelope(persisted ? { ok: true, job: persisted } : { ok: false, error: `Job not found: ${args.jobId || args.id}` });
  }
  if (action === "result") {
    const live = deps.jobs.result(args.jobId || args.id);
    if (live.ok !== false) return envelope(live);
    const info = args.root ? deps.rootResolver.rootFromCandidate(args.root) : null;
    const persisted = info ? deps.jobs.loadPersisted(info.root, args.jobId || args.id) : null;
    return envelope(persisted ? { ok: persisted.status === "succeeded", job: persisted, result: asJsonObject(persisted.result) } : live);
  }
  if (action === "cancel") return envelope(deps.jobs.cancel(args.jobId || args.id));
  if (action === "list") {
    const info = args.root ? deps.rootResolver.rootFromCandidate(args.root) : null;
    return envelope(deps.jobs.list(info ? info.root : null));
  }
  return envelope({ ok: false, error: `Unknown cadre_job action: ${action}` });
}

// src/mcp/application/packets/artifact.ts
function artifactPacket2(deps, args) {
  const action = args.action || "catalog";
  const root = deps.rootResolver.requireCadreRoot(args);
  if (["import", "sync"].includes(String(action)) && args.execute === true) {
    return syncedEnvelope(root, `artifact:${action}`, () => deps.core.artifactPacket(root, args));
  }
  return envelope(deps.core.artifactPacket(root, args));
}

// src/mcp/application/router.ts
function resourceUriFromToolArgs(args) {
  const uri = asOptionalString(args.uri);
  if (!uri) throw Object.assign(new Error("cadre_resource requires uri"), { code: -32602 });
  const responseMode = asOptionalString(args.responseMode || args.response_mode);
  if (!responseMode || /[?&](responseMode|response_mode)=/.test(uri)) return uri;
  const separator = uri.includes("?") ? "&" : "?";
  return `${uri}${separator}responseMode=${encodeURIComponent(responseMode)}`;
}
function createToolCall(deps) {
  return async function toolCall(name, args = {}) {
    if (name === "cadre_resource") return asTextJson(resourceRead(resourceUriFromToolArgs(args), deps));
    if (name === "cadre_workflow") return asTextJson(await workflowPacket2(deps, args));
    if (name === "cadre_project") return asTextJson(await projectPacket(deps, args));
    if (name === "cadre_status") return asTextJson(statusPacket(deps, args));
    if (name === "cadre_track") return asTextJson(trackPacket(deps, args));
    if (name === "cadre_parallel") return asTextJson(parallelPacket(deps, args));
    if (name === "cadre_mutate") return asTextJson(mutatePacket(deps, args));
    if (name === "cadre_complete_task") {
      const root = deps.rootResolver.requireCadreRoot(args);
      if (args.async === true) return asTextJson(envelope({ ok: true, job: deps.jobs.start("complete_task", root, args) }));
      return asTextJson(syncedEnvelope(root, "complete_task", () => deps.core.completeTask(root, { ...args, execute: false })));
    }
    if (name === "cadre_job") return asTextJson(jobPacket(deps, args));
    if (name === "cadre_review") return asTextJson(await reviewPacket(deps, args));
    if (name === "cadre_intel") return asTextJson(await intelPacket(deps, args));
    if (name === "cadre_artifact") return asTextJson(artifactPacket2(deps, args));
    throw Object.assign(new Error(`Unknown tool: ${name}`), { code: -32602 });
  };
}
function createMcpRuntime(deps) {
  const toolCall = createToolCall(deps);
  async function handle2(message) {
    const method = message.method;
    const params = asJsonObject(message.params);
    if (method === "initialize") {
      return {
        protocolVersion: PROTOCOL_VERSION,
        capabilities: { resources: { listChanged: false }, tools: { listChanged: false } },
        serverInfo: { name: "cadre", version: "1.0.0" },
        instructions: SERVER_INSTRUCTIONS
      };
    }
    if (method === "notifications/initialized") return void 0;
    if (method === "ping") return {};
    if (method === "tools/list") return { tools: TOOLS };
    if (method === "tools/call") {
      const name = asOptionalString(params.name);
      if (!name) throw Object.assign(new Error("tools/call requires params.name"), { code: -32602 });
      return toolCall(name, asJsonObject(params.arguments));
    }
    if (method === "resources/list") return resourceList();
    if (method === "resources/templates/list") return resourceTemplatesList();
    if (method === "resources/read") {
      const uri = asOptionalString(params.uri);
      if (!uri) throw Object.assign(new Error("resources/read requires params.uri"), { code: -32602 });
      return resourceRead(uri, deps);
    }
    throw Object.assign(new Error(`Method not found: ${method}`), { code: -32601 });
  }
  return { handle: handle2 };
}

// src/mcp/infrastructure/job-manager.ts
var import_node_fs29 = __toESM(require("node:fs"));
var import_node_path53 = __toESM(require("node:path"));
var import_node_crypto3 = __toESM(require("node:crypto"));
var import_node_child_process9 = require("node:child_process");
var JobManager = class {
  jobs;
  ttlMs;
  constructor() {
    this.jobs = /* @__PURE__ */ new Map();
    this.ttlMs = 60 * 60 * 1e3;
  }
  jobDir(root) {
    return import_node_path53.default.join(root, "cadre", "jobs");
  }
  jobPath(root, id) {
    return import_node_path53.default.join(this.jobDir(root), `${id}.json`);
  }
  serialize(job) {
    return {
      id: job.id,
      type: job.type,
      root: job.root,
      args: job.args,
      status: job.status,
      started_at: job.started_at,
      finished_at: job.finished_at,
      stdout_tail: job.stdout.slice(-8e3),
      stderr_tail: job.stderr.slice(-8e3),
      result: asJsonObject(job.result),
      exit_code: job.exit_code,
      signal: job.signal,
      artifact_path: job.artifact_path || import_node_path53.default.relative(job.root, this.jobPath(job.root, job.id))
    };
  }
  persist(job) {
    try {
      import_node_fs29.default.mkdirSync(this.jobDir(job.root), { recursive: true });
      const artifactPath = this.jobPath(job.root, job.id);
      job.artifact_path = import_node_path53.default.relative(job.root, artifactPath);
      import_node_fs29.default.writeFileSync(artifactPath, `${JSON.stringify(this.serialize(job), null, 2)}
`);
    } catch {
    }
  }
  loadPersisted(root, id) {
    if (!id) return null;
    const file = this.jobPath(root, id);
    try {
      const parsed = asJsonObject(JSON.parse(import_node_fs29.default.readFileSync(file, "utf8")));
      return {
        ...parsed,
        persisted: true,
        stale: parsed.status === "running"
      };
    } catch {
      return null;
    }
  }
  cleanup() {
    const now = Date.now();
    for (const [id, job] of this.jobs.entries()) {
      const finished = ["succeeded", "failed", "cancelled"].includes(job.status);
      if (finished && now - Date.parse(job.finished_at || job.started_at) > this.ttlMs) this.jobs.delete(id);
    }
  }
  summaryFromState(job, persisted = false) {
    const stdout = typeof job.stdout === "string" ? job.stdout : typeof job.stdout_tail === "string" ? job.stdout_tail : "";
    const stderr = typeof job.stderr === "string" ? job.stderr : typeof job.stderr_tail === "string" ? job.stderr_tail : "";
    const root = typeof job.root === "string" ? job.root : "";
    const id = typeof job.id === "string" ? job.id : "";
    return {
      id,
      type: typeof job.type === "string" ? job.type : "",
      root,
      status: typeof job.status === "string" ? job.status : "running",
      started_at: typeof job.started_at === "string" ? job.started_at : null,
      finished_at: typeof job.finished_at === "string" ? job.finished_at : null,
      exit_code: typeof job.exit_code === "number" ? job.exit_code : null,
      signal: typeof job.signal === "string" ? job.signal : null,
      stdout_tail: stdout.slice(-4e3),
      stderr_tail: stderr.slice(-4e3),
      artifact_path: typeof job.artifact_path === "string" ? job.artifact_path : root && id ? import_node_path53.default.relative(root, this.jobPath(root, id)) : null,
      persisted,
      stale: persisted && typeof job.status === "string" && job.status === "running"
    };
  }
  persistedJobIds(root) {
    try {
      return import_node_fs29.default.readdirSync(this.jobDir(root)).filter((name) => name.endsWith(".json")).map((name) => name.slice(0, -5));
    } catch {
      return [];
    }
  }
  getManaged(id) {
    this.cleanup();
    if (!id) return null;
    return this.jobs.get(id) || null;
  }
  start(type, root, args = {}) {
    this.cleanup();
    const id = `job_${import_node_crypto3.default.randomUUID()}`;
    const runner = currentMcpServerPath();
    if (!runner) throw new Error("Cadre MCP runtime not found for async job runner");
    const proc = (0, import_node_child_process9.spawn)(process.execPath, [runner, "--cadre-job-runner"], {
      cwd: mcpRuntimeRoot(runner),
      stdio: ["pipe", "pipe", "pipe"]
    });
    const job = {
      id,
      type,
      root,
      args,
      status: "running",
      started_at: (/* @__PURE__ */ new Date()).toISOString(),
      finished_at: null,
      stdout: "",
      stderr: "",
      result: null,
      exit_code: null,
      signal: null,
      proc
    };
    this.jobs.set(id, job);
    this.persist(job);
    proc.stdout.on("data", (chunk) => {
      job.stdout += chunk.toString("utf8");
    });
    proc.stderr.on("data", (chunk) => {
      job.stderr += chunk.toString("utf8");
    });
    proc.on("close", (code, signal) => {
      job.exit_code = code;
      job.signal = signal || null;
      job.finished_at = (/* @__PURE__ */ new Date()).toISOString();
      try {
        job.result = JSON.parse(job.stdout || "{}");
      } catch {
        job.result = { ok: false, error: "Job returned invalid JSON", stdout_tail: job.stdout.slice(-4e3) };
      }
      const resultObject = asJsonObject(job.result);
      if (job.status !== "cancelled") job.status = code === 0 && resultObject.ok !== false ? "succeeded" : "failed";
      job.proc = null;
      this.persist(job);
    });
    proc.stdin.end(JSON.stringify({ type, root, args }));
    return this.summary(job);
  }
  summary(job) {
    return this.summaryFromState(asJsonObject(job), false);
  }
  get(id) {
    return this.getManaged(id);
  }
  cancel(id) {
    const job = this.getManaged(id);
    if (!job) return { ok: false, error: `Job not found: ${id}` };
    if (job.proc && job.status === "running") {
      job.status = "cancelled";
      job.proc.kill("SIGTERM");
      job.finished_at = (/* @__PURE__ */ new Date()).toISOString();
      this.persist(job);
    }
    return { ok: true, job: this.summary(job) };
  }
  result(id) {
    const job = this.getManaged(id);
    if (!job) return { ok: false, error: `Job not found: ${id}` };
    return { ok: job.status === "succeeded", job: this.summary(job), result: asJsonObject(job.result) };
  }
  list(root = null) {
    this.cleanup();
    const live = Array.from(this.jobs.values()).map((job) => this.summaryFromState(asJsonObject(job), false));
    const persisted = root ? this.persistedJobIds(root).filter((id) => !this.jobs.has(id)).map((id) => this.loadPersisted(root, id)).filter((job) => job !== null).map((job) => this.summaryFromState(job, true)) : [];
    const jobs = [...live, ...persisted].sort((a, b) => {
      const left = Date.parse(String(a.started_at || a.finished_at || 0));
      const right = Date.parse(String(b.started_at || b.finished_at || 0));
      return Number.isFinite(right) && Number.isFinite(left) ? right - left : 0;
    });
    return { ok: true, jobs };
  }
};

// src/mcp/infrastructure/lsp-daemon-client.ts
var import_node_child_process10 = require("node:child_process");
var LspDaemonClient = class {
  proc;
  nextId;
  pending;
  buffer;
  constructor() {
    this.proc = null;
    this.nextId = 1;
    this.pending = /* @__PURE__ */ new Map();
    this.buffer = "";
  }
  ensure() {
    if (this.proc && !this.proc.killed) return;
    const daemon = currentMcpServerPath();
    if (!daemon) throw new Error("Cadre MCP runtime not found for LSP daemon");
    this.proc = (0, import_node_child_process10.spawn)(process.execPath, [daemon, "--cadre-lsp-daemon"], {
      cwd: mcpRuntimeRoot(daemon),
      stdio: ["pipe", "pipe", "pipe"]
    });
    this.proc.stdout.on("data", (chunk) => this.read(chunk));
    this.proc.stderr.on("data", () => {
    });
    this.proc.on("exit", () => {
      for (const pending of this.pending.values()) {
        clearTimeout(pending.timer);
        pending.reject(new Error("LSP daemon exited"));
      }
      this.pending.clear();
      this.proc = null;
    });
  }
  read(chunk) {
    this.buffer += chunk.toString("utf8");
    while (this.buffer.includes("\n")) {
      const index = this.buffer.indexOf("\n");
      const line = this.buffer.slice(0, index).trim();
      this.buffer = this.buffer.slice(index + 1);
      if (!line) continue;
      let message;
      try {
        message = asJsonObject(JSON.parse(line));
      } catch {
        continue;
      }
      const id = Number(message.id);
      if (!Number.isFinite(id)) continue;
      const pending = this.pending.get(id);
      if (!pending) continue;
      this.pending.delete(id);
      clearTimeout(pending.timer);
      if (message.error) {
        pending.reject(new Error(String(asOptionalString(message.error?.message) || "LSP daemon error")));
      } else {
        pending.resolve(message.result);
      }
    }
  }
  request(method, params = {}, timeoutMs = 12e4) {
    this.ensure();
    const id = this.nextId++;
    const body = JSON.stringify({ id, method, params });
    const promise = new Promise((resolve, reject) => {
      const timer = setTimeout(() => {
        this.pending.delete(id);
        reject(new Error(`LSP daemon request timed out after ${timeoutMs}ms`));
      }, timeoutMs);
      this.pending.set(id, { resolve, reject, timer });
    });
    this.proc?.stdin.write(`${body}
`);
    return promise;
  }
  shutdown() {
    if (!this.proc || this.proc.killed) {
      return Promise.resolve({ ok: true, alreadyStopped: true });
    }
    const proc = this.proc;
    return new Promise((resolve) => {
      proc.once("exit", () => resolve({ ok: true }));
      proc.kill("SIGTERM");
    });
  }
};

// src/mcp/infrastructure/root-resolution.ts
var import_node_fs30 = __toESM(require("node:fs"));
var import_node_path54 = __toESM(require("node:path"));
var import_node_url3 = require("node:url");
function isDirectory(file) {
  try {
    return import_node_fs30.default.statSync(file).isDirectory();
  } catch {
    return false;
  }
}
function hasCadreDirectory(dir) {
  return isDirectory(import_node_path54.default.join(dir, "cadre"));
}
function isCadreStateDirectory(dir) {
  return [
    "tracks.json",
    "setup_state.json",
    "product.json",
    "tech-stack.json",
    "workflow.json",
    "config.json",
    "repos.json"
  ].some((name) => import_node_fs30.default.existsSync(import_node_path54.default.join(dir, name))) || isDirectory(import_node_path54.default.join(dir, "tracks"));
}
function hasCadreProjectState(dir) {
  return hasCadreDirectory(dir) && isCadreStateDirectory(import_node_path54.default.join(dir, "cadre"));
}
function normalizePathCandidate(value) {
  if (typeof value !== "string" || value.trim() === "") return null;
  let candidate = value.trim();
  if (candidate.startsWith("file://")) {
    try {
      candidate = (0, import_node_url3.fileURLToPath)(candidate);
    } catch {
      return null;
    }
  }
  candidate = import_node_path54.default.resolve(candidate);
  try {
    if (import_node_fs30.default.existsSync(candidate) && !import_node_fs30.default.statSync(candidate).isDirectory()) candidate = import_node_path54.default.dirname(candidate);
  } catch {
    return null;
  }
  return candidate;
}
function findCadreRoot(start) {
  let dir = normalizePathCandidate(start);
  if (!dir) return null;
  while (true) {
    if (hasCadreProjectState(dir)) return dir;
    if (import_node_path54.default.basename(dir) === "cadre" && isCadreStateDirectory(dir)) return import_node_path54.default.dirname(dir);
    const parent = import_node_path54.default.dirname(dir);
    if (parent === dir) return null;
    dir = parent;
  }
}
function rootFromCandidate(candidate) {
  const normalized = normalizePathCandidate(candidate);
  if (!normalized) return null;
  const cadreRoot = findCadreRoot(normalized);
  if (cadreRoot) return { root: cadreRoot, has_cadre: true };
  return { root: normalized, has_cadre: false };
}
function requireCadreRoot(args = {}) {
  const info = rootFromCandidate(args.root);
  if (info && info.has_cadre) return info.root;
  throw Object.assign(
    new Error(
      `This Cadre MCP tool requires { root } pointing at, or inside, a project containing cadre/. Received: ${args.root || "(missing)"}`
    ),
    { code: -32602 }
  );
}

// src/mcp/presentation/server-runtime.ts
var runtime = createMcpRuntime({
  core: cadre_core_exports,
  jobs: new JobManager(),
  lspDaemon: new LspDaemonClient(),
  rootResolver: { requireCadreRoot, rootFromCandidate }
});
var handle = runtime.handle;

// src/mcp/presentation/stdio-transport.ts
function send2(payload) {
  process.stdout.write(`${JSON.stringify(payload)}
`);
}
function respond(message, result) {
  if (!Object.prototype.hasOwnProperty.call(message, "id")) return;
  send2({ jsonrpc: "2.0", id: message.id, result });
}
function respondError(message, error) {
  if (!Object.prototype.hasOwnProperty.call(message, "id")) return;
  const numericCode = isRecord(error) && typeof error.code === "number" ? error.code : -32603;
  send2({
    jsonrpc: "2.0",
    id: message.id,
    error: { code: numericCode, message: errorMessage(error) }
  });
}
function startStdioTransport(handle2) {
  let buffer = "";
  process.stdin.on("data", (chunk) => {
    buffer += chunk.toString("utf8");
    while (true) {
      const lineEnd = buffer.indexOf("\n");
      if (lineEnd === -1) return;
      const line = buffer.slice(0, lineEnd).replace(/\r$/, "");
      buffer = buffer.slice(lineEnd + 1);
      if (!line.trim()) continue;
      let message;
      try {
        message = asJsonObject(JSON.parse(line));
      } catch (error) {
        respondError({ id: null }, error);
        continue;
      }
      Promise.resolve(handle2(message)).then((result) => respond(message, result)).catch((error) => respondError(message, error));
    }
  });
}

// src/mcp/cadre-server.ts
async function withHiddenModeStripped(fn) {
  const originalArgv = process.argv;
  process.argv = [
    originalArgv[0] || process.execPath,
    originalArgv[1] || "cadre-server.js",
    ...originalArgv.slice(3)
  ];
  try {
    await fn();
  } finally {
    process.argv = originalArgv;
  }
}
var mode = process.argv[2];
async function main() {
  if (mode === "--cadre-job-runner") {
    await runJobRunner();
    return;
  }
  if (mode === "--cadre-lsp-daemon") {
    runLspDaemon();
    return;
  }
  if (mode === "--cadre-lsp-setup") {
    await withHiddenModeStripped(() => runCli2());
    return;
  }
  if (mode === "--cadre-lsp-review") {
    await withHiddenModeStripped(() => runCli());
    return;
  }
  startStdioTransport(handle);
}
main().catch((error) => {
  if (mode === "--cadre-job-runner") {
    process.stdout.write(`${JSON.stringify({ ok: false, error: errorMessage(error), stack: error instanceof Error ? error.stack : void 0 }, null, 2)}
`);
  } else {
    process.stderr.write(`${errorMessage(error)}
`);
  }
  process.exit(1);
});
