Run the remaining native Cadre v5 workflow audit in this disposable clone, using only the configured cadre MCP candidate runtime. Source skills are under {{HARNESS_ROOT}}/harness/skills. Read the applicable source skills; do not use older installed procedures, edit the harness, install plugins, publish, or touch any real project. This prompt authorizes only the following exact synthetic fixture operations; label approvals as test-fixture authorization, never production human approval.

Scope: add a plain JavaScript normalizeName(value) function. Initial approved scope trims strings, no dependencies. Then revise that planned track to also reject non-string input with TypeError. The single delivery phase has two tasks in one commit group `normalize`: function and tests. Every task retains verification and handoff evidence. Refresh project engineering guidance to require tests for invalid inputs, preserving all prior approvals/history and writing the refresh record; do this at a quiescent boundary before execution. Use candidate_apply for track/revise/refresh, immutable receipt trailers, one commit per approved operation, and receipt reconciliation. The cloned initialization receipt must remain unchanged.

Exercise implement, group task reversal, resume/recovery, and review remediation on this fixture. For the reversal exercise, after the shared product commit and group completion but before phase verification, prepare the exact additive Git reversal of the shared product commit. Approve the WHOLE group in the fixture proposal, include sharedCommit and affectedTaskIds in a staged revert record, preserve the original execution journal, then use git revert and one candidate_apply reconciliation commit. The reversal is explicitly authorized only in this disposable test. Inspect current state after reversal; reimplement the same approved behavior with a new shared commit and retain reversal evidence.

For review remediation, explicitly seed one synthetic test defect in the product after implementation: allow a non-string object with a trim method. Record it as an intentional fixture fault, not a claim that verification passed. Review must identify the non-string acceptance failure, stage a findings artifact and appended remediation phase with preserved completed gates/journal, and use candidate_apply workflow review followed by a new execution. Correct the input guard, test it, finish and clean-review the result under the fixture authorization.

Avoid redundant status, separate manual-verification start, execution-start commits, phase-record commits, clean-evidence-only commits and follow-up provenance commits. Use node --test for real behavior checks. If a runtime defect blocks any flow, report exact tool/input/error and stop without patching canonical state to evade it. Output JSON listing completed workflows and recovery cases, commit/call counts if available, tests, and blockers. Keep all output only in this fixture or stdout.
