Run a disposable native Cadre v5 regression session in the current empty sample directory. This is an explicitly authorized test of the local candidate runtime, not production work. Do not touch FRM, Dhivon, the harness checkout, installed plugins, or any other project. Do not install or publish. Use the cadre MCP configured for this session and agent Git tools for commits. Read source skill procedures from {{HARNESS_ROOT}}/harness/skills/{create,track,implement,review,archive,status,wisp}/SKILL.md. Source v5 rules take precedence over older installed skill rules.

Test fixture scope is exactly: create a tiny JavaScript project with greet(name) returning `Hello, ${name}!`, one node:test test, no dependencies. Create Cadre context describing only that scope, then one track with one delivery task in commit group `greeting`, its phase verification, and final track verification. The fixture's approval authority is this test prompt: all deterministic staging, preview/apply, scoped product changes, verification, clean review, archive and commits are authorized only for that exact synthetic outcome. Label authorization as test-fixture approval, never fabricate a production human decision. No clarification is needed for this test. Read and inspect every actual preview and stop if its scope differs. Product/engineering constraints: no network, preserve receipts and evidence, only this sample directory may be modified.

Exercise create -> track -> implement -> clean review -> archive, plus status and read-only Wisp. Use one initialization commit, one combined track commit, one product commit, one execution-completion commit, one clean-review commit, and one archive commit. No extra bookkeeping commits. Use receipt trailers from runtime and candidate_apply reconcile; do not patch canonical state to work around tool failures. Use composite record_verification, no separate manual starts. Do at least two context reads, the second using the retained token. Test real code with node --test. Use default Track authorization with the above fixture verification approval.

If a tool defect blocks the flow, report exact tool/input/error and stop rather than fixing runtime or masking it. Final output: JSON with completed workflows, commit count, tool calls if known, verificationStartCalls, tests, blockers, and elapsed time if known. Do not infer tokens from bytes.
